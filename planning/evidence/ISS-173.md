# ISS-173: PR description and incident mapping

Incident narratives obscured the contracts injected into worker prompts.
`docs/loop.md` now groups the standing contracts under Rules, Planning and
Running; each incident's complete original block, including its continuation
paragraphs, lives verbatim beneath a title in its evidence file. Rules 1–9,
the existing Planning and Running reference material, Milestones and Not carried
forward are unchanged. Adapter code, prompt composition and tests are unchanged.

This is the PR-body handoff: include the mapping below in the PR description.
The independent reviewer must compare each evidence block with its standing
contract in `docs/loop.md`, including the shared authority sentences, rather
than checking only the diff. The accepted independent G0 answer is supplied by
the ordinary review handoff, not by this author document.

## Reference and preservation

The draft cites `6f4175b`, which is unavailable in this checkout and the local
executor repository. The available issue-registration commit `322fcba` contains
the 21 incident blocks below, byte-identical to those at author base
`425d9c7b12750931c95ed7d8c7cebc4f14c278a8`. Between those available revisions,
the only changes to `docs/loop.md` are the ISS-172 rule 4 update and the new
ISS-163 block. The latter is also preserved and mapped separately below.
This comparison does not claim direct inspection of the unavailable revision.

Each evidence file is the original text from its `ISS-NNN records` opening
through the next incident opening (or Planning), with only a title prepended.
This includes the Windows fixture incident inside ISS-160 and all schemas,
historical constraints, rejected alternatives, hashes, runs, PRs and comments.
Evidence is an incident archive, not an additional process document.

## Original 21-block mapping

The quoted text locates the start of each contract. Subsequent paragraphs in
that block retain its detailed conditions. The Running sentences beginning
“Host installation requires independent exact-head PASS” and “Readiness for
ISS-146 requires” retain shared installation, preservation and unpark limits;
the following milestone-ownership sentence retains the rollback boundary.
Rules 3–9 continue to govern gates, review, retries, authority, stops and history.

| Incident and verbatim narrative | Standing contract in `docs/loop.md` |
| --- | --- |
| [ISS-161](ISS-161.md) | Rules / Saved-cycle recovery: “A matching native source-author FAIL parks the item through ordinary stop handling and advances to unrelated ready work.” The following paragraphs retain terminal matching, original-stop replay, participants, inert old records and explicit unpark. Running retains installation and separate resume authority. |
| [ISS-160](ISS-160.md) | Rules / Saved-cycle recovery: “Before scanning failed attempts, composition observes the matching failed author terminal at the pinned conflict seed” starts the failed-projection contract, followed by the single unresolved-seed successor, charged allowances, DELTA review and terminal stops. Running / Worktree setup retains “Vitest runs files serially with one worker”; Running retains the amended answer and explicit planning unpark prerequisite. |
| [ISS-155](ISS-155.md) | Rules / Delivery, conflicts and gates: “Conflict boundaries use anchored literal prefix/suffix checks” retains the ordered non-overlapping scan, Git hunk grammar, immutable outside bytes and bounded lifecycle. Rules 6, 8 and Running retain preservation. |
| [ISS-151](ISS-151.md) | Running / Worktree setup and provider admission: “New ordinary attempts use the local source branch” retains the run-hashed branch, detached seats, saved setup branch, unchanged publication identity, preserved worktrees and collision diagnostics. |
| [ISS-150](ISS-150.md) | Rules / Review and corrective authors: “Reviewer prompts require the JSON object alone” retains the 2000-character cap, extraction grammar, invalid verdict checks, measured overlength diagnostic and existing retry; author parsing remains unchanged. |
| [ISS-139](ISS-139.md) | Rules / Review and corrective authors: “Initial and delta reviewers receive the selected author's captured trace” retains attempt, terminal and candidate paths, resume/retry evidence, execution-versus-claims scrutiny and independent read-only verdicts. Running preserves failed candidates and forbids implied restart or completion. |
| [ISS-140](ISS-140.md) | Rules / Review and corrective authors: “A corrective author may finish with PASS without changing the rejected candidate.” The rest retains commit reuse, distinct bases, full implementation diff, initial no-op rejection, current completion/review, gates and unchanged ceilings. |
| [ISS-141](ISS-141.md) | Rules / Delivery, conflicts and gates: “Complete failed-run logs live in the delivery runtime's `hosted-failure.log`” retains complete capture, exact identity and one fetch per run. “For a receiptless author, the observer validates the trace identity” retains death classification, the existing receipt window/retry, pre-dispatch log acquisition, tracked patch preservation, old trace, publication precondition and replay without refetch. Running preserves partial work and host-only recovery. |
| [ISS-142](ISS-142.md) | Rules / Review and corrective authors: “The delivery consumer checks the identity and verdict fields it needs” retains acceptance of native retry context, existing records and consumed retry, with no migration, imported verdict or budget reset; current review and delivery gates still apply. |
| [ISS-143](ISS-143.md) | Rules / Delivery, conflicts and gates: “When all required checks and all workflow runs are absent” retains ten-second startup waits, twelve reobservations, advisory-row handling, exact identity checks, normal validation once evidence appears, persistent-absence failure and unchanged budgets. Rule 9 permits repeat observation after interruption. |
| [ISS-144](ISS-144.md) | Rules / Saved-cycle recovery: “Native resume observes an unfinished saved selection's issue” retains closure observation before reconstruction or stop replay, accumulated participants, unchanged attempts/stops, repeated-resume skip, unknown-identity refusal and no invented delivery or milestone evidence. Running retains host installation/resume authority. |
| [ISS-145](ISS-145.md) | Running / Accepted replans and stopped-gate recovery: “Accepted replans retain the original main base and participant history” retains forward refresh to the old published branch from a new local branch, unchanged historical records, no migration/restart and replacement of the singleton string by the ISS-154 packet. |
| [ISS-154](ISS-154.md) | Running / Accepted replans and stopped-gate recovery: “`LoopConfig.acceptedReplan` is an exact-key object” introduces the retained full packet, lineage reservation, paths, publication and host-evidence schemas, stop decisions and immutable snapshot contract. “The ruled purchase-limit continuation permits one final Astra/high correction” retains the scoped reads, exact-head PostgreSQL evidence, Sol/high review, ceiling five, no further repair, and operator installation/verifier flow. |
| [ISS-148](ISS-148.md) | Rules / Delivery, conflicts and gates: “Native delivery fetches actual `refs/remotes/origin/main`” retains unpublished rebase versus published merge, typed stops, refresh state, current DELTA review and gate receipts, publication reconciliation and the changed-keys `afterMirror` board gate; full-board selection/checks remain. |
| [ISS-147](ISS-147.md) | Rules / Delivery, conflicts and gates: “A reviewed delivery candidate's conflict consumes one resolution” retains the intermediate two-parent seed, marked-hunk-only scope, unsupported/failure stops, independent semantic DELTA review, charged retry/history, exhaustion and confirmed-publication conflict reconciliation with the forward lease. Running retains rollback, milestone ownership and host authority. |
| [ISS-152](ISS-152.md) | Rules / Delivery, conflicts and gates: “Native delivery captures the complete gate output and terminal execution” retains exact commands, diagnostic recognition, isolated base control, attribution classes, the shared correction reservation, constrained author and fresh DELTA review, all gates, terminal stops and replay. Running retains ISS-146 readiness/dispatch limits. |
| [ISS-156](ISS-156.md) | Running / Worktree setup and provider admission: “A delayed synthetic helper reproduces auth failure before HTTP” retains the split real probe/logical replay, malformed payload, `[10]` wait, helper/auth assertions, unchanged hanging-probe test and production behavior, and no aggregate-load attribution. Saved-stop recovery remains separately authorized. |
| [ISS-157](ISS-157.md) | Running / Accepted replans and stopped-gate recovery: “Optional `gateStopAuthorization` has only `stateDirectory`” retains all four fields, qualifying stops, repair ancestry, the single reservation, fresh refresh/review/gates, pending notes, separate terminal history, unchanged ceilings and host-only grant/install/resume conditions. |
| [ISS-159](ISS-159.md) | Running / Worktree setup and provider admission: “Setup retains each invocation's command, role, head, sanitized stdout/stderr” retains exclusive capture, bounded sanitization, capture-failure termination, evidence-only diagnostics, dependency replay limits, setup order/flags and the sole `controllerRevision` comparison exception. |
| [ISS-162](ISS-162.md) | Running / Worktree setup and provider admission: “The pool keeps a model listed while all of its credentials are suspended” retains the listing and trace-classification limitation. “The launch probe also reads the pool supervisor's per-account, per-model routing status” retains ready/unmentioned admission, blocked-model timing against the existing absolute deadline, refusal/ladder behavior, uncertainty waits and unused pace/usage data. Planning's ISS-158 ladder reference is unchanged. |
| [ISS-166](ISS-166.md) | Rules / Review and corrective authors: “The repair author prompt names the predecessor source state directory” retains prior failed-attempt context at launch outside the fingerprint and instructions to read attempts, terminals and traces as evidence; schemas, reviewer prompts, ladders and write boundaries remain unchanged. Both rejected alternatives and their reasons remain verbatim in evidence. |

## Later block retained as well

[ISS-163](ISS-163.md) maps to Planning / Pinned self selection, beginning
“Before each fresh self selection, supervision fetches current main and pins
one immutable commit”. The complete standing contract retains pinned Git reads,
full-board acquisition, typed failures without fallback or polling, saved
selection behavior, legacy fingerprints, the transitional marker and the
operator-census condition for a reviewed stopped-install cutover. Running
retains installation and separate resume authority. No census is authorized.

## Scope and validation

No line-count gate, prompt filter, new process document or runtime mechanism
was added. The detailed contracts are kept even where they limit the reduction
in document length. Existing routing examples, draft grammar, executor paths
and the ISS-135 Running reference remain in place.

Validation in the author worktree passed:

- `pnpm typecheck`
- `pnpm format:check`
- `pnpm planning:check`
- `pnpm test`: 20 files passed; 887 tests passed and 3 skipped, with tests unchanged.
- A one-off preservation audit matched every evidence body to the author-base
  text, matched the original 21 blocks to `322fcba`, and verified unchanged
  rules 1–9, original Planning/Running references and final sections.
- `git diff --check`; HEAD remains at the supplied author base.

The commands and results are captured in the author execution trace. Independent
review must still confirm the mapping against both homes.
