**Recorded delegated decision (not Todd's personal words): ISS-146 delivery re-entry, todd-skelton/orchestration-platform#457**

Decided by the independent decision lane `astra-iss146-delivery-reentry-decision-r1` (Codex gpt-6-astra/high, row 8, override-Todd) under Todd's rulings 5838576100, 5838600629 and 5838628573. This is a FINAL process decision. The report is at `.orchestrator/logs/astra-iss146-delivery-reentry-decision-r1.report.md`. The host concurs and records it here as the allowance authority.

- **Q1 CONFIRMED.** Stop `loop-stop:m1-iss146-147-20260914T2325:11:2` (`unreviewed-delivery-source`) is an executor defect, not a work failure, and ISS-146 stays unparked. On every re-entry, installed `queue.ts:3976` requires the immutable original review (`01a0ab80`) to equal the persisted accepted review. After publication, `:3105` has already advanced that to the refresh DELTA review (`01a0dc32`), so any re-entry fails. The lane reproduced this read-only on the real records.
- **Q2: route A, with conditions. B (host merge of PR #673) is rejected.**
- **C1.** This decision adds exactly **ONE reviewer-only refresh DELTA launch** for ISS-146 in run `m1-iss146-147-20260914T2325`, implementation attempt 2. It is usable only after the executor repair lands and is safely installed. All 26 participants stay charged. The ceilings are unchanged: implementation 4 with 2 used, and 64 native launches. No author, repair, extra attempt, replenished retry or second launch is authorized. The prior renewal 5838763854 is preserved.
- **C2.** Register ISS-214, the bounded integration-continuation delivery re-entry defect, in platform milestone `Chase Sets delivery adapter`, using ordinary planning and readiness. An ordinary M1 run delivers it after the selector is shown to choose it. There must be no overlapping live M1 owner, and the ISS-146 run is not reused for this work.
- **C3-C5 (acceptance).**
  - A real-path red-before on the recorded persisted shape at `353eda5f`, covering repeated delivery and restart re-entry.
  - Separate validation of the immutable original review and the refresh-bound current review, with negative controls for genuinely unreviewed sources.
  - No migration of saved records.
  - The smallest authority-bound native admission of the one extra reviewer launch. `spent-resolution.json` (`launchLimit` 3) stays unchanged.
- **C6.** After a reviewed, three-OS-green landing and a contract-compliant executor upgrade, resume the SAME run via `scripts/executor/start-loop.ps1`. A later conflict or non-PASS parks.
- **C7.** PR #673 stays OPEN and DRAFT. No host ready flip, merge, rebase, CI rerun or repair.
- **C8/C9.** Upgrade and start remain separately gated. Gates are unchanged, and the product capacity floor is preserved.

No Todd action is requested.