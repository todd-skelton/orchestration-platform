---
key: ISS-041
title: "Run the walking-skeleton cycle end to end"
labels: ["type:slice", "area:core", "priority:p0"]
milestone: "Minimum orchestration kernel"
parent: EPIC-KERNEL
blocked_by: [ISS-003]
---

## Context and evidence

The proportionality review (pressure round 34) found that no issue produces an
observable end-to-end cycle before `ISS-026` and that
`@orchestration-platform/contracts` has no runtime consumer.
Every composition and protocol decision therefore defers its first integration
evidence to the most expensive slice. This slice buys that evidence early with
a deliberately disposable vertical fixture.

## Scope fence

In: one quarantined fixture composition that acquires a lease via create-once
file semantics, reads a stub adapter frontier from fixture JSON, invokes one
inline trivial plan function shaped as `orchestration-module/v1`, launches an
echo worker process, reduces a stub review acceptance, appends and replays a
journal through the published `ISS-002` parsers, and tears down a disposable
state root. Crash/resume at each step boundary against a published skeleton
step table.

Out: the production state service, pointer protocol, broker, credential
handling, real Codex dispatch, module registry admission, promotion,
supervision, and any authority claim. The skeleton produces evidence, never
authority, and no production slice may depend on its code.

## Decisions already made

The skeleton lives at `fixtures/walking-skeleton/` and declares
`@orchestration-platform/contracts` as a real workspace dependency, making it
the first runtime consumer of the published parsers. Its workspace membership
lands through the declared shared-scaffold amendment path in
`docs/planning/work-model.md`: one reviewed change adds a predeclared
`fixtures/*` workspace glob and updates every census table restating it in the
same change. Its step order follows `docs/architecture/routine-cycle.md`
ordinals with stub owners; a divergence between skeleton behavior and the
published cycle contract is replan evidence filed against the cycle document,
and the divergence ledger is a declared input of `ISS-026`'s independent
review packet. The skeleton is deleted or demoted to a conformance fixture
when `ISS-026` lands; it never ships in a release bundle.

## Published journal prerequisite

Round408 defined the public bytes and fold required by this fixture:
create-once header, length-framed canonical events, complete project-path step
outputs, exact restart prefix and acyclic terminal receipt. The fixture cannot
substitute private rows, in-memory arrays or newest-file selection. It must
preserve referenced evidence and show restart without repeating effects. This
contract surface is published and the final fixture physically appends, syncs,
reads back, parses and replays those OPJ1 bytes. It remains evidence only and
grants no production authority.

## Implemented evidence packet

The final fixture composes the exact fifteen published routine ordinals under
one caller-provided disposable root. Its accepted path uses a seeded immutable
subject from a distinct author cycle, executes one fixed echo child, reduces a
separate stub review, plans a nonmutating COMPLETE disposition, emits exact
step-12/13 skips, reclaims the managed input and closed handles, and reaches an
acyclic COMPLETED receipt only after physical OPJ1 replay. A rejecting review
instead reaches FOLLOW_UP/REPLAN and FAILED_KNOWN without mutation.

The process-level matrix kills a fresh Vitest worker at all 38 journal and
owner boundaries. Each row binds the exact persisted byte length and prefix
digest, permits at most one child launch, and proves no append, deletion or
other effect on restart. The 37 pre-cleanup rows fail closed as SESSION_HELD;
the post-cleanup SESSION_CLOSED row returns CYCLE_ALREADY_TERMINAL. This is not
lease adoption or a production resume claim.

Node 24 Linux verification passes `skeleton:cycle` (1/1),
`skeleton:negative-controls` (42/42), and the complete clean-status bootstrap
(121 test files passed, one skipped; 1,477 tests passed, 13 skipped). Round415
independent review passes the full composition and Round416 passes the final
test-only timing bound. The required macOS, Windows and Linux transcripts remain
the hosted PR/merged-main acceptance gate; local execution does not substitute
for them.

## Acceptance criteria and evidence

1. A clean checkout runs one complete skeleton cycle on macOS, Windows, and
   Linux, producing a joined journal whose every record round-trips through the
   published contracts parsers. Evidence: three-OS transcripts plus a workspace
   dependency census showing contracts consumed by name, not relative path.
2. Killing and restarting the skeleton at every step boundary resumes exactly
   once or reaches a named refusal per the published skeleton step table; no
   step repeats an external side effect. Evidence: boundary fault matrix.
3. Malformed frontier records, a rejecting stub review, and a second concurrent
   lease each produce a typed refusal, never a partial cycle. Evidence:
   executed negative controls.
4. All skeleton writes stay inside one disposable state root; the run leaves no
   delta outside it. Evidence: before/after filesystem manifest comparison.
5. Every observed divergence between the skeleton and `routine-cycle.md` is
   filed as a replan record in the divergence ledger, a declared input of
   `ISS-026`'s independent review packet that `ISS-026` cites. Evidence:
   divergence ledger, empty or resolved.

## Verification plan

`pnpm run skeleton:cycle`

`pnpm run skeleton:negative-controls`

## Predicted footprint and dependencies

Owns only `fixtures/walking-skeleton/` and exact ISS-041 capability slots,
plus the declared contracts dependency inside that fixture and the single
shared-scaffold amendment adding the predeclared `fixtures/*` workspace glob
with every census table updated together. Blocked by `ISS-003`.
That single amendment also registers fixture source/tests in root
`tsconfig.json` and `vitest.config.ts`, includes `fixtures/` in
`scripts/format.mjs`, and updates the frozen lock importer,
`scripts/verify/bootstrap-contracts.mjs` and its tests,
`docs/architecture/bootstrap-manifests.md`, and ISS-000's workspace census.
The final packet activates exactly the two ISS-041 root capability slots. Its
README and divergence ledger distinguish fixture evidence from production
authority and record the remaining hosted acceptance gate.
It blocks nothing: it is evidence, not a correctness dependency, and `ISS-026`
consumes its divergence ledger as context. Beyond that one amendment, there is
no shared-manifest, production package, schema, or glossary edit. The final
packet also updates the planned-command execution census for the two
implemented slots and adds group-local test deadlines to three existing
synthetic native integration matrices whose assertions and production behavior
are unchanged.

## Operator actions

None.

## Glossary impact

None; cycle, journal, and refusal terms are registered already.

## Independent review packet seed

Attack skeleton behavior silently diverging from the published cycle contract,
production code importing skeleton fixtures, authority language leaking into
skeleton receipts, resume repeating the echo-worker side effect, and success
that depends on warm local state. Use the divergence ledger and the three-OS
transcripts.
