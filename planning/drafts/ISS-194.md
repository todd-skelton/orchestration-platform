---
key: ISS-194
title: "Reduce the per-call Git spawn volume of delivery workspace verification"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

`verifyWorkspace` in `scripts/dogfood/delivery-adapter.ts:696-722` spawns 17 Git
processes on every call at current main
`817d07532375283c2ee1115e7c0460f3f8630b11`, and it is called once per draft
observation and once per gate across three real `deliver()` passes. In the owning
dogfood closure cases that is about 32 calls, about 544 of the roughly 638
built-in Git processes a single case spawns - about 85% of all Git work in a case
that has 2187ms of headroom under a 30000ms limit.

The failure that demonstrates the cost is ISS-182. Its attempt 2 stopped
`author-failed` at `465f1beb2f16c6d77a8b3e79bfac247884976f08` on branch
`codex/iss-182`, open as draft PR #576, whose whole diff is
`test/dogfood/refresh.test.ts`, +20/-14. At that head
[Windows job 105540420500](https://github.com/todd-skelton/orchestration-platform/actions/runs/35326425131/job/105540420500)
ran `refresh.test.ts` for 1504596ms and recorded 3 failed, 1079 passed and 17
skipped; the six-deletion sibling case `mirrors the exact six ISS-174 deletions
(autocrlf: true)` timed out at 30017ms against the unchanged 30000ms limit. No
base control ran on that runner, so the candidate is not attributed as the cause
of that timeout - and equally, the candidate did not carry the owning file under
the unchanged gate.

The attempt-2 author measured the dominant cost with `GIT_TRACE` over three
repeated samples per arm, retained read-only at
`\\wsl.localhost\Ubuntu\root\orchestration-m1\runtime\m1-sibling-mirror-recovery-20260917T0848\iss-182-attempt-2\source\author-temp`.
Those traces are **pre-ISS-189**: each base run of the partial-mirror-true
closure case spawned 670 Git processes, of which 581 (86.7%) are the five
`verifyWorkspace` command shapes and 576 are the 32 `verifyWorkspace` cycles
themselves at that runtime's 18 spawns per call. The residual 89 spawns are the
fixture construction - `init`, `clone --bare`, three `worktree add`, `fetch`,
`commit`/`add`/`reset`, `ls-tree`, eight `merge-base` and the plan's draft `show`
reads - every one of which ISS-182 AC1 requires the fixture to keep. `601f79d`
(ISS-189, PR #583) has since removed one `rev-parse --git-common-dir` per call,
so current main is 17 spawns per call and about 638 per closure-true case. The
shape of the cost is unchanged: it is runtime Git process volume on the
`verifyWorkspace` path, not fixture work.

Two green Windows logs at current main measure how little room is left. At
`490fe76` the closure cases took 27565ms and 27813ms and the six-deletion cases
22865ms and 21655ms over a 1358391ms file; at `2e04eb5` they took 23932ms and
26201ms, and 22275ms and 22366ms, over 1344928ms. Worst-case headroom is
30000 - 27813 = **2187ms, 7.3%**, while #576's runner was **10.8-11.9%** slower
on the same file (1504596ms against 1358391ms and 1344928ms). 27813ms at that
observed slower rate is about 31114ms - over the limit. A runner no slower than
one already observed this month puts the closure-true case over the gate with no
product defect present.

The independent planning review of the ISS-182 replan
(`ROOT/.orchestrator/platform-iss182-replan-review-r2.report.md`, verdict
BLOCK_FIXABLE, its F2 and its "Recommendation to the host") recommended
registering exactly this issue and confirmed ISS-189 as proof the work is
reachable and bounded: 3 changed runtime lines plus 389 lines of coverage.

**ISS-182 stays parked and is NOT a dependency of this issue.** ISS-182 owns the
fixture fence inside `test/dogfood/refresh.test.ts`; it has spent its named
fixture candidate for -7 of 670 spawns and reported this runtime volume as its
blocker. Its un-park condition names this issue's landing *plus* a measured
threshold: at this issue's final head both closure variants measured on a hosted
Windows runner under the unchanged 30000ms test/hook limits,
`fileParallelism: false` and the existing platform skips, with worst-case
headroom stated and normalised against a runner at least as adverse as #576's
10.8-11.9% - in arithmetic form, worst observed closure case x 1.12 under
30000ms, i.e. the worst observed case at or below about **26785ms**. Landing
alone is not the threshold: ISS-189 landed, reduced this very volume, was never
measured against these cases, and left them at 2187ms. This issue owns the
reduction and its measurement; it does not un-park ISS-182 and does not amend it.
`blocked_by` is therefore `[]`.

## Decision

Quality profile: core-library (one runtime script function and its coverage).

Constraint: every fact `verifyWorkspace` establishes today must still be
established, live, on every call, from the same working directories, with the
same refusal outcomes and the same fail-fast ordering. No retained record shape,
persisted artifact, state-directory file, `DeliveryConfig` field, adapter
interface, test timeout, worker setting, `vitest.config.ts` key, fixture body or
platform skip changes. No runtime Git behavior changes except how many processes
deliver the same reads.

### Which of the 17 may be derived once, and why the answer is none

The named reducible shape - the `remote get-url` / `--push` pair per root (6 of
17) as "immutable remote configuration" and `--git-common-dir` per worktree (3 of
17) as "worktree family membership" - **does not survive contact with the
coverage ISS-189 landed**, and this brief rejects it explicitly so that no
implementer re-proposes it:

- Within one `verifyWorkspace` call there is no duplicate `(command, cwd)` pair
  left. ISS-189 took the last one. The nine `assertWorktreeRepository` reads are
  three distinct commands at three distinct working directories; a call-local
  cache saves exactly zero spawns.
- Across calls, the remote URLs are not immutable and are proven not to be.
  `test/dogfood/delivery-adapter.test.ts:968` ("rechecks real worktree-local
  %s-controller %s %s URLs after %s target drift") verifies a workspace, then
  sets a **worktree-local** `remote.origin.url` or `pushurl` through
  `extensions.worktreeConfig`, then requires the next call to refuse. Any cache
  spanning two calls returns the stale URL and that case fails.
- Across calls, worktree family membership is not immutable either.
  `test/dogfood/delivery-adapter.test.ts:1043` ("canonicalizes a real repository
  alias while retaining fresh worktree-family checks") rewrites the review
  worktree's `.git` gitfile to a foreign clone between two calls and requires the
  second to refuse.
- Across one `deliver()` pass, `test/dogfood/delivery-adapter.test.ts:1120`
  ("refuses intervening real workspace drift between %s") drifts
  `remote.origin.pushurl` between two draft mutations and between a gate and
  publication, inside a single `deliveryStep`, and requires the next
  `verifyWorkspace` to refuse with `candidate-workspace-drift`. A per-pass cache
  is precisely what that case exists to reject.
- Independently of any test: `assertGitTarget` is the anti-exfiltration
  authority. Caching it across calls means a mid-pass repoint of `origin` to a
  foreign remote is not seen before the next push. That is a security regression,
  not an optimisation.

Combining the pair into one process was also checked and rejected on measured Git
behavior. `git remote -v` reports **all** push URLs but only the **first** fetch
URL: with two `remote.origin.url` entries configured,
`remote get-url --all origin` printed both and `git remote -v` printed one. The
`["same", "reviewWorktree", "fetch", "multiple"]` tuple at
`delivery-adapter.test.ts:968` adds a second fetch URL exactly so that
`urls.length !== 1` refuses; under `remote -v` it would be accepted.
`git config --get-all` is not a substitute either: `remote get-url` applies
`url.<base>.insteadOf` rewriting, which that same test depends on. The six
`remote get-url` spawns are therefore irreducible, and `assertGitTarget`
(`:505-521`) must not change at all - four other call sites (`:585`, `:605`,
`:1485`, `:1517`) depend on its exact two-spawn behavior and its
`delivery-repository-mismatch` / `delivery-repository-unverified` codes.

Replacing the three per-worktree `rev-parse --git-common-dir` with one
`git worktree list --porcelain` at the repository root is rejected for the same
reason: that asks the root which worktrees it has registered, while the shipped
check asks each worktree which common directory *it* resolves to. The
administrative entry survives a rewritten gitfile, so the root-side question
cannot see the drift `:1043` demands be seen.

**Conclusion: none of the 17 may be derived once per call, once per verification
pass, or once per `deliver()` pass. All 17 facts stay live and per call.** The
answer to the spawn volume is not a cache; it is fetching more than one live fact
per process.

### Chosen shape

Leave `assertGitTarget` and `assertWorktreeRepository` (`:523-538`, nine spawns)
exactly as ISS-189 landed them, and replace the eight authority reads inside
`verifyWorkspace` (`:699-717`) with four. One `git status --porcelain=v2 --branch`
per working directory reports the resolved HEAD object id, the current branch and
the working-tree state in a single process, so each
`(rev-parse HEAD, status --porcelain)` pair and the
`(branch --show-current, status --porcelain)` pair at the repository root collapse
to one spawn each, at the same working directory, in the same order, comparing the
same values. **17 spawns per call become 13**, a 23.5% per-call reduction, about
128 fewer spawns per closure case (544 -> 416 of about 638, about 20% of all Git
work in the case).

Per call, the disposition of each invocation is:

| #     | Invocation                        | Working directory | Fact                       | Disposition                              |
| ----- | --------------------------------- | ----------------- | -------------------------- | ---------------------------------------- |
| 1     | `rev-parse --git-common-dir`      | `repositoryRoot`  | root common directory      | keep live, own spawn                     |
| 2     | `remote get-url --all origin`     | `repositoryRoot`  | effective fetch URL set    | keep live, own spawn                     |
| 3     | `remote get-url --push --all`     | `repositoryRoot`  | effective push URL set     | keep live, own spawn                     |
| 4     | `rev-parse --git-common-dir`      | `worktree`        | worktree family membership | keep live, own spawn                     |
| 5     | `remote get-url --all origin`     | `worktree`        | effective fetch URL set    | keep live, own spawn                     |
| 6     | `remote get-url --push --all`     | `worktree`        | effective push URL set     | keep live, own spawn                     |
| 7     | `rev-parse --git-common-dir`      | `reviewWorktree`  | worktree family membership | keep live, own spawn                     |
| 8     | `remote get-url --all origin`     | `reviewWorktree`  | effective fetch URL set    | keep live, own spawn                     |
| 9     | `remote get-url --push --all`     | `reviewWorktree`  | effective push URL set     | keep live, own spawn                     |
| 10-11 | `rev-parse HEAD` + `status`       | `controllerRoot`  | revision, cleanliness      | one `status --porcelain=v2 --branch`     |
| 12-13 | `branch --show-current` + `status`| `repositoryRoot`  | branch is main, cleanliness| one `status --porcelain=v2 --branch`     |
| 14-15 | `rev-parse HEAD` + `status`       | `worktree`        | candidate head, cleanliness| one `status --porcelain=v2 --branch`     |
| 16-17 | `rev-parse HEAD` + `status`       | `reviewWorktree`  | candidate head, cleanliness| one `status --porcelain=v2 --branch`     |

No row is derived once per pass or per `deliver()`; every row is read live on
every call. Rows 1-9 keep their own process for the reasons above; rows 10-17
share four processes with the four they are paired with.

The whole runtime footprint is the body of one function;
`assertWorktreeRepository`, `assertGitTarget`, `DeliveryConfig`, the adapter
interface and every `delivery.ts` call site are untouched.

Confirmed against a real Git before this brief was written, and to be
re-confirmed against the executable resolved at implementation time:

- `git status --porcelain=v2 --branch` on a clean tree with no upstream prints
  exactly `# branch.oid <sha>` and `# branch.head <name>`.
- With an upstream configured it also prints `# branch.upstream <ref>` and
  `# branch.ab +N -M`. **A clean tree that is ahead of its upstream still prints
  `# branch.ab +1 -0`.** "Clean" therefore means "no line that does not begin
  with `# `", never "exactly two lines" and never "empty output". Today's
  `status --porcelain` returns `""` for an ahead-but-clean tree and the
  replacement must agree.
- `# branch.head` is `(detached)` on a detached HEAD, where
  `branch --show-current` returns `""`. Both fail the `!== "main"` comparison, so
  the repository-root predicate is preserved; state the equivalence rather than
  assuming it.
- `# branch.oid` is `(initial)` on an unborn HEAD, where `rev-parse HEAD` exits
  non-zero. Today that throw is caught by `verifyWorkspace` and becomes `false`;
  `(initial)` compares unequal to any 40-character head and also becomes `false`.
  Same outcome, different path - say so and cover it.
- Output is split on `/\r?\n/`, consistent with the rest of this file, and
  `git()` trims.

G0, the smaller alternatives, all rejected: a call-local cache saves zero spawns
(no duplicates remain); a pass-scoped or `deliver()`-scoped cache of any of the 17
is refused above; `remote -v` and `worktree list --porcelain` lose demonstrated
refusals; `rev-parse --git-common-dir HEAD` in one spawn is a real further -2 but
requires `assertWorktreeRepository` to return heads across the function boundary,
disturbing ISS-189's landed lines for two spawns, so it is not taken here;
`rev-parse --abbrev-ref HEAD` instead of `branch --show-current` prints `HEAD`
when detached and buys nothing the v2 header does not already give. Deleting the
orchestrator's own `verifyWorkspace` demand at `delivery.ts:1161` because
`runGate` re-verifies at `delivery-adapter.ts:822` would remove a whole redundant
call, but it moves the guarantee from the adapter-agnostic orchestrator into one
adapter that the `DeliveryAdapter` interface does not require to self-guard, and
gates are a small minority of the 32 calls. Not built here.

Accepted cost: the repository-root branch read and the three cleanliness reads now
share a parser with the head reads, so a single output-shape mistake affects four
predicates instead of one. That is bought back by AC2's preserved eleven-variant
drift matrix, which fails if any one of those predicates stops discriminating.

Revisit only if measurement at the implementation head shows 13 spawns per call
does not put the worst observed closure case at or below about 26785ms. Report the
actual remaining dominant cost; do not raise a timeout, add a skip, change worker
or file-parallelism settings, stub `verifyWorkspace`, or weaken a refusal to pass
a gate.

## Done when

- **AC1 Thirteen spawns per call, every fact still live.** `verifyWorkspace`
  spawns exactly 13 Git processes per call and still establishes all 17 facts on
  every call: the root common directory and both effective remote URL sets at
  `repositoryRoot`, `worktree` and `reviewWorktree`; the controller revision and
  controller cleanliness at `controllerRoot`; the `main` branch and cleanliness at
  `repositoryRoot`; and the candidate head and cleanliness at `worktree` and
  `reviewWorktree`. `assertGitTarget` (`:505-521`) and `assertWorktreeRepository`
  (`:523-538`) are byte-unchanged and their nine spawns remain. The four remaining
  authority spawns are one `status --porcelain=v2 --branch` per working directory,
  issued in the current order - controller, repository root, worktree, review
  worktree - after `assertWorktreeRepository`, so a family or target mismatch
  still refuses before any authority read. Cleanliness is decided by "no line
  outside the `# branch.*` headers", proven against a fixture whose branch is
  ahead of a configured upstream and therefore reports `# branch.ab +1 -0` while
  clean. Detached HEAD and unborn HEAD still refuse. No `DeliveryConfig` field,
  adapter interface member, exported signature, state-directory record, retained
  artifact, `delivery.ts` call site, timeout, worker setting, `vitest.config.ts`
  key, fixture body or platform skip changes, and no cache, memo, module-level
  map, `WeakMap`, request-scoped object or lazily-initialised promise retains any
  of the 17 facts beyond the single read that uses it.
- **AC2 Fresh authority and intervening drift are still caught.** The
  eleven-variant matrix at `test/dogfood/delivery-adapter.test.ts:805` stays
  eleven distinct cases and still asserts the exact call sequence, with the count
  assertion at `:873` moved from 17 to 13 and the expected argument vectors
  updated. Because `root-dirty` and `root-branch`, and each `%s-HEAD`/`%s-dirty`
  pair, now arrive through one command per working directory, each must drift a
  different field of that command's output and each must still refuse on its own;
  a matrix that collapses to seven cases is a weakened gate, not a simplification.
  The real-Git cases at `:912` (`foreign-fetch`, `foreign-push`, `extra-push`),
  `:968` (worktree-local fetch/push URL drift including the `multiple` tuples),
  `:1043` (repository alias with a rewritten review gitfile) and `:1120` (drift
  between drafts and between gate and publication inside one `deliveryStep`) keep
  their current assertions and still refuse; only the command allow-lists those
  cases enumerate may change, and each must be updated to the new shape rather
  than broadened to accept any command. Add a fresh-workspace case proving the
  first call on a newly constructed workspace reads all 13 and accepts.
- **AC3 Negative control: nothing derived survives a single read.** Add one
  focused case that verifies a workspace, mutates a fact of each kind - a head, a
  working-tree state, the root branch, a remote URL and the review worktree's
  common directory - and asserts that the *very next* `verifyWorkspace` call both
  refuses and re-issues the owning command at the owning working directory, with
  no call skipped relative to the 13. Deleting the refusal assertion must fail the
  case. The control must observe real command issue (the
  `workspaceCommands.observe` hook already in this file) rather than assert on an
  internal structure, and it must not rely on ordering with AC2's matrix.
- **AC4 Measured spawn reduction, same host, repeated samples.** Record
  `GIT_TRACE` spawn counts for both closure cases
  (`test/dogfood/refresh.test.ts:898`, partial mirror false and true), base versus
  candidate, at least three repeated samples per arm on the same host, using the
  attempt-2 harness at `.../iss-182-attempt-2/source/author-temp/` as the
  template. Report total spawns per case, the per-command-shape histogram, the
  `verifyWorkspace` share and the exact delta, labelling command-shape attribution
  as such rather than claiming a pure call-site count. The expected shape is about
  -128 spawns per closure case; report what is measured, not what is expected, and
  retain the traces. Repeated runs until a favourable sample appears are not
  measurement.
- **AC5 All four ordinary local gates.** `pnpm typecheck`, `pnpm format:check`,
  `pnpm planning:check` and the complete `pnpm test` pass at the implementation
  head, each resolved from the manifest at that head, with one worker, file-serial
  execution, 30000ms test/hook limits and existing skips unchanged. Capture each
  command, its exit code and the suite's file/test/skip counts. A focused file, a
  passing subset or lint alone is not acceptance. AC2's added fresh-workspace case
  and AC3's control raise the reported test total on every OS; state the delta
  against each OS's own baseline at that head and confirm no existing skip changed.
- **AC6 Independent exact-head review.** An independent reviewer PASSes the exact
  final head with its own execution evidence, covering the AC3 control, the AC2
  matrix preservation and the G0 answer on caching. Any push or rebase invalidates
  a prior PASS; no historical verdict, author PASS or planning review is imported
  as that authority.
- **AC7 Executed three-OS bootstrap with the Windows owning execution.** Ubuntu,
  Windows and macOS bootstrap jobs are green at the final head, observed after
  completion from actual job and step conclusions with run, job, attempt and head
  recorded. Capture the complete Windows execution of
  `test/dogfood/refresh.test.ts` and `test/dogfood/delivery-adapter.test.ts`,
  recording durations for **both** closure variants (`reobserves synthetic closure
  after plan persistence (partial mirror: false|true)`) and **both** six-deletion
  variants (`mirrors the exact six ISS-174 deletions ... (autocrlf: false|true)`)
  under the unchanged 30000ms limit, plus the run's file/test/skip counts and the
  whole file's wall-clock. State the worst-case headroom and normalise it against
  a runner at least as slow as #576's observed 10.8-11.9%: worst observed case
  x 1.12 must be under 30000ms, and the margin must be named. Use the file
  wall-clock against 1358391ms/1344928ms to state how fast this runner actually
  was, so a fast runner cannot be mistaken for headroom. Local Linux success, a
  rollup row, a skipped job, a failed-only rerun or this issue's historical logs
  cannot substitute.
- **AC8 No process-document change.** `docs/loop.md` needs no edit: per-call Git
  invocation shape inside one adapter function is not a loop rule, and the Running
  section already records the one-worker, file-serial constraint and the retained
  timeouts. If the implementer finds a rule sentence genuinely needed, the only
  permitted change is one clause stating that workspace verification derives no
  fact across calls; say which sentence changed and why. No other document,
  README, workflow or `AGENTS.md` edit.

## Evidence and footprint

Read-only host evidence under
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/`:
`platform-iss182-replan-review-r2.report.md` (the independent review that
recommended this issue, with its own recount of the traces), and under
`artifacts/`: `platform-576-windows-105540420500.log`
(SHA-256 `61a0dbab...86dab2`, the demonstrated timeout),
`platform-587-windows-105975837620.log` (`3a0a5103...c7136e`, `490fe76` green)
and `platform-windows-105982180024.log` (`0c56c51c...c23b53`, `2e04eb5` green).
`GIT_TRACE` traces are retained read-only at
`\\wsl.localhost\Ubuntu\root\orchestration-m1\runtime\m1-sibling-mirror-recovery-20260917T0848\iss-182-attempt-2\source\author-temp`
(`trace-refresh-base-mirrortrue-{1,2,3}.log`,
`trace-refresh-mirrortrue-{1,2,3}.log`). These are historical captures, not
fixtures to execute, edit or rerun; their spawn decomposition is pre-ISS-189 and
must be dated as such in any citation.

Salvage pointer: `codex/iss-182` at `465f1beb2f16c6d77a8b3e79bfac247884976f08`
(draft PR #576) may be **read** for its `GIT_TRACE` harness and measurement method
only. Its diff is a spent `refresh.test.ts` fixture candidate worth -7 of 670
spawns; do not reuse it, do not reuse its gates or any PASS recorded there as
current authority, and do not push to that branch.

Implementation footprint: the body of `verifyWorkspace` in
`scripts/dogfood/delivery-adapter.ts` and its coverage in
`test/dogfood/delivery-adapter.test.ts`. No other runtime file, no `delivery.ts`,
no fixture, workflow, dependency, `vitest.config.ts`, `package.json`, lockfile or
documentation change. Planning footprint now: this draft and one additive
`planning/roadmap.json` row under M2. The roadmap stays the only index.

## Don't-rebuild pointers

- `601f79d` / PR #583 (ISS-189) - landed prior art on this exact function: root
  common-directory reuse inside `assertWorktreeRepository`, 18 spawns per call
  down to 17, plus 389 lines of coverage. Its saving is already inside the
  `490fe76` and `2e04eb5` timings quoted above. Do not re-take it, do not count it
  again as available headroom, and keep its lines byte-unchanged.
- ISS-182 owns the `refresh.test.ts` fixture fence and **stays parked**. It is not
  a dependency, is not amended here, and is not un-parked by this brief; its
  un-park is host-owned and requires the measured threshold quoted in `## Why`.
- ISS-126 owns two-root delivery - *which* root each read targets. That separation
  already ships (`repositoryRoot` and `controllerRoot` are distinct in the current
  function), and this issue changes only how many processes deliver the same reads.
  Not a dependency.
- ISS-193 (`db9285a`, PR #587) owns bounded `rm` retries at the dogfood cleanup
  sites. It removes the `EBUSY`-on-cleanup failure mode, not case duration. Do not
  re-implement or re-tune it.
- ISS-171 owns the unresolved-seed `queue.test.ts` fixture Git setup; ISS-168 owns
  `test/executor.test.ts` detached-launch fixture isolation; ISS-169 already
  removed redundant refresh pinning; ISS-178 owns the sibling-mirror product
  contract the closure cases exercise. All unchanged here.
- ISS-160 / PR #509 owns `vitest.config.ts`'s one-worker, file-serial, 30000ms
  settings. No further serialization or configuration work belongs to this issue.
- ISS-185 owns hosted run/job attribution and failure-log binding; ISS-188 owns
  same-run hosted status observation. Hosted evidence here is read, not repaired.

## Verification and sequencing

Implementation runs through the M1 self loop in its own worktree; this planning
lane runs no implementation gate. Commands at the implementation head:

```bash
pnpm exec vitest run test/dogfood/delivery-adapter.test.ts
pnpm exec vitest run test/dogfood/refresh.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm test
```

Every full Windows-host gate goes through
`D:\Users\ToddS\Source\Repos\chase-sets\.orchestrator\invoke-heavy-verifier.ps1`,
one admitted owner at a time and in the foreground; direct heavy Node, Vitest or
build commands acquire the same lock before their body. Never raise a timeout,
edit a product file or skip a test to pass a gate.

Overlapping defect-class constraints (`references/defect-classes.md` is not
present in this checkout, so the class names come from the constraints recorded in
ISS-168, ISS-171 and ISS-193):
`vitest-wall-clock-timeout-under-parallel-host-load` - the #576 timeout is not
attributed to a cause for want of a base control on that runner; any later load
claim requires an unchanged isolated timing collapse **and** a complete
one-worker, file-serial gate. AC7's runner-speed normalisation exists so a fast
runner is not read as headroom.
`aggregate-failure-dismissed-as-environmental-without-a-discriminating-control` -
Ubuntu/macOS green and prior Windows-green main pushes do not exonerate the
demonstrated timeout; AC4's base-versus-candidate repeated sampling is the
discriminating measurement.
`missing-fail-closed-regression-evidence` - AC3 proves the failure path is
retained and that nothing is derived across a read.
`cached-authority-masks-live-drift` - the whole `## Decision` span analysis, AC1's
no-cache clause and AC2's four real-Git drift cases answer this class directly; it
is the single most likely way this change goes wrong.
`lifecycle-repair-misses-sibling-retained-state-entrypoint` - AC1 fixes the count
at the function, not at one call site, and leaves all `delivery.ts` call sites
intact.
`stale-review-bound-to-wrong-head` - AC6 binds the exact final head.
`unrunnable-verification-command` and `format-check-not-run` - AC5 resolves all
four gates from the manifest at that head.
`trigger-job-masks-real-deploy` and
`mandated-external-assertion-contradicts-authority` - AC7 uses actual job and step
conclusions, never a real run identity paired with invented facts.
`parallel-node-verifiers-exhaust-host-memory`,
`heavy-verifier-lock-survives-forced-wrapper-termination` and
`worker-tool-ceiling-kills-heavy-verifier-wrapper` - one foreground admitted
owner, no bypass, no lock steal, no unrelated process kill.

External authority and evidence timing: no acceptance criterion depends on data
from an external authority acquired now. AC7's three-OS conclusions must be
observed at the final head after those jobs complete and rechecked before landing;
the retained logs above cannot supply them. The `status --porcelain=v2 --branch`
and `remote get-url` semantics recorded in `## Decision` were confirmed against a
real Git on 2026-09-19 and must be re-confirmed at implementation time against the
executable the adapter actually resolves, on the Windows runner as well as the
Ubuntu executor, before any of them is relied on.

Sequencing: M2 registration. Host publication of this planning change, three-OS
landing and canonical issue/project registration precede readiness, and
`pnpm planning:board-check` reconciles the board. This brief grants no rerun of
#576 or any other job, no ISS-182 un-park, no executor installation, restart,
preserved-run resume, new native author or `ready` label.

## Not built

| Not built                                                | Reason                                                                 |
| -------------------------------------------------------- | ---------------------------------------------------------------------- |
| Cache of any of the 17 facts, at any span                | Four landed cases refuse it; the remote check is anti-exfiltration      |
| `git remote -v` in place of the `get-url` pair           | Reports only the first fetch URL; loses the `multiple` refusal          |
| `git config --get-all` for remote URLs                   | Skips `url.<base>.insteadOf` rewriting the tests depend on              |
| `git worktree list --porcelain` for family membership    | Asks the root, not each worktree; cannot see a rewritten gitfile        |
| `assertGitTarget` or `assertWorktreeRepository` edits     | Four other call sites and ISS-189's landed lines depend on them         |
| Removing `delivery.ts:1161`'s verification before a gate | Moves an orchestrator guarantee into one adapter's self-guard           |
| Larger timeout, new skip, worker or parallelism change   | Weakens or repeats an existing gate                                     |
| Stubbed `verifyWorkspace` in the closure cases           | ISS-182 AC1 forbids it; it hides the cost instead of removing it        |
| Fixture, `refresh.test.ts` or `vitest.config.ts` change  | ISS-182, ISS-171 and ISS-160 own those surfaces                         |
| ISS-182 un-park, #576 rerun or branch edit               | Separately owned host authority, not implementation                     |

## Out of scope

No #576 branch edit, CI rerun, merge, `ready` label or historical result
replacement. No ISS-182, ISS-171, ISS-168 or ISS-126 amendment. No product
(chase-sets) change. No suite-wide performance sweep, no other adapter function,
and no reduction of Git work outside `verifyWorkspace` - the roughly 89 residual
fixture spawns per closure case are ISS-182's fence and AC1-required there.
Review attacks a cached or memoised fact at any span, a collapsed drift matrix, a
cleanliness predicate that misreads `# branch.ab` or an upstream header, a
detached or unborn HEAD that newly passes, a spawn count claimed rather than
traced, green evidence from the wrong head or a test subset, and any runner-speed
claim the captured logs do not support.
