---
key: ISS-002
title: "Define versioned configuration, state, and receipt contracts"
labels: ["type:slice", "area:contracts", "priority:p1"]
milestone: "Portable substrate"
parent: EPIC-RUNTIME
blocked_by: [ISS-001]
---

## Context and evidence

Repeated review proved that unsigned rows cannot authenticate producer epochs,
current pointers cannot assert their own predecessor, recovery journals cannot
be unselected side channels, and bootstrap genesis cannot name a nonexistent
self epoch. The proportionality review (pressure round 34) then proved the
opposite failure: sparse authority trees, a node-inventory census, a
materialization coordinator, an epoch-split commit run, and arbitrary-precision
ordinals defended threats the single-writer topology already excludes, and the
never-deployed schema generations they superseded had accumulated into a frozen
diagnostic namespace with no consumer. The approved architecture in the four
contract documents, as amended by the `Proportionality and schema lifecycle`
decisions, is authoritative.

## Scope fence

In: platform-neutral schemas, total parsers, canonical bytes, compatibility,
framed digest domains over safe-integer ordinals, closed record/array
snapshots, eleven runtime pointer families, the hash-chained linear
`authority-history/v1` log with reviewed-operation/successor-core/genesis-
selection contracts, external destination-owner/anchor contracts, stable
physical identity and versioned observations, the closed ordinary/rotation
single-epoch commit evidence and packet unions, release/gate/fence/cleanup, authorization,
reservation/descriptor/attempt-log records, the closed
`dispatch-brief/v1` schema, the engine-vocabulary lint, and pure transition
validators.

Out: filesystem mutation, locks, private capabilities, process launch, broker
operations, release policy, and adapter/project vocabulary. This issue defines
contracts but never makes a candidate or receipt its own authority.

## Decisions already made

- The acyclic graph is `Dv=value`, `Dr=pointer-cas-proposal-receipt/v1(prior
triple,Dv,closed producer)`, and `Dt=tip(Dv,Dr)` under exact framing.
- Proposal intents are `VALUE_PROPOSED|TOMBSTONE_PROPOSED`; classifications are
  `PENDING|SELECTED|LOST_CONFLICT|UNKNOWN`; timestamps are create-once.
- Pointer instance and mutation identity bind kind, canonical path, identities,
  transaction, source, position, prior triple, successor `Dv`, and outcome.
- The registry has exactly eleven kinds, including authority rotation and the
  generic `POINTER_MUTATION_RUN_CURRENT` META_LEAF. No materialization
  coordinator or retention/compaction kind exists.
- Terminal removal retains a selected tombstone; no post-genesis bare absence
  is authority.
- `Dphys` is stable physical identity; helper/profile/custody/readback facts are
  versioned `Dobs` observations. `Ddest` frames raw `Dphys` alone. A FULL_REQUIRED
  destination owner and external anchor serialize E0/reinstall/teardown.
- E0 uses external ACTIVE/CONSUMED evidence and an immutable core that excludes
  proposal/`Dr`/tip/`Dt`; its bootstrap producer has no selected epoch. Runtime
  and external post-selection receipts complete the graph without a cycle.
- `state-mutation-authority-value/v1` binds the selected `authority-history/v1`
  head ordinal and record digest. Both history arms bind the exact shared
  successor core `Dsc`: `G`, authority `Dp`, successor ordinal, reviewed release
  manifest/bytes/subject/review, derived BOOTSTRAP_INSTALL/STABLE_PROMOTION
  operation `Dop`, helper/profile/ABI/lock/state-component, and custody facts.
  GENESIS binds ordinal zero, genesis predecessor, and `Dgb` over selected
  owner/anchor ACTIVE, use intent, and bootstrap identity/transaction/grant;
  downstream selection evidence binds `Dbg`, E0 selection/readbacks/`Dgp`, and
  owner/anchor CONSUMED without entering the record. ROTATION binds exact prior
  head, retiring `Dp/Dt/Dv/Dr`, and `Drot`, recomputed from rotation
  transaction, retiring authority/head, successor ordinal, `Dop`, and `Dsc`.
  Both exclude successor value/proposal/tip/head and downstream artifacts.
- The normative simplified-authority schema ledger in
  `docs/architecture/supervisor-contract.md` fixes every JSON member name,
  canonical member order, scalar/nullability/enum rule, persistence status,
  digest domain, ordered framed part, and excluded field for `Dop`, `Dsc`,
  `Dgb`, `Dgse`, `Drot`, both history arms, selected authority value, and both
  `Dcommit` arms. Symbolic labels are not implementer-selected field names.
  History records serialize as `authority-history-record/v1`; only their digest
  uses domain `authority-history/v1`.
- The normative external-bootstrap schema ledger incorporated by
  `contract-decisions.md` fixes the literal physical identity/locator and
  destination-absence observations,
  destination-owner/successor-review, anchor/use/consumption/teardown, and E0
  records. It fixes their nested records, scalar and leaf-byte grammars,
  nullability and lifecycle branch matrix, persistence, digest framing,
  exclusions, and cross-record equalities. The use intent contains only
  independent preimage facts; `Dgb/Dh/Dv/Dbg` bind it downstream. Digest-table
  labels are not JSON members, and downstream receipts never enter the upstream
  graph they attest.
- The normative active-release ledger incorporated by
  `contract-decisions.md` fixes direct seven-member `active-release/v1`, its
  immutable release-root equality, stable installation-scoped `Dp`, empty VALUE
  position, common `Dv/Dr/Dt`, and initial reviewed-bootstrap producer branch.
  E0 consumes the actual selected value/proposal/tip, rederives all four
  digests, and binds release/subject, manifest, installed-byte read-back,
  independent review, and installer directly to the exact
  `BOOTSTRAP_INSTALL` operation and `Dsc`. Later promotion remains
  selected-stable-epoch-only; no candidate verdict, nested proof record, or
  digest-only expectation grants authority.
- Ordinals and counts are canonical decimal strings bounded by the safe-integer
  range. Grammar, length, and lexicographic comparison refuse above
  `Number.MAX_SAFE_INTEGER` before conversion. No arbitrary-precision type exists.
- Every commit run is single-epoch. `pointer-mutation-commit-evidence/v1` is a
  closed `ORDINARY|AUTHORITY_ROTATION` union. ORDINARY retains checkpoints 0–8
  and `SELECTED|LOST_CONFLICT|UNKNOWN_TERMINAL` resolution. Rotation is a special single-epoch arm
  under the old capability that appends the chain record and then performs the
  authority CAS as its final action; its run-current journal legitimately
  rests at CAS-armed. Prior selected plus exact matching head-plus-one is
  resumable under the old epoch; successor selected plus its exact chain record
  and the old CAS-armed checkpoint derives `SELECTED`; otherwise rotation is
  `UNKNOWN`. It binds selected old checkpoint 5, expected successor/history,
  and old/successor/empty authority slot; checkpoints 6–8, ordinary resolution,
  later selector artifacts, post-CAS new-epoch writes, and rotation receipts
  refuse.
- The `Dcommit` outcome byte is an exact branch-local declaration-order tag.
  ORDINARY uses `0x00=SELECTED`, `0x01=LOST_CONFLICT`, and
  `0x02=UNKNOWN_TERMINAL`; AUTHORITY_ROTATION independently uses
  `0x00=RESUMABLE`, `0x01=SELECTED`, and `0x02=UNKNOWN`. The digest frames the
  fixed raw byte rather than enum text or a caller-selected number.
- `pointer-mutation-commit-evidence/v1.priorCheckpointEvidence` is the nullable
  closed prior-run checkpoint composition and the sole source of the run-ID
  prior-checkpoint input. For ORDINARY it is null exactly for run ordinal zero;
  positive ordinals require the immediately prior run's complete evidence,
  recomputed `Dcore`, selected run-current tuple, and `Dpost`, equal-bound to
  checkpoint zero. Rotation requires run ordinal zero and null prior evidence;
  a crash reuses its same checkpoint-5 CAS-armed run and same transaction/CAS.
  Every checkpoint target `Dp` is recomputed from its existing complete
  platform identity tuple; repeated caller-selected identities never suffice.
- Checkpoint cores exclude their selecting run-current graph; a downstream
  post-selection observation feeds the next core. `PROPOSED` is live-only;
  ordinary durable resolution is `SELECTED|LOST_CONFLICT|UNKNOWN_TERMINAL`;
  rotation has only the derived `Dcommit` arm above.
- The ordinary-only `pointer-mutation-commit-resolution/v1` census replaces
  the deleted sparse `producerEpochKey` with exactly four required selected
  producer-authority fields: `producerAuthorityPathInstanceDigest`,
  `producerAuthorityTipDigest`, `producerAuthorityValueDigest`, and
  `producerAuthorityReceiptDigest`. The tuple equals the run and KNOWN packet
  authority. Its closed SELECTED/LOST_CONFLICT/UNKNOWN_TERMINAL arms bind
  exactly one selected-tip/conflict/unknown digest to `outcomeEvidenceDigest`;
  rotation, membership, root, proof, and run-current selector fields refuse.
- `pointer-mutation-run-intent/v1` is a create-once closed
  `ORDINARY|AUTHORITY_ROTATION` union. Both arms bind `SINGLE_EPOCH`, exact
  target identity, `G`, selected old authority `Dp/Dt/Dv/Dr`, and creation
  time. Rotation alone carries the closed rotation input and recomputed `Drot`,
  expected successor `Dv`, expected head ordinal/record digest, and `Dsc`;
  ordinary forbids them. No caller-selected epoch or rotation key exists.
- `pointer-mutation-conflict-evidence/v1` closes the losing proposal, selected
  real winner, and conflict receipt under one target identity. The exact
  three-field `pointer-evidence-slot/v1` admits null, generic selected evidence,
  or that conflict composition; LOST_CONFLICT requires the conflict arm,
  positive non-conflict outcomes require generic selected evidence, and unknown
  requires null.
  ORDINARY SELECTED additionally equal-binds the target mutation, recomputed
  selected tip, and proposal producer `Dt/Dv/Dr` to the resolution and
  old/KNOWN packet tuple. LOST_CONFLICT equal-binds the losing mutation,
  recomputed conflict receipt, and both losing-proposal and conflict-receipt
  producer `Dt/Dv/Dr` to that tuple. Producer `Dp` remains bound by the common
  packet and resolution fields and is not invented as a duplicate generic
  proposal/conflict member.
- `authority-history-binding/v1` is the non-persisted six-field packet
  composition of `G`, selected head, the dense complete record array from zero
  through head, and exact genesis-selection evidence. It reuses the full linear
  walk and adds no path, tree, proof, inventory, checkpoint, compaction, or
  capability.
- `pointer-evidence-packet/v1` separates `HISTORICAL_READ` from
  `MUTATION_COMMIT`. Mutation outcome cross-binds top authority and identities
  to the exact registry slot: selected target, real winner, or empty for
  `SELECTED`, `LOST_CONFLICT`, or `UNKNOWN`. Unknown evidence is a fixed-size
  closed `UNREADABLE|MALFORMED|IMPOSSIBLE` union with closed enums, digest, and
  safe length—never arbitrary JSON, native text, paths, or arrays. Pure
  structural success never grants a live capability.
  Its outer `currentAuthoritySelection` and `authorityHistoryBinding` are both
  non-null and cross-bound for historical reads, every ordinary outcome, and
  rotation RESUMABLE/SELECTED. Rotation UNKNOWN requires both exactly null,
  matching UNKNOWN/null packet authority and its empty authority slot; partial
  nullability or any capability use refuses.
- Recovery authorization uses the exact acyclic core projection and ordered
  native-consume/select/post-receipt and revoke sequence.
- Attempts use a CAS-selected reservation that is durable before process
  creation and carries the descriptor inputs, a LIVE descriptor binding that
  reservation, attachment, and an append-only `attempt-log/v1` whose records
  chain by predecessor digest and safe-integer ordinal; the TERMINAL record
  carries the folded terminal facts, and each operation verifies the full
  log, which stays small because attempts are rare.
- `dispatch-brief/v1` is the closed structure a planning module emits for a
  worker; every human-readable section derives from typed fields, and the
  host-rendered bytes are digest-bound in the dispatch plan.
  The normative ledger in `contract-decisions.md` fixes one envelope plus its
  three closed nested records, exact schema literals, release-reviewed module
  dispatch catalog, exact `dispatch-action-core/v1` framing, step-4
  action/subject/descriptor bindings, step-7 role and route-selected active
  host-renderer binding, exhaustive per-kind requirements, and opaque
  resources. Nine
  directive kinds cover scope, decisions, constraints, non-goals, acceptance,
  verification, operator actions, and review attack surface without a
  free-form prose or arbitrary schema-reference field.
- Closed arrays accept exact same-realm mutable/sealed/frozen arrays and reject
  reflective or structural extras exactly as `contract-decisions.md` states.

## Acceptance criteria and evidence

1. Every public parse/serialize/validate call is total and snapshots every
   envelope, record, and nested array before semantic reads. Executable mutants
   cover transparent/throwing proxies, symbols, accessors, non-enumerables,
   subclasses, exotic prototypes, holes, custom iterators, and frozen arrays.
2. Canonical serialization and every digest domain have exact-byte goldens,
   cross-domain negatives, raw-digest framing tests, and cross-OS vectors.
   Ordinal goldens cover zero, one, `2^53 - 1`, and refusal above the bound
   before numeric conversion.
3. The registry census is exactly eleven kinds and exact path constructors,
   schemas, roots, archives, source tokens, and genesis rules.
   Unknown/case/encoding/cross-family paths and source-token collisions refuse.
4. Proposal/conflict tests prove same mutation+bytes idempotence, different
   bytes conflict, real-winner lost classification, malformed `UNKNOWN`, retry
   timestamp reuse, and no self-asserted predecessor.
5. Cleanup validates exactly ten states and twelve mutation edges, no write
   self-loops; fence validates only its one forward edge. Full ordered history,
   ordinal, predecessor, root, path, digest, and terminal-head mutants execute.
6. Auth core tests mutate every common and mode-union field and prove the gate
   binds selected CREATED `Dt/Dv/Dr` without a digest cycle. Consume, attach,
   revoke, and native-receipt matrices fail closed with one mutant per bound
   field plus one coordinated all-field mutant per matrix.
7. Reservation/descriptor/attempt-log goldens prove UUID uniqueness-only,
   predecessor reuse, chained ordinal continuity, first in-progress absence,
   attachment-to-descriptor binding, and the TERMINAL record carrying the
   folded terminal facts with no separate summary document.
8. Record-class tests prove every class is FULL_REQUIRED and no compaction
   path exists; loss of any required record is `UNKNOWN` and blocks mutation,
   and the deleted retention kind, classification, and audit mode are
   unreachable.
9. Physical identity/observation, destination-owner, successor review-core/
   post-receipt, external anchor/use/consumption, E0 core/post, expiry, race,
   teardown, and exact reinstall matrices prove one owner per physical target.
   The E0 matrix also parses the exact seven-member `active-release/v1`, derives
   its stable `Dp` and selected `Dv/Dr/Dt`, proves the initial null-prior/null-
   epoch reviewed-bootstrap proposal, and binds all six digests to the exact
   reviewed `BOOTSTRAP_INSTALL` operation/`Dsc` and immutable release root.
   Independent and coordinated mutants replace each value member, release-root
   identity, position evidence, mutation ID, producer kind/digest, prior/epoch
   branch, selected value/proposal/tip, or E0 sibling; each refuses. Tests also
   prove a candidate/self epoch cannot select N0 and that a later promotion
   cannot use the bootstrap producer branch.
10. Authority-history tests prove the exact closed formulas for `Dop`, `Dsc`,
    `Dgb`, branch-specific `Dh`, derived `Drot`, and downstream genesis-
    selection evidence. GENESIS covers owner/anchor ACTIVE, use intent,
    bootstrap identity/transaction/grant, E0 core/selection/readbacks/`Dgp`,
    owner/anchor CONSUMED, consumption, and no retiring epoch without a cycle.
    ROTATION covers exact prior selected head, retiring `Dp/Dt/Dv/Dr`, reviewed
    operation/release/subject, and successor helper/profile/ABI/custody. Both
    reject successor value/proposal/tip/head and downstream-artifact insertion. They prove append verification,
    and the full-chain walk over constructed canonical ordinal paths against
    the selected head with no directory enumeration: fork/gap/reorder/
    truncation and head-ordinal/digest mismatch refuse. A head-plus-one record
    is accepted only when its ordinal is head plus one, its predecessor digest
    equals the head record digest, and its rotation identity and successor
    authority facts equal the selected armed rotation intent, the CAS-armed
    run-current journal plus its create-once intent record; when no armed
    intent is selected, any head-plus-one file refuses, and head-plus-two is
    absent. Scale evidence references the ISS-006-published 1,000-record,
    five-seconds-per-OS golden and budget; checkpointing stays parked unless
    that measured gate fails.
    Schema-census mutants remove, add, rename, null, reorder, or cross-type every
    literal ledger member and substitute `authority-history/v1` as a record
    `schemaVersion`; every mutant refuses before digest comparison.
11. Commit tests prove the closed ORDINARY/ROTATION evidence union. ORDINARY
    retains all nine stages, selected generic META_LEAF paths, acyclic graph,
    and selected/lost/unknown resolution. Rotation binds old E(n) intent and
    selected checkpoint 5, expected successor `Dv`/target mutation/head/
    record/`Drot`/`Dsc`, then exact RESUMABLE old slot, SELECTED successor slot,
    or UNKNOWN empty slot. Tests reject checkpoints 6–8, ordinary resolution,
    later selector artifacts, and every post-CAS new-epoch write. Packet tests
    cross-bind `Dcommit`, top authority, all identity fields, and exact slots,
    and reject unknown union extras, open enums, unsafe lengths, native text,
    paths, JSON, and arrays.
    They also execute the exact outcome-to-slot matrix: ordinary selected and
    lost require their selected target/real winner slot, ordinary unknown is
    empty, and rotation resumable/selected/unknown require old/successor/empty.
    Empty-at-positive, wrong-kind, stale, and nonempty-at-unknown slots refuse.
    Digest mutants prove ORDINARY outcome tags are exactly `00/01/02` for
    SELECTED/LOST_CONFLICT/UNKNOWN_TERMINAL and rotation tags are independently
    `00/01/02` for RESUMABLE/SELECTED/UNKNOWN; text encoding, swapped tags,
    cross-branch meanings, and any other byte refuse.
    Packet nullability mutants prove both outer authority members are non-null
    and cross-bound for historical reads, ordinary outcomes, and rotation
    RESUMABLE/SELECTED, while rotation UNKNOWN requires both null; every
    one-null/one-non-null combination and UNKNOWN capability attempt refuses.
    Ordinary run-lineage mutants change or omit prior checkpoint evidence,
    cross zero and positive ordinal nullability, skip/reuse/reorder the prior
    run ordinal, substitute prior `Dcore`, selector `Dp/Dt/Dv/Dr`, or `Dpost`,
    and copy a forged run ID through every sibling. Rotation mutants try a
    positive run ordinal, non-null prior evidence, or another checkpoint-5 run;
    each refuses. Target-identity mutants independently and coordinately
    substitute kind/path/installation/project/state/transaction/source and
    copied target `Dp`; every case refuses before `Dcommit` or packet use.
    Resolution census mutants remove/rename/null each producer-authority field,
    restore `producerEpochKey`, substitute a partial or coordinated wrong
    producer tuple, cross it with the packet/run epoch, and mutate every
    outcome-arm nullable/evidence equality; each refuses before `Dcommit` use.
    Intent census mutants remove/rename/null/extra every common member, swap the
    selected old tuple, restore an epoch key, cross the target arm, or mismatch
    rotation input/`Drot`/successor/head/record/`Dsc`; each refuses before a run
    is admitted. Conflict-composition mutants substitute loser, winner, receipt,
    kind, target, or nested arm and prove LOST_CONFLICT cannot carry a generic
    or caller-asserted winner. Independent and coordinated mutants pair a valid
    resolution with a different selected tip or conflict receipt, change the
    selected/losing proposal mutation, and substitute each proposal/conflict
    producer `Dt/Dv/Dr`; each refuses before `Dcommit`. Authority-history-
    binding mutants truncate, extend, reorder, gap, fork, change
    `G`/head/genesis selection, or add a proof/inventory/compaction member; each
    refuses before packet use.
    A packet-census mutant proves exactly eleven total registry-ordered slots,
    exactly ten non-target slots, one target occurrence, and no omitted kind.
    A crash between append and CAS resumes only via the same transaction under
    the old capability re-driving the same CAS.
12. `dispatch-brief/v1` tests prove its closed structural parser and that no
    free-form prose field exists. Tests cover the envelope plus exactly three
    nested schemas; every exact schema literal and wrong-family, missing, null,
    case, unknown, and future variants; the release-reviewed bounded catalog;
    catalog-key prose/vocabulary smuggling; exact action-core schema/domain/
    framing/order/exclusions and byte goldens; step-4 descriptor, action,
    capability, and subject equality; exact closed `worker-host-identity/v1`
    schema/domain/framing/order/exclusions/stability and 1–256 sorted unique
    capability array; exact closed worker-host renderer mapping and total
    admission relation; step-5 admitted exact capability membership; step-7
    role, opaque host identity, and installed active host-renderer artifact
    equality; all nine
    directive kinds; exhaustive
    operator-action `PRESENT|ABSENT` XOR and per-kind minimums; nonempty fixed
    template/typed-value rendering; `(directiveKind,code)` directive and four-
    part catalog-key uniqueness; zero/one/256/257, dense/hole/extra, mutable/
    sealed/frozen array cases; opaque resource identity/access with no resource
    kind; and proxy/symbol/accessor mutants. Unsupported references, moved
    plans/subjects, changed roles, duplicate/substituted catalogs, stale/
    candidate/self-reported renderers, missing/extra/future capability records,
    capability duplicate/case substitution, moved-registration/coordinated
    identity-artifact/cross-host/manifest/read-back substitution, and raw
    catalog rendering refuse. `ISS-011` emits the
    structure/catalog; each host issue owns its versioned template and artifact,
    beginning with `ISS-021`; `ISS-020`/`ISS-014` own N0/successor reviewed
    manifest and install read-back mappings; `ISS-008` owns dispatch-plan/
    rendered-byte binding; `ISS-012` owns step-5 opaque host selection; and
    `ISS-026` owns step-4/step-7 composition equality.
13. Superseded pre-deployment schema generations are deleted from the package
    and its tests; no diagnostic or archive namespace exports. The shipped
    census is `v1` for every family, compatibility refuses unknown and future
    versions, and the API-surface test proves no superseded symbol is
    reachable. No deployment migration is claimed.
14. A generated vocabulary lint proves no engine contract field name, closed
    enum value, release-reviewed dispatch-catalog value, or runtime catalog
    lookup uses adapter or consumer vocabulary from the `ISS-001` denylist;
    seeded declaration, catalog, and runtime-term mutants fail the contracts
    build or admission.

## Verification plan

`pnpm --filter @orchestration-platform/contracts test`

`pnpm run contracts:compatibility-check`

## Predicted footprint and dependencies

The historical reconciliation base at `e30ff05` is a 45-path multi-issue delta
from the named branch-B base, not the ISS-002 runtime implementation. Its exact
base-path census is five
architecture documents (`bootstrap-manifests`, `contract-decisions`,
`routine-cycle`, `state-machines`, and `supervisor-contract`), compatibility,
deletion of the consumer migration document and JSON snapshot, two platform
planning documents, the root package manifest, the ADOPTION/RUNTIME/SELFHOST
epics, issue drafts ISS-002/004/006/014/015/020/026/030/036/038/039/040/044/045,
ten pressure-history/reconciliation records, `planning/roadmap.json`, two board
checker files, two planning checker files, and two planning/board tests.
ISS-002 owns the contract-design subset; program planning owns topology,
evidence history, and tooling. This historical path census is not a substantive
content boundary: each later exact-head gate separately enumerates every
non-pressure path changed after `e30ff05`, its bounded change, the current
unique-path total, and the current pressure-record total. The board checker
reads only this repository and the single delivery Project registered by
`planning/roadmap.json`; consumer backlogs and Projects are outside platform
authority.

The later implementation phase owns
`packages/contracts/`, `test/contracts/`, and exact ISS-002 capability slots
already declared by the repository convention, including deletion of the
superseded sparse-tree, coordinator, census, arbitrary-precision decimal, and
legacy authority-diagnostic sources while retaining the bounded decimal parser.
It may not edit planning policy/topology, root wrappers/manifests, shared
scripts, glossary, unrelated packages, or implementation-owned state.
The active-release slice is bounded to the existing pointer/genesis contract
modules, their public export/compatibility registries, and focused contract
tests; it adds no package, command, filesystem mutator, release producer,
cleanup/gate/fence schema, or recovery implementation.

Directly blocked by `ISS-001`; `ISS-000` remains its transitive prerequisite.
It provides schema authority to `ISS-006`
and `ISS-022`; `ISS-004` remains the producer of mutations.

## Producer and consumer ownership

`ISS-002` produces schemas, canonicalizers, pure relative validators, path
registries, the linear authority-history contract, run schemas, and goldens.
That ownership includes the exact `Dop`/`Dsc`/`Dgb`/`Dgse`/`Drot`/`Dh` formulas
and the ORDINARY/AUTHORITY_ROTATION `Dcommit` union.
It issues no brand or capability. `ISS-004` produces ROTATION chain and run
records; `ISS-020` produces the GENESIS record and downstream selection
evidence; `ISS-022` supplies admitted
lock/custody facts; `ISS-014`, `ISS-030`, and `ISS-032` consume the
selected authority transition. `ISS-041` is the first runtime consumer of the
published parsers. Runtime consumers validate through this package and the
ISS-004 live service; they cannot reinterpret fields.

## Operator actions

None.

## Glossary impact

None; this amendment uses architecture-owned platform vocabulary.

## Independent review packet seed

Attack every digest cycle and excluded field, physical aliases and parallel
owners, stale intents, E0 self-epoch, chain fork/gap/reorder/truncation, head
substitution, forged derived rotation terminal truth, unresolved
head-plus-one pending records, copied/revoked handles, META_LEAF recursion,
non-generic storage, crash ambiguity, packet-purpose widening, ordinal
overflow handling, fake lost, bare absence, array reflection, auth omissions,
superseded symbols
reachable after deletion, prose smuggled through dispatch-brief fields, and
vocabulary-lint bypass through abbreviation.
