---
key: ISS-168
title: "Isolate the detached-launch fixture from inherited supervisor state"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

Second independent complete-sweep semantic review PASS; host publication and
registration pending. No ready label. Reserved key ISS-168; ISS-164 through ISS-167
stay reserved.
Existing native milestone 8. Second planning pass, not a new source attempt.

ISS-163/#515, M1 `m1-iss146-147-20260914T2325`, cycle 5, attempt 1 stopped
`gate-attribution-unknown:test` after accepted source and independent Opus PASS:
https://github.com/todd-skelton/orchestration-platform/issues/515#issuecomment-5703158646
No closed implementation PR or blocking stopping-review finding IDs exist.
This is a new fixture defect, NOT replacement of ISS-163's selection outcome.

Read-only retained source: branch
`codex/run-8206815a33822cf85ef7cf38c7489e0119b73ff7765e4a052e629fec938661bb/iss-163-attempt-1`
at `afc0a079c7e078e91c49aadf74591a0fcd7e722f`.
Do not transplant its selection implementation or reuse its PASS/gates as
repair-head authority. Preserve that source and all original records.

Evidence root:
`/root/orchestration-m1/runtime/m1-iss146-147-20260914T2325/iss-163-attempt-1/source`.
`gate-9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08/{candidate.log,candidate-terminal.json}`:
complete `pnpm run test`, accepted head, exit 1/no signal; 883 passed, 1 failed,
2 existing skips. Sole failure: `test/executor.test.ts:193`, empty launch stdout.

Remote main/stable executor `bdfbeca128ff0f56d4560247355ba63720d7c561`
still copies `process.env` into this fixture's bash child (line 191).
`run-loop.sh:8,18` uses and exports `LOOP_DETACHED=1`; inherited 1 enters the
already-detached branch, bypassing banner and URL initialization.
Host reproduction: Chase Sets root `.orchestrator/artifacts/iss163-detach-repro.mjs`;
`/tmp/iss163-host-detach-{Ubd66L,V9SYbK}` retained. At 2026-09-16 19:18:34 UTC,
absent selector produced banner and synthetic 10.9.8.7 URLs; selector 1 produced
empty stdout/URLs. Host evidence only, not integrated-test PASS.

### Don't-rebuild pointers

- `test/executor.test.ts`
- `scripts/executor/run-loop.sh`
- `scripts/executor/start-loop.ps1`
- `scripts/dogfood/queue.ts`
- `planning/drafts/ISS-157.md`

## Decisions already made

Production detach and ISS-162 URL/bridge behavior stay unchanged. ISS-146/#457's
`test/dogfood/adapter.test.ts` provider isolation, scope and budget remain accepted
and separate. Shipped ISS-156 auth-clock isolation/ISS-157 recovery and reserved
DB ISS-164-166/reviewed-integration ISS-167 do not fix this boundary. No new Decision.

## Scope fence

Bound the implementation to the existing `test/executor.test.ts` fixture
environment and remove only inherited `LOOP_DETACHED` at that subprocess
boundary. Preserve the fixture's existing TASK_ROOT, config, fake `ip`, fake
`pnpm`, PATH, assertions, polling bounds, and production launcher unchanged.
Non-goals: changes to run-loop/start-loop, provider probing, runtime, schemas,
queue/executor/worker/config, other suites, dependencies, timeouts, skips,
assertion removal, selection repair, source transplantation, installation or restart.
Do not add a committed bypass switch, environment helper/framework, generic
sanitizer, provider scrubber, or a second behavior case merely to prove the test.

## Done when

1. **AC1: Correct branch despite inherited state.** Make the discriminating
   regression the same integrated Linux test under a hostile parent
   `LOOP_DETACHED=1`: current main/test-only control fails the banner, and the
   candidate passes the existing banner, URL, auth, config, CODEX_HOME, cwd,
   and empty-log assertions. Preserve exact args and both base/status URLs.
   Evidence: `exports the provider and pool status URLs into the detached
   supervisor` in `test/executor.test.ts`, not an environment-object unit test.
2. **AC2: Discriminating negative control.** Execute that integrated test on
   current main without selector removal and on the repaired candidate, with
   the same hostile parent and all other fixture inputs fixed. Retain exact
   heads, command and banner-failure output for current-main red and candidate
   green. Failure at startup or child exit instead of the banner does not qualify.
   This is a test-only control, not a committed bypass API or second behavior case.
   The expected control failure does not waive any full gate.
3. **AC3: Genuine, bounded fixture.** Prove the shipped bash detach path invoked
   the owned synthetic `pnpm` and that its detached fake child exited before
   fixture cleanup. Banner/result-file presence or launcher exit alone is not
   child-exit proof. Keep that proof within existing polling/test/hook bounds.
   Evidence: integrated test output plus fixture/diff inspection. Keep existing
   timeouts and OS skips exactly unchanged, including Linux-only and existing
   bridge/PowerShell availability behavior. No real supervisor worker or restart.

## Defect-class constraints

- `missing-fail-closed-regression-evidence`: exact hostile-selector discrimination
  in AC2 fixes every non-governing input; current-main red/candidate green must
  distinguish the named banner failure, not a different startup/exit failure.
- `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`
  and `vitest-wall-clock-timeout-under-parallel-host-load`: this reproduced branch
  defect is not a load diagnosis. No subset waives the aggregate. Any later load
  claim requires unchanged isolated timing collapse AND complete one-worker,
  file-serial gate; never raise timeouts, edit product files or skip tests.
- `parallel-node-verifiers-exhaust-host-memory` and
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: foreground admitted gates,
  finish owned children, no parallel bypass, process kills or owner theft.
- `unrunnable-verification-command` and `format-check-not-run`: manifest-resolved
  native commands and exact-head formatting; lint alone is not acceptance.
- `stale-review-bound-to-wrong-head`: independent review binds the exact repair
  head; pushes/rebases require fresh review. No old PASS import.
- Review/three-OS-green landing precedes later host installation; no installation
  while review is in flight. This is delivery sequencing, not new fixture machinery.

## Verification plan

Future native author, in its repair worktree under existing foreground machine
admission (commands below are requirements, not executed planning evidence):

```bash
pnpm exec vitest run test/executor.test.ts
LOOP_DETACHED=1 pnpm exec vitest run test/executor.test.ts -t 'exports the provider and pool status URLs into the detached supervisor'
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm test
```

Run the hostile command on current main/test-only control and repaired candidate
for AC2; the candidate's ordinary suite must also pass. The complete test gate retains the
shipped one-worker/file-serial configuration and 30000 ms test/hook timeouts.
Every full Windows-host gate uses
`D:\Users\ToddS\Source\Repos\chase-sets\.orchestrator\invoke-heavy-verifier.ps1`;
direct heavy commands need the same lock before their body. No new runner.
Native independent exact-head review and executed Ubuntu/Windows/macOS bootstrap
green remain required. Windows/macOS CI skips of the Linux-only case do not prove it.
Planning-only lint: Chase Sets `scripts/brief-lint.mjs` on this draft/rendered body.

## Footprint & chain

One implementation file: `test/executor.test.ts`. Direct path inspected:
fixture exec -> run-loop parent -> setsid/nohup bash -> synthetic pnpm.
The two other tests exercise pool-bridge and PowerShell command doubles, not
this selector; no companion edit is demonstrated. No runtime/documentation edit.
Blocked by: none. Blocks: eventual ISS-163/#515 gate-stop recovery, not its
accepted source implementation. At later registration mirror that dependency
natively; do not make ISS-168 depend on stopped ISS-163. No wiring done here.

## Operator actions

None for fixture implementation. After second independent semantic PASS, host
registration/planning-PR landing/project membership and dependency wiring precede
a separate ordinary native repair run. Actual exact-head review, full native gates
and executed Ubuntu/Windows/macOS bootstrap green precede repair landing and safe
host installation with supervisors/workers absent. The existing ISS-157 four-field
grant then admits the same original stopped selection through the canonical launcher
and fresh native refresh/DELTA/gates. `ISS-168-host-recovery.md` states the ordered
handoff. No extra authority question, new framework, borrowed budget or action grant.

## External authority probe & evidence timing

none - AC1-3 use local synthetic inputs and shipped code, no external authority data.
Future hosted-green/install/recovery facts must be observed at their actual
admission moment; historical CI and this local reproduction cannot supply them.

## Review packet seed

Full-path/system due execution-boundary scope. Attack false green from the
already-detached branch, premature child cleanup, weakened assertions and stale
PASS recovery. Compare the actual launcher and integrated hostile-parent
current-main red/candidate green with all other inputs fixed. Require owned fake
invocation and exited child, not merely banner/file presence. No environment or
recovery framework; no full-gate or hosted PASS before execution.

QUALITY_PROFILE: contract

## Intent surfaces

| Acceptance criterion | Exercised surface |
| --- | --- |
| AC1 | Linux fixture subprocess and real detached-launch outputs |
| AC2 | Inherited selector at that same subprocess boundary |
| AC3 | Synthetic executable invocation, child exit and existing OS behavior |

## Simplest shape

Remove only inherited LOOP_DETACHED at the existing fixture subprocess boundary; prove it with the same integrated hostile-parent test.

## Not built

| Not built | Reason |
| --- | --- |
| Production selector change | Selector correctly prevents repeated detach |
| Generic sanitizer, allowlist, helper or bypass switch | Only inherited LOOP_DETACHED is demonstrated broken |
| Recovery mechanism or retry | ISS-157 already owns saved-stop recovery |

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

none — no data path changes.

## Contract compatibility

none — no schema, event, or contract changes.

## Glossary impact

none — no new or renamed public names.

## Tier + routing hint

Full path/system; native self routing. Planning dispatch specified
codex/gpt-6-astra/high/row7/override-Todd; expected USD unknown (telemetry only).
