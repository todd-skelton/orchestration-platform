---
key: ISS-211
title: "Retry transient GitHub observations at draft mirroring and learning-note reconciliation"
labels: ["type:slice"]
milestone: "Unattended self-improvement"
blocked_by: []
---

## Why

Run `m1-iss210-20260925T0020` recorded `cycle-1-stop-1` with
`draft-ISS-210-state-unknown` after two attempts and four native launches.
Attempt 2 had independent exact-head PASS and its first three local gates, but
had not reached a draft intent or publication. The host then observed a
`learning-note-state-unknown` exit and contemporaneous WSL TLS failures; the
read-only issue observation found no comment. The retained evidence and
synthetic no-retry probes P3/P4 are in
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/logs/astra-m1-iss210-stop-recovery-r1.report.md:69-90,116-147`.
They demonstrate these single-call failure boundaries, not that the exact
failed delivery request was a TLS timeout or that retries repair WSL networking.

ISS-210 needed two authorized host recovery stages before its separate
[PR #660](https://github.com/todd-skelton/orchestration-platform/pull/660)
merged on 2026-09-25 at 13:34:53Z as
`310891e17320d57963a4cfbe9d8d721f5cfee27b`. That host-assisted
delivery does not satisfy M1's no-per-item-host-step exit criterion
(`docs/loop.md:1175-1178`). This is the next M1 intake, not an amendment to
ISS-210 or a replay of its saved candidate.

Re-derived against `origin/main` `310891e17320d57963a4cfbe9d8d721f5cfee27b`:
`scripts/dogfood/delivery-adapter.ts:1135-1153` catches every initial draft
read exception as unknown and treats a wrong number as unknown;
`scripts/dogfood/delivery.ts:465-499,1204-1224` observes once before mutation
and once after it, preserving intent and refusing unknown. The learning-note
read/post/read sequence is `scripts/dogfood/supervision.ts:816-834`; its
`issue()` implementation conflates command and acquired-data failures at
`:1035-1061`, while `scripts/dogfood/supervise.mjs:402-407` reports an
unclassified note exception as unknown. `supervision.ts:946-959,973-1015`
retains the pending marker and records completion only after reconciliation.

## Decision

The loop must tolerate short transport loss at these observations without treating uncertain mutation or wrong authority as success.
A generic retry of every unknown is smaller to call but would repeat semantic refusals and could double-post a note.
Choose a small fixed retry at the two owning GitHub boundaries, with observation-first reconciliation after any uncertain post and no blind mutation retry.
Accept a bounded delay and a non-parking unknown stop when transport remains unavailable or the post outcome cannot be established.
The choice is wrong if an actual adapter/caller regression still requires a per-item host restart after one transient read, or if it duplicates a write or relaxes an identity refusal.

## Done when

1. **AC1 Bounded transport only.** At draft mirroring and learning-note
   reconciliation, retry only classified GitHub transport failure during a
   read, at most three total read attempts with deterministic 1-second and
   2-second waits. Use the actual adapter error boundary, not an arbitrary
   `{state:"unknown"}` retry or a generic GitHub framework. Acquired malformed
   data, wrong issue/key, authorization refusal (including 401/403), and
   semantic drift never become retryable success or `needs-mutation`. Preserve
   the existing request, test and job timeouts. Retain only sanitized, bounded
   transport diagnostics through the existing unknown/error evidence path;
   neither tokens nor authorization headers may appear there. Evidence:
   adapter/caller tests inject distinct transport, parse, identity, refusal
   and drift cases, including an exhaustion control, with fake time.
2. **AC2 Draft before and after the write.** A transient transport failure
   followed by an authoritative matching observation works both before a
   draft intent and during post-mutation confirmation without a stop, new
   worker launch or implementation attempt. Observe again before any edit;
   a confirmed draft is a no-op. A lost successful edit response must be
   reconciled by observation without a second edit. Persistent transport
   failure remains the existing explicit non-parking draft state/outcome
   unknown stop, never a fabricated receipt, `needs-mutation`, completion or
   PASS. Preserve sibling draft identity and body-only mirror refusals.
   Evidence: actual `githubDeliveryAdapter` observation and `deliveryStep`
   tests count calls, intents, receipts and writes for both observation phases.
3. **AC3 Marker-first learning note.** Use the existing stop marker and saved
   intent. After a possibly successful comment with a lost response, retry
   observation first; one observed matching comment satisfies reconciliation
   without posting again. A second post is allowed only after authoritative
   absence AND proof the prior operation could not have been accepted (for
   example a classified pre-send failure); absence alone after an uncertain
   response is insufficient, so unresolved outcome stops non-parking. Keep
   any posting allowance fixed and bounded; retain duplicate-marker,
   wrong-key/number, closed-issue and malformed-observation refusals. Write
   stop-completion only after the matching note is confirmed. Evidence:
   `stopCycle`/`reconcilePendingStop` and actual supervision-adapter tests
   distinguish a pre-send failure, lost successful response, existing note,
   duplicate and unresolved post without duplicate writes. Capture the
   authoritative issue/comment probe at the post-response reconciliation
   moment, not from a pre-post or cached observation.
4. **AC4 Discriminating replay regressions.** Exercise one transient read then
   success, persistent failure/exhaustion, wrong/malformed identity, lost
   successful mutation response, pre-existing note, confirmed draft no-op,
   and the pending run-stop first and second invocation paths through the
   owning adapters and callers, not a disconnected helper. Assert no parking,
   invented cycle completion, reset of history/attempts/launches, or duplicate
   writes. Remove just the retry in a targeted mutant: the
   success-after-transient regression must fail while other controls remain
   valid. Freeze unrelated inputs so each refusal test detects its named
   failure rather than an earlier fixture failure. Evidence: executed focused
   tests with fake time, call counts and saved-record assertions.
5. **AC5 Compatibility and qualification.** Existing saved config, stop
   intents, markers and receipts resume without backfill or manual edits;
   same-run first/second invocation semantics and the run-versus-item parking
   policy remain unchanged. Run ordinary full platform gates under host
   admission, obtain independent exact-head review, and require final-head
   Ubuntu/Windows/macOS bootstrap green before landing. Do not transfer the
   new behavior into ISS-210's historical candidate or treat its old PASS as
   review of this change. Evidence: legacy-shape replay, complete gate
   receipts and exact-head hosted check identities.

## Overlap and salvage

At this main, [PR #660](https://github.com/todd-skelton/orchestration-platform/pull/660)
delivered Windows bootstrap sharding, not GitHub observation retry. ISS-178
shipped sibling body mirroring; ISS-188 shipped same-run hosted status
observation without a retry loop; ISS-143 observes hosted-check startup;
ISS-193 retries Windows fixture cleanup, and ISS-103/183 retry worker
failures/verdicts. Open PR #577 contains only ISS-189/190 planning drafts for
bounded delivery reads and setup settlement, not this draft/note transport
boundary. None replaces the marker-first reconciliation or the draft read.
Do not rebuild their behavior, expand this into hosted checks, or treat an
adjacent observer as coverage of both named boundaries.

Read-only historical pointer: ISS-210 PR #660 branch
`codex/iss-210-attempt-2` at exact head
`d9759e6e6cde5ef36fab6fabea970cc49667999b`; its merge is now main.
Use its stop report and old source only as evidence. Do not reuse the old
candidate, review, gates, receipts or runtime as authorization, and do not
resurrect the superseded PR #659 (`codex/iss-210`). There is no stopped
ISS-211 implementation PR to salvage or stopping review finding ID.

## Footprint and verification

One reviewable M1 change. The owning calls are
`scripts/dogfood/delivery-adapter.ts::observeDraft` and
`scripts/dogfood/supervision.ts::postLearningNote`/`issue`; their callers
`scripts/dogfood/delivery.ts::confirmMutation` and
`scripts/dogfood/supervise.mjs` must be inspected and receive only the minimal
diagnostic plumbing if needed. Focused tests belong in
`test/dogfood/delivery-adapter.test.ts`, `test/dogfood/delivery.test.ts` and
`test/dogfood/supervision.test.ts`, plus one short `docs/loop.md` note about
this incident and the bounded behavior. No configuration, record schema,
generic retry facility or broad source sweep. A transport classifier must
prove that a failure occurred before trustworthy data was acquired; a
successful command with malformed JSON is not transport. A missing or
ambiguous error provenance fails closed rather than inferring a safe retry.

Independent review checks classification at both CLI boundaries, mutation
ordering and marker identity, cap/exhaustion evidence, sanitized diagnostics,
all retry-removal and refusal controls, unchanged stop policy and exact-head
gate evidence. G0: there is no simpler behavior that retains both transient
read recovery and uncertain-post safety; retrying the whole delivery/stop
operation would risk duplicate mutation. No new public term, UI, schema or
product data path.

## Out of scope

No generalized GitHub retry framework, retry of every unknown, repeated
uncertain posts, semantic/gate/review failure recovery, parking or completion
policy change, attempt/launch/ceiling increase, new timeouts, skipped tests,
routing changes, network/proxy/TLS configuration, mutation-authority change,
manual publication, live-run restart or claim of an unattended ISS-210 streak.
