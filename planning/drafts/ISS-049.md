---
key: ISS-049
title: "Implement adapter dispatch allocation and resource mapping"
labels: ["type:slice", "area:integrations", "priority:p1", "risk:authority"]
milestone: "Minimum orchestration kernel"
parent: EPIC-ADOPTION
blocked_by: [ISS-008]
---

## Registration context and outcome

ISS-013 intentionally stopped before workspace allocation, materialization, and
adapter-owned resource mapping because no live dispatch consumer supplied their
complete authority inputs. Register this follow-on after `ISS-008`: its actual
dispatch plan, host mapping, reservation, and ownership evidence are the first
honest consumer. Before that edge clears, this issue remains thin and blocked.

## Scope, non-goals, and acceptance evidence

In: elaborate the existing allocation/materialization representations against
the concrete ISS-008 consumer; implement bounded adapter callbacks, opaque
resource mapping, before/after observations, idempotent allocation identity,
and two distinct fixture adapters. Out: mutation apply, review authority,
reclaim/recovery, CI/deploy, state transfer, provider credentials, and consumer
policy.

Acceptance evidence must prove complete mapping and ownership before launch,
zero callbacks on stale/unknown/multiply-owned inputs, identical retry evidence,
and cleanup of a failed pre-launch allocation without granting reclaim authority.
Verification and exact commands are due in a reviewed elaboration before the
edge becomes runnable.

## Verification plan

Registration is checked by the repository planning checker. Before `ISS-008` clears the edge,
a reviewed elaboration must replace this with exact executable allocation and
consumer tests; planning success is not behavior evidence.

## Predicted footprint and review attack surface

Expected ownership is adapter-sdk allocation/materialization source, ISS-008
consumer fixtures, and focused tests only; no new public family is predicted.
Review should attack path smuggling through opaque IDs, allocation without a
selected dispatch plan, stale reservations, double ownership, partial mapping,
retry creating a second resource, and adapter facts becoming engine authority.

## Operator actions

None.
