---
key: ISS-215
title: "Admit one authorized next ordinary attempt after a terminal integration-continuation park"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: [ISS-214]
---

Unready planning draft. This is M1 self-loop mechanism work in M2, not permission
to unpark or resume ISS-146/#457. Its GitHub identity, board mirror, native
dependency and readiness remain host-owned after independent COMPLETE planning
review.

## Why

ISS-146/#457's [completed cycle-11 stop](https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5846435730),
`loop-stop:m1-iss146-147-20260914T2325:11:3`, is
`continuation-failed` after two implementation attempts and 26 accumulated
participants. The persistent
`/root/orchestration-m1/runtime/integration-continuation-23709074995ed14aaffb5cda4d8dc3adbaf167be4ac8512a7f9456e9d00d3a5b.json`
still claims ISS-146 attempt 2 and the cycle-4 stop. The [ISS-214 re-entry
record](https://github.com/todd-skelton/orchestration-platform/issues/368#issuecomment-5846477359)
reports an exercised refresh-bound review followed by real hosted red, not an
admitted new attempt. Independent COMPLETE planning review F1
(`chase-sets/.orchestrator/logs/platform-iss146-plan-review-r1.report.md`)
identified the missing executor path: `scripts/dogfood/queue.ts:770-776` and
`:796-799` refuse ordinary composition with that claim and the exhausted
reviewed attempt, while `:1325`/`:2856` refuse advancing the failed
continuation. `scripts/dogfood/continuation.ts:139-142`
allows acceptedReplan only from 4 to 5. `docs/loop.md:270-292,321-328,1035-1049,1109-1150,1195-1215`
distinguishes these lineages. The demonstrated gap is recovery admission, not
a fresh diagnosis or fix of the Windows delivery-gate failure.

The [FINAL independent delegated decision](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5846783657)
(`astra-iss146-reentry-route-decision-r1`, Q1-Q4/C1-C8) conditionally authorizes
exactly one same-run absolute attempt 3 after this mechanism and the separate
ISS-146 replan qualify. It is not installation, readiness or execution evidence.
Older #4388 rulings [5691451762](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5691451762)
(attempt 2, non-PASS parks), [5776393948](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5776393948)
(one integration), [5838763854](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5838763854)
(spent resolution, no attempt 3/4), and [5843781301](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5843781301)
(one reviewer-only allowance) remain limited to their own instruments. The
standing [5838600629](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5838600629)
and [5838628573](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5838628573)
delegate this process decision; ceiling four alone is not authority.

Read-only salvage is OPEN/DRAFT [PR #673](https://github.com/todd-skelton/orchestration-platform/pull/673),
branch `codex/iss-146-attempt-2` @ exact head
`e3b1506a98069dfe874b9b319effb8099833f009`, and source branch
`codex/run-8206815a33822cf85ef7cf38c7489e0119b73ff7765e4a052e629fec938661bb/iss-146-attempt-2`
@ `539a817b6dcee565fb734a191985a224a0c314b7`. There is no closed
implementation PR or source-review blocking finding ID. Neither artifact's
PASS, gates, marked seed, branch, workspace, publication nor spent grant is
reusable as attempt-3 acceptance. The planning predecessor at
`a8cb9303f7e245f9eb847bd0b65dd1d3e223ae09` failed COMPLETE review
F1-F3; F1 is this mechanism's blocker, while F2/F3 and N1-N4 belong to the
separate ISS-146 replan/attempt-3 Windows work.

## Decision

CLAIM and the completed continuation terminal refuse an ordinary successor,
while all prior records, charges and the published attempt-2 artifact must
remain unchanged. A generic renewal or bare planning unpark is not viable:
the former broadens spent authority, and the latter cannot cross either native
guard. Choose one exact 2-to-3 admission record in native composition, entering
ordinary attempt creation with a fresh base and brief. Accept the downside of
an issue-specific single-use config surface that may later need retirement and
a second authority observation at admission.
The choice is wrong if a saved successor can mint a second author/publication
or if an unchanged old continuation can advance after its terminal failure.

### Don't rebuild

- ISS-167 owns same-attempt integration continuation and its once-only claim;
  ISS-199 owns conflict K/U census and fences; ISS-200 owns the spent-resolution
  allowance. ISS-214 repairs refresh-bound review re-entry, then stopped at
  hosted gates. None grants a new implementation attempt after terminal park.
- `acceptedReplan` is a different-run 4-to-5 instrument, not 2-to-3.
  `gateStopAuthorization` continues an existing stopped gate/publication, not a
  new source attempt. Ordinary failed-attempt advancement is blocked here by
  the claim and continuation terminal. Preserve those paths and their tests.
- ISS-146 still owns committed-gate source isolation, diagnostics and the
  Windows two-class repair. The adjacent mechanisms do not complete #457.

## Done when

1. **Absent authority refuses.** With the recorded persisted CLAIM shape
   present and no new applicable authority, actual production composition
   refuses `integration-continuation-required` before worktree setup or worker
   launch. The old integration packet, spent-resolution renewal, unused
   reviewer-only grant, a `ready` label, or the number 4 alone cannot admit
   another attempt. Test each non-authority control without varying another
   rejecting input.
2. **One exact successor.** A separately recorded delegation for the exact
   repository/ISS-146/#457, same run, latest completed
   implementation/integration terminal, claim and prior publication admits
   exactly absolute attempt 3,
   `iss-146-attempt-3`, once. The native transition supersedes only CLAIM's
   ordinary-composition exclusion for that successor; it never deletes,
   overwrites or edits CLAIM, old stops, reservations, configs, attempts,
   worktrees or receipts. Re-entry resumes that successor rather than
   spending admission again. Attempt-2 continuation replay stays terminal.
3. **Accounting and failure.** Retain both consumed implementation attempts
   and the same run's complete accumulated participants, including intervening
   issues and all 26 now in stop 11:3, not merely the source's eight or
   integration's 23. Retain failed-author/routing history and spent retry,
   correction and resolution allowances. A renamed config or run cannot
   replenish them. Preserve four implementation attempts and 64 native
   launches, with no automatic attempt 4, second re-entry, new-run budget or
   refund. Attempt-3 work non-PASS parks; host uncertainty retains existing
   non-parking behavior. Negative controls reject stale/mismatched terminal,
   authority, claim or publication, attempts 1/2/4, and fresh-run reset.
4. **Fresh native attempt.** Create a distinct native source workspace from
   the newly selected, pinned current-main base and repaired brief, not cycle
   11's selection pin, rejected head, old workspace or unaccepted marked seed.
   Enter the ordinary path at attempt creation with `attemptBase = selected.base`;
   do not run old attempt-1/2 projections, write their records, rebase from the
   rejected candidate or seed the prompt with its prescribed findings. Its local
   branch follows `codex/run-<sha256(run)>/iss-146-attempt-3`; publication
   uses `codex/iss-146-attempt-3` and a new PR. Give the author old work only
   as read-only evidence, never imported PASS/gate authority or in-place
   repair. Keep ordinary fresh source review, refresh DELTA where needed,
   final-head gates and publication identity checks.
5. **Prior publication untouched.** Explicitly leave PR #673 OPEN/DRAFT on
   `codex/iss-146-attempt-2` at
   `e3b1506a98069dfe874b9b319effb8099833f009`, with no mutation. The
   successor does not publish to it, move its branch, reuse its worktree,
   mark it ready, close it, rerun its CI or borrow reviews. Document the
   mechanism's rule for leaving prior publications untouched and creating a
   distinct new publication in `docs/loop.md`, not a standing #673 disposition;
   any later closure needs a separate host disposition.
6. **Production-path red/green.** A synthetic-ID fixture of the recorded
   persisted shape runs the real production composition path red-before
   (`integration-continuation-required`) and green-after (only attempt 3).
   Cover in-process and restarted replay, interruption at the one native
   admission, full retained accounting, stale/mismatched terminal authority,
   attempts 1/2/4, fresh-run reset, failed-successor replay, and no second
   publication or launch. Vary each pinned field alone: authority author, exact
   body digest, CLAIM byte digest, terminal history digest, PR OPEN/DRAFT state
   and head/ref; each mismatch refuses before reservation. A refused unavailable
   admission stop followed by retry with the unchanged declaration and authority
   admits once; an intervening ISS-146 attempt/admission record instead refuses
   as stale. Reject combination with each other admission packet. Assert
   old-record bytes stay unchanged. Keep
   non-governing fixture inputs fixed for each fail-closed negative control
   and make named body/author bypass mutants fail their controls. Label every
   synthetic identity synthetic; never alter real external-authority facts in
   a fixture.
7. **Author-local qualification.** The author supplies the red-before and
   green-after production-path outputs, focused queue/continuation and caller
   lifecycle regressions, and `docs/loop.md` text distinguishing this
   admission from ISS-167/200, acceptedReplan 4-to-5 and
   `gateStopAuthorization`. Run the manifest-resolved `pnpm typecheck`,
   `pnpm format:check`, `pnpm planning:check` and `pnpm test` with the
   required heavy admission. No mechanism worker installs or resumes a live
   executor; independent exact-head review and final-head hosted gates are
   separate delivery qualification below.

## Scope and authority

Expected implementation footprint: `scripts/dogfood/queue.ts` and/or
`scripts/dogfood/continuation.ts`, their production composition caller,
focused `test/dogfood/*` lifecycle cases and `docs/loop.md`. No generic
renewal framework, selector bypass, retry-policy change, broad state
migration, Windows repair, controller takeover, ceiling change or mutation
of CLAIM/old stops/reservations/configs/attempts/worktrees/receipts. Ordinary
successor state may be written in its own new directory/record; no historical
record is repinned. This issue grants no new product scope or #673 action.

The host-supplied, reviewed declaration is one optional closed
`terminalAttemptAdmission` config object, separate from the unchanged
`integrationContinuation`, `acceptedReplan` and `gateStopAuthorization` packets;
`validateLoopConfig` rejects coexistence with any of those three. Its proposed
exact fields are `schemaVersion: "dogfood-terminal-attempt-admission/v1"`,
`repository: "todd-skelton/orchestration-platform"`, `issueKey: "ISS-146"`,
`issueUrl: "https://github.com/todd-skelton/orchestration-platform/issues/457"`,
`run: "m1-iss146-147-20260914T2325"`, `priorAbsoluteAttempt: 2`,
`nextAbsoluteAttempt: 3`, `terminalMarker:
"loop-stop:m1-iss146-147-20260914T2325:11:3"`, `terminalReceiptUrl:
"https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5846435730"`,
`claim: "integration-continuation-23709074995ed14aaffb5cda4d8dc3adbaf167be4ac8512a7f9456e9d00d3a5b"`,
`claimSha256: "9454103dc2741a5a6cf2172d71676cb21dcea6d9f80a3e468c0ef4e2aa3bfc9a"`
(SHA-256 of the exact CLAIM record bytes),
`terminalHistoryDigest: "9b168dc33e82d3cdaa52f8e2040e23851bc47d8c4f17790976f0930685aebac0"`
(`queueDigest` of the completed cycle-11-stop-3 26-entry history),
`priorPublication: {number: 673, url:
"https://github.com/todd-skelton/orchestration-platform/pull/673", sourceBranch:
"codex/iss-146-attempt-2", head:
"e3b1506a98069dfe874b9b319effb8099833f009", state: "OPEN",
isDraft: true}`, `authorityUrl:
"https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5846783657"`,
`authorityAuthor: "todd-skelton"`, and
`authorityBodySha256: "030c9a950622e70cb5bdaa7a3a51eec7c62aebbf1c16570bb386af9ec52dcdce"`
(SHA-256 of the exact 26895-byte comment body observed created and updated
`2026-09-26T13:48:24Z`).
Do not accept a URL's shape, an old cached observation or a label as approval.
At the exact admission boundary, after safe installation and current selection
and immediately before the one reservation/setup/launch, reobserve the
completed stop and receipt, full run history, claim, PR identity and actual
authority comment (ID, URL, author, complete body and observed UTC instant).
Require exact equality of comment ID/URL and author, SHA-256 of its exact body
bytes, CLAIM bytes, `queueDigest` of the terminal history, and publication
number/URL/source head ref and oid/OPEN/DRAFT state to the declared fields;
closed, merged, ready or moved #673 refuses. Require the completed terminal
marker/receipt, repository, issue, run and attempts 2-to-3 to agree as well.
"Latest completed terminal" means the latest completed ISS-146
implementation/integration stop, with no later ISS-146 attempt directory,
successor admission record or reservation. A pre-admission refusal at attempts
2 writes its own stop marker without a history charge and does not supersede
that terminal; an intervening attempt or admission record makes the declaration
stale. `terminal-attempt-admission-authority-unavailable` is a run-scope,
non-parking retryable refusal for unavailable external observation;
`terminal-attempt-admission-mismatch` is an item-scope parking refusal for a
contradictory or stale identity/content. Both refuse before reservation, setup
or launch, retain old artifact bytes, and never consume the admission. Bind
only matching observations and the declaration to a write-once successor
admission record outside the old artifacts; replay uses that saved binding,
not a new authority spend. This defines the host config for review, not an
instruction for this planning worker to install it.

## Defect-class constraints

- `state-machine-steady-state-missing`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint`: enumerate
  no-grant, admission-refused-then-retried, admission-interrupted,
  successor-in-flight, successor-failed and successor-replayed states through
  both composition and production caller;
  retained state must be steady on the next ordinary invocation, with no
  duplicate worker, publication or charge. Old integration remains terminal.
- `missing-fail-closed-regression-evidence`,
  `mandated-external-assertion-contradicts-authority`: hold all other inputs
  valid while varying each pinned authority/terminal/claim/publication field,
  kill the corresponding bypass mutant, probe real external facts at admission,
  and use visibly synthetic identities in offline tests.
- `presence-evidence-treated-as-completeness`,
  `stale-review-bound-to-wrong-head`: claim/stop/PR presence is not completed
  terminal or current authorization. Check the full persisted state and exact
  successor head/review before delivery; old PASS never grants attempt 3.
- `future-capability-documented-as-current`,
  `container-controller-installed-before-review-terminal`: docs distinguish
  planned admission from shipped behavior until landing; installation waits
  for terminal independent exact-head PASS and final-head hosted green.
- `parallel-node-verifiers-exhaust-host-memory`, `format-check-not-run`,
  `unrunnable-verification-command`: use the host-wide admission wrapper for
  full/heavy gates, run the actual manifest scripts, never raise timeouts or
  skip tests. A narrower passing subset does not replace the complete gate.

## Delivery qualification and host sequence

C1-C8 of the FINAL decision constrain the handoff, not author-local success:

- **C1-C2:** The host recorded the delegated ruling on #4388 and must correct
  the earlier bare-attempt-3 route on platform #368; retain older
  comments/terminals. After COMPLETE planning
  acceptance, publish/register the real ISS-215 issue in GitHub milestone 8,
  mirror the exact main draft/project, and establish both directions of the
  native ISS-215 Blocks #457 relationship. ISS-214/#675 is CLOSED. The
  exceptional planning writer is registered; M1, not the host, implements.
- **C3-C4:** #457 remains OPEN/unready until mechanism proof, qualified
  installation and coherent repaired ISS-146 readiness packet. No resume,
  bare ready flip, claim removal, count edit, current-run repin or fresh-run
  escape. Admit only same-run attempt 3 with all 26 participants and historical
  charges/allowances; no automatic attempt 4.
- **C5-C6:** Preserve PR #673 open/draft and unchanged. A separate COMPLETE
  semantic ISS-146 replan, not inherited DELTA from the BLOCK_REPLAN
  predecessor, must bind the latest terminal, this landed mechanism and the
  delegated authority, apply F2/F3/N1-N4, mirror exact main draft/live issue,
  and receive a planning-repair receipt bound to that body. The two Windows
  failure classes and their capture/remedy belong to ISS-146 attempt 3.
- **C7-C8:** Independent exact-head review, all required final-head local and
  hosted jobs green, and a clean compatible executor precede host-only
  installation. Host rechecks no live/uncertain supervisor or worker and uses
  only canonical Windows `scripts/executor/start-loop.ps1` for a subsequent
  same-run start; never displace another loop. Keep product capacity and shared
  verifier admission. No ownership transfer, pilot, external-approval waiver,
  PR #673 mutation, runtime hand edit, timeout increase or test skip. New
  execution evidence is observed at its exact head, not inferred here.

## Review packet seed

Review both claim and reviewed-exhausted guards (`queue.ts:770-776,796-799`),
the transition seam, all production callers, admission
interruption/replay, failed-successor steady state, full history accounting,
fresh base/brief, distinct publication and every fail-closed negative. The
fixture must fail on current `origin/main` for the claimed guard and pass only
with the authorized mechanism; a mocked helper alone is insufficient. Recheck
the external authority probe at the lifecycle moment rather than trusting
this planning observation. The narrower issue does not repair ISS-146 Windows
failure or certify host installation.

### Intent surfaces

<!-- prettier-ignore-start -->
| Acceptance criterion | Exercised surface |
| --- | --- |
| 1-2 | Native composition guard, exact authority and one successor record |
| 3 | Full-run history, charges, failure and restart |
| 4-5 | New source/base/brief, source branch and independent publication |
| 6 | Production caller red/green fixture, interruption and negative controls |
| 7 | Docs, local gates and evidence handoff |

### Simplest shape

One exact native 2-to-3 admission record entering the ordinary path at attempt
creation with `attemptBase = selected.base`, no old-record projections or
rejected-candidate prompt seed; no generic renewal or second integration
allowance.

### Not built

| Not built | Reason |
| --- | --- |
| Windows gate repair | ISS-146 attempt 3 owns its two-class diagnosis and fix. |
| Generic renewal or fresh-run migration | Would reset or broaden the recorded lineage. |
| Selector bypass or controller takeover | ISS-215 becomes selectable through normal M2 readiness and closed ISS-214. |
| PR #673 disposition action | The ruling explicitly leaves it open/draft, untouched. |

### UI states and design-system sources

none - no UI surface changes.

### Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| One admission | Same run, exact stop/claim/PR, next attempt 3, existing 4/64 ceilings | Existing native records and one successor reservation | One authority/lineage observation at admission; replay reads the saved binding, no polling |

### Contract compatibility

| Changed contract | Compatibility posture | Removed path |
| --- | --- | --- |
| Optional terminal attempt admission | Omission keeps current claim refusal; old packets and records unchanged | None |
<!-- prettier-ignore-end -->

### Glossary impact

`terminalAttemptAdmission` is a proposed host-facing declaration for this
bounded successor only, not a general renewal or a shipped current feature.

### Tier + routing hint

Full native lifecycle/system, ordinary self routing from the installed
executor and retained failure/rung history; no reset or model override in this
planning draft. Independent reviewer probes the exact production path.
