---
key: ISS-054
title: "Implement repository-protection receipt and verifier-anchor contracts"
labels: ["type:slice", "area:contracts", "priority:p1", "risk:authority"]
milestone: "Minimum orchestration kernel"
parent: EPIC-RUNTIME
blocked_by: [ISS-002]
---

## Context and evidence

Pressure Round 21 assigned `repository-protection-receipt/v1` and
`bootstrap-verifier-anchor/v1` schemas and parsers to ISS-002 so ISS-036 could
produce durable protection evidence for ISS-038. Live reconciliation after
ISS-002 closed found no schema, parser, registry entry, serializer, digest, or
executable contract vector for either family. The nearby implemented
`github-conformance-protection-snapshot/v1` is advisory conformance evidence and
cannot substitute for the pre-N0 authority boundary.

## Scope fence

In: exact closed schemas for the repository-protection receipt and verifier
anchor; total parsing; canonical bytes and domain-separated identities;
compatibility and public-schema registration; pure relations for expiry,
producer, ruleset, workflow, asset, and anchor binding; and exhaustive closed-
snapshot and single-field mutation vectors.

Out: GitHub API access, ruleset or environment mutation, artifact upload or
download, CLI acquisition, Sigstore/TUF verification, filesystem persistence,
credential use, receipt production, policy selection, installation, release,
or any authority grant. ISS-036 remains the first producer and ISS-038 the first
authority consumer.

## Contract boundary

`repository-protection-receipt/v1` binds repository and ruleset numeric
identities, the ruleset semantic digest and protected path/reviewer/actor
policy, exact synthetic workflow paths/refs/digests/triggers/permissions,
complete API route/page/ETag observations, producer workflow/run/attempt and
intended artifact name, verifier-anchor digest, issue and expiry times with a
maximum seven-day interval, and closed disposition. It excludes its enclosing
artifact ID and archive digest because those values exist only after upload;
ISS-036 transports them separately through protected environment values.

`bootstrap-verifier-anchor/v1` binds GitHub CLI 2.93.0, exact official asset and
checksum identities for Windows, macOS, and Linux, repository numeric identity,
expected OIDC issuer, allowed signer workflow identity, creation time, and
operator-confirmation claim. It accepts no custom trust root, candidate digest,
kit digest, local credential, mutable latest-version selector, or executable
path.

Parser success proves only structural and identity consistency over supplied
records. It never proves API freshness, protection existence, operator action,
artifact authenticity, public attestation verification, or effective authority.

## Acceptance criteria and evidence

1. Both families have one literal closed field census, total parser, canonical
   serializer route, framed identity, schema registry entry, compatibility
   entry, and public export. Evidence: exact canonical goldens and registry/
   surface census.
2. Numeric IDs, hashes, versions, paths, refs, timestamps, pagination, ETags,
   permissions, triggers, workflow identity, asset identity, disposition, and
   seven-day expiry constraints refuse independently. Evidence: one-axis
   mutation matrix plus boundary-time vectors.
3. Protection receipt policy and observation arrays are dense, bounded,
   canonical, duplicate-free, and reflectively closed before semantic reads.
   Evidence: mutable/sealed/frozen positives and proxy, cross-realm/subclass,
   holes, symbols, extras, accessors, custom iterators, and descriptor traps.
4. The receipt cannot contain post-upload artifact identity or archive digest,
   and the anchor cannot contain candidate-controlled trust or executable
   fields. Evidence: extra/renamed/substitution refusal vectors and public type
   census.
5. Pure supplied-record relations bind the receipt to one parsed anchor and
   exact synthetic workflow/ruleset observations without performing lookup or
   granting authority. Evidence: digest, identity, time, path, permission, and
   producer substitution vectors.

## Verification plan

`pnpm exec vitest run test/contracts/repository-protection.test.ts`

`pnpm exec vitest run test/contracts/verifier-anchor.test.ts`

`pnpm exec tsc --noEmit`

## Predicted footprint and dependencies

One or two focused contract source files, public export/registry/compatibility/
serializer wiring, focused contract tests, and the inseparable ISS-036 edge and
ownership reconciliation. No root script, package, dependency, workflow,
provider, adapter, probe, filesystem, credential, or runtime change.

Directly blocked by closed ISS-002 contract primitives. Blocks ISS-036, which
produces the first live records, and transitively ISS-038, which consumes their
selected outcome.

## Operator actions

None.

## Glossary impact

None; repository protection, verifier anchor, receipt, authority, and
observation are already registered platform terms.

## Independent review packet seed

Attack advisory protection evidence presented as authority, partial pagination,
mutable latest selectors, artifact self-reference, candidate-controlled trust,
custom roots, absent OS asset identities, moved workflow refs, widened
permissions, stale/long expiry, numeric/string ID confusion, array reflection,
serializer/registry omissions, and parser success presented as live protection
or operator authorization.
