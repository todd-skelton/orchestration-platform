---
key: ISS-054
title: "Implement repository-protection receipt and verifier-anchor contracts"
labels: ["type:slice", "area:contracts", "priority:p1", "risk:authority"]
milestone: "Minimum orchestration kernel"
parent: EPIC-RUNTIME
blocked_by: [ISS-002]
---

## Context and evidence

Pressure Round 21 assigned `repository-protection-receipt/v1` and
`bootstrap-verifier-anchor/v1` schemas and parsers to ISS-002 so ISS-036 could
produce durable protection evidence for ISS-038. Live reconciliation after
ISS-002 closed found no schema, parser, registry entry, serializer, digest, or
executable contract vector for either family. The nearby implemented
`github-conformance-protection-snapshot/v1` is advisory conformance evidence and
cannot substitute for the pre-N0 authority boundary.

## Scope fence

In: exact closed schemas for the repository-protection receipt and verifier
anchor; total parsing; canonical bytes and domain-separated identities;
compatibility and public-schema registration; pure relations for expiry,
producer, ruleset, workflow, asset, and anchor binding; and exhaustive closed-
snapshot and single-field mutation vectors.

Out: GitHub API access, ruleset or environment mutation, artifact upload or
download, CLI acquisition, Sigstore/TUF verification, filesystem persistence,
credential use, receipt production, policy selection, installation, release,
or any authority grant. ISS-036 remains the first producer and ISS-038 the first
authority consumer.

## Contract boundary

The literal `Pre-N0 repository-protection and verifier-anchor ledger` in
`docs/architecture/contract-decisions.md` is the sole implementation authority.
It closes every top-level and nested member, order, scalar grammar, enum,
array bound, uniqueness rule, nullability cell, time relation, digest domain,
semantic projection, exclusion, and pure binding relation for both families.

`repository-protection-receipt/v1` has exactly fourteen top-level members. It
binds 1-based safe-decimal repository/ruleset/producer/workflow identities; the
framed ruleset semantic digest; bounded protected path and workflow arrays;
complete seventeen-key versioned permission snapshots; closed REST/GraphQL
request identities, ordered page chains, and terminal pagination evidence; the
live REVIEW producer workflow/run/attempt/start and intended artifact name; its
distinct completed successful BUILD trigger-source run; the exact pre-run
protected-environment anchor binding; strict issued/expiry times no more than
seven days apart; and closed
`ACCEPTED|REJECTED|BLOCK_REPLAN` disposition. It excludes enclosing artifact
ID/archive identity because those exist only after upload.

`bootstrap-verifier-anchor/v1` has exactly ten top-level members. It binds
literal GitHub CLI `2.93.0`/release `v2.93.0`, exactly one official asset/
checksum/executable identity for each of Windows, macOS, and Linux, repository
numeric identity, literal OIDC issuer and default online Sigstore/TUF trust
mode, exact allowed signer workflow identity, creation time, and a closed
operator-confirmation claim. It accepts no custom trust root, candidate/kit
digest, local credential, mutable latest-version selector, or executable path.

The pure four-input binder compares parsed receipt/anchor values to one fresh
closed supplied observation, recomputes semantic/family/terminal identities,
requires `issuedAt <= evaluatedAt < expiresAt`, and binds `evaluatedAt` to a
fresh at-most-five-minute terminal read-back. It compares immutable request and
effective-semantics identities while independently validating fresh times,
ETags, Link/cursor chains, and terminal evidence; historical capture metadata
is never required to equal fresh metadata. Its return contains only detached
parsed values and digests.

The fresh anchor-variable source update must remain no earlier than anchor
creation, strictly before the historical REVIEW producer start, and no later
than fresh observation start. Historical and fresh ETags/update times need not
match, but a post-producer same-value rewrite or retarget-then-restore is stale
substitution and refuses by its moved source update time.

The binder also enforces
`confirmation <= anchor creation <= anchor-variable publication < REVIEW producer start <= receipt issue`.
The WORKFLOW_RUN historical observation binds the separate completed successful
BUILD run that triggered REVIEW. The producing REVIEW run has no conclusion or
completion member because it writes and uploads the receipt while still live;
its run ID must differ from the triggering BUILD run ID.

Parser success proves only structural and identity consistency over supplied
records. It never proves API freshness, protection existence, operator action,
artifact authenticity, public attestation verification, or effective authority.

Round 445 closes the remaining digest precision without changing either value
schema. Every derived identity now has an exact domain, ordered `FramePart`
recipe and closed canonical projection. Canonical record-key insertion order is
irrelevant; frame-part tag or order changes are protocol changes. GraphQL page
request identities recompute from request identity plus the nullable cursor.
The first REST page request identity recomputes from request identity plus null
position; later REST request identities are supplied leaves linked exactly from
the prior sole `NEXT` relation because the compact receipt carries no raw Link
target request bytes.

Request-material, response-body, raw-Link-header, and later REST-request hashes
are supplied opaque SHA-256 claims where their preimages are absent. Historical
PULL_REQUEST and PULL_REQUEST_REVIEWS reduced-value digests are also opaque
claims whose closed preimages remain in the ISS-036 authenticated archive.
Opacity does not prove provenance, completeness, or that a request was made or
followed. ISS-036/038 composition owns those checks. The pure parser checks
grammar and exact joins and recomputes request identity, the first/GraphQL page
identities, complete reduction, terminal pagination, WORKFLOW_RUN and all six
shared semantic reductions, ruleset semantics, family identities, and fresh
terminal evidence.

Independent review attempt 3 at
`b5f6f8d5112a30f8a4e58283e88d935938a01039` returned BLOCK: REST relation-row
tuple uniqueness still admitted duplicate relation labels and globally cyclic
request chains, and fresh variable update time was not bound before the
historical producer. Definition repair attempt 4 adds relation-name uniqueness,
exact optional FIRST/PREV/LAST index and boundary rules, mandatory nonterminal
NEXT, global page/NEXT uniqueness, and the fresh update chronology. It preserves
every Round 441/442 attempt and all digest, opaque-leaf, schema and binder-result
choices from the prior definition attempt.

## Acceptance criteria and evidence

1. Both families have one literal closed field census, total parser, canonical
   serializer route, framed identity, schema registry entry, compatibility
   entry, and public export. Evidence: exact canonical goldens and registry/
   surface census.
2. Numeric IDs, hashes, versions, paths, refs, timestamps, pagination, ETags,
   permissions, triggers, workflow identity, asset identity, disposition, and
   seven-day expiry constraints refuse independently. Evidence: one-axis
   mutation matrix plus boundary-time vectors.
3. Protection receipt policy and observation arrays are dense, bounded,
   canonical, duplicate-free, and reflectively closed before semantic reads.
   Evidence: mutable/sealed/frozen positives and proxy, cross-realm/subclass,
   holes, symbols, extras, accessors, custom iterators, and descriptor traps.
4. The receipt cannot contain post-upload artifact identity or archive digest,
   and the anchor cannot contain candidate-controlled trust or executable
   fields. Evidence: extra/renamed/substitution refusal vectors and public type
   census.
5. Pure supplied-record relations bind the receipt to one parsed anchor and
   exact synthetic workflow/ruleset observations without performing lookup or
   granting authority. Evidence: digest, identity, time, path, permission, and
   producer substitution vectors.
6. The receipt vectors pin the exact fourteen-member top level, every nested
   census, 1/64 path/request/page bounds, exactly two BUILD/REVIEW workflows,
   complete seventeen-row effective permission snapshots, REST version/method/
   Link chains, GraphQL document/variables/cursor chains, terminal reduction,
   BUILD-to-REVIEW source identity, pre-producer chronology, expiry at 1 ms and
   seven days, and refusal at zero/reversed/seven-days-plus-1-ms. Evidence:
   canonical goldens, one-axis matrices, and deletion mutants for each equality
   and chronological edge.
7. The anchor vectors pin the exact ten-member top level, three-row
   `LINUX|MACOS|WINDOWS` census, asset/checksum/executable identities, fixed CLI
   release/OIDC/trust/confirmation literals, signer equality, and comparison
   time. Evidence: canonical goldens and absent/duplicate/substituted OS,
   mutable-selector, custom-root, executable-path, and combined-substitution
   negatives.

## Verification plan

### Definition precision and source follow-on — Round 445

Definition acceptance requires independently calculated canonical and digest
goldens for request identity, first REST page identity, null/non-null GraphQL
cursor identities, complete reduction, both terminal-pagination branches, the
seven recomputable purpose reductions, ruleset semantics, fresh terminal
evidence and the receipt family. Frame-type, frame-order and projection-member
deletion/substitution mutants must change or refuse the relevant digest, while
record-key insertion-order variants retain identical canonical bytes.

Pagination vectors cover 1, 2 and 64 pages; refuse 0/65, skipped, duplicated or
reordered ordinals, duplicate REST relation names, incorrect or
boundary-forbidden FIRST/PREV/NEXT/LAST targets, globally repeated request or
NEXT-target digests, `A -> B -> A`, REST NEXT/target/next-row disagreement,
early or missing terminal cells, GraphQL cursor mismatch/repetition, and
terminal digests formed from a count. Positives cover omitted optional
FIRST/PREV/LAST on eligible pages and no invented links on one-page, first-page,
or final-page boundaries. Opaque-leaf vectors prove only grammar and join
behavior: mutate each leaf independently and require all enclosing recomputable
digests to move; no pure positive may assert authenticated response, Link
origin, reducer preimage or completeness. The binder vectors pin its exact
six-member frozen return census and delete or mutate every chronology,
equality, freshness, semantic and terminal-evidence relation, including fresh
update before anchor, equal to/after producer start, after observation start,
same-value rewrite, and retarget-then-restore with a moved update time.

After this definition packet lands and receives independent review at its exact
head, the smallest source follow-on is the standalone protection-receipt parser
and digest surface; fresh observation and the four-input binder remain a later
slice. Its footprint is exactly seven files: new
`packages/contracts/src/repository-protection.ts`, new
`test/contracts/repository-protection.test.ts`, existing contract `index.ts`
and `registry.ts`, existing
`packages/conformance/src/iss002-bundle-paths.mts`, this draft, and one additive
source-attempt pressure record. The source packet adds no schema, runtime,
package, dependency, workflow, provider, adapter, probe, filesystem,
credential, receipt production or authority operation.

### First implementation slice — standalone verifier anchor (Round 442)

The first source packet implements only `bootstrap-verifier-anchor/v1`, its
closed parser, canonical serialization/framed identity, public registration and
focused contract vectors. It does not implement the protection receipt or the
four-input cross-family binder and does not close this issue. The remaining
receipt and binder must consume this same parsed identity without treating its
structural asset/operator claims as authentication.

The source footprint is exactly one new contract module and one focused test,
existing contract index/registry wiring, this draft, and the Round 442 record.
The hosted census failure at source attempt 1 additionally requires the two
sorted source/test entries in the existing ISS-002 stable-bundle path census;
the revised source footprint is seven files and preserves the original parser
and tests unchanged.
No package, dependency, workflow, acquisition, provider, filesystem or authority
operation enters the packet. Local verification uses typecheck, planning/board,
format and diff checks; executable tests run only in hosted three-OS CI.

`pnpm exec vitest run test/contracts/repository-protection.test.ts`

`pnpm exec vitest run test/contracts/verifier-anchor.test.ts`

`pnpm exec tsc --noEmit`

### Packet A source — closed structural parsing (Round 451)

Decision #268's OWNER ruling and Decision #272's extension replace the earlier
seven-file source follow-on with two independently reviewed source packets.
Packet A is artifact attempt 5 of the cumulative ceiling of eight and starts
from protected main `c517ce0bbb7e93ddac4dd275a9c94ab0005bd453`, which carries
the independently reviewed Round 451 phase-gate plan.

Packet A adds only the package-private structure-only parse module
`packages/contracts/src/repository-protection-parse.ts` and one focused test.
It implements one typed parser for each of the twenty-five accepted structural
input kinds — the fifteen receipt-tree record kinds, the two standalone
public-helper record kinds `completeReductionPage` and `rulesetSemantics`, and
the eight helper-argument kinds — plus ten typed array parsers. Two internal
stages run in order: a closure/type stage that snapshots each root once with
the repository hostile-reflection primitive and refuses before constructing any
parent whose child failed, and a local structure stage over typed values only.

Packet A registers nothing. It does not edit `packages/contracts/src/index.ts`
or `registry.ts`, exports no public name, computes no digest, performs no
cross-record equality, chronology, pagination handoff, ordinal adjacency or
semantic projection, and leaves `repository-protection-receipt/v1` reporting
`schemaVersion:unsupported` through the public index. Those relations and the
whole public surface arrive together in Packet B, which also re-exports the
retained `RepositoryProtectionReceipt` type so the reviewed public surface is
unchanged.

Packet A acceptance evidence is the versioned test-local structural-input
census compared to the codepoint-sorted accepted schema field arrays, the
complete no-throw mutation matrix over every receipt-tree occurrence and every
standalone helper-parameter root, the five prior failure instances by name, the
accepted nullable positives, committed deletion mutants that each remove one
typed-child gate, hostile-reflection negatives with zero trap executions at
every root, scalar/local/structural census vectors, and detached deeply frozen
success on the canonical nine-purpose fixture plus one-, two- and
sixty-four-page REST and GraphQL observations.

Packet A's footprint is exactly five files: the new parse module, the new
`test/contracts/repository-protection.test.ts`, the existing
`packages/conformance/src/iss002-bundle-paths.mts` stable-bundle census, this
draft and the Round 451 record. A sixth file, any package, config or workflow
change, or any public export is footprint growth and parks the sub-slice.
Local evidence at the exact head is `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm planning:board-check` and `git diff --check`; no
local Vitest, native, probe, provider or administrative operation runs.
Executable contract evidence comes only from hosted Linux, macOS and Windows
bootstrap after independent exact-head review attempt 6 by a lane that did not
author Packet A. Packet B is artifact attempt 7 and may not begin until Packet
A lands on protected `main`. The author claims no review, no test execution and
no acceptance.

### Packet B source — relations, pagination and the public surface (Round 451)

Packet B is artifact attempt 7 of the cumulative ceiling of eight admitted by
Decision #272 and starts from protected main
`1a951ce6afdc9c59d0bc5b820d8c56a1cffc4b0f`, which carries the independently
reviewed Packet A (PR #280).

Packet B adds the new relations module
`packages/contracts/src/repository-protection.ts` and consumes only values
already parsed by the unchanged Packet A module. It contains no parser, no
fragment parser, no raw-object cast, no non-null assertion over raw input and
no caller-asserted typed fast path: every public entry point invokes its mapped
Packet A parser before any pure computation, and every relation receives only
parsed records, scalars and dense arrays and returns a typed issue list. Only a
zero-issue relation result produces public parser success.

The module exposes exactly the twelve previously reviewed public names —
`computeGitHubApiCompleteReductionDigest`, `computeGitHubApiPageRequestDigest`,
`computeGitHubApiRequestIdentityDigest`,
`computeGitHubApiTerminalPaginationDigest`,
`computeRepositoryProtectionReceiptDigest`,
`computeRepositoryProtectionRulesetSemanticDigest`,
`parseRepositoryProtectionContract`, `parseRepositoryProtectionReceipt`,
`repositoryProtectionPermissionNames`, `repositoryProtectionPurposes`,
`repositoryProtectionSchemaFields` and `repositoryProtectionSchemaVersions` —
plus the re-exported retained `RepositoryProtectionReceipt` type, so the
reviewed public type surface is unchanged. Generic parse, canonical-byte parse,
framed serialization, registry vocabulary, compatibility and package export are
added in the same packet, so no interval exists in which the public family
accepts a structure-only value.

The implemented relations are protected-path and workflow ordering and
uniqueness; the exact BUILD and REVIEW role census; the workflow role-to-event
pairing that no single trigger record can observe; distinct workflow
identities; REVIEW trigger source equality with the BUILD row; producer binding
to the REVIEW row; environment anchor equality; the complete
`variableUpdatedAt < producer.startedAt <= issuedAt` chronology together with
the observation and page windows; purpose and request-identity uniqueness; REST
ordinal adjacency with exact indexed Link targets, boundary rules, mandatory
nonterminal `NEXT`, terminal null `nextRequestDigest`, unique NEXT targets and
`A -> B -> A` refusal; GraphQL cursor acquisition, non-repetition and terminal
state; the triggering BUILD binding with distinct run identities; and the exact
request-identity, page-request, complete-reduction, terminal-pagination, seven
recomputable reduced-value, ruleset-semantic and family digest recipes.
Response bytes, the raw Link header, later REST page requests and the two
historical-only pull-request reductions remain explicitly opaque supplied
claims that the contract only encloses.

Packet B's footprint is exactly seven changed files: the new relations module,
the existing `packages/contracts/src/index.ts`,
`packages/contracts/src/registry.ts`,
`packages/conformance/src/iss002-bundle-paths.mts` and
`test/contracts/repository-protection.test.ts`, this draft and the Round 451
record. The Packet A parse module is read-only input and is unchanged. A second
test, another source module, a runtime, package or workflow change, or another
pressure record is footprint growth and parks the sub-slice.

Packet B acceptance evidence is the twelve-name public surface census with the
retained type re-export; the registry vocabulary, compatibility and
serialization census; the canonical-byte round trip with noncanonical and BOM
refusals; the archived independent canonical and framed digest goldens; every
pure relation and chronology join; one-, two- and sixty-four-page REST and
GraphQL chains with zero-row, sixty-five-row and cyclic refusals; opaque-leaf
enclosure; one exact-issue deletion mutant for every cross-record equality,
chronology, branch join, page handoff, terminal cell, projection member, frame
tag or order and opaque-leaf enclosure, including the role-to-event pairing;
positives for all three dispositions with only `ACCEPTED` eligible for the
later binder; and every Packet A vector retained unchanged. Local evidence at
the exact head is `pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`,
`pnpm planning:board-check` and `git diff --check`; no local Vitest, native,
probe, provider or administrative operation runs. Executable contract evidence
comes only from hosted Linux, macOS and Windows bootstrap after independent
exact-head review attempt 8 by a lane that did not author Packet B. The author
claims no review, no test execution and no acceptance.

### Packet C/D sub-slice — fresh observation, binder, and closure (Round 456)

Decision #282 Ruling 1 admits one new ISS-054 sub-slice covering both
remaining packets under one ordinary ceiling of eight: plan (1, this entry
and Round 456), independent semantic plan review (2, BLOCK_FIXABLE on
findings F1-F3), bounded same-file correction (3, this correction),
independent re-review (4, pending by a different lane), Packet C source (5),
C review (6), Packet D source (7), D review (8). Decision #272's standing
rule reserves one bounded same-file correction and one independent
re-review following a first plan-review BLOCK; that reserved budget is now
fully consumed by attempts 3 and 4. No roadmap or board change; ISS-036
keeps its single `blocked_by: [ISS-054]` edge. This entry is attempt 1; it
claims no review, no source and no acceptance.

**Scope: C then D then E.** Slice C (Packet C) adds closed typed parsing for
the fresh `observedProtection` read-back record — the ten-member root, the
six-purpose fresh observation array, and (if needed) the derived
`apiTerminal` row — reusing Packet A's row, environment-binding and
ruleset-semantics parsers verbatim. Slice D (Packet D) adds the pure
four-input binder (`receipt`, `anchor`, `observedProtection`, `evaluatedAt`):
the `repository-protection-terminal-evidence/v1` recipe, every cross-record
chronology and equality relation the ledger states at
`docs/architecture/contract-decisions.md:6877-6977`, and the six-member
detached frozen return. D waits for C to land on protected `main`. Slice E
is a closing acceptance-census round (two planning files, authored by a lane
that authored none of A through D) that maps each of this issue's seven
acceptance criteria and the ledger's negative corpus at 6979-7008 to a
landed vector before ISS-054 closes.

**Exact footprints (Decision #282 Ruling 1).** Packet C: exactly four files
— a delimited section appended to
`packages/contracts/src/repository-protection-parse.ts` (Packet A's parsers
unchanged); `test/contracts/repository-protection.test.ts`; this draft; and
`planning/pressure-tests/2026-09-05-round-456.md`. Packet D: exactly six
files — new `packages/contracts/src/repository-protection-binding.ts`; one
`export * from "./repository-protection-binding.js"` in
`packages/contracts/src/index.ts`; two sorted rows in
`packages/conformance/src/iss002-bundle-paths.mts`; new
`test/contracts/repository-protection-binding.test.ts`; this draft; and
Round 456. `packages/contracts/src/registry.ts` is not touched by either
packet: `observedProtection` has no `schemaVersion` member and registers no
family.

**Anchor-cast waiver (Ruling 2).** The verifier-anchor whole-tree cast at
`packages/contracts/src/verifier-anchor.ts:168` is waived for Packet D.
Every field D reads off the anchor is in fact validated before `ok: true`
(`repositoryId` at `:115`, `createdAt` at `:116`,
`signerWorkflow.repositoryId` at `:146-148`, `operatorConfirmation.confirmedAt`
at `:163-164`), and the tree is fully detached and frozen by
`snapshotJson`. Packet D consumes the landed anchor parser as-is and its
review packet states which fields it reads and where each is validated. If a
Packet D reviewer BLOCKs on the cast, a separate two-file repair slice with
its own review is the bounded follow-up, not footprint growth inside D.

**The straddle positive.** The ledger requires fresh page times within
`observedProtection.startedAt..completedAt` "rather than before receipt
issue" (ledger 6887-6888). Combined with `issuedAt <= evaluatedAt`
(6927) and `evaluatedAt == completedAt` (6891-6892), a fresh window that
starts slightly before `receipt.issuedAt` and ends at or after it is
admitted by the ledger. Slice D must not add an unstated
`observedProtection.startedAt >= receipt.issuedAt` relation, and its vectors
must include a positive pinning exactly this straddle so a later lane cannot
silently tighten it.

**Closure criteria C/D satisfy.** Of this issue's seven acceptance criteria,
criterion 5 ("pure supplied-record relations bind the receipt to one parsed
anchor and exact synthetic workflow/ruleset observations without lookup or
authority") is unsatisfiable without Slice D and is the single criterion C
and D exist to satisfy; criteria 1, 4, 6 and 7 are already satisfied by the
landed Packets A/B and the anchor and need nothing from C or D; criteria 2
and 3 are satisfied and then extended by C's fresh-record axes and the full
hostile-reflection corpus over the new roots. ISS-036 additionally needs
Slice D for the stale-substitution and post-producer-move negative controls
that only the binder can judge.

See `planning/pressure-tests/2026-09-05-round-456.md` for the full ledger
citation tables, the phase-gate analysis, the structural-gap discussion, and
the attempt and routing records.

### Packet C source — fresh observed-protection typed parsing (Round 456)

Packet C is artifact attempt 5 of the eight admitted by Decision #282
Ruling 1 and starts from protected main
`26d8f01bc7b8f66150cc444a16682d1799d9ceea`, which carries the landed,
independently reviewed Round 456 sub-slice plan (PR #283). Decision #272's
reserved correction budget was consumed by attempts 3 and 4, so a BLOCK, a
surviving counterexample or footprint growth at review parks the sub-slice.

Packet C appends one delimited fresh observed-protection section to the
package-private `packages/contracts/src/repository-protection-parse.ts` and
adds three package-private entry points and no public name:
`parseObservedProtectionStructure` for the ten-member fresh
`observedProtection` root (ledger 6879-6882), `parseFreshApiObservations`
for the ordered six-purpose fresh observation array (6884-6886), and
`parseFreshApiObservation`, which narrows Packet A's purpose-agnostic
`parseHistoricalApiObservation` to those six purposes and to a literal null
`triggeringBuild` by construction. Packet A's parsers, its exported
`repositoryProtectionSchemaFields` object and every landed Packet A and
Packet B vector are unchanged; the fresh field array and the six-purpose
census are module-private consts, because adding keys to the exported field
object would change a reviewed public value. `parseEnvironmentBinding`,
`parseProtectedPathPolicies`, `parseReviewPolicy` and `parseWorkflows` are
reused verbatim.

Local structure stays inside the one record: the ten-member census, the
ordered six-purpose census, literal null `triggeringBuild` on every fresh
row, `startedAt <= completedAt`, the 300,000 millisecond window (6891), and
every fresh page `observedAt` inside `startedAt..completedAt` (6887-6888).
`evaluatedAt` is a binder input, not a member of this record. Packet C
registers nothing, edits neither `packages/contracts/src/index.ts` nor
`registry.ts`, and needs no bundle-census row because both paths are already
registered. It computes no digest and performs no cross-record equality,
chronology join, purpose join, semantic projection or terminal-evidence
framing; `apiTerminals` is derived by the binder and has no parser here, and
the stale-substitution refusal (6941-6942, 7003-7005) is cross-record, so it
is carried as an explicit non-goal with a statement vector rather than left
as a silent omission.

Packet C acceptance evidence is the versioned structural-input census
extended to the two new record kinds with the landed Packet A census
re-asserted unchanged; a canonical six-purpose fixture carrying one GraphQL
and five REST observations; one-, two- and sixty-four-page REST and GraphQL
page arrays with their empty and sixty-five-row refusals; the accepted fresh
nullable positives; one-axis scalar mutants with exact issue codes on every
fresh root scalar; the missing, unknown, renamed and `schemaVersion`-bearing
root refusals with a reordered top level held as a positive; the
nine-purpose, skipped, duplicated, reordered and historical-only purpose
refusals; a non-null `triggeringBuild` refusal on each of the six rows;
freshness mutants at 300,000, 300,001 and 0 milliseconds and reversed;
record-local page-window mutants before, after and at both boundaries; the
opaque-leaf boundary vector; the no-throw mutation matrix and the full
hostile-reflection corpus with zero trap executions over twenty-five new
fresh roots and paths; seven committed deletion mutants, one per new
typed-child gate, each shown to make the committed mutant survive; and the
assertion that the public surface is still exactly the reviewed twelve
Packet B names plus the retained `RepositoryProtectionReceipt` type.

Packet C's footprint is exactly four files: the appended parse module, the
existing `test/contracts/repository-protection.test.ts`, this draft and
`planning/pressure-tests/2026-09-05-round-456.md`. A fifth file, a
bundle-census row, any package, config or workflow change, or any public
export is footprint growth and parks the sub-slice. Local evidence at the
exact head is `pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`,
`pnpm planning:board-check`, `git diff --check` and an empty
`git status --porcelain`. `pnpm planning:board-check` reports
`ISS-054 body does not match its source draft` until the controller host
re-mirrors board issue #255, alongside a pre-existing `ISS-048` mismatch
that reproduces unchanged with this draft's Packet C record stashed and is
not attributable to this packet. No local Vitest, test runner, shim, native,
probe, provider or administrative operation runs; local Windows cannot run
Vitest. Executable contract evidence comes only from hosted Linux, macOS and
Windows bootstrap after independent exact-head review attempt 6 by a lane
that authored neither the Round 456 plan nor Packet C. Packet D is artifact
attempt 7 and may not begin until Packet C lands on protected `main`.
Routing: exact reported model `claude-opus-5`, effort `high`, `row: 7`,
`placement: provisional`, budget intent at most $25, actual spend unknown,
no new artifact spend ceiling requested or inferred. The author claims no
review, no test execution and no acceptance.

### Packet D source — the pure four-input binder (Round 456)

Packet D is artifact attempt 1 of the ordinary eight-lane ceiling that
Decision #290 ruling 2 A gives it as its own fresh sub-slice, and starts from
protected main `10f1e31ab96aa271a0ae9b2330c27614001c49d6`, which carries
landed Packet A (PR #280), Packet B (PR #281), the Round 456 plan (PR #283)
and Packet C (PR #291). Attempt 2 is an independent exact-head review by a
lane that authored none of A through D; the Decision #272 standing rule
governs any BLOCK. Round 456 section 2.2 is the binding definition and
nothing here widens it.

Packet D adds one new module,
`packages/contracts/src/repository-protection-binding.ts`, holding the
`repository-protection-terminal-evidence/v1` recipe (ledger 6900-6917), the
pure four-input cross-family binder (6877-6878, 6926-6958) and the
six-member detached deeply frozen return (6960-6970). It exports one public
name, `bindRepositoryProtectionEvidence`, plus the two types
`RepositoryProtectionBinding` and `RepositoryProtectionBindingResult`. Round
456 section 3.4's recommendation is followed: no standalone terminal-evidence
helper accepting a supplied `apiTerminals` projection is exported, so
`apiTerminals` stays derived inside the binder and no `apiTerminal` parser
kind exists anywhere.

The entry point invokes the mapped parser of each of the four inputs before
any computation: Packet B's `parseRepositoryProtectionReceipt` for the
receipt, which itself invokes Packet A's closed structural parser first;
`parseBootstrapVerifierAnchor` for the anchor; Packet C's
`parseObservedProtectionStructure` for `observedProtection`; and a scalar
canonical-timestamp parser for `evaluatedAt`, which lives here because
`evaluatedAt` is a binder input and not a member of either record. Parse
issues are returned under the input's own path prefix and no relation runs
until all four inputs are typed. There is no raw cast, fragment parser,
non-null assertion over raw input, `any`, `catch` or caller-asserted typed
fast path in the module; relations take only parsed records, scalars and
dense arrays and return a typed issue list; only a zero-issue result
succeeds. The six shared rows are joined by purpose plus exact
`requestIdentityDigest` through a purpose-keyed lookup, never by array index.
No prose or JSDoc in the module describes the result as freshness,
authentication, completeness, currentness or authority.

Anchor-field provenance, required by Decision #282 ruling 2 A because that
ruling waives the `verifier-anchor.ts:168` whole-tree cast for Packet D:

| Anchor field Packet D reads | Where Packet D reads it | Validated before `ok: true` at |
|---|---|---|
| `repositoryId` | the four-way repository-identity agreement (6943-6944) | `verifier-anchor.ts:115` |
| `createdAt` | both anchor-creation chronology legs (6930-6931, 6934-6935) | `verifier-anchor.ts:116` |
| `operatorConfirmation.confirmedAt` | the head of the historical chronology (6930-6931) | `verifier-anchor.ts:163-164`, including `confirmedAt <= createdAt` |
| `signerWorkflow.repositoryId` | not read by the binder; it is the anchor-internal leg of the same four-way agreement | `verifier-anchor.ts:146-148`, including equality with the top-level ID |
| the whole record, as canonical bytes for `Danchor` | `framedDigest("bootstrap-verifier-anchor/v1", [frame.canonical(anchor)])` | every member: literals `:113-114`, `repositoryId` `:115`, `createdAt` `:116`, assets `:118-137`, signer `:139-149`, confirmation `:150-165` |

Detachment is sound independently of the cast: `snapshotClosedRecord`
recursively snapshots and freezes the whole tree and refuses proxies,
accessors, non-enumerables and traps at every depth
(`runtime.ts:102-113, 157-178`). Packet D changes no anchor byte.

Packet D acceptance evidence is one canonical positive over a receipt reusing
the landed nine-purpose shape, an anchor fixture and a six-purpose fresh
observation, with independently recomputed and pinned goldens for `Danchor`,
`Dprotection` and `terminalEvidenceDigest` derived from the ledger recipes
transcribed in the vector file rather than imported from the module; one-axis
mutants with exact issue codes for every relation of Round 456 section 1.3;
the refusal categories, including `REJECTED` and `BLOCK_REPLAN` receipts that
parse but may not bind, repository-ID disagreement at each of the four
positions, anchor-digest disagreement at each of the four legs, environment
and variable name disagreement, and a fresh array that is short, duplicated
or carries a historical-only purpose; the evaluation-window and 300,000
millisecond boundaries in both directions; the four historical comparisons of
6930-6931, each shown non-tautological against a transcribed scratch copy
with that one comparison deleted; the stale-substitution mutants refusing by
the moved update time with their exact code; the passing negative controls
for every over-binding axis of 6939-6941 and 6948-6950; the section 3.5
straddle positive; the opaque-leaf boundaries; the terminal-evidence mutants
including the frame-tag, frame-order and record-key-insertion-order vectors
and the non-circularity of the projection by construction; a nineteen-row
binder deletion-mutant table in which each mutant's complete issue array is
its gate's own codes, so deleting the gate would let the mutant survive; an
eight-row table naming every relation that is deliberately defence in depth
with the landed gate that actually discharges it; the no-throw and
hostile-reflection corpus at all four input positions with zero trap
executions; and the public census asserting that Packet B's twelve names are
unchanged and that the module adds exactly one public name.

Packet D's footprint is exactly six files: the new binding module, one added
export line in `packages/contracts/src/index.ts`, two sorted rows in
`packages/conformance/src/iss002-bundle-paths.mts`, the new
`test/contracts/repository-protection-binding.test.ts`, this draft and
`planning/pressure-tests/2026-09-05-round-456.md`. `registry.ts` is not
touched: `observedProtection` has no `schemaVersion` member (6879-6882) and
the binder introduces no schema family, so no vocabulary or compatibility row
exists for it. A seventh file, a registry change, any package, config or
workflow change, or any anchor change is footprint growth and parks the
sub-slice. Local evidence at the exact head is `pnpm typecheck`,
`pnpm format:check`, `pnpm planning:check`, `pnpm planning:board-check`,
`git diff --check` and an empty `git status --porcelain`;
`pnpm planning:board-check` reports `ISS-054 body does not match its source
draft` until the controller host re-mirrors board issue #255. No local
Vitest, test runner, shim, native, probe, provider or administrative
operation runs; local Windows cannot run Vitest. Executable contract evidence
comes only from hosted Linux, macOS and Windows bootstrap after the
independent exact-head review. Routing: exact reported model
`claude-opus-5`, effort `high`, `row: 7`, `placement: provisional`, budget
intent at most $25, actual spend unknown, no new artifact spend ceiling
requested or inferred. The author claims no review, no test execution and no
acceptance.
## Predicted footprint and dependencies

One or two focused contract source files; public export, schema registry,
compatibility, and serializer wiring; two focused contract tests; and the
inseparable ISS-036 edge and ownership reconciliation. No root script, package,
dependency, workflow, provider, adapter, probe, filesystem, credential, or
runtime change.

Directly blocked by closed ISS-002 contract primitives. Blocks ISS-036, which
produces the first live records, and transitively ISS-038, which consumes their
selected outcome.

## Operator actions

None.

## Glossary impact

None; repository protection, verifier anchor, receipt, authority, and
observation are already registered platform terms.

## Independent review packet seed

Attack advisory protection evidence presented as authority, partial pagination,
mutable latest selectors, artifact self-reference, candidate-controlled trust,
custom roots, absent OS asset identities, moved workflow refs, widened
permissions, stale/long expiry, numeric/string ID confusion, array reflection,
serializer/registry omissions, semantic-digest projection drift, incomplete or
outdated permission namespace, BUILD/REVIEW trigger inversion, request-method/
branch crossing, partial REST Link or GraphQL cursor traversal, caller-count
terminal claims, historical/fresh metadata equality, producer/workflow
mismatch, BUILD/REVIEW run aliasing, future-dated anchor setup, producer self-
completion, signer/repository mismatch, anchor/environment substitution, and
parser success presented as live protection or operator authorization.
