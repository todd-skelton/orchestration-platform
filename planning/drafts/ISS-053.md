---
key: ISS-053
title: "Implement adapter state export and import mapping hooks"
labels: ["type:slice", "area:integrations", "priority:p2", "risk:authority"]
milestone: "Self-hosting release"
parent: EPIC-ADOPTION
blocked_by: [ISS-020]
---

## Registration context and outcome

State export/import mapping and import-plan hooks have no installed-release or
bootstrap state consumer in ISS-013. Register implementation after `ISS-020`,
whose independently authorized N0 installation supplies exact release, state,
custody, destination, and genesis identities. This issue remains thin and
blocked until that concrete source exists.

## Scope, non-goals, and acceptance evidence

In: elaborate neutral export mapping, moved-export detection, import planning,
idempotent apply/read-back evidence, and two behaviorally distinct fixture
adapters using ISS-020's installed state. Out: live first-consumer import,
cutover, state mechanism implementation, raw credential/secret export, release
installation, rollback, and consumer policy.

Acceptance evidence must prove every exported identity maps exactly once,
moved/partial/foreign exports refuse, plans bind source and installed target,
replay creates no second import, and no secret or private absolute path enters
artifacts. Exact commands are due at reviewed elaboration.

## Verification plan

Registration is checked by the repository planning checker. Before `ISS-020` clears the edge,
a reviewed elaboration must name exact export, import, replay, redaction, and
consumer tests; planning success is not behavior evidence.

## Predicted footprint and review attack surface

Expected ownership is adapter-sdk export/import hooks and N0-backed neutral
fixtures/tests; concrete first-consumer code remains ISS-024. Review should
attack moved exports, duplicate/omitted mappings, source/target substitution,
partial apply, replay, secret leakage, and import facts granting state authority.

## Operator actions

None.
