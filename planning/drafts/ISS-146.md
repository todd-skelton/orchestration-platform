---
key: ISS-146
title: "Confine author scratch and expose complete delivery-gate failures"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: [ISS-145, ISS-160]
---

Unready planning re-entry proposal. No fresh ruling, terminal re-entry, attempt
grant, `ready` publication, executor install or preserved-run resume.
QUALITY_PROFILE: contract

## Why

Chase Sets #7766's reviewed JPEG corrective delivery failed `check:structure`
on two empty git-ignored author-scratch directories absent from its commit and
hosted CI. Its 500-character stop note hid the actual subcheck. Learning note:
https://github.com/chase-sets/chase-sets/issues/7766#issuecomment-5666590839;
Todd's ruling:
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5666535088.
This is a recurring loop defect, not a JPEG product change.

## Done when

- Author scratch/evidence stays in existing `author-temp` outside source, on
  initial, corrective and resumed launches; prove all three with fixtures.
- Delivery gates use the exact committed candidate tree or refuse source-tree
  contamination naming the ignored/untracked path, without deleting scratch
  or tracked work. Prove empty ignored scratch, clean committed tree, actual
  committed structure failure and nonempty contamination.
- Retain complete failing command, log and terminal, exposing artifact path
  in stop/recovery inputs, not only a 500-character excerpt. Prove full log
  and candidate/base/host/unknown attribution without weakening uncertainty.
- Prove resumed delivery/replay without another source author, implementation
  attempt or corrective cycle; old runtime/reviews/charges remain unchanged.
- Document the bounded native behavior and its limits; no #7766 restart or
  mutation of its preserved worktree/runtime is granted.

## Recovery context

Unpublished: no closed implementation PR or blocking source-review finding
IDs. Planning PR #619 F1 found a cwd-relative child-fixture write omitted by
a `cwd`-only inventory. Retained source review
`01a0ab80-0b08-7df2-979a-1839cba1e791` passed C only. Read-only salvage:
`codex/run-8206815a33822cf85ef7cf38c7489e0119b73ff7765e4a052e629fec938661bb/iss-146-attempt-2`
@ C `539a817b6dcee565fb734a191985a224a0c314b7`.
Integration M `7ad17d138806810ff1a5804e099147d65d2d98ca`, seed S
`dd0f4aeb8e1f9c006c722fb477e789950841a6fb` (parents C/M), failed author
`01a0c912-7f58-7962-87ee-1e068c347c12`, original claim
`integration-continuation-23709074995ed14aaffb5cda4d8dc3adbaf167be4ac8512a7f9456e9d00d3a5b.json`.
Completed cycle-10 terminal:
https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5776598110.
Read-only runtime: `/root/orchestration-m1/runtime/m1-iss146-147-20260914T2325`.
Never reuse S's failed partial work, old gates, historical PASS or spent grant
as new acceptance.

Current `origin/main` is `88caeeae11733b2b511f077a2b98ab9532464fcc`,
ISS-200 PR #633 merged 2026-09-23T13:08:39Z. ISS-199 already ships the K/U
census/resolver; ISS-200 ships optional `spentResolution` admission. Neither
ships unpublished C's outcome. M stays pinned in this packet, then later main
descending from M is merged, never substituted into S or rebased over S.
Absolute attempt **2 of 4**, native launch ceiling **64** unchanged. The
shared mechanical retry is unspent: at most three new launches (author,
DELTA, one shared retry) within remaining capacity. Non-PASS parks, never
automatically advances to attempt 3.

### changedSinceLastFailure
terminalReceipt: https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5776598110; comment ID 5776598110; lineage root m1-iss146-147-20260914T2325/ISS-146; issue #457; unpublished PR none; head 539a817b6dcee565fb734a191985a224a0c314b7; absolute attempt 2; stop loop-stop:m1-iss146-147-20260914T2325:10:1
changedFact: architectural - ISS-200 spentResolution shipped as PR https://github.com/todd-skelton/orchestration-platform/pull/633 merge 88caeeae11733b2b511f077a2b98ab9532464fcc; observed 2026-09-23T13:08:39Z
invalidationReason: The K-only grant failed when unmarked auto-merged preservation was necessary; the new bounded spent-resolution case retains the spent claim and C/M/S, admits a freshly ruled subset of U, and requires new DELTA review without another implementation attempt.
newCeiling: attempts=4; nonPass=PARK; authority=https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5691451762

The packet alone does not reopen terminal park. A planning-repair/v1 completion
receipt and a fresh independent complete-sweep semantic PASS must both bind
the exact current issue-body revision/hash, terminal receipt and immutable
C/M/S. They establish only material change, not attempt/scope authority.

## Decisions and operator actions

The seven-file/absolute-2 decision
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5691451762
retained 4/64 and non-PASS park. Its provider fixture isolation did not change
production probe order. The corrected four-K continuation
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5776393948
is spent by the failed author and terminal. ISS-199 D2
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5778928187
permits unready planning only. Standing Todd deadlock authority
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5729706822
and https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5729982369
does not renew a consumed grant or explicit ceiling.

**Proposed Todd ruling, NOT AUTHORITY (verbatim proposal):** Authorize exactly
one ISS-200 `spentResolution` for #457/ISS-146's completed cycle-10 stop,
original claim, C/M/S and failed author above, same run at absolute attempt 2,
unchanged attempt ceiling 4 and native launch ceiling 64. Retain every original
v1 claim field and its four `allowedPaths` byte-identically. Resolve only the
captured marked hunks in K:

- `docs/loop.md`: retain C's scratch/committed-gate contract and main's later
  loop/recovery policy; no lost main paragraphs or out-of-hunk bytes.
- `scripts/dogfood/dispatch-adapter.ts`: retain author-temp across initial,
  corrective and resumed launches and main's admitLaunch/probe order.
- `scripts/dogfood/queue.ts`: retain candidate gate-failure evidence and main's
  prior-attempt context, then correctiveEvidence once, without charge reset.
- `test/dogfood/flow.test.ts`: assert scratch guidance, substantive progress
  and final JSON-only verdict together without weakening either side.

Allow preservation edits only in seed-bound U paths
`scripts/dogfood/delivery-adapter.ts` and
`test/dogfood/delivery-adapter.test.ts`. In production retain C's disposable
exact-commit candidate gate and complete command/log/terminal, while retaining
main's `board-failure.log` on internal board-check failure and scoped-static
stale/missing attribution. Treat only executor-authored `Exit:`/`Cleanup:`
footer metadata after a valid VERIFY_STATIC_RUN block as outside its failing
diagnostic; arbitrary mixed diagnostics remain unknown. In the test path
adjust the bound candidate cwd, persistent drift and base/candidate isolation
assertions below; regress footer-safe attribution without relaxing failure
classification. No other U path, K outside-hunk edit, source repair, further
resolution, gate correction or attempt 3. One author/DELTA pair and at most
the remaining shared mechanical retry; non-PASS parks. A fresh Todd-authored
GitHub comment must rule these exact path/semantics strings before reservation.
Missing, stale, wrong-body or non-Todd authority refuses; this proposal is
not a ruling or host-admissible config.

Host later captures actual comment author/ID/URL/body and observed UTC instant
after safe ISS-200 installation, immediately before reservation. Recheck
terminal/claim/C/M/S then. Host owns issue publication, readiness, safe
executor install and canonical preserved-run resume; none is authorized here.
No credentials or operator action are needed for this planning-only edit.

## Integration precision

ISS-200 starts new worktrees at S and saves a new ISS-199 census before author
dispatch; the old pinned worker has no census. K is exactly the four native
`conflict.files` above, one marked hunk each, with outside-hunk bytes and line
endings fixed. U requires same-path mode-100644 nonbinary C/M/S blobs, three
distinct blob IDs; ordinary U eligibility is NOT permission. The two ruled
paths are a subset of eight observed U candidates. Changed U stays regular
text without markers; no add/delete/rename/mode/binary or unrelated edit.
Unchanged U is allowed. New head gets independent DELTA review of K, changed
U and direct callers, followed by fresh final-head local/after-mirror/hosted
gates and native landing. A clean merge, old PASS or a no-op at S is not PASS.

`delivery-adapter.ts`'s candidate command currently uses `config.worktree`
(`:942`); the C behavior executes at a disposable exact-commit cwd. Preserve
host classification for install/startup/log I/O/cleanup, candidate/base/unknown
discrimination, and persistent workspace drift. Internal `planning:board-check`
at `:948`-`:962` still reads the live board, has no candidate command cwd,
and retains its contamination refusal and main's `board-failure.log`.

In the newly ruled U test path, PR #619 F1's four sites against `77cbb9d4`
are `:370` host startup cwd, `:465` typecheck command cwd, `:727` scoped-static
cwd and `:439` drift inducer. First three must use disposable candidate cwd.
At `:439` the gate child has neither `resolve` nor `current`; interpolate
`` ${JSON.stringify(resolve(current.worktree, "feature.ts"))} `` into its
template at build time to target the persistent author tree. At `:482` and
`:753` retain `not.toBe(current.worktree)` AND add a base-vs-candidate cwd
inequality. Preserve exact-head discriminators, base-drift/host, all seven
other drift controls and `:749`'s cwd override. A candidate-side write in its
OWN disposable tree must not raise persistent `candidate-workspace-drift`.
Mutants using author cwd fail exact `:465`/`:727`; shared base/candidate cwd
fails `:482`/`:753`; drift fails before `:439` repair and passes after.

The failed author's full test was 9 failed, 1383 passed, 5 skipped: eight old
`:764` cwd expectations and `refresh.test.ts:947`'s missing board log. Keep
the latter assertion unchanged. The read-only scoped-static probe found that
C's `Exit:`/`Cleanup:` footer after valid main staleness output defeated
`staticArtifactDiagnostics` (`delivery-adapter.ts:67`-`:96`). A narrow footer
recognition in that same U production path preserves main's attribution; tests
in the same U test path must distinguish executor metadata from arbitrary
mixed runner output. If this instead requires general parser policy or a
third path, stop and file a separate fixed-scope ISS, not a widened packet.

## Scope fence

In: original five outcomes through separately ruled K and two-path U.
Non-goals: new recovery mechanism, provider change, JPEG product work,
consumed-grant replay, runtime patch, wider U, cap reset, timeout/skip waiver.

### Don't-rebuild pointers

- `scripts/dogfood/continuation.ts`
- `scripts/dogfood/conflict.ts`
- `scripts/dogfood/queue.ts`
- `scripts/dogfood/delivery-adapter.ts`
- `scripts/dogfood/refresh.ts`

## Defect constraints

- `missing-fail-closed-regression-evidence`: with all other inputs valid,
  separately vary absent/wrong fresh authority, claim/author/seed/stop, unruled
  U, K outside-hunk edits, stale review and terminal replay; bypass mutants
  must turn green to red on the named governing guard, not an unrelated one.
- `mutable-reference-breaks-candidate-identity`, `reviewed-plan-not-bound`,
  `stale-review-bound-to-wrong-head`: exact C/M/S, saved K/U blob identities,
  final-head execution/DELTA/hosted gates; never import historical PASS.
- `change-scope-classifier-false-negative`, `grammar-enumerated-by-hand-not-derived`:
  Git-derived K/U is eligibility, not permission; test both directions,
  unsupported text/mode and unruled U independently.
- `retained-artifact-schema-ratchet`, `retry-events-double-count-terminal-outcome`:
  old pinned workers stay K-only, old attempts/participants/charges remain;
  one reservation and no refund/double count on replay.
- `state-machine-steady-state-missing`, `validation-misses-sibling-seed-path`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint`,
  `steady-state-fast-path-skips-required-reconciliation`: direct/saved/closed
  queue and reservation/launch/commit/gate/publication/park interruptions;
  delivered/parked replay inert while notes/closure reconcile once.
- `presence-evidence-treated-as-completeness`,
  `canonical-artifact-directory-masks-missing-payload`: directory or bounded
  excerpt never substitutes for complete log, terminal and gate attribution.
- `review-probe-live-provider-credential-inheritance`: isolated synthetic
  provider fixtures; no inherited live credentials or changed probe order.
- `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`,
  `vitest-wall-clock-timeout-under-parallel-host-load`: no subset waiver;
  timeout is load-sensitive only after unchanged isolated timing collapse
  AND complete one-worker/file-serial gate. Never raise timeouts, edit product
  files to mask it or skip tests.
- `parallel-node-verifiers-exhaust-host-memory`,
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: full Windows gates only
  via foreground exclusive `invoke-heavy-verifier.ps1`, finish descendants,
  never steal another owner.
- `container-controller-installed-before-review-terminal`,
  `future-capability-documented-as-current`: ISS-199/200 shipped capability,
  not #457 authority; exact-head independent PASS and three-OS green before
  host-only safe install/resume.
- `format-check-not-run`, `unrunnable-verification-command`: execute actual
  manifest-resolved typecheck, format, planning and test gates at new head.

## Verification plan

Planning-only: `node scripts/planning/check.mjs` and brief lint; not an
implementation handoff. Later native author: `pnpm typecheck`,
`pnpm format:check`, `pnpm planning:check`, `pnpm test`; focused
`pnpm exec vitest run test/dogfood/adapter.test.ts test/dogfood/queue-adapter.test.ts test/dogfood/delivery-adapter.test.ts test/dogfood/refresh.test.ts`.
Full gates require foreground machine admission, one worker/file-serial,
unchanged timeout/skip policy; Windows host uses the named heavy verifier.
Independent exact-head DELTA and three-OS bootstrap remain mandatory.

## Footprint & chain

ISS-199 -> ISS-200 -> separately authorized #457, not replacement by an
adjacent mechanism. K: docs/loop.md, dispatch-adapter.ts, queue.ts,
flow.test.ts. U: delivery-adapter.ts and delivery-adapter.test.ts only.
Direct callers: dispatch initial/corrective/resumed; queue fresh/saved/closed,
refresh resolver/DELTA, original/refreshed/after-mirror gates, terminal/stop,
publication/landing. No new issue or decision filed by this planning lane.

## External authority probe & evidence timing

The 2026-09-22 terminal and 2026-09-23 ISS-200 merge above are captured
historical probes, not a fresh scope ruling. At real admission AFTER safe
install and BEFORE spent-resolution reservation, capture Todd-authored comment
ID/URL/exact body/UTC observation plus live terminal/claim/C/M/S/seed census.
Text matching alone is not approval. Missing/contradictory evidence refuses;
replay uses the saved capture without probing anew. No provider payload is an
acceptance input for this issue.

## Review packet seed

Attack K outside-hunk immutability, only-two-U permission, stale/absent ruling,
candidate-vs-base cwd, persistent drift, board log, static footer false
attribution, double launch and inert replay. Use retained cycle-10 stop,
failed-author trace, C/M/S census and discriminating mutants. PR #619 F1
explains the child-relative write; its prior pressure PASS does not bind this
new revision. Fresh independent complete sweep is required for re-entry.

### Intent surfaces

| Acceptance criterion | Exercised surface |
| --- | --- |
| Original 1 | Initial/corrective/resumed scratch |
| Original 2 | Committed-tree delivery gates |
| Original 3 | Full diagnostic/terminal attribution |
| Original 4 | Contamination and delivery replay |
| Original 5 | Loop documentation/operator boundary |

### Simplest shape

Preserve C through ISS-200's single native spent continuation and two ruled U paths; add no new controller mechanism.

### Not built

| Not built | Reason |
| --- | --- |
| New recovery mechanism | ISS-199/200 already ship the bounded path. |
| Wider footer parser | Only executor metadata from C's own gate is implicated. |

### UI states and design-system sources

none — no UI surface changes.

### Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Spent delivery | At most three new launches, remaining 64 ceiling | Exact C/M/S and saved K/U | Existing state/Git/gates; no polling |

### Contract compatibility

| Changed contract | Compatibility posture | Removed path |
| --- | --- | --- |
| Optional ISS-200 spent case | Old v1 claim byte-identical; old workers K-only | None |

### Glossary impact

none — no new or renamed public names.

### Tier + routing hint

Full path/system; existing self routing, no new placement policy.
