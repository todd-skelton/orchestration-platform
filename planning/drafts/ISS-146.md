---
key: ISS-146
title: "Confine author scratch and expose complete delivery-gate failures"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: [ISS-145, ISS-160]
---

Unready: recorded renewal exists; no repaired-body re-entry receipt, fresh
independent DELTA/readiness reconciliation or resume.
QUALITY_PROFILE: contract

## Why

#7766: reviewed delivery failed `check:structure` on empty ignored scratch
outside commit/CI; 500-character stop hid the subcheck (#7766 comment
5666590839; #4388 ruling 5666535088). Loop defect, not JPEG.

## Done when

- `author-temp` holds scratch/evidence outside source on initial, corrective
  and resumed launches; fixture all three.
- Gates use exact committed candidate or refuse named ignored/untracked
  contamination without deletion; test empty scratch, clean commit, real
  structure failure and nonempty contamination.
- Retain complete failing command/log/terminal and artifact path in stop and
  recovery, beyond excerpt; test candidate/base/host/unknown attribution.
- Replay without source author, attempt or correction; retain runtime,
  reviews and charges.
- Document bounds; no #7766 restart or preserved-state mutation.

## Recovery context

No closed PR/blocking source-review IDs. #619 F1: child-fixture
cwd write; review `01a0ab80-0b08-7df2-979a-1839cba1e791` passed C only.
Read-only salvage:
`codex/run-8206815a33822cf85ef7cf38c7489e0119b73ff7765e4a052e629fec938661bb/iss-146-attempt-2`
@ C `539a817b6dcee565fb734a191985a224a0c314b7`.
M `7ad17d138806810ff1a5804e099147d65d2d98ca`, S
`dd0f4aeb8e1f9c006c722fb477e789950841a6fb` (parents C/M), failed author
`01a0c912-7f58-7962-87ee-1e068c347c12`, claim
`integration-continuation-23709074995ed14aaffb5cda4d8dc3adbaf167be4ac8512a7f9456e9d00d3a5b.json`.
Cycle-10 stop: #457/5776598110; read-only runtime
`/root/orchestration-m1/runtime/m1-iss146-147-20260914T2325`.
No reuse: failed S work, old gates/PASS, spent grant.

Historical capability landing: `88caeeae11733b2b511f077a2b98ab9532464fcc`
(#633 merged 2026-09-23T13:08:39Z). Freshly observed `origin/main`:
`7b360fd402e815e92c6dbe696b028825646b2f79` (2026-09-25).
ISS-199 K/U and ISS-200 spent admission don't ship C's outcome. Pin M; merge
later main, not replace/rebase S. Attempt **2/4**,
launch ceiling **64**; max three launches including later-main DELTA.
Non-PASS parks, not attempt 3.

### changedSinceLastFailure

terminalReceipt: https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5776598110; comment ID 5776598110; lineage root m1-iss146-147-20260914T2325/ISS-146; issue #457; unpublished PR none; head 539a817b6dcee565fb734a191985a224a0c314b7; absolute attempt 2; stop loop-stop:m1-iss146-147-20260914T2325:10:1
changedFact: architectural - ISS-200 spentResolution shipped as PR https://github.com/todd-skelton/orchestration-platform/pull/633 merge 88caeeae11733b2b511f077a2b98ab9532464fcc; observed 2026-09-23T13:08:39Z
invalidationReason: The K-only grant failed when unmarked auto-merged preservation was necessary; the new bounded spent-resolution case retains the spent claim and C/M/S, admits a freshly ruled subset of U, and requires new DELTA review without another implementation attempt.
newCeiling: attempts=4; nonPass=PARK; authority=https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5838763854

Re-entry: a planning-repair/v1 completion receipt and fresh independent
complete-sweep semantic PASS must bind this repaired current issue-body
revision/hash, terminal receipt ID/URL and C/M/S; neither grants attempt
authority. Reconcile the exact live #457 body and board before readiness.

## Decisions and operator actions

#4388 historical rulings: 5691451762 retained absolute 2/4, 64, non-PASS park
and provider-fixture isolation without production probe change; 5776393948's
four-K grant is spent; ISS-199 D2 5778928187 permits unready planning only;
5729706822 and 5729982369 did not renew grant or ceiling. Under #4388
5838600629's delegated decision boundary, the independent
`astra-iss146-reentry-decision-r1` decision was recorded at
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5838763854
(comment ID 5838763854, 2026-09-25T20:03:01Z). This is a recorded delegated
renewal, not Todd's personal decision text or an attempt reset. It authorizes
exactly one ISS-200 `spentResolution` for this completed cycle-10 stop, original
claim, C/M/S and failed author above, in the same run at absolute attempt 2.
Ceilings remain 4 implementation attempts and 64 native launches; both
implementation attempts and all 23 observed historical participants remain
charged. It authorizes one preservation-author/independent-DELTA pair and only
the remaining shared mechanical retry: at most three additional launches
including any later-main DELTA, or two if already consumed. Neither attempt 3
nor attempt 4 is authorized; non-PASS/work failure returns to PARK. Retain every
original v1 claim field and its four `allowedPaths` byte-identically, including
the cycle-4 stop and old authority URL. Only the new `spentResolution` carries
the fresh authority URL/body. The following six normative rule strings remain
byte-identical to the landed brief. Each rule is one unwrapped line; its packet
`semantics` is exactly the text after its `path` and ": ". Resolve only captured
marked hunks in K:

- `docs/loop.md`: retain C's scratch/committed-gate contract and main's later loop/recovery policy; no lost main paragraphs or out-of-hunk bytes.
- `scripts/dogfood/dispatch-adapter.ts`: retain author-temp across initial, corrective and resumed launches and main's admitLaunch/probe order.
- `scripts/dogfood/queue.ts`: retain candidate gate-failure evidence and main's prior-attempt context, then correctiveEvidence once, without charge reset.
- `test/dogfood/flow.test.ts`: assert scratch guidance, substantive progress and final JSON-only verdict together without weakening either side.

Preservation edits only in seed-bound U:

- `scripts/dogfood/delivery-adapter.ts`: retain C's disposable exact-commit candidate gate, its drift refusal and complete command/log/terminal, plus main's `board-failure.log` on internal board-check failure and scoped-static stale/missing attribution; only executor-authored `Exit:`/`Cleanup:` footer lines after a valid VERIFY_STATIC_RUN block are outside its diagnostic; other mixed output stays unknown.
- `test/dogfood/delivery-adapter.test.ts`: only in the scoped-static attribution group, expect the disposable candidate cwd, keep base-control cwd isolation and regress footer-safe attribution without relaxing classification; C's typecheck gate group, including drift, stays unchanged.

No other U path, K outside-hunk edit, source repair, further resolution, gate
correction or attempt 3. One author/DELTA pair, at most three launches
including any later-main DELTA; non-PASS parks. Before reservation the host
re-observes the actual comment's author/ID/URL/complete body/UTC and checks
the six unwrapped strings against the packet. The existing native admission
requires author login `todd-skelton`, exact URL/ID and authorityBody equality;
the account identity does not mean Todd personally wrote the delegated ruling.
Missing, stale, wrong-body, wrong-author or unreadable authority refuses. Do
not forge a capture, edit the old ruling or weaken this guard. Host owns
publication, installation and resume; the controller only admits the capture.

## Integration precision

ISS-200 starts at S, saves census before dispatch; old workers stay K-only.
K: four native `conflict.files` hunks, outside bytes/line endings fixed.
U: same-path mode-100644 nonbinary C/M/S, three distinct blobs; eight eligible
but only two ruled, unchanged allowed. Changed U stays regular marker-free
text, no add/delete/rename/mode/binary. Exact-head DELTA reviews K/U/direct
callers; fresh local/after-mirror/hosted gates precede landing. Merge, old
PASS and S no-op are insufficient.

`delivery-adapter.ts:942` uses `config.worktree`; C uses disposable exact
commit. Preserve host install/startup/log I/O/cleanup, candidate/base/unknown
and persistent drift. Board check `:948`-`:962` reads live board, not
candidate cwd; retain contamination refusal and `board-failure.log`.

PR #619 F1 sites bind `77cbb9d4`, not S. At S test blob `806143ed`, C's
typecheck group (`:389`-`:536`) already asserts disposable cwd (`:445`,
`:503`-`:505`, `:522`) and disposable drift (`:402`/`:455`); leave it
unchanged. Change only `:764`-`:768` to disposable cwd; keep `:787`-`:793`
with `:789` override, adding base/candidate cwd inequality. Author-cwd,
shared-cwd and disposable-drift mutants fail at `:767`, `:793`, `:468`.

Failed author: 9 failed/1383 passed/5 skipped: eight `:764` cwd and
`refresh.test.ts:947` board log (retain assertion). C's `Exit:`/`Cleanup:`
footer defeated main's `staticArtifactDiagnostics` (`delivery-adapter.ts:67`-
`:96`); recognize only executor metadata, not mixed runner diagnostics.
General parser policy or third path requires separate ISS.

At freshly observed `origin/main` `7b360fd4`, the owning-symbol crosswalk for
the later-main merge is `delivery-adapter.ts:67` (`staticArtifactDiagnostics`),
`:942` (gate cwd) and `:954`-`:956` (board log). Current
`delivery-adapter.test.ts:799`-`:803` holds the scoped-static cwd expectation;
`:822`-`:828` holds base override/isolation. Current `refresh.test.ts:945`-
`:956`, specifically `:954`, holds the board-log assertion. These current-main
coordinates do not replace the S-specific sites above or authorize a
`refresh.test.ts` edit. Recheck this crosswalk if ISS-211 or another merge
moves the callers before publication; preserve later-main changes through the
native merge without expanding the seed-bound grant.

## Scope fence

Five outcomes: ruled K/two-path U.
Non-goals: new recovery, provider/JPEG work, grant replay, runtime patch, wider U,
cap reset or timeout waiver.
Shipped: `scripts/dogfood/{continuation,conflict,queue,refresh}.ts`;
main's `delivery-adapter.ts` board/static, not C's.

## Defect constraints

- `missing-fail-closed-regression-evidence`: independently vary authority,
  claim/author/seed/stop, unruled U, K outside-hunk, stale review and replay;
  valid controls and named-guard bypass mutants.
- `mutable-reference-breaks-candidate-identity`, `reviewed-plan-not-bound`,
  `stale-review-bound-to-wrong-head`: pin C/M/S, K/U blobs and final-head
  execution/DELTA/hosted evidence, not old PASS.
- `change-scope-classifier-false-negative`, `grammar-enumerated-by-hand-not-derived`:
  Git K/U eligibility is not permission; test both directions, text/mode,
  unruled U.
- `retained-artifact-schema-ratchet`, `retry-events-double-count-terminal-outcome`:
  old workers K-only; retain charges/participants, one reservation, no refund.
- `state-machine-steady-state-missing`, `validation-misses-sibling-seed-path`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint`,
  `steady-state-fast-path-skips-required-reconciliation`: direct/saved/closed
  queue and reservation-to-park interruptions; inert replay, notes reconcile.
- `presence-evidence-treated-as-completeness`,
  `canonical-artifact-directory-masks-missing-payload`: require complete log,
  terminal and attribution, not directory/excerpt.
- `review-probe-live-provider-credential-inheritance`: isolated synthetic
  provider fixture; no live credentials or probe-order change.
- `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`,
  `vitest-wall-clock-timeout-under-parallel-host-load`: no subset waiver;
  load-sensitive only after unchanged isolated collapse AND full serial gate;
  no timeout raise, product edit or skip.
- `parallel-node-verifiers-exhaust-host-memory`,
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: native M1 workers run the
  shipped foreground gates in their WSL worktree; Windows-hosted full gates
  and diagnostics use canonical exclusive `invoke-heavy-verifier.ps1` admission
  in the foreground. Finish; never steal the lock or waive admission.
- `container-controller-installed-before-review-terminal`,
  `future-capability-documented-as-current`: shipped ISS-199/200 is not #457
  authority; exact-head PASS/three-OS green precede host install.
- `format-check-not-run`, `unrunnable-verification-command`: run actual
  manifest-resolved typecheck, format, planning and test at new head.

## Verification plan

Planning: lint/check. Native M1 author: manifest-resolved `pnpm typecheck`,
`pnpm format:check`, `pnpm planning:check` and `pnpm test` foreground in its WSL
worktree under the shipped loop contract; focused adapter/queue/delivery/refresh
tests. Windows-hosted gates/diagnostics stay host-owned with canonical exclusive
admission, not a new Windows wrapper on the native author. No timeout/skip
changes. Fresh final-head/after-mirror gates, exact-head DELTA and three-OS
hosted bootstrap remain required.

## Footprint & chain

ISS-199 -> ISS-200 -> ruled #457, not adjacent replacement. Check K/U, launch
variants, queue/replay, DELTA, gates, stop, landing.

## External authority probe & evidence timing

Terminal/merge facts aren't rulings. After install/before reservation, re-read
the recorded delegated-renewal comment 5838763854 at the URL above and capture
its actual author/ID/URL/complete body/UTC with the live terminal/claim/C/M/S/
seed census. The host checks the actual account and immutable body, not an
assertion of personal authorship. Missing/contradictory evidence refuses;
replay uses the saved capture.

## Review packet seed

Sweep K/U, ruling, cwd/drift, C's typecheck group unchanged, board log, footer and replay against stop,
failed trace, C/M/S and mutants. #619 F1 PASS is stale.

### Intent surfaces

<!-- prettier-ignore-start -->
| Acceptance criterion | Exercised surface |
| --- | --- |
| 1 | Launch scratch |
| 2 | Committed gates |
| 3 | Full failure evidence |
| 4 | Replay |
| 5 | Operator boundary |

### Simplest shape

ISS-200's single spent continuation, two ruled U paths; no new mechanism.

### Not built

| Not built | Reason |
| --- | --- |
| Recovery/parser expansion | ISS-199/200 ship recovery; only C's executor footer is implicated. |

### UI states and design-system sources

none — no UI surface changes.

### Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Spent delivery | Three launches at most, ceiling 64 | C/M/S and K/U | Existing Git/gates; no polling |

### Contract compatibility

| Changed contract | Compatibility posture | Removed path |
| --- | --- | --- |
| ISS-200 spent case | v1 claim unchanged; old workers K-only | None |
<!-- prettier-ignore-end -->

### Glossary impact

none — no new or renamed public names.

### Tier + routing hint

Full path/system. Only NEW workers in the distinct spent-resolution directory
use installed current `SELF_ROUTING`: retained `authorFailures.count=2` selects
`claude-fable-5-1/high` author rung 2, and retained reviewer rung 0 selects
`claude-opus-5-5/high` (`queue.ts:1154`-`:1171`, `:1473`-`:1482`;
`routing.ts:16`-`:27` at `7b360fd4`). Retained participants, saved placements,
fingerprints and configs remain historical and charged; no predecessor model
evidence transfers. An incompatible saved configuration or unavailable actual
route stops admission, not a reason to rewrite retained files or restart workers.
