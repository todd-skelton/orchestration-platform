---
key: ISS-214
title: "Accept the refresh-bound review when an integration continuation re-enters delivery"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

ISS-146/#457 stopped as executor failure `unreviewed-delivery-source` at
`loop-stop:m1-iss146-147-20260914T2325:11:2`, after two implementation attempts,
not as failed product work. The [host diagnosis](https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5843715770)
and [FINAL independent decision](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5843781301)
govern this bounded repair (C1-C9). The decision is delegated under Todd's
rulings, not new personal words from Todd; the earlier
[renewal](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5838763854)
remains consumed. No new product requirement is added to #457.

At `origin/main` `353eda5f3d340a4d48003eb1cfb6f9780bba19cb`, the retained
`/root/orchestration-m1/runtime/m1-iss146-147-20260914T2325/iss-146-attempt-2/`
has immutable source PASS review `01a0ab80-0b08-7df2-979a-1839cba1e791`.
Its `integration/spent-resolution/attempt.json` is in `delivery` at head
`e3b1506a98069dfe874b9b319effb8099833f009` and refresh PASS review
`01a0dc32-9c90-7821-80c3-352e5d169945`. The saved `source/native-refresh.json`
selects `refresh-353eda5f3d340a4d48003eb1cfb6f9780bba19cb/`, where
`delivery-source.json`, `reviewer-terminal.json` and `publication.json` bind
that head/review and PR #673. The reservation `integration/spent-resolution.json`
has `launchLimit: 3`, initial history 23; participants 24-26 spent all three.
`cycle-11-stop-2.json` retains 26 participants and the exact stop marker.

`scripts/dogfood/queue.ts:3960-3981` verifies the original source, but then
requires its review to equal both the immutable integration grant and the
advanced accepted review. `:3102-3111` persists that different refresh review
on `observing-hosted-checks`; `:2661` requires a fresh integration review, while
`:1173-1225,3373-3379` preserve/enforce the spent reservation. The next
delivery invocation contradicts its own accepted refresh. Fixing that comparison
alone leaves no capacity for the one later-main DELTA review that the decision
allows.

Read-only salvage: OPEN DRAFT [PR #673](https://github.com/todd-skelton/orchestration-platform/pull/673),
branch `codex/iss-146-attempt-2` @ exact head
`e3b1506a98069dfe874b9b319effb8099833f009`. There is no closed defect
implementation PR or stopping review finding ID. Read its source/records only;
do not reuse the old candidate, PASS, gates, checks, publication or reservation
as acceptance of this repair or of a later refreshed head. Keep PR #673 open and
draft for the resumed loop's native reconciliation.

## Decision and don't-rebuild check

Separate immutable original-source provenance from current refresh authority,
then admit exactly one newly authorized reviewer-only refresh launch for this
recorded lineage without widening or rewriting its old reservation. Reuse the
native queue, `refresh.ts` selected directory and PASS parsing, and
`delivery-adapter.ts` source verification; no generic recovery or budget system.
ISS-142 already accepts the producer's `retryContext` in reviewed source records
but does not handle a changed review identity. ISS-167 supplies same-attempt
integration continuation; ISS-199 supplies the seed-bound K/U census and fences;
ISS-200 supplies the spent-resolution reservation and three-launch accounting.
ISS-172's G0 publication text and existing PR-body reconciliation remain intact.
None supplies post-publication refresh-review re-entry or this one additional
authority-bound launch. Adjacent lineage is not #457 completion.

## Done when

1. **AC1, real-path red/green and steady re-entry.** An offline, synthetic-ID
   regression constructs the recorded persisted shape, not a simplified helper:
   original immutable PASS source review; spent-resolution `delivery` attempt
   advanced to a different passing refresh review; `native-refresh` selecting
   that directory; matching `delivery-source`, reviewer terminal and publication;
   original 23 plus three charged reservation launches. Through the real queue
   delivery path, reach publication/`observing-hosted-checks`, persist the
   advanced attempt, then re-enter delivery in-process and after process-restart
   reconstruction. First demonstrate `unreviewed-delivery-source` on baseline
   `353eda5f3d340a4d48003eb1cfb6f9780bba19cb`; after repair, observe the
   same publication with unchanged main and no duplicate author, reviewer,
   publication or charge. Cover ordinary integration continuation and its
   spent-resolution composition wherever they share this predicate.
2. **AC2, two independently checked identities.** Keep validating the original
   candidate, source terminal, original PASS review and integration grant against
   the immutable source. Check the accepted head/review against the refresh
   directory selected by `native-refresh`, its delivery-source/native PASS
   evidence and participant history; a correctly bound post-publication refresh
   review need not equal the original review. Do not trust only `attempt.reviewId`,
   publication or an arbitrary saved delivery-source. With otherwise valid
   fixtures, independently reject missing/non-PASS original or refresh review,
   wrong original/current head or review ID, and a directory or lineage that
   fails to bind the accepted source. Preserve existing current-main, exact-head
   gates, independent review and publication reconciliation.
3. **AC3, one bounded reviewer admission.** Accept the existing saved records
   unchanged: no migration, hand edit, deletion, new reservation, historical
   fingerprint or original claim change. In particular, retain the old
   `spent-resolution.json` packet, initialHistory and `launchLimit: 3`. The
   native admission for exactly one *additional refresh DELTA reviewer launch*
   must bind the recorded authority URL/comment ID below to run
   `m1-iss146-147-20260914T2325`, ISS-146/#457, absolute attempt 2, the
   spent-resolution reservation and current stop/publication lineage. Capture
   the authoritative comment at admission immediately before granting that
   launch, after safe installation and against then-current persisted lineage;
   URL syntax, a prior observation or an old grant is not approval. A new
   grant-consumption record and ordinary state advancement are allowed, not
   rewritten history. Absent, unreadable, stale or mismatched authority retains
   the exhausted refusal. Valid authority admits only that reviewer; author,
   source/gate repair, another implementation attempt, second launch, a worker
   retry that launches again, changed run/claim/stop/publication, and repeated or
   restarted consumption cannot gain capacity. Preserve 26 charged participants
   including launches 24-26, implementation ceiling 4 with 2 consumed, native
   run ceiling 64, and the shared retry's prior usage. No fresh-run-name escape.
4. **AC4, qualification of the bounded change.** Focused queue and adapter
   tests exercise admission, ordinary and spent compositions, delivery/replay,
   each fail-closed control with unrelated fixture inputs fixed, and actual
   charge/write counts. Use synthetic identities unmistakably distinct from the
   real run, head, review, stop and URL. A targeted comparison/allowance mutant
   must fail its named regression rather than an earlier fixture refusal. Update
   `docs/loop.md` with the recorded blocker, two review identities and the
   one-use allowance only when shipped. Run `pnpm typecheck`,
   `pnpm format:check`, `pnpm planning:check` and `pnpm test` in the native author
   worktree without timeout, skip or test weakening. Windows full gates use the
   specified exclusive heavy-verifier wrapper. These are author-local
   deliverables, not a claim that #457 resumed, landed or deployed.

## Authority, follow-through and timing

The authority for AC3 is the recorded FINAL delegated decision
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5843781301,
comment ID `5843781301`, not this draft or the older renewal. At real
admission, observe its current author/ID/URL/body and exact C1/C5 scope together
with the saved stop, reservation, publication and participant count, before
the one extra launch. Offline fixtures fake that observation at the same
lifecycle boundary; they do not contact GitHub or providers.

After this defect is independently reviewed, final-head three-OS-bootstrap
green and natively landed, the host alone may safely install a clean executor
with supervisors/workers absent and then resume the **same** ISS-146 run through
`scripts/executor/start-loop.ps1`. Record config path/hash, run, installed SHA
and supervisor/start identity; do not stop M2 or patch a live executor. The
resumed loop, not this author, must prove post-publication re-entry, forward
PR #673 reconciliation, any later-main exact-head DELTA, new-head gates/hosted
checks, native landing and applicable deploy verification while preserving all
six-path resolution semantics. Later conflict/non-PASS parks this renewal;
host/executor uncertainty remains non-parking, not new launch permission.
Old checks at `e3b1506a98069dfe874b9b319effb8099833f009` cannot pass a later
head. No ready flip, host merge, CI rerun, runtime edit, reservation reset or
claim of #457/M1/M2 completion follows merely from shipping the fix.

## Footprint and defect constraints

Expected implementation: `scripts/dogfood/queue.ts`, focused
`test/dogfood/queue.test.ts`, `test/dogfood/queue-adapter.test.ts` and relevant
integration-continuation/refresh tests under `test/dogfood/`, plus `docs/loop.md`.
Inspect the actual queue delivery, source/refresh adapter, supervision resume
and publication callers; change only the identity check and one native
authority-bound admission. No config/record migration, reservation or history
rewrite, generic framework, host implementation, selector change, ceiling rise,
timeout change, skip, source/gate repair, additional attempt or provider work.

- `state-machine-steady-state-missing`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint` and
  `steady-state-fast-path-skips-required-reconciliation`: enumerate original,
  refreshed, published and repeated/restarted states for both continuation
  compositions; the day-after path must reconcile the same publication.
- `retained-artifact-schema-ratchet` and
  `retry-events-double-count-terminal-outcome`: old records remain readable
  without backfill; count every existing and new launch once, never refund or
  double count a terminal outcome on replay.
- `mutable-reference-breaks-candidate-identity`, `reviewed-plan-not-bound`
  and `stale-review-bound-to-wrong-head`: bind immutable head/review, source,
  selected refresh, authority and publication lineage; an old PASS/check never
  authorizes a changed head.
- `presence-evidence-treated-as-completeness` and
  `canonical-artifact-directory-masks-missing-payload`: a directory, publication
  or receipt alone is not review completeness; parse PASS and cross-check the
  required records, identity and participant history.
- `missing-fail-closed-regression-evidence`: vary one invalid field at a time
  with the other prerequisites valid; prove the named refusal through the
  actual queue path and a targeted bypass-negative control.
- `mandated-external-assertion-contradicts-authority` and
  `review-probe-live-provider-credential-inheritance`: probe authority at the
  actual admission boundary; offline synthetic doubles and read-only review
  must neither invent an external fact nor inherit provider credentials.
- `parallel-node-verifiers-exhaust-host-memory`,
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`,
  `vitest-wall-clock-timeout-under-parallel-host-load` and
  `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`:
  foreground exclusive heavy admission; load attribution only after unchanged
  isolated timing collapse and a complete one-worker/file-serial gate, never a
  timeout/product/test waiver.
- `format-check-not-run`, `unrunnable-verification-command`,
  `container-controller-installed-before-review-terminal` and
  `future-capability-documented-as-current`: execute the actual commands and
  report exit codes; independent exact-head PASS and final-head hosted green
  precede host-only installation. Documentation does not grant a live resume.

## Review packet

Independent reviewer: attack original versus refresh identity, every replay
entrypoint, missing/non-PASS and mismatched evidence, one-use grant/charge and
wrong-lineage/fresh-run controls. Verify the C1-C9 decision without treating its
permission as a code review or hosted proof. G0: the native source, refresh and
reservation paths already own the state; a separate recovery framework would
add authority rather than remove the demonstrated contradiction.

No UI, schema, public glossary, new service or product data path. The planned
M2 row is scheduling of an executor defect through an ordinary M1 run, not a
dependency on ISS-136..138 or a change to #457's acceptance outcome. Keep this
draft unready until registration, complete-sweep independent planning review
and the ordinary readiness check have passed.
