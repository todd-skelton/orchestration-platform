---
key: ISS-052
title: "Implement release-candidate and CI/deploy observations"
labels: ["type:slice", "area:integrations", "priority:p1", "risk:authority"]
milestone: "Self-hosting release"
parent: EPIC-ADOPTION
blocked_by: [ISS-014]
---

## Registration context and outcome

Release-candidate preflight and CI/deploy observation have no live release
source in ISS-013. Register implementation after `ISS-014`, whose stable
promotion path supplies an independently reviewed candidate, exact checks,
gate/fence state, and final release identity. This issue remains thin and
blocked until those facts exist.

## Scope, non-goals, and acceptance evidence

In: elaborate release-candidate current-source preflight plus neutral CI/deploy
observation callbacks against ISS-014 facts and two fixture adapters. Out:
promotion decisions, repository-provider implementation, credentials, project
mutation apply, candidate self-certification, rollback, and N0 genesis.

Acceptance evidence must distinguish exact current, moved, unavailable, and
malformed release subjects; bind check/deploy observations to the reviewed
candidate and gate/fence; and prove observation success cannot promote or write.
Exact verification commands are due at reviewed elaboration.

## Verification plan

Registration is checked by the repository planning checker. Before `ISS-014` clears the edge,
a reviewed elaboration must name exact release-currentness, CI/deploy, and
consumer tests; planning success is not behavior evidence.

## Predicted footprint and review attack surface

Expected ownership is adapter-sdk release preflight/observation callbacks and
ISS-014 consumer fixtures/tests, with no new provider package. Review should
attack latest-run lookup, moved candidate, check aggregation gaps, deploy-name
confusion, observation as permission, self-review, and release-state writes.

## Operator actions

None.
