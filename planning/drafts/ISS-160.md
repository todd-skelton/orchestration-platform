---
key: ISS-160
title: "Advance the stopped conflict attempt to failed before ordinary continuation"
labels: ["type:slice", "ready"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

ISS-146 / #457 unpublished conflict-author FAIL:
https://github.com/todd-skelton/orchestration-platform/issues/457#issuecomment-5689997022.
Read-only salvage branch:
`codex/run-8206815a33822cf85ef7cf38c7489e0119b73ff7765e4a052e629fec938661bb/iss-146-attempt-1`
at `810fe31b16d286af838a8dc491bfed5305810ad0` (marker seed, never PASS).
Parents: reviewed source `2a83519c5053b0f3db13344061e0a0751ac86342`
and integration main `0a21eb5a446f30f921bfeca11d6a463e99700d50`. Never reuse
unresolved code, historical PASS, or old gate receipts as delivery authority.

Retained root: `/root/orchestration-m1/runtime/m1-iss146-147-20260914T2325`.
Read `cycle-1-stop-3*.json`, `iss-146-attempt-1/attempt.json`, participant records,
`source/native-refresh.json`, source review, and its named refresh directory's
author terminal/trace. Failed author: `01a0a78f-f5d9-7fb3-a5ee-d5ac34b92fd9`.

Observed 2026-09-16T01:20:42Z: delivery/attempt 1 projection has TWO participants;
participant files/completed stop have THREE. Refresh: no accepted head,
`resolutionUsed: true`, `flowRetried: false`, retries 0; no gate-correction/publication.

Main: `d2c956bcd8cbe70719d2d8159df78cef34135bc2`. `supervision.ts:172` skips
completed stops; later composition at `queue.ts:762` cannot advance delivery.

### Don't-rebuild pointers

- `scripts/dogfood/queue.ts`
- `scripts/dogfood/supervision.ts`
- `scripts/dogfood/flow.ts`
- `scripts/dogfood/refresh.ts`
- `scripts/dogfood/conflict.ts`

## Done when

1. **AC1: Honest failed projection.** Before its failed-attempt scan, composition
   observes this recorded author FAIL and advances delivery to failed once. Keep
   original base, attempt 1, historical review ID/findings; use actual failed seed
   as failed head, clear acceptedStage/stateDirectory per ordinary failure, and
   recover all three native participants. Never invent reviewer findings or relaunch
   the author. Seed/main, stops/completions, source/review, refresh, author records
   and original worktree remain unchanged. Evidence: retained-shape queue fixture,
   historical-byte comparison and nonfailed/in-flight author control.
2. **AC2: Ordinary successor.** Keep completed-stop skipping and cycle order.
   After explicit planning unpark, later selection in the SAME run composes
   absolute attempt 2 in new `iss-146-attempt-2` worktrees. Author base is seed;
   mainBase is its recorded main. Only this unresolved-seed path defers pre-author
   rebase until native delivery refresh after author/review; never fake a successful
   rebase. Preserve full diff against main and the run's accumulated history,
   including intervening issues, instead of replacing it with old history at 775.
   Evidence: real-Git composition/supervisor fixture fails before and reaches
   attempt 2 after; no-unpark control is idle absent other eligible items; an
   intervening issue's launches stay charged.
3. **AC3: Inherited native lifecycle.** Reuse setup, author, commit reconciliation
   and exact-head independent DELTA; provide both parents, source review/trace,
   failed-author trace and selected brief. Review semantic changes/direct callers
   against all acceptance, inheriting old review as context, never current authority.
   Carry consumed conflict, worker-retry and local-correction state into actual
   launch/delivery decisions. `resolutionUsed` stays true despite the new directory.
   Evidence: native lifecycle with fake workers, pre-consumed retry/correction
   controls and carry-forward bypass mutants. Ordinary hunks and unrelated
   failed-review continuation remain unchanged.
4. **AC4: Bounded delivery.** Current-main refresh requires fresh DELTA and all
   local/after-mirror gates, publication, hosted checks, landing and applicable
   deployment at final head. New conflict stops; local correction requires remaining
   ISS-152 eligibility. Successor author FAIL/blocking DELTA parks without source
   repair or automatic attempt 3; exhausted correction also stops. Evidence:
   non-PASS/moved-main fixtures and stale PASS/receipt/renewed-allowance bypass
   controls with unrelated inputs valid.
5. **AC5: Replay.** Interrupt/double-replay existing native fixtures before/after
   projection write, setup, launch observation, terminal, lost commit response,
   DELTA, refresh, gate/correction, publication and completed/stopped steady states.
   Resume one attempt-2 identity without duplicate launch/attempt/publication/merge.
   Pending notes reconcile first; completed stops never become delivery PASS.
   Historical bytes stay identical except current projection. Document in
   `docs/loop.md`; landing never readies, installs, restarts or completes #457.

## Scope fence

In: failed projection and minimum native continuation plumbing for this stop.
Non-goals: #457 semantic implementation; changing hunk grammar; published recovery;
live state/worktree edits; fresh-run reset; new host grant, reservation, key-alias
defenses, terminal-park packet parser, general authority framework or ledger;
ISS-154 4-to-5 or ISS-157 gate-stop changes; extra attempts or waived gates.

### Simplest shape

Observe the real failed author, advance the current projection, and reuse ordinary attempt-2 composition with retained context and limits.

### Not built

| Not built | Reason |
| --- | --- |
| Grant/reservation protocol | Planning unpark is the authority; one operator/writer already owns resume. |
| Phase-only patch | Pre-author rebase, history replacement and fresh allowance defaults still fail. |
| New semantic resolver | Ordinary author plus inherited DELTA already supplies the lifecycle. |

## Decisions and operator actions

Settled: https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5666535088
authorized scratch/tree isolation and JPEG-only cleanup;
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5666826318
authorized the consumed hunk resolution, not semantic retry.

Stopped config (host-relative):
`.orchestrator/platform-runs/m1-iss146-147-20260914T2325/loop.json`.
`nativeLaunchCeiling: 64`, `attemptCeiling: 4`; absolute attempt 2, no reset or
increase. Three existing launches stay charged, plus every later real run launch.
No new config guard; USD is telemetry.

Pending #457 authority only:
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5690656885
proposes ONE seven-file semantic attempt 2 for #457, adding
`test/dogfood/adapter.test.ts` ONLY to isolate
`CODEX_PROVIDER_BASE_URL` / `CODEX_PROVIDER_AUTH_COMMAND` in its author-temp fixture.
Do not reorder production provider probing or weaken author-temp/correction/resume
assertions. Not authorization; Todd's answer is pending.

F2 exact-candidate archive probe, 2026-09-16T01:19:12Z: loopback + `/bin/false` caused
`provider-unavailable` before expected `unsupported-adapter-configuration`;
clearing only those variables passed all three original launch/evidence assertions.
Zero HTTP/workers; no install. NOT integrated-test/full-gate PASS. Main keeps
probe-before-validation; ruled integration must prove focused and full gates.

Standing handoff authority (`.orchestrator/platform-handoff.md` sections 3-4):
independent plan PASS -> ISS-160 registration -> normal native M1 implementation
-> exact-head independent review/three-OS bootstrap green landing -> safe host install,
supervisors absent. Then #457 only: Todd's amended answer -> explicit planning unpark
-> native attempt 2. #457 stays unready. ISS-158/159 independent; no provider credentials.

## Defect constraints

- `state-machine-steady-state-missing`, `steady-state-fast-path-skips-required-reconciliation`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint`: AC2/5 cover all entrypoints,
  inert terminal replay and pending notes.
- `retained-artifact-schema-ratchet`: historical records readable without new fields.
- `retry-events-double-count-terminal-outcome`: charge each real launch once;
  retain global history and spent allowances.
- `mutable-reference-breaks-candidate-identity`, `stale-review-bound-to-wrong-head`:
  pin seed/parents and fresh exact-head review/gates.
- `presence-evidence-treated-as-completeness`, `canonical-artifact-directory-masks-missing-payload`:
  directory/intent/seed is not completed work.
- `missing-fail-closed-regression-evidence`: vary only governing inputs; execute
  carry/stop/identity bypass mutants, not vacuous unrelated-guard controls.
- `review-probe-live-provider-credential-inheritance`: scrub real authority;
  local fake URL and absolute fake executable only.
- `container-controller-installed-before-review-terminal`: exact-head independent
  PASS/hosted green precede host install.
- `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`,
  `vitest-wall-clock-timeout-under-parallel-host-load`: no subset waiver; load needs
  unchanged isolated timing collapse AND complete one-worker/file-serial gate;
  no timeout increase, product edit or skip.
- `parallel-node-verifiers-exhaust-host-memory`, `worker-tool-ceiling-kills-heavy-verifier-wrapper`:
  machine-admitted foreground gates; finish children, never steal owners.
- `format-check-not-run`, `unrunnable-verification-command`: format and resolve
  scripts at exact head; lint is not delivery proof.
- `future-capability-documented-as-current`: all recovery here is proposed, not shipped.

## Verification plan

Native WSL: `pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`, `pnpm test`.
Focused: `pnpm exec vitest run test/dogfood/queue.test.ts test/dogfood/queue-adapter.test.ts test/dogfood/supervision.test.ts test/dogfood/supervise.test.ts test/dogfood/flow.test.ts`.
Installed bounded foreground machine admission owns every command: no exclusive-lock
bypass or parallel full verifiers. Windows wrapper: host-only.

## Footprint & chain

`scripts/dogfood/queue.ts` owns projection, composition, history, failure and limits;
`flow.ts` only needed inherited worker-retry input. Reuse `refresh.ts` inputs and
unchanged `conflict.ts`. `docs/loop.md`; existing `test/dogfood/queue.test.ts`,
`queue-adapter.test.ts`, `supervision.test.ts`, `supervise.test.ts`, `flow.test.ts`.
Callers: supervise composition, source/review, delivery refresh/correction, saved
selection, pending/completed stops. Register native Blocks #457 / reverse Blocked by;
not yet wired. The pending seven-file question bounds #457, not this prerequisite.

## External authority probe & evidence timing

No new external payload. Terminal/stop capture post-author failure, not readiness.
Existing timing stays; observe amended answer/unpark for #457 dispatch only.

## Review packet seed

Attack history loss, skipped transition, seed rebase, renewed allowances, attempt 3,
stale PASS and replay using native snapshots/fixtures. Independent sweep pending.

QUALITY_PROFILE: contract

### Intent surfaces

| Acceptance criterion | Exercised surface |
| --- | --- |
| AC1 | Native stopped-attempt projection |
| AC2 | Supervisor selection and queue composition |
| AC3 | Native author/DELTA and consumed allowances |
| AC4 | Final-head delivery and bounded work stop |
| AC5 | Replay, steady states and operator documentation |

### UI states and design-system sources

none — no UI surface changes.

### Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Selected stopped issue | Existing four-attempt scan | Existing runtime paths | One projection transition; ordinary successor records |

### Contract compatibility

| Changed contract | Compatibility posture | Removed path |
| --- | --- | --- |
| Failed conflict continuation | Old evidence unchanged; current projection advances | Stale delivery projection for this observed failure |

### Glossary impact

none — no new or renamed public names.

### Tier + routing hint

Full path/system; existing self routing, no model-policy change.
