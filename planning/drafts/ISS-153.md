---
key: ISS-153
title: "Parse ordered acceptance criteria before dispatch"
labels: ["type:slice", "ready"]
milestone: "Unattended self-improvement"
blocked_by: []
---

## Why

Run `m1-iss152-recovery-20260915T0115` completed ISS-147, selected ISS-152
(#474) for cycle 2 at base `8bed5d914012943f34781afdbdacc43605e26a2d`,
then stopped before any worker launch with `selected-issue-criteria-missing`.
The preserved evidence is
`/root/orchestration-m1/runtime/m1-iss152-recovery-20260915T0115/cycle-2-stop-1.json`;
the learning note is #474 comment 5673598982.

The registered ISS-152 draft has a populated `## Done when` section containing
seven ordinary ordered Markdown items (`1.` through `7.`). The self adapter's
`listItems` recognizes only unordered `-`, `*`, and `+` markers, so valid
registered planning produced a body that the delivery selector cannot consume.
`pnpm planning:check` and `pnpm planning:board-check` both accepted the draft.
The stopped runtime, worktrees, selection, and zero-attempt accounting stay
preserved. This issue repairs the parser; it does not edit or restart that run.

### Don't-rebuild pointers

- `adapters/self.mjs::listItems`
- `adapters/self.mjs::issueContext`
- `test/dogfood/queue.test.ts::repositoryPolicy.issueContext`
- `planning/drafts/ISS-152.md`
- `docs/loop.md::Planning`

## Done when

- **AC1: Consume the recorded ordered form.** The self adapter recognizes each
  top-level `N.` item under `## Done when` as one acceptance criterion and
  preserves its indented continuation lines. A fixture using the verbatim
  marker/continuation shape of ISS-152 yields exactly seven criteria in AC1 to
  AC7 order. Evidence: focused self-adapter test through `issueContext`.
- **AC2: Preserve existing forms and the refusal.** Existing unordered `-`,
  `*`, and `+` items still produce the same criteria. A missing heading, an
  empty section, prose without a supported list marker, and continuation text
  before the first item still fail with `selected-issue-criteria-missing`; the
  parser never falls back to the whole issue body. Evidence: table-driven
  positive and negative adapter tests.
- **AC3: Keep planning and execution aligned.** `docs/loop.md` states that
  `## Done when` accepts unordered items or ordinary `N.` ordered items, and a
  regression uses a registered draft rather than a test-only alternate parser.
  Evidence: documentation assertion plus `pnpm planning:check`.
- **AC4: Retain the loop boundary.** Selection, routing, readiness labels,
  attempt counts, worker prompts, and Chase Sets adapter behavior are
  unchanged. The fix creates no worker, issue mutation, or runtime migration
  while parsing. Evidence: existing adapter/queue/supervision suites plus a
  negative control with a valid selection and invalid criteria.

## Out of scope

Non-goals: editing ISS-152 acceptance semantics; hand-writing or migrating the
stopped runtime; restarting M1; changing Chase Sets issue parsing; accepting
arbitrary prose as criteria; CommonMark generalization beyond the recorded
top-level list forms; routing/model changes; additional retry or attempt budget.

## Verification

`pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`, and `pnpm test`.
Focused evidence lives in the self-adapter/queue tests and must execute both the
ISS-152-shaped ordered fixture and the no-supported-item negative.

## Footprint

`adapters/self.mjs`, the closest existing self-adapter tests under
`test/dogfood/`, and `docs/loop.md`. One slice; no prerequisite. ISS-152 may be
retried only after this issue lands and the host installs the reviewed stable
executor through the canonical restart path.

## Decisions and operator actions

The smallest fix is to accept the already-registered ordered list at the self
adapter boundary. No unresolved decision. Operator action after reviewed,
hosted-green landing: install the stable executor while M2 supervision permits,
then restart M1 canonically; implementation workers do neither.

## External authority

None. The evidence is the preserved local stop and registered draft.

## Review packet

Full-path review should attack ordered-marker recognition, continuation
boundaries, malformed/no-list refusal, accidental whole-body fallback, and any
change to unordered or Chase Sets behavior. The omission-revealing artifact is
the exact ISS-152-shaped fixture driven through the real self `issueContext`.

