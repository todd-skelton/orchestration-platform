---
key: ISS-165
title: "Propagate the optional native profile channel through existing adapter composition"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: ["ISS-164"]
---

## Why

UNPUBLISHED provisional M1 component; Blocked by ISS-164 finite parent and [chase-sets/chase-sets#8040](https://github.com/chase-sets/chase-sets/issues/8040), Blocks ISS-166 qualification. Platform main `bdfbeca128ff0f56d4560247355ba63720d7c561`.
`scripts/dogfood/queue.ts` RepositoryQueueAdapterOptions already has native?: Adapter; repositoryQueueAdapter uses options.native ?? codexAdapter. boundedNative spreads ...native. `scripts/dogfood/repair-adapter.ts` repairPromptAdapter also spreads ...native. These preserve a new optional method without facade changes. Pressure-r2 F3/F4 split propagation from finite transport and semantic invocation.

Diagnostic salvage: `codex/run-58a3cf67c20f06aa986ba4f0f276a1bfb20517a515a7c47a9c60c69b133c8ba7/cs-7844-attempt-1` @ `9e32272fc1e00aa321113237cd9bb90f63e3360d`. Never reuse patches/skips/receipts/verdicts. No stopped component PR; historical platform-iss162 filenames stay DB history. #515/ISS-163 selection unchanged.

## Don't-rebuild pointers

- `scripts/dogfood/supervise.mjs`
- `scripts/dogfood/flow.ts`
- `scripts/dogfood/queue.ts`
- `scripts/dogfood/repair-adapter.ts`
- `scripts/dogfood/dispatch-adapter.ts`

## Decisions already made

[5700949493](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5700949493): r1q/r1r split, finite parent/private stdio/PID namespace, rule9 single writer (`docs/loop.md`). [5700051115](https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5700051115): preserve Windows admission; no service/generic commands/interop weakening. No install/transfer authority.

## Out of scope

### Scope fence

Add optional typed Adapter.nativeDbProfile and compose the existing native adapter with ISS-164's channel using options.native; independently prove preservation to each consumer.
Non-goals: transport/parent reimplementation, production request trigger/declaration/pre-review DB interpretation (ISS-166), queue/dispatch/repair facade edits, new modules, product/worker/runtime/live-executor/AgentPools changes, ownership transfer, new attempts, evidence import or P2/#8033.

## Simplest shape

Add the optional type in flow.ts and compose it in supervise.mjs through existing options.native and object spreads.

## Not built

| Not built | Reason |
| --- | --- |
| Service/pool/generic receiver | Finite parent and existing owner suffice |
| Copied client/cgroup/receipt framework | Existing slot and kernel lifecycle facts suffice |
| Facade/production selector | Existing seams; ordinary trigger downstream |

## Proposed source contract

In flow.ts add optional nativeDbProfile(identityData): Promise<NativeDbReply> and closed request/reply types matching incumbent/ISS-164 v1. No semantic call in runStep or pre-review. In supervise.mjs construct existing codexAdapter(loop.gitExecutable), spread it into options.native and bind nativeDbProfile to the already-owned channel.request when available. No channel: method absent (caller must classify unsupported), never a success stub. Channel closure returns typed refusal/unknown per ISS-164, no attempt/PASS/park effect.

Keep one run-owned channel across source/repair/reviewer/refresh/correction; no worker transport, second channel or copied lock. Do not alter queue.ts, dispatch-adapter.ts or repair-adapter.ts: compiler-visible options.native?: Adapter plus ...native already supports this addition. If actual exact-head type inspection disproves that fact, report the concrete failure for scoped replan, not speculative facade edits.

TEST-ONLY trigger: capture actual Adapter at existing flow/repair/refresh step boundary and call nativeDbProfile with run synthetic-native-component and disposable synthetic Git identities. Replace external execution only, NOT queue/spread/stream wiring. No production call/flag/selector/declaration. ISS-164 independently proves finite transport; ISS-166 alone owns FIRST ordinary selected-issue request, installed reciprocal exclusion and interpretation. Absence before then is expected, not DB success.

Closed v1 request keys: schemaVersion=1, correlation, profile=reconciliation-pg16/v1, run, issue, attempt, executorHead, product={repository,head,tree}, declaration={version=1,profile,files:[{file,cases}],mutants:[{id,file,cases,assertion}]}, patchDigests:[{id,digest}], stagedInputDirectory. Reply keys: schemaVersion=1, correlation, status=completed|refused|unknown, owner=null|{lockId,head,lane}, evidencePath=null|path, diagnostic=null|text. Completed requires owner/evidencePath, not PASS; declaration passed, not interpreted.

`closed-schema-shallow-validation`: recursively close every request/reply/owner/nested identity/declaration/patch object at the existing stream boundary; types cannot replace parsing and do not add a second validator. Bounds identical to dependency: request/reply 65536/8192 UTF-8 bytes; IDs 1..128 ASCII [A-Za-z0-9._:-]; repository owner/name segments 1..100; case 1..512, diagnostic/assertion 1..2048, path 1..1024. Issue/attempt/PID integer 1..2147483647, correlation safe integer 1..9007199254740991 increasing, one pending. Git hashes 40 lowercase hex, digests 64, lock ID 32 lowercase hex. Exactly 3 profile files, unique cases 1..256/file, 0..3 unique mutants/digests; required fields and exact enums, no command/env. Relative paths no traversal; absolute paths contained under approved parents, no reparse escape. Instants real UTC RFC3339 with Z, never date-only. No caller duration; inherited bounded waits unchanged, expiry unknown. Schema6 bounds/cleanup remain incumbent-only; schema2-5 readers unchanged. No signature/anti-replay/hostile-state ceremony.

## Done when

1. **AC1 Typed composition.** Supervisor composes channel onto codexAdapter via options.native without loss of existing methods. All no-native calls retain behavior; native unsupported never success. Evidence: `pnpm typecheck` plus `test/dogfood/supervise.test.ts` and `queue-adapter.test.ts` synthetic boundary caller reaches ISS-164 channel; omit options.native and intended reachability assertion fails with other inputs fixed. Changed-file control proves queue/dispatch/repair facades untouched.
2. **AC2 Caller preservation.** Source/repair runStep, inherited correction/reviewer, reviewRefresh/delta, delivery refresh/conflict, correctGate and resumed completion retain the SAME optional method and typed result. Evidence: `flow.test.ts`, `queue-adapter.test.ts`, `delivery-adapter.test.ts` capture each actual composed adapter at its existing entry and invoke only from synthetic test harness; no production request exists. Remove member at each relevant spread in disposable control, intended assertion red. No invocation or history mutation from retained completed state alone.
3. **AC3 Boundary/steady state.** Existing channel parser receives request unchanged; completed/refused/unknown survive propagation without verdict import. Missing/closed channel stops diagnostically; new run never uses prior parent. Evidence: `adapter.test.ts` through composed adapter uses minimum/maximum valid data and isolated nested-unknown/missing-key/wrong-shape/date-only/range/duplicate-correlation/oversize negatives; malformed data never reaches wrapper. No-native legacy run/history regression and two closed-state replays inert.

## Intent surfaces

| Acceptance criterion | Exercised surface |
| --- | --- |
| AC1 | Supervisor options.native and compiler-visible Adapter type |
| AC2 | Actual source/repair/reviewer/refresh/correction entry adapters |
| AC3 | Typed channel results, legacy no-native and closed-state replay |

## Footprint & chain

Production: `scripts/dogfood/supervise.mjs` composition and `scripts/dogfood/flow.ts` types; named tests/docs. Queue/dispatch/repair adapters stay byte-identical.
Callers: source/repair runStep, inherited reviewer/correction, reviewRefresh/delta, delivery refresh/conflict, correctGate, resumed completion; requests TEST ONLY until ISS-166.
Native Blocked by ISS-164/external owner; Blocks ISS-166. Host wires real external dependency. Serial shared-file change, no qualification cycle/ready with blockers.

| AC trace | Entry to effect, non-test files |
| --- | --- |
| AC1 | supervise.mjs -> queue.ts -> flow.ts Adapter (3) |
| AC2 source/refresh/correction | queue.ts -> flow.ts consumer (2) |
| AC2 repair | queue.ts -> repair-adapter.ts -> flow.ts consumer (3) |
| AC3 | flow.ts contract -> supervise.mjs channel (2) |

Independent contract endpoints: finite stream ISS-164; qualification ISS-166, not seven-file canonical-to-DB acceptance.

## Verification plan

Implementation commands only. ROOT canonical container; PlatformWorktree/Branch/Head exact clean attached target, Lane valid label. Foreground; direct heavy commands require machine admission before body.

```powershell
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm exec vitest run test/dogfood/supervise.test.ts test/dogfood/adapter.test.ts test/dogfood/flow.test.ts test/dogfood/queue-adapter.test.ts test/dogfood/delivery-adapter.test.ts --maxWorkers=1 --no-file-parallelism
& "$ROOT/.orchestrator/invoke-heavy-verifier.ps1" -Gate test -Worktree $PlatformWorktree -Lane $Lane -Branch $PlatformBranch -ClaimedHead $PlatformHead
git diff --check
```

Branch mode only, no ImmutableHead mixture. Native wrapper invocation is unchanged ISS-164 contract with both AnchorBranch/AnchorHead; adapter never invokes wrapper directly or runs battery.

## Defect-class constraints

`closed-schema-shallow-validation` above is parser correctness. Foreground exact owner cleanup; no age/name/group/periodic sweep, unrelated kill or parent-death release. Reconstruct env without credentials/PG selectors/Node hooks; omitted-role/PGHOSTADDR sentinels. Separate clean exact anchor/executor/product/install identities, no rewind/verdict import.
All named retained-state callers/steady replay; unchanged legacy/no-native histories, no extra attempts/park/double count. Directory/reply != executed evidence. Freeze ambient selectors/other inputs for intended-red controls. Labeled synthetic != installed/timed proof. Exact independent PASS before install; changed-file format/manifest commands. Load claim: unchanged isolated collapse AND complete one-worker/file-serial gate, never timeout/product/test/skip changes.

## External authority probe & evidence timing

ROOT `.orchestrator/controller-native-db-primitive-r2.report.md` captured during lifecycle Windows loss 2026-09-16T16:36:28Z: init/sleeper survived; exact pidfd reconciliation removed both 16:36:29Z; 16:37:30Z no survivors/root. Inode reused. Not transport/slot/DB proof. Component captures request/acquisition/tuple/release; no provider API.

## Operator actions

Host DELTA/registration and reviewed install, existing no-live-owner/native-pause window; no credentials/live edits. P2/#8033 HOLD.

## Review packet seed

Invariants: owner/channel/history. Failure modes: lost caller, false completion/cleanup. Evidence: per-AC negatives/lifecycle observations.

QUALITY_PROFILE: contract

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Optional profile method | One run/channel/pending request; dependency bounds above | None | Existing request/reply only |

## Contract compatibility

| Changed contract | Compatibility posture | Removed path |
| --- | --- | --- |
| Adapter.nativeDbProfile | Optional; no-native/old histories unchanged | None |

## Glossary impact

Terms: typed incumbent profile capability.

| Public term | Owning glossary or contract |
| --- | --- |
| nativeDbProfile, NativeDbReply | `scripts/dogfood/flow.ts` and incumbent closed v1 |
| run, attempt, host stop | `docs/loop.md` |

## Tier + routing hint

Full path; system
