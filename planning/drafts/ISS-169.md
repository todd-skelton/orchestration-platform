---
key: ISS-169
title: "Reduce saved-gate fixture Git work under the unchanged Windows gate"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

UNREGISTERED: semantic PASS, planning PR/board consistency pending. Milestone 8; ISS-110 open. Not #519/#515/#457 recovery; ISS-168 separate.

[Host finding](https://github.com/todd-skelton/orchestration-platform/issues/367#issuecomment-5703645951): open planning-only #519, `codex/platform-native-db-registration` @ `b62299c9ee0798401affa05975183412d9483e9e`; main `bdfbeca128ff0f56d4560247355ba63720d7c561`. Read-only evidence only; never reuse gates/PASS/runtime records, reset budgets or invent native resume authority.

Bootstrap 35138686485/job 104937422106: Windows 30000ms timeouts, refresh.test.ts:815; 844 pass/3 fail/13 existing OS skips, 2067.41s. Failed (mirror,refresh,correction,repaired): (F,T,F,F), (T,T,F,F), (F,F,T,F). Ubuntu/macOS passed. ROOT artifacts: `pr519-windows-35138686485.log`; unchanged `pr519-windows-local-focused.log`: 6 pass/171 filtered, 140.78s total/139.66s tests. Not reproduction, suite-green or contention proof. WSL #515 six at 1344-1847ms/883 passes with unrelated ISS-168 failure is not Windows proof.

## Don't-rebuild pointers

- `test/dogfood/refresh.test.ts`
- `vitest.config.ts`
- `scripts/dogfood/refresh.ts`
- `scripts/dogfood/queue.ts`
- `docs/loop.md`

## Decisions already made

No unresolved product decision. ISS-160/#509 already ships serial files/one-worker; ISS-157 ships recovery. ISS-164/165 DB planning is unrelated.

## Scope fence

Fixture setup only; existing directly owning helper only if necessary.
Non-goals: product/runtime/controller/config/workflow edits, timeout/worker/skip changes, Git mocks, checkpoint deletion, generic guards, artificial-delay causation, retry-until-green, waivers, executor or budget changes.

## Simplest shape

Remove redundant setup Git calls; retain recovery assertions.

## Not built

| Not built | Reason |
| --- | --- |
| Cache/framework | Existing fixture suffices |
| Split tests/relocated hooks | Moving work is not reducing it |

## Concrete candidate

`fixture()` creates reviewTree at head then `pinSource()` repeats HEAD lookup/checkout unchanged. Separate initial evidence writing from repinning; reuse that head only initially, retaining real lookup/checkout after mutations. Replace user.email/user.name config writes with fixture-local `git -c` options, preserving identity without global/env mutation. Estimate: four fewer processes/case, 24 total; speedup unmeasured. No native HEAD/status/ancestry cache. Same native author may choose a smaller supported refactor; insufficient savings requires the missing dominant-cost/remedy fact, not expanded scope.

## Done when

1. **AC1 Semantics.** All six tuples retain real private repos/worktrees, no-grant refusal, one reservation/DELTA launch, running-review replay/zero gates, new descendant containing main, exact gate order/directories, no extra author, history prefix plus one reviewer, replay with one publication, identical reservation/retained bytes. Prove unchanged matrix assertions and full refresh.test.ts including refusal/interruption/new-stop/moving-main siblings.
2. **AC2 Reduced work.** Same-host before/after per-tuple total/setup/recovery timing and Git counts via commands capture/temporary timing show removed operations, identical state/identity/assertions, no shifted work. Retain comparison; deterministic hosted reproduction not required. No reduction means no causal PASS.
3. **AC3 Delivery.** Independent exact-head review and three-OS bootstrap green, with actual full Windows owning test execution at unchanged 30000ms, serialization/skips. Retain failure/per-case evidence. Focused/WSL/aggregate success is insufficient.

## Verification plan

`node node_modules/vitest/vitest.mjs run test/dogfood/refresh.test.ts`; focus: `-t 'continues a saved queue gate stop once'`. Full test: `& "$ROOT/.orchestrator/invoke-heavy-verifier.ps1" -Gate test -Worktree $Worktree -Lane $Lane`. ROOT=`D:/Users/ToddS/Source/Repos/chase-sets`; artifacts=`.orchestrator/artifacts`. Foreground admission required. Scoped: `pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`; hosted: `pnpm run verify:bootstrap`. Not run by planning.

## Footprint & chain

`test/dogfood/refresh.test.ts`; no ISS-168 dependency. Reviewed/three-OS-green ISS-169 landing precedes authorized planning-only #519 integration on repaired main with final-head review/gates. Red registration blocks sequencing; no host patch/invented ISS-124/141 tuple.

## External authority probe & evidence timing

Jobs API 2026-09-16T19:59Z: Windows step failed 19:43:24Z at #519 head; Ubuntu/macOS executed green. `trigger-job-masks-real-deploy`/`mandated-external-assertion-contradicts-authority`/`stale-review-bound-to-wrong-head`: capture candidate run/head/job/step/timestamps/conclusions after execution; recheck head before landing. Missing/skipped/red/coarser or altered facts never authorize. `vitest-wall-clock-timeout-under-parallel-host-load`: retain failure; load attribution needs unchanged isolated timing collapse AND full serial gate. No timeout/product/skip edits or lock bypass.

## Operator actions

Host registration/integration/admission later; no provider credentials.

## Review packet seed

Not applicable - fast path/system. Review AC1-AC3 for lost checkpoints, stale heads and hidden costs.

QUALITY_PROFILE: hot-path

## Intent surfaces

| Acceptance criterion | Exercised surface |
| --- | --- |
| AC1 | Saved-stop queue recovery matrix and siblings |
| AC2 | Fixture real-Git setup and full timed cases |
| AC3 | Exact-head Windows/Ubuntu/macOS bootstrap |

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Six private cases | Existing checkpoints | N/A | Fewer setup Git calls; recovery unchanged |

## Contract compatibility

none — no schema, event, or contract changes.

## Glossary impact

none — no new or renamed public names.
