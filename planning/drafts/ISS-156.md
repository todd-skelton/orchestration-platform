---
key: ISS-156
title: "Separate provider auth-process evidence from the synthetic outage clock"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

ISS-155 (#488), run `m1-iss155-20260915T1900`, attempt 2 passed independent
Opus review at `705c98bc36c5f9d373ef34d0a86e4dc25abfc472`, then delivery's
full test failed at `test/dogfood/adapter.test.ts:539`. Expected malformed models
diagnostic; received `provider authentication command failed`. Native delivery
stopped `gate-attribution-unknown:test`; no base control ran. The retained full
log has 19 files, 1 failed/731 passed/1 skipped tests. Learning note:
https://github.com/todd-skelton/orchestration-platform/issues/488#issuecomment-5687186310

Observed main on 2026-09-15:
`71e85afcf77b1a82ac15d7a88c50ec894ca885ba` (local and `git ls-remote`).
`adapter.test.ts` and `dispatch-adapter.ts` are byte-identical at main and the
failed candidate. This is a new fixture gap, not replacement of ISS-155's
literal-conflict outcome. PR #490 is OPEN at attempt 1
`b6edde95e1c7cf045789ad91a8c91bfbd53384c4`, branch `codex/iss-155`;
its Windows bootstrap failed on LF/CRLF. There is no closed PR or blocking
review finding ID: both native reviews passed with notes. Preserve that history.

Read-only retained-source pointer (not authority to adopt or publish):
`codex/run-fab2ef9feb088b53ad717433e2fe4bfe939e05fef8db8b0329822da851afa13f/iss-155-attempt-2`
at `705c98bc36c5f9d373ef34d0a86e4dc25abfc472`.
Source: `/root/orchestration-m1/worktrees-iss155-20260915T1900/iss-155-attempt-2-source`.
Runtime: `/root/orchestration-m1/runtime/m1-iss155-20260915T1900/iss-155-attempt-2/source`.
Under runtime, `gate-9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08/`
contains `candidate.log` and `candidate-terminal.json` (exit 1, exact head and
command). Do not reuse historical PASS as new-head authority, copy the candidate's
conflict implementation into this repair, or replace retained logs/terminals.

Host's `.orchestrator/artifacts/iss156-provider-reproduce.mjs` and
`iss156-provider-reproduction.log` in the Chase Sets container import unchanged
native functions with a synthetic helper sleeping 50 ms and no real provider.
Control 1: injected clock/outage ceiling 10 ms plus real abort/process yields
auth failure, requests=0. Control 2: isolate the malformed-response probe from
that synthetic deadline, yielding malformed models response, requests=1.
Control 2 is diagnostic, NOT a proposed timeout increase. This explains a
fixture race, not the original aggregate's load attribution.
`gateDiagnostics('test', retainedFullLog)` returns `[]`; its root cause remains
unestablished and parser changes are outside this issue.

### Don't-rebuild pointers

- `test/dogfood/adapter.test.ts`
- `scripts/dogfood/dispatch-adapter.ts`
- `scripts/dogfood/delivery-adapter.ts`
- `scripts/dogfood/queue.ts`
- `planning/drafts/ISS-155.md`

### Existing coverage

Existing auth/model integration and hanging-probe ceiling tests already cover
the native functions. ISS-129 supplies outage handling; ISS-149 supplies model
selection; ISS-152 supplies gate attribution/correction; ISS-148 supplies main
refresh. None separates this fixture's clocks. ISS-157 handles the independently
confirmed saved-stop barrier; fixing this fixture alone cannot resume ISS-155.

## Done when

- **AC1: Deterministic discriminator.** In a disposable test workspace, run the
  old fixture's coupled 10 ms clock/real delayed helper and observe auth failure
  before HTTP (requests=0); the repaired arrangement reaches HTTP (requests=1).
  Requests 0 -> 1 is the sole discriminator. Preserve `provider-unavailable`,
  the exact repaired malformed diagnostic and logical waits `[10]` as invariants,
  not separation evidence; waits stay identical. Keep payload/delay; record
  old/repaired heads and outputs. Prove logical-clock assertions no longer
  depend on auth process startup time, without raising any timeout.
- **AC2: Preserve integration and ceilings.** Actual synthetic helper execution
  and refreshed helper output still supply the Authorization header on each
  probe. Retain HTTP 503, success, present/missing/empty model list and every
  malformed payload assertion. Prove auth-before-HTTP with a normally exiting
  child: observe its invocation sentinel with requests=0 before the
  first HTTP call. That ordering assertion has no abort, ceiling or timing budget.
  Separately retain the existing injected hanging-probe abort/exhaustion test
  unchanged (`adapter.test.ts:442-465`); it spawns no child. Evidence: focused
  `adapter.test.ts` results, sentinel and request counts. No real
  provider, credentials or identity; production `probeProvider`/`waitForProvider`
  behavior and code remain unchanged.
- **AC3: Verified bounded repair.** Author/review evidence includes exact-candidate
  native WSL typecheck, format, planning and complete test PASS. Preserve existing
  platform-specific tests; no new skip, weaker assertion or flaky-test retry.
  Evidence: full command outputs independently inspected at handoff.

## Scope fence

In: deterministic separation in the provider fixture, its direct negative controls,
and a concise ISS-156 incident paragraph in `docs/loop.md`.
Non-goals: production/provider timeouts, product changes, diagnostic parser repair,
retries as a substitute for determinism, new provider framework, gate-stop
continuation, ISS-155 implementation, runtime edits, loop start or executor install.

### Simplest shape

Keep real helper integration and the existing injected-probe outage test separate; use the existing probe seam rather than adding a production abstraction.

### Not built

| Not built                | Reason                                                    |
| ------------------------ | --------------------------------------------------------- |
| Longer timeout or retry  | Hides the coupled clocks instead of separating them.      |
| Provider fake framework  | Existing function injection and synthetic helper suffice. |
| Diagnostic-parser change | Empty extraction is observed, not explained.              |

## Footprint & chain

Only `test/dogfood/adapter.test.ts` and `docs/loop.md`. Put the incident immediately
before `## Planning` (non-chronological order); prove clean native three-way merge
against retained ISS-155 prose in scratch, never editing its candidate. Inspect,
`dispatch-adapter.ts`: direct `probeProvider`, `waitForProvider`, and their worker
launch composition are the callers whose behavior must survive. No config edits.
Serial M2 loop-defect chain: ISS-156 -> ISS-157 -> saved ISS-155 delivery ->
original ISS-146 recovery. ISS-157 is blocked by this independently landed repair,
not the reverse. Host registers the native Blocked by / Blocks relation after
independent planning review; no registration/readiness is granted by this draft.
ISS-136/137/138 stay in M3; this work does not transfer any M2 product milestone.

## Decisions already made

Todd's ISS-156 dispatch selects fixture isolation and forbids timeout increases,
skips, product edits and speculative parser scope. No unresolved decision.
ISS-147's governing Chase Sets #4388 comment `5666826318` authorized one bounded
conflict author/DELTA; ISS-154's comment `5681842811` governs a separate ruled
continuation. Neither authorizes new ISS-155 attempts or changes those ceilings.

## Verification plan

Implementation, in native WSL: `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm test`; focused:
`pnpm exec vitest run test/dogfood/adapter.test.ts`.
Native WSL owns its verifiers; do not import the Windows heavy-wrapper dependency.
Run admitted heavy verification foreground and bounded, without duplicate writers.
For an actual timeout preserve first output; only unchanged isolated timing
collapse PLUS a complete one-worker/file-serial gate establishes load sensitivity.
Do not turn a passing subset into aggregate evidence. Planning runs only planning,
changed-plan formatting and Chase Sets `scripts/brief-lint.mjs` on this brief.

## Defect-class constraints

- `vitest-wall-clock-timeout-under-parallel-host-load` and
  `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`:
  AC1 isolates the fixture cause; aggregate evidence still follows the protocol above.
- `review-probe-live-provider-credential-inheritance` /
  `windows-posix-cli-shim-resolves-real-provider-binary`: synthetic absolute helper,
  scrubbed credentials and invocation sentinel; no PATH-resolved provider CLI.
- `missing-fail-closed-regression-evidence`: vary only malformed payload or abort;
  demonstrate each relevant negative turns red under its named bypass mutant in
  a disposable workspace. Do not commit mutants or add a mutation framework.
- `golden-provenance-nonexistent-base-sha` /
  `mandated-external-assertion-contradicts-authority`: resolve the retained commit,
  distinguish observed incident from synthetic controls; never alter a real
  external identity's facts to fit an assertion.
- `format-check-not-run`, `unrunnable-verification-command`,
  `parallel-node-verifiers-exhaust-host-memory` and
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: use the declared native
  manifest commands and foreground admission, never steal ambiguous ownership.
- `stale-review-bound-to-wrong-head` / `trigger-job-masks-real-deploy`: independent
  review and actual three-OS jobs bind this repair head, not historical green.

## Operator actions

Host supplies the read-only artifacts; no provider credentials needed. This issue
does not resume any run. After ISS-157 also lands, its host-only procedure applies.
Original PR #490, #488 and ISS-146's stopped run remain untouched by this worker.
Native delivery owns exact-head three-OS `bootstrap` green after author/review,
before landing, not author handoff evidence.

## External authority probe & evidence timing

AC1-2 use synthetic local processes/payloads, not an external contract change.
Native delivery consumes hosted authority only after execution. Observed
PR #490 run `35012868031` at the old head has Ubuntu/macOS pass and Windows fail;
it is not this repair's evidence. Capture this repair's exact-head job results
at delivery. No new GitHub API shape or pre-merge timing assumption is introduced.

## Review packet seed

Attack a fake malformed-response pass that never executes HTTP, cached helper
output, deleted malformed cases and discarded abort coverage. Inspect the
request-count discriminator and unchanged production diff. Fixture proof is not
full-gate proof. COMPLETE r1 retained; inherited DELTA completion only. Still unready.

## Tier + routing hint

Full path, system contract. Planning dispatch: row 7 Astra/high, override-Todd,
expected USD unknown; implementation uses existing native self routing.

QUALITY_PROFILE: contract

### Intent surfaces

| Acceptance criterion | Exercised surface                |
| -------------------- | -------------------------------- |
| AC1                  | Logical clock / auth boundary    |
| AC2                  | Auth/HTTP/models; injected abort |
| AC3                  | Native author/review gates       |

### UI states and design-system sources

none — no UI surface changes.

### Data-path envelope

none — no data path changes.

### Contract compatibility

| Changed contract    | Compatibility posture                  | Removed path                                     |
| ------------------- | -------------------------------------- | ------------------------------------------------ |
| Test fixture timing | Production API and deadlines unchanged | Real auth spawn inside synthetic-clock assertion |

### Glossary impact

none — no new or renamed public names.
