---
key: ISS-060
title: "Land the synthetic self-host BUILD/REVIEW workflow bytes and issue form, reviewed but not enabled"
labels: ["type:slice", "area:integrations", "priority:p1", "risk:authority"]
milestone: "Self-hosting release"
parent: EPIC-SELFHOST
blocked_by: [ISS-054]
---

## Supervised dogfood priority (Todd ruling #323)

#319 A permits the one-record correction and independent verification of
the parked readiness plan; it does not authorize workflow implementation.
After that repair, keep this release-preparation issue off the first ISS-071
trial path. Unpark for a named runnable consumer; preserve inert artifact
placement, static test coverage and the separately authorized O3 live boundary.

See `docs/planning/supervised-dogfood.md` for the immediate trial boundary.


## Context and evidence

Decision #302's readiness audit (§3, CF-5) identifies the synthetic BUILD and
REVIEW workflow bytes, and the issue form `ISS-036`'s live window enables,
as byte content that can be written and reviewed without ever being enabled
on a triggerable ref. This issue lands those bytes.

## Scope fence

In: the exact bytes of the synthetic BUILD workflow (`on: pull_request` with
activities `[opened, reopened, synchronize]`) and the synthetic REVIEW
workflow (`on: workflow_run` with `[completed]` and
`requiredConclusion: SUCCESS`, sourced from the BUILD workflow's exact
path/ref/digest) — `contract-decisions.md:6761-6770` — each with an explicit,
complete seventeen-name permission block including every effective implicit
`NONE` (`:6712-6760`). Plus `.github/ISSUE_TEMPLATE/self-host-work.yml`
producing the exact fenced `orchestration-work/v1` block
(`docs/architecture/self-host-github-contract.md:3-10`). Landed on a
non-default branch or under a path the default `on:` triggers cannot reach,
so the bytes are reviewed without being live.

Out: any enablement, any run, any `workflow_dispatch`, any arbitrary input
(`planning/drafts/ISS-036.md`'s scope fence). `ISS-036`'s operator action O3
enables these bytes live; this issue only lands and reviews them.

## Acceptance criteria and evidence

1. A test parses each workflow's YAML and asserts the trigger row and the
   seventeen-row permission array the receipt will bind.
2. `test/conformance/workflow-structure.test.ts` — whose only subject today
   is `.github/workflows/conformance.yml`
   (`test/conformance/workflow-structure.test.ts:11`) — is extended by this
   issue's implementation with assertions over these two new files; the
   already-registered `test:harness-workflow-mutations` command (`ISS-006`,
   `package.json:29`) then runs the extended file and passes against all
   three workflows.
3. The issue-form template produces the exact fenced `orchestration-work/v1`
   block shape `self-host-github-contract.md:3-10` requires; a test parses a
   fixture submission and rejects a malformed block.

## Verification plan

`test/conformance/workflow-structure.test.ts` currently parses only
`.github/workflows/conformance.yml`
(`test/conformance/workflow-structure.test.ts:11`); it has no coverage of
the two new BUILD/REVIEW files today. This issue's implementation extends
that same test file with assertions over the new bytes; the
already-registered `test:harness-workflow-mutations` command (`ISS-006`,
`package.json:29`) then runs the extended file, with no new root-script or
`implementedRootScripts` registration. The issue-form fixture parser
(`probes/self-host-github/test/workflow-shape.test.ts`, named in the
predicted footprint below) is a separate deliverable verified by its own
test; no currently registered command executes it, and this issue makes no
claim that one does. Local verification before landing is `pnpm typecheck`,
`pnpm format:check`, `pnpm planning:check`, and `pnpm planning:board-check`,
plus independent exact-head review. The hosted three-OS bootstrap
conformance run against these bytes is future verification, exercised only
once `ISS-036`'s operator action O3 lands them on a triggerable ref; that
live run is out of scope for this issue.

## Predicted footprint and dependencies

Creates `.github/workflows/self-host-github-probe-build.yml`,
`.github/workflows/self-host-github-probe-review.yml`,
`.github/ISSUE_TEMPLATE/self-host-work.yml`, and
`probes/self-host-github/test/workflow-shape.test.ts`. Extends
`test/conformance/workflow-structure.test.ts` with coverage of the two new
workflow files; the already-registered `test:harness-workflow-mutations`
command (`ISS-006`) is unchanged. No root script change. Blocked directly by
`ISS-054`, whose `repositoryProtectionPermissionNames`
export is the authority for the seventeen permission names and their order.
Consumed by `ISS-062`.

## Operator actions

None.

## Glossary impact

None; workflow permission census terms are registered already.

## Independent review packet seed

Attack a permission block that omits a name and therefore relies on an
implicit default the receipt cannot represent; a REVIEW workflow triggerable
by anything but a successful BUILD; `workflow_dispatch` or a `latest`-run
selector sneaking into either workflow; an issue-form field that admits
free-form executable instruction text.

## ISS-054 surface consumed

`repositoryProtectionPermissionNames` as the authority for the seventeen
names and their order.

## Does not claim

That the workflows run, that GitHub accepts them, or that a run-scoped token
can perform the reads inside them (that is `ISS-036`'s live acceptance
criterion 3, F3a/F3b).
