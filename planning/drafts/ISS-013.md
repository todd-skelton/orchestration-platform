---
key: ISS-013
title: "Define and validate the project adapter SDK"
labels: ["type:slice", "area:integrations", "priority:p1"]
milestone: "Minimum orchestration kernel"
parent: EPIC-ADOPTION
blocked_by: [ISS-003]
---

## Context and evidence

The engine must not know how a consumer represents work, readiness, CI,
deployment, priorities, or operator actions. See the platform/project boundary
in `docs/architecture/vision.md` and contracts from `ISS-002`.

## Scope fence

In: typed adapter compatibility, complete work-frontier snapshot, current
breaker-policy/trip facts, PROJECT and REVIEW step-6 preflight, two static
fixture adapters, and the public `project snapshot` handler/result plus their
closed schemas, envelopes, registry and census amendments.

Out: a specific code-host implementation, consumer policy, live credentials,
first-consumer cutover, dispatch allocation/resource mapping (`ISS-049`),
reviewed project mutation/full conformance (`ISS-050`), recovery/reclaim
(`ISS-051`), release/CI/deploy observations (`ISS-052`), and state export/import
hooks (`ISS-053`).

## Decisions already made

Adapters return facts and explicit mutation plans; the engine owns lifecycle
mechanics. Every mutation has a dry-run observation and idempotency identity.

ISS-003 supplies the replaceable static handler interface and leaves `project`
as a handler-free placeholder. This slice owns project result schemas and
their exact supported-parser registrations, the corresponding
`orchestration-command-result/v1` command/result mappings, and replacement of
only that command-family placeholder. Success must parse under the command's
registered project result schema before serialization; config result bytes
cannot stand in for project behavior. Unknown/future schemas, mismatched
command/results, and malformed registrations still refuse. Shared scaffold
changes name ISS-013 and update each registration and every census table
restating it together in one reviewed amendment, as required by the work model.

## Bounded snapshot proposal

The `Project snapshot literal ledger proposal (ISS-013)` in
`docs/architecture/contract-decisions.md` proposes the two literal public
schemas needed for the read-only fixture-adapter snapshot slice. It is an
independently reviewable proposal, not implementation or self-certification.
It closes configuration, exact compatibility, pagination, opaque frontier
facts, and every snapshot outcome; it does not complete the other outcomes
in this issue. No new issue, dependency, milestone, or consumer is introduced.

Plan/apply, allocation/reclaim, CI/deploy, breaker-policy definitions, module
authorization, and export/import remain in the existing issue scope but out of
this bounded proposal. A policy or provenance digest with no literal input
definition cannot stand in for them. The bounded CLI integration supplies the
required representation amendment under ISS-013 ownership: one implemented
project family handler, an exact `project-facts/v1` result-schema row for
snapshot, and unchanged schema-free plan/apply rows. The reviewed public
command census determines executable versus placeholder rows before submitted
metadata; missing schema/handler metadata refuses rather than downgrading.
Only the exact placeholder rows return their ISS-013 owner with zero handler
calls. Registration v1 and the static registry list remain unchanged. This
amendment requires the ordinary independent review before publication; it does
not implement or change plan/apply behavior.

The falsifiable prediction is that two public schemas plus closed inline
pagination/row shapes suffice for both fixtures through the same SDK path,
without a descriptor, plugin registry, open payload, runtime loading, state,
process, or provider credential. Review this prediction before the first
snapshot implementation packet: a need for another layer or a new product
policy is replan evidence, not permission to grow the ledger silently.

| Mechanism | In-scope threat / deletion test | Smallest credible alternative and choice |
| --- | --- | --- |
| Closed config and exact compatibility | unsupported adapter receives a call; delete pre-call admission | retain comparisons against reviewed source constants; a descriptor registry is larger |
| Bound complete pagination | missing/mixed pages appear as no work; omit terminal or digest check | retain one observation ID, cursor census, and digest of the literal frontier array; no receipt/history protocol |
| Complete failure union | unknown facts become empty success; replace failure with an empty array | retain three snapshot states and fixed CLI diagnostics; no generic failure payload |
| Mixed command family                  | missing implementation metadata becomes a placeholder; delete snapshot schema/handler | use existing result-schema and command-owner census; no new version or row-state field                          |
| Fixture-only read path | source-control assumptions enter engine contracts; remove second fixture | retain two fixtures through one SDK; live provider and all mutation machinery stay out |

Future implementation verification must include these executable-test-shaped
cases; this documentation proposal claims none has run:

| Case | Input/control | Required observable evidence |
| --- | --- | --- |
| Exact schema/canonical bytes | both families; every arm; missing/extra/null/future fields; hostile records/arrays | exact golden bytes/digests or total refusal without invoking input code |
| Compatibility before reads | unsupported engine/adapter/schema/capability or loaded-config mismatch | named failure and zero adapter callbacks |
| Actual snapshot path | two distinct fixture adapters, same SDK and canonical argv | exact multi-page frontier/result envelope; second fixture has no branch/worktree model |
| Empty versus missing | terminal empty page; unavailable/unknown; dangling/repeated cursor; missing page | only proven empty census succeeds; exact failure state/exit otherwise |
| Binding and bounds | moved config/observation/digest; duplicate work; oversized rows; timeout/late promise; structurally valid second COMPLETE page with a wrong valid observation UUID and repeated prior nextCursor `a` | terminal refusal/unknown/unavailable, no later call and no replacement result; combined fault is exactly UNKNOWN/CHANGED_FRONTIER with deterministic canonical result bytes/digest |
| Eligibility | READY, NOT_READY, UNKNOWN, UNAVAILABLE rows; capability substitution | complete known facts or named failure, never silently filtered work |
| Public integration | config substitution, wrong command, failed facts as success; unchanged plan/apply/config commands | closed parser/envelope/census agreement after the separate registration prerequisite |
| Portability | identical fixture transcripts under Windows, macOS, Linux | identical canonical facts/digests and no state writes, process launch, or provider calls |

The initial ledger proposal changed only this draft and the architecture
ledger. The CLI integration additionally amends the predeclared adapter handler
and declaration, CLI registry declaration/dispatcher/serializer, existing shared
handler failure type in `packages/config/src/config-command.ts`, public command
result/owner census, ISS-000 bootstrap verifier and manifest, and their focused
tests. One `packages/adapter-sdk/src/project-command.ts` reads the canonical
adapter data file and statically composes the existing two SDK fixture wrappers.
It reuses the successful configuration provenance projection; SDK path admission
is not expanded. Relative adapter paths use invocation cwd; ordinary regular
file reads are capped at 65536 bytes before parsing and handles close on all
paths. No root containment/reparse authority is inferred from this data file.
The host reconciles the board from this exact draft adjacent to its reviewed
merge. All other ISS-013 outcomes remain incomplete.

## Bounded current-policy/trip proposal

The `Project current-policy/trip literal ledger proposal (ISS-013)` in
`docs/architecture/contract-decisions.md` proposes one additional closed public
fact family and inline SDK request/response records for ISS-025 and routine
step 3. It binds the exact admitted configuration/snapshot, a statically
composed policy version, a fresh current-source read, and a complete capability
decision census. Two fixture adapters evaluate different trip thresholds over
the same neutral frontier. No new issue, edge, milestone, provider integration,
CLI command, module authorization, config payload, or production authority is
introduced. Snapshot behavior and its existing public mappings remain unchanged.

This proposal excludes generic breaker history, open receipts, recovery
transactions/probes/receipts and all hold clearance. Those literal contracts
remain UNDEFINED under ISS-025; only afterward can ISS-013 define the complete
project recovery-observation binding. An opaque digest is not a verified
receipt substitute. `NO_TRIP` is not recovery, and this packet does not complete
AC7 or routine step 3. Allocation/reclaim, plan/apply, other project observations,
and state export/import remain outside this bounded packet.

Predicted footprint: this draft and the architecture ledger only. A later
implementation would add the one fact parser/serializer under contracts,
the in-process reader and existing fixture extensions under adapter-sdk, and
focused compatibility/SDK fixtures. It must name any necessary public schema
census updates together; this proposal does not authorize registry edits or
CLI publication. The falsifiable prediction is that one public union, the
existing frontier shape, static policy versions, and one bounded callback
suffice without a policy-payload framework or generic breaker-state changes.
Review by the first implementation packet; needing another authority surface
or receipt is replan evidence, not permission to silently widen this slice.

| Mechanism | In-scope threat / deletion test | Smallest credible alternative and choice |
| --- | --- | --- |
| Exact configuration/snapshot/policy bindings | substituted input appears current; remove one comparison | reuse existing literal config/frontier and source constants; no descriptor or policy digest |
| Returned current frontier and complete decisions | stale/partial source or omitted capability looks permitted; replace frontier with an unverified digest or omit a decision | one bounded literal source read and exact census; no source receipt/history protocol |
| Closed failure union and callback bound | failure or late result becomes permission; delete a terminal check | one callback, fixed deadline, fixed reason priority; no retries or provider timeout policy |
| Recovery exclusion | NO_TRIP or opaque open digest clears an existing hold; infer recovery from either | defer recovery observation to actual ISS-025 receipt contracts; no permissive partial recovery arm |

Review attack surface and cheapest discriminating evidence: every union-arm
canonical golden; config/snapshot/policy/echo mutation with zero-call admission
canaries; empty-frontier requests carrying the exact admitted capability census
(`[]` accepts no decisions; `["work.read"]` accepts one NO_TRIP decision; swapping
them refuses); identical-frontier 0/1/2 NOT_READY policy contrast; sorted missing/extra
and malformed decision censuses; fresh-source change; combined-fault priority;
4999/5000 ms and late-settlement controls; three-OS byte parity and a vocabulary
scan. The ledger gives exact expected outcomes. This documentation packet
claims no implementation tests or lifecycle evidence. The host owns serial
verification, independent exact-head review, and adjacent draft/board
reconciliation; authoring or committing the proposal is not certification.

## Bounded recovery-observation literal prerequisite proposal

The sixth literal group in `contract-decisions.md` proposes the complete
inline recovery-permission request/response against actual generic breaker
receipt, transaction, policy and snapshot identities. This explicitly follows
the earlier current-policy proposal's recovery exclusion; it leaves the
public TRIP/NO_TRIP family unchanged and does not reinterpret NO_TRIP as
permission. These project observations remain ISS-013-owned. The paired
generic opening/start/completion/checkpoint bytes are proposed in the same
packet so an opaque receipt digest cannot replace an undefined preimage.

This is data-only prerequisite closure. Actual fresh-source reads, callback
admission, bounded adapter-defined probe behavior, fixture recovery predicates
and policy version changes require their own reviewed source before execution.
Existing trip-only fixture policies do not silently gain recovery semantics.
There is no new public family, CLI, provider, SDK callback or authority grant.
Acceptance requires independent exact-head review plus the packet's full
scope/identity/cross-outcome and malformed-observation vectors; host document
verification precedes code. Footprint is this ownership note and the shared
bounded ledger/round-387 packet. No tests, probes or AC7 completion are claimed.

## Bounded step-6 preflight literal prerequisite proposal

The `Project preflight literal proposal (ISS-013)` in `contract-decisions.md`
closes only the existing `project-preflight/v1` family for the started ISS-041
consumer, after its complete module/action and route contracts. It retains
step 6 for both SELECTED worker routes and declared NO_WORKER actions. This
is an author proposal pending independent review, not parser/runtime delivery
or completion of this issue's acceptance criteria.

Scope is the full public eligible/refused/unknown result, one closed inline
current-observation union reusing project facts and concrete immutable review
subjects, complete canonical identities, and the actual supplied module/action/
mapping/route/observation relation. Ordinary work conservatively requires an
unchanged whole frontier: unrelated changes produce FRONTIER_CHANGED rather
than silently trusting the old module choice. This explicit replan avoids a
second module invocation or arbitrary dependency-expression evaluator. Review
targets retain their concrete source namespace and exact designated identity;
carrying candidate references does not grant adapter release authority.

Non-goals are SDK callbacks/services, provider reads, CLI publication, new
public families, allocation/ownership, dispatch, mutation, private journals,
breaker permission, source/materialization authority and ISS-026 work. The
existing snapshot SDK and source/subject owners remain responsible for fresh
observations and actual retained preimages. A supplied coherent tuple is only
structural evidence; later dispatch still refuses multiply-owned resources
and revalidates immediately before ownership/launch. NO_WORKER skips neither
preflight nor the later disposition/mutation gates.

Acceptance evidence after review is the ledger's complete shape/null/reason
matrix, independent canonical bytes/full frames/digests and inline hashes,
supplied-reference mutations, ordered target/census bounds, deterministic
combined-refusal priority, and worker/workerless fixture continuation controls.
Verification is host-owned targeted formatting/planning/whitespace checks and
ordinary independent exact-head review before code; later parser compatibility
and consumer execution remain separate. No tests or runtime outcomes are claimed.

Predicted footprint now is this note, one bounded ledger subsection, the
minimal routine step-6 unknown qualifier and round 395. Later parser work is
one family/helper/hash/relation plus focused tests and inseparable registration/
census/harness updates under ISS-013; no CLI command mapping changes. Attack
surface is stale/partial/foreign observations, coordinated action/route/target
substitution, missing-to-empty conversion, chosen-row-only checks, source or
candidate authority inferred from digests, and workerless preflight bypass.
One existing family plus closed inline reuse is the smallest proposal; needing
new resource/release authority or a larger service is replan evidence, not
permission to extend this packet. Host draft/board reconciliation follows review.

## Bounded worker-result review preflight implementation increment

Round424 follows the started `consumeReview` step-6 consumer after the PROJECT
SDK extraction. The fixed review source already rereads a retained concrete
worker-result subject before dispatch; this increment moves only its admission,
fresh observation and public project-preflight/v1 binding into the SDK. Exact
current identity remains ELIGIBLE consistency evidence, a valid changed subject
is TARGET_CHANGED, and unavailable or malformed source evidence remains typed
SOURCE_UNKNOWN. None is review acceptance, dispatch permission or mutation
authority. The final-cycle copy remains a later bounded consumer migration.

The tempting two-fixture mutation slice is not runnable yet. Existing consumers
do not issue APPLY/PROJECT, the two fixture adapters define no reviewed effect
mapping or transaction/progress owner, and the predeclared plan/apply CLI inputs
omit required public relation preimages. This increment therefore adds no
mutation, allocation/reclaim, command, conformance, recovery, provider,
credential, journal or release path. It changes SDK preflight source/tests, the
existing review consumer, this note and round424 only; roadmap and board state
stay unchanged.

Acceptance evidence is zero source calls before complete review/route admission,
exact full-subject current/changed outcomes, typed rejected/malformed/thenable
source uncertainty, project/policy observation-ID reuse refusal, retained
input/action/mapping across the source await, and the existing review consumer's
canonical tuple, changed-target no-allocation and outside-manifest controls.
Verification is focused SDK/review-consumer tests, typecheck, format, planning,
clean diff, later independent exact-head review and three-OS CI. Review should
attack digest-only targets, source/path substitution, callback assimilation,
identity/time reuse, route mutation and any inferred review authority. No
acceptance or issue completion is claimed by this authored packet.

## Final-cycle worker-result preflight migration

Round425 consumes the remaining live step-6 REVIEW preflight copy in
`consumeFinalReviewCycle` through the Round424 SDK boundary. The caller passes
its already admitted module input, action, fixed mapping, selected route, fixed
current-subject read, deterministic identity source and existing clocks. Exact
current evidence remains ELIGIBLE; a valid changed subject is retained and
terminalizes step 6 as REFUSED/TARGET_CHANGED, while malformed or absent source
evidence terminalizes as REVIEW UNKNOWN/SOURCE_UNKNOWN without a retained
subject. Invalid or reused observation identity refuses before TERMINAL:6.
Every non-eligible row stops before step 7 or any dispatch, worker, review or
mutation effect and retains the uncertain fixture session.

The bounded five-file footprint is final-cycle source, accepted-cycle and
negative-control tests, Round425 and this note. Evidence is the unchanged exact
30-event/15-step replay with an explicit step-6 public binding, changed-source
and unavailable-source terminal censuses, identity refusal before output, and
the retained seeded-author dispatch-collision control. Review should attack
changed-to-UNKNOWN conversion, missing tuple members, extra/missing retained
subject evidence, identity sequencing, post-refusal files and any authority
inferred from consistency. SDK callback/thenable/route/detachment vectors stay
in Round424 rather than being repeated.

This migration adds no public contract, journal schema, release-candidate path,
mutation, allocation/reclaim change, recovery, command, provider, credential,
release or effective authority. It does not complete ISS-013, change roadmap or
board state, or self-certify; focused author checks require independent review
and hosted CI.

## Bounded dispatch resource/materialization ownership note

Round 398's three-family routine proposal preserves ISS-013 ownership of actual
project allocations, source/subject materialization and later absence. Its
closed resource intents precede launch allocation claims under one dispatch
attempt/owner transaction, with an exact full census even on partial failure.
The brief footprint and caller empty arrays prove neither allocation nor
absence; host-owned resources remain with the selected host. Reclaim is a
separate complete step-14 family, never inferred from worker exit.

Bounded rendered input/stdout/stderr references identify retained raw bytes,
not a validated immutable worker subject or completed review. This packet adds
no adapter allocation/reclaim callback, resolver, SDK service or mutation
family. The shared ledger/round 398 states the scope, explicit chronology
replan, vectors, footprint and authority attacks. This note claims no tests,
PASS or ISS-013 acceptance; independent review precedes code and actual owner
admission remains mandatory.

## Bounded disposition operation/target ownership note

Round 401 closes disposition/follow-up claims and preserves ISS-013 ownership
of actual base/result materialization and project mutation request/plan/apply.
The original opaque action target is not equated to the later worker-result
identity; the actual adapter/materializer must prove that source relation.
APPLY/PROJECT remains a request for step-12 owner planning, never permission to
mutate. Release ASSEMBLE_CERTIFY/PROMOTE choices and successor verification are
structural closure only; they do not add project SDK release authority.

Explicit nonmutating COMPLETE and real no-mutation skips do not replace
project-mutation-request/v1, project-mutation-plan/v1 or project-apply-receipt/v1
in the full public census, or clear resource ownership. The ledger/round 401
states the complete target/outcome matrix, scope/non-goals, vectors, footprint
and review attacks. No SDK/service, mutation, tests, PASS or acceptance
completion is claimed; source and runtime admission remain separately gated.

## Reclaim owner observation proposal

Round405 consumes the full round398 dispatch and round404 mutation allocation
censuses without treating changed project resources as temporary allocations.
Its explicit v1 replan reuses complete4096-byte ABSENT/PRESENT values for known
managed-resource before/after observations; unsupported workspace semantics
remain UNKNOWN. Actual ADAPTER source must prove the complete physical mapping,
ownership, fresh admission, deletion/read-back and retained evidence. No generic
path/resolver, SDK reclaim callback or absence authority is introduced. The
ledger/round405 states the complete scope, vectors, footprint and review attacks;
no implementation, test PASS or ISS013 acceptance is claimed here.

## Option-B completion split and dependent audit (2026-09-01)

The owner-selected Option B moves every outcome without a live round-425
consumer to explicit follow-ons. `ISS-049` starts only after dispatch `ISS-008`;
`ISS-050` after independent review authority `ISS-009`; `ISS-051` after
supervision/recovery `ISS-030`; `ISS-052` after promotion `ISS-014`; and
`ISS-053` after installed N0 state `ISS-020`. Their thin registrations do not
grant authority or invent consumers. The representation proposals retained
below are history and input for those owners, not remaining ISS-013
implementation scope.

The four existing dependents consume only the landed subset. `ISS-011` needs
neutral adapter compatibility, snapshot/preflight facts, and static references
to package modules; it emits plans but does not apply project mutation.
`ISS-025` consumes the landed current-policy/trip surface while owning the
generic breaker/recovery lifecycle; adapter recovery execution follows the
supervisor in `ISS-051`. `ISS-036` is the live GitHub probe that produces
repository mutation/CI/deploy evidence; it does not require a generic callback
to exist before measuring that provider boundary. `ISS-028` fills a static
consumer composition skeleton and deliberately leaves mutation/import/shadow
extensions for later owners. No dependent needs an unstarted ISS-013 outcome,
so their existing `ISS-013` edges remain correct and unblock when this split
lands and ISS-013 closes.

## Acceptance criteria and evidence

1. Snapshot and current-policy readers exercise complete, unknown, unavailable,
   changed and malformed states through two behaviorally distinct fixtures.
   Evidence: landed state-enumeration and source-freshness tests.
2. Compatibility negotiation rejects unsupported engine/adapter pairs before
   external reads or writes. Evidence: canary adapter asserting zero calls.
3. PROJECT preflight consumes a fresh complete project observation and binds the
   exact module/action/mapping/route tuple; changed or unavailable facts stop
   before dispatch. Evidence: landed SDK and walking-skeleton controls.
4. Both live REVIEW consumers use that same SDK boundary; exact current,
   changed, malformed, unavailable and reused-observation paths preserve their
   public result and stop before step 7. Evidence: Round424/425 tests and exact
   final-cycle replay.
5. `@orchestration-platform/adapter-sdk` replaces only the `project snapshot`
   placeholder and admits its public result through the closed envelope.
   Evidence: registry/census agreement, typed snapshot goldens, and command/
   result/schema substitution controls. Plan/apply remain explicit placeholders
   owned by `ISS-050`.
6. Every unstarted outcome has exactly one consumer-gated follow-on registration
   and is absent from this issue's completion claim. Evidence: roadmap/draft/
   board synchronization and transitive-reduction checks.

## Verification plan

`pnpm --filter @orchestration-platform/adapter-sdk test`

Include public-contract compatibility/golden and CLI envelope regression
evidence for the snapshot and preflight rows. The owner-bearing
`adapters:conformance` placeholder moves to `ISS-050` and is not activated by
closing ISS-013.

## Predicted footprint and dependencies

The landed subset occupies existing `packages/adapter-sdk/` snapshot,
current-policy and preflight sources; the static snapshot CLI handler; their
closed contract/envelope/registry rows; two fixture consumers; and focused
tests. This split adds planning only. The five follow-ons own all future
allocation, mutation/conformance, recovery/reclaim, release observation, and
state-transfer source. ISS-013 remains blocked by ISS-003 and unblocks its four
existing dependents on closure; no new source, command, schema, or glossary edit
is authorized here.

## Operator actions

None.

## Glossary impact

None; adapter vocabulary is registered already.

## Independent review packet seed

Attack an adapter smuggling policy into engine state, apply without fresh dry
run, compatibility checks after external calls, partial frontier pagination,
unknown authority coerced to empty, result-schema substitution, and drift
between the project registration, public envelope and census. Use the complete
adapter call/state matrix.

## Complete project mutation representation (round 404)

Close project-mutation-request/v1, project-mutation-plan/v1 and
project-apply-receipt/v1 together before complete public event/journal parsing.
The bounded proposed v1 operation is a compare/replace of complete small
adapter-owned byte resources, with explicit absent/present values and all
before/after bytes. Opaque IDs are source-mapped within the actual adapter
configuration, never paths, commands or authority. Partial projections and
omitted effects refuse; unsupported source semantics require replan.

The request binds the full actual APPLY/PROJECT disposition/context, immutable
target and an owner-issued stable transaction. Dry run binds a fresh complete
project/resource observation and every planned effect. Apply recomputes the
exact plan/CLI plan ID, immediate complete before state and actual postimage;
known no-write refusal stays distinct from partial/uncertain effects. Repeated
committed work returns identical evidence, never another application. Counts,
IDs and canonical records do not prove live permission or current progress.

This changes only literal representation, not SDK execution or CLI availability.
ASSEMBLE_CERTIFY and PROMOTE remain release-owner choices refused by the project
binder. Independent review, full compatibility cells, source/effect mapping,
materialization/review, actual freshness/credential/transaction authority and
step14 reclaim remain required. The nonmutating ISS041 path does not waive
complete parsing of these three families. No issue completion is claimed.

The plan also retains the complete ADAPTER-owned managed resource-intent census;
all apply arms retain matching round398 allocation claims keyed to the mutation
transaction. These allocations are distinct from the project resources being
changed. Partial/unknown apply cannot omit them before step14 reclamation.
