---
key: ISS-004
title: "Implement atomic state, locking, and recovery primitives"
labels: ["type:slice", "area:runtime", "priority:p1", "risk:authority"]
milestone: "Portable substrate"
parent: EPIC-RUNTIME
blocked_by: [ISS-022]
---

## Context and evidence

`ISS-002` defines pure pointer/history/run contracts; `ISS-022` must prove the
kernel exclusion and custody premise. Receipts do not authenticate writers.
Production authority is the selected epoch, canonical custody, a revocable
in-process capability, and the fixed OS lock. The proportionality review
(pressure round 34) removed the sparse trees, node-inventory census, and
materialization coordinator this slice formerly implemented; authority history
is now one hash-chained append-only log.

## Scope fence

In: fixed `installation/state-mutation.lock`, singleton in-process state service,
private WeakMap/nonces and purpose-scoped RPC handles, twelve-kind registry,
generic value/proposal/conflict/tip/tombstone/archive/retention storage,
run-current META_LEAF/checkpoints, linear authority-history appends and
full-chain verification, deterministic mutation/reconciliation, authority
rotation, exact-byte CAS/readback, crash recovery, and canonical
unsupported/unknown results.

Out: record schema ownership, process policy, broker/release production,
distributed/network locks, leases/PIDs/timeouts as authority, and any fallback
not selected by a new replan.

## Decisions already made

The exact lock/RPC sequences, twelve pointer kinds, Dv/Dr/Dt graph, linear
authority-history chain, generic META_LEAF storage, single-epoch commit runs,
closed ORDINARY/AUTHORITY_ROTATION commit evidence, packet purposes, proposal classification, canonical paths, tombstones, and
epoch crash semantics in `supervisor-contract.md` as amended by
`contract-decisions.md` are mandatory. The old capability performs rotation,
appending the chain record and then performing the authority CAS as its final
action; its run-current journal legitimately rests at CAS-armed. All old
handles revoke at selection. Prior selected plus the exact matching
head-plus-one record is resumable under the old epoch; successor selected plus
the exact chain record and old CAS-armed checkpoint derives `SELECTED`; every
other combination is `UNKNOWN`, with no post-CAS new-epoch write. Bootstrap destination owner,
anchor, genesis, and reinstall are external ISS-020 operations.
ISS-004 consumes the literal simplified-authority schema ledger in
`supervisor-contract.md`; it may not translate `Dsc`, `Drot`, `Dh`, or
`Dcommit` prose labels into locally named JSON fields. It writes history only
as `authority-history-record/v1` at the ordinal path and hashes it only under
the distinct `authority-history/v1` domain.

## Acceptance criteria and evidence

1. Cross-process races choose one selected tip and classify every competing
   proposal from actual evidence; same mutation+bytes retries idempotently and
   same instance/different bytes refuses. Barrier-controlled tests cover every
   mutation boundary and source/path collision.
2. Historical and mutation callbacks have different runtime handle classes;
   copied/serialized/proxied/foreign/stale handles refuse. Every callback
   revokes on completion, release, crash, custody movement, or rotation.
3. Ordinary commits select acyclic run-current checkpoint cores before/after
   target CAS, validate authority/custody, and terminalize exactly. Every fault
   boundary yields selected prior/successor or explicit recoverable ambiguity;
   no syscall outcome is guessed. They retain all nine stages and the exact
   selected/lost/unknown ordinary resolution union.
4. Rotation under the old capability proves the new reviewed active release and
   helper/profile/ABI/custody, resolves every pending proposal among the other
   eleven kinds, requires zero unrelated PENDING/UNKNOWN, appends the exact
   chain record binding shared `Dsc`, predecessor head, retiring
   `Dp/Dt/Dv/Dr`, derived `Drot`, and next safe-integer ordinal, and
   performs the authority CAS as its final action with verified readback,
   resting at CAS-armed while old handles revoke at selection. Terminal truth
   is derived from prior/successor selection, exact chain record, and the old
   CAS-armed checkpoint; all other combinations are UNKNOWN and no new-epoch
   write terminalizes the old run. Rotation commit evidence binds the old
   selected checkpoint 5, expected successor `Dv`/target mutation/head/record,
   and old/successor/empty authority slot. Checkpoints 6–8, ordinary resolution,
   later selector artifacts, and successor-epoch writes refuse. A crash between append and CAS resumes only
   via the same transaction under the old capability re-driving the same
   CAS; the pending record is the sole permitted head-plus-one excess.
   The runtime validates the ledger's exact direct-field union and
   outcome-to-slot equality before exposing the resumable CAS or successor
   evidence; structural packet validity alone is insufficient.
   Rotation-v-target races have one linear order; candidate self-rotation,
   rollback, wrong custody, mixed epoch, chain fork, and ordinal gap refuse.
5. Reservation selection reuses one winner per predecessor triple, resolves UUID
   conflicts, and enforces RESERVED→CONSUMED→TERMINAL→TOMBSTONE with chained
   safe-integer ordinals that refuse above the bound. The CAS-selected
   reservation is durable before process creation and carries the descriptor
   inputs; the descriptor exists only as LIVE and binds it.
6. Terminal removal archives first and selects durable tombstone; crashes on
   every side of archive/tombstone preserve one authority. Bare absence after
   genesis and unauthorized archive selectors refuse.
7. Authority history and run audit are FULL_REQUIRED. Eligible retention
   compaction requires selected checkpoint/plan/exact deletion/completion,
   never compacts pending, never touches the authority-history chain, and
   emits durable `AUDIT_DEGRADED` only for the allowed old-terminal-loss case
   and exact operation matrix.
8. Unsupported filesystems, lock helpers, or custody guarantees refuse before
   mutation; no PID, age, lease, timeout, symlink, unsigned lock, or silent
   fallback is accepted.
9. Chain verification walks every record from genesis while holding the lock,
   constructing each record's canonical ordinal-derived path with no directory
   enumeration: injected, missing, reordered, truncated, or digest-mutated
   records and a head whose ordinal or digest mismatches the walked result
   all refuse. Constructed-path absence probes prove head-plus-one is absent
   or matches the armed rotation intent and head-plus-two is absent. Scale
   runs reference the ISS-006-published 1,000-record, five-seconds-per-OS golden
   and budget. Checkpointing remains parked until this measured gate fails.

## Verification plan

`pnpm --filter @orchestration-platform/state test`

`pnpm run test:state-fault-matrix`

## Predicted footprint and dependencies

Owns predeclared `packages/state/`, `test/fault-injection/state/`, and exact
ISS-004 capability slots. Directly blocked by the passed `ISS-022` decision;
`ISS-002` is already a transitive prerequisite.
No contracts, root wrapper, planning, glossary, or consumer package edits.

## Producer and consumer ownership

This issue produces the shared runtime state service, live capabilities,
authority-history ROTATION appends and full-chain verification, single-epoch authority runs,
run-current journal, and pointer mechanism. ISS-002 owns their schemas.
ISS-014, ISS-020, ISS-030, and ISS-032 request typed transitions; they do not
implement runtime CAS. ISS-020 separately implements external
destination-owner/anchor CAS. ISS-006 consumes goldens; ISS-022 owns proof,
not production code.

## Operator actions

None.

## Glossary impact

None.

## Independent review packet seed

Attack lock inheritance/owner death, copied or revoked handles, read-vs-mutate
capability confusion, epoch/head change at every read, chain-record forgery,
META_LEAF recursion/non-generic paths, every checkpoint/CAS crash, fake loss,
pending rotation, path collisions, tombstones, candidate self-authorization,
old-epoch post-CAS mutation, forged derived rotation terminal truth,
unresolved pending head-plus-one records, and injected/missing/reordered
chain records.
