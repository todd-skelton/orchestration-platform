---
key: ISS-171
title: "Reduce unresolved-seed queue fixture Git setup under the unchanged Windows gate"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

Independent brief DELTA PASS (r6); awaiting planning landing/registration, NOT READY. #525: OPEN/type:slice, markerless intake; learning note filed. Milestone 8 (M2). ISS-170 is reserved for DB qualification; canonical ISS-166 owns operator handoff. Platform-native implementation only; one bounded author performs the causal comparison and repair. No retry, merge, ready, executor-install or #515/#457 resume authority is granted.

[PR #519 Windows bootstrap run 35154815135/job 104991626151](https://github.com/todd-skelton/orchestration-platform/actions/runs/35154815135/job/104991626151) failed 2026-09-16T22:25:19Z: queue.test.ts:2469 unresolved-seed successor matrix, `spent-legacy-correction` and `correction`, each 30000ms timeout. 845 pass / 2 fail / 13 existing OS skips; 1855.70s total. ROOT=`D:/Users/ToddS/Source/Repos/chase-sets`; captured `ROOT/.orchestrator/artifacts/platform-519-windows-35154815135.log` lines 190-220,750-780. Ubuntu/macOS green is host intake, not a fresh API probe in this plan.

Historical evidence branch `codex/platform-native-db-registration` @ `d766c0ad6744040194bc154ff4fe88a16fb5bd6f`; then `origin/main`=`1a819abe826b3e7f0ffe8e11a6658da4ad650aaa`. Diff was five planning drafts plus roadmap only; runtime/tests/config/docs identical. #519 is now merged `b2c55706` from `46ae637`; host reports three-OS run 35166479753 SUCCESS and unchanged-head Windows retry 35154815135 attempt 2 passed. Original failures remain history. Read only; never reuse its review/gates/runtime records for the repair. No stopping-review finding IDs or terminal-park receipt was supplied. No repeat retry authority or #519 action.

Don't-rebuild check: ISS-160/#509 already ships file-serial/one-worker tests; no further serialization. Accepted ISS-169 owns refresh.test.ts's earlier #519 run 35138686485 failures and stays unchanged. This issue owns queue.test.ts, not refresh recovery or DB planning. Existing queue lifecycle/real-Git adapters and all 16 matrix tuples remain the test subject.

## Don't-rebuild pointers
- `test/dogfood/queue.test.ts`
- `test/dogfood/refresh.test.ts`
- `planning/drafts/ISS-169.md`
- `vitest.config.ts`
- `docs/loop.md`

## Decisions already made
Native authority remains as defined in `docs/loop.md`. Test fixtures only; retain 30000ms test/hook timeout, serial files and all existing OS skips. No product ruling needed within this fence. Host preserves #515/#457, executor and historical budgets. This is an independent fixture repair, not an ISS-169 amendment or a #519 recovery decision.

## Scope fence
Only `test/dogfood/queue.test.ts`, its local loopFixture/stoppedConflictFixture setup and directly owning test assertions.

Non-goals: runtime/source/config/workflow/gate changes, Git mocks, cached runtime HEAD/status/ancestry, dropped assertions/checkpoints, prebuilt shared repositories, new framework, relocated untimed hooks, artificial delays as causation, timeout/skip/concurrency changes, hidden new retry budgets, WSL executor or canonical-checkout changes.

## Concrete candidate
`loopFixture()` lines 317-321 runs git init, two separate repository-local user.name/user.email config processes, add and commit. The stoppedConflictFixture at 2218 calls it once per matrix tuple. A bounded candidate is to write the same fixed `[user]` stanza to the freshly initialized PRIVATE fixture's `.git/config` using existing filesystem helpers instead of those two subprocesses; preserve all generated config bytes and effective identity, never touch user/global config. Real subsequent Git commits still consume persisted local config. Static estimate: two fewer setup Git processes per tuple, 32 across 16 tuples; timing/speedup unmeasured. Verify identity/config equivalence and full queue helper siblings. No moved-main updater change is necessary to address the two failing modes.

Do NOT copy ISS-169's initial-head repinning optimization blindly: this fixture creates actual feature/main divergence, failed merge, marker seed, preserved worktree and bare remote. Their commits, checkouts, rev-parse and failure checks establish different real states and are not demonstrated redundant. Dominant cost is UNKNOWN. The same native author may choose a smaller supported setup reduction inside this file after counting, but must deliver measured removed work and repair, not stop with a diagnosis-only success. If savings cannot qualify the unchanged gate, report the concrete cost/semantic contradiction and missing bounded remedy; no magical speedup claim or widened authority.

## Done when
1. **AC1 Semantics.** Preserve all 16 unresolved-seed successor tuples and owning siblings, real private repos/worktrees/remote, actual divergent main/reviewed parents and unaccepted seed, immutable retained bytes, attempt 2 and spent modern/legacy allowance behavior, one source launch on replay, independent exact-head DELTA, no-op/lost-commit/refusal paths, stale review/receipt refusals, moved-main/new-conflict paths, correction-author/reviewer counts, no unauthorized publication, lost publish/merge reconciliation, exact gate order and once-only deployment. Evidence: unchanged assertions plus full queue.test.ts at unchanged 30000ms, with effective local Git identity comparison for the setup rewrite.
2. **AC2 Actual reduction.** Capture same-host baseline/candidate per-mode Git subprocess counts and setup/lifecycle/total timing, including both failing modes and all 16 tuples; temporary instrumentation includes underlying real adapter Git calls and is not a shipped runtime change. Removed work must not reappear in hooks/cleanup. Keep a reverted-intervention comparison for the two formerly failing modes to distinguish reduced setup cost from run noise; deterministic hosted timeout reproduction is not presumed. Evidence: exact-head before/after/control table, operation diff and retained-state/assertion equivalence. Smaller process count is evidence of work reduction, not by itself causal proof of timeout resolution.
3. **AC3 Delivery.** Independent exact-head review and actual Windows/Ubuntu/macOS hosted bootstrap all green at the final repair/integration head, including full Windows owning tests with unchanged timeout/serialization/skips. Evidence: head/run/job/step/timestamps/conclusions and per-case result capture after execution. Focused/WSL/aggregate success, skipped jobs and #519's separately authorized unchanged-head retry cannot substitute.

## Verification plan
Authorized native author only, in its own worktree. Preserve the first failure; baseline and candidate use the same commands/host/profile. Focused matrix then whole owning file:

```powershell
node node_modules/vitest/vitest.mjs run test/dogfood/queue.test.ts -t 'runs the unresolved-seed successor through native lifecycle'
node node_modules/vitest/vitest.mjs run test/dogfood/queue.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
```

Direct heavy invocations require machine admission before command bodies. The one full local gate is `& "$ROOT/.orchestrator/invoke-heavy-verifier.ps1" -Gate test -Worktree $Worktree -Lane $Lane`, foreground/exclusive. No redundant full battery; hosted bootstrap runs the existing verify:bootstrap profile. These are author obligations, not runs performed by this planner.

## Footprint & chain
Implementation: only `test/dogfood/queue.test.ts`. Callers: loopFixture and stoppedConflictFixture's matrix, conflict-status/refusal/retained-projection siblings; full owning file verifies shared fixture setup. Planning PR: `planning/drafts/ISS-171.md` plus one M2 row in `planning/roadmap.json`; no ISS-169/170 dependency. Independent brief review, learning note and unready intake #525 are complete; do not duplicate it. Host lands the exact draft/M2 row through the permitted planning-only path used for #519, then reconciles #525's canonical planning-key, source link/verbatim body and roadmap-backed project registration and runs `pnpm planning:board-check`. Explicit readiness follows executor/admission gates, then native selection, repair, independent exact-head review and final repair-head three-OS gates. No automatic readiness. #519's retained failures and single separately authorized unchanged-head retry (4388/5705601408) remain external history, not this issue's authority or evidence.

## External authority probe & evidence timing
The captured failed log is lifecycle evidence at Windows test execution, not proof of machine contention. Final hosted acceptance needs job/step execution at the final head, captured after completion and rechecked before landing. No implementation depends on a new external API shape; no live probe authorized for this planner. Preserve observed failure facts and distinguish host-provided Ubuntu/macOS conclusions from locally captured Windows results.

## Defect-class constraints
`vitest-wall-clock-timeout-under-parallel-host-load`: load attribution requires unchanged isolated timing collapse AND complete one-worker/file-serial gate, not Linux success or plausible Git cost; preserve failure/no timeout/product/skip edits. `parallel-node-verifiers-exhaust-host-memory` / `heavy-verifier-lock-survives-forced-wrapper-termination`: one admitted owner, no bypass/ambiguous lock steal/unrelated process kill. `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`: unchanged source and focused green do not exonerate candidate or qualify full suite. `lifecycle-repair-misses-sibling-retained-state-entrypoint`: preserve every replay/refusal/checkpoint and retained-state sibling, not only correction success. `stale-review-bound-to-wrong-head`: fresh independent exact-head review; push invalidates prior authority. `mandated-external-assertion-contradicts-authority` / `trigger-job-masks-real-deploy`: actual job/step conclusions, no real identity paired with invented facts. `unrunnable-verification-command`: manifests/scripts resolved at pinned head; no arbitrary brief command execution in readiness.

## Operator actions
Host owns the learning note, unready intake, planning-only landing, registration/board reconciliation and admitted verification window. Native loop exclusively owns runtime implementation/review and PR publication/integration; one bounded native author has no push/merge authority. No incumbent runtime lane or launch exception. No retry or #519 action is granted; no provider credentials.

## Review packet seed
Full-path invariants: real Git checkpoint/head authority and spent allowances unchanged. Failure modes: lost local identity, configuration rewriting collateral, stale-head cache, removed failure checkpoint, time shifted outside measured case, incidental green mistaken for repair. Evidence packet: exact heads, one-file diff, all-16 count/timing/control table, full queue and hosted three-OS outcomes. Independent review still required, not supplied here.

## Tier + routing hint
full path; system

QUALITY_PROFILE: hot-path

## Simplest shape
Remove demonstrably redundant private fixture setup subprocesses without changing lifecycle work.

## Not built
| Not built | Reason |
| --- | --- |
| Shared repository cache or fixture framework | One local setup reduction; real isolated histories remain |

## Intent surfaces
| Acceptance criterion | Exercised surface |
| --- | --- |
| AC1 | Queue successor matrix and shared-fixture siblings |
| AC2 | Real-Git setup and complete lifecycle timings |
| AC3 | Final-head three-OS bootstrap |

## UI states and design-system sources
none — no UI surface changes.

## Data-path envelope
| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Private queue fixtures | Existing 16 modes/checkpoints | N/A | Fewer setup subprocesses; runtime Git unchanged |

## Contract compatibility
none — no schema, event, or contract changes.

## Glossary impact
none — no new or renamed public names.
