---
key: ISS-022
title: "Probe portable filesystem and process guarantees"
labels: ["type:probe", "area:runtime", "priority:p1", "risk:compatibility"]
milestone: "Portable substrate"
parent: EPIC-RUNTIME
blocked_by: [ISS-006]
---

## Question

Can the exact Node 24 candidates in the `Portable primitives probe and
capability ledger` on hosted macOS, Windows, and Linux prove the physical,
helper/ABI/custody, create-once, lock/owner-death, replace, CAS/readback,
same-lock absence, process, probe-handle, and parser facts required before
`ISS-004`, `ISS-005`, `ISS-020`, and `ISS-031` select production mechanisms?

Rounds 243 and 244 blocked the former packet. This repair is limited to their
named gaps: literal physical/custody and observation/decision digest formulas,
one authenticated diagnostic terminal chain, an honest direct-child process
boundary, and default lock-handle non-inheritance. It adds no helper, principal
isolation, reboot lane, or production service.

Rounds 245 and 246 closed those literal gaps but found one new cycle: decision
publication named an installed release that cannot exist before ISS-020/N0.
This slice replaces only that unavailable writer with
`DstableHarnessSubject`, derived from the already-authenticated ISS-006 stable
harness/test/registry/contract inputs and executed from their exact protected-
main checkout.

## Probe method and lifecycle timing

Use the stable `ISS-006` harness with suite `iss022-portable-primitives` and one
required job per hosted OS. Candidate children mutate only one stable-parent
runner-temp custody root. The authority ledger fixes the Node calls, 21 vector
IDs and order, physical component and `Dphys/Ddest` formulas, helper/ABI/custody
receipts, fixed bytes, barriers/crash prefixes, errno reduction, observation and
capability-decision records, PASS provider join, and BLOCK_REPLAN diagnostic
provider/terminal join.

This issue measures process-crash/reopen behavior only. Reboot, cross-volume
mount mutation, persistent-host re-entry, native process enumeration, and
production custody remain later selections. The direct-child handle is
authority only for that child; a grandchild named by candidate IPC/stdout is
UNSUPPORTED, not inferred identity. Probe-local `WeakMap`/nonce behavior does
not claim production ISS-004 equivalence.

## Required durable evidence

1. The stable registry executes the exact 21-vector ledger once per required OS
   and rejects any missing/extra/renamed/duplicate row, candidate registry,
   excluded matrix cell, substituted runner, or mixed attempt.
2. The stable parent derives every physical component, `Dphys`, and `Ddest` from
   the literal namespace/stat/statfs/ancestor/leaf frames and exact case,
   Unicode, link, and parent-swap corpus. It never enumerates for an absent
   target. Any moving/contradictory input is UNKNOWN; any unprovable alias or
   primitive is UNSUPPORTED.
3. Exact Node executable bytes and canonical Node/ABI values derive `Dhelper`
   and `Dabi`; filesystem/statfs/case/Unicode inputs derive each OS's
   `DosProfile`. The synthetic custody namespace, root-handle readback, instance,
   and receipt derive non-null ADMITTED locator inputs only while stable. Every
   actual consumer must match its Node/ABI/OS profile and separately instantiate
   its own equal-bound custody receipt; probe custody is never production
   authority. The generic `conformance-environment/v1` parser admits
   `filesystemProfileDigest` as SHA-256 or null but grants no profile. An
   ISS-022 `selection` non-null composition derives a pre-custody environment
   identity from stable ABI/helper/OS-profile/raw-environment values, uses it
   to derive the custody instance and receipt, then constructs and hashes the
   final environment. It equal-binds `Dabi`, `DosProfile`, `DhelperProfile`,
   and `DcustodyReceipt` without accepting an environment digest from a caller.
   `selection:null` retains exact non-null `Dabi` and `DhelperProfile` after
   executable custody while its filesystem-profile and custody digests are
   null.
4. Create once produces one exact `41` winner and 31 `EEXIST` losers. Lock
   contention produces one holder and one `EEXIST`; the default child receives
   no holder descriptor. Post-death acquisition is one attempt with no deletion,
   retry, PID, age, lease, timeout, or stale-owner inference.
5. Replace crashes before create, after create, and after file sync reopen `41`;
   after rename and directory sync reopen `42`. Missing directory sync support
   is UNSUPPORTED. No result claims reboot durability.
6. CAS runs only under the admitted lock, refuses predecessor mismatch without
   write, and lets exactly one of two contenders select/read back `42`. No
   unlocked or in-process-only emulation can PASS.
7. Under that same lock, the two exact constructed unused authority paths return
   `ENOENT`; path substitution, unexpected errno, a concurrent writer, or later
   presence refuses the existing absence records and bindings.
8. The stable parent authorizes only its direct `ChildProcess` handle and exact
   terminal events. Grandchild IPC/output, PID reuse claims, surviving-tree
   claims without a handle, signal ambiguity, or timeout is non-PASS. This row
   intentionally measures the Node gap instead of manufacturing a helper.
9. The probe-local handle fixture rejects clone, worker transfer, serialization,
   cross-process replay, and reuse after callback release. ISS-004 separately
   proves the production handle implementation.
10. The parent and three fresh children parse identical ISS-002 canonical and
    hostile bytes to byte-identical normalized results. Child claims alone never
    become authority.
11. Vector, observation, custody, decision-core, provider-review, independent-
    review, final-decision, diagnostic-provider, and terminal parsers recompute
    every domain and ordered frame. PASS requires exactly 63 PASS observations,
    three exact helper/ABI/OS-profile slots, the successful provider/terminal
    chain, the exact ISS-006 stable harness writer subject, and a distinct-actor
    approved review of the exact core. Job receipts close `normalizedResult` to
    `PASS|FAIL|UNSUPPORTED|UNKNOWN`; every non-PASS receipt is diagnostic-only
    and the aggregate remains PASS-only. The `selection:null` diagnostic arm
    requires the complete ordered 21-row report, at least one physical
    UNKNOWN/UNSUPPORTED row, a non-PASS observation/receipt, null filesystem and
    custody digests, and no selectable profile/custody/locator token or
    decision-core OS-profile slot. Its exact helper/ABI evidence remains
    non-null. A caller-filled or all-physical-PASS null, ISS-002 null, mixed arm,
    sentinel digest, or synthetic environment/time/details refuses.
12. Workflow and access mutants prove the existing four-job topology, protected
    dispatch, stable/candidate separation, all-attempt joins, current live
    protection, zero bypass, 30-day retention, unique writer/time identity, and
    no checkout/external write, privilege, native addon, broker, principal
    helper, compiled executable, credential, cache, or secret.

## Disposition rule

PASS selects only the exact non-null ledger tokens after the complete three-OS
provider/terminal/independent-review join. Any UNSUPPORTED, UNKNOWN, provider
gap, or incomplete arm returns BLOCK_REPLAN with no selectable profile. A
native helper, global sequencer, OS command, or other fallback requires a new
reviewed architecture selection backed by the measured row; single-OS inference
or silent weakening is forbidden.

## Scope fence

In: disposable runner-temp mutations, stable receipts, PASS provider join, the
context-bound nullable diagnostic environment arm, and authenticated diagnostic
failure join. Out: production state/custody/process implementation, helper
selection, new environment schema members, reboot/mount setup, project policy,
candidate authority, privilege, and promotion.

## Verification plan

`pnpm run probe:portable-primitives`

`pnpm run probe:portable-primitives:verify-receipts`

## Predicted footprint and dependencies

Owns `probes/portable-primitives/` as workspace package
`@orchestration-platform/portable-primitives`, its fixtures/parsers/receipts,
and exact ISS-022 capability slots. The ISS-000 scaffold amendment replaces the
two root placeholders and their command/package censuses. The stable harness
adds only the ISS-022 suite/vector/job/runner-token/handler/bundle rows, generic
environment and job-receipt parser unions, stable receipt producer/reducer,
ISS-022 pre-custody profile/environment producer, suite-bound observation/
job-receipt joins, and their refusal mutants; generic observation download
pattern, diagnostic record/terminal arm, `if:always()` record condition,
provider-review projection, the existing two root commands and exact
external-output command arms, canonical
`planning/decisions/ISS-022/<Dcore>/` three-file publication, and their
conformance tests. No schema member, workflow job, permission, helper, issue, or
roadmap edge changes. No production consumer implementation is authorized.

The minimal widening touches the existing job-receipt parser catalog and
mutants in `packages/conformance/src/contracts.ts` and
`test/conformance/contracts.test.ts`, plus the existing receipt producer and
PASS-only aggregate mutants in `packages/conformance/src/reducer.ts` and its
tests. The acyclic repair changes the existing custody-instance digest input in
`probes/portable-primitives/src/physical.ts`, derives it in
`packages/conformance/src/iss022-profile.ts`, constructs the final environment
in the existing ISS-022 suite/observation path, and adds only the matching
profile, suite, observation, and reducer mutants.

## Producer and consumer ownership

ISS-022 produces `DprimitiveDecisionCore`, `DprimitiveReview`,
`DprimitiveDecision`, and synthetic physical/locator/custody evidence. ISS-004
consumes state/epoch primitives, ISS-005 process facts, ISS-020 destination-
owner/anchor facts, and ISS-031 the selected backend/attack constraints. Each
verifies the three exact files, its matching OS profile, and its actual custody
instance; no consumer can reinterpret null or combine runs.

## Operator actions

Provide standard hosted macOS, Windows, and Linux runners and retain their
ordinary ISS-006 artifacts for at least 30 days. No reboot, privilege,
self-hosted runner, mount, credential, or native installation is needed. After
evidence reduction, a GitHub actor different from the decision-core PR author
must review that core-only PR; publication cannot proceed from self-review.

## Glossary impact

None.

## Independent review packet seed

Attack every component and record frame, namespace/root substitution, absent-
ancestor walk, stat width, signed filesystem type, helper/ABI/custody null arm,
case/Unicode/link/parent swap, create/lock/CAS race, owner-death residue, default
descriptor inheritance, crash byte, same-lock absence, grandchild self-report,
probe-handle copy, parser difference, missing/extra provider row/artifact,
diagnostic writer/time/protection/retention join, mixed run, unsupported-to-PASS
normalization, OS-slot reuse, custody/profile mismatch, decision-core path or
byte substitution, self/stale/unmerged review, stable-harness/candidate/writer
substitution, publication-file census, write outside runner temp, and any
helper/reboot/production creep.
