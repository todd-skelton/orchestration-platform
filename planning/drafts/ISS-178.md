---
key: ISS-178
title: "Mirror changed sibling drafts before the self delivery board gate"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

Existing [#543](https://github.com/todd-skelton/orchestration-platform/issues/543), not
a replacement for #529. UNREADY pending COMPLETE review, timing approval and registration.

[Stop 5709687464](https://github.com/todd-skelton/orchestration-platform/issues/529#issuecomment-5709687464)
at 2026-09-17T05:59:55Z records one ISS-174/#529 attempt in
`m1-author-parser-recovery-20260917T0546`. Author Astra/high passed; controller committed
`a0ffac88e8ad1cff0db47659f37d14fc46662ef0`; independent Opus/high passed that head.
No blocking review finding IDs exist; the stop is
`gate-attribution-unknown:planning:board-check`.

The candidate removes only the obsolete quality-profile line and adjacent blank line
from ISS-146/#457, ISS-164/#517, ISS-165/#518, ISS-167/#524, ISS-171/#525 and
ISS-176/#537, besides process prose. The saved plan mirrors only #529; all six bodies
fail afterMirror. ISS-174 AC3 promises sync **after landing**, too late for this gate.
Remote main observed during planning remains `804e9357a88868daab31b211966f645baeca6c02`;
rederive against current main at implementation.

Read-only source branch:
`codex/run-3423a650c8b9ccc884eb080d0e4532b7331a868e189893a73b9f12f27fdfaa32/iss-174-attempt-1`
at `a0ffac88e8ad1cff0db47659f37d14fc46662ef0`.
No PR exists; `codex/iss-174` is only the planned publication branch. Reuse deletions
as regression input, never after-landing sync, historical plans/PASS/receipts as authority.

## Decision

Constraint: preserve board validation and sibling authority.
Simplest alternative: widen the existing mirror list, not add a stage.
Chosen: mirror authorized changed open bodies after refresh/review, subject to host timing approval.
Accepted downside: ahead-of-main bodies block fresh selection until landing or host reconciliation.
Revisit when: existing mirror replay fails or reconciled main/body still blocks selection.

## Authority and ordering

ISS-174 AC3 authorizes mechanical removals, not arbitrary sibling replans. No separate
resolved Decision governs timing. Before readiness, host must approve and record on #543
and in #529's planning amendment: replace after-landing-only sync with native
pre-afterMirror publication of the reviewed authorized body delta, then reconcile
published main/body. This draft supplies no ruling and edits no saved brief or runtime.
Without approval #543 remains unready; no gate bypass is an alternative.

Derive targets from the exact reviewed candidate against its refreshed delivery main
base, reusing candidate planning-delta discovery and canonical body rendering. Keep
selected-key behavior; add only changed, already registered, open siblings with actual
existing native identities. Board drift alone is not a candidate change. Sibling scope
is body-only: no title, milestone, frontmatter, roadmap, dependency, label, ready/status
or project mutation. No creation, retarget, reopening or selected-key seed fallback for
missing siblings. Missing/duplicate identities and candidate omissions fail, not skip.

Selected brief and independent review authorize sibling meaning, not changed paths;
reject unrelated AC/replan edits. Reuse head/plan binding and observe-before-mutate;
matching bodies are no-ops. Preserve full-board CLI/selection AND scoped native gate.

Lifecycle: reviewed head -> beforeMirror -> per-draft observe/apply -> afterMirror ->
publication/hosted checks/merge -> published-main steady state. Partial mirror/lost
response resumes saved delivery without duplicate writes. Pending publication reconciles
normally. Unpublished main movement requires existing refresh/DELTA and new gates before
applying content; clean rebase or old PASS is not authority. Retain old records.

Saved selection keeps its pinned brief/delivery identity, never adopting mirrored prose.
While open bodies lead main, fresh selection must fail existing full-board checks, not
select candidate prose. The single writer finishes this delivery before selecting another
item. After landing, next selection pins fresh main with matching bodies. If delivery is
abandoned after mirror, preserve the refusal: a separate host decision must reconcile
bodies back to actual published main before fresh selection. No automatic rollback,
selection overlay or manual candidate-body sync. Leave current #531/#537 work alone.

## Done when

1. **AC1 Regression.** Drive real self mirror planning, delivery reconciliation and native
   afterMirror with actual Git base/candidate commits and the exact six deletions:
   selected-only behavior yields six mismatches; repair mirrors six existing open siblings
   and passes before publication without host sync. Evidence: owning lifecycle regression
   with only GitHub transport replaced by a faithful fixture.
2. **AC2 Discrimination.** Hold valid planning, review, identities/project constant;
   suppress one sibling mirror, retaining its old body. The gate names that sibling and
   refuses publication. Missing sibling and duplicate key also refuse without creation,
   retarget or fake receipt. Evidence: same-lifecycle negatives, plus a comparison bypass
   that makes the missing-body assertion fail.
3. **AC3 Boundaries.** Unchanged/closed siblings receive no writes; selected seed behavior
   stays selected-only; native numbers, metadata, labels/readiness/project stay unchanged.
   Unrelated drift is not repaired; candidate deletion/omission fails; full-board consumers
   retain refusals. Evidence: adapter/gate boundary cases and independent review rejecting
   unauthorized sibling AC changes.
4. **AC4 Lifecycle.** Prove partial mirror/lost response/repeated resume, matching no-op,
   saved selection, pending publication and fresh selection before/after landing follow
   the ordering/steady state above. Moved main requires fresh exact-head DELTA/gates,
   preserves unrelated registrations and never applies stale content. Cover original,
   review-repaired, gate-corrected and refreshed entrypoints without history/allowance
   changes. Evidence: native delivery/refresh/selection tests and stale-review negatives.
5. **AC5 Delivery.** Native implementation passes `pnpm typecheck`, `pnpm format:check`,
   `pnpm planning:check`, complete `pnpm test`, ordinary independent exact-head review,
   and executed Ubuntu/Windows/macOS bootstrap jobs at final repair head. Evidence:
   native outputs and actual jobs, never skips, synthetic receipts or passing subsets.

## Don't-rebuild pointers

- `adapters/self.mjs:planningMirror`
- `scripts/planning/candidate-board.mjs:candidatePlanningKeys`
- `scripts/dogfood/delivery.ts:deliveryStep`
- `scripts/dogfood/delivery-adapter.ts:githubDeliveryAdapter`
- `scripts/dogfood/refresh.ts:refreshDelivery`

ISS-148 (refresh/scoped gate), ISS-163 (pinned selection), ISS-157 (stop recovery) already
ship; none adds sibling targets. #531 protection, #537 ops and #542 parsing are not coverage.

## Footprint & chain

Implementation: `adapters/self.mjs`, minimal reuse in `scripts/planning/candidate-board.mjs`,
ordering in `docs/loop.md`, owning tests. Callers: `mirrorPlanning`/`selfPlanFromSnapshots`
-> `repositoryDeliveryPolicy` -> `deliveryStep` -> `observeDraft`/`applyDraft` ->
`runGate`/`checkCandidateBoard`; queue original/repair/correction/refresh delivery;
supervision `nextCycle` -> `selectCandidates`/`issueContext`. Change shared delivery code
only if a regression proves list lifecycle insufficient. Use existing delivery-adapter,
delivery, refresh, queue/supervision and planning-contract suites.

M2 delivery repair; fix lands before #529 recovery. Host wires Blocks #529 on #543 after
approval, not the reverse. No edge mutation here; approval blocks readiness, not a
fabricated roadmap dependency.

## Defect constraints

- `validation-misses-sibling-seed-path` / `lifecycle-repair-misses-sibling-retained-state-entrypoint`:
  exercise every caller, retained partial mirror and refresh, not only helpers;
  Chase Sets' no-mirror behavior stays unchanged.
- `state-machine-steady-state-missing` / `steady-state-fast-path-skips-required-reconciliation`:
  prove partial, complete, pending, landed and abandoned states; no-op writes never skip
  outstanding gates or main/body reconciliation.
- `mutable-reference-breaks-candidate-identity` / `stale-review-bound-to-wrong-head` /
  `reviewed-plan-not-bound`: existing exact head/plan authority and independent DELTA
  after integration; no cross-head receipt reuse.
- `pagination-truncates-authoritative-state` / `presence-evidence-treated-as-completeness`:
  complete board/project acquisition; issue/receipt presence is not body equality.
- `fixture-only-health-integration` / `missing-fail-closed-regression-evidence`:
  real callers, fixed other inputs and executed comparison-bypass negative for AC2.
- `retained-artifact-schema-ratchet`: no new required fields or history migration.
- `vitest-wall-clock-timeout-under-parallel-host-load` /
  `parallel-node-verifiers-exhaust-host-memory`: admitted foreground full gates;
  load attribution needs unchanged isolated timing collapse AND complete one-worker/
  file-serial gate; no raised timeout, product change or test skip.

## Verification and authority probe

Stop comment and source `board-failure.log`, `gate-4-failure.json`, `delivery-plan.json`
under `/root/orchestration-m1/runtime/m1-author-parser-recovery-20260917T0546/iss-174-attempt-1/source`
capture actual GitHub comparison **after mirror, before publication**: only #529 targeted,
six bodies disagree. This is lifecycle-time evidence, not post-hoc replay. ROOT
`.orchestrator/artifacts/platform-board-mirror-intake.md` records separate main board PASS
at 06:05Z, not candidate PASS. Reobserve open/closed identities at mirror planning,
including siblings closing before delivery; old traces cannot establish current state.

Planning: `pnpm planning:check`, scoped Prettier, native parser, body cap and additive diff.
AC5 uses native admission; Windows full test uses ROOT
`.orchestrator/invoke-heavy-verifier.ps1 -Gate test -Worktree <repair-worktree> -Lane <lane>`.
No bootstrap/full battery in this planning lane.

## Operator actions and review packet

Host owes timing amendment, independent COMPLETE review, landing and #543 canonical
body/project before readiness. No provider credentials. #529 resume needs reviewed
three-OS-green fix, safe install with supervisors absent and separate canonical host
`gateStopAuthorization`. Preserve source/history/count and single-use recovery;
no new attempt/re-entry grant.

Reviewer attacks: unrelated drift, timing, partial-mirror selection, moved main, identity
and sibling scope. Read terminals/diff and real gate consumer, not intake's whole-board
shorthand. Require AC1-4 negatives/lifecycle and AC5 actual jobs. COMPLETE review pending.

## Simplest shape and not built

One existing mirror list widened to authorized changed open siblings.

| Not built                       | Reason                                 |
| ------------------------------- | -------------------------------------- |
| New protocol/receipt/classifier | Existing review/mirror suffice; rule 9 |
| Board waiver or blanket repair  | Drift is not authority                 |
| Rollback/selection overlay      | Host resolves abandonment              |
| Host candidate/late sync        | Bypasses authority or gate ordering    |

## Out of scope

Non-goals: arbitrary sibling replan/AC/label/status changes, closed drafts, board relaxation,
host runtime/source patches, new #529 attempts, parser/protection/ops/product work, auto-install.
Glossary impact: none; existing draft/candidate/mirror/selection terms.
Tier + routing hint: full path, system; planning row 7 Astra/high override-Todd,
expected USD unknown; implementation/review use ordinary native self routing.
