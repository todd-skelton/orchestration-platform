---
key: ISS-178
title: "Mirror changed sibling drafts before the self delivery board gate"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

[#543](https://github.com/todd-skelton/orchestration-platform/issues/543), not #529's
replacement. UNREADY pending inherited DELTA, landing and registration.

[Stop 5709687464](https://github.com/todd-skelton/orchestration-platform/issues/529#issuecomment-5709687464)
at 2026-09-17T05:59:55Z records #529 attempt 1's gate stop, not review failure:
`gate-attribution-unknown:planning:board-check`. Brief review r1 at
`a515986ad9978ea0245f1d5335d835cfa9cc47a0` found F1: list-only widening replays CLOSED
siblings and repairs metadata through the selected-issue observer/applier.

The candidate removes only `QUALITY_PROFILE` and its adjacent blank line
from ISS-146/#457, ISS-164/#517, ISS-165/#518, ISS-167/#524, ISS-171/#525 and
ISS-176/#537, besides process prose. Only #529 is mirrored; six bodies fail afterMirror.
Original AC3's after-landing sync was too late.
Host main: `ffe1e0bf`; local base: `804e9357a88868daab31b211966f645baeca6c02`.
Planning integration/review owed; implementation rederives against current main.

Read-only salvage:
`codex/run-3423a650c8b9ccc884eb080d0e4532b7331a868e189893a73b9f12f27fdfaa32/iss-174-attempt-1`
at `a0ffac88e8ad1cff0db47659f37d14fc46662ef0`.
No PR exists; `codex/iss-174` is planned only. Reuse regression deletions, never
after-landing sync or historical plans/PASS/receipts as authority.

## Decision

Constraint: preserve board and sibling authority.
Simplest alternative: mirror list plus sibling-aware observe/apply.
Chosen: authorized open-body mirroring after refresh/review.
Accepted downside: ahead bodies block selection until landing or host reconciliation.
Revisit when: replay or reconciled selection fails.

## Authority and ordering

Todd #4388/comment 5691367823 delegation is ratified by ROOT
`.orchestrator/artifacts/recovery-r17-ratification.md` B and
`fable-recovery-decisions-r17.report.md` B: r1 timing conditions 1/2/5/6 stand;
C3'/C4' replace C3/C4; host posts separately. ISS-174 AC3 permits only `QUALITY_PROFILE`
plus adjacent-blank removal from drafts open at authoring, published natively before
afterMirror to registered currently OPEN siblings. Amended brief plus exact-head review,
not paths, authorizes meaning; other sibling hunks block review. No host candidate sync.

Use reviewed candidate versus refreshed main delta/rendering. Preserve selected seed and
full-draft behavior; add changed registered OPEN siblings with native identities.
Drift is not a candidate change. Sibling authority is body-only: no
title, milestone, frontmatter, roadmap, dependency, labels/readiness/status or project
changes; no creation, retarget, reopening or selected-key seed fallback.

F1: reobserve each saved-plan sibling before mutation/replay, not only at plan creation.
Missing, duplicate, reopened/retargeted or wrong identity refuses, naming the sibling.
Identity-valid CLOSED is satisfied without writes, ignored by the scoped gate.
OPEN title/milestone drift refuses even with matching body; never repair metadata.
Three-way against reviewed refreshed bodies: target = no-op; base = body-only apply;
other = refusal, never overwrite unexpected prose.
Reuse head/plan binding, draft receipts and lost-response reconciliation; no new
receipt/protocol or migration.
Preserve full-board CLI and scoped native gate.

Author-time file scope differs from runtime write scope: a later-closed sibling retains
its hunk for ordinary refresh/DELTA and unchanged gate judgment, never forced removal or
silent content skip. Refusal means STOP/intake, not a mechanism. Deletion/omission fails.

Lifecycle: reviewed head -> beforeMirror -> per-draft observe/apply -> afterMirror ->
publication/hosted checks/merge -> published main. Partial mirror/lost response resumes
without duplicate writes; pending publication reconciles normally. Unpublished main
movement requires refresh/DELTA and new gates before applying content; clean rebase/old
PASS is not authority. Retain old records.

Saved selection pins its brief/identity, never mirrored prose. Ahead-of-main bodies refuse
fresh full-board selection; the single writer finishes delivery first. After landing,
selection pins fresh main with matching bodies. Abandonment retains refusal until a
separate host decision authorizes restoration to published main: a WRITE, not read-only
reconciliation. No rollback/selection overlay. #531 and ISS-177 owners unchanged; no touch
live #537, and never start #529 while #537 delivery is live in any runtime.

## Done when

1. **AC1 Regression.** Real self planning/delivery/afterMirror with actual Git base/candidate
   commits and the exact six deletions yields six selected-only mismatches; repair mirrors
   six existing open siblings and passes before publication without host sync. Evidence:
   lifecycle regression replacing only GitHub transport with a faithful fixture.
2. **AC2 Discrimination.** Hold planning, review, identities/project valid; suppress one
   sibling mirror. Its old body makes the gate name it and refuse publication. Missing
   sibling/duplicate key refuse without creation/retarget/fake receipt. Evidence: lifecycle
   negatives and an executed comparison bypass failing the missing-body assertion.
3. **AC3 Boundaries.** Unchanged/CLOSED siblings get no writes; selected seed/full-draft,
   native numbers, metadata, labels/readiness/project stay unchanged.
   OPEN title/milestone drift and other-body refuse with zero edits; base-body alone permits
   one body-only edit, target-body no-op. Candidate deletion/omission fails; full-board
   consumers retain refusals. Evidence: adapter/gate controls; independent review rejects
   unauthorized sibling AC changes.
4. **AC4 Lifecycle.** Prove partial mirror/lost response/repeated resume, matching no-op,
   saved selection and fresh selection before/after landing follow the ordering above.
   Pending publication reconciles. Moved main requires fresh exact-head DELTA/gates,
   preserves unrelated registrations and never applies stale content. Cover original,
   review-repaired, gate-corrected and refreshed entrypoints without history/allowance
   changes. Persist a multi-draft plan, close an unprocessed sibling after persistence and
   during partial-mirror replay: zero edits to it, scoped afterMirror passes. OPEN controls
   vary title and milestone separately (also with body drift or target body): zero edits;
   body-only base-to-target edits once, lost response/repeated resume never writes twice.
   Evidence: native lifecycle/stale-review controls; synthetic closure/drift identities,
   explicitly labeled, never altered real AC1 facts.
5. **AC5 Delivery.** Native implementation passes `pnpm typecheck`, `pnpm format:check`,
   `pnpm planning:check`, complete `pnpm test`, ordinary independent exact-head review,
   and executed Ubuntu/Windows/macOS bootstrap jobs at final repair head. Evidence:
   native outputs and actual jobs, never skips, synthetic receipts or passing subsets.

## Don't-rebuild pointers

ISS-148/163/157 ship refresh/scoped gate, pinned selection and stop recovery, not sibling
body-only replay. #531/#537/#542 are not coverage.

## Footprint & chain

Implementation: `adapters/self.mjs`, minimal `scripts/dogfood/delivery-adapter.ts`
sibling observe/apply, `scripts/planning/candidate-board.mjs:candidatePlanningKeys`,
`docs/loop.md`, owning tests. Callers: `mirrorPlanning`/`selfPlanFromSnapshots`
-> `repositoryDeliveryPolicy` -> `deliveryStep` -> `observeDraft`/`applyDraft` ->
`runGate`/`checkCandidateBoard`; queue original/repair/correction/refresh delivery;
`scripts/dogfood/refresh.ts:refreshDelivery`; supervision `nextCycle` ->
`selectCandidates`/`issueContext`; their existing suites.

Host wires native #543 Blocks #529 after registration; no keyed ISS-174 dependency.

## Defect constraints

- `validation-misses-sibling-seed-path` / `lifecycle-repair-misses-sibling-retained-state-entrypoint`:
  exercise every caller, retained partial mirror and refresh;
  Chase Sets' no-mirror behavior stays unchanged.
- `state-machine-steady-state-missing` / `steady-state-fast-path-skips-required-reconciliation`:
  prove partial, complete, pending, landed and abandoned states; no-op writes never skip
  outstanding gates or main/body reconciliation.
- `mutable-reference-breaks-candidate-identity` / `stale-review-bound-to-wrong-head` /
  `reviewed-plan-not-bound`: existing exact head/plan authority and independent DELTA
  after integration; no cross-head receipt reuse.
- `pagination-truncates-authoritative-state` / `presence-evidence-treated-as-completeness`:
  complete board/project acquisition; issue/receipt presence is not body equality.
- `fixture-only-health-integration` / `missing-fail-closed-regression-evidence`:
  real callers, fixed other inputs and executed comparison-bypass negative for AC2.
- `retained-artifact-schema-ratchet`: no new required fields or history migration.
- `mandated-external-assertion-contradicts-authority`: label synthetic controls; never
  alter facts under real external identities; contradictory probes stop.
- `unrunnable-verification-command`: resolve commands against the bound manifest.
- `vitest-wall-clock-timeout-under-parallel-host-load` /
  `parallel-node-verifiers-exhaust-host-memory`: foreground native full gates under existing process rules;
  load attribution needs unchanged isolated timing collapse AND complete one-worker/
  file-serial gate; no raised timeout, product change or test skip.

## Verification and authority probe

Stop comment and source `board-failure.log`, `gate-4-failure.json`, `delivery-plan.json`
under `/root/orchestration-m1/runtime/m1-author-parser-recovery-20260917T0546/iss-174-attempt-1/source`
capture GitHub **after mirror, before publication**: only #529 targeted, six mismatches.
ROOT `.orchestrator/artifacts/platform-board-mirror-intake.md` records separate 06:05Z
main PASS, not candidate PASS. F1's observer probe omitted state and returned needs-mutation
for CLOSED. Capture current identity/state/body at planning AND mutation/replay.

Planning: `pnpm planning:check`, scoped Prettier/parser/body/diff checks.
AC5 commands run in native WSL per `package.json`/`docs/loop.md`, not the incumbent
Windows wrapper or new admission. No bootstrap/full battery in this planning lane.

## Operator actions and review packet

Host owes ruling publication, inherited DELTA resolving COMPLETE r1 F1, current-main
planning integration/review, three-OS-green landing, bodies/project/full-board before ready.
No #529 resume grant.
Later #529 recovery requires the timing amendment on current main with its full-board
check, reviewed three-OS-green repair, safe install with supervisors absent and separate
exact `gateStopAuthorization`. Saved #529 brief/selection/planningRevision/source/fingerprint
stay immutable. Existing authorityUrl/recovery context must carry the timing ruling to
ordinary refresh/DELTA with the pinned brief; no brief swap. Irreconcilable means STOP/intake,
not a mechanism. Preserve history/counts/single-use recovery; no extra attempt.

Review attacks F1, three-way bodies, selection/main movement and sibling authority.

## Simplest shape and not built

Existing mirror list plus sibling-aware observe/apply.

| Not built                       | Reason                                 |
| ------------------------------- | -------------------------------------- |
| New protocol/receipt/classifier | Existing review/mirror suffice; rule 9 |
| Board waiver or blanket repair  | Drift is not authority                 |
| Rollback/selection overlay      | Host resolves abandonment              |
| Host candidate/late sync        | Bypasses authority or gate ordering    |

## Out of scope

Non-goals: arbitrary replans, closed writes, board relaxation, host source/runtime patches,
#529 attempts, parser/protection/ops/product work, auto-install.
Glossary: none. Full path/system; native routing unchanged.
