---
key: ISS-073
title: "Allowlist native child environments at both dispatch hops"
labels: ["type:slice", "area:core", "priority:p0"]
milestone: "Minimum orchestration kernel"
parent: EPIC-KERNEL
blocked_by: [ISS-072]
---

## Context and evidence

Round 476 records that the superseded five-name exclusion could pass unknown
credentials. Credential material reached retained logs in the observed run;
validity, scopes and unauthorized operations are unverified, and invalidation
was not performed. Source 1 and review 2 passed only that superseded candidate.
Source reservations 3, 5 and 7 were actual attempts 3, 4 and 5 and stopped on
controller preflight defects without source edits; skipped reviewer reservations
4, 6 and 8 were not launched. This corrective source is actual attempt 6 under
the same ordinary eight-attempt ceiling.

## Scope and non-goals

In: build each child environment from an empty object at controller-to-observer
and observer-to-native-worker. Copy only `PATH`, `PATHEXT`, `SYSTEMROOT`,
`WINDIR`, `COMSPEC`, `TEMP`, `TMP`, `TMPDIR`, `CODEX_HOME`, `HOME`,
`USERPROFILE`, `HOMEDRIVE`, `HOMEPATH`, `APPDATA`, `LOCALAPPDATA`,
`XDG_CONFIG_HOME`, and `XDG_DATA_HOME`. POSIX matches only these exact uppercase
names. Windows matches case-insensitively, emits canonical uppercase names and
prefers an exact canonical entry. Preserve exact required-location values and
the unchanged parent; omit every unknown name without logging it.

Out: broker, scheduler or public-contract expansion; arbitrary environment
overrides; credential rotation/invalidation; provider, sandbox or controller
environment mutation; filesystem/keychain isolation; network testing;
publication, promotion or activation.

## Acceptance criteria and evidence

1. Both production spawns have explicit environments produced from the empty
   allowlist builder; hosted fixtures fail either hop independently and inspect
   the actual observer and worker child environments.
2. Synthetic exact names, aliases, known delivery/cloud variables, proxies,
   `NODE_OPTIONS`, askpass, agent socket and arbitrary secrets are absent.
   Exact provider-auth location values and required startup-name presence remain.
3. POSIX exact-case and Windows case-insensitive/canonical behavior are asserted,
   parent objects remain equal to their snapshots, and fixture output contains
   no environment values. Direct-Node TypeScript import coverage remains.

## Verification plan

Source 1/review 2 are evidence only for the superseded candidate. Attempt 6 may
run installed static TypeScript, pinned Prettier and the planning entrypoint; no
local runner, native build, addon/probe/lock experiment or network check. Review
7 must independently attack every changed assertion. The controller must still
run four actual package gates, and hosted CI must execute the meaningful tests
on Windows, macOS and Linux before acceptance. No such result is claimed here.

## Predicted footprint and review attack surface

Predicted full change: both dogfood spawn sources, the focused adapter test,
provider and controller fixtures, supervised-dogfood note, this draft, kernel
epic, M2 roadmap row and round 476. Attack either missing explicit `env`, parent
spread, wildcard/prefix copying, case aliases, Windows duplicate resolution,
unknown leakage, lost startup/auth locations, parent mutation, values in output,
fake launch substitutes and claims of broader isolation or authority.

## Falsifiable prediction

Each actual child has only canonical allowlist names and exact required location
values, while its parent is unchanged. Any unknown name at either hop, a missing
explicit environment, a changed required value, or a platform case mismatch
falsifies this bounded repair.
