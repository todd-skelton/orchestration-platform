---
key: ISS-163
title: "Select self work from immutable current-main planning without moving the executor"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

M1 `m1-iss159-160-20260916T0240` completed ISS-160:2, then stopped:
`BOARD_CONTRACT_MISMATCH: #508 carries unregistered planning key ISS-161`.
Retained supervisor.log:1055-1056:
https://github.com/todd-skelton/orchestration-platform/issues/367#issuecomment-5699263165

Executor: `a3c69cd26a1e48f161fe3ea65536a86eb2c7a37c`. PR #507 registered
ISS-161; #510 merged as `05fccc7d3c132c9878838f8c6e060d5f425e1d39`.
Historical salvage: `codex/iss-160-attempt-2` @
`a789b347ccbd443c5bad42c281b890153805c5a2`, diagnostic only. No failed closed
implementation PR; never reuse #510's PASS, gates or attempts.

nextCycle selects before currentMain fetch; queue issueContext also reads stale data.

Host recovery (#367 comment 5699303691) reached ISS-161:1, not zero-touch/M2 exit.

### Don't-rebuild pointers

- `scripts/dogfood/supervision.ts`
- `scripts/dogfood/queue.ts`
- `adapters/self.mjs`
- `scripts/planning/check.mjs`
- `scripts/planning/candidate-board.mjs`

## Decisions already made

Installed Git-show DATA reads enumerate the full draft tree, not registry rows.

ISS-133 resume/ISS-148 refresh do not fix selection; scoped afterMirror cannot
replace full board. ISS-137 is no prerequisite/upgrade permission. ISS-144 closed
resume, ISS-159/160/161 setup/conflict/source-FAIL and ISS-162 DB are separate.

F2 chooses (b); smaller (a) fails a real consumer:
`flow.ts:328-335` fingerprints `{config,prompts}` and refuses unequal saved
fingerprints; `queue.ts:2538-2550` resumes `step(item.source,...)` with rebuilt
prompts. Synthetic source probe `legacy-ISS160-base-ne-executorHEAD-source-fingerprint`
(2026-09-16T15:19:23.608Z), base a3c69cd/executor HEAD 05fccc7 above:
ISS-160 body equal/rules unequal; base reuse throws `conflicting-run-configuration`,
absent/HEAD passes. No runtime writes.

New self selections carry optional `planningRevision === base`: explicitly a
transitional behavior marker, not independent provenance. Thread to issueContext;
absent keeps executor-root reads, including direct calls. Reject malformed or
unequal revisions at the boundary; no backfill/migration/fingerprint waiver.
Retire producer/branch at a reviewed stopped-install cutover only after host
read-only census finds zero unfinished/re-enterable pre-cutover self cycles
(legacy or marked). Completed history stays readable/byte-identical. Not a
time-based sunset; no census/restart authorized here.
Chase Sets selection/routing/context is unchanged.

## Scope fence

In: self-selection/context, non-parking refusals, regressions.
Non-goals: executor auto-upgrade/pause, selector rehome, whole-board relaxation,
key exceptions, host loop duplication, migration, manual main edits, attempt
reset, product/DB work, live restart/config/install.

## Done when

1. **AC1: Current data, stable code.** Initial/successor self selection after
   completion/item stop fetches `refs/remotes/origin/main` before planning.
   Base/planningRevision share one immutable SHA, never re-resolved. Installed
   selector uses full snapshot/board. Same-main/empty-ready retain
   ordering, earliest milestone, readiness/dependencies and targetMilestone
   forwarding. `fresh self selection sees later registration` in supervision.test.ts
   uses real local old/current Git and selector; unchanged source fails.
2. **AC2: Executable brief.** Actual queue composition with the old clean
   executor gets new-key title/body/criteria/rules/source base at the pin.
   `new selection context remains pinned after main moves` in
   queue-adapter.test.ts moves remote after selection then persists/resumes;
   inputs stay pinned. Selector-only fix fails. No fetched JavaScript executes.
3. **AC3: Honest refusal.** Classify by call site, never message substring:
   currentMain/Git acquisition failure is `current-main-unavailable`;
   loadBoardSnapshot acquisition (including pagination/census failure) is
   `issue-observation-unavailable`. Validation of acquired planning/board stays
   outside those catches: orphan draft keeps PLANNING_CONTRACT_MISMATCH;
   malformed/unregistered keys, wrong bodies/milestones and duplicate claims
   keep BOARD_CONTRACT_MISMATCH, both surfaced as `queue-internal-error`.
   Preserve full error text in nonempty supervisor `diagnostics`, via
   DeliveryBlocked -> preserveReason -> QueueBlocked when typed. Per-issue
   observation is unchanged.
   Never idle/fallback/ignore/poll. Registration ahead of its board item also
   yields `queue-internal-error`; host same-run retry waits for authority to
   agree. Use blocked output, not an issue note.
   `selection authority failure refuses` varies fetch failure, incomplete census, orphan draft
   or unknown key; asserts reason AND full originating diagnostics for each.
   Bare-reason/substring controls fail.
4. **AC4: Resume and safety.** Saved selections resume without selection/fetch.
   Retain original file identities AND bytes for selections, attempts, stops,
   participants and spent allowances, not rewritten equal JSON. New selections
   resume pinned; fingerprints stay strict. Completion advances once.
   Pre-persist failure reobserves; post-persist resumes. Failed fresh selection
   changes no label, attempt, worker or completion.
   Evidence: `selection refresh preserves legacy resume and M2` covers open,
   closed/completed cycles and repeat resume; compare retained file IDs/raw hashes,
   excluding NEW completion/history records.
   `legacy selection base differs from executor HEAD`: absent yields HEAD body/rules and original
   source fingerprint; marked yields base body/rules. Unconditional-base control
   fails. Absent-revision ISS-107/152 direct calls retain repository-adapter.test.ts
   outputs; they are NOT marked saved selections.
   Installed HEAD/branch/index/worktree and synthetic M2 sentinel stay unchanged.
5. **AC5: Bound the change.** Configured gitExecutable reads create no scratch
   worktree/install/resource owner or cleanup; partial reads never select.
   Document refresh versus stopped install/retry: rules can describe uninstalled
   behavior but grant no code authority. Diff/caller inspection and
   AC1-4 prove this. Landing grants no install/restart/unpark/M2 exit.

## External authority probe & evidence timing

Log captures between-cycle stop. R1 selection-boundary probe
2026-09-16T14:44:56.658Z: main 05fccc7, shipped loader/selector, 179 issues;
#508 OPEN/type:slice, not ready. a3c69cd mismatched; main validated, returned `[]`.
Availability, not recovery. R1 retains output; synthetic ready controls only.

## Defect-class constraints

- `lifecycle-repair-misses-sibling-retained-state-entrypoint` and
  `validation-misses-sibling-seed-path`: inventory initial/successor/saved
  nextCycle/context/both adapters; preserve delivery/board CLI.
- `state-machine-steady-state-missing`: exercise no-selection, pinned in-flight,
  completed/item-stopped, idle, same-main successors and repeated resume.
- `mutable-reference-breaks-candidate-identity` and
  `retained-artifact-schema-ratchet`: one commit for all bytes; preserve legacy
  records without fabricated lineage.
- `fixture-only-health-integration` and
  `mandated-external-assertion-contradicts-authority`: real composition/timed
  authority probes; separate synthetic/live facts.
- `pagination-truncates-authoritative-state` and
  `presence-evidence-treated-as-completeness`: full board census/draft inventory;
  incomplete authority cannot select/idle.
- `missing-fail-closed-regression-evidence`: named revision/refusal bypasses,
  other inputs fixed; candidate green/control red, no mutant framework.
- `unrunnable-verification-command`, `parallel-node-verifiers-exhaust-host-memory`
  and `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`:
  resolve scripts; admitted serial full gates, no subset as load authority,
  timeout increases or skips.

## Verification plan

Planning: `node scripts/planning/check.mjs`, `git diff --check`, Chase Sets
`scripts/brief-lint.mjs` on draft/rendered body. Native M1: suites below,
`pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`,
complete `pnpm test` and three-OS bootstrap. Every full Windows-host gate uses
the container's invoke-heavy-verifier.ps1. No direct heavy bypass/new runner.

## Footprint & chain

scripts/dogfood/{supervision.ts,queue.ts,supervise.mjs,
repository-adapter.ts}; adapters/self.mjs and self.d.mts;
scripts/planning/check.mjs and check.d.mts; docs/loop.md. Inspect flow.ts fingerprint
and board-check.mjs loader/validator; no relaxation/new taxonomy.

Callers: nextCycle -> selectCandidates; supervise.mjs -> queueConfigFromLoop ->
issueContext; loadRepositoryAdapter wraps both. Verification/test-edit inventory:
test/dogfood/{supervision.test.ts (selector/context), repository-adapter.test.ts
(exports, ISS-107/152, criteria), delivery-adapter.test.ts (policy/subprocess),
queue.test.ts (context wrappers), refresh.test.ts (delivery exports)} are all five
self import suites; queue-adapter.test.ts tests composition;
test/planning-contracts.test.ts tests census. Keep delivery/refresh/board CLI and
chase-sets-adapter.test.ts behavior, including both targetMilestone forwards.
Native M1 only. Blocked by: none; Blocks: none. No ISS-162 edits.

## Review packet seed

Full-path/system: attack stale/mixed reads, fingerprint drift, orphan omission,
M2 leakage using AC1-4 and bypass results.
Inherit the initial COMPLETE sweep in platform-iss163-pressure-r1.report.md at
ed21ea1683820aba3a5f27dbbae094e0561debaa (F1-F4, G0 BLOCK_REPLAN).
Next review per dispatch: inherited DELTA of remedies/direct callers. No PASS/ready.

## Operator actions

None for implementation. Review/registration separate; host installation needs
exact-head PASS/three-OS green and supervisors absent. Do not stop live M2.

QUALITY_PROFILE: contract

## Intent surfaces

| Acceptance criterion | Exercised surface          |
| -------------------- | -------------------------- |
| AC1                  | nextCycle/full selector    |
| AC2                  | Queue brief/restart        |
| AC3                  | Observation/blocked output |
| AC4                  | Resume/adapter boundary    |
| AC5                  | Git/install boundary       |

## Simplest shape

Read pinned main data through installed code.

## Not built

| Not built                     | Reason                       |
| ----------------------------- | ---------------------------- |
| Updater/temporary checkout    | Git reads suffice            |
| Retry service/migration       | Existing lifecycle suffices  |
| Scoped selector/key exception | Mismatch must refuse         |
| Legacy base reuse             | F2 fingerprint failure above |

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

| Data path | Bound                 | Index expectation | Per-item I/O               |
| --------- | --------------------- | ----------------- | -------------------------- |
| Selection | One fetch/full census | Commit/tree       | All drafts, no issue fetch |
| Context   | One revision          | Git objects       | Draft/rules                |

## Contract compatibility

| Changed contract   | Compatibility posture                | Removed path             |
| ------------------ | ------------------------------------ | ------------------------ |
| planningRevision   | Absent: legacy; retirement above     | New-cycle HEAD reads     |
| gitExecutable      | Optional; native supplies configured | Bare Git on new reads    |
| Pre-selection stop | Diagnostics; host retry              | Pre-candidate issue note |

## Glossary impact

| Public term      | Owning glossary or contract             |
| ---------------- | --------------------------------------- |
| planningRevision | RepositoryAdapter inputs; SelectedIssue |
| gitExecutable    | LoopConfig; RepositoryAdapter inputs    |
