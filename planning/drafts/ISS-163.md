---
key: ISS-163
title: "Select self work from immutable current-main planning without moving the executor"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

M1 `m1-iss159-160-20260916T0240` completed ISS-160:2 (cursor 1/items 1/
participants 12), then stopped before selection: `queue-internal-error`,
`BOARD_CONTRACT_MISMATCH: #508 carries unregistered planning key ISS-161`.
Retained supervisor.log lines 1055-1056 and incident:
https://github.com/todd-skelton/orchestration-platform/issues/367#issuecomment-5699263165

Clean executor: `a3c69cd26a1e48f161fe3ea65536a86eb2c7a37c`. Registration
PR #507 merged `f3b6c157f0840e89195f0df3fefd8007105905d1` at
2026-09-16T12:25:40Z; PR #510 merged at 14:33:00Z as current main
`05fccc7d3c132c9878838f8c6e060d5f425e1d39`.
Its historical branch/head is `codex/iss-160-attempt-2` @
`a789b347ccbd443c5bad42c281b890153805c5a2`: diagnostic-only salvage, not
implementation authority. No closed-unmerged PR/stopping review finding IDs
exist for this defect. Never reuse #510's PASS, green 35105999686 or attempts.

nextCycle selects from stableExecutorRoot before currentMain fetch; fixing
only selection leaves queueConfigFromLoop -> issueContext reading stale data.

Host fast-forward a3c69cd -> 05fccc7 with supervisors/workers absent and
canonical same-run restart reached ISS-161:1 author observation:
https://github.com/todd-skelton/orchestration-platform/issues/367#issuecomment-5699303691
Host recovery is not zero-touch success/M2 exit. ISS-163 neither replaces
#367's outcome nor ISS-161's source-FAIL repair; both retain their scope.

### Don't-rebuild pointers

- `scripts/dogfood/supervision.ts`
- `scripts/dogfood/queue.ts`
- `adapters/self.mjs`
- `scripts/planning/check.mjs`
- `scripts/planning/candidate-board.mjs`

## Decisions already made

Read current main as DATA through installed code using candidate-board's
immutable Git-show pattern. Enumerate drafts from the commit tree, not just
the registry: orphan/missing drafts still fail full planning validation.

ISS-133 supports stopped-upgrade delivery resume, not automatic between-cycle
advancement. Intended executor upgrades still require supervisors absent;
open ISS-137 pause is neither dependency nor permission to move live M2.
ISS-148 supplies candidate integration and immutable planning reads, not this
selection fix; its scoped afterMirror gate cannot replace full selection.
ISS-144 owns closed saved-selection reconciliation; ISS-159/160/161 own setup,
conflict continuation/source FAIL, not this gap. ISS-162 DB work is independent.

New self selections carry optional `planningRevision` equal to existing `base`,
the fetched immutable main SHA. Queue composition passes it to issueContext
for draft/criteria/rules reads. Absent on old saved selections means their
unchanged context path; no backfill/migration. Reject present malformed or
different revisions at the input boundary. This is data provenance, not an
executor version or policy. Chase Sets selection/routing/context is unchanged.

## Scope fence

In: fresh self-selection/context reads, non-parking failures and regressions.
Non-goals: executor auto-upgrade/pause, selector rehome, whole-board relaxation,
issue-key exceptions, host loop duplication, runtime migration, manual main
edits, attempt reset, product/DB work, live restart/config edits or installation.

## Done when

1. **AC1: Current data, stable code.** On initial fresh self selection and
   each successor after completion/item stop, resolve actual origin/main
   before loading planning. Installed self selection uses that full snapshot
   and full live board. Base/planningRevision use the same SHA, never a second
   ref resolution. Same-main and empty-ready states retain ordinary
   ordering, earliest-milestone, readiness and dependency behavior. Evidence:
   `fresh self selection sees later registration` in the existing supervision
   suite uses real local Git old-executor/current-origin commits and the real
   self selector, not only injected candidates. Old behavior must fail.
2. **AC2: Executable selected brief.** With the executor still on the old
   clean head, feed AC1's selection through actual queue composition: the new
   key's title, body, criteria, rules and source base come from that exact
   planningRevision. Move the remote ref after selection and prove those
   inputs stay pinned through persist/resume. No fetched JavaScript executes.
   Evidence: `new selection context remains pinned after main moves` in
   queue-adapter.test.ts; a selector-only fix must fail this check.
3. **AC3: Honest refusal.** Fetch failure/missing commit remains
   `current-main-unavailable`; unreadable or incomplete board observation is
   `issue-observation-unavailable`, a retryable run stop, never idle or fallback
   to the old checkout. Preserve BOARD_CONTRACT_MISMATCH diagnostics
   for genuine malformed/unregistered keys, wrong bodies/milestones and
   duplicate claims against the pinned snapshot. A registration/board race
   stops for a later ordinary retry; no catch-and-ignore or unbounded polling.
   Without a selected issue, report via the existing supervisor blocked output,
   not a fabricated issue note. Evidence: `selection authority failure refuses`
   varies only the failing authority input with all other inputs valid; cover
   fetch failure, incomplete census, orphan draft and genuine unknown key.
4. **AC4: Resume and safety.** Saved in-flight selections take the existing
   resume path without fresh selection/fetch. Legacy selections lacking the
   optional field, failed attempts, stops, participants and spent allowances
   remain byte-identical; new pinned selections resume at the same revision.
   Completion selects the next cycle once. A pre-persist failure restarts with
   a fresh observation; post-persist restart resumes that selection. No label,
   attempt, worker or completion mutation occurs on failed fresh selection.
   Evidence: `selection refresh preserves legacy resume and M2` covers open,
   closed and completed saved cycles, repeat resume and a Chase Sets adapter
   sentinel. Assert installed HEAD/branch/index/worktree and M2's sentinel
   state unchanged. Do not touch a real live M2 run to prove this property.
5. **AC5: Bound the change.** Git reads create no scratch worktree/install or
   resource owner; success/error/interruption need no cleanup. Partial reads
   never select. Document data refresh versus stopped installation and same-run
   retry. Evidence: independent diff/caller inspection and AC1-4 with ordinary
   native review/gates. Landing grants no install/restart/unpark or M2 exit.

## External authority probe & evidence timing

The retained log captures the actual between-cycle stop after registration.
A read-only selection-boundary probe at 2026-09-16T14:44:56.658Z resolved main
to 05fccc7, then called shipped loadBoardSnapshot/selectCandidates: total 179;
#508 OPEN, labels `["type:slice"]` (active, not ready). Exact a3c69cd planning
gave the same mismatch; main planning passed full validation, returning `[]`.
This proves availability, not a second #508 selection or automatic recovery.
The ignored report retains command/output. Ready regression controls use
synthetic identities, never altered #508 facts. No new endpoint/credentials.

## Defect-class constraints

- `lifecycle-repair-misses-sibling-retained-state-entrypoint` and
  `validation-misses-sibling-seed-path`: inventory initial/successor/saved
  nextCycle, queue issueContext and both adapters. Expose the second stale
  read; leave delivery refresh/direct board CLI unchanged.
- `state-machine-steady-state-missing`: no-selection, pinned in-flight,
  completed/item-stopped and idle are existing states. Exercise routine
  same-main successors and repeated resume, not only first refresh.
- `mutable-reference-breaks-candidate-identity` and
  `retained-artifact-schema-ratchet`: read every snapshot byte at one exact
  commit, pin new context, preserve legacy records without fabricated lineage.
- `fixture-only-health-integration` and
  `mandated-external-assertion-contradicts-authority`: test the real native
  composition with timed authority evidence; distinguish synthetic/live facts.
- `pagination-truncates-authoritative-state` and
  `presence-evidence-treated-as-completeness`: keep the existing complete board
  census and full draft inventory; incomplete authority cannot select/idle.
- `missing-fail-closed-regression-evidence`: execute named local bypass controls
  for changed revision binding/observation refusal with other inputs fixed;
  candidate green/control red. No mutant framework.
- `unrunnable-verification-command`, `parallel-node-verifiers-exhaust-host-memory`
  and `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`:
  resolve commands at implementation head, serialize full gates through the
  admitted wrapper, and never turn a narrow pass into aggregate/load authority.
  Do not raise timeouts or skip tests.

## Verification plan

Planning: `node scripts/planning/check.mjs`, `git diff --check`, and installed
Chase Sets `scripts/brief-lint.mjs` on draft/rendered body; no broad tests.
Native implementation: existing supervision.test.ts/queue-adapter.test.ts
controls, then `pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`,
complete `pnpm test` and three-OS bootstrap. Every full Windows-host gate uses
the container's invoke-heavy-verifier.ps1. No direct heavy bypass/new runner.

## Footprint & chain

Implementation: scripts/dogfood/{supervision.ts,queue.ts,supervise.mjs,
repository-adapter.ts}; adapters/self.mjs and declaration; planning/check.mjs
and declaration; existing supervision/queue-adapter/planning tests; docs/loop.md.
Reuse candidate-board's read pattern, not its scoped gate. One native M1 slice.
Blocked by: none; Blocks: none; ISS-133/148 already landed. ISS-162 owns its
draft/roadmap entry in a separate branch; no dependency or edits there.

## Review packet seed

Full-path/system, native M1 only. Attack ordering, stale issueContext,
mixed revisions, legacy fingerprint drift, orphan omission and M2 leakage.
Inspect AC1-4 outputs, unchanged-source comparison and named bypass results.
Independent complete-sweep pressure review of this exact brief is required
before ready; this unready candidate claims none.

## Operator actions

None for implementation. Pressure review/host registration remain separate.
After native landing, exact-head PASS/three-OS green, host installation still
requires supervisors absent. Do not stop live M2 for this repair.

QUALITY_PROFILE: contract

## Intent surfaces

| Acceptance criterion | Exercised surface |
| --- | --- |
| AC1 | Native nextCycle and full self selector |
| AC2 | Queue composition, selected brief and restart |
| AC3 | Authority observation and blocked output |
| AC4 | Saved-cycle resume and adapter boundary |
| AC5 | Git reads and installation boundary |

## Simplest shape

Read pinned main data through installed code.

## Not built

| Not built | Reason |
| --- | --- |
| Executor updater/temporary checkout | Git reads suffice |
| Retry service/migration | Existing lifecycle suffices |
| Scoped selector/key exception | Genuine mismatch must refuse |

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --- | --- | --- | --- |
| Fresh selection | One fetch/full board census | Git commit/tree | Full draft inventory, no per-issue fetch |
| Context | One immutable revision | Git objects | Snapshot draft/rules |

## Contract compatibility

| Changed contract | Compatibility posture | Removed path |
| --- | --- | --- |
| Optional self planningRevision | Absent retains legacy; no migration | New cycles' executor-HEAD planning reads |

## Glossary impact

none — no new or renamed public names.
