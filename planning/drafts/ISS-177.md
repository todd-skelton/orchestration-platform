---
key: ISS-177
title: "Accept a completed author's sole final JSON verdict after narrative"
labels: ["type:slice"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

Existing unready intake: https://github.com/todd-skelton/orchestration-platform/issues/542.
The [#525 stop](https://github.com/todd-skelton/orchestration-platform/issues/525#issuecomment-5709330533)
records `source-flow-state-unknown` in cycle 9 of
`m1-iss146-147-20260914T2325`. Installed executor
`fd12e67ec4cf3ba6ae7fb0b49494b0d957a2fdf1` rejected the completed author's
narrative-plus-JSON as `malformed-worker-verdict`; removing only the prefix
in memory parsed. That control is NOT a native PASS or receipt.

At planning base `804e9357a88868daab31b211966f645baeca6c02`,
`dispatch-adapter.ts:322-354` already extracts reviewers' sole final object,
but line 422 still uses whole-message `JSON.parse` for authors. ISS-150
explicitly excluded authors, and `adapter.test.ts:994` preserves that old
boundary. ISS-171/172/173 do not repair this parser. No closed repair PR or
stopping-review IDs are supplied; #525 is not replaced.

## Decision

Constraint: change framing tolerance only, retaining native identity, completion,
schema, head and review authority. Simplest alternative: the JSON-only prompt,
which already failed here. Chosen: reuse ISS-150's private extractor unchanged
for authors, with an optional local rename. Accepted downside: parsing tolerates
a prefix the prompt forbids. Revisit when a recorded trace demonstrates another
framing defect; malformed payloads retain existing non-PASS behavior.

## Don't-rebuild pointers

- `scripts/dogfood/dispatch-adapter.ts`
- `scripts/dogfood/flow.ts`
- `scripts/dogfood/queue.ts`
- `test/dogfood/adapter.test.ts`
- `planning/drafts/ISS-150.md`

## Done when

1. **AC1 Recorded framing.** The verbatim final message from the captured trace
   below parses as an author verdict through `parseTrace` and
   `codexAdapter().observe`: narrative beginning "Summary of the work before
   the final verdict.", Markdown Change/Measurements paragraphs and a Gates
   paragraph, blank line, then one compact JSON object and no trailing prose.
   Preserve its 2560-character message and 1068-character summary; the total
   message is not subject to the summary cap. Plain JSON still works. Evidence:
   a minimal recorded-event fixture in `test/dogfood/fixtures/` and focused
   cases in `test/dogfood/adapter.test.ts`, failing against the unchanged base.
2. **AC2 Validation.** With that framing, reject two JSON objects (including an
   earlier non-verdict object), trailing prose, absent/malformed verdict,
   missing/extra author keys, wrong types, invalid enum, wrong run/role,
   malformed head, and a 2001-character author summary; accept a 2000-character
   summary. A validly shaped but wrong base head must still fail at
   `flow.ts`'s `author-wrong-head` check. Retain expected thread identity,
   exactly one successful terminal turn, incomplete/dead/failed-turn handling,
   and last-agent-message selection. A valid FAIL stays failed. Evidence:
   adapter/flow negative controls, including changed thread, missing/duplicate
   completion and a prefixed wrong-head verdict; no coercion or truncation
   into PASS.
3. **AC3 Reviewer compatibility.** Existing ISS-150 prefix, non-JSON brace,
   nested/escaped-string, ambiguity and trailing-text cases stay unchanged,
   as do reviewer keys/findings/G0, serialized 2000-character cap, length
   diagnostics and single retry. Author prompts, output schema and summary
   cap stay unchanged. Evidence: full `test/dogfood/adapter.test.ts` and
   `test/dogfood/flow.test.ts`; update only the obsolete author-whole-message
   expectation, not reviewer assertions.
4. **AC4 Saved completion.** A saved source with an author attempt, completed
   prefixed trace and exit 0, dirty permitted work, and no author terminal
   resumes through the real observer into ordinary terminal persistence,
   candidate commit/reconciliation and independent exact-head review, with
   zero new author launches. Exercise first resume, interruption after commit,
   pending-review and post-review replay. Preserve attempt, trace, prior stop,
   candidate/history and spent allowances; create only ordinary missing records.
   Blocking review or malformed payload never accepts/publishes the source.
   Evidence: flow/queue fixtures with real parser/observer and Git, launch
   counts, exact-head and retained-byte checks; restoring whole-message author
   parsing must break the positive.
5. **AC5 Delivery boundary.** Update only the standing author extraction rule
   in `docs/loop.md`, not archived incident narrative. Require independent
   exact-head review, native local gates and final-head Ubuntu/Windows/macOS
   bootstrap green. Evidence: gate output and executed head/run/job/step
   conclusions. #525's original acceptance, independent review, local gates,
   hosted checks and landing remain mandatory; parsing does not accept its work.

## Evidence and authority timing

Probe `2026-09-17T05:37:18.143Z` reads the completed-turn trace after exit 0,
the lifecycle moment needing its verdict.
ROOT is `D:/Users/ToddS/Source/Repos/chase-sets`:
`ROOT/.orchestrator/artifacts/platform-author-prefix-intake.md` and
`ROOT/.orchestrator/artifacts/iss171-author-parser-repro.mjs` / `.log`.

Retained source directory:
`/root/orchestration-m1/runtime/m1-iss146-147-20260914T2325/iss-171-attempt-1/source`.
`author-attempt.json` names worker `01a0adcf-b2d2-7273-a8d8-3671ae765990`
and `author-239a3e0b-0d58-447a-a0e5-d873a19aa6b7.jsonl`;
trace SHA-256 `ba416b2bb60792af25adca761ca6f8426861377515442491cd30e116eda6f37c`.

Read-only source pointer, not closed-PR salvage:
`codex/run-8206815a33822cf85ef7cf38c7489e0119b73ff7765e4a052e629fec938661bb/iss-171-attempt-1`
at `804e9357a88868daab31b211966f645baeca6c02`, worktree
`/root/orchestration-m1/worktrees-20260914T2325/iss-171-attempt-1-source`.
Only `test/dogfood/queue.test.ts` was modified; no author terminal existed.
Do not reuse that unaccepted edit as this repair or import its verdict.
No new external API shape is needed. Recheck completed hosted jobs before landing.

## Verification plan

Future implementation only: in the native WSL implementation worktree, run
focused tests, then ordinary native gates once, per `docs/loop.md` and
`package.json`:

```bash
pnpm exec vitest run test/dogfood/adapter.test.ts test/dogfood/flow.test.ts test/dogfood/queue.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm test
```

No incumbent Windows wrapper or additional native admission dependency.
No duplicate battery, timeout increases, skips or product edits for green.
Load attribution needs unchanged isolated timing collapse and a complete
one-worker/file-serial gate. Planner: planning:check and scoped formatting only;
no implementation verification.

## Footprint and chain

Implementation: dispatch adapter, the three test files above, minimal recorded
fixture and short loop.md contract edit. Existing flow/queue consumers need
coverage, not runtime edits. Callers: launch's incomplete parse and observer,
then `step` for source, corrective, conflict and gate-correction authors;
reviewers share extraction but retain validation.

Blocked by: none (`M2`, `blockedBy: []`); native M1 implements. Host-only gates:
independent COMPLETE brief review, reviewed three-OS-green planning landing,
EXISTING #542 canonical marker/source link/verbatim draft, project membership,
`pnpm planning:board-check`, explicit readiness. No duplicate intake.

## Defect-class constraints

- `closed-schema-shallow-validation`: retain strict five-key/value validation.
- `retained-artifact-schema-ratchet`: no new fields, migration or history rewrite.
- `lifecycle-repair-misses-sibling-retained-state-entrypoint` /
  `state-machine-steady-state-missing`: AC4 covers retained/replayed states;
  keep dead-worker behavior unchanged across shared callers.
- `fixture-only-health-integration`: real observer/lifecycle, no injected PASS.
- `mandated-external-assertion-contradicts-authority` /
  `trigger-job-masks-real-deploy`: unaltered recording; mutated controls use
  synthetic identities; real hosted steps, not trigger/author claims.
- `stale-review-bound-to-wrong-head` / `unrunnable-verification-command`:
  final-head review/gates; resolve commands from manifests.
- `parallel-node-verifiers-exhaust-host-memory`: one native full gate at a time;
  retain the repository's one-worker/file-serial test settings.

## Simplest shape and not built

Share the existing private extractor at its single role branch.

| Not built                          | Reason                                  |
| ---------------------------------- | --------------------------------------- |
| Parser/framework/receipt/adoption  | Existing extraction and records suffice |
| Author retry/unknown-stop recovery | Only valid completed framing failed     |
| Synthetic PASS/#525 implementation | Original work still needs acceptance    |

## Out of scope and operator actions

Non-goals: generic parser hardening; new retry/adoption/receipt protocols;
schema/prompt/routing/ceiling changes; runtime/executor/config edits by this
lane; start/restart/install/publication/merge/readiness authority.

Only the native loop may reconcile the original saved source after independently
reviewed three-OS-green repair landing, safe host install with supervisors
absent (reobserve the recorded empty census), and canonical authorized resume.
Preserve original stop/worktree/candidate/trace/history. A new repair run may
serve unrelated ready work while #525 stays unready; no transfer, duplicate
owner, fresh #525 attempt or budget reset. Host owns GitHub/install/resume;
no provider credentials or residual decision needed.

Review packet: full-path/system. Attack ambiguity, summary versus serialized
caps, parser-bypassing fixtures, wrong heads, duplicate launches and history
loss using AC1-4 results. Glossary impact: none. Independent brief PASS pending.
