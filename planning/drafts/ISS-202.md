---
key: ISS-202
title: "Replace native model selectors with authorized successors"
labels: ["type:slice"]
milestone: "Unattended self-improvement"
blocked_by: []
---

## Why

Todd authorized successor replacement for incumbent and native routing in
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5786554032.
The host capture is
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/artifacts/successor-routing-authority-20260923.md`.
This grants intake, not installation/start authority or quality evidence.
Incumbent-only candidate `fbbf002` must not supply native rows or defaults.

Re-derived at local `origin/main` `156f912af89dbfcaeb887cb5219516864a618c40`.
Fresh M1 intake, not closed-PR recovery. Local drafts/roadmap contain no successor
replacement; ISS-149/158 already supply routing/escalation, not these selectors.
ISS-199/201 and separately owned ISS-200 are distinct. No native ladder has Terra.

Evidence pointers (at that base):

- `scripts/dogfood/routing.ts:16`: self ladder; whole-ladder independence;
  nonempty strings, not a closed model registry.
- `adapters/chase-sets-routing.json:1`: all fourteen row/review combinations.
- `scripts/dogfood/queue.ts:745`: pre-setup routing, including reconstruction.
- `scripts/dogfood/flow.ts:368`: full config fingerprint equality precedes
  replay of saved placement/rung.
- `test/dogfood/routing.test.ts:50`: reusable finite matrix/independence controls.

## Decision

Successors must preserve native policy and saved identities.
Leaving defaults unchanged is simplest but does not implement Todd's ruling.
Choose literal replacements in existing ladders and current instructions, retaining roles, efforts and order.
Accept a quiescent fresh-run cutover rather than saved-run migration.
The choice is wrong if fresh selection uses a predecessor, history changes or the provider cannot admit an existing effort.

## Done when

1. **Exact matrix.** Replace every shipped `gpt-5.6-luna` placement with
   `gpt-6-luna`, `gpt-5.6-sol` with `gpt-6-sol`, and `claude-opus-5` with
   `claude-opus-5-5`, for self and rows 2/3/4/7/10/14/15 at both review rows.
   Preserve efforts, roles and order: row 2 Luna high/xhigh then
   Sonnet medium; row 4 Astra medium/high then Fable high; row 7/self Astra
   high/xhigh then Fable high; row 10 Sol high then Astra high then Fable high.
   Row 3 and Opus rows 14/15 keep their ladders. Astra/Fable/Sonnet are unchanged.
   Review 11 stays high, review 12 medium; self stays Opus high then Sol high.
   Evidence: exact-array fixtures for all fourteen pairs plus self; reviewers
   disjoint from the entire author ladder, with unused-rung overlap negatives.
2. **Fresh selection.** Fresh self composition and Chase
   composition using the shipped table select those successors before setup.
   Evidence: resolver/queue fixtures and existing marker, malformed-row,
   missing-table and static-self-fallback controls. Shipped defaults never
   select predecessors. Generic operator strings are not a closed roster:
   no global blacklist, static config rewrite or role expansion.
   Remove Terra's current dispatch-roster entry; retire a route only if found.
3. **Compatibility.** Preserve old participant model
   strings, efforts, routing indexes, attempts, failure counts, retries, ceilings
   and launch charges. Evidence: a literal predecessor fixture (not derived
   from updated defaults) remains readable; same-config replay retains its
   saved placement/rung without another launch or budget; recomposition
   with changed ladders retains `conflicting-run-configuration` before launch.
   Keep the old-model learning-note control. Ladder fingerprints preclude
   unconditional resume compatibility: no aliases, backfill or waiver.
   Existing flow/queue controls retain author failure classification/escalation,
   reviewer refusal-only escalation, corrective and delta behavior.
4. **Current documentation only.** Update current selector/placement guidance
   in `docs/model-selection.md` and the current routing example in `docs/loop.md`
   with this policy and cutover limit. Todd's replacement authority transfers
   no predecessor benchmark, score, verdict or capability evidence.
   Native Opus row 14/15 roles grant no incumbent permission or new native roles.
   Evidence: independent doc/table comparison; preserve historical text/fixtures.

## Verification and boundaries

Footprint: the two ladder sources; affected assertions in
`test/dogfood/routing.test.ts`, `test/dogfood/flow.test.ts` and
`test/dogfood/queue.test.ts`; the two docs. Read-only callers:
`adapters/self.mjs`, `adapters/chase-sets.mjs`, queue composition and prerequisite
composition in `scripts/dogfood/supervision.ts`. Queue-adapter, refresh,
supervision and integration-continuation tests cover inheritance, not new defaults.
No production caller changes; literal legacy fixtures remain history.

Implementation verification, under foreground machine admission:

```sh
pnpm exec vitest run test/dogfood/routing.test.ts test/dogfood/flow.test.ts test/dogfood/queue.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
```

Then complete `test` once through
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/invoke-heavy-verifier.ps1`,
with exact worktree/lane/branch/head. No redundant battery, timeout raise, skip
or load attribution from a focused pass. Retain native authoring, independent
exact-head review, all local/after-mirror gates and final-head three-OS bootstrap.
Planning runs only the dependency-free checker and existing scoped formatter.

Defect-class constraints: `validation-misses-sibling-seed-path` and
`lifecycle-repair-misses-sibling-retained-state-entrypoint` require the caller
and fresh/saved-path controls above; `retained-artifact-schema-ratchet` requires
literal legacy readability without rewritten identity; `allowed-values-aliased-as-defaults`
forbids deriving new roles/defaults from provider availability.
`future-capability-documented-as-current` forbids documenting this plan as shipped.
`external-contract-internal-only-validation` requires host admission evidence,
not fixture success. `container-controller-installed-before-review-terminal`
and `stale-review-bound-to-wrong-head` retain exact-head review/install authority.

Host-only, not an AC claiming availability: immediately before an authorized
quiescent install, capture native account_pool/CLI admission for each exact
successor model/effort on the target host, with UTC instant and result. Catalogue
presence/earlier Pool14 probes are neither timely admission nor quality evidence.
Require reviewed three-OS-green code, supervisors absent and authorized fresh
runs/paths. Preserve old runs/configs: no automatic resume, budget reset or
exhausted-lineage re-entry. Preserved-run continuation needs separate disposition.
This grants no probe/install/start or #457 renewal; leave live ISS-199 alone.

Review attacks: matrix omissions, unused-rung overlap, fresh fixtures masquerading
as old history, fingerprint waivers and inherited benchmark claims. Use the
finite matrix and literal predecessor replay, not a Quality Packet. Remain
unready until reviewed publication, main-bound mirror and ordinary native
readiness; implementation is M1 only. No dependencies. Glossary impact: none.

## Out of scope

Incumbent policy transplantation, new roles/efforts, routing or migration
frameworks, cost/telemetry work, failure/retry/budget changes, global string
purges, historical draft edits, live configs/executor/runtime, provider changes,
ISS-199's live WSL run, ISS-200, and installation/start/unpark authority.
