---
key: ISS-159
title: "Retain dependency-install diagnostics through native setup recovery"
labels: ["type:slice", "ready"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

M2 `m2-market-diagnostics-20260915T2345`, #8021/milestone 160, stopped
pre-worker: `dependency-install-failed`, executor
`2b8507e68b7054e6956b777bb84e1df75315ba30`:
https://github.com/chase-sets/chase-sets/issues/8021#issuecomment-5689981462
Runtime: `/root/orchestration-m2/runtime/m2-market-diagnostics-20260915T2345`.
`cs-8021-attempt-1/setup`: pilot complete, source intent/no result/diagnostic.
Attempt 1, history `[]`, routing 7/11, no launches; source/review dependencies
absent. Pnpm cause remains UNKNOWN.

Main rechecked: `d2c956bcd8cbe70719d2d8159df78cef34135bc2` (PR #498).
No closed implementation PR; not replacement of #8021/ISS-110.
Planning predecessor `8efdd903aa56bed38d15eac7af9bad86d49b2f9b` on
`codex/platform-iss159-planning-20260916`; inherited complete sweep
`platform-iss159-pressure-r1.report.md` F1/F2/F3.
No raw-fd, broad equality or Todd-draft-approval reuse.
Read-only incident branch:
`codex/run-8a1a348d675fcc9c8b06cc38a282f3683a9efdaf681b2fd57c9e3b31e4124015/cs-8021-attempt-1`
at `5a9ffe8249bff7a31979200971084764a2f7c7ec`. No product transplant, invented
result, inherited PASS or reconstructed old stdout.

### Don't-rebuild pointers

- `scripts/dogfood/setup-adapter.ts`
- `scripts/dogfood/setup.ts`
- `scripts/dogfood/queue.ts`
- `scripts/dogfood/supervision.ts`
- `scripts/dogfood/delivery-adapter.ts`

### Existing coverage

ISS-152 `gateCommand` retains logs/terminal facts; ISS-141 passes log paths.
ISS-146 owns delivery diagnostics, ISS-151 saved branches,
ISS-157 gate-stop grants, ISS-158 / PR #498 routing, #8021 product diagnostics.
None covers setup; no gate-stop grant reuse.

## Done when

- **AC1: Execution evidence.** Actual installer retains both streams
  through close/flush, without excerpt caps except AC5 credential suppression,
  plus executable, full argv/launcher prefix, cwd, role, exact head,
  exit code/signal or safe spawn/launcher
  failure fields. Attempt storage only. Interrupted output is nonterminal;
  I/O failure is visible, not success; terminate/reap the owned child on capture
  failure, finish both pumps and close files before returning. Evidence:
  `setup-adapter.test.ts` uses real bounded children: exit 7 with both streams
  and trailing output, exit 0, missing executable, launcher/I/O failure.
  No mocked install callback or product install.
- **AC2: Paths reach recovery.** Setup result/error -> queue -> supervisor note
  carries only the failing role's absolute path, no excerpts/argv; fit the
  500-character detail without truncating paths, else use the existing run-state
  evidence anchor. Discover prior invocations; never overwrite. Evidence: real-child
  setup -> queue -> supervisor tests read the files, not mock-only forwarding.
- **AC3: Executor upgrade.** Incident-shaped saved selection,
  attempt/setup and completed stop reach absent source install under the new
  executor at the SAME controllerRoot. Ignore exactly `{controllerRevision}`
  in saved setup-plan comparison; retain ALL other native identity, roots,
  controller, base/pilot/branches/worktrees and dependency launcher/flags checks.
  Old plan stays byte-identical; current executor validation remains required.
  Census admits per-invocation names
  by an owned-name rule, regular files only, no subdirectory; create with `wx`.
  Evidence: local-Git queue-adapter and setup tests: old-executor
  stop -> new-executor composition -> source failure -> explicit resume/success.
  Failed terminal/log survives success; pilot is not reinstalled; review waits
  for source success. Separately change only saved `base`, then only
  `dependencies.ignoreScripts`: each stops `conflicting-record:setup-plan`;
  removing its comparison makes that control red. Live executor drift still
  stops. No migration/hand edit or controllerRoot relocation support.
- **AC4: No false progress.** Preserve exact
  `install --offline --frozen-lockfile --ignore-scripts` and role order. Saved
  intent/absent may replay; present/unknown without completion stays unknown.
  Completed/present is steady/inert; drift stops. One invocation per unresolved
  role per native pass, no silent retry. Evidence: setup/queue fresh, interrupted,
  failed, resumed, complete and drift controls preserve attempt 1, retries,
  history/routing; no worker on failure. Only native success advances setup.
- **AC5: Credential-safe evidence.** Option (a): setup-local byte-state sanitizer
  on stdout/stderr pipes BEFORE writing; no raw file.
  Reject (b): `defaultInstall` inherits env/launcher/config; offline/no-scripts
  prevents fetch/scripts, not credential-bearing errors.
  Case-insensitive markers `authorization`, `proxy-authorization`,
  `cookie`, `set-cookie`, `_authToken`, `_auth`, `_password` suppress the rest
  of the line to CR/LF, with whitespace/quotes/separators.
  `://` or `//` suppress URL authority/tail through ASCII whitespace/EOF,
  including userinfo, query and non-secret URLs; preserve the scheme.
  Stack-frame `file:` paths are suppressed; sanitized parent `cwd`/`argv`
  retain path evidence.
  Emit one `[REDACTED]` per suppressed region; retain delimiters and all other
  bytes. <=32 pending prefix bytes/stream (longest marker 19),
  constant suppression state, no buffered line/URL. EOF: safe bytes only.
  Read <=16 KiB per pump step; await writes with backpressure,
  <=1 pending write per stream; no unbounded promise queue.
  Sanitize parent metadata before serialization; retain argv structure and
  detection state across elements. Safe error code/syscall/path, not
  message/stack. No env dump/raw notes/upload/generic redactor.
  Evidence: real-child labeled fake credentials in BOTH streams,
  split at every marker/value boundary (also split transforms: child writes
  can coalesce), CRLF, EOF, >32-byte secrets/URLs without newlines;
  slow-sink controls retain leading/trailing output in bounded memory.
  Files/notes contain no marker-borne or URL-borne fake secrets; direct-fd bypass
  is red. Control: suppress Node `file:` frames, retain safe parent paths.
  Test metadata/errors; never read real secrets or edit env/config.

## Scope fence

In: setup diagnostics and saved-plan compatibility.

Non-goals: pnpm fixes, online fetch/cache warming, retry/timeout changes,
dependency/lockfile edits, new API/framework, resets/migration/clearing state,
product/routing.

## Decisions already made

Supervisor-owned defect intake under the ISS-159 repair handoff places this in
platform M2 (8, not product 160). Todd approved the pilot, not this draft:
https://github.com/chase-sets/chase-sets/issues/4388#issuecomment-5689576390
Run start, not draft approval:
https://github.com/todd-skelton/orchestration-platform/issues/367#issuecomment-5689978715
No worker restart, terminal-park re-entry or ceiling increase authorized.

## Simplest shape

Pipe through AC5 into per-invocation files, forward the failure path, reuse absent-dependency replay with only controllerRevision exempted.

## Not built

| Not built              | Reason               |
| ---------------------- | -------------------- |
| Stderr tail            | Incomplete           |
| New API/grant          | Replay exists        |
| Overwrite/block replay | Loses history/replay |
| Framework/multi-issue  | One path             |
| Raw fd                 | Unsafe               |

## Footprint & chain

`scripts/dogfood/{setup-adapter,setup,queue}.ts`, existing tests,
one `docs/loop.md` paragraph before `## Planning`.
Callers: supervise selection/stop -> queue composition -> setup -> queue -> note;
direct setup tests share the loop. Both adapters use it. Pending notes finish;
completed notes stay history. Copy module-private `gateCommand`'s wx/terminal/
flush shape into setup-adapter, replacing fd capture with AC5 pipes; no export,
delivery-gate edit or new module.

Native M1 implements after DELTA review/registration, no prerequisites. Do not
rehome ISS-136/137/138 or edit ISS-158. Ready label is not review PASS.

## Verification plan

Gates, then exact-head three-OS `bootstrap`:

```text
pnpm exec vitest run test/dogfood/setup-adapter.test.ts test/dogfood/setup.test.ts test/dogfood/queue-adapter.test.ts test/dogfood/queue.test.ts test/dogfood/supervision.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm test
```

Windows full gates use
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/invoke-heavy-verifier.ps1`
(`test`); heavy commands require exclusive admission, foreground to completion.
Native foreground gates remain mandatory.
No timeout increase/skip. Load attribution requires unchanged isolated timing
collapse AND complete one-worker/file-serial gate. Planning: direct checker,
named formatting/brief-lint only; no install/full battery.

## Defect-class constraints

- `state-machine-steady-state-missing`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint`,
  `validation-misses-sibling-seed-path`: AC3/4 cover direct setup, queue and
  pending/completed stop replay states.
- `retained-artifact-schema-ratchet`, `retry-events-double-count-terminal-outcome`:
  AC3 keeps old records unchanged, old logs unavailable; no new worker attempt.
- `canonical-artifact-directory-masks-missing-payload`,
  `presence-evidence-treated-as-completeness`: AC1/4 require terminal/marker
  checks, not log presence or interrupted output.
- `raw-exception-message-in-structured-log`, `playwright-trace-secret-exposure`:
  AC5 checks retained artifacts, not message-only masking.
- `missing-fail-closed-regression-evidence`: isolate each failure variable;
  candidate-green/bypass-mutant-red: nonzero-exit admission, dropped forwarding,
  AC3 differing-base/ignoreScripts. No evidence framework.
- `fixture-only-health-integration`, `trigger-job-masks-real-deploy`: real child
  and queue-to-supervisor proofs; synthetic success is not M2 progress/deploy.
- `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`,
  `vitest-wall-clock-timeout-under-parallel-host-load`,
  `parallel-node-verifiers-exhaust-host-memory`,
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: verification protocol above;
  no host speculation/process interference.
- `format-check-not-run`, `unrunnable-verification-command`:
  run declared checks; commands resolve in package.json.

## Operator actions

Host alone: after reviewed/hosted-green landing, safe install with supervisors
absent, resume SAME M2 run/config; observe install/workers/delivery. Preserve
state; no M1/ISS-158 interference, pnpm fallback or Git-table bypass. Restart
is not ISS-110 zero-touch evidence.

## External authority probe & evidence timing

None: local execution/replay. `mandated-external-assertion-contradicts-authority`:
use labeled synthetic identities, never altered facts with real external IDs;
terminal/forwarding evidence is actual, not mock-derived.

## Intent surfaces

| Acceptance criterion | Exercised surface |
| -------------------- | ----------------- |
| AC1                  | Installer         |
| AC2                  | Setup/queue/stop  |
| AC3                  | Upgrade/replay    |
| AC4                  | Role loop         |
| AC5                  | Files/notes       |

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

| Data path | Bound | Index expectation | Per-item I/O |
| --------- | ----- | ----------------- | ------------ |
| Capture   | AC4/5 | No DB             | Stream/flush |

## Contract compatibility

| Changed contract | Compatibility posture   | Removed path           |
| ---------------- | ----------------------- | ---------------------- |
| Diagnostics      | Old absence unavailable | Output loss            |
| Saved plan       | Old bytes/live checks   | Revision-only conflict |

## Glossary impact

none — no new or renamed public names.

## Review packet seed

Inherited sweep; DELTA: F1-F3/direct callers, AC1-5, retained failures/census,
completed-stop replay. No self-certified PASS.

## Tier + routing hint

Full path; system. Self routing; planning row 7 override-Todd.

QUALITY_PROFILE: contract
