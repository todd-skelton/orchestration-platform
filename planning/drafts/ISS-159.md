---
key: ISS-159
title: "Retain dependency-install diagnostics through native setup recovery"
labels: ["type:slice", "ready"]
milestone: "Chase Sets delivery adapter"
blocked_by: []
---

## Why

M2 run `m2-market-diagnostics-20260915T2345`, milestone 160 / #8021, started
`2026-09-16T00:14:00Z`, executor `2b8507e68b7054e6956b777bb84e1df75315ba30`,
PGID 3075556 / supervisor 3075574. Pre-worker `dependency-install-failed` stop:
https://github.com/chase-sets/chase-sets/issues/8021#issuecomment-5689981462
Runtime: `/root/orchestration-m2/runtime/m2-market-diagnostics-20260915T2345`.
`cs-8021-attempt-1/setup` has pilot completion at
`6107022715ee9460b6e1fc460d5a5704bc7fd090`, source intent at
`5a9ffe8249bff7a31979200971084764a2f7c7ec`, no source result/diagnostic.
`cycle-1-stop-1.json` / `attempt.json`: attempt 1, history `[]`, routing 7/11,
no launches. Source/review `.modules.yaml`: absent (host `test -f`, exit 1);
944 GB free. Underlying pnpm failure UNKNOWN, not a cache/disk diagnosis.

Platform main rechecked at that executor SHA on 2026-09-16. No closed PR or
blocking review IDs: new platform gap, not replacement of #8021/ISS-110.
Read-only incident branch:
`codex/run-8a1a348d675fcc9c8b06cc38a282f3683a9efdaf681b2fd57c9e3b31e4124015/cs-8021-attempt-1`
at `5a9ffe8249bff7a31979200971084764a2f7c7ec`. No product-code transplant,
invented result, inherited PASS or reconstruction of unavailable old stdout.

Real-child probes confirm `defaultInstall` discards stdout/stderr/errors.
Saved replay retries only absent source; changing controller revision alone
reproduces `conflicting-record:setup-plan`.

### Don't-rebuild pointers

- `scripts/dogfood/setup-adapter.ts`
- `scripts/dogfood/setup.ts`
- `scripts/dogfood/queue.ts`
- `scripts/dogfood/supervision.ts`
- `scripts/dogfood/delivery-adapter.ts`

### Existing coverage

ISS-152 `gateCommand` streams logs/terminal facts; ISS-141 passes log paths.
Reuse those patterns. ISS-146 owns delivery diagnostics, ISS-151 saved branches,
ISS-157 gate-stop grants, ISS-158 / PR #498 routing, #8021 product diagnostics.
None covers setup; do not borrow the gate-stop grant or interrupt ISS-146.

## Done when

- **AC1: Complete execution evidence.** Actual installer retains both streams
  through close/flush, without excerpt caps, plus executable, full argv including
  launcher prefix, cwd, role, exact head, exit code/signal or safe spawn/launcher
  failure fields. Attempt storage only. Interrupted output is nonterminal;
  I/O failure is visible, not success. Evidence:
  `setup-adapter.test.ts` uses real bounded children: exit 7 with both streams
  and trailing output, exit 0, missing executable, launcher/I/O failure. Not a
  mocked install callback or real product dependency install.
- **AC2: Paths reach recovery.** Setup result/error -> queue -> supervisor note
  exposes absolute evidence paths, not excerpts. Recovery discovers prior
  invocations; new stops never overwrite old evidence. Evidence: queue-adapter,
  queue and supervision tests drive this chain and read referenced files.
- **AC3: Replay after executor upgrade.** Incident-shaped saved selection,
  attempt/setup and completed stop reach absent source install under the new
  executor. Old plan stays byte-identical, including historical controller
  revision; current executor validation remains required separately. Preserve
  selected base/pilot/branches/worktrees. Admit owned diagnostic storage through
  setup census. Evidence: local-Git queue-adapter and setup tests: old-executor
  stop -> new-executor composition -> source failure -> explicit resume/success.
  Failed terminal/log survives success; pilot is not reinstalled; review waits
  for source success. No migration/hand edit.
- **AC4: No manufactured progress.** Preserve exact
  `install --offline --frozen-lockfile --ignore-scripts` and role order. Saved
  intent/absent may replay; present/unknown without completion stays unknown.
  Completed/present is steady/inert; drift stops. One invocation per unresolved
  role per native pass, no silent retry. Evidence: setup/queue fresh, interrupted,
  failed, resumed, complete and drift controls preserve attempt 1, retries,
  history/routing; no worker on failure. Only native success advances setup.
- **AC5: Credential-safe evidence.** Before persistence, redact authorization,
  cookie and credential-bearing URL values; retain non-secret diagnostics and
  safe error codes/classification, not arbitrary exception text. No env dump,
  raw log in notes or external upload. Evidence: synthetic secrets in both
  streams (split writes too), errors and command metadata are absent from
  files/notes while ordinary text remains. Follow existing boundary practice;
  setup has no redactor to assume safe. No generic security framework.

## Scope fence

In: setup diagnostics and minimal saved-plan executor compatibility.

Non-goals: diagnose/fix pnpm, online fetch, cache warming, retry policy, timeout
changes, dependency/lockfile edits, new continuation API, generic authority
framework, attempt reset, state migration, clearing intent/stop, product/routing.

## Decisions already made

Todd selects one ISS-159 in platform M2 (milestone 8, not product 160), no code
prerequisite/new Decision. Pilot assignment: #4388 comment 5689576390; start:
https://github.com/todd-skelton/orchestration-platform/issues/367#issuecomment-5689978715
No worker restart, terminal-park re-entry or ceiling increase authorized.

## Simplest shape

Retain per-invocation logs/terminal facts, forward their path through existing setup/stop plumbing, and reuse native absent-dependency replay.

## Not built

| Not built              | Reason                  |
| ---------------------- | ----------------------- |
| Stderr tail            | Incomplete evidence     |
| New API/grant          | Replay exists           |
| Overwrite/block replay | Loses evidence/recovery |
| Framework/multi-issue  | One diagnostic path     |

## Footprint & chain

`scripts/dogfood/{setup-adapter,setup,queue,supervision}.ts`, existing tests,
one `docs/loop.md` paragraph before `## Planning`; signature shims if needed.
Callers: canonical start -> supervise selection/stop reconciliation -> queue
composition -> setup -> queue result/exception -> supervisor note; direct setup
tests share that role loop. Self/Chase Sets both use it. Pending notes finish;
completed notes stay history. No delivery-gate edits or new module.

No blockers; native M1 implements after pressure/registration. Do not rehome
ISS-136/137/138 or edit ISS-158. Ready label is not pressure-review PASS.

## Verification plan

Future implementation, then exact-head three-OS `bootstrap` before landing:

```text
pnpm exec vitest run test/dogfood/setup-adapter.test.ts test/dogfood/setup.test.ts test/dogfood/queue-adapter.test.ts test/dogfood/queue.test.ts test/dogfood/supervision.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm test
```

Windows full gates use
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/invoke-heavy-verifier.ps1`
(`test`); heavy commands require exclusive admission. Native foreground gates
remain mandatory. No timeout increase/skip; load attribution requires unchanged
isolated timing collapse AND complete one-worker/file-serial gate. Planning
runs only planning/changed-format/brief-lint and bounded synthetic probes.

## Defect-class constraints

- `state-machine-steady-state-missing`,
  `lifecycle-repair-misses-sibling-retained-state-entrypoint`,
  `validation-misses-sibling-seed-path`: AC3/4 enumerate states, direct setup,
  queue composition and pending/completed supervisor stop replay.
- `retained-artifact-schema-ratchet`, `retry-events-double-count-terminal-outcome`:
  AC3 keeps old records readable/unchanged and logs unavailable; distinct install
  evidence is not a new worker/implementation attempt.
- `canonical-artifact-directory-masks-missing-payload`,
  `presence-evidence-treated-as-completeness`: AC1/4 require actual terminal and
  native marker checks, not log presence. Interrupted evidence is not success.
- `raw-exception-message-in-structured-log`, `playwright-trace-secret-exposure`:
  AC5 checks retained artifacts, not message-only masking.
- `missing-fail-closed-regression-evidence`: isolate each failure variable;
  execute candidate-green/bypass-mutant-red controls for nonzero-exit admission
  and dropped diagnostic forwarding. No new evidence framework.
- `fixture-only-health-integration`, `trigger-job-masks-real-deploy`: real child
  and queue-to-supervisor proofs; synthetic success is not M2 progress/deploy.
- `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`,
  `vitest-wall-clock-timeout-under-parallel-host-load`,
  `parallel-node-verifiers-exhaust-host-memory`,
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: verification protocol above;
  no unproven cache/disk/load diagnosis or unrelated process interference.
- `format-check-not-run`, `unrunnable-verification-command`:
  run actual declared checks; these commands resolve at the platform manifest.

## Operator actions

No provider credentials. After reviewed/hosted-green landing, host checks M2
absent and shared executor safe, installs stable executor and canonically starts
SAME run/config. Do not interfere with ISS-146/M1. Preserve original state;
observe actual install, worker trace/progress and delivery. Further pnpm failure
is evidence, not fallback authority. Restart is not ISS-110 zero-touch evidence.
Earlier shared-Windows-Git `/root/...` worktree refusal is not fixed here;
no guard bypass, cleanup or dispatcher change.

## External authority probe & evidence timing

None: ACs concern local execution/close/replay, not provider acceptance. Historical
stdout is unavailable; host observes actual recovery/deployment separately.

## Intent surfaces

| Acceptance criterion | Exercised surface               |
| -------------------- | ------------------------------- |
| AC1                  | Actual installer/logs           |
| AC2                  | Setup/queue/supervisor/recovery |
| AC3                  | Saved composition/upgrade       |
| AC4                  | Role loop/accounting            |
| AC5                  | Files/notes credential boundary |

## UI states and design-system sources

none — no UI surface changes.

## Data-path envelope

| Data path | Bound                             | Index expectation | Per-item I/O    |
| --------- | --------------------------------- | ----------------- | --------------- |
| Capture   | Three roles; AC4 invocation bound | No DB             | Stream/finalize |

## Contract compatibility

| Changed contract | Compatibility posture             | Removed path          |
| ---------------- | --------------------------------- | --------------------- |
| Diagnostics      | Additive; old absence unavailable | Discarded output      |
| Saved plan       | Old bytes/current executor check  | Upgrade-only conflict |

## Glossary impact

none — no new or renamed public names.

## Review packet seed

Attack lost output/forwarding, secrets, overwritten failures, census, old-plan
conflicts and completed-stop replay. Evidence: AC1-5 files, caller/state inventory,
real installer controls. No framework expansion or self-certified PASS.

## Tier + routing hint

Full path; system. Native self routing; planning row 7 override-Todd.

QUALITY_PROFILE: contract
