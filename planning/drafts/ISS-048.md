---
key: ISS-048
title: "Run the hosted native-lock compilation and diagnostic experiment"
labels: ["type:probe", "area:runtime", "priority:p0", "risk:compatibility"]
milestone: "Portable substrate"
parent: EPIC-RUNTIME
blocked_by: [ISS-006]
---

## Context and measured blocker

The authenticated ISS-022 result at decision core
`2e08babc6c141ef071eb26e1de399a603ec35f433473d3b60bcf7aa78da62a92`
is `BLOCK_REPLAN`: all three OS-profile slots and every selectable capability
slot, including `runtimeLock`, `destinationLock`, and `custody`, are null. The
original 21-vector probe established useful diagnostic facts, but it did not
select an unrelated-process lock. PR #192 landed the separately coordinated
native candidate, stable witness, build inputs, and refusal fixtures. The
remaining prerequisite is a registered and scheduled hosted native-compilation
path that can build those reviewed sources and run the already approved bounded
experiment on provider-fresh macOS, Windows, and Linux runners.

This issue registers that path. It neither changes the prior result nor grants
authority to the candidate, build, workflow, or diagnostic output.

## Scope fence

In: the smallest reviewed ISS-006 harness/dispatch extension for the literal
`iss022_native_lock_experiment` action, `ISS022_NATIVE_LOCK_EXPERIMENT` runner,
three required OS jobs, stable-only aggregate/record path, authenticated
candidate materialization, source-only hosted compilation, retained build and
provider census, and the exact four-case/twelve-control diagnostic experiment
already frozen in `docs/architecture/contract-decisions.md`.

Out: local compiler discovery or installation, local or self-hosted probe
execution, prebuilt or committed binaries, package lifecycle hooks, candidate
build scripts, production mechanism selection, existing ISS-022 capability or
profile receipt mutation, CAS/storage/absence replacement, process-tree
ownership, credentials, privilege, persistent hosts, release promotion, and
operator-held mutation or promotion authority. This slice does not implement
ISS-004 or reopen the parked ISS-022 source-repair attempt.

## Decisions already made

Use Node 24 with `NAPI_VERSION=8` and the exact OS recipes, stable witness,
candidate interface, fixed lock file, lifecycle barriers, provider evidence,
and refusal semantics in the landed bounded native-lock ledger. Compile only
the two reviewed C sources in separate runner-temp directories with the hosted
image's unprivileged compiler/SDK and verified official Node distribution
inputs. Stable N builds and evaluates candidate N+1; candidate code never
loads in the stable parent, reducer, record job, or diagnostic verifier.

Run exactly, in order, `NATIVE_UNRELATED_EXCLUSION`,
`NATIVE_NORMAL_RELEASE`, `NATIVE_DEFAULT_NON_INHERITANCE`, and
`NATIVE_HOLDER_DEATH_ONCE`. Exercise all twelve frozen refusal controls and
perform one post-death acquisition attempt with no wait, retry, deletion, or
cleanup before that attempt. An unavailable compiler, SDK, header, import
library, or required provider fact is `UNSUPPORTED` or `UNKNOWN`; it never
authorizes installation, fallback, or a partial result.

Each OS report retains exactly twelve control rows with
`{controlId,refused,result}`. Executed controls use only
`{true,"REFUSED"}` or `{false,"VIOLATED"}` for the last two fields.
Unexecuted controls use only `{null,"UNSUPPORTED"}` when stable prerequisite
evidence proves unsupported, or `{null,"UNKNOWN"}` otherwise. In particular,
an unavailable real witness cannot be represented as a successful or failed
refusal. Each control is evaluated independently, so stable parser/guard
controls that remain executable still retain their actual result.

The experiment emits diagnostic artifacts only. The stable reducer must keep
all selectable native capability/profile slots null and must not call an
existing capability receipt, decision-core, profile, or publication writer.
Only a later independently reviewed ISS-022 selection may consume a complete
three-OS result.

## Acceptance criteria and evidence

1. The protected ISS-006 workflow accepts only the literal experiment action
   and dispatches exactly one macOS, one Windows, and one Linux observation job
   plus the stable-only aggregate and record jobs. Workflow mutants reject a
   changed action, runner token, job census, input-selected matrix, optional OS,
   missing `if:always()` collection, candidate registry, or broader permission.
2. Each observation job materializes the exact reviewed stable and candidate
   sources through the authenticated candidate-file consume/delete boundary,
   verifies the actual Node distribution inputs, and compiles only those two C
   sources with the frozen argv on the hosted toolchain. The retained build
   census binds source/revision, stable fixture/build/loader bytes, Node
   headers/import library, compiler/SDK and Node identity, argv, outputs, and
   all lengths and SHA-256 values. Mutants reject source, revision, flag,
   header, output, architecture, ABI, loader-path, environment, or extra-file
   substitution.
3. Every OS reports the exact four cases in order and all twelve negative
   controls. A runnable control must execute and retain its actual refusal or
   violation; a control blocked by an unavailable exact prerequisite must use
   the nullable tagged arm and cannot claim refusal. Stable observations prove
   same-file identity, unrelated-process exclusion, explicit release while the
   holder lives, default non-inheritance, and the single immediate post-death
   witness attempt. Missing barriers, candidate-only claims, an inherited or
   wrong handle, wait/retry/delete behavior, a second post-death attempt, an
   invalid control arm, or an incomplete control census refuses the result.
4. Each OS uploads its one exact immutable diagnostic archive. The stable-only
   aggregate downloads exactly three same-attempt archives and retains the
   complete build, case, control, transcript, cleanup, and provider census.
   The record/verifier checks the exact protected revisions, jobs, conclusions,
   artifact bytes, protection state, and at least 30 days remaining retention.
5. The final disposition is diagnostic `PASS|FAIL|UNSUPPORTED|UNKNOWN` for each
   premise and the complete three-OS census. Collection success cannot upgrade
   a failed premise. Every existing ISS-022 selection/profile/capability slot
   remains null, the original 21-vector result remains byte-identical, and no
   release, production, or promotion receipt is produced.

## Verification plan

Delivery is staged at the independently measured boundaries below. The first
slice runs the focused conformance contract and hosted-plan tests, typecheck,
format, bootstrap verification, planning checks, and three-OS CI. Later slices
add their focused observation and terminal commands before workflow activation.
Only after all stages land may protected main accept the literal dispatch and
verify its terminal three-OS artifacts from stable N. No local native command or
probe is permitted in any stage.

Before stage-two source, round 430 closes the control result vocabulary and
retained-file presence rules as a docs-only definition change. Its evidence is
planning validation, exact issue/draft parity, focused format/diff checks,
three-OS bootstrap CI, and independent review. It adds no runtime or dispatch
surface and does not count unavailable controls as executed.

Round 431 then measures acquisition as an independently testable stage-two
boundary. The first stage-two source slice downloads only exact-version official
Node inputs and captures the already-installed hosted toolchain through fixed,
mockable process boundaries. It neither materializes candidate source nor calls
the builder or coordinator. The following slice owns candidate consume/delete,
preparation, all controls/cases, and archive sealing as one complete single-OS
observation transaction.

## Predicted footprint and dependencies

Rounds 429 and 431 measured that the registered end state crosses independently
attackable plan, acquisition, single-OS observation, and hosted aggregation
boundaries. Delivery therefore has four ordered implementation stages:

1. register the literal action, closed runner token, exact required three-OS
   registry/matrix, authenticated stable bundle, and case/control/prerequisite
   censuses in the existing hosted plan path; keep the workflow trigger absent;
2. acquire exact-version official Node distribution inputs and capture only the
   already-installed hosted compiler/SDK with fixed commands and retained
   evidence; keep candidate materialization and native compilation unreachable;
3. implement one complete stable single-OS observation owner for authenticated
   candidate consume/delete, source-only build, four cases, twelve exact
   executed-or-unavailable control rows, and the retained archive, delivered as
   the ordered sub-slices 3.0 through 3.5 defined below;
4. activate the exact workflow trigger and add the stable-only three-OS
   aggregate, provider record, and terminal diagnostic verifier.

Each stage must leave the ordinary conformance action byte-stable and all
ISS-022 selection/profile/capability writers unreachable. Stage one may touch
only the conformance runner-token catalog, hosted plan implementation, one
private native-lock plan census module, their focused tests, and this planning
record. Stage two may add only the private acquisition owner, its mocked tests,
and authenticated stable/test census updates. Later predicted footprints are
bounded to their named owner layers.
No stage adds a public capability schema, package dependency, installed tool,
production consumer, or binary. A stage that crosses the next boundary is new
replan evidence.

This registration inserts `ISS-048` after `ISS-006` and before `ISS-022`.
`ISS-006` supplies the only admitted protected hosted harness. `ISS-022`
consumes a complete independently reviewed diagnostic result and remains the
only owner of lock selection; `ISS-004`, `ISS-005`, `ISS-020`, and `ISS-031`
remain blocked behind it. The edge replacement is the transitive reduction;
retaining a direct `ISS-006 → ISS-022` edge would be redundant.

### Stage-three ordered sub-slices

Decision #278 ruling 1 admitted one bounded stage-three replan and definition
round and fixed the order below. The archive root, the three sibling
runner-temp roots, the archive-relative mapping, the archive status of
`case-diagnostics.json` and the custody root, the post-case second census, the
retained-file bound, the per-OS reducer choice, the line-ending normalization
decision and the Vitest `.mjs` registration are closed in the "Single-OS archive
layout, roots, and the post-case census" subsection of the bounded native-lock
experiment ledger in `docs/architecture/contract-decisions.md`. Each slice below
is a separate packet under the ordinary eight-lane attempt ceiling, with its own
pressure-test round record, its own independent exact-head review by a different
lane, and the hosted three-OS bootstrap as its executable evidence. Stage four
is unchanged and no slice adds a workflow trigger.

Standing fences apply to every slice and are not repeated per slice: no local
compiler, addon, probe, or lock experiment; provider-fresh hosted runners only;
the ordinary conformance action byte-stable; every ISS-022
selection/profile/capability writer unreachable; no synthetic result becomes a
native observation; no worker certifies itself; no new public schema, package
dependency, installed tool, production consumer, or committed binary; no change
to the four case IDs, twelve control IDs, four control arms, report members, or
per-OS vocabularies. Every source slice's footprint also includes
`scripts/conformance/hosted-native-lock-plan.mts` and
`test/conformance/hosted-plan.test.ts`, because any stable or test file it adds
must enter the authenticated census in strict raw-UTF-8 order. Common author
verification is the local planning, typecheck, format, and diff gates; every
executable test runs only in the hosted three-OS bootstrap, and every
stage-three test is a `.test.ts` file. Two further slices are registered in the
ledger subsection and are sequenced after slice 3.5: a bounded `.gitattributes`
normalization slice before any stage-four source, and the separate Vitest
`.test.mjs` discovery slice. Neither is folded into a stage-three packet.

Sub-slice 3.0, landed. Extend the stage-one native-lock harness and test census
to the landed classifier. Landed on protected main as PR #279 at exact
`e1009e0` and recorded in round 454, after a read-only audit, one source
attempt, and an independent exact-head review returning PASS, with the hosted
three-OS bootstrap green on the first attempt. It added the three classifier
modules declared by `stableCaseFiles` to `harnessPaths` and the landed
classifier test to `testPaths` in strict raw-UTF-8 order, with one real-clone
digest-moving mutant per new row. Nothing in this sub-slice remains open.

Sub-slice 3.1. Own candidate consume/delete, acquisition, and the source-only
build as one private preparation transaction.

- Scope fence. One new private module that, given an authenticated hosted
  native-lock plan context and a canonical runner temp, allocates the three
  sibling roots defined in the ledger subsection, calls the landed acquisition
  owner, then inside the landed authenticated candidate consume/delete path
  calls the landed build-inputs preparation, and returns the pending
  preparation plus an honest available, unsupported, or unknown state.
- Decisions relied on. The ledger's build-identity and hosted-execution
  clauses; the landed required-input census items one through six; the ledger's
  three-root, emptiness, and externality definitions; round 431's measured
  acquisition boundary.
- Acceptance evidence. Synthetic vectors only, through the landed mockable
  process and acquisition boundaries: a happy path whose synthetic compiler
  writes a non-loadable output yields two built rows with a null loaded member
  and a sealed census equal to the actual tree; an unsupported acquisition never
  invokes preparation and stays unsupported; a malformed, redirected, or
  oversize acquisition is unknown; an unavailable toolchain is unsupported; an
  ambiguous toolchain capture is refused rather than taken as the unavailable
  branch; a tampered candidate subject digest refuses and the candidate root is
  still deleted; a consumer that throws mid-build still deletes the candidate
  root; a candidate root moved during the consume refuses; a non-empty
  acquisition or preparation root refuses; overlapping roots refuse; and a file
  added under the archive root after the builder returns is refused by the
  post-case census.
- Controls. None are emitted. Two guards are exercised as ordinary mutants and
  later back `WRONG_RANGE_OR_FLAGS` and the pre-load half of
  `BUILD_OR_LOADER_SUBSTITUTION`. `BYPASS_LOCK`, `PREMATURE_UNLOCK`, and
  `RETAIN_AFTER_RELEASE` are unsupported or unknown by construction here and
  until stage four executes.
- Exact footprint. `scripts/conformance/hosted-native-lock-preparation.mts`
  (new), `test/conformance/hosted-native-lock-preparation.test.ts` (new),
  `scripts/conformance/hosted-native-lock-plan.mts`,
  `test/conformance/hosted-plan.test.ts`, and one pressure-test round record.
- Non-goals. Addon load, any `.node` require, fixture spawn, case, control,
  report, archive, workflow, runner-token branch, or provider call.
- Review attack surface. A candidate root outliving its consume; the build
  reading candidate bytes outside the consume window; acquisition output placed
  inside the preparation root so the externality check is bypassed; an unknown
  capture silently taken as the unavailable branch; a real network or compiler
  call escaping a boundary in a test; a deleted candidate root reported as a
  successful build; the loaded member filled before any load.
- Routing. Source row 7, `claude-opus-5` at high effort, placement provisional,
  `gpt-5.6-sol` high fallback; review row 11, `claude-opus-5` high by a
  different lane; budget intent at most $15 source and $25 review.

Sub-slice 3.2. Implement the captured-input control census and the closed
control record vocabulary.

- Scope fence. A private control module producing ordered control records for
  exactly the twelve IDs, with the nine captured-input rows executed against the
  landed guards and parsers and the three witness-dependent rows emitted from
  stable prerequisite evidence in a nullable arm. It writes the fixed control
  input and observation files, honouring the rule that an unavailable row
  forbids its input file. No native call, no spawned process, no archive.
- Decisions relied on. The ledger's four closed control arms, its control and
  case vocabularies, its control order, independence, and prerequisite
  derivation rules, its twelve-row census table, and its fixed control paths and
  presence rules; round 430 in full.
- Acceptance evidence. For each of the nine executable rows the named mutant
  reaches the real guard and is refused, and deleting that guard makes the row
  violated. Arm mutants for every invalid pairing, a missing row, a duplicate
  row, a reordered census, and an input file present on an unavailable row are
  each refused. An unavailable witness must not erase an executable parser row.
- Controls. Executable synthetically here: `INHERITABLE_FLAGS`,
  `INHERITED_IDENTITY`, `WRONG_CUSTODY`, `WRONG_RANGE_OR_FLAGS`, the pre-load
  half of `BUILD_OR_LOADER_SUBSTITUTION`, `MALFORMED_OR_FORGED_FACTS`,
  `FALSE_DEATH_OR_RETRY`, the per-OS half of `MISSING_OR_MIXED_CENSUS`, and
  `CAPABILITY_CONFUSION`. Unsupported or unknown by construction until stage
  four: `BYPASS_LOCK`, `PREMATURE_UNLOCK`, `RETAIN_AFTER_RELEASE`, the post-load
  half of `BUILD_OR_LOADER_SUBSTITUTION`, and the cross-OS and cross-attempt
  half of `MISSING_OR_MIXED_CENSUS`.
- Exact footprint. `probes/portable-primitives/experiment/controls.mjs` (new),
  `test/native-lock-experiment/controls.test.ts` (new),
  `scripts/conformance/hosted-native-lock-plan.mts`,
  `test/conformance/hosted-plan.test.ts`, and one pressure-test round record.
- Non-goals. The three witness-dependent fixtures, control-phase wiring, cases,
  report, archive, workflow.
- Review attack surface. A synthetic mutant reported as a native observation; a
  refused arm for a row whose mutant never reached the guard; blanket
  unsupported propagation hiding an executable parser row; the control-local
  refused word leaking into the per-OS vocabulary; a guard-deletion mutant that
  stays green; an input file written for an unavailable row; a control census
  derived from anything but the landed control ID list.
- Routing. Source row 4, `gpt-5.6-sol` at high effort; review row 12,
  `claude-opus-5` high by a different lane; budget intent at most $15 source and
  $25 review.

Sub-slice 3.3. Wire the control phase and add the three call-interception
fixtures.

- Scope fence. The three stable-authored JavaScript call-interception fixtures
  for `BYPASS_LOCK`, `PREMATURE_UNLOCK`, and `RETAIN_AFTER_RELEASE`, run through
  the landed control-phase seam before the case phase, releasing known resources
  on the same fixed file between fixtures. No new native binary, no intentional
  inheritance, and no additional holder-death run.
- Decisions relied on. The ledger's control order, independence, and
  prerequisite-derivation rules and its first three control rows; the landed
  case-context control-phase seam.
- Acceptance evidence. Each fixture's claim is refused by a real witness call,
  never by the fixture's own report; with no witness available each of the three
  emits the nullable arm derived from the same stable prerequisite facts the
  report carries; a fixture reporting success while the witness contends is
  violated; the control reset refuses while any resource is open or the journal
  is unfrozen; the case phase refuses if a control left the witness locked.
- Controls. These three remain unsupported or unknown until stage four. This
  slice proves the arm derivation, never a refusal by a real contending witness.
- Exact footprint.
  `probes/portable-primitives/experiment/control-fixtures.mjs` (new),
  `probes/portable-primitives/experiment/controls.mjs` (edit),
  `test/native-lock-experiment/control-fixtures.test.ts` (new),
  `scripts/conformance/hosted-native-lock-plan.mts`,
  `test/conformance/hosted-plan.test.ts`, and one pressure-test round record.
- Non-goals. Cases, report, archive, workflow, any extra death run.
- Review attack surface. A fixture's own claim accepted as refusal evidence; a
  control that leaves the witness locked into the case phase; a second
  holder-death attempt hidden in a control; a synthetic addon reaching a path a
  real run would treat as native evidence; the death case repeated to find a
  favourable result.
- Routing. Source row 7, `claude-opus-5` at high effort, placement provisional,
  `gpt-5.6-sol` high fallback; review row 11, `claude-opus-5` high by a
  different lane; budget intent at most $15 source and $25 review.

Sub-slice 3.4. Produce the exact per-OS diagnostic report from a completed
preparation, control phase, and case phase.

- Scope fence. Run the landed case-context setup, the control phase, the case
  runner, and finalize; complete each build's loaded member from the actual
  post-load rehash; assemble the report's exact members; and recompute the
  per-OS result in the separate per-OS reducer the ledger subsection fixes,
  leaving the landed transcript reducer byte-stable.
- Decisions relied on. The ledger's report member table, event shapes, error
  number reduction, precedence rules, and the requirement that an observed
  per-OS result needs four observed case rows and twelve refused controls; the
  ledger subsection's reducer choice.
- Acceptance evidence. The report member census is exact and closed;
  coordinates equal the plan, provider, and actual process inputs; a case row
  with no events is unknown unless a verified unsupported prerequisite
  propagates unsupported; an observed result is unreachable while any control is
  not refused; the precedence reduction retains every individual failure; the
  loaded member stays null whenever no load happened; and a report whose result
  disagrees with the recomputed reduction refuses.
- Controls. Unchanged from sub-slices 3.2 and 3.3. In any test-only run all four
  cases are unsupported or unknown by construction, so this slice's positive
  evidence is structural and never a native observation.
- Exact footprint. `probes/portable-primitives/experiment/report.mjs` (new),
  `probes/portable-primitives/experiment/per-os-reduction.mjs` (new),
  `scripts/conformance/hosted-native-lock-preparation.mts` (edit),
  `test/native-lock-experiment/report.test.ts` (new),
  `scripts/conformance/hosted-native-lock-plan.mts`,
  `test/conformance/hosted-plan.test.ts`, and one pressure-test round record.
- Non-goals. Archive sealing, upload, aggregate, provider record, terminal
  verifier, workflow, and any edit to the landed transcript reducer.
- Review attack surface. Collection success upgrading a failed premise; an
  observed result reachable with an incomplete control census; the loaded member
  filled from the pre-load hash; a case result inferred from the candidate's own
  reply; a watchdog row not forcing unknown; vocabulary crossing between build,
  case, control, and report rows; a per-OS reducer trusting the submitted
  result.
- Routing. Source row 7, `claude-opus-5` at high effort, placement provisional,
  `gpt-5.6-sol` high fallback; review row 11, `claude-opus-5` high by a
  different lane; budget intent at most $15 source and $25 review.

Sub-slice 3.5. Seal the one immutable per-OS diagnostic archive and expose the
stable single-OS observation entry, still unreachable from any trigger.

- Scope fence. Derive the finite retained-file census from the report, builds,
  and fixtures using the ledger subsection's archive root, mapping, second
  census, and bound; hash every retained file with the existing artifact
  manifest machinery; refuse missing and extra files; write the immutable output
  directory; and add the native-lock runner-token and per-OS job entry parallel
  to the ordinary hosted observation entry without editing it or the landed
  observation-archive verifier.
- Decisions relied on. The ledger's archive name and five-archive attempt
  census, its path hygiene rule, its fixed control and transcript paths and
  presence rules, the ledger subsection in full, and round 429's non-goal that
  stage three adds no workflow surface.
- Acceptance evidence. Exact-census refusal for a missing control file, an extra
  file, a path with a dot or parent component, a symlink, and a duplicate; the
  ordinary six-file observation census and the ordinary hosted observation entry
  remain byte-identical under a byte assertion; and the new entry refuses a
  wrong runner token, a wrong job identifier, a wrong OS family, and a context
  whose provider-run digest does not recompute.
- Controls. The per-OS half of `MISSING_OR_MIXED_CENSUS` becomes fully
  executable here; its cross-OS and cross-attempt half stays unknown until stage
  four.
- Exact footprint. `scripts/conformance/hosted-native-lock-observation.mts`
  (new), `test/conformance/hosted-native-lock-observation.test.ts` (new),
  `scripts/conformance/hosted-native-lock-preparation.mts` (edit),
  `scripts/conformance/hosted-native-lock-plan.mts`,
  `test/conformance/hosted-plan.test.ts`, and one pressure-test round record.
- Non-goals. Any workflow file; the aggregate, record, and terminal verifier of
  stage four; and any change to the landed hosted observation, hosted aggregate,
  or GitHub artifact modules.
- Review attack surface. An archive census derived from a caller-supplied list
  instead of the report; hashes substituted for retained bytes; the ordinary
  observation path altered; a hidden trigger or dispatch registration; an entry
  point reachable from the landed workflow; fabricated retention or attempt
  facts.
- Routing. Source row 4, `gpt-5.6-sol` at high effort; review row 11,
  `claude-opus-5` high by a different lane; budget intent at most $15 source and
  $25 review.

## Operator actions

None at registration. Hosted execution later uses ordinary provider-fresh
GitHub macOS, Windows, and Linux runners and existing repository Actions access.
No local compiler, native install, self-hosted runner, credential, privilege,
spend, administrative change, or promotion decision is requested.

## Glossary impact

None.

## Independent review packet seed

Attack candidate self-certification, stable/candidate revision or authorship
collapse, candidate source in a stable-only process, workflow/action/job/runner
substitution, compiler or header provenance, shell or environment injection,
source/output time-of-check drift, extra loadable files, prebuilt binaries,
wrong file/identity/range/handle, premature unlock, inheritance, Windows release
lag, retries or sleeps before owner-death acquisition, missing controls, forged
provider/artifact/retention facts, contradictory nullable control arms,
fabricated refusal under unavailable prerequisites, mixed attempts, partial-OS inference,
diagnostic success converted to capability authority, and any non-null profile
or production selection.
