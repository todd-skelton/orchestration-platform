---
key: ISS-050
title: "Implement reviewed project mutation and adapter conformance"
labels: ["type:slice", "area:integrations", "priority:p1", "risk:authority"]
milestone: "Minimum orchestration kernel"
parent: EPIC-ADOPTION
blocked_by: [ISS-009]
---

## Registration context and outcome

Project plan/apply and the full adapter-conformance command remain placeholders
because no live disposition produces `APPLY/PROJECT`. Register their follow-on
after `ISS-009`, whose independently reviewed result and disposition supply the
first complete target, effect, transaction, and review-authority inputs. This
issue remains thin and blocked until that concrete consumer exists.

## Scope, non-goals, and acceptance evidence

In: elaborate and implement the existing project-mutation request/plan/apply
representations, fresh dry-run/read-back, idempotency, the public plan/apply
handlers, and the two-fixture `adapters:conformance` matrix. Out: candidate or
self-review as permission, release promotion, dispatch allocation, recovery,
resource reclaim, provider-specific mutation, and consumer policy.

Acceptance evidence must prove exact reviewed target binding, no write before a
fresh complete plan, one selected idempotent effect set, complete postimage or
typed uncertainty, replay without a second apply, and refusal of changed review,
target, mapping, observation, or transaction facts. Exact verification commands
are due at reviewed elaboration before execution.

## Verification plan

`pnpm run adapters:conformance`

The existing root command remains a fail-closed capability placeholder until
this issue is elaborated and implemented; registration does not activate it.

## Predicted footprint and review attack surface

Expected ownership is existing adapter-sdk project mutation handlers, fixture
adapters, contract consumers, CLI rows, and their focused conformance tests.
This registration transfers only the existing fail-closed root
`adapters:conformance` placeholder owner literal from ISS-013 to ISS-050; it
adds no implementation or successful command path.
Review should attack review-result substitution, dry-run/apply drift, partial
effects, idempotency collisions, ambiguous postimages, placeholder success,
and any adapter plan writing engine or release authority.

## Operator actions

None.
