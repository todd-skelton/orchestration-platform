---
key: ISS-210
title: "Run the full Windows bootstrap test suite in three isolated serial shards"
labels: ["type:slice"]
milestone: "Unattended self-improvement"
blocked_by: []
---

## Why

Todd's standing faster-and-cheaper priority explicitly authorizes this bounded
M1 intake: reduce merge-critical elapsed CI time without weakening verification.
The agreed decision is
`ROOT/.orchestrator/logs/astra-platform-windows-ci-latency-decision-r1.report.md`,
sections 1, 3, 4, 5 and AGREE-able wording. ROOT is
`D:/Users/ToddS/Source/Repos/chase-sets` (`/mnt/d/Users/ToddS/Source/Repos/chase-sets`
on WSL). This is authorized platform work, not a speculative optimization
program or a claim that successful slow runs reproduce ISS-160's timeout.

The decision's six measured successful three-OS runs have these job wall times
in minutes, rounded to two decimals, excluding pre-start queue delay:

| Actions run                                                                                    | Windows | macOS | Ubuntu |
| ---------------------------------------------------------------------------------------------- | ------: | ----: | -----: |
| [36037838652](https://github.com/todd-skelton/orchestration-platform/actions/runs/36037838652) |   58.38 | 12.22 |   7.08 |
| [36033097373](https://github.com/todd-skelton/orchestration-platform/actions/runs/36033097373) |   40.72 | 14.10 |   7.53 |
| [36028938088](https://github.com/todd-skelton/orchestration-platform/actions/runs/36028938088) |   57.97 | 14.72 |   7.12 |
| [36022068921](https://github.com/todd-skelton/orchestration-platform/actions/runs/36022068921) |   57.53 | 16.82 |   6.33 |
| [36021537309](https://github.com/todd-skelton/orchestration-platform/actions/runs/36021537309) |   53.10 | 16.60 |   5.60 |
| [36043226758](https://github.com/todd-skelton/orchestration-platform/actions/runs/36043226758) |   56.58 | 14.53 |   9.52 |

These are attributed measurements from the decision's Actions timestamp reads,
not this planning lane's reruns. Run 36037838652 at
`9dbadc52abcf3e145fd75dd13a7fe50fd9c7d0cb`, Windows job 107762260838,
spent 57m51s in bootstrap itself. Its host-extracted Vitest sample reports
3459.77 seconds across 25 files: refresh 1337s and queue 823s dominate.
The three partitions estimate 22.28, 13.72 and 21.66 test-body minutes;
these are scheduling estimates, not measured hosted shard times or cost savings.
Three runners add setup/runner-minutes and consume concurrency. Faster elapsed
time is the primary win; cheaper billed CI and lower token spend remain unproven.

Re-derived against current `origin/main`
`ce4bd4475598d8df475d94173d837229bef3c451`:
`.github/workflows/bootstrap.yml:12-29` runs the full bootstrap on three OSes;
`scripts/verify/bootstrap.mjs:33-47` rejects arguments and runs all four gates;
`vitest.config.ts:5-14` owns discovery, serialization and 30-second limits;
`adapters/self.mjs:26-30` owns the three exact required names;
`scripts/dogfood/delivery-adapter.ts:281-359,1435-1477` owns current-run job
attribution and failure logs. Preserve those boundaries, not a second observer.

## Decision

The complete Windows suite and existing merge guarantee must survive a shorter critical path.
Keeping one serial runner preserves coverage but retains the measured 41-to-58-minute wait, while two runners have an estimated ideal 28.83-minute test-body floor before setup.
Choose three standard hosted Windows runners for refresh, queue and the canonically discovered remainder, with one unchanged required Windows aggregate.
Accept extra setup minutes and concurrent capacity consumption without claiming a cost reduction.
The choice is wrong if equivalent full execution cannot meet the three-run 30-minute target or preserve useful failure diagnostics within this footprint.

## Done when

1. **AC1 Complete explicit partition.** Add an explicit hosted-Windows shard
   mode to the existing bootstrap entrypoint: `test/dogfood/refresh.test.ts`
   alone, `test/dogfood/queue.test.ts` alone, and every other file from the
   ordinary full-suite discovery. Derive the remainder from the canonical
   `test/**/*.test.ts` discovery and effective include/exclude semantics, not
   a handwritten list or count-balanced partition. Fail on unknown selectors,
   missing either singleton, discovery errors or empty selections. At the exact
   candidate head, executed shard file/test identities must be pairwise disjoint
   and their union equal that head's ordinary full Windows suite; compare
   passed/failed/skipped/todo counts and identities, not totals alone. Preserve
   existing platform-conditional skips, with no new skips; 25 historical files
   is not a ceiling on new focused tests. Evidence: qualification-only same-head
   full Windows baseline plus all three shard reports, a new discoverable-file
   remainder control, and executed unknown-selector, missing-singleton,
   overlap, omission and empty-selection controls. Freeze unrelated inputs;
   execute named guard-bypass mutants so each regression fails when its own
   check is removed, rather than passing through another refusal.
2. **AC2 Preserve each runner and all default gates.** Use exactly three
   separate standard `windows-latest` hosted test runners, with at most three
   simultaneous Windows test jobs. Every shard retains format, typecheck,
   planning and before/after source-status checks, plus its complete assigned
   tests. Keep one worker, file-serial/nonconcurrent tests, 30-second test and
   hook timeouts, ordinary isolated hosted workspace/temp roots, fail-fast false
   and the existing 100-minute job limit. Default local `pnpm test` and
   `pnpm run verify:bootstrap` remain full and unsharded; Ubuntu and macOS keep
   their full bootstrap, setup and required names unchanged. Evidence: focused
   invocation/workflow controls and final-head local/full three-OS execution;
   unchanged ISS-160 real-Git fixtures/assertions and zero new timeout/flaky
   retries. An unexplained timeout fails acceptance. Load attribution needs an
   unchanged isolated timing collapse AND a complete one-worker/file-serial
   gate; no timeout increase, product edit or skip is a remedy.
3. **AC3 Unique all-success aggregate.** Keep exactly one required
   `Node 24 / windows-latest`, now a lightweight aggregate depending on the
   three distinctly named Windows shards; it need not use a fourth Windows
   runner. Remove the old unsharded Windows matrix leg rather than duplicate it.
   Run the aggregate after dependency failure and explicitly require all three
   successes. Execute all-green and, for EACH shard, failed, cancelled,
   timed-out, skipped, missing and pending controls: none of the latter may
   certify green or borrow an old success. Early/forced workflow cancellation
   can prevent even an always-run aggregate from executing; its missing or
   cancelled context must remain non-green, never synthetic success. Evidence:
   focused tests of the actual workflow/aggregate composition, named bypass
   mutants and existing owning-run observer tests for failed-only reruns,
   current effective attempts, duplicate names and missing checks. Keep exactly
   the existing Ubuntu/Windows/macOS required names, adapter, ruleset,
   head-attribution and observer contracts; no continue-on-error escape.
4. **AC4 Useful failed-shard evidence.** Retain shard names, run/head/attempt/job
   IDs, job URLs, complete outputs and file/test summaries in workflow evidence.
   Demonstrate that the EXISTING failure-observation path exposes the underlying
   failed-shard diagnostic, including cancellation/job-timeout, not merely the
   aggregate's nonzero result or a link without diagnostics. The observer only
   selects required contexts, and `--log-failed` omits cancelled jobs: exposing
   an aggregate alone does not solve this. Evidence: focused real-composition
   controls and captured completed-run/job/log probes at failure acquisition,
   with owning head/attempt checked before and after capture. A missing log
   remains an explicit failure, not fabricated output. Keep any needed evidence
   plumbing inside the workflow/bootstrap footprint; if this needs runtime
   delivery changes, stop and return to bounded planning rather than expand it.
5. **AC5 Measured hosted qualification.** With host-authorized normal runs,
   achieve Windows-green in at most 30 minutes in EACH of three consecutive
   complete hosted runs at one unchanged candidate head, all three OS suites
   successful, AC1 identities/counts equal and no ISS-160 timeout. Measure from
   earliest Windows shard start through required aggregate completion, including
   setup, imbalance and aggregate scheduling. Also record workflow-created to
   Windows-green, per-shard queue delay, setup time, per-file durations, summed
   Windows runner-minutes, aggregate-runner minutes and full workflow critical
   path. Capture Actions timestamps, runner class, head/run/attempt/job IDs and
   conclusions with UTC observation instants; report unavailable queue timestamps
   as unavailable rather than equating creation time with actual job eligibility.
   Retain all consecutive samples, including failures; no cherry-picking or new
   auto-retry policy. Report failure of the target and replan, never add runners,
   split fixtures or weaken tests. Evidence: same-head three-run table and raw
   reports, ordinary local gates, independent exact-head review and final-head
   three-OS bootstrap green. Triple-run and unsharded-baseline proof are bounded
   qualification, not permanent duplicate merge gates. Update only a short
   `docs/loop.md` note explaining the partition and unchanged guarantees.

## Overlap and salvage

Read-only GitHub checks on 2026-09-24 found no open issue/PR or merged sharding
implementation covering this behavior. Checked the full open issue/PR lists,
shard searches, the registered drafts and workflow/bootstrap/Vitest history.
ISS-160 [#504](https://github.com/todd-skelton/orchestration-platform/issues/504)
is CLOSED. PR [#509](https://github.com/todd-skelton/orchestration-platform/pull/509)
is still OPEN, branch `codex/iss-160` at exact head
`38596f23b04fcf1af26b19bfcde7d0a5df55ff30`; it is not a closed salvage PR.
Its successor [#510](https://github.com/todd-skelton/orchestration-platform/pull/510)
is MERGED, branch `codex/iss-160-attempt-2` at exact head
`a789b347ccbd443c5bad42c281b890153805c5a2`, merge
`05fccc7d3c132c9878838f8c6e060d5f425e1d39`. Reuse only historical evidence;
preserve main's already-landed serial protection. Do not copy old runtime code,
fixtures, old gate receipts or review verdicts over main or reopen ISS-160.

ISS-206/#650 already bounds jobs and captures cancelled REQUIRED-job logs;
it does not shard tests or prove cancellation diagnostics behind an aggregate.
ISS-208/#654 owns criteria parsing, ISS-209 owns pinned-brief recovery, and
ISS-207 owns supervisor survival. None replaces this outcome. Existing
ISS-169/171 fixture work and ISS-194/#593 Git-spawn optimization are adjacent,
not sharding authority. No stopping-review finding IDs or decision issues
govern this fresh intake; the named agreed decision report governs it.

## Footprint and sequencing

One reviewable M1 change: `.github/workflows/bootstrap.yml`,
`scripts/verify/bootstrap.mjs`, focused partition/argument/aggregate tests
(extend `test/bootstrap-workflow.test.ts`), focused preservation cases in the
existing delivery/observer tests, and a short `docs/loop.md` note. A tiny helper
directly used by bootstrap/workflow is acceptable only if needed to test this
path. Callers to inspect: package `verify:bootstrap`, all hosted OS invocations,
local default invocation, aggregate dependencies and the existing owning-run
observer/failure-log consumer. `adapters/self.mjs`, required-check/ruleset tools,
runtime delivery/selection/retry and `vitest.config.ts` are preservation surfaces,
not implementation edits. No new dependency or configuration framework.

Register this row after ISS-209. Native selection sorts keys
(`adapters/self.mjs:132-140`), so ISS-210 follows ready ISS-209 without a new
priority mechanism. A fully ready ISS-207 takes the next UNPINNED slot ahead
of ISS-210; an unready ISS-207 does not block ready ISS-210. No artificial
dependency between them and no readiness waiver. Preserve the live
`m1-iss208-209-20260924T1945` run and ISS-208/209's selected/pinned history;
no preemption, re-pin, rerun, cancellation or restart. Genuine stops use native
recovery, not an optimization detour. Normal host registration must leave
main/draft/board agreement intact before a selection boundary. Leave M2 alone;
neither landing this workflow nor this draft grants executor installation.

## Verification and authority timing

Future native implementation commands, not permission for this planning lane:

```sh
pnpm exec vitest run test/bootstrap-workflow.test.ts
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm test
pnpm run verify:bootstrap
```

Add the actually authored focused partition tests to the worker's evidence;
focused tests never replace a full gate. Native workers retain their gate
contract. Any Windows-host full gate uses
`D:/Users/ToddS/Source/Repos/chase-sets/.orchestrator/invoke-heavy-verifier.ps1`;
direct heavy commands also require machine admission, foreground ownership
and completed children. Never steal a lock or kill unrelated work. This planning
lane runs only typecheck, format:check and planning:check, with no tests/heavy gate.

Existing external-authority evidence: the decision report section 2 captures
completed Actions job start/end fields and required-context/ruleset observations.
`ROOT/.orchestrator/artifacts/iss206-cancelled-log-probes/log-probes.md:9-15`
records completed job 105908007608 (`cancelled` after its job limit), empty
run-level `--log-failed`, and available per-job log; the neighboring job,
check-run, annotation and log files retain the probe. That is the completed-run
log-acquisition lifecycle used by the observer, not evidence that a future
aggregate exports those diagnostics. AC3/4 require timely candidate capture
when dependencies settle, before aggregate evaluation and at completed-run log
acquisition, including current-attempt failed-only rerun behavior. AC5 captures
the actual qualification lifecycle, not a post-merge reconstruction. Store full
logs to files before bounded inspection. Synthetic controls must use explicitly
synthetic IDs, never real IDs paired with invented conclusions.

Operator actions: independent planning review and normal host registration;
host authorization/capture of the qualification runs and any provider-behavior
probe not already established by retained evidence. No cloud-provider credentials
are needed. Recommend standard existing capacity only; if three runners cannot
be admitted without buying capacity, report that constraint, do not purchase.
Keep the draft unready until independent planning review resolves the timely
aggregate/cancellation evidence feasibility and normal registration is complete.
No unresolved product ruling within the agreed scope; a runtime-log expansion
or paid-capacity request requires separate disposition, not an implied decision.

## Defect constraints and review

- `grammar-enumerated-by-hand-not-derived`,
  `structural-guard-certifies-undiscovered-db-test` and
  `incomplete-structural-guard`: derive the actual executable set from Vitest's
  authoritative discovery, prove new-file inclusion, and compare identities.
  Do not count files merely present on disk or maintain a remainder allowlist.
- `presence-evidence-treated-as-completeness` and
  `production-profile-failure-hidden-by-narrowed-test-profile`: reports existing
  or equal totals alone are insufficient; require complete executed sets, every
  shard nonempty and the same full Windows profile, including original fixtures.
- `workflow-terminalizer-missing-early-cancellation`,
  `trigger-job-masks-real-deploy` and
  `enforcement-exit-code-swallowed-by-advisory-boundary`: exercise actual job
  composition, all terminal/nonterminal outcomes and early/forced cancellation;
  aggregate existence is not shard execution. Never green a skipped/missing job
  or add a second terminalizer/runtime service to hide the evidence gap.
- `fixture-only-health-integration`, `external-contract-internal-only-validation`
  and `mandated-external-assertion-contradicts-authority`: trace workflow output
  through the real failure consumer; bind timed provider probes to actual facts
  and keep synthetic proof separate. A cancelled shard's opaque aggregate log
  fails AC4 even when synthetic all-success tests pass.
- `missing-fail-closed-regression-evidence` and
  `ambient-changed-files-input-invalidates-negative-controls`: control ambient
  selectors/environment, hold unrelated inputs valid, and execute targeted
  bypass mutants for partition and aggregate refusals.
- `validation-misses-sibling-seed-path`: inspect every bootstrap caller listed
  above; local defaults and Ubuntu/macOS must not inherit the Windows selector.
- `skipped-result-counted-as-failed-spec`: retain passed/failed/skipped/todo
  identity accounting; intentional baseline test skips are not failed tests,
  but skipped Windows shard JOBS can never satisfy the required aggregate.
- `vitest-wall-clock-timeout-under-parallel-host-load` and
  `aggregate-failure-dismissed-as-environmental-without-a-discriminating-control`:
  retain the first failure and require the unchanged isolated/complete-serial
  controls in AC2; focused green or faster partitions do not exonerate a failure.
- `parallel-node-verifiers-exhaust-host-memory` and
  `worker-tool-ceiling-kills-heavy-verifier-wrapper`: shared-host admission is
  mandatory, poll owned foreground commands in short bounded calls and let all
  children end before release. Hosted runners are separate machines.
- `mutable-reference-breaks-candidate-identity`,
  `stale-review-bound-to-wrong-head` and `reviewed-plan-not-bound`: bind the
  baseline, shards, three qualification runs and fresh independent review to
  the exact candidate; never transfer old branch/head PASS to a new revision.
- `format-check-not-run`, `unrunnable-verification-command` and
  `future-capability-documented-as-current`: use manifest-defined commands,
  check the final diff, and keep estimates/proposed behavior distinct from
  executed evidence and shipped behavior.

Independent review attacks discovery drift, duplicate/omitted tests, ignored
selectors, accidental same-runner concurrency, aggregate early success, stale
rerun borrowing, and loss of cancelled-shard diagnostics. Its omission-revealing
packet is the full-versus-shards identity partition, executed bypass results,
actual workflow/observer call chain, timed Actions captures and the consecutive
run/resource table. G0: no simpler shape meets the target while retaining these
guarantees; two heavy-file exceptions and a discovered remainder need no scheduler.
Full-path/system surface; no Quality Packet under the self planning contract.
Glossary impact: none, no new or renamed public terms; no UI or product data path.

## Out of scope

No coverage reduction, new skips, assertion/fixture rewrites, test-file splitting,
same-runner parallelism, timeout or runner-class changes, paid capacity,
adapter/ruleset/required-check changes, observer/retry/selection changes,
attempt renewal, cache of mutable fixtures/Git directories, timing database,
dynamic scheduler, benchmark dashboard or generalized CI framework. No live
runtime or executor edits, provider access, M2 change, restart or host ownership
transfer. This intake is not a terminal-park re-entry or implementation PASS.
