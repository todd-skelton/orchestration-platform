---
key: ISS-032
title: "Implement confined credential broker and recovery authorization"
labels: ["type:slice", "area:credentials", "priority:p1", "risk:authority"]
milestone: "Minimum orchestration kernel"
parent: EPIC-RUNTIME
blocked_by: [ISS-003, ISS-004, ISS-031]
---

## Context and evidence

ISS-031 selects the only admissible custody topology. Recovery authorization
must be acyclic, broker-authenticated, transaction-bound, and separately
attached to a selected live attempt. Pointer receipts alone do not authenticate
the broker or writer.

## Scope fence

In: confined broker core/service topology selected by ISS-031, credential
references, recovery authorization core, CREATED/CONSUMED/REVOKED state,
native consume/removal and post-selection receipts, authorization attachment,
active-client/successor admission transitions, IPC framing/attestation/replay
defense, teardown, and lifecycle fault/attack tests.

Out: generic pointer CAS/lock/private state capability, contracts, supervisor
attempt creation, release policy, bootstrap workflow, arbitrary callbacks/
network endpoints, secret export, or consumer/project vocabulary.

## Decisions already made

The exact core census/mode unions and consume/revoke/attachment ordering in
`credential-broker.md` are mandatory. ISS-004 performs selections through a
live mutation handle and selected run-current checkpoint. Gate binds the
selected CREATED tip/value/proposal plus core digest; the core excludes gate/
lifecycle/consume/revoke/attachment, so no cycle exists. Historical producers
use scoped ISS-004 producer projections derived from the selected authority
value and the fully walked `authority-history/v1` chain; a missing, unverified,
or truncated chain cannot issue a producer projection. Current projections come
from the selected authority value and chain head. Broker code never walks,
appends, or reinterprets the chain itself.

## Acceptance criteria and evidence

1. The selected ISS-031 cross-principal custody topology denies direct store,
   endpoint, handle-duplication, impersonation, copied executable, replay, wrong
   principal, and worker-role attacks on all three OSes, rerunning the
   ISS-031-owned shared native-store attack suite against the production broker
   service. Secrets never cross IPC or enter argv/environment/journal/receipt.
2. Core canonicalization validates the exact common census and exact BOOTSTRAP/
   SUCCESSOR unions, including distinct predecessor/successor manifests and
   precomputable fence identity. `candidateOperationManifestDigest` is excluded
   and its presence fails as an unknown field. Executable mutants cover one
   mutant per common-census field, one per mode-union field, plus one
   coordinated mutant recomputing all excluded siblings of the candidate
   operation-manifest digest together; excluded fields cannot enter core.
3. CREATED prebinds consumeOperationId/native receipt path. Consumption writes
   and verifies native receipt, requests ISS-004 CONSUMED selection containing
   its digest/core/gate, then writes post-selection receipt over `Dt/Dv/Dr`.
   Partial/reordered/repeated/wrong-native/wrong-gate variants refuse.
4. Revocation writes native removal proof first, selects REVOKED second, and
   writes post-selection receipt third. Archive/tombstone follows exact shared
   removal protocol; bare absence cannot revive authority.
5. Attachment is a separate selected pointer. ATTACHED composes full validators
   for current release, broker, auth, gate/fence roots and ordered histories,
   reservation, LIVE descriptor/launch, argv/process, attempt-log predecessor,
   and current epoch. The TERMINAL attempt-log record is not a launch input
   by construction; a mutant proves the channel refuses stale
   TERMINAL-record replay.
6. First attachment uses the explicit attempt-log genesis record; later
   attachment binds the immediately prior selected TERMINAL attempt-log
   record. Each attachment verifies the full `attempt-log/v1` chain; gap,
   fork, and ordinals above the safe-integer bound refuse.
7. Stable N alone prepares successor admission. The exact selected promotion
   activates N+1 and revokes N; candidate/self-activation, wrong generation,
   moved release/executable/manifest, skipped active pointer, or concurrent
   successor refuses. Pre-fence and abort clients cannot attach or widen scope.
8. All public APIs are total through ISS-002 closed snapshots. Envelope and
   every nested slot test transparent/throwing proxies, symbols, accessors,
   extras, exotics, and mutable/sealed/frozen arrays.
9. Every selected authorization/attachment transition uses the `ORDINARY`
   `MUTATION_COMMIT` arm,
   a live ISS-004 handle, and exact run-current core/selector/post evidence.
   Historical-read, serialized, stale-root, copied, or revoked authority refuses.
10. E(n) broker/client authority revokes at authority CAS. The credential
    service accepts E(n+1) only after ISS-004 derives the rotation SELECTED arm
    from old checkpoint 5, exact successor authority/slot, and the appended
    `Drot`/`Dsc` chain record against the walked head; it
    cannot retain an old client, manufacture chain records, or forge derived
    rotation terminal truth.

## Verification plan

`pnpm --filter @orchestration-platform/credentials test`

`pnpm run test:credential-cold-session-matrix`

## Predicted footprint and dependencies

Consumes the exact authorization state/attachment/archive lifecycle matrices
and inherited core/native/post receipts bound by
`planning/iss-002-schema-disposition.json`; no attachment may name a future
terminal attempt-log record.

Owns predeclared credential/broker packages, native service assets admitted by
ISS-031, `test/credentials/`, and exact ISS-032 slots. Adds ISS-004 as a direct
dependency because authorization/attachment selection must use its private
state mechanism. No contracts, state, release, supervisor, root, planning, or
glossary edits.

## Producer and consumer ownership

ISS-032 produces core/state/native receipts, attachment, revocation, active
client, and admission facts. ISS-004 supplies live handles, selects
pointers/run-current state, verifies the authority-history chain, and supplies
current projections; ISS-014 supplies release/
gate/fence facts; ISS-030 supplies reservation/descriptor/attempt-log facts;
ISS-020 owns final bootstrap composition and provisioning transaction.

## Operator actions

Only the explicit ISS-031/ISS-020 service-install and provisioning actions; no
manual secret copying or alternate endpoint.

## Glossary impact

None.

## Independent review packet seed

Attack custody escape, core/gate cycles, field omissions, receipt reordering,
native-vs-selected mismatch, self-asserted attachment, future-terminal-record
binding, truncated or forked chain walks, stale/fabricated producer
projections, read-handle mutation, run-current crash/fork, candidate
activation, mixed epoch, proxies, and secret leakage.
