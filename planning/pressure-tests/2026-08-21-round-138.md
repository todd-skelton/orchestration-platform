# ISS-002 pressure test — round 138

## Review basis

This is a fresh independent cold removal review of the repaired bounded
recovery-authorization archive literal ledger at exact initially clean and
pushed head `4b9ef7c89c8cfd248d51cb1e2a738e75ff8a5200` on
`iss-002-versioned-contracts`, with exact parent `5acb3c5`. Local `HEAD`, the
local remote-tracking ref, and the read-only `ls-remote` result for
`origin/iss-002-versioned-contracts` all resolved to that exact commit before
the disposition was written.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
implementation authority in `docs/architecture/contract-decisions.md`, the
registered `planning/drafts/ISS-002.md`, rounds 133–137, and the exact
`5acb3c5..4b9ef7c` delta. Relevant common pointer position/value/proposal/tip,
mutation-ID, and `pointer-tombstone-value/v1` contracts were inspected only to
attack the proposed composition. The downstream ISS-004 and ISS-032 drafts
were read only to verify the named ownership boundary. No Chase Sets
repository, project, issue, board, backlog, source, or other consumer system
was read or touched.

The repair changes only `docs/architecture/contract-decisions.md` and
`planning/drafts/ISS-002.md`: 14 insertions and five deletions. It adds no
source, test, package, dependency, command, runtime, roadmap, or board-topology
change. `git diff --check 5acb3c5..4b9ef7c` passed.

## Disposition

`PASS` for the repaired bounded recovery-authorization archive planning slice
at exact reviewed head `4b9ef7c89c8cfd248d51cb1e2a738e75ff8a5200`.

OP-R137-1 is removed. Later composition now directly requires the archive's
`revokePostSelectionReceiptDigest` to equal recomputed actual `Drrp`, while the
generic tombstone independently requires `terminalProofDigest = Drrp`. The
ISS-002 evidence matrix now attacks the archive field, the actual receipt, and
both together through every affected downstream digest. The coordinated case
cannot pass merely by recomputing `Draa` and the tombstone: it must still meet
the actual receipt's authenticated transaction, operation, selected `Dt`,
path, graph, and chronology seams.

No definite semantic, trust-model, dependency, or deletion-grade evidence
blocker remains. The four-member archive is closed, proportional, and
constructible; its path, `Draa`, serializer route, exact authorization
TOMBSTONE position, generic tombstone composition, chronology, acyclicity,
FULL_REQUIRED treatment, and ISS-004/ISS-032 ownership are coherent.

## OP-R137 removal and composed mutant attacks

The sole authority now says, in the archive ledger:

```text
recompute actual Drrp
require archive.revokePostSelectionReceiptDigest = actual Drrp
follow actual Drrp's selected Dt
recompute the actual selected REVOKED Dp/Dv/Dr/Dt graph
```

It separately requires the generic tombstone to bind:

```text
archiveDigest = recomputed Draa
terminalProofDigest = actual Drrp
prior Dt/Dv/Dr = actual selected REVOKED graph
```

These equalities close the prior gap. Replacing only the archive member with
`X` and recomputing `Draa` and `archiveDigest` refuses because `X != actual
Drrp`. Replacing only the actual receipt and recomputing its `Drrp` refuses
against the unchanged archive member. Replacing both and recomputing every
downstream archive/tombstone digest is not a bypass: the actual receipt remains
bound to the constructed transaction/operation path, archive transaction,
selected REVOKED `removalOperationId`, selected `Dt`, actual state graph, and
inclusive time order. A mutant must alter a discriminating authenticated seam,
not merely produce another internally hashed proof bag.

The repaired ISS-002 matrix expressly requires all three classes: independent
archive-field substitution, independent actual-receipt substitution with
recomputed `Drrp`, and coordinated substitution through every affected
downstream digest. Structural archive tests correctly do not claim these
composition equalities; ISS-004 owns them.

## Exact archive census and proportionality

`recovery-authorization-archive/v1` has exactly four unique required members in
ascending canonical JSON member order:

```text
archivedAt:timestamp
revokePostSelectionReceiptDigest:sha256
schemaVersion:recovery-authorization-archive/v1
transactionId:uuid-v7
```

Each retained member has one bounded role:

- `archivedAt` is the durable archive event time after exact revoke-receipt
  read-back.
- `revokePostSelectionReceiptDigest` is the one direct identity of that exact
  upstream observation and is now equal-bound to actual `Drrp`.
- `schemaVersion` closes the record and selects the parser, digest, serializer,
  and compatibility route.
- `transactionId` is the archive's canonical path input and later record/path/
  receipt transaction equality.

Deleting any member loses a directly testable property: durable event order,
upstream receipt identity, branch closure/dispatch, or archive-own location.
Although actual `Drrp` also commits the receipt transaction, it is not a path
locator; retaining the archive-local transaction is the smallest self-
contained defense against moved archive bytes.

The archive correctly omits `Dp`, `Dv`, `Dr`, `Dt`, `Dac`, operation, core,
gate, lifecycle, native receipts, mode, generation, and removal disposition.
Actual `Drrp` commits the revoke receipt's transaction, operation, time, and
selected `Dt`; actual `Dt` commits `Dp/Dv/Dr`; and later composition parses the
actual REVOKED value/proposal/tip. Copying those facts into the archive would
recreate a proof bag without adding a locator, provenance source, or replay
discriminator.

## Path, identity, constructibility, and serializer

The archive path is exact:

```text
installation/recovery-authorizations/<transactionId>/archive.json
```

There is no directory enumeration, latest-file choice, caller path, digest
lookup, alternate root, or implicit operation search. The authenticated
transaction context constructs the archive/state paths. The selected REVOKED
state supplies `removalOperationId`, which constructs the actual revoke-receipt
path. Reading that record yields actual `Drrp` and its selected `Dt`; that tip
then authenticates the actual `Dp/Dv/Dr` graph. The archive therefore needs no
copied operation or selected graph fields.

The sole archive identity is:

```text
Draa = SHA256(frame("recovery-authorization-archive/v1", canonical archive bytes))
```

The frame has exactly one canonical-record part after its literal domain. The
existing generic serializer success arm `{ ok: true, bytes, digest }` is
sufficient and must return exact canonical bytes and exact `Draa`. Untagged
canonical JSON, raw JSON, field-wise framing, common pointer `Dv`, another
archive family, or a caller-selected domain is not an alternate identity.
Exact bytes, a tagged digest golden, untagged and cross-domain inequalities,
schema-specific dispatch, and hostile-input totality are cheap discriminating
evidence within the predicted footprint.

## Authorization TOMBSTONE position and generic tombstone

The authorization-state removal position is exactly the closed record:

```json
{ "mode": "TOMBSTONE", "parts": {} }
```

It hashes only in the derived
`authorization-state-position-tombstone/v1` domain. The same empty shape under
VALUE remains the distinct `authorization-state-position/v1` identity; another
tombstone-capable pointer family uses its own derived domain. Specialized
authorization parsing must accept only TOMBSTONE with exact empty parts, and
common dispatch must select the domain from authenticated pointer kind plus
mode. VALUE substitution, nonempty or nested parts, wrong family, untagged
hashing, or another family's resulting digest refuses.

Empty parts are sufficient. `pointer-tombstone-value/v1` already carries the
archive digest, terminal proof, and exact prior selected `Dt/Dv/Dr`. The common
mutation ID additionally commits authenticated pointer kind/path/`Dp`, the
TOMBSTONE position digest, prior triple, successor tombstone `Dv`, and `REMOVE`
outcome. Copying `Draa`, `Drrp`, or prior graph members into position parts
would add no independent binding.

Later ISS-004 composition must require exact
`RECOVERY_AUTHORIZATION_STATE`, `archiveDigest = Draa`,
`terminalProofDigest = archive.revokePostSelectionReceiptDigest = actual
Drrp`, prior `Dt/Dv/Dr` equal the actual selected REVOKED graph, the same
transaction-scoped `Dp`, and a `TOMBSTONE_PROPOSED/REMOVE` proposal selected
under live stable external mutation authority. Parsing the position,
tombstone, archive, or receipt alone grants none of that authority.

## Acyclic graph and chronology

The complete dependency order is acyclic:

```text
selected REVOKED Dt
  -> revoke post-selection receipt Drrp
  -> archive Draa
  -> common tombstone proposal and selected tombstone Dt
```

Equivalently, following archive evidence backward is `Draa -> Drrp -> selected
Dt -> actual Dp/Dv/Dr`. The selected REVOKED graph contains no `Drrp`, `Draa`,
or tombstone digest. The revoke receipt contains upstream selected `Dt` but no
archive or tombstone identity. The archive contains only upstream `Drrp`. The
tombstone contains upstream archive/terminal/prior evidence, none of which
contains its successor tombstone digest, proposal receipt, or tip. The repaired
equality adds no graph edge and creates no digest cycle.

The inclusive millisecond chronology is sufficient:

```text
selected REVOKED state recordedAt
  <= revoke receipt recordedAt
  <= archive archivedAt
  <= tombstone tombstonedAt
```

Equality is valid at the canonical timestamp precision. ISS-032 owns the
durable production observations and exact create-once/read-back behavior;
ISS-004 owns recomputation and enforcement of the authenticated composed order.
No caller request time, filesystem metadata time, age, lease, PID, or timeout
is admitted as authority.

## Ownership, FULL_REQUIRED behavior, and trust boundary

ISS-032 owns actual core/state/native/post-selection/archive production,
create-once persistence, byte-identical retry/read-back, and durable time
production. ISS-004 owns authenticated paths, selected state graph, operation
and transaction equalities, receipt/archive/tombstone digests, chronology,
prior triple, stable-epoch proposal, exact selected read-back, and CAS. This
planning slice authorizes only structural parsers, path construction, digest
and serializer routes, compatibility registration, and the exact TOMBSTONE
position route.

The archive, receipt, tombstone, and pointer history are immutable
`FULL_REQUIRED` evidence. Missing, conflicting, noncanonical, truncated,
unreadable, deleted, or bare-absent evidence is `UNKNOWN`, never successful
removal. A selected tombstone is retained; there is no record deletion,
retention window, compaction, degraded-audit, archive replacement of required
history, or authority resurrection route.

All vocabulary remains engine-neutral: transaction, operation, receipt,
selected state, archive, pointer, timestamp, and digest are opaque platform
concepts. No Git, GitHub, repository, provider product, endpoint, consumer,
credential-store product, or operating-system secret vocabulary enters the
contract. Archive bytes are evidence rather than authority. A parser,
serializer, digest, path, worker assertion, receipt, candidate verdict, or
candidate epoch cannot issue a capability, perform CAS, certify itself, or
route its own verdict into promotion. Only stable external selected authority
with a live non-serializable ISS-004 mutation handle may request the later
transition.

The pre-existing ISS-032 criterion saying the consume receipt is written over
`Dt/Dv/Dr` remains stale relative to the sole ISS-002 authority, which reduced
that public receipt to `Dt`. It does not widen or block this independently
reviewed archive slice because `contract-decisions.md` is the sole ISS-002
authority and ISS-032 is not executable yet. It must be corrected before
ISS-032 implementation so deleted copied receipt fields are not resurrected.

## Proportionality and deletion test

| Mechanism | Concrete in-scope threat | Deletion result | Smallest disposition |
| --- | --- | --- | --- |
| Closed four-member archive | Unknown bytes or a moved transaction are treated as the durable archive event. | Deleting closure, schema, time, or transaction/path equality admits a direct structural mutant. | Retain exactly four fields; add no proof envelope. |
| Archive-local actual `Drrp` equality | Archive claims a different revoke observation than the actual receipt read back. | Deleting the equality admits the exact OP-R137 arbitrary-digest mutant even when `Draa` and tombstone are recomputed. | Retain one digest and direct equality to actual `Drrp`. |
| Independent/coordinated composition matrix | A forged sibling or fully rehashed proof bag crosses an untested seam. | Deleting a seam lets an archive-only, receipt-only, or coordinated downstream-rehash mutant survive. | Retain the three required mutant classes in ISS-004; add no caller proof bundle. |
| Tagged `Draa` and serializer route | Untagged or another family's bytes share archive identity. | Domain/route deletion admits pinned fallback or cross-domain mutants. | Retain one one-part frame and existing serializer arm. |
| Empty authorization TOMBSTONE position | VALUE, nonempty parts, or another family substitutes removal position. | Deleting mode/parts/domain dispatch admits a direct position mutant. | Retain exact empty parts and the derived authorization tombstone domain. |
| Generic tombstone bindings | Archive, receipt proof, prior graph, or transaction pointer is substituted. | Deleting any equality admits an independent or coordinated substitution. | Reuse the common tombstone/proposal/mutation graph; add no specialized tombstone. |
| FULL_REQUIRED/UNKNOWN | Missing evidence or bare absence is treated as completed removal. | Deletion permits resurrection or unauditable terminal state. | Retain all evidence; no deletion, retention, or compaction. |
| ISS-004/ISS-032 deferral | Structural bytes or candidate output certifies mutation. | Deleting the ownership fence admits caller proof bags or self-authorized CAS. | Retain structural-only ISS-002 scope and stable external authority. |

## Acceptance and predicted-footprint executability

The bounded implementation may touch only the existing recovery and pointer
contract modules, public exports/serializer/registry/compatibility routes, and
focused recovery/archive tests. It may add one total closed four-member archive
parser, reuse the existing transaction archive path, add exact `Draa` and its
serializer route, and add the exact authorization TOMBSTONE-position parser and
common digest dispatch. No new package, dependency, command, provider adapter,
persistence layer, graph validator, runtime mutator, capability, CAS, archive
enumerator, deletion route, or promotion mechanism is authorized.

Structural evidence can remove, add, rename, reorder, null, and cross-type each
archive member; reject every omitted copied proof field; pin exact canonical
bytes/path/`Draa` and serializer success shape; kill untagged and cross-domain
fallbacks; cross VALUE/TOMBSTONE, empty/nonempty parts, and other-family
positions through specialized and common APIs; and prove hostile reflective
inputs never throw. Those tests must not claim actual receipt equality,
selected graph/currentness, chronology, persistence, tombstone selection, CAS,
deletion, or runtime behavior.

The later ISS-004 matrix owns the repaired direct archive-member-to-actual-
`Drrp` equality and independently mutates archive member, actual receipt,
selected graph, generic tombstone archive/terminal/prior fields, position,
mutation, producer epoch, chronology, and selected read-back. The coordinated
archive/receipt mutant must recompute every affected downstream digest and
still refuse at an authenticated upstream seam. ISS-032 owns create-once,
retry/read-back/loss, and production-time faults.

## Verification evidence

All six required gates ran strictly one at a time. The standalone full-suite
process fully exited before the separate bootstrap verifier began. Direct
standalone testing used the increased 600,000 ms test and hook ceiling.

The initial `pnpm run planning:check` launcher emitted no checked-in planning
script output and failed closed after about 125 seconds because it could not
fetch or verify signed release bytes for pinned `pnpm@11.22.0`. The gates then
used the identical checked-in script or lockfile-pinned binary directly. No
source, dependency, or semantic command was substituted. Board, formatting,
standalone testing, and bootstrap used approved process/network access only as
required for their read-only child processes and registered board reads.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`.
   It read only this repository and its registered delivery project.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 33 files and 384 tests in 77.47 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 33 files and 384 tests in 79.21 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This `PASS` clears exact planning head
`4b9ef7c89c8cfd248d51cb1e2a738e75ff8a5200` only for implementation of the
bounded structural recovery-authorization archive parser/path/`Draa`/
serializer/registry surface and exact authorization TOMBSTONE-position parser/
digest route described above. It does not clear ISS-004 composition, ISS-032
production or persistence, provider/native authority, broker mutation,
capability issuance, pointer proposal/CAS, filesystem deletion, runtime
recovery, release promotion, or candidate self-certification.

After this report is written, the sole intended worktree change is
`planning/pressure-tests/2026-08-21-round-138.md`; no implementation, test,
authority document, draft, issue, roadmap, board, or other planning file is
edited, and no commit is created.
