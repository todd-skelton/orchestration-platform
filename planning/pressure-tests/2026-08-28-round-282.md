# ISS-022 parser-equivalence normalizer follow-up — round 282

`PASS` for the Round-281 repair on the exact worktree diff.

The reducer now recomputes the observer lifecycle: an incomplete exit/close
pair is admitted only when an error, stderr overflow, or timeout triggered the
non-terminal path and the post-signal deadline expired. Exact zero-event,
exit-only, and close-only rows without that evidence are refused. Mutants also
prove observer-reachable error, timeout, and overflow rows remain `UNKNOWN`
rather than becoming synthetic evidence.

The repair leaves the prior boundaries intact. Recursive closed snapshots and
the exact one-row/three-child schema remain required; PASS still requires three
accepted byte matches with child digests equal to the parent digest; a coherent
fully observed difference is only `UNSUPPORTED`; summary disagreement or
incomplete evidence is only `UNKNOWN`; malformed tuples, digest contradictions,
proxies, accessors, extra fields, paths, PIDs, and result/PASS claims refuse.

Focused parser-probe and handler tests pass 28/28 and root typecheck passes.
Proportionality remains clean: the repair is one local lifecycle predicate and
decisive mutants, with no registry, receipt, writer, helper, isolation, or
production mechanism added.
