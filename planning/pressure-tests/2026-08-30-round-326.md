# ISS-003 Node/native identity follow-up review — round 326

`PASS` for the exact delta
`9994eff475c85bc16051030e49869b077ff73897..37553055995ca022aa57e65f30d370e280770d01`.
Round 325 remains unchanged historical `PASS` at
`0a3391c992c9c30791d82832919237aa3ab28b09`; this further round is authorized
only by the named downstream Node/native identity blocker.

The blocker is closed. `observation-core.h:30-45` obtains `FILE_ID_INFO`,
reparse attributes, and `BY_HANDLE_FILE_INFORMATION` from the same retained
Win32 handle. `dwVolumeSerialNumber` supplies the 32-bit Node device coordinate
and the high/low file-index words form the unsigned 64-bit inode. Those are the
same assignments libuv makes to Windows `st_dev` from
`VolumeSerialNumber.LowPart` and `st_ino` from `FileId.QuadPart`; no pathname,
JS number, second observation callback, or inferred 128-to-64 truncation is
used.

The addon emits each coordinate as canonical decimal plus fixed lowercase hex
(`addon.cc:57-83,93-98`). The wrapper accepts only plain exact two-member
coordinate records, canonical unsigned decimal, 8/16-digit hex, pairwise
numeric equality, and explicit 32/64-bit maxima
(`windows-reparse-fact.ts:65-88,97-134`). It also cross-checks Node device
against the low 32 bits of the separately returned 64-bit volume serial. The
existing exact 32-hex-digit `FILE_ID_INFO` value remains independent; this
avoids asserting a false portable equivalence between a 128-bit identifier and
libuv's 64-bit file index.

Malformed leading-zero, decimal/hex disagreement, device/volume substitution,
inode substitution, and `2^64` overflow rows refuse
(`windows-reparse-fact.test.ts:83-109`). The retained-handle replace/restore
row compares the expanded identity before, during, after, and against the
replacement (`:186-199`). Most decisively, an actual Node retained
`FileHandle.stat({bigint:true})` equals both native decimal and hex coordinates
on hosted Windows (`:158-173`), so representation correctness is not resting
on a mirrored fake.

Manifest v2 exactly censuses both coordinate members and the expanded identity,
and the build script refuses schema/member drift (`manifest.json:2-4`;
`windows-reparse-fact.mjs:17-34`). Production/test artifact names, sources,
loader separation, and transient cleanup are otherwise unchanged. The exact
checkout has no `.artifacts`, tracked native binary, or status delta.

Hosted run `33327115061` records exact head
`37553055995ca022aa57e65f30d370e280770d01`. Windows job `99299109598` passed
all 8 focused rows and 81 files/767 tests; Ubuntu `99299109563` and macOS
`99299109593` passed 763 with only the 4 Windows rows skipped. All three
reported no source-tree status change. `git diff --check` and artifact/status
censuses pass locally; no local full/bootstrap or native build was run.

## Proportionality

The concrete threat was inability to compare a loader-retained Node handle to
the native observation. Removing either same-handle coordinates or wrapper
equivalence preserves that blocker. Decimal strings prevent precision loss;
fixed hex makes bounds and mutation evidence closed. Passing a handle across
N-API or adding a callback/broker is larger. The 159-addition/7-deletion repair
is therefore bounded, and this record is materially shorter than its delta.
