# ISS-022 base physical observation review — round 264
`BLOCK_REPLAN`.
The executor creates the namespace and opens/closes the root independently per case, and its reuse test requires a second call on that root to fail `EEXIST`.
Consequently `PHYSICAL_EXISTING` and `PHYSICAL_ABSENT_LEAF` cannot run under the ledger's one custody namespace/instance or one original root handle retained across every physical barrier; separate test roots silently produce unrelated `DhostNamespace` and root identities.
Minimal repair: establish one root-handle/namespace session once, retain its original namespace bytes and handle, execute both base rows through that session, compare all root readbacks around each row, then close once; hostile alias/swap rows remain out of scope.
Within each isolated call, open-before-namespace, 32-byte O_EXCL/write/sync/close/readback, fstat/lstat/stat/statfs/realpath checks, constructed ENOENT without enumeration, regular-file checks, canonical leaf bytes, null-on-instability derivation, path omission, and cleanup are correct.
No PASS or production authority is emitted; focused tests pass 3/3 and root typecheck passes, but they currently encode the split-custody defect.
