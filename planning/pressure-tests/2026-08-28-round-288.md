# ISS-022 lock-source census review — round 288

## Disposition

`PASS`.

The exact census delta is one alphabetically placed entry:
`probes/portable-primitives/src/lock.ts` in `iss002HarnessPaths`. The stable
manifest therefore hashes the newly executed lock source with the existing
ISS-002 harness bytes; no test-bundle or other path changed.

`pnpm exec vitest run test/conformance/stable-bundles.test.ts` passed 6/6. Its
complete portable-source census, manifest-order check, LF pin, and compiled/
parked-custody exclusions confirm no extra source, compiled artifact, helper,
or custody widening. Round 287's timer/kill, overflow, descriptor, reducer, and
two-PR trust findings are unchanged.
