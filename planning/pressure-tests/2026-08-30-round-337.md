# ISS-022 verification wrappers — round 337

Disposition: `BLOCK_FIXABLE` at
`d476520174b1a44b7a1272d7b19e07bcff31b4ca`, base
`5c1f072e7ee4816dafcde82672ccde558e15a73e`.
Independent cold reviewer: `/root/iss022_wrappers_review`, Sol high, row 11.

The new publication checker accepted a Uint16Array with Uint8Array's prototype:
copy construction truncated its elements into valid canonical bytes. Its
`instanceof` check also invoked a byte Proxy's prototype trap before refusal.
The host reproduced both defects against unchanged production using six cases
across all three filenames: three malformed arrays returned `ok:true`, and
three Proxy traps ran once. Four existing publication cases still passed.

Required repair: reject proxies first, require the intrinsic Uint8Array brand,
and retain the exact-prototype check and detached copy. Six focused regression
cases provide the deletion test; one intrinsic check is smaller than any new
framework. All other inspected wrapper, census, canonical-byte and digest-join
paths stayed bounded. Local verification and valid records grant no authority.
