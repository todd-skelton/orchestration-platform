# ISS-006 external-child repair review — round 240

`PASS`.

The stable root and child resolve to the caller's already-authority-checked stable tree; non-file, symlink, escape, unreadable, or moved-path observations fail closed.
The copied child, stable module, and candidate module are the exact three external-runtime artifacts, so `argv[1]` and both module URLs expose no checkout path.
Copy precedes candidate execution, and hosted stable-byte recheck plus fail-closed preparation leaves no new in-scope TOCTOU route.
All three artifacts are removed before `rmdir`; residue or removal failure refuses, while hosted identity-checked recursive cleanup remains the outer fence.
Focused tests pass 21/21 with one skip; exact temporary-bundle reproduction passed three walks at 600009700, 372973300, and 387626900 ns with both parents empty.
Proportionality: one validated copy and one census row reuse the existing external root; no authority, isolation, checkout write, dependency, or binary is added.
