# ISS-022 independently authored native witness — round 368

Final static `PASS` at `15ee71d4883344f4ca8a0734b8acfde7fe0e8005` from
independent reviewer `/root/iss022_native_witness_review`, Sol high, row 11.
The witness and tests were authored without reading candidate implementation.

Initial `b4ffcf807c7692fd9ab3863a6aaf392124a468fc` was BLOCK_FIXABLE for
Darwin device sign extension. Repair `daf244783862672250492ae952716f2ef04e348c`
fixed C conversion, but follow-up found a related signed-stat test oracle.
The host executed the exact oracle with three explicitly synthetic boundary
inputs: Darwin high-bit/max and inode high-bit falsely refused unsigned
identities. This was test evidence, not a native filesystem observation.

After two blocks, `/root/iss022_witness_oracle_replan` accepted one test-only
replan for that newly executed defect, followed by one focused independent
review. This did not reset the normal review/repair allowance. The final repair
normalizes expected stat BigInts at native width; addon claims remain unchanged.
Eleven acceptance and ten refusal vectors defend precision and width boundaries.

Native operations, custody `{identity,facts}`, Windows checks, mandatory addon
authentication and native prerequisites remain unchanged. Same-thread second
initialization refuses; general embedded-environment support is not claimed.
The native witness challenges actual lock claims without another service.
Neither source review nor synthetic oracle evidence establishes compilation,
locking, inheritance, custody or holder-death behavior. Host execution remains
required; no capability or release authority is granted.
