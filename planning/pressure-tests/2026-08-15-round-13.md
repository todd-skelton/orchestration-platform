# Pressure test — round 13

**Disposition:** `BLOCK_REPLAN`

The thirteenth independent review found three implementation blockers. The
39-issue packet remains unregistered pending a fresh review of the repairs.

| Finding | Repair applied |
| --- | --- |
| `OP-R13-001` — Same-user workers could potentially bypass broker clients and read native credential-store items or host keys directly. | Made native-store denial an explicit admission gate. `ISS-023`, `ISS-031`, and `ISS-035` now run byte-hashed direct `CredReadW`, `SecItemCopyMatching`, and Secret Service attack helpers; `ISS-021`, `ISS-032`, and `ISS-038` rerun installed-byte attacks. Same-user read success forces `BLOCK_REPLAN` toward a separately confined broker/worker principal or non-exportable signing boundary, with exact IPC attacks required for cross-principal designs. |
| `OP-R13-002` — The private composition contract required closed bundles but named no bundler or executable configuration. | Pinned `esbuild@0.28.2`; assigned the sole script/config and exact two-target option set to `ISS-000`; fixed output identities and target-scoped alias resolution; added clean three-OS builds plus third-target, override, externalization, emitted-specifier, tarball, install, and source-map mutants. |
| `OP-R13-003` — Portable modules had no runtime ABI, loader, static registry, or installed composition owner. | Added `docs/architecture/module-abi.md`; made `ISS-011` own exact entrypoints and the closed manifest, `ISS-026` own step-4 admission/invocation/refusal, `ISS-033` statically embed the four N0 modules and depend directly on `ISS-011`, and `ISS-015` prove candidate-module non-execution before N1 promotion. |

## Re-review focus

- Confirm credential isolation is a proved OS boundary, not a package-private or
  sanitized-environment claim.
- Confirm the declared esbuild path is sufficient and exclusive on all targets.
- Confirm N0 can execute its released modules but cannot dynamically load the N1
  candidate module before promotion.
