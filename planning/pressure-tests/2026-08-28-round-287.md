# ISS-022 lock-row narrow-repair review — round 287

## Disposition

`PASS`.

Round 286's named helper-closure blocker is repaired without weakening either
stack layer.

## Pressure result

- `executePortablePrimitiveLockAttemptWithoutTimer` performs one spawn and
  calls only `observePortablePrimitiveChildWithoutKill`. That observer has no
  timer or kill, continues draining both pipes after the combined 64 KiB cap,
  discards excess chunks, awaits natural `close`, returns `event:null` on
  overflow, and fixes `timedOut:false`.
- The reviewed source closure measured zero timer/kill sites in both the entry
  point and its dedicated observer, one natural-close wait, and retained two
  `SIGKILL` sites in the separate generic bounded observer. The structural test
  names the direct call, checks the dedicated observer and reachable parsing/
  custody helpers, and carries the generic-kill negative control; it no longer
  proves safety from a wrapper-only slice.
- The stable reducer requires a coherent child with
  `outputLimitExceeded:false`; the explicit overflow mutant therefore
  normalizes `LOCK_HOLDER_DEATH` to UNKNOWN and cannot become PASS or
  UNSUPPORTED authority.
- Round 285's descriptor repair remains sound: the stable parent owns the exact
  handle and nonce, default access is coherent `REFUSED/EBADF`, parent readback
  matches the nonce, explicit mapping reads that exact nonce as `ACCESSED`, and
  every accessed, mismatched, missing, or contradictory arm is non-PASS.

## Verification and proportionality

- Focused Vitest: 18/18 passed for the raw executor and stable reducer suites.
- Portable-primitives and conformance TypeScript checks passed.
- Excluding these separate review records, the mechanical split remains PR1
  raw fixture/portable proof at 480/-44 and PR2 stable reducer/mutants at
  428/-7. Each preserves a coherent layer and is bounded to one review sitting.

No deletion, retry, PID, age, lease, stale-owner inference, timeout, or kill is
reachable from the sole post-death acquisition path. Hosted three-OS evidence
remains the probe's later capability gate; this review grants no profile token.
