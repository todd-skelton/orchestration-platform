# Pressure round 428 — ISS-013 Option-B outcome split

Status: planning-only split under owner ruling #234. No adapter source,
mutation, provider access, issue closure, release, recovery, or authority claim.

## Measured frontier and disposition

Round425 completed the last live REVIEW preflight migration. The remaining
ISS-013 outcomes have no current consumer: dispatch allocation/materialization,
APPLY/PROJECT mutation and full conformance, recovery/reclaim, release-candidate
and CI/deploy observation, and state export/import. Implementing them inside
ISS-013 would invent callbacks and authority inputs merely to close a Wave-0
issue.

Option B moves those groups to five thin registrations, each behind the exact
consumer named by the owner ruling: ISS-008→ISS-049, ISS-009→ISS-050,
ISS-030→ISS-051, ISS-014→ISS-052, and ISS-020→ISS-053. The grouping follows
authority ownership rather than package layout. Each draft states scope,
non-goals, acceptance evidence, predicted footprint, and review attack surface;
exact runtime verification is due before its edge becomes runnable.

## Existing-dependent reduction audit

ISS-011 consumes neutral plans and the landed snapshot/preflight/compatibility
surface, not project apply. ISS-025 consumes landed policy/trip facts and owns
the generic breaker lifecycle; concrete adapter recovery follows supervision.
ISS-036 is the live provider probe that will produce mutation/CI/deploy facts,
so a generic implementation cannot precede it by declaration. ISS-028 fills a
static composition skeleton whose later extensions intentionally remain
placeholders. None requires an unstarted outcome, so their existing ISS-013
edges stay unchanged and unblock on ISS-013 closure.

## Verification and review attack surface

`pnpm run planning:check`, live `pnpm run planning:board-check`, format checks,
independent exact-head review, and three-OS hosted CI are required. Review should
attack an omitted unstarted outcome, duplicate ownership across follow-ons, a
hypothetical consumer, a dependency cycle or redundant edge, a dependent that
secretly needs moved behavior, placeholder success, issue closure before the
split lands, or any mutation/release/recovery/resource authority granted by
planning prose.

Prediction: after this split, ISS-013 can close with its landed evidence while
each remaining outcome becomes runnable only when its named consumer supplies
complete inputs. Review that prediction when each edge clears; a different
consumer or authority surface requires an ordinary planning amendment.

The only non-planning byte change is the existing fail-closed
`adapters:conformance` root placeholder's owner literal from ISS-013 to ISS-050.
It still exits capability-not-implemented and grants no executable behavior.

### Follow-up after independent review

The first review found stale ISS-013 ownership prose and missing downstream
consumer gates. Historical proposal notes now name the new implementation
owners while preserving current plan/apply placeholder literals until ISS-050
elaboration. `ISS-033` directly consumes ISS-049 through ISS-052, replacing its
now-transitively implied ISS-026/038 edges; `ISS-024` directly consumes ISS-053.
The new ISS-030→ISS-051→ISS-033→ISS-020 path also makes ISS-019's former direct
ISS-030 edge redundant, so the checker-required transitive reduction removes it.
This is the one allowed follow-up repair for the named blocker.
