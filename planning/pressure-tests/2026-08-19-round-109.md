# Pressure test — round 109 fail-closed bootstrap retirement repair review

**Disposition:** `PASS` for the bounded Round 108 safety repair only

**Reviewed candidate head:** `c4dbae3f82835d8840221b0898cc2554bcd5928f`

**Reviewed parent boundary:** `607b36a51dd48fbb481b2c17ec468e9b95e07055`

This was a fresh cold independent ISS-002 review of the exact initially clean
head above on `iss-002-versioned-contracts`. The review read only this
repository: `AGENTS.md`, `docs/planning/work-model.md`, the complete decision
authority in `docs/architecture/contract-decisions.md`, ISS-002, Round 108, the
exact parent-to-candidate diff, and the affected source and tests. No consumer
repository, consumer project, consumer issue, consumer board, consumer
backlog, or Chase Sets surface was accessed. The board gate read only this
repository and its registered platform delivery project.

## Disposition boundary

`PASS` removes the two accepting holes identified by Round 108:

1. current CONSUMED and successor-origin retirement graphs now fail closed
   with the exact finding `priorProvenance:full-binding-required`; and
2. fully rehashed tails in which anchor RETIRED is proposed before the
   lifecycle archive, or owner RETIRED is proposed before anchor RETIRED, now
   refuse at the exact new chronology findings.

The existing complete genesis-origin ACTIVE `RETIRE_UNUSED` graph remains a
valid positive control. The repair preserves the previously reviewed local
retirement edges, exact destination-owner teardown archive, Dobs/Dabs
separation, acyclic direction, and public totality.

This is deliberately **not** a PASS for complete bootstrap retirement. The
public function still has no positions for the actual ACTIVE predecessor and
E0/consumption composition needed by CONSUMED, or for the actual prior RETIRED
owner/archive, review core, and post-selection composition needed by a
successor origin. Those branches are incomplete and unauthorized, not
implementation-ready. Their named fail-closed result is the safety boundary;
it is not evidence that their full provenance has been implemented.

This disposition grants no ISS-002 completion, merge, release, installation,
promotion, runtime mutation, live capability, self-certification, reinstall,
rotation, packet, or producer authority.

## Round 108 orphan and digest-only graphs now refuse

The repair applies its provenance refusal only after every direct input needed
by the existing composition has passed its closed parser. It derives the branch
from parsed records rather than from an open caller flag:

```text
prior anchor lifecycle == CONSUMED
    OR
anchor successorReviewCoreDigest != null
        -> priorProvenance:full-binding-required
```

That disjunction covers all three currently incomplete cases:

| Supplied current graph | Parsed retained fact | Exact result |
| --- | --- | --- |
| Genesis-origin CONSUMED | prior anchor lifecycle is `CONSUMED` | `priorProvenance:full-binding-required` |
| Successor-origin ACTIVE | anchor review-core digest is non-null | `priorProvenance:full-binding-required` |
| Successor-origin CONSUMED | both facts are present; either is sufficient | `priorProvenance:full-binding-required` |

An approved read-only in-memory probe used the exact checked-out fixture and
public validator. All three cases returned only the named refusal. The complete
genesis-origin ACTIVE fixture, whose lifecycle is ACTIVE and review-core digest
is null, continued to return `[]`.

### Lifecycle and review substitutions do not bypass the guard

The guard is not the only branch predicate. The already-retained teardown
composition also derives the branch from the retirement transition and closes
the corresponding records:

- ACTIVE requires an anchor `ACTIVATE` proposal, its null prior triple,
  lifecycle ordinal zero, and either the exact owner genesis branch when the
  anchor review digest is null or the exact owner successor branch when it is
  non-null;
- CONSUMED requires the anchor and owner `CONSUME` shapes, non-null immediate
  predecessor triples, and the current anchor/owner selected-triple
  equalities; and
- the owner value's lifecycle and successor review-core role must match the
  anchor-derived branch.

Directly changing a CONSUMED lifecycle label to ACTIVE caused closed lifecycle
branch refusal. Directly nulling the successor review digest caused anchor,
owner, and selected-branch mismatches. Rehashing only siblings cannot erase
those cross-branch predicates. A coordinated rewrite that changes every
current record into the complete admitted genesis ACTIVE/null-review shape is
not a bypass: it is a different graph and is exactly the one positive branch
this bounded repair intentionally retains.

No lifecycle/review substitution made an incomplete supplied branch return
`[]`, and the delta introduces no alternate public retirement entry point,
optional bypass flag, review digest alias, or advisory downgrade.

## Fully rehashed chronology refuses

The outer validator now independently parses the lifecycle archive before
reading `archivedAt`. All three compared timestamps have therefore passed the
same canonical RFC 3339 UTC, exact-millisecond grammar. Lexicographic comparison
of those fixed-form canonical strings has the same order as recorded time.

The two new predicates are exactly the Round 108 lower bounds:

```text
anchorRetiredProposal.proposedAt >= lifecycleArchive.archivedAt
ownerRetiredProposal.proposedAt  >= anchorRetiredProposal.proposedAt
```

The existing inner predicate
`lifecycleArchive.archivedAt >= absence.observedAt` remains unchanged.

The independent in-memory probe did not rely on stale-digest mutants. Starting
from the complete genesis ACTIVE fixture, it rebuilt in dependency order:

1. the anchor RETIRED proposal mutation ID and proposal digest;
2. the selected anchor RETIRED tip;
3. the owner teardown archive and digest;
4. the owner RETIRED value and digest;
5. the owner RETIRED proposal mutation ID and proposal digest; and
6. the selected owner RETIRED tip.

The results were:

```text
anchorRetiredProposal.proposedAt = 2026-08-19T12:01:30.000Z
lifecycleArchive.archivedAt      = 2026-08-19T12:02:00.000Z
result = [anchorRetiredProposal.proposedAt:before-lifecycle-archive]

ownerRetiredProposal.proposedAt  = 2026-08-19T12:02:30.000Z
anchorRetiredProposal.proposedAt = 2026-08-19T12:03:00.000Z
result = [ownerRetiredProposal.proposedAt:before-anchor-retired]
```

An equality-edge rebuild set both proposal times to the archive time. It
returned `[]`. That is the intended `must not precede` rule from Round 108;
the repair neither silently tightens it to strict `after` nor widens it beyond
equality. If both proposal times precede the archive, the first predicate still
refuses. If the owner precedes the anchor at any otherwise valid time, the
second predicate still refuses.

The committed chronology test changes the proposal timestamps without
rehashing their descendants. It does assert the exact unique named findings,
so deletion of either comparison is still detected, but the independent probe
above supplies the stronger fully rehashed control requested by Round 108.
That repository test alone must not be cited as evidence of full branch
provenance or complete retirement readiness.

## Retained local properties

### Retirement edge bindings

The exact delta does not remove or weaken any local binding. The anchor
mutation composition still recomputes the prior selected anchor tip/value/
proposal, incremented lifecycle ordinal, transition, `TEARDOWN` source,
teardown-receipt evidence, RETIRED value, proposal mutation ID/receipt, and
selected tip. The owner mutation composition still independently recomputes
the prior selected owner graph, incremented ordinal, installation/destination/
anchor identity, `ANCHOR_RETIRED` source, current Dobs, owner archive, RETIRED
value, proposal, and selected tip.

The direct equalities for both final selected tips, the selected anchor
RETIRED triple embedded in the owner value, all ten semantic owner-archive
members, and the separate Dobs/Dabs roles are unchanged. The focused and full
regression suites remain green.

### Totality and malformed archive handling

The repair adds the lifecycle archive to the outer closed-parse census before
its timestamp is read. An approved independent matrix replaced each of all 21
public validator inputs, one at a time, with `null` and with an
`ownKeys`-throwing proxy. All 42 calls returned one or more findings; zero
threw and zero returned `[]`.

The new parse cannot widen acceptance. A missing, malformed, future, exotic,
or hostile lifecycle archive now returns its named outer parse findings before
the new comparison. Valid records continue through the existing nested
teardown composition, which parses the same archive under its original closed
schema and retains every archive/receipt equality.

### Authority and acyclicity boundary

The safety repair adds only one closed parse, one fixed provenance refusal, two
timestamp lower-bound comparisons, and tests. It adds no schema member, digest
domain, persistence path, record, receipt, graph inventory, proof tree,
coordinator, filesystem mutation, lock, capability, handle, runtime service,
selector, process action, installer, rotation, promotion, or verdict.

The existing acyclic direction is unchanged: absence precedes the lifecycle
archive; the teardown receipt consumes that archive; anchor RETIRED consumes
the receipt; the owner archive consumes the selected anchor RETIRED triple;
and owner RETIRED consumes the owner archive. No upstream record names a future
owner RETIRED value, proposal, or tip.

## Exact delta and proportionality

The exact
`607b36a51dd48fbb481b2c17ec468e9b95e07055..c4dbae3f82835d8840221b0898cc2554bcd5928f`
delta has two paths, 44 insertions, and five deletions:

- `packages/contracts/src/retirement.ts`: 11 insertions for the closed archive
  parse, provenance refusal, and chronology comparisons; and
- `test/contracts/anchor-teardown.test.ts`: 33 insertions and five deletions to
  preserve the one genesis ACTIVE positive, require the named refusal for the
  three incomplete branches, keep the crossed-transition control on the valid
  branch, and add the two chronology assertions.

`git diff --check` passes. No authority decision, planning topology, package,
schema census, schema version, registry row, export, digest framing, path
constructor, runtime service, adapter, capability, or unrelated contract
family changed.

| Mechanism | Concrete in-scope threat | Deletion/attack result | Smallest credible shape |
| --- | --- | --- | --- |
| Parsed-branch provenance refusal | An orphan/digest-only CONSUMED or successor graph is locally rehashed and retired before its full provenance inputs exist. | Deleting the guard restores all three Round 108 accepted fixtures. The exact-head probe shows each now has the fixed refusal. | Retain one derived two-clause refusal. Do not add a schema or pretend omitted provenance is implemented. |
| Anchor chronology lower bound | A rehashed anchor RETIRED selection claims to predate its lifecycle archive. | A complete rebuilt tail now returns only the exact anchor chronology finding. | Retain one comparison over already-canonical timestamps. |
| Owner chronology lower bound | A rehashed owner RETIRED selection claims to predate anchor RETIRED. | A complete rebuilt tail now returns only the exact owner chronology finding. | Retain one comparison over already-canonical timestamps. |
| Outer lifecycle-archive parse | A malformed or hostile archive reaches the new timestamp read. | Null/hostile matrix refuses without throwing; deleting the parse makes the new read unsafe or untyped. | Reuse the existing closed archive parser; add no record. |
| Existing local edge composition | A locally substituted prior owner, anchor RETIRED triple, archive, teardown, Dobs, or Dabs is accepted. | Existing direct-equality and focused regression matrices remain green; the exact delta deletes none of them. | Retain the existing pure compositions and archive; add no coordinator. |

Full successor/CONSUMED composition is intentionally outside this repair. It
must later reuse the existing prior-owner, successor-review/post, E0, and
consumption validators with actual branch inputs. The present fail-closed guard
is smaller than prematurely exposing another partially bound input union.

## Verification evidence

All complete gates ran in the mandated strict sequence. The standalone full
suite exited before bootstrap began; bootstrap exited before focused contracts;
focused compatibility began only after focused contracts exited. No full and
bootstrap process overlapped.

1. Planning — PASS, `planning contracts verified` through the checked-in
   `node scripts/planning/check.mjs`. The restricted `pnpm run planning:check`
   failed closed before its script because the signed pnpm 11.22.0 registry
   metadata could not be fetched.
2. Board — PASS, `board contracts verified`, against only this repository and
   its registered platform delivery project. The restricted run failed closed
   at the read-only GitHub CLI subprocess with `spawn EPERM`; the identical
   approved read-only rerun passed.
3. Format — PASS, all matched files use Prettier style. The restricted run
   failed closed at the pinned Prettier subprocess with `spawn EPERM`; the
   identical approved read-only rerun passed.
4. Typecheck — PASS with the pinned TypeScript 7.0.2 compiler.
5. Standalone `pnpm test` gate — PASS on the identical approved direct
   read-only Vitest run: 28 files and 328 tests in 193.52 seconds. The
   restricted wrapper failed closed before its script on the same signed-pnpm
   registry-verification path.
6. Separate `pnpm run verify:bootstrap` — PASS on the approved identical
   wrapper rerun: manifests and registries, formatting, typecheck, 28 files/328
   tests in 161.56 seconds, private compositions, planning contracts, and
   source-tree status invariance. Its restricted invocation failed closed
   before the script on the same pnpm infrastructure path.
7. Focused ISS-002 contracts — PASS, 18 files and 131 tests in 7.52 seconds.
8. Focused ISS-002 compatibility — PASS, one file and one test in 1.08
   seconds.

The first restricted in-memory attack probe failed at esbuild child-process
creation with `spawn EPERM`; the approved corrected read-only in-memory probes
passed. They wrote no fixture, source, test, generated artifact, or probe file.
No isolated failing-test retry was used. Before this required review artifact
was written, every gate and probe left the source tree clean. The exact HEAD
remained `c4dbae3f82835d8840221b0898cc2554bcd5928f` throughout.

## Final status

`PASS` for removal of the Round 108 accepting holes at exact candidate head
`c4dbae3f82835d8840221b0898cc2554bcd5928f`, with parent boundary
`607b36a51dd48fbb481b2c17ec468e9b95e07055`.

Genesis-origin ACTIVE `RETIRE_UNUSED` remains valid. Current CONSUMED and every
successor-origin retirement remain deliberately incomplete and unauthorized,
and now fail closed with `priorProvenance:full-binding-required`. Both fully
rehashed reverse-time tails refuse, timestamp equality retains the decided
`must not precede` semantics, mixed lifecycle/review substitutions do not
bypass the guard, and local edge/totality properties remain intact.

This is an exact-head PASS for a bounded fail-closed safety repair, not a full
retirement PASS and not authority to implement or accept successor/CONSUMED
retirement without the missing complete provenance composition and a fresh
independent review.
