# Pressure round 452 — hosted Windows timeout budget slice

Status: budget-slice source attempt 1 under Decision #271, rooted at protected
main `73ab243ee83d75dbbfb2269b5e629f0dbb4e06f4`. This candidate claims no
independent review, hosted execution, merge, certification, or authority.

## Lineage and scope

Todd's OWNER ruling in [Decision #271](https://github.com/todd-skelton/orchestration-platform/issues/271)
admits this bounded slice. PR #269 hosted run `33973428070`, Windows job
`101325867948`, timed out at the default 5000 ms in
`fixtures/walking-skeleton/test/review-consumer.test.ts:659`; its test blob
`7b98a52b9ee15a61a8849cbccb3b72bba338c2d6` is byte-identical at base and
head. The job recorded 1555 passed, 1 failed, and 6 skipped; Ubuntu and macOS
passed. PR #270 hosted run `33973522673`, Windows job `101326112230`, timed
out after 5175 ms against 5000 ms in
`test/portable-primitives/physical-alias-executor.test.ts:24`; its test blob
`5ccd7e0d357dcb9e78590a01f3c0dc710509664a` is byte-identical at base and
head. That job recorded 1556 passed, 1 failed, and 6 skipped.

Both runs also logged the pre-existing EPERM teardown message unlinking
`windows_reparse_fact.node`; it appears on green main runs and is out of
scope. Precedent PRs #196, #228, #229, and #232 add explicit 30-second (and,
in two cases, 60-second) per-test budgets to slow tests; 44 tests already
carry such budgets, while these two did not. Decision #271 item 3 classifies a
hosted timeout in a byte-identical test on a head with independent review PASS
as infrastructure, not an artifact attempt.

This source attempt adds only the established 30-second per-test budget to the
named test at line 659 and the named test at line 24, plus this record. The
first file follows its existing `30000` convention; the second follows PR
#228's `30_000` convention. No other test in the describe block changes.

## Non-goals

There is no assertion, test-body, skip, retry, semantic, global Vitest timeout,
configuration, workflow, package, native, probe, lock, or teardown change.
This packet does not alter either parked PR artifact, rerun hosted CI, close a
roadmap issue, grant authority, certify a worker or release, or create a new
spend ceiling.

## Acceptance evidence and verification

Acceptance requires an independent exact-head review PASS by a different lane,
the required local static gates at the exact final head, and hosted three-OS
CI. Hosted CI is the only executable test evidence; no local Vitest, test,
native compiler, addon, probe, or lock experiment is authorized or claimed.
Required local gates are `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm planning:board-check`, `git diff --check`, and a
clean `git status --porcelain` after commit.

The attempt ledger is: budget-slice source attempt 1; independent exact-head
review attempt 2 is external and not yet done; hosted three-OS bootstrap is
pending. Routing is model-routing row 2 (routine scoped fix), gpt-5.6-terra
medium, with provisional placement. Budget intent is at most $15; actual spend
is unknown and this record requests no new ceiling.

## Predicted footprint

The exact footprint is three files:

- `fixtures/walking-skeleton/test/review-consumer.test.ts`
- `test/portable-primitives/physical-alias-executor.test.ts`
- `planning/pressure-tests/2026-09-05-round-452.md`

## Review attack surface

Review the exact two test titles and their third arguments; reject a timeout on
any other test, an assertion/body/skip/retry change, a global timeout or
workflow/configuration change, an omitted or altered lineage fact, invented
test evidence, self-review or self-certification, footprint growth, attempt
ledger reset, EPERM noise treated as a packet defect, or any claim that this
candidate closes an issue or grants authority.
