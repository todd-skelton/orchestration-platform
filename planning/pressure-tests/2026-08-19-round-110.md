# Pressure test — round 110 successor ACTIVE retirement composition review

**Disposition:** `BLOCK_REPLAN`

**Reviewed candidate head:** `d6f96e1836c9bbbe5fadf728f0ed8ea328932afe`

**Reviewed parent boundary:** `ac060b5a82de9683181febb32bef68a705d21961`

This was a fresh cold independent ISS-002 review of the exact initially clean
head above on `iss-002-versioned-contracts`. The review was bounded to the new
public `validateBootstrapSuccessorActiveRetirementBinding` composition and its
complete fixture: actual prior selected owner RETIRED triple/archive, closed
successor review core and expected preimage, selected successor owner ACTIVE,
successor post-selection receipt and expected readbacks, and the existing
local RETIRE_UNUSED retirement tail.

The review read only this repository's `AGENTS.md`,
`docs/planning/work-model.md`, the complete decision authority in
`docs/architecture/contract-decisions.md`, ISS-002, Rounds 108 and 109, and the
exact parent-to-candidate diff. No consumer repository, consumer project,
consumer issue, consumer board, consumer backlog, Chase Sets surface, runtime,
or author reasoning was accessed. The board gate read only this repository and
its registered platform delivery project.

## Disposition summary

`BLOCK_REPLAN`. The new composition closes the Round 108 successor provenance
input omission and independently survived coordinated substitutions of the
actual prior triple/archive, `Dsrc`, selected current owner, and `Dsrp`
readbacks. It also keeps genesis, CONSUMED, and malformed successor branches
fail closed, preserves the existing local retirement tail and chronology, and
is total across all 29 public inputs.

It nevertheless does not compose the recorded chronology of those complete
records. The committed positive fixture, and therefore the public validator,
accepts a `Dsrc`, successor ACTIVE selection, and `Dsrp` recorded before the
actual prior owner RETIRED proposal they consume. The validator also accepts a
`Dsrp` recorded after the first local retirement-tail observation. The complete
accepted graph is consequently not the required ordered provenance:

```text
actual prior owner RETIRED -> Dsrc review -> successor owner ACTIVE
                           -> Dsrp -> Dabs -> local retirement tail
```

These are accepting holes, not stale-digest failures. All supplied records are
closed and the accepted positive graph is otherwise fully bound. This status
grants no ISS-002 completion, merge, release, installation, reinstall,
promotion, runtime mutation, live capability, rotation, packet, producer
authority, or self-certification.

## `OP-R110-001` — accepted `Dsrc` predates the actual prior RETIRED graph

The new fixture constructs `prior = retirementFixture()` and supplies its
actual owner RETIRED tip/value/proposal and teardown archive to both successor
validators. It recomputes `Dsrc`, binds that digest into the successor anchor
and selected owner ACTIVE graph, and builds `Dsrp` from the actual selected
successor graph. Structurally, this is the complete graph omitted in Round 108.

Its canonical recorded times, however, are:

```text
Dsrc independentReview.reviewedAt       2026-08-19T11:58:00.000Z
successor owner ACTIVE proposedAt        2026-08-19T11:59:00.000Z
Dsrp observedAt                          2026-08-19T12:00:00.000Z
actual prior owner RETIRED proposedAt    2026-08-19T12:04:00.000Z
public composition result                []
```

The review core hashes the selected prior RETIRED triple and archive. Its
independent review cannot truthfully predate the prior RETIRED proposal bytes
by six minutes, and the successor ACTIVE selection and post-selection receipt
cannot precede the selected predecessor they claim to advance. Nevertheless,
the committed test names this graph as the valid positive and requires `[]`.

This is not a caller-digest placeholder. The prior records are actual closed
records; their triple and archive digests are recomputed through `Dsrc`, the
successor selected graph, `Dsrp`, and the complete local retirement tail. The
missing relation is a direct recorded-time lower bound between the parsed
prior owner RETIRED proposal and the parsed independent review.

The bounded repair is to require
`Dsrc.independentReview.reviewedAt >= priorOwnerRetiredProposal.proposedAt`
after both closed parses. The fixed canonical RFC 3339 UTC millisecond grammar
makes direct text comparison exact. Revise the positive fixture so its actual
prior retirement precedes review, then successor selection, then `Dsrp`; add a
fully rebuilt reverse-time negative that keeps every digest and expected
preimage consistent. No new schema, receipt, digest domain, clock record, or
coordinator is needed.

## `OP-R110-002` — `Dsrp` may be recorded after local retirement begins

The local tail begins with the supplied same-lock
`DESTINATION_STATE_ROOT_ABSENT` observation (`Dabs`), then proceeds through the
lifecycle archive, teardown receipt, anchor RETIRED selection, owner teardown
archive, and owner RETIRED selection. The accepted fixture records:

```text
Dsrp observedAt               2026-08-19T12:00:00.000Z
Dabs observedAt               2026-08-19T12:01:00.000Z
lifecycle archive archivedAt  2026-08-19T12:02:00.000Z
```

The wrapper composes the existing successor-post and local-retirement
validators but never compares their recorded times. An approved read-only
in-memory attack changed only the closed `Dsrp.observedAt` to
`2026-08-19T12:01:30.000Z`: after Dabs, but before the lifecycle archive. No
downstream digest needs rebuilding because `Dsrp` is deliberately downstream
and does not enter the local retirement graph. Every structural and readback
binding remained valid, and the public result was still `[]`. Moving Dsrp
after the entire tail, to `12:05:00.000Z`, likewise returned `[]`.

The bounded repair is to require
`successorPostSelection.observedAt <= absence.observedAt` after both records
pass their closed parsers. That is the exact boundary between completed
successor provenance and the first local retirement action. Add equality and
reverse-order controls through the public wrapper. The already-existing local
order remains unchanged.

## Complete provenance bindings independently retained

The chronology blockers do not erase the new structural composition. A
read-only in-memory probe rebuilt the complete fixture and independently
attacked each boundary. It wrote no source, test, fixture, generated artifact,
or probe file.

### Valid graph and caller-digest substitution

The exact committed complete graph returned `[]`. Replacing only
`Dsrc.priorRetiredTipDigest` refused through the review and post bindings. A
stronger coordinated attack recomputed the independent-review candidate,
`Dsrc`, its expected candidate digest, successor anchor, current owner ACTIVE
value/proposal/tip and mutation ID, all local retirement descendants, `Dsrp`,
and every post expected/readback digest while leaving the actual prior tip
unchanged. It refused only at:

```text
successorReviewBinding:priorRetiredTipDigest:mismatch
```

Thus coordinated caller copies of a forged review digest do not substitute
for the actual parsed prior owner record.

### Actual prior selected triple

A coordinated attack replaced the actual prior selected tip with a closed tip
whose `valueDigest` named another valid digest. It recomputed that tip's digest,
the current successor proposal's prior tip, the candidate, `Dsrc`, all digest
copies, current selected owner graph, `Dsrp` readbacks, and local retirement
tail. It refused at both independently composed edges:

```text
successorReviewBinding:priorTip.valueDigest:mismatch
successorPostBinding:ownerMutation:priorTip.valueDigest:mismatch
```

The actual prior tip/value/proposal relation therefore cannot be replaced by a
coordinated triple digest.

### Actual prior teardown archive

An independent valid-scalar replacement of the prior teardown archive's
absence digest refused through its direct review/archive/value/proposal
equalities. A coordinated version additionally recomputed the archive digest,
review candidate, `Dsrc`, review expectation, successor selected graph,
`Dsrp`, and the complete local tail. It still refused because the actual prior
RETIRED value and proposal selected the original archive:

```text
successorReviewBinding:priorProposal:transitionEvidenceDigest:mismatch
successorReviewBinding:priorValue:teardownArchiveDigest:mismatch
```

### `Dsrp` and selected successor owner

Replacing only `Dsrp.valueReadbackDigest` refused at both expected and derived
readback equalities. Replacing the post record and its caller expectation
together refused at the independently derived current-owner bytes:

```text
successorPostBinding:valueReadbackDigest:derived:mismatch
```

Changing the current selected owner's ordinal and rebuilding its value,
proposal mutation ID, proposal digest, tip, local retirement descendants,
`Dsrp`, and all expected readbacks refused at:

```text
successorPostBinding:ownerMutation:ownerOrdinal:not-successor
```

The current owner is therefore an actual parsed ACTIVE successor selection,
not a digest supplied by the caller.

## Guard removal, CONSUMED, totality, and retained local tail

`removeLocalProvenanceGuard` removes only the exact legacy
`priorProvenance:full-binding-required` result emitted by the narrower local
retirement validator. The wrapper then independently derives its admitted
branch from closed records: anchor review-core digest non-null, prior anchor
lifecycle ACTIVE, and selected owner lifecycle ACTIVE.

Independent probes routed a complete genesis ACTIVE local retirement, a
locally complete CONSUMED retirement, and an ACTIVE graph with the wrong
successor identity through the new wrapper. None returned `[]`. Genesis and
CONSUMED both retained `successorBranch:not-active-successor`; the wrong
successor graph retained the owner-mutation identity, predecessor, and readback
findings. CONSUMED therefore remains incomplete, fail closed, and unauthorized;
the new wrapper does not treat it as complete provenance.

For totality, each of all 29 public inputs was independently replaced with
`null` and with an `ownKeys`-throwing proxy. All 58 calls returned one or more
findings; zero threw and zero returned `[]`.

The existing local chronology also remains active through the wrapper. Fully
rehashing the anchor RETIRED proposal, selected tip, owner archive, owner
RETIRED value/proposal/tip, and every descendant produced the exact findings:

```text
anchorRetiredProposal.proposedAt:before-lifecycle-archive
ownerRetiredProposal.proposedAt:before-anchor-retired
```

A separately rehashed local owner archive with a substituted teardown receipt
digest refused at `ownerArchive.teardownReceiptDigest:mismatch`. The wrapper
therefore retains the already-reviewed local retirement tail; the blockers are
the two missing cross-composition chronology edges.

## Test-discrimination audit

The new committed test proves that the complete fixture reaches the new public
entry point and that deleting or grossly miswiring either successor-validator
call would be detected by one direct `Dsrc` tip-digest mutation and one direct
`Dsrp` review-core mutation. Those controls are useful but insufficient as
removal evidence:

- its positive fixture is the impossible reverse chronology in
  `OP-R110-001`, so it codifies an accepting hole;
- no committed test orders the actual prior RETIRED proposal before Dsrc
  review or rebuilds the reverse relation;
- no committed test orders `Dsrp` before Dabs or moves it after the first
  local retirement observation;
- no committed test attacks the wrapper's genesis, CONSUMED, or wrong-
  successor guard behavior;
- no committed test independently or coordinately replaces the actual prior
  selected triple, prior teardown archive, selected current owner, or a Dsrp
  readback plus its expected copy; and
- the existing malformed-input test exercises the older 21-input validator,
  not all 29 positions of the new public validator.

The independent matrix above confirms that the implementation currently
survives those structural and totality attacks. It cannot turn the accepted
chronology inversions into valid evidence or substitute for executable
regression coverage of the repaired public surface.

## Authority, acyclicity, exact delta, and proportionality

The exact
`ac060b5a82de9683181febb32bef68a705d21961..d6f96e1836c9bbbe5fadf728f0ed8ea328932afe`
delta has two paths, 377 insertions, and five deletions:

- `packages/contracts/src/retirement.ts`: 102 insertions for one pure public
  composition; and
- `test/contracts/anchor-teardown.test.ts`: 275 insertions and five deletions
  for the complete fixture, wrapper helper, and one focused test.

`git diff --check` passes. No schema member, digest domain, persistence path,
registry row, compatibility version, filesystem mutation, lock, handle,
runtime service, packet, installer, release producer, rotation, promotion,
candidate verdict, or unrelated contract family changed. The new function
returns structural findings only. It grants no live mutation capability and
cannot certify itself.

The graph remains acyclic: prior retirement selects its archive, `Dsrc`
consumes the prior selected graph, current owner ACTIVE consumes `Dsrc`, Dsrp
observes that selection, and the local teardown tail never feeds a future
digest upstream. The missing chronology checks operate over already-closed
canonical timestamps and do not require a future digest or new record.

| Mechanism | Concrete in-scope threat | Deletion/attack result | Smallest credible shape |
| --- | --- | --- | --- |
| Prior RETIRED-to-review chronology | Dsrc claims independent review of selected prior records before their proposal bytes exist. | The committed positive returns `[]` with review at 11:58 and actual prior RETIRED proposal at 12:04. | Add one direct canonical timestamp lower bound and rebuild the positive/negative fixtures. |
| Dsrp-to-local-tail chronology | Local retirement begins before the successor post-selection observation is recorded. | Dsrp at 12:01:30 after Dabs at 12:01 still returns `[]`; 12:05 after the full tail also returns `[]`. | Add one direct canonical timestamp upper bound against Dabs and equality/reverse controls. |
| Actual prior selected triple/archive composition | Rehashed caller digests stand in for real selected predecessor records. | Coordinated tip and archive replacements refuse at direct triple/archive equalities. | Retain the two existing pure successor validators and actual inputs; add no proof object. |
| Dsrc/current owner/Dsrp composition | A caller substitutes review, current selection, or post readbacks consistently. | Coordinated Dsrc, owner ordinal, and post+expected replacements all refuse at independently derived relations. | Retain the current pure composition. |
| Successor-only guard | Removing the local incomplete-provenance finding accidentally admits genesis or CONSUMED. | Genesis and CONSUMED remain nonempty; wrong successor identity also refuses. | Retain the three parsed branch predicates. |
| Local retirement tail and chronology | A valid successor wrapper drops the previously reviewed teardown/archive/order checks. | Fully rehashed local archive and reverse-time attacks retain their exact findings. | Retain the existing local composition; add only the two cross-composition comparisons above. |

The replan is two timestamp comparisons plus discriminating fixture/test
repairs. It is not authority for a new schema, receipt, digest, sequence log,
inventory, proof tree, coordinator, capability, or runtime writer.

## Verification evidence

All complete gates ran in the mandated strict sequence. The standalone full
suite exited before bootstrap began; bootstrap exited before focused
contracts; focused compatibility began only after focused contracts exited.
No full and bootstrap process overlapped.

1. Planning — PASS, `planning contracts verified` through the checked-in
   direct checker. The restricted `pnpm run planning:check` failed closed
   before its script because signed pnpm 11.22.0 registry metadata could not
   be fetched.
2. Board — PASS, `board contracts verified`, against only this repository and
   its registered platform delivery project. The pnpm wrapper hit the same
   metadata failure; the restricted direct checker then failed closed at the
   read-only `gh` subprocess with `spawn EPERM`; the approved identical direct
   rerun passed.
3. Format — PASS, all matched files use Prettier style. The pnpm wrapper hit
   the same metadata failure; the restricted direct checker failed closed at
   the pinned formatter subprocess with `spawn EPERM`; the approved identical
   direct rerun passed.
4. Typecheck — PASS with the installed pinned TypeScript compiler.
5. Standalone full test gate — PASS on the approved direct Vitest run: 28
   files and 329 tests in 114.40 seconds. Its restricted startup failed closed
   at Vite's child-process creation with `spawn EPERM`.
6. Separate bootstrap verification — PASS on the approved direct wrapper:
   manifests and registries, formatting, typecheck, 28 files/329 tests in
   110.53 seconds, private compositions, planning contracts, and source-tree
   status invariance. Its restricted start failed closed at subprocess
   creation with `spawn EPERM`.
7. Focused ISS-002 contracts — PASS, 18 files and 132 tests in 6.84 seconds,
   on the approved direct rerun after the restricted Vite startup failed
   closed with `spawn EPERM`.
8. Focused ISS-002 compatibility — PASS, one file and one test in 1.00
   seconds, on the approved direct rerun after the restricted Vite startup
   failed closed with `spawn EPERM`.

The first restricted in-memory matrix failed closed at esbuild child-process
creation with `spawn EPERM`; the approved identical read-only rerun and two
bounded chronology probes passed. They wrote no source, test, fixture,
generated artifact, or probe file. Before this required pressure record was
written, every gate and probe left the source tree clean. The exact HEAD
remained `d6f96e1836c9bbbe5fadf728f0ed8ea328932afe` throughout.

## Final status

`BLOCK_REPLAN` for exact candidate head
`d6f96e1836c9bbbe5fadf728f0ed8ea328932afe`, with parent boundary
`ac060b5a82de9683181febb32bef68a705d21961`.

The successor-only wrapper now requires actual prior RETIRED records, Dsrc,
selected owner ACTIVE, Dsrp, and the complete local retirement tail, and its
structural/totality boundaries independently resist coordinated replacement.
It nevertheless accepts review/activation/post records dated before their
actual prior RETIRED predecessor and accepts Dsrp after local retirement has
begun. Repair the two canonical chronology edges and add discriminating public
tests, then return the exact repaired head to fresh independent review.

