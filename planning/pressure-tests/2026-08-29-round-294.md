# ISS-022 profile/custody and suite-composer follow-up review — round 294

## Disposition

`PASS`.

This is the one permitted post-repair review of the exact uncommitted aggregate
on `codex/iss-022-suite-composer` at base
`db22f2d820bafaf50fc90590aa645983263ea3b8`. Both Round-293 blockers are
closed; no further repair round is justified.

## Repaired blockers

1. `withIss022ExecutableCustody()` opens the resolved initial
   `process.execPath` once and retains that same regular-file handle around all
   eight ordered handler calls and their child launches. After the callback it
   re-resolves and re-stats the path, re-stats the retained handle, and rehashes
   that handle from offset zero. Path/realpath/identity, byte digest, and size
   must still agree. Dynamic path replacement and in-place byte mutation during
   the callback refuse, and `finally` closes the handle only after verification.
2. The closed report has exactly one nullable `selection`. Acceptance first
   snapshots and independently re-normalizes all 21 executions. ADMITTED
   construction then requires every physical row 0–5 to be PASS and re-runs the
   six-row physical normalizer before deriving the profile. Any valid UNKNOWN
   or UNSUPPORTED physical row selects the null arm while preserving the exact
   21-row diagnostic report. Null with six PASS rows and non-null with any
   non-PASS row both refuse.

## Focused attacks

- Helper/ABI/profile/custody/locator coordinates are reconstructed and
  canonical-equal-bound; altered or stale mismatched captures cannot cross-bind.
- Deep closed snapshots refuse proxies, accessors, extra candidate text,
  malformed execution or selection records, and post-snapshot mutation.
- Missing, extra, duplicated, renamed, or reordered executions refuse; the
  sorted 21-entry census and authoritative 21-row execution order remain
  distinct checks.
- Cleanup remains mandatory in `finally`; removal failure or a surviving
  custody root overrides an otherwise successful result with refusal.

Focused evidence passed:

`pnpm exec vitest run test/conformance/iss022-profile.test.ts test/conformance/iss022-suite.test.ts test/conformance/stable-bundles.test.ts`
(3 files, 15 tests),
`pnpm exec tsc --noEmit -p packages/conformance/tsconfig.json`, and
`git diff --check`.

## Proportionality

Layer A remains 531 lines (profile plus focused tests), layer B remains 401
lines (suite plus focused tests), and shared export/census amendments remain 10
insertions. The profile layer owns capture and selection derivation; the suite
only stacks orchestration and report acceptance on its public constructor. The
retained handle, six-row gate, closed reconstruction, independent census/order,
and strict cleanup each answer a demonstrated in-scope substitution or
lifecycle threat; deleting any one reopens an existing mutant. No helper,
service, provider synthesis, or production mechanism was added.
