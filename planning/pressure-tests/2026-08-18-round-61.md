# Pressure test — round 61 independent host-capability review

**Disposition:** `PASS`

**Reviewed head:** `9f23f5b867c5f2baa46b1d34db19a319b26e89ff`

This was a fresh independent cold review of the exact clean head above on
`iss-002-versioned-contracts`. Round 59's `BLOCK_REPLAN` and round 60's author
repair were treated only as non-authoritative history; no earlier PASS was
borrowed. `docs/architecture/contract-decisions.md` was the sole decision
authority for the host-capability repair. The worktree was clean at the start
of review.

## Disposition

The round-60 repair closes `OP-R59-001`. It replaces the undefined artifact-
internal capability census with one public, provider-neutral capability array
carried by the exact host identity projection and repeated in the reviewed
release-manifest row. The identity formula, total mapping admission, route
membership, step-7 revalidation, producer/consumer ownership, and negative
evidence are now exact and mutually consistent. No new blocker was found.

ISS-002 implementation is explicitly authorized at this exact reviewed head,
within the issue's stated scope and predicted implementation footprint. This
PASS does not authorize merge, release, promotion, installation, runtime
mutation, or self-certification, and any substantive planning or contract
change requires a new exact-head review.

## Direct contract attack results

- **Capability grammar and census:** `capabilityNames` is a dense 1–256 array.
  Every member matches `[a-z][a-z0-9._:-]{0,63}`: one to 64 ASCII bytes,
  lowercase at the first byte, and only the closed ASCII continuation set.
  Strictly increasing ASCII byte order makes duplicates impossible and fixes a
  single case-sensitive array representation. Zero, 257, holes, extras,
  malformed names, duplicate names, reordering, and case substitution refuse;
  one and 256 are admitted. The common closed-array rules retain mutable,
  sealed, and frozen exact arrays while refusing proxies, subclasses, exotic
  prototypes, custom iterators, symbols, accessors, non-enumerable indices, and
  structural extras.
- **Three-member identity:** `worker-host-identity/v1` has exactly
  `capabilityNames`, `hostRendererArtifactDigest`, and `schemaVersion` in
  canonical member order. The schema literal and digest domain are both
  `worker-host-identity/v1`; the ordered frames are text schema version,
  raw-32 artifact digest, and canonical capability-array bytes. The identity
  digest, provider/model/selector/executable/path/credential/runtime/route/
  dispatch/launch/output facts, and mutable evidence are excluded. Any
  artifact, capability, renderer, or template change produces a new identity.
  No host-supplied identity string is authority.
- **Four-member renderer row:** `worker-host-renderer-artifact/v1` has exactly
  `capabilityNames`, `hostRendererArtifactDigest`, `schemaVersion`, and
  `workerHostIdentityDigest` in canonical order. Both digests are SHA-256, the
  capability array obeys the exact identity rules, and unknown, missing, null,
  wrong-family, wrong-case, or future members and literals refuse.
- **Total 1–16 mapping admission:** the relation snapshots the complete dense
  `workerHostRendererArtifacts` array, parses every row and nested capability
  array, recomputes every identity from that row's exact artifact digest and
  canonical capabilities, requires digest equality, then proves the 1–16
  census and unique opaque identity keys. It has no partial-success arm: one
  unreadable row invalidates the whole mapping. Zero, 17, holes, extra rows,
  duplicate identities, and coordinated identity/artifact/capability
  substitutions therefore refuse.
- **Step 5 and step 7 use one relation:** ISS-012 alone emits the step-5
  `route-selection/v1` opaque identity after applying the complete installed
  active-release mapping relation and proving exact case-sensitive membership
  of the action core's `capabilityName`. Step 7 selects exactly one row by that
  unchanged identity, proves executing artifact equality, reapplies the same
  total relation and membership rule, and equal-binds the requested and brief
  roles before rendering or ownership publication. Missing, duplicate, stale,
  moved, cross-host, capability-incompatible, case-substituted, manifest/read-
  back-substituted, or candidate-supplied rows refuse without a second route,
  plan, registry, or digest.
- **Host/release/route/dispatch ownership:** ISS-002 owns the public schemas,
  formulas, parser, and pure relations. ISS-021 and ISS-045 each emit their own
  artifact, capability projection, identity input, templates, and conformance
  evidence. ISS-020 owns the reviewed N0 manifest/install read-back and ISS-014
  owns reviewed successors. ISS-012 owns route selection, ISS-008 owns step-7
  dispatch binding, and ISS-026 composes their unchanged values. Existing
  transitive dependencies carry ISS-002 authority into each consumer; no new
  direct edge or mutation owner is justified.
- **Provider boundary:** provider/model evidence may refine routing but cannot
  create or substitute the opaque host identity. Provider and executable names
  remain excluded from engine schemas and confined to provider-neutral routing
  policy or concrete host integration drafts. Two hosts may declare the same
  capability, but each retains a distinct reviewed artifact/capability identity
  and neither host's evidence satisfies the other's row.
- **Self-certification:** a host build supplies deterministic bytes and identity
  inputs only. External review, reviewed release-manifest construction,
  installed-byte recomputation/read-back, active-release selection, route
  admission, and stable promotion remain separate. A candidate, host, worker,
  receipt, or self-reported digest cannot add, choose, or authorize a row.
- **Action core and module catalog:** `dispatch-action-core/v1` remains the
  exact six-member projection and six-part framed digest over action,
  capability, subject, descriptor, requested role, and schema literal. Step 4
  equal-binds the brief action to it. The release-reviewed dense 1–256 catalog
  retains exact six-member rows, unique
  `(actionKind,capabilityName,directiveKind,code)` resolver keys, one
  accessor/template result per key, and exact equality with the descriptor's
  duplicate-free worker-required action/capability pair projection. A changed
  catalog capability changes the admitted action core and must independently
  satisfy the selected host row; catalog/host drift cannot borrow membership.
- **Brief cardinality, no prose, and rendering:** one envelope plus exactly
  three nested dispatch-brief schemas remain closed. All eight non-operator
  directive kinds require one or more unique `PRESENT` rows.
  `OPERATOR_ACTION` remains the exhaustive XOR of one-or-more unique present
  rows or exactly one `ABSENT` row with null code. Each present row selects a
  nonempty core scalar and a fixed reviewed 1–8192-byte template. Raw action,
  capability, catalog, accessor, template, schema, and resource values are not
  rendered; no free-form prose or arbitrary schema-reference field exists.
- **Removal and lifecycle invariants:** scoped searches found no restored
  `resourceKind`, `referenceSchemaVersion`, `referenceDigest`, old
  `actionPlanDigest`, or `templateRegistryDigest`. No dynamic/filesystem/
  network renderer, package-internal capability extractor, resource class/path,
  superseded authority generation, sparse membership proof, node inventory,
  materialization coordinator, retention/compaction pointer kind, or diagnostic
  namespace was restored. The existing eleven-kind FULL_REQUIRED pointer
  census, bounded decimal/framing rules, authority-history chain, ordinary/
  rotation commit split, ten cleanup states, twelve non-self cleanup edges,
  one fence edge, compatibility refusal, and vocabulary lint remain intact.

No bounded remedy is required. Implementation must realize these exact public
relations and the ISS-002 mutant matrix; it may not reinterpret the reviewed
planning prose or revive a deleted surface.

## Bounded proportionality check

| Mechanism | Concrete in-scope threat | Deletion test and smallest credible alternative | Result |
| --- | --- | --- | --- |
| One action-core digest | A brief can move across action, capability, subject, descriptor, or role. | Deletion admits substitution; the six-field projection is smaller than a whole-plan cycle or second plan. | Retain unchanged. |
| One release-reviewed dispatch catalog | Open keys can select unreviewed instructions. | Deletion reopens prose/dynamic resolution; one bounded four-key resolver is the smallest discriminator. | Retain unchanged. |
| Operator `ABSENT` XOR | Omission can masquerade as validated absence. | Deletion loses intent; one absent record is the smallest explicit representation. | Retain unchanged. |
| Capability array in the identity and mapping row | Build, install, route, and dispatch can otherwise interpret opaque artifact internals differently. | A package-internal record/extractor or subordinate digest is larger and less portable. One bounded canonical array on the existing public surfaces gives every owner identical semantics. | Retain repaired shape. |
| One total 1–16 mapping relation | Partial or divergent row parsing can route through a malformed active release. | Per-consumer interpretation recreates the defect; one pure whole-array relation reused at steps 5 and 7 is the smallest common check. | Retain repaired shape. |
| Per-host artifact/identity mapping | Multiple reviewed hosts may render different bytes for the same capability. | Deletion loses selected-host equality; one four-member row is smaller than a dynamic registry or provider-specific engine field. | Retain repaired shape. |

The repair deletes the undefined package-internal census/extraction obligation
and adds only one existing-vocabulary array plus its pure relation. Every
retained mechanism answers a concrete moved-input, malformed-mapping, or
unreviewed-instruction threat in the current two-host architecture; no
hypothetical Byzantine or unrestricted multi-writer machinery was added.

## Footprint, dependencies, and verification

The immediate repair range
`c0f6115acb824d9ff1c490766edcf083bb851d3d..9f23f5b867c5f2baa46b1d34db19a319b26e89ff`
changes exactly 12 paths: the governing decision and routine-cycle documents,
ISS-002, seven affected producer/consumer drafts, and the round-59/60 history
records. It changes no implementation, topology, root wrapper, shared script,
glossary, unrelated package, or runtime state.

The post-base range
`e30ff0508eb8d07ce3ec8d299943bd82bf56f9ef..9f23f5b867c5f2baa46b1d34db19a319b26e89ff`
contains 56 unique paths: 18 pressure records and 38 non-pressure paths. The
full historical range
`e77fc77a35d91d35d80cda3887f64c65840182b7..9f23f5b867c5f2baa46b1d34db19a319b26e89ff`
contains 92 unique paths: 28 pressure records and 64 non-pressure paths. The
historical deleted `approved.ts`, `diagnostic.ts`, `inventory.ts`, `v2.ts`, and
their tests remain absent from the current public surface.

- Initial and pre-write `git status --short --branch` and `git rev-parse HEAD`
  — clean `iss-002-versioned-contracts` at the exact reviewed full head.
- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `node scripts/planning/board-check.mjs` — sandboxed `gh` spawn refused
  `EPERM`; the approved read-only rerun passed (`board contracts verified`).
- `node scripts/format.mjs check` — sandboxed formatter spawn refused `EPERM`;
  the approved read-only rerun passed.
- `pnpm exec tsc --noEmit -p tsconfig.json` — PASS.
- `pnpm exec vitest run test/contracts test/planning-contracts.test.ts
  test/board-contracts.test.ts` — PASS (9 files, 69 tests).
- `pnpm --filter @orchestration-platform/contracts test` — PASS (7 files, 21
  tests through the current ISS-002 capability slot).
- `pnpm run contracts:compatibility-check` — the sandboxed pinned-package
  signature lookup failed closed; the approved read-only rerun passed (1 file,
  1 test).
- `git diff --check
  c0f6115acb824d9ff1c490766edcf083bb851d3d..9f23f5b867c5f2baa46b1d34db19a319b26e89ff`
  — PASS.
- Scoped identity/mapping/ownership, stale embedded-census, removed-surface,
  and provider-vocabulary searches — PASS. They also confirm that the current
  contracts implementation intentionally contains no dispatch-brief or worker-
  host implementation before this authorization gate; no executable surface
  was treated as hidden planning authority.

## Routing log

Task `ISS-002-round-61`; reported model `gpt-5.6-sol`; effort `high`; row `11`;
placement `provisional` Sol fallback in the Codex harness; transcript: this
exact-head cold review with sequential read-only checks; budget intent: normal
review cap, with no subagent or second premium attempt.

## Implementation authorization

This exact-head `PASS` authorizes implementation of ISS-002 only, constrained
to its stated contract-design decisions, acceptance evidence, verification
plan, and predicted implementation footprint. A worker or candidate release
does not certify itself. Independent implementation review and the stable
promotion path remain mandatory before any merge, release, installation, or
runtime authority.
