# ISS-022 five-row replace review — round 289

## Disposition

`PASS` for the exact `+451/-2` uncommitted diff over
`aba239ebfca9654abd1b7b227c9909b4717d60fb`.

## Pressure result

The five rows are a closed provider-owned census in ledger order. The stable
parent selects each requested barrier, the bound child event must report that
same barrier before a row can PASS, and exact stdout, empty stderr, provider
SIGKILL, no timeout/output overflow, and the barrier-specific final readback
must agree. An `ERROR` line killed by the first-line observer is not confused
with a reached-barrier crash: either coherent natural exit or observer SIGKILL
can preserve its errno, but that arm reduces only to `UNSUPPORTED` or `UNKNOWN`.

The reachable unsupported reduction is fail closed. Before rename only `41` is
admitted; the directory-sync row alone permits `41|42`, so Windows directory
open/sync `EPERM` after a successful rename and readback `42` remains
`UNSUPPORTED`, never PASS. Unexpected errno, absent errno, final read failure,
wrong byte, mixed exit/signal, malformed output, timeout, and output overflow
cannot select the replace token.

Each sequential row uses an exclusively created child directory below the
canonical, source-disjoint custody root; execution is awaited through child
close before one final readback. This leaves no row-shared target or live child
for cross-row contamination. The exact reviewed worker and stable handler own
execution and normalization; child output contains no verdict. This slice
claims process-crash/reopen behavior only and adds no reboot/durability,
candidate-certification, helper, or production authority.

Snapshots reject proxies, accessors, exotic prototypes, sparse/extra census
members, unknown keys, and barrier/order substitutions before disposition.
Focused verification passed: `44/44` tests in
`test/conformance/iss022-handler.test.ts` and
`test/portable-primitives/filesystem-executor.test.ts`; `git diff --check` is
clean.

## Proportionality

The concrete threats are barrier substitution, forged terminal evidence,
unsupported-to-PASS elevation, and shared-custody contamination. Deleting the
closed five-row table, child-envelope checks, or per-row directory/readback
would reopen one of them. The smaller credible shape is the implemented single
worker mode, one seven-field raw record, and one normalizer; no new receipt,
helper, reboot lane, retry, or policy mechanism is introduced.
