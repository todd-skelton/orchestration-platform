# ISS-022 hosted-Linux profile repair review — round 297

## Disposition

`PASS`.

`ed669ee..3e97183` changes only `test/conformance/iss022-profile.test.ts` (mode retained, 5 insertions/1 deletion); production bytes are unchanged. The mutant now maps exact `CASE_SENSITIVE` to `CASE_INSENSITIVE_LOWERCASE` and every other observed value to `CASE_SENSITIVE`. Those are the contract's only valid values, so the selected value is always replaced by the other valid value on Linux, macOS, and Windows; validation must refuse the substituted profile.

`3e97183..632b3bb` contains exactly the six reviewed PR2 paths and does not change the repaired profile test. Evidence was static exact-diff inspection only; no suite was run.
