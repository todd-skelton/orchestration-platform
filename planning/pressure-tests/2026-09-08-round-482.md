# Pressure test round 482: ISS-078 bounded queue repair

Profile: `contract`. Predecessor review
`01a08410-ea2f-70e3-bdc8-da2d8deddfd5` is the complete sweep; this note covers
only its six prescribed repairs. The original seven criteria, implementation
attempt ceiling of four, initial launch ceiling of eight, prior participants,
and external promotion boundary remain unchanged.

## G0

PASS. The strictly smaller complete repair changes the existing queue boundary:
carry the loaded command root, bind review identity before delivery, preserve
per-measure usage through the legacy repair bridge, prevalidate completion, add
process-boundary command fixtures, and co-locate the explicit repository adapter
with the queue engine. No parallel queue, component implementation, scheduler,
publication feature, toolchain change, or report-recovery path is added.

Co-location is the one organization change needed for the three-file trace. The
engine remains the `queueStep(config, adapter)` function and has no effect route
except `QueueAdapter`; repository decisions remain in the separately named
`repositoryQueueAdapter` function. Keeping the former adapter file or adding a
second runner would retain or increase the rejected four-file path.

## Focused evidence map

- F1: `supervise.mjs` derives the loaded root; adapter authority, setup, and
  delivery receive that root. Hosted mismatch expects refusal before effects.
- F2: source, repair, accepted, and completed review IDs must equal the final
  reviewer in their exact stage history before delivery intent.
- F3: repair history accepts both legacy usage forms plus exact per-measure
  partial usage. Known tokens survive unavailable cost without invented USD.
- F4: all existing item completions and any queue completion are validated
  before the item loop. Orphan/conflicting completion refuses; valid restart
  returns without component entry.
- F5: hosted process tests invoke `supervise.mjs` for refusal, observed wait,
  completion, and completed restart, asserting exit, closed output, and calls.
- F6: the static entry trace is `supervise.mjs` -> `queue.ts` -> accepted
  component, while the explicit policy adapter and injected engine remain.

Static source, type, format, planning, and JSON-length checks are author evidence.
No local runner, native fixture, Git experiment, hosted claim, self-promotion, or
ISS-079/080/081 behavior is part of this repair. Four controller gates,
independent delta review, and actual hosted Windows/macOS/Linux execution remain
external requirements.
