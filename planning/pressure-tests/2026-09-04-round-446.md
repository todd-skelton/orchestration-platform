# Pressure round 446 — neutral worker-result review disposition definition

Status: bounded docs-only ISS-011 definition, absolute planning-artifact attempt
2. Protected base is `957e55b4344099a2d606d64e562d4dc839e1ac17`.

Absolute attempt 1 was the read-only next-slice audit over that protected base
and the exact Round 444 source head
`27d44959ee6811749690d269fef64bad54432730`. The audit selected review
disposition because the walking-skeleton has a concrete disposition consumer
and no delivery or repair module has an executable consumer. This authored
packet claims no independent PASS, source execution or authority.

The exact Round 444 source subsequently merged as main
`71a2b9cd9bb882896b2fecdb2465feb6a4c926ab`. Protected-main run
`33938253448` was still running when this packet was authored; that chronology
does not supply review or execution evidence for the changed disposition bytes.

## Declared lineage and scope

Rounds 401 and 434 through 444 are mandatory inputs. That includes both
abandoned planning-source rounds 435/437, their exact heads, reviews and hosted
failures; the landed Round 439 planning source and pin; the Round 443 review
planning definition attempts; and Round 444's plan-only source attempt. The
unrelated ISS-054 Rounds 440 through 442 remain chronological records rather
than ISS-011 attempt resets. Renaming a branch, artifact or phase does not erase
any prior attempt or evidence.

This packet defines only the neutral review module's optional pure disposition
phase. It updates the module ABI, adds the adjacent ISS-011 note and records
this round. It adds no source, test, public contract, manifest row, generator,
registry output, dependency, runtime invocation, provider access, mutation or
authority.

## Literal disposition policy

The Round 443 review descriptor remains unchanged except that its ordered
`dispositionCodes` becomes exactly
`["review.complete","review.reject","review.unknown"]`. The following source
exports exactly `descriptor`, `disposition`, and `plan`. Step-4 selection,
compatibility, action, schemas, catalog, role, complete worker-result target and
result shapes remain semantically unchanged. Because the descriptor and source
bytes change, Round 444's reviewed source identity and every affected
descriptor, core, brief and plan golden must be independently recomputed; the
old identities grant the new source no admission or review inheritance.

The disposition parses and retains the existing closed `DispositionInput`,
requires canonical-byte-equal identity with the exported descriptor, and
requires the module input's review subject to be `worker-result-subject/v1`.
Parser, descriptor or subject refusal throws without producing an action
disposition. For an admitted subject it binds the supplied action-plan digest,
complete disposition-input digest and complete worker-result-subject digest
with subject kind `WORKER_RESULT`.

| Review authority    | Code              | Exact output                                                               |
| ------------------- | ----------------- | -------------------------------------------------------------------------- |
| `accepted`          | `review.complete` | `COMPLETE`                                                                 |
| `rejected`          | `review.reject`   | `FOLLOW_UP/REPLAN`, module `review`, same complete worker-result target     |
| absent or `unknown` | `review.unknown`  | `UNKNOWN/AUTHORITY_UNPROVEN`                                                |

The module parses the constructed output but does not receive captured stream
bytes and does not certify the supplied history. The caller retains the exact
input and actual stdout/stderr across the await and supplies all four preimages
to `validateActionDispositionBinding`. That existing binder owns the complete
action, route, preflight, dispatch, launch, terminal, skip, review request,
attempt, authority, code, subject and outcome relations. A structurally valid
module output may still fail that external binding. Parser success proves no
authenticity, observation, review acceptance, journal history, reclaim or
effect authority.

## Consumer seam, non-goals and proportionality

The following source cannot reuse the fixture module's
`fixture.review-consumer` descriptor or `fixture.review` action as if they were
production identities. `fixtures/walking-skeleton/test/consumer.test.ts` must
provide the actual branch/queue loader, snapshot, policy, composition and
production review-plan seam. Test-local composition in
`fixtures/walking-skeleton/test/review-consumer.test.ts` must reuse the current
review evidence builders and public binders to carry that production input and
action through coherent route, preflight, worker and review evidence while
retaining actual captured stdout/stderr for the disposition binder. Fixture
runtime remains byte-stable. If the seam requires another production or fixture
runtime file, source stops for a fresh footprint replan.

This is smaller than delivery or repair definition because neither has an
executable module consumer. A rejected disposition's `REPLAN` intent is a
record for later intake, not repair-module semantics or scheduler authority.
Also excluded are release-candidate disposition, findings policy beyond the
existing declared-code relation, worker launch, route selection, mutation,
journal/reclaim ownership, public-family changes, manifest/build activation,
registry loading and ISS-011 closure. Accepted `COMPLETE` means only completed
nonmutating review work; final cycle completion retains every later gate.

## Acceptance, verification and predicted footprint

Definition acceptance is the complete three-code/three-authority matrix; exact
action, input, subject and follow-up bindings; explicit parser versus external
raw-byte binding ownership; a production-descriptor consumer seam; and the
identity-rebaseline rule. Source vectors must cover malformed input, alternate
descriptor, null/candidate/changed worker-result subjects, all authority cells,
missing review, declared-code changes, every upstream record and stream
substitution, repeated canonical equality, complete target substitution and
the unchanged empty manifest/generator. Deleting any parser check, descriptor
or subject equality, authority arm, code mapping, target join or external-byte
binding must fail its matching vector.

Definition verification is `pnpm run planning:check`, live
`pnpm run planning:board-check` after root issue reconciliation, targeted
format checking of these three files, `git diff --check`, exact three-file diff
inspection and independent exact-head review. No test, compiler, module, native,
provider or fixture command runs for this packet.

The following source footprint is exactly
`modules/review/src/index.ts`,
`fixtures/walking-skeleton/test/consumer.test.ts`,
`fixtures/walking-skeleton/test/review-consumer.test.ts`, and one source pressure
record. It must replace the Round 444 source pin and all descriptor-derived
goldens. Planning source/pin, fixture runtime, public contracts, empty manifest,
generator, generated stub, root configuration and dependencies remain
unchanged. Source adds local typecheck and hosted focused/full Linux, macOS and
Windows evidence, followed by independent exact-head review.

## Review attack surface

Attack fixture identities presented as production evidence; an empty, partial
or reordered code census; stale Round 444 source or descriptor-derived hashes;
review acceptance treated as self-certification; `COMPLETE` treated as cycle
completion; rejected review without exact target-preserving `REPLAN`; unknown
authority converted to failure, follow-up, completion or permission; a partial
target digest; parser success presented as full binding; captured streams
omitted, copied or moved across the await; a disposition export admitted with
empty codes; hand-built records bypassing the actual preparation/evidence seam;
fixture-runtime expansion hidden outside the predicted footprint; manifest or
registry activation; invented delivery/repair behavior; and any reset of Rounds
434 through 444 or this artifact's audit attempt.
