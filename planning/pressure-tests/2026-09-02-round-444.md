# Pressure round 444 — quarantined worker-result review source

Status: ISS-011 source candidate, absolute source-artifact attempt 1, from
independently reviewed Round 443 definition head
`3de3b05971ecf27787e6da6e17049d9b486cb135`. Definition attempt 3 supplied the
independent PASS; this source still requires its own exact-head review and
hosted execution evidence.

## Declared inputs and scope

Round 443 supplies the complete descriptor, pure step-4 policy, consumer seam,
and three-file source footprint. Rounds 434 through 439 remain mandatory
lineage, including abandoned 435/437 heads, independent reviews, hosted
failures, and the 438 exact-byte replan. Round 439 planning source and pins
remain unchanged. This fresh review source artifact does not repair or reset
those artifacts or the Round 443 definition attempts.

The packet adds only `modules/review/src/index.ts`, focused additions to
`fixtures/walking-skeleton/test/consumer.test.ts`, and this record. It imports
only public contracts and exports only descriptor/plan. The manifest,
generator, generated stub, fixture runtime, planning source/pins, public
contracts, architecture, root configuration, dependencies, and authority
surfaces remain unchanged.

## Behavior and identity boundary

The exact review descriptor declares the two existing adapter tuples, one
`review.worker-result/work.read` action, required worker with review role,
eight catalog rows and empty disposition codes. Structural input failures
throw without a bound result. A parse-valid alternate descriptor, null subject,
or release-candidate subject yields only bound REFUSED/INPUT_REFUSED.

A valid prior worker-result subject yields a complete review action/brief with
null work ID and the whole immutable subject digest. Both actual TRIP and
NO_TRIP policy arms preserve the action; empty, reordered, or changed frontier
rows never choose another target. No result supplies review acceptance,
breaker clearance, runtime admission, or full-cycle authority. The module has
no disposition, and plan-only ABI validity cannot bypass the later handler
admission required before worker effects.

The test's only new-module invocation wrapper normalizes and checks the source
bytes immediately before every plan call. The candidate normalized SHA-256 is
`80a50dfdcb364697f10975cb146f49894cb2d2a0b5845c0ec39df5a839f13863`.
Independent exact-head review must recompute it and inspect those same bytes
for the sole public-contract dependency and absence of ambient/effect access.
Static/dynamic import, require, clock, random, process, network, navigator,
WebSocket, structuredClone, and an unlisted comment mutation all go through
the same identity refusal helper. They are never executed and prove identity
change only; no semantic scanner or complete ambient-name census is claimed.

## Acceptance evidence and verification

The focused vectors use actual branch/queue configuration, snapshot, policy,
and composition before retaining input across the awaited call and validating
the returned binding. They pin the entire descriptor and result structure,
canonical result bytes, descriptor/whole-subject/core identities, canonical
brief and footprint hashes, and an explicitly zero-input-digest plan projection.
The actual full result separately binds its real input digest. Goldens were
authored from literal contract records; the new module was not loaded locally
to derive them.

Positive and negative vectors cover both policy arms; frontier noninterference;
same-cycle and malformed/relational input refusals without output; valid
descriptor/null/candidate refusals; every complete worker-result component and
ordered result entries; stale-target reuse even after repairing the outer
input digest; result/role/action/catalog/directive joins; exact footprint
goldens; repeated-input equality; source identity; and unchanged empty
manifest/generator/planning identities. Footprint policy is pinned by the
module golden rather than falsely attributed to a generic contract binder.

Local verification is `pnpm typecheck`, `pnpm planning:check`, targeted pinned
formatting and `git diff --check`. Dependencies may be restored only offline
with a frozen lockfile and lifecycle scripts disabled. No local Vitest,
native compiler, addon, probe, or lock experiment is run. Hosted focused/full
suites on Linux, macOS, and Windows plus independent exact-head review remain
required; this author packet claims neither execution PASS nor certification.

Author checks completed: offline frozen installation with scripts disabled,
typecheck, planning check, targeted pinned formatting for both TypeScript files
and this record, normalized source identity, and diff checks. The initial
sandboxed pnpm script invocations failed their registry-signature fetch; the
permitted escalated retries passed without disabling signature verification.
Both obsolete lookups exited. The root owns live issue/board reconciliation;
no board parity or hosted test result is claimed here.

## Non-goals and review attack surface

No disposition/finding payload, release-candidate planning, worker launch,
routing/model/host choice, rendering, provider observation, credential,
mutation, journal, reclaim, production cycle, delivery/repair semantics,
registry/build activation, or ISS-011 closure is included. Seeded prior subject
records are input, not evidence of an executed author cycle. Any changed
definition, extra file, new dependency/public family, runtime integration, or
authority claim forces replan.

Review should attack copied or partial target identities, same-cycle self-review,
descriptor/catalog changes, TRIP accidentally suppressed by frontier-planning
logic, policy facts or a planned review interpreted as authority, hidden ambient
effects, a plan call bypassing byte admission, reintroduced source classifiers,
goldens derived from production output, footprint-policy overclaims, and
changes to the existing planning lineage or activation surfaces. Deleting the
identity comparison must admit changed bytes and fail its refusal vectors;
deleting an exact target or output field must fail its binding/golden vector.
These deletion checks are review/hosted evidence, not local executions.
