# ISS-022 custody result clarification — round 366

`PASS` at `b3d57401f91d0b64bcfd01a0c57bb85be7257470`, against previously
reviewed `aac9d7a71818095997d3c8188b8721ad04f1df2b`. Independent reviewer:
`/root/iss022_native_lock_ledger_review`, Sol high, row 11.

The witness author found a concrete ambiguity: Windows metadata-policy
rejection after successful native calls could have the same call census as
accepted custody. The sixteen-line repair closes only the test-only
`describeCustody` result as `{identity,facts}`. Policy, native-call or close
failure yields null identity and an UNKNOWN barrier; actual call and close
records remain intact. Earlier native facts cannot restore rejected identity.

No blocker remains in this repair. Candidate interfaces and six-field facts
are unchanged. This wrapper is smaller than inventing native error codes,
call records or exception properties; it preserves the independently observed
failure without extending the experiment. Review was read-only and established
no native behavior, capability or release authority.
