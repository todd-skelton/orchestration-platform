# Pressure test — round 24

**Disposition:** `BLOCK_REPLAN`

The twenty-fourth independent review found eight composition-timing gaps across
recovery and repository-protection bootstrap. The 39-issue packet remains
unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R24-001` — A pre-activation fence could be visible before recovery authorization was consumed. | Consumption/read-back now precedes fence publication; abort revokes the consumed binding, and no visible fence may coexist with unconsumed authority. |
| `OP-R24-002` — One recovery-authorization shape required successor fields that N0 cannot possess. | Defined a closed bootstrap-N0/successor union with exact required and canonical-null fields plus mode-specific terminal evidence. |
| `OP-R24-003` — No-launch transactions could not satisfy an unconditional launch-terminal cleanup predicate. | Split N0, uninterrupted successor with authoritative pointer absence, and attached-successor terminal revocation predicates. |
| `OP-R24-004` — No actor could attach authorization to READY before child creation. | Attachment now occurs on the launched predecessor's authenticated LIVE broker channel after verifying the immutable READY→LIVE chain; the shim receives no broker client. |
| `OP-R24-005` — Child-exit proof was required before terminal state, but the child waited for that terminal handshake. | Child writes the terminal cycle receipt while live; the monitor terminalizes the pointer and clears the fence; archive alone requires child exit. |
| `OP-R24-006` — “Digest at generation zero refuses” accidentally rejected generation-zero LIVE. | Limited null/refusal to generation-zero ordinal-zero and added explicit golden cases for generation-zero LIVE and later-generation READY. |
| `OP-R24-007` — The probe receipt attempted to bind production workflow bytes owned by a downstream issue. | Protection receipt now binds ISS-036 synthetic workflow identities and ruleset/path-policy semantics; the later host-custody review receipt binds actual ISS-038 production workflow digests. |
| `OP-R24-008` — The protection receipt needed the verifier anchor before the defined environment transport set it. | Operator now sets/read-backs the anchor variable before the producer run; only the four finalized artifact values are set afterward. |

## Re-review focus

- Crash before/after consumed authorization and fence publication, including
  pre-activation abort.
- Execute N0, uninterrupted successor, and attached-recovery successor cleanup.
- Verify the LIVE attachment and child/monitor terminal handshake at every fault
  boundary.
- Ensure probe-owned versus production-owned workflow identities and pre-/post-
  upload protected variables cannot be reordered or substituted.
