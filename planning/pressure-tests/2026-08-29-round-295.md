# ISS-022 profile-layer split review — round 295

## Disposition

`BLOCK_REPLAN`.

The isolated PR1 is mechanically independent: its test directly calls the
physical handler, executable-custody wrapper, constructor, and validator; no
profile source, test, export, or bundle row imports `iss022-suite` or another
stashed PR2 source. The shared amendment is exactly five insertions: one public
export, one source-census row, one test-census row, and their two exact expected
rows.

## New split blocker

The split drops authority evidence that Round 294 relied on. The profile test
does not replace `process.execPath` during the custody callback, so it never
proves that dynamic path substitution refuses. Its only non-PASS physical
mutation is row 2; it does not attack formerly ignored rows 0, 4, and 5 or prove
that each yields the null selection arm and that restoring a non-null selection
refuses. Those attacks remain only in the stashed PR2 suite test, so PR1 cannot
land independently with the claimed executable-custody and all-six-physical
selection authority. Move the bounded mutants into the profile test without
adding a suite dependency.

Focused evidence otherwise passed:
`pnpm exec vitest run test/conformance/iss022-profile.test.ts test/conformance/stable-bundles.test.ts`
(2 files, 9 tests),
`pnpm exec tsc --noEmit -p packages/conformance/tsconfig.json`, and
`git diff --check`.
