# ISS-002 pressure test — round 154

## Review basis

This is a fresh independent cold repair/removal review of the reservation
lifecycle public type at exact initially clean and pushed head
`fb194ad65dfd57ef4fae650873d8ad80f2e45afd` on
`iss-002-versioned-contracts`, with exact parent `875709e`. Local `HEAD` and the
read-only `ls-remote` result for `origin/iss-002-versioned-contracts` both
resolved to that exact commit before review.

The authority basis was re-established from `AGENTS.md`,
`docs/planning/work-model.md`, the sole recovery-attempt reservation value
ledger in `docs/architecture/contract-decisions.md`, the incorporated
supervisor contract, the registered `planning/drafts/ISS-002.md`, Round 153,
and the exact `875709e..fb194ad` delta. The public export chain and focused
compile-time evidence were inspected directly; parent repair claims were not
accepted as proof. No Chase Sets repository, project, issue, board, backlog,
source, or other consumer system was read or touched.

The exact repair changes only three files: it removes the stale type from
`definitions.ts`, derives the owning type from the exact runtime tuple in
`attempt.ts`, and adds compile-time plus runtime focused evidence. The delta is
18 insertions and two deletions. It changes no parser, record, schema census,
digest, transition, serializer, registry, persistence path, package,
dependency, command, capability, process, runtime, roadmap, or board topology.
`git diff --check 875709e..fb194ad` passed.

## Disposition

`PASS` for the focused OP-R153-1 repair and the completed bounded recovery-
attempt reservation value implementation at exact reviewed head
`fb194ad65dfd57ef4fae650873d8ad80f2e45afd`.

The stale four-literal lifecycle union is gone. The owning attempt module now
exports one runtime-derived exact type:

```ts
export const recoveryAttemptReservationLifecycles = Object.freeze([
  "RESERVED",
  "CONSUMED",
  "TERMINAL",
] as const);

export type RecoveryAttemptReservationLifecycle =
  (typeof recoveryAttemptReservationLifecycles)[number];
```

The public index exports that type through the attempt module, and no duplicate
type remains in definitions. TOMBSTONE is not assignable. The checked-in exact-
equality and negative assignment evidence both fail under an in-memory
TOMBSTONE reintroduction mutant. No runtime or authority surface was widened.

## OP-R153-1 removal verification

Repository search and exact diff establish one owner:

- `packages/contracts/src/definitions.ts` no longer declares or exports
  `RecoveryAttemptReservationLifecycle`;
- `packages/contracts/src/attempt.ts` owns the runtime tuple and derives the
  public type from its indexed member union; and
- `packages/contracts/src/index.ts` already exports `attempt.ts`, so consumers
  import the exact owning type from the public package surface.

The resulting type is structurally equal to only:

```text
"RESERVED" | "CONSUMED" | "TERMINAL"
```

The common selected `pointer-tombstone-value/v1` remains a separate record
family after reservation TERMINAL. Neither the runtime tuple nor erased type
can represent a reservation-schema TOMBSTONE lifecycle.

The test declares an invariant type comparison whose value must be `true` and
also contains a compile-time negative:

```ts
// @ts-expect-error TOMBSTONE is not a reservation value lifecycle
const forbiddenLifecycle: RecoveryAttemptReservationLifecycle = "TOMBSTONE";
```

This uses two independent compiler discriminators. Exact equality catches any
added, deleted, or replaced member; the expected error catches specifically
that TOMBSTONE must remain unassignable.

## In-memory type mutation evidence

The TOMBSTONE mutant was supplied only through TypeScript 7's virtual
filesystem callback. The compiler service read an in-memory version of
`attempt.ts` whose runtime tuple was:

```text
RESERVED, CONSUMED, TERMINAL, TOMBSTONE
```

All other source and test files came from the unmodified checkout. No
repository file, generated file, compiler configuration, cache registration,
or dependency was edited. The full project semantic check reported both
expected focused failures in `reservation-value.test.ts`:

1. `TS2322`: the exact-equality expression became `false`, which is not
   assignable to the required `true`; and
2. `TS2578`: the TOMBSTONE assignment became valid, making
   `@ts-expect-error` unused.

The mutant is therefore deletion/reintroduction-discriminated at the public
type surface. It cannot survive behind type erasure or a green runtime-only
test.

## Runtime and authority reconfirmation

The repair changes no runtime code apart from adding an erased type alias
derived from the already-reviewed tuple. The Round 153 otherwise-clean
implementation remains intact:

- exact RESERVED eight-member and CONSUMED/TERMINAL nine-member closed
  branches;
- exact scalar and nullable-cell validation;
- only null→RESERVED→CONSUMED→TERMINAL;
- exact five-field preservation and inclusive timestamp comparison;
- canonical detached bytes;
- exact `serialization:pointer-context-required` detached serializer refusal;
- only common pointer `Dv` under authenticated `Dp`;
- exact v1 compatibility registration;
- hostile reflective totality; and
- absence of descriptor, launch, attempt-log, archive, reservation-tombstone,
  UUID allocation, IO, CAS, capability, process, broker, command, or runtime
  authority.

The implementation delta does not touch any of those functions or tests except
to add the type evidence. Focused runtime tests remain green, and the public
runtime lifecycle array remains exactly three values.

Parser success, the exact TypeScript type, canonical bytes, or a common `Dv`
does not select a reservation, launch a process, issue a capability, certify a
candidate, or promote a release. Stable external mutation authority and later
ISS-004/ISS-030 composition remain required.

## Proportionality

| Mechanism | Concrete in-scope threat | Removal evidence | Smallest retained shape |
| --- | --- | --- | --- |
| Runtime-derived lifecycle type | A second hand-written type drifts from parser/runtime values. | Reintroducing TOMBSTONE changes the exact union and fails compilation. | One indexed type derived from the owning tuple. |
| Exact-equality assertion | Any extra/missing lifecycle remains assignable unnoticed. | TOMBSTONE mutant produces TS2322. | One local zero-runtime type comparison. |
| Negative TOMBSTONE assignment | The forbidden fourth lifecycle reappears while runtime tests erase types. | TOMBSTONE mutant produces unused-expect-error TS2578. | One checked compile-time negative. |
| No duplicate definitions owner | Two public lifecycle contracts diverge again. | Definitions search/diff shows the old export deleted. | Own tuple/type/parser together in `attempt.ts`. |
| No runtime widening | A type repair becomes descriptor/tombstone/runtime machinery. | Three-file delta contains no such surface. | Type and evidence only. |

The repair removes duplicated authority and derives rather than invents. It is
smaller than adding a schema version, migration, runtime conversion, or
compatibility alias.

## Verification evidence

The in-memory compiler mutant fully exited before unmutated testing. The focused
suite exited before the compatibility slot. All required gates then ran
strictly one at a time. The standalone full-suite process fully exited before
the separate bootstrap verifier began. Direct checked-in scripts or lockfile-
pinned binaries avoided the known signed package-manager launcher
infrastructure dependency; no semantic gate was substituted. Test processes
used the increased 600,000 ms test and hook ceilings.

Focused evidence:

1. In-memory TOMBSTONE type reintroduction — KILLED by TS2322 and TS2578 in the
   focused reservation value test.
2. Reservation value/identity/pointer suites — PASS: 3 files and 23 tests in
   842 milliseconds.
3. ISS-002 `contracts:compatibility-check` capability slot — PASS: 1 file and
   1 test in 794 milliseconds.

Required gates:

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`;
   it read only this repository and its registered delivery project.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 37 files and 407 tests in 79.09 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 37 files and 407 tests in 75.34 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This clean repair/removal round clears exact head
`fb194ad65dfd57ef4fae650873d8ad80f2e45afd` as the bounded recovery-attempt
reservation value implementation authorized by the Round 152 ledger.

It does not authorize descriptor/log/archive/tombstone composition, UUID
allocation, persistence, selected-family/currentness validation, proposal/CAS,
capability issuance, broker calls, process launch/adoption, runtime recovery,
release promotion, or candidate self-certification. Any such widening requires
its own reviewed authority.

After this report is written, the sole intended worktree change is
`planning/pressure-tests/2026-08-23-round-154.md`; no source, test, authority
document, draft, issue, roadmap, board, or other planning file is edited, and
no commit is created.
