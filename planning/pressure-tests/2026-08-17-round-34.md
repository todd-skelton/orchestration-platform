# Pressure test — round 34 (corrected record)

**Disposition:** `BLOCK_REPLAN`

This is the corrected historical record of the proportionality attack. The
original draft usefully found over-engineering, but it assigned new work to
keys already owned by open PR49 and proposed a successor-epoch write that the
selected single-epoch model forbids. Neither draft is authority; the repaired
architecture and issue drafts are the current plan.

| Finding | Selected repair |
| --- | --- |
| `OP-R34-001` — Sparse trees, 256-sibling proofs, node inventory, a materialization coordinator, and epoch-split rotation defend threats excluded by the kernel-exclusive single-writer topology. | Delete them from the planned current surface. Use one FULL_REQUIRED linear append-only `authority-history/v1` chain and twelve pointer kinds. |
| `OP-R34-002` — Arbitrary-precision decimal machinery exceeds any plausible installation need. | Canonical decimal strings stop at `Number.MAX_SAFE_INTEGER`; grammar and string comparison refuse overflow before conversion. |
| `OP-R34-003` — Never-deployed superseded schemas had become a public diagnostic surface with no reader. | Delete their implementation and tests before N0. Git history is the only archive; unknown/future schemas fail closed. |
| `OP-R34-004` — The authority chain did not state a non-cyclic genesis/rotation shape. | Define a closed `GENESIS|ROTATION` union; both exclude successor value/proposal/tip/head, and the selected authority binds head ordinal plus record digest. |
| `OP-R34-005` — Packet mutation results could float free of their registry slot, and UNKNOWN could carry unbounded host data. | Cross-bind top authority and identity to selected target / real winner / empty slot. Make UNKNOWN a fixed-size closed `UNREADABLE|MALFORMED|IMPOSSIBLE` union. |
| `OP-R34-006` — Integration, operator visibility, and reviewer discrimination arrive too late or not at all. | Reserve PR49-owned `ISS-039/040`; add bounded, blocks-nothing `ISS-041` walking skeleton, `ISS-042` operator status, and `ISS-043` review calibration. |
| `OP-R34-007` — The dependency graph restated transitive prerequisites and obscured the runnable frontier. | Admit the exact transitive reduction only after proving the 544-pair pre-existing closure unchanged and checking lifecycle/external-authority gates semantically. |
| `OP-R34-008` — Mandatory repeated clean-removal rounds could turn simplification into an unbounded review ritual. | Require one deletion test and smallest-alternative justification in every replan/review; repairs return to the ordinary independent-review gate. |

The original 183-source migration snapshot is unchanged. PR49 alone owns its
post-census `#6999/#7000` addendum and the reserved `ISS-039/040` drafts.
