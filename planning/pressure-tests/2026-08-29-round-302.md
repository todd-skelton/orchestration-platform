# ISS-003 post-repair follow-up review — round 302

`PASS` at exact `origin/main`/worktree head
`add69913e8d97c043703f62e7690a95d89e5b337`.

Round 300's two blockers are closed. Placeholder dispatch remains handler-free
and derives the exact `{code:"CAPABILITY_NOT_IMPLEMENTED",owner:issue}`
diagnostic from the same registration's `issue`; the envelope has `result:null`,
one diagnostic, and one per-registration golden, with no second registry. An
ISS-013 implementation replaces only that placeholder through the already
closed handler/result-schema ABI.

Before filesystem access, malformed top-level invocation/flags and their
accessor/proxy/trap cases map to `ARGV_REFUSED`; malformed environment and its
corresponding cases map to `CONFIG_REFUSED`; non-absolute selected paths and
missing/relative selected OS-default bases map to `PATH_REFUSED`. Shape-check
throws are caught into the assigned row, so the loader's no-throw result union
is deterministic for every Round 300 case.

The repair adds no mechanism, command, consumer, or authority boundary.
`--reveal` remains absent from the authoritative command row, registration, and
census. Scope, redaction, engine/adapter separation, deferral, and predicted
footprint remain unchanged and proportional.

Focused evidence: `pnpm run planning:check`,
`node scripts/verify/bootstrap-contracts.mjs`, and `git diff --check` pass.
