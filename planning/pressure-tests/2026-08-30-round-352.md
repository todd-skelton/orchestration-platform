# ISS-013 snapshot CLI and mixed rows — round 352

Disposition: `BLOCK_FIXABLE` at `a1071a607a725b7096bda2bebcbbd84a2e19dcda`,
base `793c5d740545b8b4b336ec481fa9fbc29d3d8ae0`.
Independent cold reviewer: `/root/iss013_cli_review`, Sol high, row 11.

Two test expectations retain superseded census facts: the dispatcher expects
only config implemented, and the public-envelope test accepts a snapshot
placeholder. Host execution passed 219 focused tests and failed exactly those
two assertions. Update the exact config/project family list while retaining
the no-output assertion; use project plan for positive placeholder controls
and explicitly refuse a snapshot placeholder.

No additional implementation blocker was found. Executable status comes from
the reviewed census before inspecting candidate metadata; malformed snapshot
rows refuse and plan/apply never receive the handler. Only COMPLETE facts can
succeed, binding precedes fixture observation, file reads are bounded and
handles close, and fixture composition/imports/build targets stay fixed.

Proportionality: deleting census admission permits metadata downgrades;
existing owner/schema fields suffice. Deleting binding or COMPLETE-only
admission permits substituted or incomplete facts; existing parsers and
literal comparisons suffice. Bounded ordinary file reading addresses large
input without introducing containment or reparse authority for a data file.
