# ISS-006 Linux exact-tree repair review — round 178

## Disposition

`PASS_TO_LAND`.

This is a fresh independent cold removal review of pushed base
`94239b2f1c852ce42febd7567c76905964321c92` plus the complete current
uncommitted Linux slice, including the two repairs required by Round 177 and
the final out-of-set parent-profile deletion mutant. The exact reviewed tree
implements the sole reduced Round-176 design without widening candidate access,
mutation authority, evidence authority, or package surface. Round 177's two
blockers are removed.

This disposition authorizes landing this exact private Linux implementation
slice. It is not an ISS-006 hosted authority claim: the complete issue still
requires the ledger's protected stable workflow, all required macOS, Windows,
and Linux evidence, provider binding, independent review, and later stable
promotion path. Candidate code and this diagnostic run certify nothing.

## Exact reviewed state

The review began and ended at:

```text
HEAD 94239b2f1c852ce42febd7567c76905964321c92
```

The prior review records read as inputs were:

```text
a3e7cb6b347119051babd1bfa434e0362450334cfadac0ac6e38e6706c589418  planning/pressure-tests/2026-08-26-round-176.md
4acea5259c22c4d025650190b02e8f4ccb6e8d0590e1a84d07da8bf01224c102  planning/pressure-tests/2026-08-26-round-177.md
```

I read `AGENTS.md`, the complete ISS-006 draft, the sole trusted-conformance
ledger in `docs/architecture/contract-decisions.md`, Rounds 173, 176, and 177,
and every current changed or added source, helper, fixture, test, sudoers, and
bundle/public-boundary file. I reviewed the implementation rather than
accepting an author characterization or candidate result.

The final review pin for the repaired helper and its executable fixture is:

```text
a407a0ec755f718077448431eaeb44afba34badd3c6492bdb40abceb9063a321  packages/conformance/src/linux-principal-dac.py
1641df04b1599c6a2157de40091f07f11cefd540f3651ad2f9af18bf8691c347  test/conformance/linux-principal-dac-fixture.py
5f8b5d53b3dd792af3a5c35af7ef482c841f36ace8787a54510e2b0e641bbbef  test/conformance/linux-principal-dac.test.ts
```

## Round-177 crash-prefix repair

The parent RESTORE admission is now the exact closed four-profile set:

1. resting original `stableUid:stableGid 0700`;
2. final grant `stableUid:<privateGid> 0710`;
3. PREPARE after `fchown` and before `fchmod`, namely
   `stableUid:<privateGid> 0700`; and
4. RESTORE after `fchown` and before `fchmod`, namely
   `stableUid:stableGid 0710`.

`restorable_parent_profiles` constructs those profiles from the canonical
durable request. `open_handles` still opens the complete ancestor chain
fd-relative with no-follow, and compares the retained parent device, inode,
path, type, UID, GID, and mode against that exact tuple. All non-parent
ancestors remain bound to their one original identity. An out-of-set
`stableUid:<privateGid> 0711` parent is refused in `open_handles` before
`restore` or any mutation; its fixture proves the profile is unchanged and no
success output is written.

Both admitted syscall intermediates are driven through the actual
`main -> open_handles -> restore` control flow. Each is restored to exact
`stableUid:stableGid 0700`, marked `parent-transition-ambiguous`, and causes the
helper command to fail after repair. A separate clean no-scratch retry through
the same entry point returns only canonical `{"ok":true}`. This is the correct
fail-closed distinction: a known safe crash prefix may be repaired, but the
attempt containing the ambiguous observation cannot be laundered into success.

The TypeScript custody treats that nonzero helper result as RESTORE refusal,
keeps the active lease and synced intent, and permits the same exact lease or
startup recovery to retry. Only a subsequent successful helper result, retained
handle/profile recheck, exact restored root/parent census, intent unlink, state
directory sync, and pathname-absence proof remove the intent. The existing
failed-RESTORE/retry and unlink-plus-directory-sync tests execute those durable
caller semantics. No command exit can erase custody on its own.

## Grant, revocation, census, and access result

The transition ordering remains exact and is now behaviorally asserted:

- PREPARE grants candidate, RPC runner, copied runtime, scratch, and execution
  root before granting the dedicated execution parent last;
- after trusted root/pipe terminality and repeated pidfd UID quiescence,
  RESTORE revokes the dedicated parent first, then restores root, candidate,
  RPC runner, copied runtime, and scratch;
- the privileged helper requires the parent census to be exactly the one named
  execution root before mutation and again under retained ancestor custody;
  the stable caller independently requires the same one-root relation; and
- scratch deletion, the exact remaining three-file root census, execution-root
  identity cleanup, and account/private-group deletion occur only after parent
  search revocation.

The Linux RPC runner performs its filesystem gate before reading the challenge
or importing candidate code. With the already-verified exact real/effective/
saved/filesystem UID/GID, sole effective primary group, `NoNewPrivs`, and zero
capabilities, it corroborates:

- exact private-GID `X_OK` on the dedicated parent;
- `R_OK`, `W_OK`, and directory listing refusal on that parent;
- search-only, not read/list/write, access on the execution root;
- read/execute but no write on the exact candidate, RPC, and copied runtime;
  and
- full candidate UID/private-GID access only to its exact scratch directory.

Parent write refusal supplies the POSIX create/rename/delete denial. The
privileged exact one-root census proves no sibling exists, and the candidate
cannot create one. Account, DAC, execution-state, helper, and stable authority
roots remain stable-owned `0700`, disjoint and nonnested; none is passed in the
candidate argv or exposed through the package API. Their stable profile and
path custody, combined with the exact candidate credential/group census, is the
authoritative isolation proof. Candidate-side checks are corroborating hostile
process data only; they neither grant access nor certify the run.

The repaired opt-in Debian Bookworm/Node 24 diagnostic ran as the unprivileged
stable `node` user and passed its one test in 10.794 seconds (Vitest duration
11.63 seconds). It exercised all three fresh production cycles, the new
pre-import filesystem gate, and teardown. After `authority.close()`, it
independently observed an empty dedicated execution parent with exact stable
UID, stable GID, directory type, no symlink, and mode `0700` before removing the
fixture directory. It also rechecked the stable-owned `0700` account, DAC,
execution-state, and authority roots and final account/group absence. The run
emitted no receipt, aggregate, provider record, capability, certification,
promotion, or installation result.

## Trust, surface, and proportionality sweep

Round 176 remains proportional and no alternate design entered the tree:

- only one fresh private GID gains one search bit on one dedicated empty
  execution parent; there is no world traversal, shared parent, named ACL,
  stable-root grant, same-principal fallback, inherited procfd route, container
  authority, or new root launcher;
- only opened, identity-checked copies of the runtime, self-contained RPC
  bundle, and candidate receive the exact root/private-group DAC relation;
- the launch remains the closed authenticated `sudo -n -D` to `setpriv`, with
  cleared groups/capabilities, `no_new_privs`, empty environment, scratch CWD,
  and exact relative copied-runtime/RPC/candidate arms;
- the stable coordinator alone creates the challenge and expected semantics,
  captures monotonic launch-to-terminal timing, parses hostile output, enforces
  three fresh cycles, requires teardown before result, and computes normalized
  success; and
- the Linux authority, coordinator, helpers, and custody APIs remain absent
  from the package-root exports. No receipt, provider, status, certification,
  promotion, installation, capability, or general command route was added.

The copied runtime row, canonical Python target custody, self-contained RPC
bundle, bounded shadow parser, private-GID parent grant, and durable crash
records each answer a demonstrated current threat. Removing any one reopens the
Node canonical-reopen failure, helper substitution, unavailable Python target,
deprecated shadow dependency, crash residue, or candidate/stable access
confusion. Broader mechanisms remain deleted.

## Authority-document and planning impact

No ISS-006 draft, sole ledger, or board amendment is warranted. The ledger and
draft already require fresh separate principals, exact least access, stable-
owned semantics and timing, terminality, process absence, revocation, cleanup,
three OS families, external provider evidence, and candidate
non-certification. Round 173 remains the Linux private-UID/GID POSIX-DAC design;
Round 176 is the bounded dedicated-parent search repair. Round 178 confirms the
exact implementation now satisfies them; it does not create a new contract or
change issue status.

## Checks actually run

1. Complete current status/diff, source/helper/test, authority, draft, prior
   round, package-export, and bundle-census inspection — PASS.
2. `pnpm exec tsc --noEmit -p tsconfig.json` — PASS.
3. Focused Vitest over isolated walk, Linux account/DAC/execution/process/
   composition, hosted diagnostic, and stable bundle census — PASS: 9 files
   and 85 tests; the one hosted file skipped on this Windows reviewer host.
4. Direct Python helper control flow was executed by the focused
   `linux-principal-dac.test.ts` fixture — PASS: all 14 cases, including both
   parent syscall intermediates and the out-of-set `0711` refusal.
5. `git diff --check` before this record — PASS apart from Git's checkout
   LF/CRLF notices.
6. Post-repair disposable Debian Bookworm/Node 24 opt-in diagnostic — PASS as
   independently reported from the exact repaired tree; three fresh production
   cycles, pre-import access checks, and final parent profile/absence executed.

## Landing authorization boundary

`PASS_TO_LAND` applies only to the exact reviewed private Linux slice and the
tests/diagnostic fixture bound above. Any implementation change after this
record requires ordinary diff review and any trust-boundary change requires a
new removal review.

This record does not dispatch a workflow, grant hosted authority, write a
provider record, receipt, or aggregate, select or promote a candidate, issue a
capability, install a release, mutate a consumer, access Chase Sets, or make an
outward change.
