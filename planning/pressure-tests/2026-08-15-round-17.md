# Pressure test — round 17

**Disposition:** `BLOCK_REPLAN`

The seventeenth independent review found five internal inconsistencies in the
successor handoff. The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R17-001` — Both `current.json` and `installation/active-release.json` appeared authoritative. | Deleted `current.json` authority; canonical `installation/active-release.json`/`active-release/v1` is now the only activation pointer everywhere. |
| `OP-R17-002` — Pre-pointer staging was described as rollback-capable while the state machine forbade rollback after first mutation. | Preserved N0's forward-only bootstrap rule, but added explicit N1+ `STAGED_PRE_ACTIVATION` and `ABORTED_PRE_ACTIVATION` states for non-authoritative staged bytes/pending admission; pointer CAS begins forward-only recovery. |
| `OP-R17-003` — The shim recovery fence lacked a schema/path/CAS owner. | Defined canonical `activation-recovery-fence/v1` at the sole state-root path with exact transaction, candidate, executable, manifest, broker admission, record, generation, lifecycle, and time fields; specified create/update/reconcile/clear and fail-closed shim behavior. |
| `OP-R17-004` — The broker's equality invariant would reject predecessor N's only post-pointer activation call. | Restricted equality to ordinary operations and defined one transaction-bound mismatch exception for the already-open predecessor activation/recovery call, with exhaustive fence/pending-admission/recovery/generation bindings and permanent closure after broker CAS. |
| `OP-R17-005` — N1 verification was invoked inline before N0's promotion cycle/tick terminated. | Promotion now records a successor-verification follow-up; N0 completes steps 14–15, clears the fence during terminalization, and exits. The next scheduler tick runs cycle five under N1; the following independent tick proves steady state. |

## Re-review focus

- Confirm there is one pointer, one fence, and an unambiguous rollback/forward-
  recovery boundary.
- Confirm no ordinary credential operation exploits the temporary pointer/client
  mismatch.
- Confirm each scheduler tick runs at most one cycle through the full handoff.
