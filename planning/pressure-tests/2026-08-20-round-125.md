# ISS-002 pressure test — round 125

## Review basis

This is a fresh independent cold implementation review of exact initially clean
candidate head `6b66d5c804706fb2cccebd79cabc803e804b3d6a` on
`iss-002-versioned-contracts`, with exact parent
`52bebcde481da2dc1b2e74c3a7e0f30475946579`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
implementation authority in `docs/architecture/contract-decisions.md`, the
registered `ISS-002` draft, rounds 122–124 as the recent finding/removal chain,
and the exact parent-to-head implementation diff. No Chase Sets repository,
project, issue, board, backlog, consumer source, or other consumer system was
read or touched.

The exact diff changes only `packages/contracts/src/index.ts`,
`packages/contracts/src/recovery.ts`, `packages/contracts/src/registry.ts`, and
the new `test/contracts/recovery-core.test.ts`: 477 insertions and one deletion.
It adds the bounded immutable-core parser/digest, public dispatch and registry
entries, and focused evidence. It adds no package, command, runtime mutator,
persistence reader, broker operation, capability, proposal, CAS, state/native/
post-selection/archive/tombstone/composition schema, planning change, or board
topology. `git diff --check` passed. The checkout was clean before this required
pressure record was written.

## Disposition

`BLOCK_REPLAN` for the bounded recovery-authorization immutable-core
implementation at exact reviewed head
`6b66d5c804706fb2cccebd79cabc803e804b3d6a`.

The round-124 removal decision remains clean, and the parser, path constructor,
dedicated `Dac` helper, registry, and focused structural evidence otherwise
conform. The implementation nevertheless exposes a second core identity
through the existing public serializer. After the newly registered parser
succeeds, `serializeContract("recovery-authorization-core/v1", core)` returns
the generic untagged `canonicalDigest` rather than the sole authorized framed
`Dac`. For the committed BOOTSTRAP golden, the public serializer returns
`7a2eea3d0f85f3373410b63171584c1d5a12bd16a9913629f1af1f25a25f4fa3` while
the authorized `Dac` is
`a8de464e22b7194daf49b52f102e6513b19255ff49d965fe68e4ef8940e67879`.
The values are unequal.

The smallest bounded repair is to route only public serialization of
`recovery-authorization-core/v1` to
`computeRecoveryAuthorizationCoreDigest(parsed.value)` while preserving the
same canonical bytes and every other schema's current behavior. Focused tests
must assert for both branches that the serializer's digest equals `Dac`, differs
from the untagged canonical digest, and changes/fails under a domain deletion or
substitution. No new schema, generic digest registry, state, composition,
broker, persistence, capability, CAS, or runtime surface is needed.

## Census, closure, and scalar attacks

An independent in-memory audit encoded the authority lists separately from the
implementation. It proved fifteen unique common names, three unique
BOOTSTRAP-only names, twelve unique SUCCESSOR-only names, empty pairwise
intersections, and exact merged branch sizes of 18 and 27. The implementation's
three exported lists equal those independent lists and are internally
ascending. Both complete branches place `candidateDigest` before
`capabilityDigest` before `capabilityReferenceDigest`; canonicalization is
independent of input insertion order.

For both complete fixtures, the audit independently removed, renamed, nulled,
reordered, and replaced every scalar with a number. It inserted every member
from the opposite branch and additional unknown members. Every malformed case
refused, while a reverse-insertion-order object parsed to the same canonical
bytes. The committed evidence additionally pins exact complete BOOTSTRAP and
SUCCESSOR byte strings and both `Dac` goldens. Opposite-branch members remain
absent rather than nullable, and `candidateOperationManifestDigest`, a verdict,
raw capability/reference material, selected-authority material, and unknown
members cannot enter the preimage.

All digest members use the lowercase SHA-256 grammar; installation, project,
transaction, and successor cycle identities use lowercase UUIDv7; both times
use exact millisecond UTC; every generation is a canonical safe-decimal string.
Equal and reversed issue/expiry boundaries refuse, proving strict
`issuedAt < expiresAt` without consulting a clock. The audit exercised zero,
safe maximum, and first overflow for BOOTSTRAP `nativeGeneration` and all four
SUCCESSOR generation fields. Zero and maximum succeed where adjacency permits;
a maximum predecessor, zero successor, equal/skipped pair, noncanonical value,
or overflow refuses. `successorBrokerGeneration` is exactly predecessor plus
one.

## Canonical identity, path, and public dispatch

The dedicated `computeRecoveryAuthorizationCoreDigest` parses first and
computes exactly
`framedDigest("recovery-authorization-core/v1",
[frame.canonical(core)])`. Independent checks proved that each result equals
that one-part formula and differs from an untagged canonical digest, a different
domain, and a two-part frame.

The public serialization route does not preserve that sole-identity rule.
`serializeContract` calls the newly reachable core parser and then returns its
unchanged generic `canonicalDigest(parsed.value)`. The public result therefore
names an untagged second digest for the same valid core. No committed recovery-
core test invokes `serializeContract`, so the exact `Dac` goldens do not
discriminate this route.

The existing path constructor returns exactly
`installation/recovery-authorizations/<transactionId>/core.json` for the
record's canonical transaction fixture and refuses a non-UUID transaction.
There is no caller path, enumeration, alternate root, latest-file rule, or
filesystem access in this slice.

The public schema census contains `recovery-authorization-core/v1` exactly
once. `parseContract` dispatches both branches through the closed parser; the
two vocabulary-registry branch entries expose the exact 18- and 27-name lists;
same-version compatibility is readable and a future version refuses.
`parseCanonicalContractBytes` accepts both exact LF-terminated canonical
records and refuses noncanonical insertion-order JSON. The registered branch
names, literals, and values pass the engine vocabulary lint with no repository,
Git/code-host, provider, credential-store, consumer, or platform-specific
identity vocabulary.

## Totality and deletion discrimination

The parser snapshots through the established closed JSON boundary before
reading fields. The independent audit proved no throw and refusal for null,
arrays, primitives, exotic instances, symbol fields, non-enumerable fields,
accessors whose getter must not run, and proxies with hostile own-key or
prototype traps; an exact null-prototype record remains accepted. The shared
runtime evidence separately covers descriptor traps, subclasses, holes,
custom-iterator arrays, and cross-realm arrays.

Six lockfile-pinned source mutations were applied only in memory, without
writing the checkout:

| Mutant | Admitted or changed result | Committed discriminator |
| --- | --- | --- |
| Change the `Dac` domain tag | Both core identities change. | Exact `Dac` goldens fail. |
| Delete branch closure | A BOOTSTRAP core with a SUCCESSOR member parses. | Opposite-branch/unknown-member assertion fails. |
| Delete the time comparison | Equal issue and expiry times parse. | Strict-time assertion fails. |
| Widen the safe-decimal maximum | `9007199254740992` parses. | Overflow assertion fails. |
| Delete successor increment comparison | A `41 -> 43` pair parses. | Adjacency assertion fails. |
| Change the core path root | The transaction maps to a different path. | Exact path golden fails. |

The focused committed file passed independently at one file and five tests.
Together with the full suite, these mutants show that the dedicated domain
helper, branch closure, timing, decimal bound, generation increment, and exact
path relation are behavioral checks rather than undiscriminated machinery.
They do not cover the generic serializer's untagged digest, which is why the
blocking public route survives all committed evidence.

## Trust and proportionality boundary

Parsing and dedicated `Dac` hashing do not certify `producerDigest`, compare a caller-supplied
expected digest, consult a wall clock, read a broker, select a pointer, or grant
a capability. The SUCCESSOR fence digest is opaque here and the derivable fence
path is not a core member. The dependency direction remains acyclic:
`Dfr -> Dac -> Dgr`. Later BOOTSTRAP composition must recompute the reviewed
`BOOTSTRAP_INSTALL` `Dsc`; later SUCCESSOR composition must recompute stable-
promotion `Dsc` and bind independently authenticated predecessor/candidate,
broker, admission, and fence facts. A candidate, worker, host assertion, core,
or this review cannot certify its producer. The generic serializer does not by
itself grant live authority, but exposing its untagged digest contradicts the
closed sole-identity contract and invites a later caller to treat that second
public result as an equality source. It must fail closed before this slice is
authorized.

| Retained mechanism | Concrete in-scope threat | Deletion result | Smallest credible shape |
| --- | --- | --- | --- |
| Direct closed two-branch parser | Unknown, opposite-mode, or cyclic future facts enter authority bytes. | The closure mutant admits a SUCCESSOR fact into BOOTSTRAP. | Retain literal flat lists; no schema language or nested proof bag. |
| Strict time and bounded adjacent generations | Reversed windows, unsafe integers, or skipped/wrapped broker generations alias authority evidence. | The timing, bound, and increment mutants each admit their bad boundary. | Retain direct scalar comparisons; no clock or broker abstraction. |
| One tagged canonical digest and one derived core path | Cross-domain bytes or redirected transaction placement substitute evidence. | The domain/path mutants change identity or placement, but the generic serializer still emits an untagged digest. | Retain one canonical frame part and the existing UUID-derived path; repair the serializer route. |
| Public dispatch, serialization, and branch registry | Canonical-byte parsing, serialization, or compatibility could bypass the closed union or its sole identity. | Dispatch and canonical-byte tests pass while the untested serializer returns a second digest. | Keep one schema version and two vocabulary projections; route this schema's serialized digest to `Dac`. |
| Explicit downstream deferral | Structural proof could be mistaken for live recovery or promotion authority. | Deleting the boundary invites copied equality and candidate self-certification. | Add no state, composition, broker, persistence, capability, or runtime surface. |

The parser and dedicated helper are proportional to the current closure,
canonical-identity, expiry, generation, path-substitution, and public-
compatibility threats. The bounded serializer repair is necessary and smaller
than a new digest registry or API. Neither the current implementation nor the
repair needs a sparse tree, inventory, coordinator, arbitrary-precision
ordinal, diagnostic, retention, or compaction mechanism.

## Verification evidence

The local `pnpm` launcher stalled before invoking even `pnpm --version` and was
interrupted after approximately 60 seconds without output. The first sandboxed
in-memory esbuild audit also failed before evaluation with `spawn EPERM`.
Following the approved fallback, every required gate used the identical
checked-in script or lockfile-pinned binary directly under read-only escalation.
This is an invocation fallback only; no test, source, dependency, or command
was substituted. All gates ran strictly one at a time, and the standalone full
suite fully exited before the separate bootstrap verifier began.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run` — PASS: 30 files and 367 tests in
   91.57 seconds. The process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests and registries,
   formatting, typecheck, 30 files and 367 tests in 79.89 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This `BLOCK_REPLAN` grants no implementation authority to exact head
`6b66d5c804706fb2cccebd79cabc803e804b3d6a`. After only the bounded serializer
repair and focused evidence above, the repaired exact head must return to the
ordinary independent review gate. Recovery-authorization lifecycle/state,
native consume or removal, post-selection observation/receipts, attachment,
archive, tombstone, composed equality, selected epoch, proposal, tip, CAS,
persistent lookup, credential-broker operations, live capability, command,
filesystem mutation, runtime promotion, and any later ledger remain
unauthorized and fail-closed.

This record grants no merge, release, installation, external mutation,
promotion, or self-certification authority. Stable release N may build and test
N+1, but only external review and the stable promotion path may grant N+1
authority; a worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-125.md`; no source, test, draft, issue,
roadmap, board, or other planning file was edited, and no commit was created.
