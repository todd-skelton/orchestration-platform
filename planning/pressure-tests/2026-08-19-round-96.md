# Pressure test — round 96 anchor teardown removal review

**Disposition:** `PASS`

**Reviewed candidate head:** `f39bd5fde10d6a3afb5d4a1ed46227237c1e44a5`

**Reviewed parent boundary:** `029212ee55078687585df1f57e068e2b0fe6735b`

This was a fresh independent local removal review of the exact initially clean
head above on `iss-002-versioned-contracts`. It is bounded to removal of
`OP-R95-001` and preservation of the `OP-R94-001` through `OP-R94-003`
closures inside bootstrap anchor teardown composition. It authorizes no merge,
bootstrap, anchor creation, E0, consumption, owner teardown archive, anchor or
owner `RETIRED` CAS, installation, release, promotion, runtime mutation, live
capability, receipt production, self-certification, or other authority.

The review read `AGENTS.md`, `docs/planning/work-model.md`, the decision
authority in `docs/architecture/contract-decisions.md`, the incorporated exact
external-bootstrap ledger and final order/equality matrix in
`docs/architecture/supervisor-contract.md`,
`docs/contracts/compatibility.md`, ISS-002, rounds 94 and 95, and the exact
parent-to-candidate delta. No Chase Sets repository, project, issue, board,
backlog, or consumer system was accessed. The board gate read only this
repository and its registered platform delivery project.

## Removal result

`OP-R95-001` is removed by moving the owner-to-anchor
`successorReviewCoreDigest` equality into the existing
`lifecycle === "ACTIVE"` branch. This is the exact normative lifetime of the
equality:

- ACTIVE genesis has null on both anchor and owner;
- ACTIVE successor has the same non-null `Dsrc` on both;
- after `CONSUME`, the successor anchor permanently retains `Dsrc`, while the
  exhaustive owner matrix requires the CONSUMED owner review member to be
  null.

The focused committed acceptance test and an independent in-memory
reconstruction both accepted all four valid combinations with `[]`: ACTIVE
genesis, ACTIVE successor, genesis-origin CONSUMED, and successor-origin
CONSUMED. The last case is the exact round-95 negative control and now passes
without weakening either ACTIVE arm.

The round-94 closures remain intact. A fully rehashed foreign anchor triple in
a selected CONSUMED owner refused all three direct owner-to-anchor equalities.
Fully null, rehashed CONSUMED predecessor triples refused independently for the
anchor and owner proposals. Genesis transition, source, complete predecessor,
ordinal, grant evidence, and foreign review core refused. Successor transition,
source, null predecessor, review evidence, and coordinated foreign review core
refused. A coordinated replacement of locator helper/custody, absence
helper/custody/state root, `Dobs`, `Dabs`, archive, receipt, and private
expectations refused the direct anchor-context equalities.

The successful independent probe therefore executed the same 15 individual or
coordinated semantic mutants as the round-95 removal review. It also replaced
each of the composition's 14 inputs in turn with null and a throwing proxy. All
28 malformed calls refused without throwing. The committed malformed/future
record coverage and full suite remained total.

Inspection found no acceptance widening and no new or changed schema, member,
enum, canonical path, digest domain or part order, export, registry row,
parser, receipt, capability, migration, coordinator, inventory, or mutation
authority. The repair changes only the lifecycle scope of one existing
comparison and adds one focused acceptance test inside the approved teardown
slice.

## Exact delta and proportionality

The exact `029212e..f39bd5f` delta has three paths, 188 insertions, and two
deletions:

- `packages/contracts/src/teardown.ts`: two insertions and two deletions,
  moving the existing review-core equality into the ACTIVE branch;
- `test/contracts/anchor-teardown.test.ts`: four insertions for the
  successor-origin CONSUMED acceptance test; and
- `planning/pressure-tests/2026-08-19-round-95.md`: 182 insertions preserving
  the prior independent blocker and remedy authority.

`git diff --check 029212e..f39bd5f` passes. No architecture decision, planning
topology, wrapper, registry, exported schema, canonical path, runtime service,
adapter, consumer, E0 or consumption implementation, owner teardown archive,
RETIRED CAS implementation, or unrelated contract family changed.

| Retained or removed mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| ACTIVE-only owner-to-anchor review-core equality | A genesis owner carries a foreign successor core, or a successor owner carries a core other than the anchor's exact `Dsrc`. | Genesis and successor foreign-core reconstructions refuse while both valid ACTIVE arms accept. | Retain the one direct equality only inside the existing ACTIVE branch; no lifecycle record or review receipt is added. |
| No CONSUMED owner-to-anchor review-core equality | An ACTIVE invariant applied after CONSUME rejects the normative successor lifecycle because the anchor retains `Dsrc` and the owner clears it. | The exact successor-origin CONSUMED fixture failed at the parent and returns `[]` at this head. | Move the comparison; changing owner or anchor schema/nullability would be larger and would contradict the exhaustive matrix. |
| CONSUMED owner-to-anchor triple equality | A selected owner embeds another anchor selection while retiring the named anchor. | The fully rehashed foreign triple refuses all three direct mismatches. | Retain the three existing comparisons; no receipt or history walk is needed. |
| Non-null CONSUMED predecessors | A hash-consistent CONSUME selection asserts genesis instead of selecting prior ACTIVE state. | Fully null, rehashed anchor and owner predecessor reconstructions each refuse. | Retain one all-non-null branch check per existing proposal. |
| Exact ACTIVE genesis/successor branches | A malformed genesis or foreign successor review authorizes unused retirement. | Transition, source, predecessor, ordinal, evidence, and review-core mutants all refuse. | Retain the direct branch checks already present; no new selector or policy is warranted. |
| Anchor-to-absence context equality | Absence of another state root under another helper/custody retires this anchor. | The fully rehashed coordinated substitution refuses custody, helper, and state-root mismatches. | Retain the three direct comparisons; no lock record or coordinator is needed. |
| Lifecycle archive and teardown receipt | Crash-resumable retirement preserves the prior anchor selection and same-lock absence without a digest cycle. | Deleting the archive loses prior lifecycle/absence preservation; reversing the edge recreates the forbidden future-digest cycle. | Retain the existing two acyclic records unchanged. |

## Verification evidence

The mandated gates ran in strict sequence. The standalone full test exited
before the separate bootstrap verifier started; neither overlapped.

1. Planning check — PASS, `planning contracts verified`, through the exact
   direct repository script `node scripts/planning/check.mjs`.
2. Board check — PASS, `board contracts verified`. Its sandboxed direct run
   failed closed at the read-only GitHub CLI subprocess with `spawn EPERM`; the
   identical approved read-only rerun passed.
3. Format check — PASS, all matched files use Prettier style. Its sandboxed
   direct run failed closed at the pinned Prettier subprocess with
   `spawn EPERM`; the identical approved read-only rerun passed.
4. Typecheck — PASS with the pinned local TypeScript compiler.
5. Standalone test position — PASS, 27 files and 299 tests in 88.77 seconds.
   Its sandboxed pinned Vitest invocation failed closed during configuration at
   `spawn EPERM`; the identical approved read-only rerun passed.
6. Separate bootstrap verification — PASS: manifests and registries,
   formatting, typecheck, 27 files/299 tests in 84.96 seconds, private
   compositions, planning contracts, and source-tree status invariance. Its
   sandboxed direct invocation failed closed at its read-only Git subprocess
   with `spawn EPERM`; the identical approved direct rerun passed.
7. Focused contracts — PASS, 17 files and 102 tests. Its sandboxed capability
   slot hit the same configuration-time `spawn EPERM`; the identical approved
   read-only rerun passed.
8. Focused compatibility — PASS, one file and one test. Its sandboxed
   capability slot hit the same configuration-time `spawn EPERM`; the
   identical approved read-only rerun passed.

The independent semantic probe was bundled and executed entirely in memory
from the exact checked-out source. Its first setup omitted re-exporting three
fixture digest helpers and stopped before the complete matrix result; the
corrected unchanged read-only probe then passed four valid lifecycle cases,
all 15 reconstructed mutants, and all 28 totality substitutions. No probe
file, implementation edit, test edit, isolated failing-test retry, or source
mutation was used.

Immediately before this report, the branch still resolved to the full
candidate SHA above, its parent still resolved to the full boundary SHA above,
the source worktree was clean, and the exact candidate diff passed
`git diff --check`. This review adds only this uncommitted round-96 record.

## Final status

`PASS` for exact candidate head
`f39bd5fde10d6a3afb5d4a1ed46227237c1e44a5`. The latest removal round is clean:
the review-core equality is ACTIVE-only, all four valid lifecycle origins
accept, the `OP-R94-001` through `OP-R94-003` closures and totality remain
intact, and the patch adds no schema/path/digest/authority surface. This PASS
is confined to bootstrap anchor teardown composition. It grants no E0,
consumption, owner teardown archive, anchor or owner RETIRED CAS, or any other
authority.
