# ISS-002 pressure test — round 121

## Review basis

This is a fresh independent cold removal review of exact initially clean head
`bf807383302e11b211a91b03d70d47de110e1dd4` on
`iss-002-versioned-contracts`, with exact parent
`04df11850e8ba1a73f847a419d310e324a0c1ee9`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
implementation authority in `docs/architecture/contract-decisions.md`, the
registered `ISS-002` draft, rounds 119–120 as the findings under retest, the
affected cleanup-history source and focused tests, and the exact parent-to-head
diff. No Chase Sets repository, project, issue, board, backlog, consumer source,
or other consumer system was read or touched.

The exact diff changes only `test/contracts/gate-fence.test.ts`: 21 insertions,
with no deletion and no production, schema, registry, API, runtime, planning,
roadmap, board, or authority-document change. `git diff --check` passed. The
checkout was clean before this pressure record was written.

## Disposition

`PASS` for the bounded structural cleanup-gate/recovery-fence implementation
gate authorized by round 118 and repaired through rounds 119–120.

The two Round 120 evidence blockers are removed by exactly two focused cleanup
history cases in one test. The first supplies a correctly rooted, dense,
correctly relinked two-head cleanup history whose ordinal-one time equals its
ordinal-zero predecessor and the immutable root time; it is accepted. The
second supplies an otherwise valid ordinal-zero, null-prior cleanup head in the
admitted non-genesis `COMPLETE/NOT_PUBLISHED` cell and receives exactly
`0:transition:not-genesis`. Neither case adds or implies a new contract,
authority source, selected-tail claim, locator, composition rule, or runtime
behavior.

## Round 120 blocker removal

### Inclusive cleanup predecessor time

The committed positive takes a valid two-record cleanup history, changes the
second `recordedAt` to equal the first, and re-links the history through the
common cleanup-head `Dv`. Consequently ordinal, prior digest, root, root-relative
time, and lifecycle edge remain valid; the inclusive predecessor-time boundary
is the only property under test. Exact HEAD accepts the history with no issues.

An in-memory Vitest transform changed only the first of the two source
predecessor-time comparisons—cleanup, while leaving the later fence comparison
untouched—from `<` to `<=`. The focused file then failed exactly the new test:
the equal-time assertion received `1:recordedAt:before-prior` instead of `[]`;
the other twelve tests passed. This kills the Round 120 strict-time mutant.

### Ordinal-zero cleanup genesis cell

The committed negative begins with the ordinary valid ordinal-zero fixture and
changes only its lifecycle from `PENDING` to `COMPLETE`, retaining
`NOT_PUBLISHED`, ordinal `0`, null prior, correct root, and `recordedAt` equal to
root `createdAt`. `COMPLETE/NOT_PUBLISHED` is an admitted cleanup cell, so
parsing and every continuity seam remain valid. Exact HEAD returns only
`0:transition:not-genesis`.

A separate in-memory Vitest transform found the single cleanup genesis-cell
condition and deleted its effect without changing any other comparison. The
focused file then failed exactly the new test: the wrong-genesis assertion
received `[]` instead of `0:transition:not-genesis`; the other twelve tests
passed. This kills the Round 120 deletion mutant rather than relying on the
previous confounded sliced-start case.

Both transforms existed only in the test runner's in-memory source transform.
They wrote no repository file. The unmodified focused baseline passed 1 file
and 13 tests, and the worktree remained clean after both probes.

## Structural, authority, and scope assessment

The parent-to-head delta contains no implementation and therefore cannot widen
the four structural parsers, two root digests, exact VALUE-position handling,
common supplied-head `Dv`, or bounded relative history walkers that round 118
authorized. It adds no durable common-CAS locator, selected-tail completeness
or currentness, first-ever archive lineage, cleanup archive, recovery
authorization, upstream composition, persistence read or mutation, tombstone,
broker call, capability, release selection, promotion, runtime transition, or
self-certification.

The equal-time case preserves the architecture's inclusive
`recordedAt >= prior recordedAt` rule. It does not establish a composed fence-to-
gate time relation. The wrong-genesis case preserves the closed cleanup graph's
existing initial cell; it does not authorize producing that head or selecting
it from persistence. Both inputs remain caller-supplied structural evidence,
and a valid shorter prefix remains only a valid earlier supplied history state.

The engine/adapter boundary is unchanged: the test adds no Git, GitHub,
filesystem, consumer, or host vocabulary to a contract. The stable-release
trust rule is also unchanged. No candidate verdict, structural validator
success, or test outcome is routed into the candidate's own promotion.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible disposition |
| --- | --- | --- | --- |
| Equal cleanup predecessor-time positive | A strict `<`/`<=` transcription error rejects the ledger's inclusive boundary. | The in-memory `<=` mutant fails only the new equal-time assertion. | Retain the one two-head positive; no clock abstraction or composed timing surface is needed. |
| Isolated ordinal-zero genesis negative | Deleting the cleanup genesis-cell comparison admits a structurally continuous history beginning in a later admitted cell. | The in-memory deleted-condition mutant accepts the new wrong-genesis history and fails only its exact assertion. | Retain the one otherwise-valid negative; no exhaustive duplicate start matrix is needed. |
| Existing bounded relative validator | Fork, gap, reorder, wrong root/prior/time, illegal edge, or invalid start can fake relative structural progress. | Rounds 119–120 and the two probes above now give each retained comparison discriminating committed evidence. | Retain the flat bounded walk; no tree, inventory, proof bundle, locator, or completeness mechanism is justified. |
| Deferred persistence and composition | Durable currentness and upstream authority require authenticated inputs absent from this slice. | Adding such behavior would trust caller expectations or invent undecided bytes. | Preserve the explicit deferral to separately reviewed ledgers and locator decisions. |

The repair is test-only and is the smallest response to both Round 120 findings.
It adds no mechanism to defend against an excluded threat.

## Verification evidence

The required gates ran through their checked-in wrappers strictly sequentially.
The standalone test process fully exited before the separate bootstrap verifier
started. No wrapper fallback, direct replacement command, or overlapping test
process was used.

1. `pnpm run planning:check` — PASS: `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS: `board contracts verified`.
3. `pnpm run format:check` — PASS: all matched files use Prettier style.
4. `pnpm run typecheck` — PASS: `tsc --noEmit -p tsconfig.json`.
5. Standalone `pnpm test` — PASS: 29 files and 362 tests in 189.30 seconds.
   The process fully exited before step 6 began.
6. Separate `pnpm run verify:bootstrap` — PASS: manifests and registries,
   formatting, typecheck, 29 files and 362 tests in 136.32 seconds, private
   compositions, planning contracts, and source-tree status invariance.

Before the required sequence, the focused exact-HEAD run
`pnpm exec vitest run test/contracts/gate-fence.test.ts` passed 1 file and 13
tests. The two independent in-memory mutation runs then each failed that file
with exactly one failed new test and twelve passing tests, as detailed above.

## Authorization boundary

`PASS` closes only the Round 120 test-evidence blockers and clears exact head
`bf807383302e11b211a91b03d70d47de110e1dd4` for the bounded structural
cleanup-gate/recovery-fence implementation gate already authorized by round
118. It does not authorize any next ledger or implementation slice.

This record grants no merge, release, promotion, installation, persistent
lookup, selected-tail completeness/currentness, first-ever archive fact,
composed validation, filesystem mutation, tombstone/archive construction,
broker call, live capability, runtime transition, or self-certification
authority. A worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-121.md`; no implementation, test,
draft, issue, roadmap, board, or other planning file was edited, and no commit
was created.
