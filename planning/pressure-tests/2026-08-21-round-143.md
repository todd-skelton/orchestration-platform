# ISS-002 pressure test — round 143

## Review basis

This is a fresh independent removal review of the repaired common-CAS selected-
object locator evidence at exact initially clean and pushed head
`2202981adf081cec0b1de08f7725544e81e982f6` on
`iss-002-versioned-contracts`, with exact parent `6bc2e9d`. Local `HEAD` and the
read-only `ls-remote` result for `origin/iss-002-versioned-contracts` both
resolved to that exact commit before review.

The authority basis was the current `AGENTS.md`, the sole implementation
authority in `docs/architecture/contract-decisions.md`, the registered
`planning/drafts/ISS-002.md`, round 142, and the exact `6bc2e9d..2202981`
delta. Existing selected-object production code and closed-snapshot helpers
were inspected only to run exact source-deletion attacks against the repaired
evidence. No Chase Sets repository, project, issue, board, backlog, source, or
other consumer system was read or touched.

The exact commit changes only
`test/contracts/selected-object-locator.test.ts`: 20 insertions and no
deletions. It changes no source, schema, serializer, registry, package,
dependency, command, runtime, authority document, draft, roadmap, or board
topology. `git diff --check 6bc2e9d..2202981` passed.

## Disposition

`PASS` for the repaired selected-object locator evidence at exact reviewed head
`2202981adf081cec0b1de08f7725544e81e982f6`.

All three round-142 findings are removed by the prescribed test-only repair.
The combined tip identity equality and each of its two members are independently
deletion-discriminated. Every public digest argument of every selected alias
constructor now has a malformed negative, and each newly covered validation
call is independently deletion-discriminated. Hostile nested expected identity,
position evidence, proposal, tip, and value inputs all refuse without throwing.

No semantic or scope blocker was found. The production implementation remains
the reviewed three pure alias constructors plus one total pure one-current-node
validator. This PASS adds no currentness, persistence, history completeness,
CAS, runtime, or promotion authority.

## In-memory mutation method

Source-deletion mutants were applied only by a per-process, pre-transform Vite
plugin passed programmatically to Vitest. Each transform matched one exact
`packages/contracts/src/pointer.ts` anchor, asserted exactly one replacement,
ran only the focused selected-object test, and disappeared when that Node
process exited. No repository source or test file, generated file, temporary
configuration, dependency, cache registration, or worktree was edited.

Each mutant ran in its own process against an otherwise unmodified source
baseline. A mutant counted as killed only when the focused assertion failed;
transform-anchor errors did not count. The unmutated focused suite was rerun
after all mutants and passed.

## Round-142 removal verification

### OP-R142-1 — combined and member-specific tip identity deletions are killed

The repair adds two independent valid closed-tip substitutions:

```text
tip.pointerKind = ACTIVATION_CLEANUP_GATE
tip.pathInstanceDigest = <different lowercase SHA-256>
```

All other supplied records and graph members remain the valid baseline. The
alternate kind is registered, so the tip parser admits it; the alternate `Dp`
is valid SHA-256, so digest grammar does not confound the identity equality.

Three isolated in-memory source mutants were run:

1. delete the complete combined `tip:identity-mismatch` branch;
2. replace only `tip.pointerKind !== trustedKind` with false; and
3. replace only `tip.pathInstanceDigest !== recomputedDp` with false.

Each mutant caused exactly the selected-graph substitution test to fail because
one newly added input returned `{ ok:true }`. The combined deletion therefore
cannot hide behind either remaining member, and each member remains
discriminating if the implementation later splits the combined branch.

### OP-R142-2 — every constructor argument is malformed-discriminated

The completed focused matrix is:

| Constructor | malformed `Dp` | malformed object digest |
| --- | --- | --- |
| selected value | yes | yes (`Dv`) |
| selected proposal | yes | yes (`Dr`) |
| selected tip | yes | yes (`Dt`) |

The three newly added cells were attacked with isolated in-memory source
mutants that replaced only the named `storageDigest(...)` call with the raw
argument:

1. selected-value `Dv` validation deletion;
2. selected-proposal `Dp` validation deletion; and
3. selected-tip `Dp` validation deletion.

Each mutant failed at its exact new `toThrow()` assertion. The other
constructor validations remained intact, so no stale sibling refusal could
confound the result. Together with the pre-existing three cells, all six public
path inputs now refuse malformed or uppercase digest text.

The valid baseline continues to pin all three exact paths, `Dp`, object class,
object digest, `.json` suffix, and class separation:

```text
installation/pointer-cas/<Dp>/objects/values/<Dv>.json
installation/pointer-cas/<Dp>/objects/proposals/<Dr>.json
installation/pointer-cas/<Dp>/objects/tips/<Dt>.json
```

### OP-R142-3 — hostile nested slots are directly total

The repair places the same throwing `ownKeys` proxy independently in:

1. `expectedIdentity`;
2. `expectedIdentity.positionEvidence`;
3. `proposal`;
4. `tip`; and
5. `value`.

For every placement, the public validator is invoked twice: `not.toThrow()`
proves totality and the second call must return `{ ok:false }`. The unmutated
focused test passes all five iterations.

The required first operation is the outer exact
`snapshotClosedRecord(expectedIdentity, proposal, tip, value)` composition.
That helper recursively snapshots descriptor values and catches reflective
traps, so each hostile nested input is correctly rejected before semantic
reads. A downstream locator-catch deletion is not a valid isolated totality
mutant for these cases because the required deep outer snapshot still removes
the hostile value upstream; totality has no obligation to depend on one
particular redundant catch. The observable public boundary is now explicit for
every nested slot without adding another snapshot layer.

## Production semantic reconfirmation

The unchanged production implementation remains aligned with the reviewed
ledger:

- the input is the exact closed four-slot composition of trusted expected
  identity, proposal, tip, and value;
- the expected identity has the existing exact eight-member census;
- `Dp` is recomputed from trusted identity and registered path rules;
- proposal kind and recomputed trusted position are equal-bound;
- `Dv` is recomputed under the trusted kind, not proposal vocabulary;
- proposal `Dp`/successor `Dv` and tip kind/`Dp`/`Dv`/`Dr` are all equal-bound;
- `Dr` and `Dt` use the existing framed proposal and tip digests;
- success returns exact `Dp/Dv/Dr/Dt`, detached records, and the exact all-null
  or complete prior `Dt/Dv/Dr` tuple;
- partial prior tuples refuse through the closed proposal parser; and
- generic structural success does not parse or grant family value semantics.

The focused baseline still proves that a hash-consistent open active-release
value may pass generic location while `parseActiveReleaseValue` refuses it.
Family composition therefore remains the required semantic authority boundary.

No schema or serializer was added. The pointer module's existing public export
is sufficient; schema versions, registry rows, canonical serializer routes, and
the exact eleven-kind/FULL_REQUIRED census are unchanged. The focused pointer
suite and ISS-002 compatibility slot remain green.

## Scope and trust-model attack

The selected aliases remain immutable non-authoritative content paths. Their
presence, even as one coherent losing candidate graph, does not prove the
canonical current tip selected them. The generic validator receives records
only and returns structural facts only.

It performs no filesystem read/write, canonical persisted-byte comparison,
directory enumeration, alias create/read-back, recursive prior walk, cycle or
terminal decision, current-tip acquisition, family root/archive lookup,
proposal, CAS, conflict mutation, capability issuance, broker call, deletion,
compaction, runtime mutation, review, release admission, or promotion.

ISS-004 still owns canonical byte reads, iterative missing/fork/reorder/cycle/
false-terminal history composition, family terminal authorization, and stable
external CAS/currentness. A candidate cannot route this structural validator's
result into its own certification or promotion.

## Proportionality and deletion test

| Mechanism | Concrete in-scope threat | Exact-head evidence | Smallest disposition |
| --- | --- | --- | --- |
| Three class-separated alias constructors | Selected digests cannot reconstruct mutation-addressed paths. | Exact paths pass; all six constructor arguments have malformed negatives; three newly missing validations are independently mutant-killed. | Retain unchanged; no index, head, schema, or mutation ID. |
| Trusted identity and proposal family/position bindings | Cross-installation/project/root/transaction/source/path/kind/position replay. | Existing scalar and coordinated graph negatives remain green. | Retain unchanged. |
| Tip kind and `Dp` equalities | A valid but foreign tip is accepted under the trusted identity. | Combined, kind-only, and `Dp`-only deletions are independently killed. | Retain unchanged. |
| One-node `Dv/Dr/Dt` graph | Supplied value/proposal/tip members disagree under recomputed hashes. | Focused substitutions and exact returned tuple pass. | Retain unchanged. |
| Complete-or-null prior tuple | Partial ancestry is treated as genesis or usable history. | Complete, null, and partial cases pass. | Retain unchanged; no history input. |
| Deep closed snapshot | Reflective nested evidence invokes user code or throws. | Five nested hostile placements explicitly return refusal without throwing. | Retain the existing outer snapshot; add no layer. |
| Generic/family parser split | Hash-consistent open value becomes family authority. | Generic success plus family-parser refusal remains explicit. | Retain unchanged. |
| No runtime/currentness surface | Structural content becomes selection or self-certification. | Test-only delta and unchanged production API add no such surface. | Retain exact scope. |

The repair adds only the cheapest discriminating evidence named by round 142.
No new mechanism was introduced.

## Verification evidence

Every command ran strictly one at a time. All mutation processes fully exited
before the unmutated focused suite. Focused and compatibility processes fully
exited before the standalone full suite. The standalone full-suite process
fully exited before the separate bootstrap verifier began. Direct checked-in
scripts or lockfile-pinned binaries avoided the known signed package-manager
launcher infrastructure dependency; no semantic gate was substituted. Test
processes used the increased 600,000 ms test and hook ceilings.

Mutation evidence:

1. Combined tip identity branch deletion — KILLED.
2. Tip-kind-only equality deletion — KILLED.
3. Tip-`Dp`-only equality deletion — KILLED.
4. Selected-value `Dv` validation deletion — KILLED at the new value-digest
   assertion.
5. Selected-proposal `Dp` validation deletion — KILLED at the new proposal-`Dp`
   assertion.
6. Selected-tip `Dp` validation deletion — KILLED at the new tip-`Dp`
   assertion.
7. Unmutated hostile nested baseline — PASS for expected identity, position,
   proposal, tip, and value, each with nonthrow plus false-result assertions.

Focused and required gates:

1. Focused locator and pointer suites,
   `node node_modules/vitest/vitest.mjs run
   test/contracts/selected-object-locator.test.ts
   test/contracts/pointer.test.ts --testTimeout=600000
   --hookTimeout=600000` — PASS: 2 files and 15 tests in 2.51 seconds.
2. ISS-002 compatibility slot,
   `node test/capability-slots/ISS-002/contracts%3Acompatibility-check.mjs
   --testTimeout=600000 --hookTimeout=600000` — PASS: 1 file and 1 test in
   1.25 seconds.
3. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
4. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`;
   it read only this repository and its registered delivery project.
5. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
6. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
7. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 35 files and 394 tests in 105.24 seconds. The
   process fully exited before step 8.
8. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 35 files and 394 tests in 87.41 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This clean implementation removal round clears exact head
`2202981adf081cec0b1de08f7725544e81e982f6` for the bounded selected-object
locator slice described by the current ISS-002 authority. Any production,
schema, persistence, history, currentness, CAS, capability, runtime, or trust-
model widening requires a new reviewed packet.

After this report is written, the sole intended worktree change is
`planning/pressure-tests/2026-08-21-round-143.md`; no source, test, authority
document, draft, issue, roadmap, board, or other planning file is edited, and
no commit is created.
