# Pressure test — round 22

**Disposition:** `BLOCK_REPLAN`

The twenty-second independent review found three serialization/lifetime defects.
The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R22-001` — An immutable transition record was said to contain all pointer fields, including its own digest. | Split shared authority/prior-pointer fields from pointer-only new-record path/digest fields; only the subsequently built current pointer contains the new record digest. |
| `OP-R22-002` — Recovery retries were unlimited but consumed forward-recovery authority could expire. | Expiry now applies only before consumption; exact `CONSUMED_BOUND` authority must precede the irreversible boundary and cannot expire before broker-internal terminal revocation. |
| `OP-R22-003` — The protection receipt attempted to contain the enclosing artifact ID/digest assigned only after upload. | Receipt now binds producer run/attempt and intended name only; protected environment values independently carry finalized artifact ID/archive digest plus receipt/anchor hashes, all verified on retrieval. |

## Re-review focus

- Confirm no record directly or indirectly requires its own digest as input.
- Let a post-activation recovery exceed the former expiry and verify it remains
  recoverable but confined to the same transaction.
- Substitute receipt, archive, numeric artifact ID, producer run, intended name,
  and protected variables independently and in combination.
