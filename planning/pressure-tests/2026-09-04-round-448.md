# Pressure round 448 — source-owned worker-result review disposition

Status: ISS-011 disposition source candidate, disposition-source attempt 1.
The exact implementation base is
`d03c007f59ff8be51a3ece54845cf75ccf261df5`, which combines independently
reviewed Round 446 definition source `6f144ef` with then-current main
`ebde782`. This candidate claims no self-review, hosted execution, registry
admission or authority.

## Lineage and bounded scope

Rounds 401 and 434 through 446 are mandatory cumulative inputs. Round 401 owns
the optional disposition ABI and closed disposition/follow-up literals. Rounds
435 and 437 remain abandoned planning-source candidates, including both Round
435 exact heads, both Round 437 exact heads, their independent reviews, and
Round 437's three-OS hosted failure. Round 438 replanned to exact-byte
admission, and Round 439's quarantined planning source and normalized pin stay
unchanged. Round 443's three definition attempts, Round 444's reviewed
plan-only source attempt, and Round 446's two definition attempts are retained;
this disposition increment neither repairs nor resets any of them. Unrelated
ISS-054 Rounds 440 through 442 remain chronological records, and neither they
nor renamed branches create a fresh attempt ledger.

This packet changes exactly the neutral review entrypoint, focused production
planning and disposition consumers, and this record. The module's descriptor
now declares exactly `review.complete`, `review.reject`, and `review.unknown`
and its namespace is exactly `descriptor`, `disposition`, and `plan`. Planning
selection, schemas, compatibility, action, catalog, role, target and result
shapes remain unchanged.

The new pure disposition parses the closed `DispositionInput`, requires
canonical equality with the exported descriptor, and admits only the complete
`worker-result-subject/v1` target. Accepted authority produces
`review.complete/COMPLETE`; rejected produces `review.reject` and a
target-preserving `FOLLOW_UP/REPLAN` for module `review`; absent or unknown
authority produces `review.unknown/UNKNOWN/AUTHORITY_UNPROVEN`. Every result
binds the complete action-plan, disposition-input and worker-result-subject
digests and declares `WORKER_RESULT`.

## Consumer evidence and identities

The existing branch/queue loader, snapshot, policy and composition seam still
prepares the production review plan. Its normalized source pin is now
`22e45e94d5359c9d1aed4d34c18800ec4c72684ef784bd652c8a86bb443148a4`.
The descriptor digest is
`c62d5a69b1b715bdc7d08793135a74bbaa0157a2f020c621de8edc07fd85dc9a`.
The unchanged branch and queue subject/footprint identities are retained while
the descriptor-derived identities are rebaselined:

| Adapter  | Core                                                               | Brief                                                              | Zero-input plan projection                                         |
| -------- | ------------------------------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------------------------ |
| branches | `0c4301647e5ee1f2d51f57335d07880e1c5331e282f4d18dfc82c054bcbed336` | `0c5d5e8a4eb98e1f6345eb7fda11ee99021e0848155e8cbe47612b5d1e76c886` | `83004d728016fa10cf3ce349521d408b3465d0109a774a5ae478f2c458eaf2b0` |
| queue    | `1740e77f78c7ed3f04b535466490292e7f8c950b4749462c71668b39b91543c9` | `f11d82f3f4f61cf6d47e1b0b71b81ec9fbf4514fb1726b5d0747622b893cbe53` | `ce76eb661f3fcb337dc9f629492740405836a0218a9b96bf22e85c0f18267d77` |

The disposition consumer never presents the fixture descriptor or fixture
action as production proof. Test-local composition begins with the actual
captured stdout/stderr and current review evidence, substitutes the production
descriptor and its real planned action, then rebuilds and validates route,
preflight, cycle-plan request, dispatch, launch, terminal, review request,
attempt and authority through the existing public contracts. The caller
retains the exact input and both raw streams across the disposition await and
supplies all four preimages to `validateActionDispositionBinding`.

Acceptance evidence covers accepted, rejected, absent and unknown authority;
the exact complete target and rejected follow-up; malformed input and
alternate/null/release-candidate targets; repeated canonical input equality;
changed action, route, preflight, dispatch, launch, terminal, review request,
evidence, attempt and authority records; disposition action/input/subject/code
substitutions; and wrong stdout/stderr bytes. Structurally parsed inputs and
outputs still fail their external relations, and neither the plan result nor a
parser supplies permission. The existing manifest and generator assertions
remain empty and unchanged.

## Non-goals, verification and footprint

There is no fixture-runtime source change, public-contract change,
manifest/generator/registry activation, dependency, provider/native access,
release-candidate disposition, findings policy beyond declared-code binding,
worker execution implementation, journal/reclaim integration, mutation,
delivery/repair semantics, architecture/draft amendment, or ISS-011 closure.
Accepted `COMPLETE` records finished nonmutating review work only; it cannot
certify a worker, candidate, reclaim or final cycle.

Author verification is limited to static `pnpm run typecheck`,
`pnpm run planning:check`, targeted Prettier checking of the exact four files,
`git diff --check`, normalized source hashing, exact four-file status and diff
inspection. No local Vitest, test, module, native, compiler, probe, provider or
lock experiment is run. Hosted focused/full Linux, macOS and Windows evidence
and independent exact-head review remain external gates. Routing followed row
4 multi-file integration on Sol/high; placement is provisional, budget intent
is at most $15 with no new ceiling, and actual spend is unknown.

Author checks completed: static typecheck, planning contract check, pinned
targeted Prettier check, normalized source hashing, diff check and exact
four-file inspection. The first sandboxed planning check stalled before its
banner; it was interrupted, and the approved escalated retry passed with
`planning contracts verified`. No signature check was disabled and no
prohibited local execution was used.

The exact footprint is:

- `modules/review/src/index.ts`
- `fixtures/walking-skeleton/test/consumer.test.ts`
- `fixtures/walking-skeleton/test/review-consumer.test.ts`
- `planning/pressure-tests/2026-09-04-round-448.md`

## Review attack surface

Attack reordered or partial disposition codes; stale Round 444 source,
descriptor, core, brief or plan identities; a plan or parsed record treated as
authority; fixture identities smuggled into the production tuple; accepted
review treated as self-certification; `COMPLETE` treated as cycle completion;
rejected review without the exact complete target and `REPLAN` intent; unknown
or absent authority converted into action; any partial target digest; a
structurally valid record that bypasses raw stream or upstream evidence
binding; copied or changed stdout/stderr across the await; missing action,
input, route, preflight, dispatch, launch, terminal, request, attempt,
authority, code or follow-up joins; runtime/manifest activation hidden outside
the footprint; and any reset or omission of the Round 401 and 434-through-446
lineage.
