# Pressure round 433 — first-consumer boundary completion

Status: bounded ISS-028 completion candidate. No live consumer access, shadow,
import, mutation, policy, credential, provider, or operator-authority action is
claimed.

## Frontier and scope

Protected base is `bc4cec8043ecbf71f69ca97f88c78f3ae37b1a83`. PR #247
landed the exact compatibility and extension ownership scaffold, but left three
acceptance claims open: a generic-schema-compatible empty configuration,
executable representative-patch collision evidence, and inspection of the
actual packed adapter plus platform artifact inputs.

This packet adds only a pure UUIDv7-gated constructor for the closed
`adapter-configuration/v1` record and test evidence for those three remaining
claims. The constructor returns data and grants no source, project, state,
mutation, or credential authority. The generic contracts parser remains the
schema authority. Existing shadow/import/mutation placeholders remain
byte-stable.

## Proportionality and removal evidence

| Mechanism | Concrete threat and deletion test | Smallest alternative |
| --- | --- | --- |
| Closed configuration constructor | a malformed project identity or widened adapter capability reaches a later generic parse; mutate each UUID component or output field | one data-only constructor using the public UUIDv7 grammar and landed constants; no loader, callback, schema, or dependency |
| Three clean-copy patch applications | later extension owners share the composition root or one another; make any representative change touch a second path | copy the four-file source census to disposable roots and apply one extension-local representative patch to each concurrently; no Git worktree or source mutation |
| Actual package inventory | undeclared, secret, local-path, or incumbent bytes enter the adapter tarball; add one file or forbidden byte | inspect one `pnpm pack` tarball against the exact manifest/source census; no install or registry publication |
| Platform input exclusion | a private platform composition embeds consumer-specific bytes; add a tracked import, path, package name, or adapter ID to a platform input | scan the tracked production composition inputs and the fixed private-composition target configuration; no bundler or release change |

## Acceptance and verification

The empty fixture must parse through the landed generic contract parser and
produce exact canonical bytes. UUID version, variant, type, and case mutants
must refuse before any record exists. Three representative patches must apply
concurrently to independent clean source copies and report exactly the three
registered extension paths, with no composition-root change. `pnpm pack` must
emit exactly `package.json` and the inspected source census; the tar bytes must
contain the same paths and no key, token, or local-user path. Tracked platform
composition inputs must contain no first-consumer package, path, or adapter ID.

Verification is typecheck, focused scaffold tests on hosted CI, all three
ISS-028 capability slots including the exact package-filter command,
format/planning/live-board/diff checks, independent exact-head review, and the
three-OS bootstrap. Local Windows does not run Vitest. Predicted footprint is
the existing adapter root, verifier declaration/test, one existing package
capability slot, and this record. A package-manifest edit, platform source edit,
new schema, consumer callback, or provider input is replan evidence.

Review should attack UUID grammar drift, a configuration result that bypasses
the generic parser, misleading empty facts, cleanup outside runner temp,
symlink or extra-file packing, tar/report disagreement, an incomplete platform
input census, sequential rather than independent patch evidence, placeholder
changes, and any inference that a clean empty fixture proves live compatibility.

## Exact-head review follow-up

The first independent pass found three bounded verifier gaps. Before spawning
`pnpm pack`, the repair now proves the exact sole package script and regular-file
source census and also sets package-manager script execution off. The platform
input detector denies every occurrence of the fixed `first-consumer` package,
path, or adapter-ID token, including relative imports. The sensitive-byte
detector adds fine-grained GitHub and npm token prefixes, narrows local user
paths so ordinary web `/home/` routes remain benign, and pins positive and
negative fixtures for every closed detector class. No production or placeholder
blob changed in this follow-up.
