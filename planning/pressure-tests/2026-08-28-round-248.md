# Test subprocess-timeout pressure review — round 248
`PASS`.
The exact diff is six test files and seven one-for-one `120_000` → `600_000` timeout replacements; no production, fixture, assertion, subprocess, cleanup, or expected-result byte changes.
All seven affected cases perform measured filesystem, bundle, child-process, or exhaustive redaction work, so the larger outer runner allowance addresses contention without converting a semantic refusal or failed assertion into PASS.
`600_000` is exactly the user-approved 600-second gate and matches existing long-running test-local ceilings; it does not alter runtime protocol deadlines.
Proportionality: seven local literals are the smallest repair—no global timeout, retry, skip, weakened assertion, or production timing change was introduced.
