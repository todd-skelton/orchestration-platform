# Pressure round 475 - actionable diagnostics from the first completed loop

ISS-072 corrective source attempt 3 of eight preserves source attempt 1 at
cc8dff79e6c8e8492ce9b23c1b1cd6abe0a2b736. The full PR delta remains based on
4802ecc0eeae3d149cde18a16eb514627b9d6511. No independent-review, hosted, or
package-gate result is claimed by this source record.

## Actual observed need and source plan

M0 completed via PR #320, but the worker's final four-key JSON could not carry a
short actionable finding into useful pilot terminal output. The trace retained
progress prose, so the operator had to recover the finding manually. The next
goal is successive useful cycles with fewer manual interventions; diagnostics
is the first observed repair. This does not claim production or self-promotion.

The expanded predicted full change is twelve allowed paths: five private dogfood
implementation/declaration paths, two focused tests, the supervised-loop note,
ISS-072's draft, the kernel epic, one M2 roadmap row, and this record. Add one
optional terminal summary with a 2,000-character bound, require a string (empty
when there are no findings) from newly constrained native outputs, retain legacy
four-key parser compatibility, and project valid diagnostics on terminal success
or named worker failure. Do not add a reporting framework or let prose affect
authority.

## Correction evidence and independent attack

Review attempt 2 remains isolated at the immutable source-attempt-1 head and does
not cover these corrections. Source attempt 3's direct installed TypeScript
check, Prettier check over the changed executable paths, and planning check each
returned success. It did not run local tests, native builds, probes, or the
candidate runner. Independent review attempt 4 attacks the corrected exact head,
including legacy parser compatibility, required new-output summary, direct
child-Node adapter resolution, malformed/non-string and overlong input,
misleading PASS/FAIL prose, unchanged exact-head/identity checks, and preservation
of `author-failed`/`reviewer-failed`. The controller alone handles package gates,
board mirroring, and hosted execution.

Falsifiable prediction: the next ordinary dogfood cycle exposes a role-attributed
actionable summary in pilot output without trace archaeology or any changed
authority outcome. Needing structured findings or still losing the diagnostic
falsifies this bounded repair and triggers evidence-led replanning.
