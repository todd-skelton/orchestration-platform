# ISS-002 pressure test — round 129

## Review basis

This is a fresh independent cold removal review of the bounded
recovery-authorization selected-state implementation at exact initially clean
and pushed head `94dca883812b0a78b72fc1a0f07fda9c26e2c67b` on
`iss-002-versioned-contracts`, with exact parent
`3289d1c`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, and the sole
implementation authority in `docs/architecture/contract-decisions.md`. The
review also read the registered `ISS-002` draft, rounds 127 and 128, the exact
parent-to-head diff, and the affected recovery, pointer, definition, public
index, registry, compatibility, and focused test surfaces. No Chase Sets
repository, project, issue, board, backlog, consumer source, or other consumer
system was read or touched.

The exact commit changes seven authorized files: 569 insertions and 24
deletions across `packages/contracts/src/{definitions,index,pointer,recovery,
registry}.ts` and focused recovery tests. It adds no package, command, runtime
module, state service, persistence implementation, planning change, dependency,
roadmap edge, or board topology. `git diff --check 3289d1c..94dca88` passed.
The checkout was clean before this pressure record was written.

## Disposition

`PASS` for the bounded recovery-authorization selected-state implementation at
exact reviewed head `94dca883812b0a78b72fc1a0f07fda9c26e2c67b`.

The implementation matches the sole authority: it provides only the exact
closed CREATED/CONSUMED/REVOKED parser, five structural cells, exact VALUE
position and common dispatch, four pure transitions, contextual common `Dv`,
generic serializer refusal, and removal/replacement of the obsolete REMOVED
lifecycle API. The committed evidence discriminates the state closure,
nullability, transition, position, serialization, and identity boundaries. No
fresh blocker or smaller bounded repair is required.

## Closed union and five structural cells

The exported branch censuses are unique, lexicographically ordered, and exact:

| Branch | Members | Structural fact |
| --- | ---: | --- |
| CREATED | 7 | Core, prebound consume operation, lifecycle, mode, time, schema, and transaction only. |
| CONSUMED | 9 | CREATED identity plus required gate root and native-consume receipt. |
| REVOKED | 12 | Preserved identity, nullable gate/consume evidence, required native-removal receipt and removal operation. |

The five valid cells are CREATED, CONSUMED, and the three REVOKED arms:

| REVOKED cell | Gate | Consume post receipt | Native consume receipt |
| --- | --- | --- | --- |
| R0 | null | null | null |
| R1 | digest | null | null |
| R2 | digest | digest | digest |

The parser snapshots the complete input before semantic reads, selects exactly
one branch census from the detached lifecycle, and validates the closed SHA-256,
UUIDv7, timestamp, mode, lifecycle, and schema literals. The REVOKED consume
pair is all-null or all-non-null, and its non-null arm requires a non-null gate.
R0, R1, and R2 parse structurally without inventing predecessor or currentness
authority.

The focused evidence removes, renames, nulls, and cross-types every member of
all five fixtures; standalone opposite-branch additions close CREATED and
CONSUMED, while the shared exact-census snapshot closes every REVOKED unknown
field. It separately attacks both partial consume pairs and the non-null pair
with null gate. Canonical-byte goldens pin all five cells, and reordered JSON
bytes refuse through the public canonical-byte parser. REMOVED and future or
cross-branch shapes refuse.

Hostile null, array, primitive, and throwing-proxy inputs refuse without
throwing through both the state parser and both validator positions. The shared
closed snapshot also rejects accessors, symbols, non-enumerables, exotic
prototypes, subclasses, cross-realm/proxy values, cycles, and other malformed
JSON shapes before state semantics can read them.

## Pure transitions and relative evidence

The public transition surface is now the one parsed-value validator exported as
`validateRecoveryAuthorizationTransition`. Its graph is exactly:

```text
null -> CREATED
CREATED -> CONSUMED
CREATED -> REVOKED
CONSUMED -> REVOKED
```

Every non-genesis edge shares the same comparisons for
`authorizationCoreDigest`, `consumeOperationId`, `mode`, and `transactionId`.
Recorded time is inclusive: equality succeeds and reversal refuses. Both R0 and
R1 are valid only directly after CREATED; R2 after CREATED refuses because the
consume pair is unexpected. After CONSUMED, structural parsing plus exact gate
and native-consume equality admits only R2; R0 and R1 cannot equal the prior
non-null native-consume evidence. R2 gate and native-consume substitutions
refuse. Self-loops, second genesis, skips, reverse edges, and REMOVED refuse.

The shared edge gate and shared preserved-field/time loop make each committed
negative deletion-discriminating: removing a graph edge restriction, a
preserved equality, or the inclusive ordering check admits a focused mutant.
The cell-specific direct-revoke and consumed-revoke checks remain structural
only. Coordinating a different otherwise valid history in both values is
intentionally outside this relative validator and must be authenticated only by
the later selected composition matrix.

## Exact position and contextual identity

The state position parser admits only the detached closed value
`{"mode":"VALUE","parts":{}}`. Missing or extra outer members, missing or
nonempty nested `parts`, TOMBSTONE, and another mode refuse. The common
`computePointerPositionDigest` dispatch invokes that exact parser for only
`RECOVERY_AUTHORIZATION_STATE`; deleting the state branch would make the
committed nonempty-parts common-dispatch negative survive. The pinned
`authorization-state-position/v1` digest is
`4cf9a17dc945568b410d1d23ffef243971cce087b0d87a7d7a97253eb352ec4c`.

The canonical pointer path remains the pre-existing exact
`installation/recovery-authorizations/<transactionId>/state.json`. The common
pointer-instance computation supplies authenticated path context `Dp`; the
state implementation adds no alternate path or state identity. All five common
`Dv` values are pinned under the existing `pointer-value/v1` formula, and
changing only supplied `Dp` changes `Dv`. Position is not folded into a new
state digest, and no standalone `computeRecoveryAuthorizationStateDigest`
surface exists.

For a valid detached state, generic
`serializeContract("recovery-authorization-state/v1", value)` returns exactly
`{ok:false, issues:["serialization:pointer-context-required"]}`. The branch is
after total parsing and returns neither bytes nor digest, so invalid values
still return parser findings and valid values cannot fall through to the
generic untagged canonical digest. Detached canonical bytes remain available
through the existing byte primitive and parse only when exact; authority
identity remains only contextual common `Dv`.

## Public lifecycle, registry, and compatibility

`RecoveryAuthorizationLifecycle` and `recoveryAuthorizationLifecycles` now
contain exactly CREATED, CONSUMED, and REVOKED. The definitions-module
string-only validator and its `REVOKED -> REMOVED` edge are deleted. Wildcard
exports expose only the recovery-module parsed validator under the established
name; the API-surface test proves there is one such export and no standalone
state-digest helper.

The recovery dispatch registers `recovery-authorization-state/v1`, the public
schema-version registry contains that exact v1, and the vocabulary registry
contains the exact field census for each lifecycle branch. Public canonical
byte parsing depends on that registration and passes for all five cells, so
deleting the version registration fails the focused evidence. Exact-current
compatibility is readable while legacy, future, absent, and unknown versions
refuse; the separate ISS-002 compatibility capability check passed.

## Scope and authority boundary

The diff adds no native-consume or native-removal receipt schema, post-selection
receipt schema, recovery archive, tombstone bytes or position, selected
proposal/receipt/tip composition, currentness validator, persistence lookup,
broker operation, credential or live capability, proposal/CAS operation,
command, filesystem mutation, state service, runtime recovery, promotion, or
release authority. Digest-named deferred evidence remains opaque structural
text. The pre-existing common pointer/tombstone/receipt and recovery path
surfaces are unchanged by this commit and grant no new state-slice authority.

The implementation cannot select itself. A parsed state, successful pure
transition, detached canonical bytes, contextual digest, candidate, worker, or
this review supplies no selected-history continuity or mutation capability.
ISS-004 remains the selected mutation owner, ISS-032 remains the broker and
authorization-fact owner, ISS-020 remains the reviewed-bootstrap producer, and
ISS-041 remains the first non-authoritative runtime parser consumer.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible shape |
| --- | --- | --- | --- |
| Three flat exact branch censuses | Opposite-lifecycle or premature proof fields enter state bytes. | Deleting shared unknown/missing closure admits focused branch additions or omissions. | Retain three flat lists; add no nested proof bag. |
| Three REVOKED cells | Partial consumption is represented as terminal revoke. | Deleting pair/gate implications admits a committed partial-cell negative. | Retain direct null/equality checks only. |
| Four-edge relative validator | A skip, reversed state, mixed transaction, or reordered value is blessed. | Deleting the shared edge, equality, or time check admits a committed negative. | Retain one shared pure validator; add no history object or selected-currentness claim. |
| Exact state position dispatch | A gate/fence/TOMBSTONE position can stand in for authorization VALUE. | Deleting state dispatch admits the committed nonempty-parts negative. | Retain one parser and one common-dispatch branch. |
| Context-required serializer refusal | Detached serialization exposes an untagged second identity. | Deleting the exact branch reaches the generic canonical digest. | Retain the existing failure arm; compute authority identity only with common contextual `Dv`. |
| One three-lifecycle public API | A legacy helper blesses REMOVED while the state parser refuses it. | Retaining the old helper/list makes the API-surface assertions fail. | Replace the old name with the sole parsed validator; add no compatibility shim. |
| Explicit downstream deferral | Opaque digest syntax is mistaken for selected receipt/broker authority. | Deleting the boundary invites caller-coordinated assertions. | Retain structural parsing only; add none of the deferred machinery. |

This is the smallest implementation of the reviewed ledger. It adds no state
proof bag, second digest domain, generic serializer redesign, state-specific
identity registry, archive coordinator, authenticated history, sparse tree,
inventory, persistent locator, capability wrapper, or runtime service.

## Verification evidence

All required gates ran strictly one at a time. The standalone full-suite
process fully exited before the separate bootstrap verifier began, and
bootstrap fully exited before the focused gates. Tests used increased
`--testTimeout=30000 --hookTimeout=30000` where the direct Vitest command was
available.

The initial `pnpm run planning:check` launcher remained silent before script
execution and then failed closed after about 124 seconds because the signed npm
registry release bytes could not be fetched or verified for pinned
`pnpm@11.22.0`. Following the authorized infrastructure-only fallback, all
gates used the identical checked-in script or lockfile-pinned binary directly.
No source, test, dependency, or semantic command was substituted. Sandboxed
board, formatting, Vitest, and bootstrap attempts failed only at child-process
spawn with `EPERM`; each identical read-only command was rerun under approved
process-spawn escalation.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`.
   It read only this repository and its registered delivery project.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=30000 --hookTimeout=30000`
   — PASS: 31 files and 373 tests in 103.34 seconds. The process fully exited
   before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries, formatting,
   typecheck, 31 files and 373 tests in 85.23 seconds, private compositions,
   planning contracts, and source-tree status invariance.
7. Direct lockfile-pinned equivalent of the focused ISS-002 contracts slot,
   `node node_modules/vitest/vitest.mjs run test/contracts --testTimeout=30000
   --hookTimeout=30000` — PASS: 21 files and 176 tests.
8. Direct lockfile-pinned equivalent of the separate ISS-002 compatibility
   slot, `node node_modules/vitest/vitest.mjs run
   test/contracts/compatibility.test.ts --testTimeout=30000
   --hookTimeout=30000` — PASS: one file and one test.

## Authorization boundary

This `PASS` clears exact head `94dca883812b0a78b72fc1a0f07fda9c26e2c67b`
only for the bounded recovery-authorization selected-state structural slice
authorized by the clean round-128 planning review. It does not authorize any
native/post-selection receipt ledger, archive/tombstone ledger or composition,
selected graph/currentness, persistence discovery, broker operation,
capability, proposal/CAS, command, filesystem mutation, runtime recovery,
promotion, or later recovery ledger.

This record grants no merge, release, installation, external mutation,
promotion, or self-certification authority. Stable release N may build and test
N+1, but only external review and the stable promotion path may grant N+1
authority; a worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-21-round-129.md`; no source, test, draft, issue,
roadmap, board, or other planning file was edited, and no commit was created.
