# ISS-003 configuration-contract post-repair review — round 307

`PASS` at exact base/HEAD
`c974d5dce218fef759cc40c944483f685b9c8ec5`.

Round 305's two blockers are closed:

1. `configuration-provenance/v1` now requires `PROJECT` for exactly
   `adapterId`, `capabilityNames`, `leaseFreshnessMs`, `maximumSessionMs`,
   `projectId`, and `wallClockSkewMs`. Its tests refuse each field's `CLI`,
   `ENVIRONMENT`, and `DEFAULT` arms, while `stateRoot` alone accepts all four
   `CLI|ENVIRONMENT|PROJECT|DEFAULT` values.
2. One frozen contract-owned 33-command census matches the existing ten CLI
   registration manifests' command identities and placeholder owners. It
   drives both parser lookup and schema vocabulary without handlers, options,
   dispatch, or runtime mutation authority. `CAPABILITY_NOT_IMPLEMENTED` now
   accepts only a census placeholder command with that command's exact owner;
   implemented config commands, unregistered and Git commands, wrong owners,
   owner extras, and vocabulary-only drift refuse. The owner-bearing failure
   has an exact canonical-byte golden.

The repair adds only the smallest semantic tables and mutants needed to stop
durable bytes from claiming impossible provenance or command ownership.
Deleting either binding makes the Round 305 probes survive; adding loader,
dispatcher, static-runner, filesystem, authority, board, or roadmap machinery
would not be proportionate. None is present in the tree.

Focused evidence: configuration, compatibility, and vocabulary tests pass (12
tests); the contracts TypeScript project, four-file Prettier check, and
`git diff --check` pass. Independent probes confirm all 18 forbidden
project-field source arms refuse, all four `stateRoot` arms pass, the census is
33 unique commands, the exact placeholder passes, and implemented, Git,
wrong-owner, and owner-extra variants refuse.
