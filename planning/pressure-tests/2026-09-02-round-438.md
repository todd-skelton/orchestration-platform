# Pressure round 438 — exact-byte planning-source replan

Status: definition-only fresh sub-slice after the declared Round 437 hosted
block. Protected base is `6d5fe5d98e60c817283e5021648b9e655ed191ca`.

## Failure boundary

Round 437 mixed two claims. Its normalized source digest correctly denied every
source mutation, but its separate TypeScript-token projection was presented as
an executable dependency/effect classifier. Hosted evidence showed the
projection stayed clean for every appended mutant. Repairing that scanner after
the sole follow-up would exceed the round, and introducing another parser or
compiler surface would be larger than the quarantine consumer needs.

## Fresh admission boundary

The next candidate has one automated source-admission rule: immediately before
every planning `plan` invocation, normalize the tracked
`modules/planning/src/index.ts` bytes and require the independently reviewed
SHA-256 literal. Any import, ambient read, effect call, or future unknown global
changes those bytes and cannot inherit the review. Seeded import and ambient
mutants prove identity denial only; they do not claim semantic categorization.

Independent exact-head review must recompute that digest and inspect the exact
admitted production bytes for the one public-contract static dependency and the
absence of filesystem, process, network, clock, randomness, credentials, host,
adapter, or mutation access. The existing TypeScript check supplies ordinary
source correctness. No scanner, parser, sandbox, VM, or ambient-name inventory
is added or retained. When the final manifest activates, its reviewed normalized
source digest and generator admission replace this temporary quarantine pin.

## Scope, non-goals, and removal evidence

The fresh source slice may reuse Round 437's planning semantics and consumer
vectors only as declared blocked inputs. It may add the planning entrypoint,
focused existing walking-skeleton consumer vectors, and one source pressure
record. It must remove the failed token scanner and may not change the manifest,
generator, generated stub, public contracts, adapters, runtime, root
configuration, dependencies, or authority surfaces.

Deleting the pre-invocation identity gate lets changed source inherit the
review. Deleting independent exact-byte review leaves the digest without a
semantic decision. Reintroducing a source classifier duplicates the failed
mechanism and forces replan. The digest proves identity continuity only; it does
not prove semantics, safety, installation, execution, release, or authority.

## Acceptance, verification, and review attack

Acceptance retains Round 437's complete descriptor/digest, exact
action/core/brief/directive/footprint, valid substitution refusals, readiness
and capability negatives, canonical ordering, repeated-input byte equality,
adapter decision projection, actual ISS-013 preparation seam, normalized
generator identity, and empty normalized manifest. Every planning invocation
must cross the exact source gate. Static/dynamic import, `require`, clock,
randomness, process, network, `navigator`, `WebSocket`, and one deliberately
unlisted identifier mutants must all change the admitted identity.

Local verification is typecheck, focused formatting, planning, and diff checks;
local Vitest remains prohibited on this Windows checkout. Hosted bootstrap runs
the focused test and full suite on all three operating systems. Independent
review should inspect the exact bytes, recompute all pinned identities, search
for every direct and indirect effect surface, mutate a source byte outside the
seeded census, prove every plan call uses the gate, and verify all placeholder
and authority surfaces remain unchanged. Predicted footprint is exactly the
planning entrypoint, focused walking-skeleton consumer test, and one source
pressure record.
