# Pressure round 456 — ISS-054 fresh-observation and binder sub-slice plan

Status: fresh ISS-054 sub-slice for the fresh-observation parser and four-input
binder, absolute plan attempt 1 of eight, base
`e6c9d4f1bfeb1347ba4f229d1c965b3c3bb7725e` (Packet B landed, PR #281). No
review, no source, and no acceptance is claimed by this record. This packet
implements no parser, test, schema, registry entry, provider action or
authority; it defines the source boundary only, materializing the completed
read-only decomposition audit into the round record and the ISS-054 draft.

## Authority

Decision #282's OWNER ruling on `2026-09-05` governs this sub-slice:

- **Ruling 1: A.** One new ISS-054 sub-slice covering both remaining packets
  is admitted under one ordinary ceiling of eight: plan (1, this record),
  independent semantic plan review (2), Packet C fresh-observation typed
  parsing source (3), C review (4), Packet D binder source (5), D review (6),
  attempts 7-8 reserved for one bounded correction plus re-review. Packet C's
  footprint is exactly four files (a delimited section appended to
  `packages/contracts/src/repository-protection-parse.ts` with Packet A's
  parsers unchanged; `test/contracts/repository-protection.test.ts`;
  `planning/drafts/ISS-054.md`; this sub-slice's pressure record). Packet D's
  footprint is exactly six files (new
  `packages/contracts/src/repository-protection-binding.ts`; one export in
  `packages/contracts/src/index.ts`; two bundle-census rows in
  `packages/conformance/src/iss002-bundle-paths.mts`; new
  `test/contracts/repository-protection-binding.test.ts`; the draft; the
  pressure record). No `registry.ts` change. D waits for C to land on
  protected `main`. A closing acceptance-census round (two planning files,
  authored by a lane that authored none of A through D, one attempt plus one
  independent confirmation) precedes ISS-054 closure. No roadmap or board
  change; ISS-036 keeps its single `blocked_by: [ISS-054]` edge.
- **Ruling 2: A.** The verifier-anchor whole-tree cast at
  `packages/contracts/src/verifier-anchor.ts:168` is waived for Packet D. D
  consumes the landed anchor parser as-is and its review packet states which
  fields it reads and where each is validated before `ok: true`. If a Packet D
  reviewer blocks on the cast, a separate two-file repair slice with its own
  review is the bounded follow-up, not footprint growth inside D.
- Standing rules from the Decision #282 body apply unchanged: the Round 451
  phase gate; goldens derived from the ledger recipes, not the implementation;
  the ledger's strict/non-strict comparison mix preserved with boundary
  vectors in both directions; over-binding refused as firmly as under-binding;
  the fresh window may straddle `receipt.issuedAt` and D carries a positive
  pinning that; local gates only, hosted three-OS as sole executable evidence;
  independent exact-head review before every PR; no provider or authority
  operation.

Decisions #268 and #272 fix the surrounding boundary. Decision #268's ruling:
"ISS-054 remains open until packet B lands and the remaining fresh-observation
and binder slices are separately admitted." Decision #272's frontier effect:
"ISS-054 stays open until packet B lands; the fresh-observation parser and
binder remain separately admitted slices." Both are now superseded in their
registration mechanics by Decision #282 Ruling 1 (Option A: one sub-slice, not
two new roadmap keys), but their substantive boundary — C and D remain
distinct from the landed A/B sub-slice and from each other — stands.

The accepted `Pre-N0 repository-protection and verifier-anchor ledger` in
`docs/architecture/contract-decisions.md` remains the sole implementation
authority for every schema, scalar, relation and refusal cited below; this
record adds no content beyond the ledger, the ruling, and the definition audit
that follows.

**Source content.** This record materializes the ISS-054 remaining-work
decomposition audit (planning lane, read-only, Claude Opus 5 high, row 8,
placement provisional, budget intent at most $12, no repository or GitHub
write performed) into the round-record and draft-entry form. Every ledger
citation carried below was independently re-verified against this record's own
exact head; three files match the audit's stated sizes exactly
(`packages/contracts/src/repository-protection.ts` 637 lines,
`packages/contracts/src/repository-protection-parse.ts` 1639 lines,
`packages/contracts/src/verifier-anchor.ts` 184 lines,
`test/contracts/repository-protection.test.ts` 2843 lines,
`test/contracts/verifier-anchor.test.ts` 483 lines), and every sampled
file:line citation reproduced below was confirmed byte-for-byte against the
working tree. No drift was found; no citation required correction.

**Form precedents.** `planning/pressure-tests/2026-09-05-round-451.md` (the
A/B phase-gate plan, its Packet A and Packet B source-attempt records, and its
attempt-ledger and routing-record conventions) and the ISS-054 draft's existing
Packet A and Packet B entries set the form this record and the paired draft
entry follow.

---

## 1. Landed census — what exists at base `e6c9d4f`

| Artifact | Path | Size | State |
|---|---|---|---|
| Packet A typed parsers (package-private) | `packages/contracts/src/repository-protection-parse.ts` | 1639 lines | landed on main at `1a951ce` |
| Packet B relations + public surface | `packages/contracts/src/repository-protection.ts` | 637 lines | landed at `e6c9d4f` (PR #281) |
| Verifier anchor | `packages/contracts/src/verifier-anchor.ts` | 184 lines | landed (Round 442 slice) |
| Contract vectors | `test/contracts/repository-protection.test.ts` | 2843 lines | landed (A + B) |
| Anchor vectors | `test/contracts/verifier-anchor.test.ts` | 483 lines | landed |

Packet B's public surface is exactly twelve names plus the re-exported
`RepositoryProtectionReceipt` type, asserted at
`test/contracts/repository-protection.test.ts:2488-2490`
(`Object.keys(protection).sort()` equals a twelve-member census, verified
present at this record's head). That assertion is load-bearing for the C/D
footprint predictions below: `observedProtection` has no `schemaVersion`
member, so it never grows this census.

### 1.1 Promise-by-promise: draft "Contract boundary" vs the landed base

Draft citations are `planning/drafts/ISS-054.md:<line>` at this record's head.

| # | Draft promise | Draft cite | Ledger cite | State at `e6c9d4f` |
|---|---|---|---|---|
| P1 | Fresh closed supplied `observedProtection` observation | 64-65 | 6877-6882 | **Absent.** No type, parser, field array or fixture anywhere. |
| P2 | Pure four-input binder (`receipt`, `anchor`, `observedProtection`, `evaluatedAt`) | 63-69 | 6877-6878, 6926 | **Absent.** |
| P3 | `issuedAt <= evaluatedAt < expiresAt` | 64-65 | 6927 | **Absent.** Receipt-internal `issuedAt < expiresAt` and the 7-day bound are Packet A local-stage checks; `evaluatedAt` does not exist in any module. |
| P4 | Fresh at-most-five-minute terminal read-back bound to `evaluatedAt` | 65-66 | 6891-6892 | **Absent.** |
| P5 | Anchor-variable source-update chronology (fresh update >= anchor creation, < historical REVIEW producer start, <= fresh observation start); stale-substitution refusal | 71-76 | 6934-6942 | **Absent (cross-family part).** Only the receipt-internal `variableUpdatedAt < producer.startedAt <= issuedAt` exists (`repository-protection.ts:474-479`, ledger 6793-6795). |
| P6 | `confirmation <= anchor creation <= anchor-variable publication < REVIEW producer start <= receipt issue` | 78-79 | 6930-6931 | **Absent.** Only `confirmedAt <= createdAt` exists inside the anchor parser (`verifier-anchor.ts:163-164`, ledger 6851-6854); the anchor-to-receipt joint is unimplemented. |
| P7 | Detached return, six frozen members only | 68-69 | 6960-6977 | **Absent.** |
| P8 | WORKFLOW_RUN binds the separate completed successful BUILD run; producing REVIEW run has no conclusion/completion; distinct run IDs | 79-82 | 6744-6751, 6776-6786 | **Present** (`repository-protection.ts:508-521`, and the `producer` field census excludes conclusion/completion). |
| P9 | Receipt/anchor closed schemas, parsers, canonical bytes, framed identity, registry, compatibility, public export | 42-61 | 6472-6871 | **Present** for both families (receipt via Packet B `index.ts`/`registry.ts` wiring; anchor already landed). |

### 1.2 Fresh observation — every member, relation and refusal, cited

Members (exactly ten, ascending canonical order) — ledger **6879-6882**:
`apiObservations`, `completedAt`, `environmentBinding`,
`protectedPathPolicies`, `repositoryId`, `reviewPolicy`, `rulesetId`,
`startedAt`, `terminalEvidenceDigest`, `workflows`. There is **no**
`schemaVersion` member, so `observedProtection` is a binder input record, not
a registrable schema family.

| Element | Ledger cite | Status at base |
|---|---|---|
| Ten-member census, "not persisted by this contract" | 6879-6882 | absent |
| Fresh `apiObservations` use the identical closed request/page/terminal row censuses and formulas | 6884-6885 | row parser reusable (see §3.3); array parser absent |
| Exactly six purposes `ENVIRONMENT\|ENVIRONMENT_VARIABLE\|REPOSITORY\|RULESET\|WORKFLOW_BUILD\|WORKFLOW_REVIEW` | 6885-6886 | absent (landed census is the nine-purpose one, `repository-protection-parse.ts:1360-1377`) |
| Ordered six-purpose sequence | 6884 with 6896-6897 | absent |
| `triggeringBuild` literal null on every fresh row | 6744, 6749-6750 with 6884 | implied by the identical row census; no fresh vector exists |
| Every fresh page time within `startedAt..completedAt`, explicitly **not** before receipt issue | 6887-6888 | absent |
| Fresh `environmentBinding`: identical five-member census, independently observed ETag and update time | 6889-6890 | parser reusable verbatim (`parseEnvironmentBinding`, `repository-protection-parse.ts:1419`) |
| Fresh observation must finish within 300,000 ms of its start | 6891 | absent |
| `evaluatedAt` must equal `completedAt` | 6891-6892 | absent |
| `terminalEvidenceDigest` framed over API terminals + environment ETag/time + semantic projection + repository/ruleset identities + exact observation start/end | 6892-6895 | absent |
| Exactly one canonical frame part; `apiTerminals` retains six-purpose order; each terminal row is exactly `purpose`, `requestIdentityDigest`, `terminalPaginationDigest` | 6895-6898 | absent |
| Exact recipe under `repository-protection-terminal-evidence/v1` | 6900-6917 | absent; domain constant does not exist in any module |
| Fresh `environmentBinding` enters the projection complete (five members); ruleset projection identical to the semantic-digest projection | 6919-6920 | absent |
| Projection excludes `terminalEvidenceDigest` itself (non-circular) | 6921-6922 | absent |
| **Refusal**: precomputed caller count, historical receipt timestamp, or an API row without terminal pagination presented as the evaluation boundary | 6922-6924 | absent |
| **Refusal**: stale or unbound fresh terminal evidence | 6994-6996 | absent |

### 1.3 Binder — every relation and refusal, cited

| Relation / refusal | Ledger cite | Status |
|---|---|---|
| Four detached supplied inputs | 6877-6878 | absent |
| Binder parses both values | 6926 | absent |
| Requires receipt disposition `ACCEPTED`; all three parse but only ACCEPTED may pass the binding relation | 6926, 6496-6498 | absent, correctly binder-owned (Packet B parses all three with no eligibility gate) |
| `issuedAt <= evaluatedAt < expiresAt` | 6927 | absent |
| Recomputes `Danchor`; four-way equality among `Danchor`, `receipt.verifierAnchorDigest`, historical env variable value, fresh env variable value | 6927-6929 | partial: only the intra-receipt leg `environmentBinding.variableValue === verifierAnchorDigest` exists (`repository-protection.ts:472-474`) |
| Acyclic historical chronology `confirmedAt <= createdAt <= receipt.environmentBinding.variableUpdatedAt < receipt.producer.startedAt <= receipt.issuedAt` | 6930-6931 | partial: the receipt-internal tail exists (`repository-protection.ts:474-480`); both anchor legs absent |
| Rationale: ordered inputs, not future-dated precomputation | 6932-6933 | n/a |
| `anchor.createdAt <= fresh variableUpdatedAt < receipt.producer.startedAt` | 6934-6935 | absent |
| `fresh variableUpdatedAt <= observedProtection.startedAt` | 6936-6937 | absent |
| That comparison also places the update no later than every fresh page observation and completion time | 6938-6939 | absent |
| Historical and fresh update times may differ within the windows; neither value nor ETag equality is required | 6939-6941 | absent |
| **Refusal**: same-value rewrite or retarget-then-restore whose fresh update time is equal to or later than the historical REVIEW producer start | 6941-6942 | absent |
| Receipt, anchor, signer-workflow and fresh-observation repository IDs agree | 6943-6944 | partial: `signerWorkflow.repositoryId === repositoryId` exists inside the anchor parser (`verifier-anchor.ts:147-148`); the three cross-record legs are absent |
| Historical and fresh environment names / variable names agree; ETags and update times independently validated and deliberately not equal | 6944-6946 | absent (the literal names are pinned per-record at 6789-6790) |
| Six shared rows joined one-for-one by `purpose` and exact `requestIdentityDigest`; PULL_REQUEST, PULL_REQUEST_REVIEWS and WORKFLOW_RUN have no fresh counterpart | 6946-6948 | absent |
| **Refusal of over-binding**: page counts, ETags, cursors, Link headers, response bytes, capture times, reduction digests and terminal digests are not equated across historical/fresh | 6948-6950 | absent |
| Each fresh row must independently terminate | 6950-6951 | absent |
| Fresh `reducedValueDigest` recomputes from the six semantic projections; API ETags/times and page metadata excluded | 6951-6955, projections at 6757-6764 | absent for fresh; the historical recompute exists (`repository-protection.ts:373-410`) |
| Semantic digest recomputed from fresh effective semantics must equal the receipt's immutable `rulesetSemanticDigest` | 6955-6957 | absent (historical recompute exists, `repository-protection.ts:521-531`) |
| Receipt producer identity remains historical, never copied into the fresh input | 6957-6958 | absent |
| Return: one detached deeply frozen record, exactly six members `anchor`, `anchorDigest`, `observedProtection`, `protectionDigest`, `receipt`, `terminalEvidenceDigest` | 6960-6970 | absent |
| `anchorDigest = Danchor`, `protectionDigest = Dprotection`, three values are the parsed detached inputs | 6972-6974 | absent |
| **Refusal**: result never contains an authority, currentness, authentication, verification, completeness or grant claim | 6974-6977 | absent |
| Dedicated deletion vectors: remove each chronology comparison; future-date confirmation/anchor/variable; historical or fresh publication equal to producer start; fresh update after fresh observation start; same-value rewrite after producer start; retarget-then-restore with a moved update time | 7001-7005 | absent |
| Authorization boundary (no API/network/mutation/upload/verification/authority) | 7010-7016 | honored by construction in every candidate slice |

### 1.4 Prior source material in the archive

`git grep -n -i 'observedProtection\|binder\|bindRepository\|terminal-evidence'
archive/iss054-historical-receipt-6e33ec3 -- packages/contracts/src
test/contracts` returns **no matches**. The archive's
`packages/contracts/src/repository-protection.ts` (1024 lines) exports
thirteen names, none of which is a binder or an observation parser; its
`test/contracts/repository-protection.test.ts` is 832 lines of
historical-receipt vectors. The same grep over the working tree and over
`4092125` matches only the unrelated advisory family
`packages/conformance/src/github-terminal.ts:363`
(`verifyGithubTerminalEvidence`), which is a different contract and must not
be reused or conflated.

**Conclusion: there is no prior source material for the fresh observation or
the binder anywhere in the repository, in any branch, or in the archive.**
Both slices are greenfield source over an already-accepted definition.
Reusable material is limited to Packet A's typed parsers, Packet B's frame
helpers and digest recipes, and the vector idioms in the two landed test
files.

---

## 2. Bounded slice definitions

Recommended split, per Decision #282 Ruling 1: **two source packets plus one
closing census round**, sharing one sub-slice registration.

- **Slice C — fresh observed-protection typed parsing** (parse-phase only).
- **Slice D — the pure four-input binder** (relation phase + public surface).
- **Slice E — ISS-054 acceptance census and closure** (planning-only).

Rationale for the C/D split rather than one packet: it is the same phase gate
Decision #268 bound 2 made mandatory and Decision #272 re-affirmed — parse to
typed values first, relate only over typed values second. A single packet
would reintroduce exactly the interleaving that produced the five known
failure instances. It also keeps each artifact inside one reviewer's working
set: C is ~400-550 new parser lines plus vectors, D is ~350-450 relation lines
plus vectors; a combined packet would be ~900 source lines and ~2000 test
lines in one review.

### 2.1 Slice C — fresh observed-protection typed parsing

**Title.** ISS-054 Packet C: closed typed parsing for the fresh
`observedProtection` read-back record.

**Scope fence — in.** One private typed parser for each new record kind of
the fresh observation tree and one typed array parser for each new array, in
the same two-stage form Packet A uses (root snapshot with the repository
hostile-reflection primitive; refuse before constructing any parent whose
child failed; local structure stage over typed values only). New kinds are
exactly: `observedProtection` root (6879-6882); the fresh six-purpose
observation array (6885-6886); and, if the terminal-evidence projection is
parsed rather than built (see §3.4), the `apiTerminal` row (6897-6898).
Local-stage checks confined to one record: the ten-member census, six-purpose
ordered census, the `startedAt <= completedAt` and
`completedAt - startedAt <= 300000 ms` window (6891), `triggeringBuild`
literal null on every fresh row (6744 with 6884), and page times inside the
observation window where the ledger scopes them to the same record
(6887-6888).

**Scope fence — out.** No public export; no `index.ts` or `registry.ts` edit;
no registry vocabulary or compatibility entry (the record has no
`schemaVersion` member, 6879-6882); no digest computation; no cross-record
equality, chronology join, purpose join, semantic projection or
terminal-evidence framing; no mutation of Packet A's landed parsers or of the
exported `repositoryProtectionSchemaFields` object (new field arrays are
module-private consts — adding keys to the exported object changes a
reviewed public value); no anchor change; no ISS-054 closure.

**Decisions relied on.** #268 bounds 2 (mandatory phase gate), 3 (structural
no-throw discriminator), 4 (independent review per packet), 7 (local gates
before review), and the boundary line that ISS-054 stays open until the
fresh-observation and binder slices land; #272's standing rule; Decision #282
Ruling 1 (Option A registration, exact four-file footprint) and Ruling 2
(anchor-cast waiver applies to D, not C).

**Acceptance evidence.**
- *Exact vectors.* A canonical six-purpose fresh observation fixture with a
  REST and a GraphQL observation; one-, two- and sixty-four-page REST and
  GraphQL page arrays; nullable-cell positives (`etag` null on GraphQL,
  `endCursor` null on the terminal page,
  `linkHeaderDigest`/`nextRequestDigest` null on a one-page REST observation,
  `triggeringBuild` null on all six rows).
- *One-axis mutants.* Every scalar grammar and every literal cell of the new
  records, one at a time, each with its exact issue code.
- *Refusal categories.* Missing / unknown / renamed / reordered top-level
  member; the historical nine-purpose array supplied where six are required;
  duplicated, skipped or reordered purposes; `WORKFLOW_RUN`, `PULL_REQUEST`
  or `PULL_REQUEST_REVIEWS` present in a fresh array (6885-6886, 6946-6948);
  non-null `triggeringBuild` on any fresh row; a `schemaVersion` member added
  to `observedProtection`.
- *Freshness mutants.* `completedAt - startedAt` at exactly 300,000 ms
  (positive), at 300,001 ms (refusal), at 0 ms (positive), reversed (refusal).
- *Chronology mutants (record-local only).* Page `observedAt` before
  `startedAt` and after `completedAt`.
- *Stale-substitution mutants.* Deferred to Slice D — every stale-substitution
  relation in the ledger (6941-6942, 7003-7005) is cross-record and cannot be
  expressed in Slice C without violating the phase gate. Slice C states this
  as an explicit non-goal so the reviewer does not read the omission as a gap.
- *Opaque-leaf boundaries.* Fresh `responseDigest`, `linkHeaderDigest`, later
  REST `requestDigest`, `queryDigest`, `documentDigest` and
  `variablesDigest` remain supplied claims (6667-6671, 6701-6706); Slice C
  asserts grammar and nullability only and carries an explicit test that no
  positive asserts API origin, authentication or completeness.
- *Hostile reflection.* Per-root proxy, accessor, non-enumerable, symbol,
  hole, cross-realm, subclass, sealed/frozen and descriptor-trap corpus with
  zero trap executions at every new root, in the Packet A idiom.
- *Structural discriminator (Decision #268 bound 3).* The no-throw matrix
  extended to every new root and every new path: null, wrong type and
  malformed nested record at each, asserting refusal and never a throw.
- *Deletion mutants.* One per new typed-child gate, each removing exactly one
  gate and each shown to make a committed mutant survive.

**Verification.** Local, at the exact head, only: `pnpm typecheck`,
`pnpm format:check`, `pnpm planning:check`, `pnpm planning:board-check`,
`git diff --check`. No local Vitest, native, probe, provider or
administrative operation. Executable contract evidence comes only from hosted
Linux, macOS and Windows bootstrap, after independent exact-head review by a
non-author lane.

**Predicted footprint — exactly four files** (Decision #282 Ruling 1):

| Path | State | Change |
|---|---|---|
| `packages/contracts/src/repository-protection-parse.ts` | existing | append a delimited fresh-observation section; Packet A's landed parsers unchanged |
| `test/contracts/repository-protection.test.ts` | existing | add Packet C describe blocks |
| `planning/drafts/ISS-054.md` | existing | add the Packet C record; mirror to board issue #255 |
| `planning/pressure-tests/2026-09-05-round-4NN.md` | new | Packet C's source-attempt update to this round record |

No `iss002-bundle-paths.mts` row is needed: both source and test paths are
already registered (`packages/conformance/src/iss002-bundle-paths.mts:59` for
the parse module and `:160` for the test; Packet B's relations module is at
`:60`). A fifth file is footprint growth and parks the slice.

*Test-file reuse justified.* The existing file already holds the 25-kind
structural census, the 57-root no-throw matrix harness, the
hostile-reflection corpus and the canonical nine-purpose fixture; the fresh
observation reuses all four. A new test file would duplicate the harness and
add a bundle-census row for no reviewer benefit. Cost: the file grows from
2843 to roughly 3600 lines.

*Alternative considered and rejected.* A new module
`repository-protection-observation-parse.ts` keeps Packet A byte-identical
but either duplicates ~20 parsers or imports them, adds a file and a bundle
row, and splits one parse phase across two modules. Recommend the append.

**Non-goals.** Public export, registry, compatibility, serializer, any
digest, any cross-record relation, any anchor change, ISS-036 production,
provider or filesystem access, authority grant, ISS-054 closure.

**Review attack surface.** A fresh parser that silently reuses the
nine-purpose census; a purpose enum widened rather than narrowed; a fresh row
admitted with a non-null `triggeringBuild`; the 300,000 ms window implemented
as `<` or against the wrong pair; an issue accumulator that continues into
another record; a discriminator read before the record's own detached
closure; a nullable positive converted to a blanket null refusal; new keys
added to the exported `repositoryProtectionSchemaFields`; the exported
public-name census silently changed; a fresh-page-window check that reaches
into the receipt; any cross-record read leaking into the parse phase; census
checks written from recall rather than derived from the accepted field
arrays and the ledger.

**Recommended routing.** Source: **row 7** (deep cross-context
implementation, contracts) — Opus 5 high provisional, Sol high fallback.
Effort score `D = 0.22*2 + 0.22*3 + 0.18*1 + 0.20*4 + 0.10*2 + 0.08*4 = 2.60`
-> high; the risk-4 raise and the deterministic-verifier lower cancel. Review:
**row 11** (high-recall review for contracts) — Opus 5 high provisional, Sol
high fallback, never Fable. This matches the observed precedent: Decision
#272's status comments record attempts 4, 6 and 8 as non-author Claude Opus 5
lanes.

### 2.2 Slice D — the pure four-input binder

**Title.** ISS-054 Packet D: the pure four-input cross-family binder and the
terminal-evidence identity.

**Scope fence — in.** One new relations module holding: the
`repository-protection-terminal-evidence/v1` recipe (6900-6917); the binder
(6877-6878, 6926-6958); and the six-member detached frozen return
(6960-6970). Every public entry point invokes its mapped parser — Packet A
for the receipt, Slice C for `observedProtection`,
`parseBootstrapVerifierAnchor` for the anchor, and a scalar timestamp parser
for `evaluatedAt` — before any pure computation. Relations receive only
parsed records, scalars and dense arrays and return a typed issue list; only
a zero-issue result succeeds.

**Scope fence — out.** No registry vocabulary entry and no compatibility
entry: `observedProtection` has no `schemaVersion` member (6879-6882) and the
binder introduces no new schema family, so `parseContract`/
`serializeContract` routing is untouched. No change to Packet A or Packet B
behaviour, to the twelve-name Packet B census, or to any landed vector. No
API, network, pagination, filesystem, credential, artifact, attestation,
installation, release or authority operation (7010-7016). No ISS-036 producer
code. No anchor-module change (Decision #282 Ruling 2 waives the cast repair
for D).

**Decisions relied on.** #268 bounds 2, 4, 7 and the boundary line; #272's
standing rule and the "fresh-observation parser and binder remain separately
admitted slices" frontier statement; Decision #282 Ruling 1 (exact six-file
footprint, D waits for C) and Ruling 2 (anchor-cast waiver, review packet
states which fields it reads and where each is validated); the accepted
ledger as sole implementation authority (draft:36-40).

**Acceptance evidence.**
- *Exact vectors.* One canonical PASS: a receipt fixture (reusing the landed
  nine-purpose canonical fixture), an anchor fixture, a fresh observation
  whose six shared rows join by purpose and `requestIdentityDigest`, and an
  `evaluatedAt` equal to `completedAt`. Independently calculated goldens for
  `terminalEvidenceDigest`, `Danchor` and `Dprotection`, derived from the
  ledger recipes rather than from the implementation.
- *One-axis mutants.* Each of the four inputs mutated on exactly one axis,
  with the exact issue code, for every relation listed in §1.3.
- *Refusal categories.* Disposition `REJECTED` and `BLOCK_REPLAN`
  structurally valid but binder-ineligible (6496-6498, 6926); repository-ID
  disagreement at each of the four positions (6943-6944); anchor-digest
  disagreement at each of the four legs (6927-6929); environment or variable
  name disagreement (6944-6945); a shared purpose missing from the fresh
  array, duplicated, or a historical-only purpose present (6946-6948); a
  fresh row whose `requestIdentityDigest` has no historical counterpart;
  semantic-digest disagreement (6955-6957); a return member added, removed,
  renamed or reordered, or a return that is not deeply frozen or not
  detached (6960-6970); a return carrying any authority, currentness,
  authentication, completeness or grant field (6974-6977).
- *Chronology and freshness mutants.* `evaluatedAt` equal to `issuedAt`
  (positive), one millisecond before (refusal), equal to `expiresAt`
  (refusal), one millisecond before `expiresAt` (positive); `evaluatedAt`
  not equal to `completedAt` (refusal); window at 300,000 ms (positive) and
  300,001 ms (refusal); each of the five historical comparisons at 6930-6931
  deleted in a scratch copy and shown to make a committed mutant survive;
  confirmation, anchor creation and variable publication future-dated
  (7001-7003); historical or fresh publication equal to producer start
  (7003); fresh update after fresh observation start (7003-7004); fresh
  update after any fresh page observation or completion time (6938-6939).
- *Stale-substitution mutants.* Same-value rewrite after producer start
  (7004); retarget-then-restore with a moved update time (7004-7005) — both
  must refuse **by the moved update time**, and each vector must assert the
  exact issue code so the refusal is not accidental. Plus the negative
  control the ledger requires in the other direction: a fresh update time
  that legally differs from the historical one, with differing ETags, must
  **pass** (6939-6941, 6944-6946), and a vector must fail if the
  implementation demands equality.
- *Straddle positive (§3.5).* A positive fixture whose fresh window starts
  slightly before `receipt.issuedAt` and ends at or after it, asserting the
  ledger admits this and Slice D must not add an unstated
  `observedProtection.startedAt >= receipt.issuedAt` relation.
- *Opaque-leaf boundaries.* No positive may assert that a fresh
  `responseDigest`, Link-header digest, later REST `requestDigest`,
  `queryDigest`, `documentDigest` or `variablesDigest` is authenticated or
  complete (6701-6706, 6667-6671); a test asserts that mutating each opaque
  leaf moves every enclosing recomputable digest and nothing more; the two
  historical-only reduced values (`PULL_REQUEST`, `PULL_REQUEST_REVIEWS`)
  must never be joined into fresh semantics (6768-6774).
- *Terminal-evidence mutants.* A caller count, a historical receipt
  timestamp, and an API row lacking terminal pagination each refused as the
  evaluation boundary (6922-6924); a `terminalEvidenceDigest` computed over a
  projection that includes itself must be impossible by construction and
  asserted (6921-6922); frame-tag and frame-order mutants must move the
  digest while record-key insertion-order variants must not (6659-6665).
- *Anchor-field provenance (Ruling 2).* The review packet states, for each
  anchor field D reads (`repositoryId`, `createdAt`,
  `signerWorkflow.repositoryId`, `operatorConfirmation.confirmedAt`), the
  exact `verifier-anchor.ts` line where it is validated before `ok: true`
  (verified in §3.2 below: `:115`, `:116`, `:146-148`, `:163-164`).

**Verification.** Identical local gate set to Slice C; executable evidence
only in hosted three-OS bootstrap after independent exact-head review.

**Predicted footprint — exactly six files** (Decision #282 Ruling 1):

| Path | State | Change |
|---|---|---|
| `packages/contracts/src/repository-protection-binding.ts` | new | binder, terminal-evidence recipe, return construction |
| `packages/contracts/src/index.ts` | existing | one `export * from "./repository-protection-binding.js"` |
| `packages/conformance/src/iss002-bundle-paths.mts` | existing | two sorted rows (source + test) |
| `test/contracts/repository-protection-binding.test.ts` | new | binder vectors |
| `planning/drafts/ISS-054.md` | existing | Packet D record; mirror to #255 |
| `planning/pressure-tests/2026-09-05-round-4NN.md` | existing by then | Packet D's source-attempt update to this round record |

*New test file justified.* `test/contracts/repository-protection.test.ts:2488-2490`
asserts `Object.keys(protection).sort()` equals exactly twelve names. Putting
the binder in a separate module leaves that reviewed assertion untouched and
keeps the review diff self-contained; folding the binder into
`repository-protection.ts` would edit the reviewed public census and push one
test file past 4,500 lines. `registry.ts` is **not** touched.

**Non-goals.** Registry/compatibility entry for a non-family record; any
change to Packet A/B behaviour or the anchor module; ISS-036 production; live
read-back; anything at 7010-7016.

**Review attack surface.** A raw cast or fragment parser in the relation
phase; a caller-asserted typed fast path; a public helper that bypasses its
parser; a historical value copied into the fresh input (6957-6958);
historical/fresh metadata equality silently required where the ledger
forbids it (6939-6941, 6948-6950); a chronology comparison implemented with
the wrong strictness (the ledger mixes `<` and `<=` at 6930-6931 and
6934-6937 deliberately); the join performed by array index rather than by
purpose plus exact `requestIdentityDigest`; a fresh row accepted without
independent termination (6950-6951); a return that is shallow-frozen,
attached to the caller's input, or carrying a seventh member; the
terminal-evidence projection drifting a member, frame tag or frame order;
the binder result described anywhere in prose or JSDoc as freshness,
authentication, completeness or authority (6974-6977); a deletion mutant
that is tautological because the deleted gate is duplicated elsewhere; a
reviewer BLOCKing on the waived anchor cast without the review packet's
field-provenance table to pre-empt the objection.

### 2.3 Slice E — ISS-054 acceptance census and closure

**Shape.** Read-only audit plus one draft closure record. Footprint: exactly
two files — `planning/drafts/ISS-054.md` (closure record and
criterion-by-criterion census, mirrored to #255) and its own sub-slice
pressure record. No source, no test, no wiring. Author must be a lane that
authored none of A, B, C or D.

**Acceptance evidence.** A table mapping each of the seven ISS-054 criteria
(draft:121-154) and each clause of the ledger's issue-wide negative corpus at
6979-7008 to a landed test name at a named head, plus an explicit list of any
clause with no landed vector. Local gates only.

**Routing.** **Row 8** (decomposition audit / final judge) — Opus 5 high
provisional, Sol high fallback. Attempt ledger: its own small ceiling; one
attempt plus one independent confirmation should suffice, per Decision #282
Ruling 1's closing-census requirement.

---

## 3. The phase gate extends; the structural gaps and the deliberately open clause

### 3.1 The C/D split is the same Round-451 gate

Round 451's rule set is: (i) snapshot each root once with the
hostile-reflection primitive; (ii) every child record has its own parser
returning `ParseResult<SpecificRecord & ContractRecord>`; (iii) never
construct a parent whose child failed; (iv) no cross-record read, `.find`,
dereference, projection or digest before the gate; (v) in the relations
packet, no fragment parser, raw-object cast, non-null assertion over raw
input, or caller-asserted typed fast path (round-451:398-400, 246-247,
159-172). The fresh observation is structurally a receipt-shaped tree
(records, dense bounded arrays, union page branches), so (i)-(iv) apply
unchanged; the binder is pure cross-record relation work, so (v) applies
unchanged. Slice C is the phase-one analogue of Packet A and Slice D the
phase-two analogue of Packet B. Round 451 itself anticipates this: "Fresh
`observedProtection` and the four-input receipt/anchor binder remain
separate later slices with their own records after Packet B lands"
(round-451:101-103), and its attack list already names "closing ISS-054
before the later fresh-observation and binder records land"
(round-451:539).

### 3.2 Structural gap 1 — the anchor is typed by a whole-tree cast (Ruling 2 waives repair)

`packages/contracts/src/verifier-anchor.ts:168` returns
`{ ok: true, value: record as BootstrapVerifierAnchor }`, where `record` is
the `ContractRecord` (`Readonly<Record<string, JsonValue>>`,
`packages/contracts/src/runtime.ts:7`) produced by `snapshotClosedRecord`.
The nested `assets`, `signerWorkflow` and `operatorConfirmation` rows are
validated in place but are never returned as typed children — the declared
type is an assertion over raw `JsonValue` members.

Two facts bound the severity, both re-verified against this record's exact
head:

- **Detachment is sound.** `snapshotClosedRecord` delegates to
  `snapshotJson`, which recursively snapshots and freezes the whole tree and
  refuses proxies, accessors, non-enumerables and traps at every depth
  (`packages/contracts/src/runtime.ts:102-113, 157-178`). So the anchor's
  hostile-reflection posture is correct; the gap is type-level only.
- **Every field the binder reads is in fact validated before `ok: true`.**
  `repositoryId` (`verifier-anchor.ts:115`), `createdAt` (`:116`),
  `signerWorkflow.repositoryId` (`:146-148`, including the equality to the
  top-level ID), `operatorConfirmation.confirmedAt` (`:163-164`, including
  `confirmedAt <= createdAt`). The cast is justified in fact, but not by
  construction — precisely the defect class Decision #268 bound 2 names.

Decision #282 Ruling 2 resolves the question this raised (waive vs. repair
vs. separate slice) as **A: waive**. Slice D consumes the landed anchor
parser as-is; its review packet must state exactly which fields it reads and
where each is validated (the four citations above). If a Packet D reviewer
BLOCKs on the cast, a separate two-file repair slice with its own review is
the bounded follow-up — not footprint growth inside D.

The anchor implementation already exposes a parsed type usable today:
`BootstrapVerifierAnchor` and `VerifierAnchorAsset` are exported
(`verifier-anchor.ts:43, 53`) and `parseBootstrapVerifierAnchor` returns
`ParseResult<BootstrapVerifierAnchor>` (`:101`); it is produced by assertion
rather than by typed-child construction, which is exactly what Ruling 2
waives.

### 3.3 Structural gap 2 — reuse boundary in Packet A (favourable)

Packet A's row-level `parseHistoricalApiObservation`
(`repository-protection-parse.ts:1275`) is purpose-agnostic: it parses the
purpose, dispatches the request branch, parses pages, and requires
`triggeringBuild` non-null exactly when the purpose is `WORKFLOW_RUN` and
literal null otherwise (`:1290-1299`). It is therefore **directly reusable
for fresh rows with no edit**. Only the array-level census is
historical-specific: `parseHistoricalApiObservations` (`:1361-1377`) fixes
the length to `repositoryProtectionPurposes.length` (nine) and asserts the
ordered nine-purpose census. Slice C adds a sibling six-purpose array parser
and reuses the row parser. `parseEnvironmentBinding` (`:1419`) is reusable
verbatim — the fresh binding has the identical five-member census and the
identical literal names (6889-6890, 6788-6790). `parseRulesetSemantics`
(`:1494`) already covers the exact five-member semantic subset the fresh
observation carries. This is why Slice C is a four-file append rather than a
new module.

### 3.4 Structural gap 3 — is `apiTerminals` a parsed record or a derived projection?

The ledger states the `apiTerminals` row census (6896-6898) as part of the
terminal-evidence recipe, not as a member of `observedProtection`
(6879-6882). So the projection is **derived by the binder** from the fresh
observation's own rows, not supplied. **Not established** whether a public
helper analogous to `computeGitHubApiTerminalPaginationDigest` should also
accept a supplied `apiTerminals` array; if Slice D exports such a helper it
must, under the phase gate, own a typed parser for that row — which would
move the `apiTerminal` kind into Slice C. **Recommendation: do not export a
standalone terminal-evidence helper that accepts a supplied projection**;
derive it inside the binder only, and keep the `apiTerminal` kind out of
Slice C. If Todd or a reviewer wants the standalone helper, it moves to
Slice C's census and Slice D's export list.

### 3.5 The deliberately open clause D must pin with a positive

The ledger requires fresh page times to lie within
`observedProtection.startedAt..completedAt` and says explicitly "rather than
before receipt issue" (6887-6888). Combined with `issuedAt <= evaluatedAt`
(6927) and `evaluatedAt == completedAt` (6891-6892), a fresh window that
starts slightly before `receipt.issuedAt` and ends at or after it is
**admitted**. The ledger states no
`observedProtection.startedAt >= receipt.issuedAt` relation. Slice D must
not add one, and Slice D's vectors must include a positive that straddles
`issuedAt` so a later lane cannot silently tighten it. This straddle
positive is carried in §2.2's acceptance evidence above and remains
**not established** as a closed question beyond "do not invent the missing
relation" — the ledger simply does not require it, and D's job is to prove
that non-requirement with a passing vector, not to resolve it further.

---

## 4. ISS-054 closure criteria

Criteria are draft:121-154. Status assumes Packet B lands (it has, at
`e6c9d4f`) and Slices C and D land as defined.

| # | Criterion (abridged) | Satisfied by | After C+D |
|---|---|---|---|
| 1 | Both families: closed field census, total parser, canonical serializer route, framed identity, registry entry, compatibility entry, public export; goldens and registry/surface census | anchor (landed) + Packet B (`index.ts`/`registry.ts` wiring, `repository-protection.test.ts:2487+`) | **Satisfied by Packet B alone.** C and D add nothing here — correctly, since `observedProtection` is not a family. |
| 2 | Every scalar/identity/time/pagination/permission/expiry axis refuses independently; one-axis matrix plus boundary-time vectors | Packet A + Packet B + anchor | **Satisfied**, extended by C's fresh-record axes and D's `evaluatedAt` boundary vectors. |
| 3 | Arrays dense, bounded, canonical, duplicate-free and reflectively closed before semantic reads; full hostile corpus | Packet A (57-root matrix) + anchor | **Satisfied** once C runs the same corpus over the new roots. |
| 4 | Receipt cannot contain post-upload artifact identity or archive digest; anchor cannot contain candidate-controlled trust or executable fields; extra/renamed/substitution refusals plus public type census | Packet A/B closed census + anchor (6843-6857) | **Already satisfied**; unaffected by C/D. |
| 5 | Pure supplied-record relations bind the receipt to one parsed anchor and exact synthetic workflow/ruleset observations without lookup or authority; digest, identity, time, path, permission and producer substitution vectors | **Slice D only** | **The single criterion that C+D exist to satisfy.** Unsatisfiable at base — no cross-family relation exists (§1.3). |
| 6 | Receipt vectors pin the fourteen-member top level, nested censuses, 1/64 bounds, two workflows, seventeen permissions, REST/GraphQL chains, terminal reduction, BUILD-to-REVIEW identity, pre-producer chronology, expiry at 1 ms and seven days, refusal at zero/reversed/7d+1ms | Packet A + Packet B | **Satisfied by Packet B.** |
| 7 | Anchor vectors pin the ten-member top level, three-row OS census, asset/checksum/executable identities, fixed literals, signer equality and comparison time | anchor (landed) | **Already satisfied.** |

Additionally, the ledger's issue-wide negative corpus at 6979-7008 contains
clauses that only D can discharge: "historical/fresh metadata equality as a
requirement" and "stale or unbound fresh terminal evidence" (6994-6996), and
the entire dedicated chronology deletion block at 7001-7008. Slice E's
census exists to confirm each of those has a landed, non-tautological
vector.

**Conclusion:** after Slice C and Slice D land and Slice E's census passes,
all seven criteria are satisfiable and ISS-054 can close. Criterion 5 is the
binding constraint; criteria 1, 4, 6 and 7 need nothing from C or D.

### What ISS-036 needs from ISS-054

From `planning/drafts/ISS-036.md`:

1. **Closed schemas and parsers, not producers.** "Closed schemas/parsers
   belong to `ISS-054`" (ISS-036:142); "ISS-036 produces probe facts and the
   protection receipt through ISS-054 parsers" (ISS-036:148-149). Satisfied
   by Packet B for the receipt and by the landed anchor.
2. **The reflective corpus at every protection and verifier-anchor entry
   point.** "All protection and verifier-anchor entry points also run the
   ISS-054 closed snapshot corpus" (ISS-036:115). Satisfied by Packet
   A/B/anchor; Slice C extends it to the fresh observation, which ISS-036
   does not itself produce.
3. **The receipt's exact member set for the producer.** Repository/ruleset
   IDs, semantic digest, path policies, synthetic probe workflow
   identities/triggers/permissions, routes/page counts/ETags, producer
   workflow/run/attempt and intended artifact name, verifier-anchor digest,
   issued time, <=7-day expiry, disposition (ISS-036:35-40). Satisfied by
   Packet B.
4. **The anchor and the environment-variable read-back protocol.** Satisfied
   by the landed anchor plus the receipt's `environmentBinding`
   (6788-6795).
5. **Negative controls ISS-036 must exercise that only the binder can
   judge.** Changing the live ruleset after receipt creation, or setting/
   moving the anchor variable after the producer run, is exactly the
   ledger's stale-substitution refusal (6941-6942) and is **Slice D's**
   relation. ISS-036 cannot demonstrate that control without the binder.

So ISS-036 needs Packet B for production and **Slice D for its
stale-substitution and post-producer-move negative controls**. Slice C is a
prerequisite of D rather than a direct ISS-036 dependency; ISS-036 does not
produce `observedProtection` (ISS-038 does, per 6879-6880). This is the
strongest argument for landing C and D before closing ISS-054 rather than
deferring the binder to ISS-038.

---

## 5. Risks

1. **Registration is settled.** Decision #282 Ruling 1 resolves the question
   the audit raised (all eight A/B attempts consumed; a new registration was
   a hard precondition): Option A is adopted, this record and the ISS-054
   draft entry are attempt 1 of the new eight-attempt ceiling.
2. **Greenfield with no source material.** §1.4 establishes there is no
   prior observation or binder code anywhere. Estimates of C at ~400-550 and
   D at ~350-450 source lines remain this lane's judgment, not repository
   evidence.
3. **Golden independence.** The terminal-evidence, `Danchor` and
   `Dprotection` goldens for D must be derived from the ledger recipes, not
   from the implementation; the attempt-8 review precedent (all seven
   archived goldens reproduced from the recipes) sets the bar.
4. **Strictness drift.** The ledger deliberately mixes `<` and `<=` across
   6930-6931, 6934-6937 and 6891-6892. A single wrong operator is a silent
   authority weakening that only an exact boundary vector catches.
5. **Over-binding is as much a defect as under-binding.** 6939-6941 and
   6948-6950 forbid requiring historical/fresh equality. A reviewer
   optimising for strictness can push the implementation into refusing
   legitimate fresh evidence; D needs explicit passing negatives for this.
6. **Test-file growth.** `repository-protection.test.ts` reaches roughly
   3,600 lines after C. Whether the repository has a proportionality gate
   for test files is **not established** —
   `planning:footprint-collision-check` and
   `test:planning-module-proportionality` are both stubbed to
   `scripts/capability-not-implemented.mjs` (ISS-028, ISS-040) and do not
   run.
7. **Board mirroring.** Any edit to `planning/drafts/ISS-054.md` requires the
   #255 board mirror or `pnpm planning:board-check` fails
   (`scripts/planning/board-check.mjs:29-70`). This is expected to fail at
   this record's own head until the controller host mirrors issue #255;
   see the gate report below.

### Smaller open items (lane-level, no further Decision needed)

- The binder's exported name is **not established** by the ledger. Suggest
  `bindRepositoryProtectionEvidence`; the return type name should avoid any
  authority-suggesting word (6974-6977).
- Whether a standalone terminal-evidence helper accepting a supplied
  `apiTerminals` projection should exist is **not established** (§3.4);
  recommendation is no.

---

## Attempt ledger

Ceiling eight, admitted by Decision #282 Ruling 1, covering both remaining
packets as one sub-slice:

1. **This plan** — bounded definition, materializing the completed
   read-only audit into this round record and the paired ISS-054 draft
   entry. Authored by this lane. No review, no source, no acceptance
   claimed.
2. Independent semantic plan review, pending, by a lane that did not author
   attempt 1.
3. Packet C source, only after attempt 2 PASS and this plan's landing on
   protected `main`.
4. Independent exact-head review of C, by a lane that authored neither the
   plan nor C.
5. Packet D source, only after attempt 4 PASS and Packet C's landing on
   protected `main`.
6. Independent exact-head review of D, by a lane that authored neither the
   plan nor D.
7-8. Reserved for one bounded correction plus one re-review at either
   stage, per Decision #272's standing rule (one bounded same-file
   correction and one independent re-review follow a first plan-review
   BLOCK; a second consecutive plan-review BLOCK parks the sub-slice and
   files a Decision).

A review BLOCK with no bounded correction available, a surviving
counterexample, or footprint growth parks the slice.

## Routing record

Task `ISS-054/Round-456/attempt-1`; exact reported model `claude-sonnet-5`;
effort `medium`; `row: 13` (documentation volume; content is fixed by the
completed row-8 audit); `placement: provisional`; budget intent is at most
$5, actual spend is unknown, and no new artifact spend ceiling is requested
or inferred. The definition audit this record materializes was authored
under `row: 8` (decomposition audit), Claude Opus 5, effort high, placement
provisional, budget intent at most $12.

## Not established

- Whether a standalone terminal-evidence helper accepting a supplied
  `apiTerminals` projection should exist (§3.4) — recommendation is no.
- The exact binder export name (§5 smaller open items) — suggested
  `bindRepositoryProtectionEvidence`, not fixed by the ledger.
- Any repository test-file proportionality limit (§5 risk 6) — the relevant
  checks are stubbed and do not run.
- The actual source-lane model for landed Packets A and B — the
  `Co-Authored-By: Claude Fable 5.1` commit trailer is the harness default
  in these lanes and is not evidence of routing.
