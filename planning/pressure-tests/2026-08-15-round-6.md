# Planning pressure test — round 6

Disposition received: `BLOCK_REPLAN`.

The sixth independent reviewer passed the 29-node mechanical graph and identified
five remaining trust, re-entry, ambient-input, and scaffold omissions.

| Finding | Replan disposition |
|---|---|
| `OP-R6-001` bootstrap actors/grant were labels, not authenticated authority | Added `ISS-029` to probe GitHub OIDC/artifact attestations plus a protected environment, numeric identities, freshness, one-use nonce, pinned verifier, and failover disposition; blocked bootstrap implementation on it. |
| `OP-R6-002` engine step protocol and next-cycle actor remained unspecified | Published exhaustive `routine-cycle/v1` and added `ISS-030` for cross-platform scheduler installation, two-plus-cycle supervision, cold-host re-entry, overlap refusal, and uninstall. |
| `OP-R6-003` Codex effective prompt/config/tool/environment subject was incomplete | Published immutable fixture bytes, exact adversarial prompts, per-OS environment maps, doctor/prompt/model/tool census, ambient-source mutation tests, and enforcement canaries. |
| `OP-R6-004` concrete adapter scaffold omitted mutation registration | Added a typed static mutation placeholder and three-way shadow/import/mutation ownership/collision evidence to `ISS-028`. |
| `OP-R6-005` Codex host package had no canonical path | Added canonical paths for every package and narrowed `ISS-021` to filling `packages/host-codex/` implementation/test files. |

This is a failed-round record, not a pass. The revised 31-issue packet requires
another fresh independent review before registration.
