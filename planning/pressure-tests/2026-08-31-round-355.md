# ISS-013 current-policy/trip ledger — round 355

Disposition: `BLOCK` at `3d884dc6ae47777144eff6202f1f21d02d0f7e5f`,
base `88be3ee6bcd06876083ba24fd311f0ed69edcd6b`.
Independent cold reviewer: `/root/iss013_breaker_ledger_review`, Sol high, row 11.

The callback request omitted the admitted configuration's capability census.
For an empty frontier, configuration `[]` requires no decisions, while
`["work.read"]` requires one NO_TRIP decision. A configuration digest and static
supported-capability set cannot supply that selected census. The minimal repair
adds `capabilityNames` from detached admitted configuration to the closed
request and pins both empty-frontier cases.

Proportionality: silently omitted configured capabilities are the concrete
threat. Removing complete-census validation loses that protection; inference
from frontier rows fails the empty case. One existing-format request field is
smaller than a new configuration channel. The remaining review found no blocker
in the project-side scope: thresholds remain adapter-owned, ISS-025 is the
named consumer, and recovery authority and AC7 remain explicitly incomplete.
This was a documentation review; no executable behavior was claimed tested.
