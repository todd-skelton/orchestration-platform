# Pressure test — round 107 genesis-selection removal review

**Disposition:** `PASS`

**Reviewed candidate head:** `868bd804e857d3fcc3477c1f71e60d1d7d8eb047`

**Reviewed parent boundary:** `2baae84b4e03220c01640d010a53919c79118c6f`

This is a fresh cold independent ISS-002 removal review of the exact initially
clean head above on `iss-002-versioned-contracts`. It is bounded to the Round
106 repair in `validateGenesisSelectionEvidenceBinding`: full existing E0-core
composition, direct history/core/selected-authority `G` equalities, and the
focused coordinated tests. It does not rely on author reasoning.

The review began with `AGENTS.md`, `docs/planning/work-model.md`, the complete
sole decision authority in `docs/architecture/contract-decisions.md`, the
incorporated literal external-bootstrap and simplified-authority ledgers and
exact `Dgse` formula/exclusions in
`docs/architecture/supervisor-contract.md`, compatibility, ISS-002, Round 106,
the exact parent-to-candidate delta, and the affected source and tests cold. No
Chase Sets repository, issue, project, board, backlog, consumer system, or
runtime was accessed. The board gate read only this repository and its
registered platform delivery project.

## Disposition summary

`PASS`. The candidate removes both Round 106 semantic admissions without
narrowing either valid external-anchor origin and without adding mutation or
self-certification authority:

1. a fully rehashed graph that copies an arbitrary all-zero `Dsc` through
   `Dgb`, GENESIS `Dh`, `Dbg`, the selected E0 graph, CONSUMED graphs,
   consumption, readbacks, and `Dgse` now refuses through the actual parsed
   successor core; and
2. a fully rehashed graph whose selected authority value names a foreign `G`
   while history/core retain the admitted `G` now refuses through both the
   direct authority/core equality and the composed full-core equality.

The repair reuses the existing pure `validateBootstrapGenesisCoreBinding`
relation. It adds no schema, digest, path, pointer, selector, receipt,
inventory, proof, coordinator, serialized capability, runtime writer, or live
handle. No fresh definite blocker was found in the reviewed claim.

## Round 106 attack reconstruction

The independent probes bundled the exact checked-out TypeScript and existing
fixture source entirely in memory. They wrote no probe, fixture, source, test,
or generated file.

### Arbitrary `Dsc`

For each valid genesis-origin and successor-origin fixture, the probe replaced
`Dsc` with the all-zero SHA-256 string in `Dgb`, recomputed `Dgb`, copied the
same value into GENESIS history, recomputed `Dh`, updated the selected
authority head, recomputed authority `Dv`, `Dbg`, E0 proposal/`Dr`, tip/`Dt`,
post-selection receipt, both CONSUMED graphs, the final consumption receipt,
all named readbacks, and the 38-member `Dgse` record. The actual reviewed
operation and actual successor-core record remained unchanged, which is the
semantic distinction the Round 106 validator failed to consume.

Both origins now returned exactly the same three upstream findings:

```text
coreBinding:genesisInput.successorCoreDigest:mismatch
coreBinding:history.successorCoreDigest:mismatch
coreBinding:successorCoreDigest:mismatch
```

An in-memory source mutant deleting only the
`validateBootstrapGenesisCoreBinding` composition returned `[]` for this exact
fully rehashed attack. The committed arbitrary-`Dsc` assertion requires named
`coreBinding` findings, so deletion of the repair edge fails that test.

### Foreign selected-authority `G`

For each valid origin, the probe changed only the selected authority value's
`globalIdentityDigest` at the semantic boundary, retained the admitted
history/core `G`, and rebuilt its `Dv`, `Dbg`, E0 proposal/`Dr`, tip/`Dt`,
post-selection receipt, CONSUMED graphs, consumption receipt, readbacks, and
`Dgse`. Both origins returned:

```text
authority.globalIdentityDigest:mismatch
coreBinding:authority.globalIdentityDigest:mismatch
```

The first finding is the new direct selected-authority/core equality; the
second is the reused full E0-core relation. Deleting the two direct `G`
comparisons in memory retained only the composed finding. The committed test
requires both, so it discriminates deletion of the direct authority equality.

A stronger coordinated probe set history, `Dbg`, and selected authority to
the same foreign `G`, rebuilt `Dh`, `Dv`, selection, consumption, readbacks,
and `Dgse`, and thereby defeated both local repeated-`G` comparisons by
agreement. Full composition still returned:

```text
coreBinding:authority.globalIdentityDigest:mismatch
coreBinding:globalIdentityDigest:mismatch
coreBinding:history.globalIdentityDigest:mismatch
```

This proves the repair authenticates `G` against the actual closed global
identity and anchor-derived authority `Dp`, not merely agreement among three
caller-supplied digest strings.

### Actual successor-core and operation preimages

Independent probes also changed the actual semantic preimages rather than a
copied digest:

- changing only actual `successorCore.successorHelperDigest` refused with the
  expected full-core successor/helper and `Dsc` findings;
- changing only actual reviewed-operation installed bytes refused with
  `coreBinding:successorCore:reviewed-operation-mismatch`.

The stronger coordinated versions recomputed the changed `Dop`/`Dsc`, updated
the use intent and its expectation, `Dgb`, `Dh`, active-release bootstrap
producer/`Dr`/`Dt`, authority `Dv`, `Dbg`, E0 selection, consumption,
readbacks, and `Dgse`. They reached only the intended remaining semantic
boundary:

```text
changed successor helper -> coreBinding:successorCore.successorHelperDigest:mismatch
changed bootstrap grant in reviewed operation -> coreBinding:reviewedOperation:not-anchor-bootstrap
```

Deleting the full-core composition made the simpler actual-core and actual-
operation substitution probes return `[]`. Thus the new inputs are consumed
as records and are not decorative digest expectations.

## Full-core composition and valid origins

The public validator adds the exact 19 inputs already consumed by the existing
full E0-core validator: reviewed operation, successor core, selected
active-release value/proposal/tip, global identity, the eight nullable
successor-provenance inputs, physical identity, locator observation, absence,
use-intent expectation, and absence expectation. The existing parsed
`Dbg`/anchor and ACTIVE anchor/owner graphs, use intent, `Dgb`, GENESIS history,
and selected authority value are forwarded as the other 12 inputs. Inspection
against the established `validateCore` fixture call confirms identical order
and branch meaning.

The composed relation therefore recomputes and equal-binds:

- `Dop` and `Dsc`, reviewed release/manifest/installed bytes/subject/review,
  authority `Dp`, ordinal, operation kind, helper/profile/ABI/lock/state-
  component, custody instance, and admitted observation;
- selected active-release `Dp/Dv/Dr/Dt`, bootstrap producer, position,
  mutation, release root, null prior, and null self-epoch;
- `G` from installation/project/state-root/custody/path/authority `Dp` and its
  repetitions in successor core, history, `Dbg`, and authority value;
- physical identity, admitted locator observation, absence, selected ACTIVE
  owner/anchor graphs, use intent, `Dgb`, GENESIS history, and `Dbg`; and
- either all-null genesis provenance or the complete existing prior-RETIRED
  owner/archive/review/post-selection successor provenance.

The committed genesis-origin and successor-origin compositions both returned
`[]` and retained their distinct fixed `Dgse` goldens. The independent attacks
above also refused through both origins. The all-null versus all-present
successor-provenance branch remains selected only by the existing anchor's
`successorReviewCoreDigest`; no new origin, fallback, or caller assertion was
introduced.

## Exact 38-member `Dgse` audit

The schema and digest code are unchanged from the Round 106 parent. The record
still has exactly 38 closed members: 36 SHA-256 digests, one UUIDv7 bootstrap
transaction ID, and the exact schema literal. The independent field census
and mutation loop confirmed all 38 are consumed:

- `Dgb`, `Dh`, `Dsc`, and `Dbg`;
- `Ddest`, selected owner ACTIVE `Dot/Dov/Dor`, `Dba`, selected anchor ACTIVE
  `Dbat/Dbav/Dbar`, use intent, bootstrap identity, transaction, and grant;
- selected E0 authority `Dp/Dt/Dv/Dr`, value/proposal/tip readbacks, and `Dgp`;
- selected anchor CONSUMED and owner CONSUMED tip/value/receipt triples;
- the anchor-consumption receipt digest; and
- anchor and owner CONSUMED value/proposal/tip readback digests.

Mutating each of the 36 digest members independently reached its exact named
`:mismatch`; changing the transaction reached
`bootstrapTransactionId:mismatch`; changing the schema literal reached
`evidence:schemaVersion:mismatch`. The parser continues to reject missing,
extra, renamed, null, wrong-type, unknown, and future members. The exact framed
semantic order and canonical evidence bytes are unchanged.

The new composition complements rather than replaces all existing direct
readback and consumption relations. `validateBootstrapAnchorConsumptionBinding`
still recomputes the ACTIVE→E0→anchor CONSUMED→owner CONSUMED graph, the final
receipt, custody/timing relations, runtime records, and every parsed readback.
The 38 direct member comparisons then bind those actual named bytes into
`Dgse`.

## Totality, acyclicity, and authority boundary

The public function remains total over its expanded unknown-input surface.
An independent matrix placed a transparent proxy, an `ownKeys`-throwing proxy,
`null`, and a primitive at each of all 19 newly accepted positions using the
successor-origin fixture so that successor-only inputs reached their parsers.
All 76 cases returned one or more findings; zero threw and zero returned an
empty finding list. The throwing-proxy run produced the expected named refusal
at every position, including both expectation-only inputs and all eight
successor-provenance inputs.

The digest direction remains:

```text
reviewed operation -> Dsc -> active-release bootstrap selection
                           -> use intent/Dgb -> GENESIS Dh -> authority Dv
                           -> Dbg -> E0 Dr/Dt/post
                           -> anchor CONSUMED -> owner CONSUMED
                           -> consumption receipt/readbacks -> Dgse
```

`Dgse` enters none of its upstream preimages. The added arguments and validator
call are in-memory validation inputs only. The changed source declares no path,
storage operation, pointer kind, receipt family, selector, capability, handle,
lock, filesystem mutation, or process action. It returns a frozen finding
array, and ISS-002 structural success remains explicitly non-authoritative for
live mutation. There is therefore no digest cycle, persistence widening,
candidate self-epoch, candidate verdict, or self-certification path.

## Test discrimination and proportionality

The committed repair tests are discriminating for the two Round 106 failures:

- the arbitrary-`Dsc` graph is fully rebuilt through all affected downstream
  siblings and requires named `coreBinding` findings; deleting the composition
  changes its result to `[]`;
- the foreign selected-authority `G` graph is fully rebuilt and requires both
  the direct and composed equality findings; deleting the direct comparisons
  removes the required direct finding; and
- changing the actual successor-core record alone returns nonempty only
  because the new composition consumes that record.

The existing core tests independently cover operation/core derivation,
active-release selection, helper/profile/ABI/lock/state-component, admitted
observation, global identity, and both origin-provenance branches. The
selection tests exercise the composition edge, both valid origins, every
`Dgse` member, the rebuilt attacks, selected E0, use intent, authority facts,
CONSUMED facts, and public malformed input. The independent actual-operation,
all-three-`G`, both-origin attack, 19-input totality, and source-deletion probes
close the bounded reviewer-only gaps without adding shipped test machinery.

| Retained mechanism | Concrete in-scope threat | Deletion/attack result | Smallest credible shape and conclusion |
| --- | --- | --- | --- |
| Existing full E0-core composition in `Dgse` admission | Arbitrary `Dsc`, foreign coordinated `G`, or substituted reviewed/core/active/custody facts are copied through a fully rehashed downstream graph. | Deleting the one composition call made arbitrary `Dsc` and actual core/operation substitutions return `[]`. | Reuse the existing pure validator with its actual inputs; no new schema or receipt. Retain. |
| Direct history/core and authority/core `G` comparisons | A local selection validator regression ceases to expose the exact repeated-`G` seam even though the full validator still refuses it. | Deleting the comparisons retained composed refusal but removed the named direct authority finding required by the committed foreign-`G` test. | Two comparisons are the entire mechanism and make the local seam explicit; no proof wrapper or digest. Bounded overlap is preferable to another representation. |
| Existing 38 direct `Dgse` comparisons | One named evidence digest/readback is substituted while its parsed record remains unchanged. | Each of 36 digest mutants and the transaction mutant reached its named mismatch; schema mutation failed parsing. | Retain direct comparisons and closed schema; no digest bag or generic token. |
| Existing consumption composition | A valid upstream selection is paired with a different ACTIVE/CONSUMED lifecycle, receipt, or readback graph. | Consumption and direct-member mutants refuse stale or substituted siblings. | Retain the existing pure composition; it is complementary to upstream core validation. |
| No `Dgse` path or live capability | Structural evidence could otherwise become a selector or certify its own mutation authority. | The exact delta adds only unknown inputs, one pure call, two comparisons, and tests. | Keep `Dgse` non-persisted and context-admission-only. |

The repair is smaller than reimplementing the full E0 relation inside
`selection.ts` and better evidenced than adding a caller-asserted validation
token. It stays inside the bounded Dgse-only semantic repair authorized by
Round 106.

## Exact delta and timeout boundary

The exact
`2baae84b4e03220c01640d010a53919c79118c6f..868bd804e857d3fcc3477c1f71e60d1d7d8eb047`
delta has two paths and 189 insertions with no deletion:

- `packages/contracts/src/selection.ts`: 55 lines adding the existing full
  core validator import/call, its 19 actual inputs, and two direct `G`
  comparisons; and
- `test/contracts/genesis.test.ts`: 134 lines forwarding those inputs,
  rebuilding the two Round 106 graphs, and asserting their refusal plus actual
  successor-core consumption.

`git diff --check` passes. No manifest, registry, schema, authority formula,
export, compatibility surface, path, or timeout changed in this exact delta.
The two user-approved timeout edits are in the reviewed parent and remain
ceiling-only; this round found no regression and does not reopen their prior
authorization.

## Verification evidence

All complete gates ran strictly sequentially. The standalone full suite exited
before bootstrap verification began; bootstrap exited before focused
contracts; focused compatibility began only after focused contracts exited.
No full and bootstrap process overlapped.

1. Planning — PASS, `planning contracts verified`.
2. Board — PASS, `board contracts verified`, against only this repository and
   its registered platform delivery project.
3. Format — PASS, all matched files use Prettier style.
4. Typecheck — PASS with the pinned TypeScript 7.0.2 configuration.
5. Standalone `pnpm test` — PASS, 28 files and 320 tests in 176.43 seconds.
6. Separate `pnpm run verify:bootstrap` — PASS: manifests and registries,
   format, typecheck, 28 files/320 tests in 241.57 seconds, private
   compositions, planning contracts, and source-tree status invariance.
7. Focused ISS-002 contracts — PASS, 18 files and 123 tests in 13.10 seconds.
8. Focused ISS-002 compatibility — PASS, one file and one test in 1.30 seconds.

The restricted planning, board, format, full-suite, and bootstrap wrapper
invocations each remained silent for roughly 125–132 seconds and failed before
their scripts because pinned-pnpm signature verification could not fetch npm
registry metadata. The identical user-approved read-only reruns passed. The
first restricted in-memory esbuild probe failed at child-process creation with
`spawn EPERM`; its identical approved in-memory rerun and all later bounded
probes passed. These are sandbox/network infrastructure failures before
semantic assertions, not contract failures.

## Authorization boundary and final status

`PASS` for exact candidate head
`868bd804e857d3fcc3477c1f71e60d1d7d8eb047`, with parent boundary
`2baae84b4e03220c01640d010a53919c79118c6f`.

This pass authorizes only the bounded Round 106 Dgse semantic-composition
repair at the reviewed exact head. It grants no ISS-002 completion, merge,
release, installation, promotion, runtime mutation, live capability, candidate
self-certification, or consumer authority. Owner teardown archive, owner or
anchor `RETIRED` CAS, rotation integration, packet/live capability, reinstall,
cleanup/fence, recovery, and later producer surfaces remain separately
unauthorized.
