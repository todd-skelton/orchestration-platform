# Round 246 — BLOCK_REPLAN
Exact-head follow-up: the Round 245 Markdown, locator-time, custody/profile, per-OS consumer, acyclic-review, retained-handle, and terminal/two-PR blockers are literally closed.
The remaining publication path is not executable at ISS-022: it requires “the stable installed release” and `stableReleaseSubjectDigest`, but ISS-022 is the prerequisite that selects mechanisms consumed by ISS-020, the sole N0 installation genesis.
Neither ISS-006 nor landed conformance defines an installed-release subject or installed `pnpm` workspace command; only the protected stable harness checkout/bundle exists before N0.
Thus no authorized writer can produce either decision core arm, including BLOCK_REPLAN, without assuming the downstream installation that this probe enables.
Minimal repair: name the existing ISS-006 stable harness/workflow authority as writer, define its subject digest from already-authenticated revision/bundle/contract inputs, and run the two closed output arms from that trusted external bundle.
Trust remains external to candidate bytes; the repaired review/provider joins and exact output census are otherwise executable and fail closed.
Proportionality: replace one unavailable authority/field rather than add a bootstrap release, helper, workflow job, isolation lane, or third publication artifact.
