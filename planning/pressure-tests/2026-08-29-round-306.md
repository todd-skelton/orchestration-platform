# ISS-022 hosted observation pressure review — round 306

## Disposition

`PASS`.

This is the bounded cold implementation review of the uncommitted 11-file,
`+438/-267` diff on `codex/iss-022-hosted-observation` at exact base/HEAD
`70a3af5195929d8ad550f76c1a78c09c8c2cfa2f`.

## Pressure result

The existing protected four-job workflow now selects only the stable, exact
three-row ISS-022 registry. Each observation job authenticates the protected
run context, stable bundle digests, registry row, job/OS/runner token, and exact
candidate snapshot, but executes only the landed stable ISS-022 suite. Candidate
bytes supply no report, environment, time, normalization, receipt, verdict, or
promotion authority.

The stable wrapper constructs the closed raw provider inventory from the exact
hosted runner values and the suite supplies its own `observedAt`. It emits only
the existing six-file observation census. Aggregate rereads the exact stable
registry, canonical environment and report, closed raw manifest, and exact
current-run/current-attempt three-directory census. The ISS-022 report parser
recomputes all 21 normalized rows, selected/null profile authority, environment,
provider-run/job coordinates, and result before generic receipt construction.
Missing, extra, cross-job, stale-attempt, environment/report/manifest, selected/
null, or rebound UNKNOWN-to-PASS substitution therefore refuses.

Literal `UNKNOWN|UNSUPPORTED` survives as the three diagnostic job receipts;
the reducer refuses those receipts and the hosted composition returns them
without writing aggregate bytes. Only three required PASS receipts can create
the existing aggregate. The workflow consequently reaches the unchanged
provider-record and terminal path only on PASS; it adds no candidate verdict or
diagnostic publication arm.

The current-run artifact prefix plus the aggregate's exact directory and file
censuses closes wildcard, missing/extra, and other-attempt drift. Provider-run,
stable bundle, registry, candidate subject, job, environment, raw manifest, and
receipt digests close writer/report/artifact substitution. The unchanged
provider-record and terminal replay continue to require provider job/artifact
identity, live protected-main state, exact retention/time ordering, and a
PASS-only aggregate. Focused generic terminal tests confirm that replacing the
hosted ISS-002 lane does not weaken ISS-002's reusable terminal contracts.

## Scope and proportionality

The 705-line replacement remained reviewable in one sitting. It reuses the
landed suite, registry, environment, receipt, aggregate, provider, and terminal
contracts; most growth is focused hosted mutants. It preserves the four-job
topology and adds no provider-terminal/publication/root command, job, helper,
schema member, issue, roadmap edge, credential, or production mechanism. The
change stays in the hosted adapter/workflow boundary; no engine policy is
introduced. A smaller change would leave the aggregate parsing the removed
ISS-002 report/registry and could not authenticate the ISS-022 diagnostic arm.

Focused evidence passed: the touched hosted plan/observation/aggregate/workflow
and stable-bundle tests (56 tests); hosted record, hosted terminal API, GitHub
artifact/action/terminal compatibility tests (27 tests); repository TypeScript
check; touched-file Prettier check; and `git diff --check`.
