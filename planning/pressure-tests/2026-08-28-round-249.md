# ISS-022 portable-probe-core pressure review — round 249
`BLOCK_REPLAN`.
The 21 IDs, closed record fields, vector/input/observation frame order, workspace/package/bootstrap census, and stable-only pure-parser boundary are correct; the focused suite passes 67/67.
The claimed literal rows are not authoritative: the ledger names `existing-leaf`/`absent-leaf`/`A`/`a` etc., but code silently canonicalizes every entry as a hex string, so another conforming reading produces a different corpus and every physical input digest.
The ledger also never enumerates each row's exact barriers/null arms/payloads, yet `seeds` invents them (including `READY` for all physical, handle, and parser rows); freezing implementation choices cannot replace the public contract.
`CAS_PREDECESSOR_MISMATCH` binds actual readback `41` and successor `42` but has no field fixing the deliberately mismatched predecessor, so materially different mismatch tests share one vector digest.
Minimal repair: add the exact serialized corpus and complete 21-row input table—including the mismatch predecessor—to the ledger/schema, then make constants and deletion tests byte-equal to it.
Proportionality: retain this small parser/scaffold slice; add no executor, helper, isolation, workflow, or production authority while closing those input bytes.
