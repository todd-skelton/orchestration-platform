# Pressure test — round 52 author dispatch-brief repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `0e9529a09ed57279721513eb4c52d8b7b05af688`

This author-side repair answers only `OP-R51-001` through `OP-R51-004`. It
grants no implementation, merge, promotion, or release authority. Round 51's
`BLOCK_REPLAN` remains governing until a fresh independent cold review of the
exact committed repair head returns a clean removal round.

## Bounded repair

- The brief ledger now says one envelope plus exactly three nested records and
  fixes all four `schemaVersion` literals, with explicit family/version mutants.
- Open action, capability, and directive codes are lookup-only values admitted
  by a finite release-reviewed `module-descriptor/v1` catalog. The host renders
  only the installed reviewed template mapping and typed action-plan values;
  it never renders raw keys. The vocabulary oracle covers catalog and runtime
  values as well as declarations.
- The arbitrary schema/digest reference pair is deleted. Each reviewed catalog
  row selects a typed accessor over the same exact step-4 action projection and
  an installed ISS-021 renderer, so no resolver service, path, network lookup,
  or open schema family is introduced.
- The action projection digest is acyclic and the brief action is equal-bound
  to the enclosing step-4 action, capability, subject, and descriptor. Step 7
  additionally equal-binds requested role and the unchanged action/subject/
  descriptor/template facts before rendering and ownership publication.
- Eight directive kinds require at least one substantive `PRESENT` row. Only
  `OPERATOR_ACTION` may use the single closed `ABSENT` form, which cannot coexist
  with a present operator action. Present uniqueness is exactly by
  `(directiveKind,code)`.
- `resourceKind` is deleted because access plus opaque identity is sufficient;
  this avoids a project/adapter vocabulary channel.

## Proportionality and ownership

The concrete threat is unreviewed instruction text or mismatched action facts
entering a worker brief. A finite descriptor catalog plus one directive union
is the smallest surface that keeps planning sections explicit while ensuring
all prose and typed accessors were reviewed in the installed release. Removing
the open reference mechanism and resource class is smaller than constraining
families that have no demonstrated consumer. No authority record, resolver,
storage protocol, renderer implementation, or launch capability moves into
ISS-002.

ISS-011 remains the module/catalog producer, ISS-021 remains the installed
template and typed-renderer owner, ISS-008 remains the dispatch-plan and
rendered-byte binding owner, and ISS-026 remains the routine-step composition
owner. ISS-002 owns only schemas, pure admission/cross-binding validators,
canonical bytes, vocabulary lint, and mutant evidence.

The architecture ABI and routine-cycle descriptions are reconciled to those
bindings. Their issue scopes already own the named production, rendering,
dispatch, and composition behavior, so this repair changes no dependency edge
or implementation footprint for those consumers.

## Required independent attack

Review the exact committed repair head cold. Attack digest cycles; moved step-4
plans, subjects, descriptors, actions, and capabilities; changed step-7 roles;
unlisted and substituted catalog keys; instruction-like codes; adapter terms
including `branch`; arbitrary schema/reference resurrection; absent directives
outside operator actions; present/absent conflicts; duplicate codes; per-kind
minimums; resource-class leakage; hostile arrays/objects; ownership drift; and
self-certification. Confirm the deleted reference/resource-kind surfaces are
not needed by ISS-011/021/008 consumers. Older PASS dispositions do not apply.
