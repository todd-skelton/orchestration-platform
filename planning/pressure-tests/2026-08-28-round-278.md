# ISS-022 physical normalization review — round 278

`BLOCK_REPLAN` against the uncommitted physical-handler slice based on
`ccadacfd106a08ef25ffb9a9c8739b88a29ba040`.

The six-row order, base `Dphys`/`Ddest` recomputation, Windows/Linux/macOS
case and Unicode expectations, retained-handle swap checks, bundle census,
workspace dependency, and absence of publication or self-authority machinery
are correctly bounded. Focused handler and stable-bundle tests pass 11/11.

One hostile-input boundary remains open. Row stability and operation members
are tested by truthiness rather than as exact booleans, and swap rows do not
validate the closed operating-system enum. More directly, the
`operationApplied=false` UNSUPPORTED arm returns before validating any nested
root or locator record, so proxy, accessor, malformed, or contradictory nested
facts can survive normalization as an accepted UNSUPPORTED execution. Existing
mutants cover outer proxies and semantic movement but not these value-type and
nested-record substitutions.

Minimal repair: snapshot or otherwise validate every row and nested value as a
closed, proxy-free data record before disposition; require exact booleans, the
closed OS enum, and coherent nullable operation error values on every arm.
Then add deterministic wrong-type/invalid-OS and nested proxy or malformed
UNSUPPORTED mutants. Preserve the current six-row reducer, recomputation,
package/lock/census footprint, and PASS/UNSUPPORTED/UNKNOWN rules.

Proportionality: this is a local validation repair to the existing handler and
tests. A new parser, writer, receipt, workflow, registry, or authority surface
would be larger and outside this slice.
