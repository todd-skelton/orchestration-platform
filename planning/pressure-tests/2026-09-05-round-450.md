# Pressure round 450 — ISS-011 dispatch-catalog census consumer evidence

Status: bounded ISS-011 consumer-test artifact, absolute test-artifact attempt
2 after the read-only Round 450 audit 1. The protected base is
`73ab243ee83d75dbbfb2269b5e629f0dbb4e06f4` (PR #266 disposition, with no
ISS-011 issue closure). This packet is not source completion, a review, hosted
execution evidence, registry admission, or authority.

## Lineage and scope

Rounds 401 and 434 through 446 remain cumulative inputs. Round 401 retains the
optional disposition ABI and closed literal families. Rounds 435 and 437 remain
abandoned source candidates, including their exact heads and reviews; Round 438
replanned their admission boundary and Round 439 retained its quarantine.
Rounds 443, 444, and 446 retain their definition and planning-source attempt
ledgers. Round 448 and PR #266 add the source-owned review disposition but do
not close ISS-011. Renaming an artifact or branch does not reset that history.

Audit 1 found an executable gap in acceptance item 9: the existing direct
`validateDispatchCatalog` seam already owns closed one-through-256 catalog
rows, resolver-key uniqueness over `(actionKind, capabilityName,
directiveKind, code)`, declared-pair uniqueness, and exact declared/catalog
pair-set equality, but its complete relation matrix was not isolated at that
seam. This attempt changes exactly that test and this receipt. It adds
structurally valid direct-validator cases for accessor-only, template-only, and
combined accessor/template substitutions under one resolver key; a valid
distinct resolver key; duplicate declared pairs; a valid declared-only pair;
and a valid catalog-only pair. The baseline catalog and declared pair remain a
valid empty finding result.

## Non-goals and acceptance evidence

This is test-only evidence for the registry/catalog consumer relation. It does
not change module source, definitions, drafts, manifests, generators, static
registries, consumers, package/build/scripts, release-manifest proof, runtime
or authority activation. Delivery and repair remain undefined. There is no
zero-consumer exemption, and tests grant no authority.

Acceptance requires the direct seam to report only
`catalog:resolver-key:duplicate` for each same-key variation; to accept the
distinct-key and baseline cases; to report only `declaredPairs:duplicate` for a
duplicate valid declared pair; and to report only `declaredPairs:mismatch` for
each valid one-sided pair-set change. The vectors deliberately retain closed
record shapes and valid directive/accessor literals so a structural or order
failure cannot satisfy the relation assertion.

## Verification, footprint, and review attack surface

Author verification is limited to `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm planning:board-check`, `git diff --check`, and
exact-head two-file inspection. Dependencies may be materialized offline with
frozen lockfile and ignored lifecycle scripts if required. No local Vitest,
test, module, native, compiler, addon, probe, or lock experiment is allowed.
Focused and full execution on hosted Linux, macOS, and Windows remains external.

The exact footprint is:

- `test/contracts/dispatch.test.ts`
- `planning/pressure-tests/2026-09-05-round-450.md`

Review must attack whether a changed accessor or template evades resolver-key
uniqueness; whether a changed code proves a genuinely distinct key; duplicate
or missing pair relations hidden by malformed rows; incorrect failure-category
expectations; a baseline that is no longer valid; production, registry,
manifest, generator, or authority claims smuggled into a test-only packet; and
any attempt-history reset. Audit 1 is complete; source 2 and independent review
3 remain external gates. Routing follows row 2 routine bounded tests on
Terra/medium, placement is provisional, budget intent is at most $15, and this
lane transcript is the routing record.
