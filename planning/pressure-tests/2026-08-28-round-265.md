# ISS-022 base physical session follow-up — round 265
`PASS`.
One public session now canonicalizes and opens the root once, creates/readbacks one namespace after the handle is live, runs `PHYSICAL_EXISTING` then `PHYSICAL_ABSENT_LEAF`, and closes the retained handle only after both rows or failure.
Each row brackets its leaf observation with fstat/lstat/stat/statfs/realpath/namespace root readbacks through that same handle; derivation remains null unless root, locator, and leaf are stable.
Tests join the existing-row after snapshot to the absent-row before snapshot, require equal `DhostNamespace` and root-readback digests, and require distinct landed `Dphys` identities.
Constructed ENOENT, regular-file checks, canonical leaves, path omission, reuse/overlap refusal, and raw non-PASS scope remain intact; hostile alias/swap behavior was not added.
Focused tests pass 3/3 and root typecheck passes; the session refactor is proportional to Round 264.
