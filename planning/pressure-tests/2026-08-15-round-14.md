# Pressure test — round 14

**Disposition:** `BLOCK_REPLAN`

The fourteenth independent review found four ownership/ordering blockers. The
39-issue packet remains unregistered pending a fresh review of the repairs.

| Finding | Repair applied |
| --- | --- |
| `OP-R14-001` — The Codex probe could test an unrelated credential topology because it did not depend on the `ISS-031` selection. | Added direct `ISS-031 → ISS-023`; bound all store/endpoint/handle/impersonation/replay controls to the exact selected artifact; synchronized the roadmap and epic DAG. |
| `OP-R14-002` — Cross-principal broker creation, executable, service lifecycle, bootstrap availability, provisioning, and teardown had no production owner. | Defined the private broker executable and explicit Windows SCM/macOS LaunchDaemon/Linux systemd topology; made `ISS-032` own lifecycle/IPC implementation, `ISS-020` own reviewed bootstrap plan/install/verify/remove commands, and `ISS-034` own production execution before N0 provisioning/install, including cold reboot and removal recovery. Added a third non-alias esbuild target. |
| `OP-R14-003` — N1 could not add a fifth static module import without an unowned composition edit. | Assigned `modules/build/generate-registry.mjs` and ignored generated registry to `ISS-011`; made the root self-host build consume it, `ISS-033` prove the four-entry N0 registry, and `ISS-015` regenerate the five-entry N1 registry from its one manifest-row change without adapter/composition edits. |
| `OP-R14-004` — Release recovery was runnable before its broker client/API owner. | Added direct `ISS-032 → ISS-014`; froze the exact `RecoveryAdminClientV1` and private-brand injection contract; synchronized the roadmap and epic DAG. |

## Re-review focus

- Confirm broker service lifecycle exists before production binding and N0
  recovery, with no same-user endpoint bypass.
- Confirm N0/N1 module registries are generated deterministically from their
  respective immutable manifests.
- Confirm new direct dependencies do not create a cycle or hide a prerequisite.
