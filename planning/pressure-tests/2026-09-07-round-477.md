# Pressure round 477 - normal controller delivery extraction

ISS-073 completed with independent review and actual three-OS proof. PRs #330
and #331 then exposed the same ordinary controller handoffs: three static gates,
approved epic/issue draft mirrors, the board gate, branch and draft-PR
publication/update, exact-head hosted observation, ordinary squash merge and
cleanup. The temporary external helpers remain source-only evidence. Their
project IDs, issue IDs and policy do not enter the portable transition logic.

Source attempt 1 stopped on a miscopied encoded presence preflight without source
edits; its failed receipt remains. Reviewer reservation 2 was not launched.
This is source attempt 2 and independent review reservation 3 under ISS-074's
ordinary eight-attempt ceiling. ISS-073's closed eight-attempt history is neither
reopened nor reset. The running pilot remains pinned to externally reviewed
`6beae9e`; main `6e86338` is tree-equivalent and may be the successor's exact
base, never self-activation or self-certification authority.

Pilot run `iss074-controller-delivery-a2-r3-20260907` has author role and exact
base `6e86338caa4243fd31ba65a5fc6a1072098e74b4`. Its literal native presence
preflight reported all first 13 credential-sensitive names absent. `HTTP_PROXY`
and `HTTPS_PROXY` were present only as the permitted advisory sandbox controls;
their values and networks were not inspected or changed.

## Source plan and authority boundary

Add one private `deliver.mjs` composition. It consumes the reviewed pilot's
narrow candidate/reviewer records once, then persists its own source evidence
outside all checkouts. A strict external controller authorization binds run,
repository, stable controller revision, exact candidate head and the five
mutation classes. The portable engine knows
neither GitHub, planning identities, gate names, mirror rules nor merge policy;
an explicit private self adapter supplies those facts.

Each provider mutation has durable intent first and provider observation both
before and after. Unknown or partial state stops closed. A later invocation
reconciles before any retry; confirmed PR or merge state prevents duplicate
mutation. Gates are policy-phased because `planning:board-check` is meaningful
only after approved draft mirroring. Merge requires every configured hosted
check exactly once and passing at the reviewed head. Cleanup is last, validates
the exact selected worktrees/branch and surviving controller checkout, and uses
delivery records after the removed worker checkouts no longer exist.

## Independent attack and evidence still required

Hosted synthetic tests execute the actual portable composition on Windows,
macOS and Linux. Attack malformed authority/records, self-review, head drift,
missing/duplicate/skipped checks, uncertain publication or merge, partial
cleanup, duplicate publication/merge, policy leakage into portable code, and
post-cleanup restart. Review every assertion and the full exact candidate.

Source may run installed static TypeScript, pinned Prettier over changed
executables and planning entrypoints. It must not run local tests, native builds,
addons, probes, locks or networks. The external controller owns all four package
gates, mirror/publication/merge authority and hosted proof. A subsequent useful
measured improvement, with actual action counts and restart evidence, is still
required; fixtures alone do not establish the real-work outcome.

Falsifiable prediction: the next useful issue needs none of the enumerated manual
bridges. A manual handoff, ambiguous success, wrong-head check, duplicate
provider mutation or cleanup touching unrelated state falsifies the extraction
and becomes the next measured correction.
