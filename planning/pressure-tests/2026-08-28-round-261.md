# ISS-022 physical identity repair follow-up — round 261
`PASS`.
The custody-instance derivation now requires a runtime string before lexical validation and framing; numeric, boolean, and null job IDs have executable refusal tests and cannot alias text bytes.
No domain, frame order, integer projection, physical-record field, canonical-leaf conversion, `Dphys`/`Ddest` route, export, or filesystem scope changed.
The pinned component and composed digest vectors remain exact; focused tests pass 6/6 and root typecheck passes.
The one-line guard plus three hostile scalars is proportional to Round 260.
