# ISS-022 hosted root-portability review — round 255
`PASS`.
The stable parent now lstat-refuses a symlink/non-directory leaf, realpaths both custody and source roots, rejects either containment direction, and passes only the canonical custody root as child argv and cwd.
Thus macOS `/var`→`/private/var` and Windows short/long or case spellings converge before comparison, while real checkout overlap still refuses rather than becoming an alias bypass.
The worker retains its independent canonical-root/source-overlap check; no candidate input, PASS/profile authority, cleanup rule, or raw-event trust boundary changed.
Focused executor tests pass 10/10 and the probe package typechecks; the 21-line repair is proportional and adds no fallback, retry, helper, workflow, or platform-specific authority.
