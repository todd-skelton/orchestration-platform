# ISS-003 configuration-contract implementation pressure review — round 305

`BLOCK_REPLAN` at exact base/HEAD
`c974d5dce218fef759cc40c944483f685b9c8ec5`.

The four-file dependency-first slice is reviewable in one sitting and stays
inside `packages/contracts` plus its focused test. It adds no loader, CLI,
static-runner, filesystem, authority, roadmap, board, adapter, Git, or GitHub
behavior. The source/path-token separation, scalar reuse, closed records,
seven-member `fieldSources` shape, success-result schema binding, diagnostic
shapes, snapshots/frozen values, exact-v1 compatibility, exports, registry
families, and representative canonical byte goldens otherwise hold.

Two public-v1 fail-open cases block landing:

1. `configuration-provenance/v1` accepts impossible provenance. ISS-003 says
   every member except `stateRoot` comes only from the project source, but the
   parser accepts `CLI`, `ENVIRONMENT`, or `DEFAULT` for any of those six
   members. A focused probe returned `ok:true` with `adapterId:"CLI"`,
   `capabilityNames:"DEFAULT"`, `leaseFreshnessMs:"ENVIRONMENT"`, and similar
   substitutions. Close those six members to `PROJECT`; retain the four-way
   source union only for `stateRoot`, and add one mutant per forbidden arm.
2. `orchestration-command-result/v1` closes command syntax, not command
   identity or placeholder ownership. A focused probe accepted
   `command:"git push"` with
   `{code:"CAPABILITY_NOT_IMPLEMENTED",owner:"ISS-999"}`. That contradicts the
   exact canonical command-row/registration-owner boundary and silently admits
   the adapter vocabulary this contract must exclude. It also disagrees with
   the registry vocabulary, whose command closed values list only the two
   config successes even though the parser test admits `project snapshot`.
   Close failure commands to the existing manifest rows and bind
   `CAPABILITY_NOT_IMPLEMENTED` to each placeholder row's exact owner using one
   contract-owned census (not a second runtime registry); add unregistered,
   implemented-command, wrong-owner, and registry-drift mutants plus an
   owner-bearing canonical-byte golden.

## Proportionality

The in-scope threat is durable bytes claiming provenance or command ownership
that no admitted invocation/registration could produce. Deleting semantic
binding makes both probes survive. The smallest repair is two closed lookup
tables and focused mutants inside this contracts slice; loader/dispatcher work
would be larger and would leave independent readers fail-open. No new schema,
command, loader mechanism, or authority surface is warranted.

Focused evidence: the three configuration/compatibility/vocabulary test files
pass (11 tests); the contracts TypeScript project, four-file Prettier check,
and `git diff --check` pass. The two adversarial parser probes above return
`ok:true` and are the named new blocker.
