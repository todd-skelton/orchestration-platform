# ISS-002 pressure test — round 162

## Review basis

This is a fresh independent cold exact-head removal/reconciliation review of the
recovery attempt-log packet at exact initially clean and pushed head
`7ab394af336a72f9a4003089b5ca95826f1903da` on
`iss-002-versioned-contracts`, with exact parent
`9676c7ff9472286699a7dc777186d38b45081980`. Local `HEAD` and a read-only
`ls-remote` of `origin/iss-002-versioned-contracts` both resolved to the reviewed
digest. The exact repair changes only `docs/architecture/state-machines.md` and
`docs/contracts/compatibility.md`: 37 insertions and 39 deletions.
`git diff --check HEAD^ HEAD` passed.

I read the repository instructions, work model, sole contract authority,
supervisor contract, public compatibility contract, state machines, current
ISS-002/ISS-004/ISS-005/ISS-006/ISS-027/ISS-030/ISS-032 drafts, Round 161,
exact diff, and current reservation, descriptor, pointer-position,
selected-object locator, and compatibility surfaces cold. I also searched the
complete current `docs/` and `planning/drafts/` packet for stale attempt-log
tag/genesis-predecessor/accumulator/rolling/checkpoint/summary/TOMBSTONE and
copied-producer semantics. This review used no authoring or implementation
authority and did not read or modify any external repository, project, or
issue.

## Disposition

`PASS` for the repaired recovery attempt-log planning packet at exact reviewed
head `7ab394af336a72f9a4003089b5ca95826f1903da`.

OP-R161-1 is closed. State machines and public compatibility now match the sole
authority, supervisor, ISS-002, and ISS-030. No definite semantic,
constructibility, acyclicity, proportionality, evidence, engine-boundary, or
self-certification blocker remains in this bounded parser/digest/path/
supplied-chain slice.

## Authoritative reconciliation

The state-machine first-IN_PROGRESS row now says ordinal `"0"` and null
predecessor record digest. It no longer gives the record a tagged genesis
predecessor. The supervisor, sole authority, and ISS-002 use the exact member
name `predecessorRecordDigest`; ISS-030 consistently says the first record has
no prior TERMINAL record.

The current public compatibility contract now fixes all of the repaired
relations:

- reservation has exactly RESERVED, CONSUMED, and TERMINAL value branches;
  later removal uses the common pointer tombstone after archive composition,
  and no reservation-schema TOMBSTONE exists;
- `attempt-log/v1` has exact nine-member IN_PROGRESS and fifteen-member TERMINAL
  branches, with terminal-only members absent from IN_PROGRESS;
- `Dattempt` is one canonical-record frame under `attempt-log/v1`; there is no
  raw genesis/later tag or repeated predecessor/ordinal frame part;
- there is no accumulator, rolling digest, checkpoint, separate terminal
  summary, copied producer epoch, or copied reservation/proof bag;
- the pure supplied-chain validator accepts a structurally valid prefix and
  rejects malformed genesis/edges, gaps, forks, reorder, and global ID reuse;
- later ISS-004 selected-current composition alone can reject a valid truncated
  suffix or stale prefix; and
- producer authority stays in each authenticated selected common proposal/tip
  graph rather than becoming an attempt-record member.

Repository-wide current-authority/draft searches found no surviving
“tagged genesis predecessor”, genesis/later attempt-log raw tag, tagged
`Dattempt`, tagged attempt-log digest/golden, full-chain pure-validator
truncation claim, accumulator/rolling/checkpoint/summary branch, or
reservation-schema TOMBSTONE requirement. References to an “explicit
attempt-log genesis record” in credential/ISS-030/ISS-032 mean the actual first
IN_PROGRESS record and do not introduce a tag or non-null predecessor.

## Reservation tagged genesis remains correctly distinct

The retained reservation predecessor-key formula is intentionally separate:

```text
Kreservation = recovery-attempt-reservation-predecessor/v1(
  transactionId,
  sourceToken,
  0x00 genesis | 0x01 selected predecessor,
  selected predecessor Dt/Dv/Dr when present)
```

Its tag distinguishes an all-null first reservation predecessor from a complete
selected TERMINAL predecessor triple before an attempt is reserved. It is not
`attempt-log/v1`, not `Dattempt`, and not the first record's
`predecessorRecordDigest`. Current remaining “tagged genesis” references occur
only in this reservation identity/path context. Deleting that reservation
branch tag would merge competition buckets; deleting the removed attempt-log
tag changes no parsed-record discriminator. The two proportionality outcomes
are therefore consistent rather than contradictory.

## Complete repaired-ledger re-audit

IN_PROGRESS has exactly nine sorted members. TERMINAL has exactly those nine
plus attachment tip, process-terminal observation, channel-denial,
revocation, disposition, and outcome identities in their sorted positions.
Terminal-only members are absent, never null. Lifecycles, sources,
dispositions, UUIDv7, SHA-256, timestamps, and bounded canonical decimal
ordinals are closed and total.

Genesis is necessarily IN_PROGRESS at ordinal `"0"` with null predecessor.
Every positive ordinal has one SHA-256 predecessor. Every later record requires
`Dattempt(prior)`, adjacent ordinal, and prior `recordedAt <= next.recordedAt`.
Only IN_PROGRESS→TERMINAL and TERMINAL→IN_PROGRESS append. Same-attempt
terminalization preserves transaction, source, attempt ID, and descriptor;
the next IN_PROGRESS preserves transaction/source and uses a new attempt ID.

The supplied-chain validator's global set is scoped correctly. It checks a new
IN_PROGRESS ID against all prior IDs, so `I(A),T(A)` succeeds while
`I(A),T(A),I(B),T(B),I(A)` refuses. It does not reject the required same-attempt
IN_PROGRESS→TERMINAL pair and does not use UUID time as ordering authority.

The pure validator honestly returns any valid nonempty supplied prefix. It has
no selected-current input and makes no fullness/currentness claim. Later
ISS-004 composition locates the selected pointer graphs under a live external
handle and requires the supplied tail to equal the authenticated selected
current value. That is the smallest available fact that can distinguish a
complete earlier prefix from a now-truncated suffix.

The standalone digest is exactly:

```text
Dattempt = attempt-log/v1(canonical attempt-log/v1 record bytes)
```

The closed bytes already commit lifecycle, bounded ordinal, and nullable
predecessor unambiguously. One domain plus one canonical part distinguishes a
record from other contracts; repeated branch, ordinal, or predecessor parts
add no surviving substitution test. There is no deployed digest migration,
alternate helper, untagged identity, or second digest surface.

Each standalone record path is constructible from its parsed transaction,
source, attempt, ordinal, and lifecycle. The common transaction/source
attempt-log pointer plus recursively locatable selected-object aliases supplies
selected prior graphs without directory enumeration, a mutation-ID locator,
index, inventory, accumulator, or second head. The VALUE position is the exact
empty-parts record. Archive and common tombstone composition remain later
work; bare absence, deletion, retention, and compaction grant no state.

`descriptorDigest` transitively and constructibly binds the selected RESERVED
reservation. Log transaction/source/attempt constructs the descriptor path;
exact descriptor bytes recompute its digest; and the descriptor directly binds
the reservation predecessor key and selected RESERVED `Dt`. Copying reservation
`Dp/Dv/Dr/Dt`, release, gate, fence, process-start, or producer-epoch siblings
into each log record adds no locator or independent replay discriminator.

The dependency order remains acyclic:

```text
selected RESERVED reservation
  -> immutable LIVE descriptor
  -> selected IN_PROGRESS log
  -> selected CONSUMED reservation
  -> selected TERMINAL log
  -> selected TERMINAL reservation
  -> later archive/common tombstone
```

No log record contains the later reservation value or tip that names that same
log selection. `Dattempt` points only backward through the predecessor digest.
A next reservation keys from the selected prior TERMINAL graph before its new
descriptor/log. Crash recovery follows authenticated common graphs and refuses
missing or conflicting evidence.

Terminal attachment and revocation remain independently nullable structural
cells; this bounded parser has no authenticated fact establishing an iff.
Process-terminal, channel-denial, applicable-revocation, and
disposition-specific outcome digests remain opaque until ISS-005/ISS-030/
ISS-032 parse independently reviewed source records, bind chronology and
applicability, and prove the selected disposition. A SHA-shaped member, parser
success, candidate/worker report, or self-asserted timestamp grants none of
those facts.

The slice adds no persistence IO, proposal/CAS, UUID allocation, capability,
process operation, broker call, command, package, runtime selection, archive,
or tombstone implementation. Engine contracts use opaque identities and closed
platform vocabulary; Git, GitHub, provider, native endpoint, argv, and consumer
policy do not enter the record. Stable external mutation authority and live
ISS-004 handles remain mandatory. A candidate never certifies, selects, or
promotes itself.

## Proportionality

| Mechanism | Concrete in-scope threat | Deletion result | Smallest retained shape |
| --- | --- | --- | --- |
| Closed 9/15 records | Partial, future, null terminal-only, or crossed bytes become state. | Deleting closure/member/scalar admits a direct mutant. | Two flat branches. |
| Predecessor plus ordinal | Gap, reorder, or forked record becomes adjacent. | Deleting either relation admits an edge mutant. | Each fact once in canonical bytes. |
| Global ID set | A completed UUID is reused after an intervening attempt. | Deletion admits A/B/A. | One set in the existing O(n) walk. |
| Structural prefix validator | Malformed supplied ancestry passes. | Deleting genesis/edge checks admits a malformed prefix. | Pure prefix validation with no fullness claim. |
| Authenticated final equality | A historical valid prefix is used as current. | Deletion admits selected-tail truncation. | One equality in later ISS-004 composition. |
| One-frame `Dattempt` | A canonical record crosses digest domains. | Removing domain/frame admits reuse; repeated fields kill no extra mutant. | One domain-separated canonical part. |
| Reservation predecessor tag | Null genesis and a selected predecessor share a reservation bucket. | Removing branch/triple parts admits wrong lineage. | Separate five-input `Kreservation`. |
| Terminal opaque identities | A downstream terminal fact is substituted independently. | Removing one leaves later composition unbound. | One digest per distinct source fact; no proof bag. |
| Stable selection boundary | Candidate-authored structural bytes certify themselves. | Removing live external selection admits self-selection. | No mutation surface in this slice. |

The predicted implementation footprint remains one contracts module, existing
pointer-position dispatcher, public serializer/export/compatibility wiring,
and focused tests. No dependency edge or runtime package is added. The exact
two-document repair deletes stale authority rather than adding machinery.
Draft/board consistency is reported by the required gate below.

## Verification

Required gates were run strictly serially with increased test timeouts; the
standalone full suite exited before bootstrap began:

The package-manager shim's pre-script version switch remains unavailable in
this environment because registry verification/fetch is blocked, as directly
observed in Round 159. The permitted same-lockfile direct equivalents were used
again. Read-only board access and bundled tool subprocesses received only their
bounded spawn permission.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS:
   `board contracts verified`; it read only this repository and its registered
   delivery project.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 38 files and 413 tests in 79.65 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 38 files and 413 tests in 88.43 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This clean removal/reconciliation round authorizes the exact recovery
attempt-log implementation slice described by the sole ledger and ISS-002 at
reviewed head `7ab394af336a72f9a4003089b5ca95826f1903da`: the exact two branches,
record path, one-frame digest/serialization, empty VALUE position, pure edge and
supplied-chain validators, compatibility/public wiring, goldens, and deletion
evidence.

It does not authorize selected-family/currentness composition, observation,
attachment, launch-pointer, archive/tombstone, persistent IO, proposal/CAS,
capability, UUID allocation, process operation, broker call, command, package,
or runtime behavior.
