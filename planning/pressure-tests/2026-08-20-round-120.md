# ISS-002 pressure test — round 120

## Review basis

This is a fresh independent cold implementation removal review of exact
initially clean head `8004bc8a8f7a66d7a000cd6efbf001b9c045b135` on
`iss-002-versioned-contracts`, with exact parent
`70226ac89ca636fd1149596c08390cb1a62b5cc9`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was only `AGENTS.md`, `docs/planning/work-model.md`, the
sole implementation authority in `docs/architecture/contract-decisions.md`,
the registered `ISS-002` draft, rounds 118–119 as the authorization and
blocking findings under retest, the common pointer/runtime/registry/definition
contracts and focused tests, and the exact parent-to-head diff. No Chase Sets
repository, project, issue, board, backlog, consumer source, or other consumer
system was read or touched.

The exact diff changes only `packages/contracts/src/pointer.ts` and
`test/contracts/gate-fence.test.ts`: 208 insertions and 2 deletions across two
paths. `git diff --check` passed. The checkout was clean before this pressure
record was written.

## Disposition

`BLOCK_REPLAN` for the bounded structural cleanup-gate/recovery-fence
implementation gate.

Round 119's public-constructor bypass is repaired. Both common position
construction and mutation identity now refuse malformed cleanup/fence VALUE
positions, valid positions retain the pinned family domains, and the repair
adds no authority or runtime surface. Most of the requested focused history
matrix is also present.

The committed evidence is still not deletion-grade for two cleanup-history
rules, however. A strict-time mutant and a deleted-genesis mutant both survive
the committed cases. The smallest repair is test-only and remains in the
existing focused gate/fence test file.

## Round 119 blocker 1 retest: common position bypass removed

`computePointerPositionDigest` now dispatches
`ACTIVATION_CLEANUP_GATE` and `ACTIVATION_RECOVERY_FENCE` VALUE evidence
through an exact nested snapshot of `parts` with only `ordinal` and
`rootDigest`, then applies the shared safe-decimal and SHA-256 scalar rules
before framing. It refuses TOMBSTONE for these still-VALUE-only families.
`computeMutationId` continues to call that common helper, so it inherits the
same refusal. The specialized wrappers first parse the family VALUE position
and then use the common path, leaving one digest domain and no alternate
authoritative encoding.

The committed test matrix attacks missing `mode`, missing `parts`, outer
extras, both missing nested members, nested extras, TOMBSTONE, noncanonical and
overflow ordinals, and an invalid root through both specialized parsers, both
common family calls, and both mutation-ID calls. Valid specialized/common
digests remain equal, and the cleanup/fence position-domain goldens remain
`24df04b494e6c8c706a87ba76d5de290759b679f0462879a17ad641c3da2fe63`
and
`7ce6a5e32c99a8fc09a66ac637ace1d2cc17a4fbab5f2583aed2418a83e62a69`.

A separate in-memory exact-HEAD bundle probe added null and array `parts` to
that matrix. For each family, all 24 attempted malformed calls (twelve shapes
through the common digest and mutation ID) refused, while the valid common
digest and mutation ID succeeded. The probe wrote no file.

Deleting the new common nested census or either scalar check is killed by the
committed common and mutation cases. The former Round 119 bypass is therefore
removed.

## Blocking finding: two cleanup history mutants survive

### 1. Inclusive prior time has no cleanup positive boundary

The cleanup validator correctly implements inclusive time with
`head.recordedAt < priorRecordedAt`. The committed cleanup histories give the
ordinal-zero head the same time as the root, but every later cleanup head is
one second later than its prior head. The isolated cleanup negative sets the
first head to `12:01:00.000Z` and the second to `12:00:30.000Z`; it proves a
true regression refuses, but it does not distinguish `<` from `<=`.

An in-memory source mutant changed only the cleanup prior comparison from `<`
to `<=`. The original accepted a correctly linked two-head cleanup history
whose two heads both used `12:00:00.000Z`; the mutant refused it with
`1:recordedAt:before-prior`. Every committed positive cleanup interior time is
strictly increasing, while the committed negative still refuses under either
comparison. The focused suite therefore does not kill this strict-time mutant.
The fence does contain an equal-prior positive, but its validator is a separate
function and cannot certify the cleanup comparison.

The smallest repair is one positive cleanup history with ordinal one correctly
linked to ordinal zero and `recordedAt` exactly equal to its prior head (and at
or after the root), expecting no issues.

### 2. Cleanup genesis-cell deletion is not isolated

Cleanup history must start at exactly `PENDING/NOT_PUBLISHED`. The committed
start-omission case uses `history.slice(1)`, so the new first record still has
ordinal `1` and a non-null prior digest. It remains refused if the explicit
cleanup genesis-cell comparison is deleted, because dense-ordinal and null-
prior checks independently reject it.

An in-memory source mutant removed only the cleanup ordinal-zero lifecycle/
publication comparison. A dense one-record history at ordinal zero with null
prior, the correct root and inclusive time, but `PENDING/PUBLISHING`, changed
from `0:transition:not-genesis` under exact HEAD to accepted under the mutant.
The committed sliced-start input still refused under that mutant with only
`0:ordinal:not-dense` and `0:priorHeadValueDigest:mismatch`. Thus deletion of
the structural genesis comparison remains green.

The smallest repair is one correctly rooted, dense, ordinal-zero, null-prior
cleanup head in any admitted non-genesis cell, expecting exactly
`0:transition:not-genesis`. No implementation, schema, or authority change is
needed.

## Remaining deletion and structural assessment

The rest of the Round 119 evidence repair is coherent:

- Both common head `Dv` byte goldens are pinned: cleanup
  `8773addd78800f0922e863ff8fc1b44d9b98e46c14a93687f82e09ef105cb831`
  and fence
  `56c1131812f99d922ef0a85253fded1e5ece049c3ebbcf23de65dc264a11030a`.
- Empty and over-six cleanup histories and empty and over-two fence histories
  each have a discriminating bound assertion. Valid one-through-six cleanup
  prefixes and one/two fence prefixes remain accepted.
- Cleanup and fence each have isolated dense-ordinal, prior-common-`Dv`, root,
  before-root, and before-prior refusals. Fence additionally has a normalized
  non-genesis start. Reorder, gap, stale/forked prior, wrong path-instance
  digest, detectable start/interior omission, and over-bound cases refuse.
- Correctly relinked illegal-edge and self-loop mutants isolate transition
  refusal for both families. The five legal cleanup paths collectively cover
  all ten cells and all twelve legal edges, and every supplied valid prefix is
  accepted. The sole fence edge and both legal prefixes are accepted.
- Parser/validator hostile reflective inputs remain total through the common
  snapshot primitives. Shared runtime tests cover proxies, symbols, accessors,
  non-enumerables, exotic and cross-realm prototypes, array holes, custom
  iterators, and mutable/sealed/frozen dense arrays.

The exact implementation remains within round 118's structural authorization.
It adds no durable locator, selected-tail completeness/currentness, first-ever
archive lineage, tombstone position, cleanup archive or recovery-authorization
bytes, composed upstream equality, exhaustive composition proof matrix,
filesystem read or mutation, broker call, capability, release selection,
promotion, runtime transition, or self-certification. It adds no package,
command, pointer kind, path, second head identity, coordinator, proof tree,
inventory, migration, or consumer vocabulary. Structural success still grants
no mutation authority.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible disposition |
| --- | --- | --- | --- |
| Exact common gate/fence VALUE dispatch | Open nested parts let a caller mint a family position and mutation ID from malformed bytes. | The prior bypass is now refused by specialized, common, and mutation APIs. | Retain the eight-line bounded common check; no second digest or schema is needed. |
| Distinct roots and common head `Dv` | Cross-family or untagged identities break root and lineage identity. | Root and both common-head goldens kill a domain or framing substitution. | Retain the existing formulas and goldens. |
| Bounded relative histories | Gaps, forks, reorder, wrong roots, bad ordinals, illegal edges, or time regression can fake structural progress. | Most comparisons now have discriminating mutants; strict cleanup prior time and deleted cleanup genesis still survive. | Add only the two focused cleanup cases above. |
| Valid shorter-prefix acceptance | A pure supplied-record validator cannot prove selected-tail currentness. | Rejecting a valid shorter prefix would invent unavailable authority. | Retain all valid prefix positives and the explicit currentness deferral. |
| Deferred locator/archive/composition | Durable currentness, deletion/loss, first-ever lineage, and upstream authority require authenticated inputs absent here. | Implementing them would trust a caller or invent undecided schemas. | Preserve deferral; add no runtime or composition mechanism. |

The two requested tests are the smallest repair: they defend rules already in
the implementation and ledger and add no mechanism at all.

## Verification evidence

The required gates ran strictly sequentially. The standalone test process
fully exited before the separate bootstrap verifier started. Only identical
direct underlying commands were rerun after package-manager registry or
sandbox subprocess failures.

1. `pnpm run planning:check` did not reach the checker during the unavailable
   signed-registry interval and produced no checker output; identical
   `node scripts/planning/check.mjs` passed with
   `planning contracts verified`.
2. `pnpm run planning:board-check` failed closed because the registry signature
   could not be verified; the direct command then hit sandbox `spawn EPERM`,
   and the approved identical `node scripts/planning/board-check.mjs` rerun
   passed with `board contracts verified`.
3. `pnpm run format:check` failed at the same registry check; direct execution
   hit sandbox `spawn EPERM`, and the approved identical
   `node scripts/format.mjs check` rerun passed: all matched files use Prettier
   style.
4. `pnpm run typecheck` passed (`tsc --noEmit -p tsconfig.json`).
5. Standalone `pnpm test` failed at the registry check; direct Vitest hit
   sandbox `spawn EPERM`, and the approved identical
   `node node_modules/vitest/vitest.mjs run` rerun passed 29 files and 361 tests
   in 85.86 seconds. The process fully exited before step 6 started.
6. Separate `pnpm run verify:bootstrap` failed at the registry check; direct
   verification hit sandbox `spawn EPERM`, and the approved identical
   `node scripts/verify/bootstrap.mjs` rerun passed: manifests and registries,
   formatting, typecheck, 29 files and 361 tests in 84.28 seconds, private
   compositions, planning contracts, and source-tree status invariance.

After the required sequence, the focused direct Vitest run over
`gate-fence.test.ts`, `lifecycle.test.ts`, `runtime.test.ts`, and
`compatibility.test.ts` passed 4 files and 21 tests in 1.06 seconds. The
read-only in-memory public-API and two source-mutant probes described above
then completed without writing a repository file.

Green regression gates do not cure the two surviving deletion mutants.

## Implementation authorization boundary

`BLOCK_REPLAN` means exact head
`8004bc8a8f7a66d7a000cd6efbf001b9c045b135` does not pass the independent
implementation gate. The malformed common-position bypass is fixed; the
smallest bounded repair is only the two focused cleanup-history tests above.
The repaired exact head returns once to the ordinary independent removal
review. No ledger, implementation, or footprint expansion is authorized.

This record grants no merge, release, promotion, installation, persistence
lookup, selected-tail completeness/currentness, first-ever archive fact,
composed validation, filesystem mutation, tombstone/archive construction,
broker call, capability, runtime transition, or self-certification authority.
A worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-120.md`; no source, test, draft,
issue, roadmap, board, or other planning file was edited, and no commit was
created.
