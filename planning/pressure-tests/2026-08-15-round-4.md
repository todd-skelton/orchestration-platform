# Planning pressure test — round 4

Disposition received: `BLOCK_REPLAN`.

The fresh independent review passed the mechanical graph and found six deeper
authority/lifecycle omissions. Registration remained frozen.

| Finding | Replan disposition |
|---|---|
| `OP-R4-001` missing/unknown worker role was not fail-closed | Settled a closed role enum, explicit implementation authority, pre-launch refusal, and omission/capability tests across contracts, dispatch, and Codex host. |
| `OP-R4-002` Codex probe lacked the routing identity contract and immutable identity proof | Added the routing dependency, pinned `gpt-5.6-luna` as a selector, separated selector from provider-resolved snapshot, and made missing immutable evidence trigger narrowing/replan. |
| `OP-R4-003` first-consumer state import had no authority/recovery lifecycle | Added a complete import state machine and `ISS-024` for census, mapping, idempotent apply, equivalence, recovery, and day-after evidence. |
| `OP-R4-004` circuit breakers had no owner or state machine | Added a policy-neutral breaker package, lifecycle, contract boundary, and `ISS-025`; wired it before dispatch, routing, certification, and consumer fixtures. |
| `OP-R4-005` workspace/temporary-resource reclamation was unowned | Added owned-resource states, adapter/host ownership, exact-process reclaim gates, and acceptance evidence in process, dispatch, adapter, and host slices. |
| `OP-R4-006` CLI grammar remained prose | Published exhaustive named argv, input schemas, result schemas, duplicate/missing/positional refusal rules, and generated parser matrices. |

This failed-round record does not claim a pass. The revised 26-issue packet is
subject to another fresh independent review before GitHub registration.
