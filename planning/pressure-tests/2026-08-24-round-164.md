# ISS-002 pressure test — round 164

## Review basis

This is a fresh independent cold implementation/removal review of the repaired
recovery attempt-log evidence at exact initially clean and pushed head
`e117eba9877cc7b5ad6df4760dd9219b82817a18` on
`iss-002-versioned-contracts`. Local `HEAD` and the read-only `ls-remote`
result for `origin/iss-002-versioned-contracts` both resolved to that exact
commit before review.

The authority basis was the current repository instructions and work model,
the sole attempt-log ledger in `docs/architecture/contract-decisions.md`, the
registered ISS-002 draft, round 163, the exact `e117eba^..e117eba` delta, and
the current attempt-log production validator/tests. Parent reasoning and prior
green runs were not treated as evidence. No Chase Sets repository, project,
issue, board, backlog, source, or other consumer system was read or touched.

The exact repair changes only
`test/contracts/attempt-log.test.ts`: 24 insertions and no deletions. It changes
no production source, schema, digest, path, registry, serializer, package,
dependency, command, runtime, authority document, draft, roadmap, or board.
`git diff --check e117eba^..e117eba` passed.

## Disposition

`PASS` for the repaired recovery attempt-log implementation/evidence at exact
reviewed head `e117eba9877cc7b5ad6df4760dd9219b82817a18`.

Both round-163 deletion survivors are removed by independently valid focused
fixtures. Deleting alternating lifecycle equality now fails at an otherwise-
valid same-lifecycle edge. Deleting IN_PROGRESS→TERMINAL attempt-ID
preservation now fails at an otherwise-valid changed-attempt edge. No sibling
ordinal, predecessor, context, descriptor, time, or uniqueness check confounds
either result.

The repair is test-only and proportional. Production already implemented the
correct relations; no additional contract field, equality, helper, history
mechanism, or runtime surface was added. No remaining definite implementation,
removal, trust-boundary, or evidence blocker was found.

## Exact repair audit

### Lifecycle-alternation deletion

The first new input is:

```text
prior: IN_PROGRESS A, ordinal 0
next:  IN_PROGRESS A, ordinal 1
       predecessor = Dattempt(prior)
       same descriptor, transaction, source
       later canonical recordedAt
```

Both records parse. Ordinal is exactly adjacent, predecessor digest is exact,
same-attempt identity is preserved, context is unchanged, and time is
nondecreasing. The expected issue is exactly `edge:lifecycle`.

The second new input covers the other branch:

```text
prior: TERMINAL A, ordinal 1
next:  TERMINAL B, ordinal 2
       predecessor = Dattempt(prior)
       different attempt as required after TERMINAL
       same transaction/source
       later canonical recordedAt
```

The new descriptor belongs to B and no rule requires preservation after a
TERMINAL record. Again, lifecycle is the sole violation and the expected issue
is exactly `edge:lifecycle`.

An isolated process-local Vite transform deleted only:

```text
if current.lifecycle != expectedLifecycle -> edge:lifecycle
```

The focused suite failed at the first new assertion, receiving no issues. The
mutant is killed. Keeping both branch cases ensures later branch-specific
refactoring remains discriminated.

### Same-attempt-ID preservation deletion

The third new input is:

```text
prior: IN_PROGRESS A, ordinal 0
next:  TERMINAL B, ordinal 1
       descriptor unchanged
       predecessor = Dattempt(prior)
       transaction/source unchanged
       later canonical recordedAt
```

The TERMINAL record is otherwise the existing valid terminal fixture.
Lifecycle alternates, ordinal and predecessor are exact, descriptor/context
are preserved, and time is nondecreasing. Only attempt ID violates the
IN_PROGRESS→TERMINAL same-attempt rule. The expected issue is exactly
`attemptId:mismatch`.

An isolated process-local transform changed the production preservation array
from `[attemptId, descriptorDigest]` to `[descriptorDigest]`. The focused suite
failed at this exact assertion, receiving no issues. The mutant is killed.

These transforms existed only inside their Vitest processes and matched one
exact source anchor each. They made no filesystem or worktree edit.

## Cold proportionality and scope audit

The repaired tests target existing required equalities only. They do not add a
new lifecycle edge, copied proof, accumulator, rolling digest, checkpoint,
summary, selected-current input, history length, retention rule, or runtime
authority.

The current production surface remains the bounded Round-162 implementation:

- exact closed 9/15 IN_PROGRESS/TERMINAL records;
- ordinal-zero/null and positive/SHA predecessor arms;
- one canonical-frame `Dattempt` and tagged serializer;
- exact transaction/source/attempt/ordinal/lifecycle record path;
- empty VALUE position with specialized/common dispatch and TOMBSTONE refusal;
- null→IN_PROGRESS, IN_PROGRESS→TERMINAL, TERMINAL→IN_PROGRESS edges;
- adjacency, predecessor digest, inclusive time, same-attempt preservation,
  next-attempt difference, and global A/B/A uniqueness;
- hostile-total supplied-prefix validation with no currentness claim; and
- exact schema/vocabulary/type registrations and deleted-surface exclusions.

The parser and pure chain validator still add no observation, attachment,
launch, archive/tombstone, persistence, proposal/CAS, capability, process,
broker, command, package, retention, compaction, or runtime behavior. A valid
prefix remains structural evidence only. Stable external selection and later
family composition remain required; a candidate cannot certify itself.

## Verification evidence

All commands ran strictly one at a time. Both mutation processes fully exited
before the unmutated focused baseline. The standalone full suite fully exited
before the separate bootstrap verifier began. The compatibility slot ran only
after bootstrap exited.

Direct checked-in scripts and lockfile-pinned binaries were used to avoid the
known signed package-manager launcher dependency. No launcher stall occurred
during this review and no semantic gate was substituted. Test processes used
the increased 600,000 ms test and hook ceilings.

1. Isolated alternating-lifecycle deletion — KILLED at the new sole-issue
   assertion.
2. Isolated same-attempt-ID preservation deletion — KILLED at the new
   sole-issue assertion.
3. Focused attempt-log baseline,
   `node node_modules/vitest/vitest.mjs run
   test/contracts/attempt-log.test.ts --testTimeout=600000
   --hookTimeout=600000` — PASS: 1 file and 9 tests in 0.76 seconds.
4. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
5. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`.
6. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
7. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
8. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 39 files and 422 tests in 71.06 seconds. The
   process fully exited before step 9.
9. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 39 files and 422 tests in 68.58 seconds, private
   compositions, planning contracts, and source-tree status invariance.
10. ISS-002 compatibility slot,
    `node test/capability-slots/ISS-002/contracts%3Acompatibility-check.mjs
    --testTimeout=600000 --hookTimeout=600000` — PASS: 1 file and 1 test in
    0.69 seconds.

## Authorization boundary

This latest clean removal round clears exact head
`e117eba9877cc7b5ad6df4760dd9219b82817a18` for completion of the bounded
recovery attempt-log slice authorized by Round 162. It does not authorize any
selected-family/currentness composition, observation, attachment,
launch-pointer, archive/tombstone, persistence, CAS, capability, process,
runtime, or trust-model widening.

After this report is written, the sole intended worktree change is
`planning/pressure-tests/2026-08-24-round-164.md`; no source, test, authority
document, draft, issue, roadmap, board, or other planning file is edited, and
no commit is created.
