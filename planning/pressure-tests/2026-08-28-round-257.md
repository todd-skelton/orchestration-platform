# ISS-022 live lock-holder cleanup follow-up — round 257
`PASS`.
All post-spawn first-event failures converge on bounded termination; malformed, oversized, timed-out, non-acquired, error, and early-close paths cannot return an unowned live child.
Kill refusal or close timeout is surfaced as cleanup failure, while the close-observation WeakSet makes repeated signaled-child cleanup safe without trusting `signalCode` alone.
The non-acquired test proves rejection precedes removable-root cleanup; the valid holder closes before the post-death EEXIST probe, with `finally` retaining cleanup ownership.
The 32-contender semantics are unchanged; only its Vitest contention bound becomes 60 seconds. Focused tests pass 11/11 and root typecheck passes.
The repair is confined to the parent lifecycle helper and its integration tests; no retry, deletion, platform authority, or worker contract was added, so scope is proportional.
