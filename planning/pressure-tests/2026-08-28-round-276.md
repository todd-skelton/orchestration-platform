# ISS-022 parser-equivalence follow-up — round 276

`PASS` at exact commit
`856a329fcc8fc50976cf0e367a7d814d2ace8c59`.

The execution deadline is cancellable and cleared after the first decisive
event. Timeout and stderr overflow latch before signaling and remain non-PASS
even when one matching code-zero exit/close tuple follows. Kill refusal or a
missing terminal event returns after the bounded post-signal deadline; exact
acceptance additionally requires one exit, one close, matching code/signal, no
error, one canonical response, and byte equality with the parent.

Deterministic mutants cover timeout and overflow races, kill refusal with a
missing close, duplicate and mismatched terminal events, missing/changed child
results, corpus movement, and deletion of the temporary child bundle. The
original eight-row corpus, stable-root bundle entrypoint, three fresh children,
LF/path census, and esbuild lock inputs remain unchanged. Focused tests pass
13/13, root typecheck passes, and the slice contains no PASS reducer, writer,
self-authority, helper, or compiled artifact.

Proportionality: the two-commit slice remains a one-sitting parser probe and
decisive test seam. The follow-up adds only the terminal facts and bounded
deadline needed to prevent a hosted-runner hang; a broker or isolation layer
would be larger and outside the admitted threat model.
