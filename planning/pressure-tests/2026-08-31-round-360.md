# ISS-013 current-policy SDK — round 360

Independent source review: `PASS` at
`a5ffbca21367541a0f34a16a8b0abf4aa5f4cace`, base
`45169c13550d20a9de2351de211709eee7ed7ad4`, by
`/root/iss013_breaker_sdk_review`, Sol high, row 11.

Ordered admission, detached census/snapshot, binding-source-census precedence,
fresh fixture reads, contrasting thresholds, native Promise handling, terminal
deadlines, and final public binding match the ledger. Root exports remain
type-only; snapshot.ts and CLI are unchanged. No recovery or hold-clearance
authority is added. Reusing public parsers and two small policies is smaller
than shared async infrastructure or another registry; removing retained checks
admits substituted source, incomplete decisions, or late results.

The host execution gate was `BLOCK`: typecheck could not resolve the new test's
bare contracts import from the root test directory. Tests had not run. This
requires the existing relative internal import pattern, not a new dependency.
The separate repair review and execution evidence must precede landing.
