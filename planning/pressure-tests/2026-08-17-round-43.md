# Pressure test — round 43

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `e30ff0508eb8d07ce3ec8d299943bd82bf56f9ef`

This was an independent cold exact-head removal-round review. The worktree was
clean before review. Round 41's historical PASS and round 42's author-side
readiness statement grant no authority to this head. The archived
`archive/2026-08-17/linear-contracts-wip` tree was not inspected or used.

## Findings

| Finding | Evidence and bounded repair |
| --- | --- |
| `OP-R43-001` — The author packet and ISS-002 footprint are not reconciled to the reviewed head and reintroduce an excluded consumer-board responsibility in prose. | Head `e30ff05` deleted both Chase Sets migration authorities and narrowed the work model/checkers to this repository and its registered delivery project. Round 42 nevertheless claims the immutable 183-record base, 185 provenance identities, #6999/#7000 terminal state, source-project absence checks, two Project connections, and 31 external mismatches. `ISS-002.md` still calls the packet 42 files, names “both planning JSON authorities,” and says the deleted 183-record base remains digest-pinned. The actual non-archive reconciliation delta from the named B base is 45 files and neither migration file exists. Repair only the stale planning packet: issue a new author-side reconciliation bound to its clean exact head; delete every source-backlog/source-project/migration-base assertion from the governing packet and the ISS-002 predicted footprint; replace the file/census statements with the exact current-tree footprint and the single roadmap-registered Project check. Do not restore the deleted migration or consumer-board checker. |
| `OP-R43-002` — The normative evidence-packet census requires both eleven and twelve registry slots. | `supervisor-contract.md` declares `pointer-evidence-packet/v1` an exact eleven-slot union, while the cross-binding paragraph says “the remaining eleven registry slots” follow the target slot. The registry and round-42 census both contain eleven kinds/slots, so the target slot plus eleven remaining slots is impossible and leaves the implementation/test oracle ambiguous. Repair “remaining eleven” to “remaining ten,” state that the target occupies its one registry-ordered slot, and require an executable census mutant proving exactly eleven total, ten non-target slots, no duplicate target, and no omitted kind. No new schema or slot is needed. |
| `OP-R43-003` — One deleted-compaction residue remains as an unverifiable acceptance demand. | `compatibility.md` says no retention pointer, compaction protocol, or degraded-audit mode exists, but its review attack surface still requires executable “compaction ordering” mutants. A nonexistent protocol has no ordering to mutate, and retaining this phrase invites the deleted machinery back into the implementation. Replace it with the already intended API/symbol reachability negative proving that no retention, compaction, or degraded-audit surface is exported or dispatchable. Do not add a compaction fixture or protocol. |

## Independently validated evidence

- The roadmap contains 46 registered issues and 73 direct dependency edges.
  The planning checker proves zero transitively implied direct edges.
- Independently recomputed closure counts are 544 for the original 39-issue
  graph, 615 through ISS-043, and 654 through ISS-045.
- The simplified schema tables retain the claimed field counts and branch
  separation: history records serialize as `authority-history-record/v1`,
  while `authority-history/v1` is only the branch-separated `Dh` domain.
- The rotation run stays single-epoch: old authority selects checkpoint 5,
  append precedes the final authority CAS, successor selection revokes the old
  capability, and RESUMABLE/SELECTED/UNKNOWN are derived without a successor-
  epoch write. Unknown or contradictory evidence exposes no capability.
- `ISS-044` and `ISS-045` do not certify an absent or unsupported OS row.
  Their disposition narrows the declared authority surface or returns
  `BLOCK_REPLAN`; Codex evidence cannot certify the Claude host.
- The current board checker validates the complete repository issue connection
  and the one roadmap-registered Project connection against independent
  `totalCount` values. Its 100/101 boundary, cursor, identity, and duplicate
  mutants remain executable after removal of the consumer-board reads.

## Bounded proportionality check

The linear history and closed ordinary/rotation commit unions defend the
in-scope single-writer crash boundary. Deleting predecessor/head binding or the
outcome-to-slot equality admits a forged or ambiguous current epoch. The
smallest credible alternative is the selected O(n) linear chain, derived
rotation truth, and one fixed registry-ordered slot per kind; that is already
smaller than the removed sparse tree, coordinator, epoch-split run, or
retention machinery. `OP-R43-002` removes a phantom twelfth slot rather than
adding evidence.

Board pagination defends truncation at the provider's 100-row cap. Deleting the
independent totals or cursor checks admits a locally consistent incomplete
census. The smallest credible alternative is the current repository connection
plus its single registered delivery-Project connection. Reading the consumer
backlog or consumer Project discriminates no platform-board defect and violates
the current scope fence, so `OP-R43-001` requires deleting the stale prose, not
restoring those reads.

The only in-scope threat from deleted compaction machinery is accidental public
reachability. An API/command/symbol negative is the cheapest discriminating
evidence. Ordering mutants require a protocol that the architecture excludes;
`OP-R43-003` therefore replaces that residue with the smaller reachability
check.

## Verification

- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `node node_modules/vitest/vitest.mjs run test/planning-contracts.test.ts test/board-contracts.test.ts`
  — PASS (2 files, 48 tests).
- The `pnpm run planning:check` wrapper produced no terminal output in this
  review environment and was interrupted; its exact owned Node command above
  completed successfully and is the recorded evidence.
- Final review state remained at the reviewed commit with only this round-43
  record added.

## Next gate

Apply only the three bounded documentation/acceptance repairs above, publish a
new author-side reconciliation bound to the resulting clean exact head, and
return that head to ordinary independent review. No implementation, board
mutation, consumer-backlog mutation, or release authority is granted.
