# Pressure test — round 47 independent review

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `c49189db1e4e29ad789d1181593e6765fd14106f`

This was a third fresh independent cold exact-head review under the ordinary
removal-review standard. The worktree was clean before review and the reviewed
head was exactly the hash above. The excluded
`archive/2026-08-17/linear-contracts-wip` tree was not inspected or used.
Rounds 44 and 46 are author-side evidence only, and no earlier PASS grants
authority to this head.

## Findings

| Finding | Evidence and bounded repair |
| --- | --- |
| `OP-R47-001` — The two footprint counts are numerically correct, but the frozen “substantive reconciliation” definition can hide later substantive edits. | The non-archive diff `e77fc77..e30ff05` is exactly 45 paths with ten pressure paths, and `e77fc77..c49189d` is exactly 49 paths with fourteen pressure paths. However, `e30ff05..c49189d` is not review receipts only: it modifies `docs/architecture/supervisor-contract.md`, `docs/contracts/compatibility.md`, and `planning/drafts/ISS-002.md` in addition to adding rounds 43–46. The first two are substantive contract/acceptance repairs required by round 43, while ISS-002 adds executable acceptance and later footprint prose. ISS-002 nevertheless calls the earlier `e30ff05` range the frozen substantive reconciliation and says later pressure receipts sit outside it; round 46 likewise says no substantive contract changes occur without acknowledging the already-landed post-freeze contract edits. A path-only freeze cannot reveal content changes to paths already in its 45-path set. Repair only the accounting prose: call `e77fc77..e30ff05` a historical 45-path base-path census, not the complete substantive content boundary; in ISS-002 and the next author reconciliation separately enumerate every non-pressure path changed after `e30ff05`, describe its bounded substantive change, and recompute both the exact-head unique-path/pressure census and the post-freeze non-pressure delta at the next committed head. Do not move the historical endpoint, delete receipts, add a checker, or alter runtime contracts to fix accounting. |
| `OP-R47-002` — Two registered drafts still park work in the deleted consumer migration manifest. | `planning/drafts/ISS-042.md` parks richer board projections “in the migration manifest,” and `planning/drafts/ISS-043.md` parks optimization/decision-autonomy work there. The current tree has no such manifest: the reconciliation deliberately deletes `planning/chase-sets-orchestration-migration.json`, and the work model makes registered platform drafts the only executable backlog while consumer backlogs/boards are independent. These phrases do not make the board checker read a consumer board, but they leave two parked surfaces governed by a nonexistent consumer artifact and weaken the decoupling/proportionality fence. Replace only those parentheticals with explicit falsifiable platform-owned unpark conditions: ISS-042 should unpark richer projections only when a registered platform consumer needs an observable not derivable from the reduced status state; ISS-043 should unpark optimization/ensembles/decision autonomy only when calibration or delivery evidence shows the current review path cannot meet its published floor. Do not restore the manifest or add consumer-board reads, issues, Projects, or sync machinery. |

## Independently validated evidence

- The two requested path censuses are exact. The frozen non-archive range has
  45 unique paths and ten pressure paths. The reviewed exact-head range has 49
  unique paths and fourteen pressure paths. The post-freeze range has seven
  paths: four pressure receipts and the three non-pressure modifications named
  in `OP-R47-001`.
- Consumer-board checker behavior is otherwise decoupled. The checker loads the
  complete issue connection only for `planning/roadmap.json.repository` and the
  one Project identified by `planning/roadmap.json.project`. It validates
  independent `totalCount` values, cursor continuity, item identity,
  duplicates, and the provider's 100/101 boundary. It has no consumer issue,
  consumer Project, or deleted migration-snapshot read. The live read-only
  board verification returned `board contracts verified`.
- The roadmap has 46 registered issues and 73 direct blocked-by edges. An
  independent traversal found zero transitively implied direct edges and
  closure counts 544 for ISS-000 through ISS-038, 615 through ISS-043, and 654
  through ISS-045.
- The registry contains exactly eleven kinds. The target occupies its one
  registry-ordered slot and exactly ten non-target slots remain. ISS-002 AC11
  requires an executable mutant for eleven total slots, ten non-target slots,
  one target occurrence, and no omitted kind. No governing target-plus-eleven
  wording remains.
- The deleted compaction surface stays deleted. The compatibility attack
  surface requires retention/compaction/degraded-audit export and dispatch-
  reachability negatives, not a compaction-ordering fixture. Every record class
  is FULL_REQUIRED, loss is `UNKNOWN`, the chain walk is O(n), and checkpointing
  remains parked behind the ISS-006 measured cross-OS gate.
- Independent ledger counts remain Dop 9/15, Dsc 17, Dgb 14, Dgse 38, Drot 12,
  Dh GENESIS 7/ROTATION 13, selected authority value 22, and Dcommit common 18
  plus ORDINARY 4 or rotation base 8 plus exactly one RESUMABLE 9, SELECTED 8,
  or UNKNOWN 1 arm. History records serialize only as
  `authority-history-record/v1`; `authority-history/v1` remains the digest
  domain.
- The trust model remains external and fail closed. Records authenticate
  relationships rather than writers; mutation additionally requires canonical
  custody, the selected authority, the kernel-exclusive lock, and an ISS-004
  private live capability. Workers, candidates, receipts, structural parser
  success, stale bytes, and unsupported host rows cannot certify or authorize
  themselves. Unknown or contradictory evidence exposes no mutation method.
- The engine/adapter boundary remains explicit: the engine sees opaque
  identities and closed platform enums, while repository, branch, worktree,
  label, milestone, queue, CI, deployment, and consumer vocabulary stay in
  adapters and are guarded by the generated denylist acceptance mutant.
- The planned executable acceptance evidence is discriminating and owned:
  exact-byte/schema mutants, chain and rotation matrices, outcome-to-slot and
  eleven-slot census mutants, deleted-surface reachability negatives, hostile
  reflective shapes, and cross-OS vectors are assigned to ISS-002/ISS-006.
  Current planning/board mutants execute now; the contract mutants remain
  acceptance obligations for the later ISS-002 implementation, not evidence
  that implementation already exists.

## Bounded proportionality check

The fixed eleven-slot packet defends omission, duplication, substitution, and
wrong-outcome authority at the mutation boundary. Deleting its census admits
incomplete evidence; adding a twelfth slot defends no in-scope threat. Target
plus ten with one census mutant is the smallest credible shape.

Complete pagination of this repository and its one registered Project defends
provider-cap truncation. Deleting independent totals or cursor/identity checks
admits an incomplete census. Reading a consumer backlog or Project discriminates
no platform-board defect. Replacing the two dangling manifest references with
measured platform-owned unpark conditions is smaller than restoring any
consumer migration authority.

The linear FULL_REQUIRED history and external custody/lock/live-capability
chain defend the actual single-writer crash and physical-destination race
boundaries. Deleting predecessor/head binding or live mutation authority admits
stale or forged epochs. The selected O(n) chain is already smaller than the
removed sparse tree, node inventory, coordinator, epoch-split run, retention,
or compaction machinery; the 1,000-record cross-OS gate is the deletion test
for keeping checkpointing parked.

For footprint accounting, the threat is unreviewed substantive content hidden
inside a path already present at the frozen endpoint. Merely counting unique
paths cannot discriminate it. Naming the immutable historical path set while
enumerating the later non-pressure range is the smallest credible alternative;
it needs no runtime mechanism, repository checker, or deleted receipt.

## Verification

- `git rev-parse HEAD`, `git status --short`, and
  `git status --porcelain=v1 -uno` — clean reviewed head
  `c49189db1e4e29ad789d1181593e6765fd14106f` before this record.
- `git diff --name-only e77fc77a35d91d35d80cda3887f64c65840182b7..e30ff0508eb8d07ce3ec8d299943bd82bf56f9ef -- . ':(exclude)archive/2026-08-17/linear-contracts-wip/**'`
  — 45 paths, ten pressure paths.
- `git diff --name-only e77fc77a35d91d35d80cda3887f64c65840182b7..c49189db1e4e29ad789d1181593e6765fd14106f -- . ':(exclude)archive/2026-08-17/linear-contracts-wip/**'`
  — 49 paths, fourteen pressure paths.
- `git diff --name-status e30ff0508eb8d07ce3ec8d299943bd82bf56f9ef..c49189db1e4e29ad789d1181593e6765fd14106f -- . ':(exclude)archive/2026-08-17/linear-contracts-wip/**' ':(exclude)planning/pressure-tests/**'`
  — the three post-freeze substantive paths in `OP-R47-001`.
- Independent roadmap traversal — 46 issues, 73 edges, zero implied direct
  edges, closure 544/615/654.
- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `pnpm exec vitest run test/planning-contracts.test.ts test/board-contracts.test.ts`
  — PASS (2 files, 48 tests).
- `node scripts/planning/board-check.mjs` — PASS read-only
  (`board contracts verified`); the sandboxed attempt failed to spawn `gh`
  with `EPERM`, and the same command completed outside that process sandbox.

## Next gate

Apply only `OP-R47-001` and `OP-R47-002`, issue a new bounded author
reconciliation, and return the resulting clean exact head to ordinary
independent review. No implementation, board mutation, consumer-backlog
mutation, merge, promotion, or release authority is granted.
