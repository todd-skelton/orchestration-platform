# ISS-022 live lock-holder review — round 256
`BLOCK_REPLAN`.
Canonical root admission, bounded/request-bound first-event parsing, close-before-kill normal termination, the one-shot EEXIST-before/after-death proof, and 30-second test/finally cleanup are correct; focused tests and typecheck pass.
The start failure path can still leak an unobservable child: malformed, oversized, or timed-out output calls `child.kill("SIGKILL")` and rejects immediately without checking the kill result or awaiting provider-observed `close`.
Because a rejected start returns no child handle, its caller cannot finish cleanup or distinguish delayed/refused termination; the test exercises only the valid `ACQUIRED` path.
Minimal repair: on every post-spawn start failure, attach/retain `close`, require a successful kill or already-terminal state, and reject only after bounded close completion; add malformed/timeout cleanup mutants.
No retry, lock deletion, PID/age inference, helper, workflow, or platform-specific authority is required.
