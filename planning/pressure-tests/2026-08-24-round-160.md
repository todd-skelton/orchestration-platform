# ISS-002 pressure test — round 160

## Review basis

This is a fresh independent cold repair/removal review of the recovery
attempt-log ledger at exact initially clean and pushed head
`9e05c9e7a894e23eea50618ec12ae00519f52462` on
`iss-002-versioned-contracts`, with exact parent
`c513e1e309c74f1e2666ff74b384742c47687313`. Local `HEAD` and a read-only
`ls-remote` of `origin/iss-002-versioned-contracts` both resolved to the reviewed
digest. The exact repair changes only
`docs/architecture/contract-decisions.md`,
`docs/architecture/supervisor-contract.md`, and `planning/drafts/ISS-002.md`:
44 insertions and 36 deletions. `git diff --check HEAD^ HEAD` passed.

I read the repository instructions, work model, sole contract authority,
supervisor contract, current ISS-002/ISS-004/ISS-005/ISS-006/ISS-027/ISS-030/
ISS-032 drafts, Round 159, exact diff, and current reservation, descriptor,
pointer-position, common-locator, and compatibility surfaces cold. This review
used no authoring context or implementation authority and did not read or modify
any external repository, project, or issue.

## Disposition

`BLOCK_REPLAN` for the bounded recovery attempt-log repair at exact reviewed
head `9e05c9e7a894e23eea50618ec12ae00519f52462`.

The exact repairs for OP-R159-1, OP-R159-2, and OP-R159-3 are substantively
sound in the sole authority and ISS-002 draft. One stale sentence remains in the
supervisor authority mirror, so the packet does not yet have one unambiguous
genesis contract. The required repair is one local sentence, not a design
change or implementation expansion.

## Definite blocker

### OP-R160-1 — the supervisor still gives the first record a tagged predecessor

The repaired sole authority now says the first record is IN_PROGRESS at ordinal
`"0"` with null `predecessorRecordDigest`, and `Dattempt` is exactly one
`attempt-log/v1` canonical-record frame. No caller-selectable or derived
genesis/later digest tag remains.

The supervisor recovery section still says:

```text
The first IN_PROGRESS record has ordinal "0", a tagged genesis predecessor,
and binds the selected reservation/descriptor.
```

Read as an attempt-log record statement, this retains the deleted tagged
predecessor arm and conflicts with the exact null member. An implementer could
therefore add a genesis predecessor value/tag to the record or digest even
though the repaired ledger forbids both. The separate reservation-predecessor
key legitimately has a tagged-genesis branch, but that is not the
`predecessorRecordDigest` of the first attempt-log record and must not be
described as one.

Smallest repair: replace the sentence with the exact record fact, for example
“The first IN_PROGRESS record has ordinal `"0"` and null
`predecessorRecordDigest`, and binds the selected reservation/descriptor.” If
the reservation's tagged-genesis key is useful context, state it separately as
reservation identity rather than attempt-record or `Dattempt` framing. Re-run
the stale-string search and synchronize no other semantics.

## OP-R159 repairs verified

### Honest supplied-chain boundary

The repaired validator takes a dense supplied array, requires at least one
record, validates genesis, every adjacent edge, and global attempt-ID
uniqueness, and returns an immutable structurally valid prefix. It explicitly
makes no lookup, fullness, or currentness claim and explicitly accepts a valid
prefix.

The truncation fact now resides only where it is observable: later ISS-004
composition locates selected pointer graphs under a live external handle and
requires the final supplied record to equal the authenticated selected current
value. Thus `[I0,T1]` remains valid structural input, while selected current
`T3` makes that old prefix noncurrent/FULL_REQUIRED `UNKNOWN`. The ISS-002
evidence matrix no longer asks the pure validator to distinguish those
indistinguishable inputs; selected-tail truncation is assigned to ISS-004.
Existing ISS-027/ISS-030/ISS-032 truncation requirements are runtime composition
requirements and do not contradict this split.

### Global attempt-ID uniqueness

The adjacent transition remains minimal:

```text
IN_PROGRESS(A) -> TERMINAL(A)
```

preserves the same attempt ID and is not rejected by the global rule, because
that rule applies only when admitting an IN_PROGRESS node and compares its ID
against IDs in prior supplied nodes. After `TERMINAL(A)`, IN_PROGRESS must use a
new ID. The supplied-chain seen set therefore accepts `I(A),T(A),I(B),T(B)` and
rejects non-adjacent `I(A),T(A),I(B),T(B),I(A)`. The draft requires that exact
A/B/A fixture and deletion mutant. It does not turn UUID time into ordering or
add persistence behavior.

### One canonical digest frame

The sole digest is now exactly:

```text
Dattempt = attempt-log/v1(canonical attempt-log/v1 record bytes)
```

The supervisor digest table and ISS-002 summary, evidence, and footprint use the
same one-frame formula. The record bytes already commit lifecycle, canonical
bounded ordinal, and nullable predecessor with an unambiguous closed object
encoding. Repeated byte tags, bounded-decimal parts, and predecessor raw-32
parts are explicitly forbidden. There is no deployed digest or migration,
alternate helper, untagged identity, rolling digest, or summary identity.
Except for OP-R160-1's prose sentence, repository search finds no stale
attempt-log tagged digest/golden/frame requirement.

## Reattacks that did not produce blockers

The exact nine-member IN_PROGRESS and fifteen-member TERMINAL censuses remain
sorted and closed. Branch-only terminal members are absent from IN_PROGRESS.
Genesis is structurally ordinal `"0"` plus null predecessor; every positive
ordinal requires a SHA-256 predecessor. Later edges require adjacent bounded
ordinal, `Dattempt(prior)`, inclusive time, and the exact lifecycle graph.
IN_PROGRESS→TERMINAL preserves transaction, source, attempt, and descriptor;
TERMINAL→IN_PROGRESS preserves transaction/source and admits a new descriptor
for the globally fresh attempt.

Each standalone path is constructible from the parsed transaction, source,
attempt, ordinal, and lifecycle. The common transaction/source attempt-log
pointer plus its recursively locatable selected-object aliases supplies the
selected prior graphs; no directory enumeration, mutation ID, index, head,
inventory, or accumulator is introduced. The empty VALUE position has no
hidden discriminator. Later archive/common tombstone composition owns removal;
bare absence, deletion, retention, and compaction grant nothing.

`descriptorDigest` remains a sufficient transitive locator. Log
transaction/source/attempt constructs the immutable descriptor path, exact
bytes recompute its digest, and the descriptor directly binds the reservation
predecessor key plus selected RESERVED `Dt`. Copying reservation `Dp/Dv/Dr/Dt`
siblings into the log adds neither a locator nor a replay discriminator.

The dependency order remains acyclic:

```text
selected RESERVED reservation
  -> immutable LIVE descriptor
  -> selected IN_PROGRESS log
  -> selected CONSUMED reservation
  -> selected TERMINAL log
  -> selected TERMINAL reservation
  -> later archive/common tombstone
```

The IN_PROGRESS record cannot contain the later CONSUMED reservation value that
names it, and TERMINAL cannot contain the later TERMINAL reservation tip. A new
attempt starts only after the selected prior TERMINAL log. One-frame
`Dattempt` hashes canonical record bytes whose predecessor points strictly
backward, so it adds no digest cycle.

Terminal attachment and revocation remain independently nullable structural
identities; this slice has no authenticated fact from which to impose an iff.
Process-terminal, channel-denial, applicable-revocation, and outcome identities
remain opaque until ISS-005/ISS-030/ISS-032 parse their independently reviewed
source records, bind chronology and applicability, and prove the selected
disposition. A SHA-shaped field, parser success, timestamp, or candidate/worker
report grants none of those facts.

The slice adds no filesystem IO, proposal/CAS, UUID allocation, capability,
process operation, broker call, command, package, runtime selection, archive,
or tombstone behavior. Engine contracts contain opaque platform identities and
closed platform vocabulary only; Git, GitHub, provider, argv, native endpoint,
and consumer policy remain outside the engine boundary. Stable external
mutation authority and live ISS-004 handles remain necessary. A candidate
never certifies, selects, or promotes itself.

## Proportionality

| Mechanism | Concrete in-scope threat | Deletion result | Smallest retained shape |
| --- | --- | --- | --- |
| Closed 9/15 records | Partial, future, cross-branch, or null branch-only bytes become state. | Removing closure/member/scalar admits a direct record mutant. | Two flat branches. |
| Canonical predecessor and ordinal | Gap, reorder, or forked record becomes an adjacent append. | Removing either relation admits one supplied-edge mutant. | Each fact appears once in canonical record bytes. |
| Global seen-ID rule | A completed UUID is reused after an intervening attempt. | Deletion admits the A/B/A chain. | One O(n) set in the already O(n) supplied-chain validator. |
| Supplied-prefix validator | Malformed supplied ancestry is treated as structural history. | Deleting an edge/genesis rule admits a local malformed chain. | Pure prefix validation; no false fullness claim. |
| Authenticated final equality | A valid historical prefix is reused as current authority. | Deletion admits selected-tail truncation at runtime. | One final equality in later ISS-004 composition. |
| One-frame `Dattempt` | A canonical record is substituted or crossed with another digest domain. | Removing domain/frame admits domain reuse; adding repeated fields kills no extra mutant. | One domain-separated canonical-record part. |
| Terminal opaque identities | One downstream terminal fact is substituted independently. | Removing its digest leaves later semantic composition unbound. | One digest per distinct source fact; no proof bag/summary. |
| Stable selection boundary | Candidate-authored structural bytes certify themselves. | Removing live external selection admits self-selection. | No mutation surface in this slice. |

The predicted footprint remains one contracts module, existing pointer-position
dispatcher, public serializer/export/compatibility wiring, and focused tests.
No dependency edge or runtime package is added. The exact repair itself stays
within the three expected authority/draft documents. Draft/board consistency is
reported by the required gate below.

## Verification

Required gates were run strictly serially with increased test timeouts; the
standalone full suite exited before bootstrap began:

The package-manager shim's pre-script version switch is unavailable in this
environment because registry verification/fetch is blocked, as directly
observed in Round 159. The permitted same-lockfile direct equivalents were used
again. Read-only board access and bundled tool subprocesses received only their
bounded spawn permission.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS:
   `board contracts verified`; it read only this repository and its registered
   delivery project.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 38 files and 413 tests in 78.84 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 38 files and 413 tests in 72.13 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This round does not authorize implementation at exact head
`9e05c9e7a894e23eea50618ec12ae00519f52462`. Reconcile the one stale supervisor
sentence with the exact null genesis member while preserving all three sound
Round 159 repairs, then return the exact repaired head for fresh independent
review.
