# Pressure test — round 54 author dispatch exactness repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `571dedf0f313337dd972485dd1f6d08d8721d4a0`

This author-side repair answers only `OP-R53-001` through `OP-R53-004`.
Round 53's `BLOCK_REPLAN` remains governing. This record grants no
implementation, merge, promotion, installation, or release authority; only a
fresh independent exact-head `PASS` may authorize implementation.

## Bounded repair

- `dispatch-action-core/v1` is now one exact six-member closed projection with
  literal schema/domain, canonical member order, scalar rules, six ordered
  framed parts, and explicit cycle exclusions. The brief equal-binds its digest
  and duplicated action facts to this single step-4 projection.
- Catalog rows are unique by
  `(actionKind,capabilityName,directiveKind,code)`; that resolver key selects
  exactly one accessor/template pair. The descriptor's separately declared
  worker action/capability pair census must exactly equal the catalog pair
  projection.
- All nine kinds are exhaustive. Eight require at least one present directive;
  operator action requires either one-or-more present rows or exactly one
  absent row, never neither or both. A present row resolves a nonempty fixed
  template plus a nonempty digest/closed-role scalar, replacing the ambiguous
  word "substantive" with executable properties.
- The unowned template-registry digest is deleted. ISS-021 alone produces the
  exact host-renderer package-artifact digest; the reviewed release manifest and
  installation read-back bind it to the selected active release; step 7 proves
  equality and records it before rendering or ownership publication. Candidate
  or worker self-report is never authority.

## Proportionality and ownership

One action projection and digest, one four-part catalog key, one operator XOR,
and one existing host artifact identity are the smallest mechanisms that close
the four observed ambiguities. No second plan/digest, resolver service, dynamic
registry, schema reference, resource class, path, network lookup, or candidate
authority is added.

ISS-002 owns only the closed projection/schema, digest formula, brief/catalog
admission, pure equalities, and mutants. ISS-011 owns catalog production;
ISS-021 owns the compiled renderer artifact; ISS-008 owns dispatch-plan and
rendered-byte/artifact binding; ISS-026 owns routine-step composition. Their
drafts and the ABI/cycle documents now state those exact consumer obligations;
no dependency edge changes.

## Required independent attack

Review the exact committed repair head cold. Attack every action-core member,
type, order, frame, domain, exclusion, and coordinated substitution; duplicate
catalog keys with changed accessor/template/both; descriptor/catalog pair drift;
missing operator action, present-plus-absent, duplicate absent, empty template
or accessor value; stale/candidate/self-reported/moved host artifacts; digest
substitution; renderer ownership; removed reference/resource-kind
resurrection; vocabulary smuggling; and candidate self-certification. Older
PASS dispositions do not apply.
