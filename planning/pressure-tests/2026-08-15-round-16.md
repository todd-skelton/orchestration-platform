# Pressure test — round 16

**Disposition:** `BLOCK_REPLAN`

The sixteenth independent review found two successor-activation blockers. The
39-issue packet remains unregistered pending a fresh review of the repairs.

| Finding | Repair applied |
| --- | --- |
| `OP-R16-001` — Native scheduler definitions pinned N0, so the fifth N1 verification cycle would invoke a stale executable. | Replaced release-specific scheduler targets with an immutable installation-scoped `supervisor-shim/v1` bound to Node/shim digests. The shim verifies atomic `active-release/v1`; promotion uses that record as the sole activation point and a recovery fence. `ISS-030` owns the cross-platform shim/handoff matrix, `ISS-027` initial N0 activation, and `ISS-015` N1 verification plus the next independent scheduler tick. |
| `OP-R16-002` — Credential references and broker client admission were bound only to N0, while N1 was expected to reuse them. | Made secrets installation-scoped and separated them from `broker-active-client/v1`. Added closed `release.successor-admin` prepare/activate/abort APIs: N0 prepares N1, active-release switches atomically, then the same N0 transaction forward-activates N1/revokes N0 without reprovisioning secrets. Added crash/replay/order/generation tests across `ISS-014`, `ISS-015`, `ISS-027`, `ISS-032`, and `ISS-034`. |

## Re-review focus

- Confirm the immutable shim starts N before activation, blocks during incomplete
  post-activation recovery, then starts N+1 for verification and scheduler tick.
- Confirm broker authority changes release clients without changing or exposing
  installation-scoped secret items.
