# ISS-022 case/Unicode alias review — round 266
`BLOCK_REPLAN`.
`observeOptionalLeaf` runs lstat/stat/realpath concurrently and converts any resulting `ENOENT` to `ABSENT`; lstat may observe an existing entry that disappears before stat/realpath, yet the row becomes clean `DISTINCT_ABSENT` instead of moving/UNKNOWN.
This also weakens the landed `PHYSICAL_ABSENT_LEAF` arm, which requires exact `lstat -> ENOENT`, not ENOENT from a later followed-stat or realpath race.
Minimal repair: decide absence only from the registered lstat call; after lstat observes presence, require coherent stat/realpath or refuse, and add disappearance plus stable `DISTINCT_EXISTING` mutants.
The exact A/a and NFC-é/NFD-e+acute corpus, Darwin/Linux/Windows expectations, dev+ino identity relation, repeated stability facts, four-row order, shared handle/namespace, path omission, cleanup, and no-Dphys/no-PASS scope are otherwise correct.
Focused tests pass 5/5 and root typecheck passes, but they exercise only native IDENTICAL/DISTINCT_ABSENT outcomes and do not close the race.
