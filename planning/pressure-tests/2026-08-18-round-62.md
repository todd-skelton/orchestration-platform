# Pressure test — round 62 author ordinary-resolution repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `3d43e7b6186dc0cfbf06553cd223eb21ca433d6b`

This author-side repair closes the implementation-discovered ambiguity in
`pointer-mutation-commit-resolution/v1`. Round 61's `PASS` does not apply to
this amended public ledger. This record grants no implementation, merge,
promotion, installation, or release authority; only a fresh independent
exact-head `PASS` may authorize the amended resolution implementation.

## Bounded repair

- The deleted sparse-model `producerEpochKey` is replaced by one exact required
  selected authority tuple: producer `Dp/Dt/Dv/Dr`, under the four literal
  `producerAuthority*Digest` member names.
- The tuple must equal both the ORDINARY run epoch and its KNOWN packet
  authority. It cannot be partial, caller-selected, or substituted by a
  membership index, proof, root, epoch key, or history head.
- The retained ordinary outcome projection is closed: SELECTED, LOST_CONFLICT,
  and UNKNOWN_TERMINAL admit only their selected-tip, conflict-receipt, or
  fixed unknown-evidence digest, and `outcomeEvidenceDigest` equals that exact
  non-null arm.
- The resolution digest retains the proportional existing formula: its own v1
  domain over canonical record bytes only. Rotation still forbids a resolution
  record and derives terminal truth from checkpoint 5 plus external evidence.

## Proportionality and trust

Four selected-authority digests are the minimum executable replacement for the
deleted epoch key: they reuse the already-required pointer selection tuple and
add no tree, membership proof, directory census, coordinator, receipt, or
capability. Structural validation still grants no authority; ISS-004 must
cross-bind the tuple to its live single-epoch handle and the packet.

## Required independent attack

Review the exact committed repair head cold. Attack omission, rename, null,
partial tuple, order, `Dp/Dt/Dv/Dr` swaps, stale and coordinated substitution,
run/packet mismatch, restored `producerEpochKey`, membership/root/proof aliases,
wrong outcome-arm nullability, evidence-digest mismatch, rotation insertion,
selector/readback/`Dpost` cycles, adapter vocabulary, and self-certification.
Older PASS dispositions do not apply.
