# Pressure round 447 — standalone repository-protection receipt source

Status: ISS-054 historical-receipt bounded repair, absolute source attempt 3.
Exact base is `ebde782ce997b7cc2789dff9d32e001d157f9d4c` on branch
`codex/iss054-protection-receipt`. This packet does not close ISS-054.

The lineage is cumulative. Round 441 supplied the original receipt/anchor
definition attempts and review. Round 442 authored, independently reviewed and
then repaired the standalone verifier-anchor source after hosted stable-bundle
census evidence. Round 445 carried receipt/binder definition attempts 1 through
4: the first source-planning pass blocked missing executable digest detail,
definition attempt 2 authored it, independent attempt 3 blocked relation-name,
global request-chain and fresh-update chronology gaps, and attempt 4 repaired
those gaps. This source attempt consumes that closed ledger without resetting or
claiming any prior attempt as source review. Source attempt 1 authored exact head
`146381c5cb7896f19e510e8e45b277db10c79fc1`; independent review attempt 2
returned BLOCK because the public request-identity helper read `apiKind` before
closing and detaching its supplied request. This attempt 3 repairs only that
boundary. Independent exact-head review attempt 4 remains required.

Routing remains the provisional deep-contracts Sol-high fallback because Opus is
unavailable. Budget intent is at most $15; actual spend is unknown and no ceiling
increase is authorized.

## Scope and non-goals

Scope is only the standalone historical `repository-protection-receipt/v1`
contract. The source adds its exact fourteen-member and nested field censuses,
total recursively detached parser, exact literals/scalars/enums/UTF-8 bounds,
positive GitHub identities, protected paths, BUILD/REVIEW workflow and trigger
relations, complete seventeen-permission namespace, nine historical purpose
rows, REST/GraphQL page chains, historical chronology, environment/producer
joins, ruleset-semantic digest and framed family identity. Generic parsing,
canonical-byte parsing, framed serialization, registry vocabulary,
compatibility, public export, and the ISS-002 stable source/test path census are
wired in the same packet.

The parser recomputes request identities, first REST and every GraphQL page
request identity, complete reductions, terminal pagination, seven reductions
whose closed projections are present, ruleset semantics, and the receipt family
identity. Query/document/variables digests, response digests, non-null raw Link
header digests, later REST request identities, and historical pull-request and
review reduced-value digests remain supplied opaque claims. Their grammar and
joins are checked; no fixture or successful parse claims authenticated provider
origin, retained preimages, request execution, followed Link provenance, or
pagination completeness.

The repair snapshots a standalone helper request before selecting its REST or
GraphQL census, then validates only the detached snapshot. Focused proxy and
accessor vectors prove that request `get`, `ownKeys`, and accessor code cannot
run. An audit of the other existing public helpers found no second discriminator
read before their primitive checks or closed snapshots; no public surface or
protocol was added.

Fresh-observation parsing and the four-input receipt/anchor binder remain
deferred. No schema invention, runtime primitive, package, dependency, workflow,
provider, adapter, network, filesystem, credential, API observation, receipt
production, artifact operation, attestation verification, ruleset/environment
mutation, installation, release, or authority grant enters this packet. The
landed verifier-anchor source remains unchanged.

## Acceptance evidence

The focused contract vectors pin every root and nested closure, literals,
positive safe decimals, SHA-256/timestamp/ETag/name/route/path/ref grammars,
three dispositions, strict issue/expiry order through the exact seven-day
boundary, ordered unique protected paths and workflows, the BUILD-to-REVIEW
source relation, distinct live REVIEW producer and completed successful BUILD
trigger, environment publication before producer start, and all historical
page/observation/issue chronology.

Permission vectors pin the versioned namespace and all seventeen ordered rows,
including `id-token`, `models`, and `vulnerability-alerts` access restrictions.
Observation vectors pin all nine purposes and request-identity uniqueness.
REST and GraphQL vectors cover 1, 2 and 64 pages and refuse 0/65, ordinal gaps,
duplicate or moved relations, wrong boundary/index targets, missing or extra
NEXT, disagreement among NEXT/next-request/following-request cells, repeated
request identities, cyclic chains, cursor mismatch/repetition, early/final
terminal inversion and caller-count substitutes. Optional FIRST/PREV/LAST links
remain optional where permitted and absent on one-page boundaries.

Independent literal goldens pin canonical request bytes, request identity,
first page, complete reduction, both terminal branches, ruleset semantics and
the receipt family frame. One-axis opaque-leaf vectors require enclosing
recomputable identities to move while preserving the leaf's unauthenticated
status. Mutable, sealed, frozen, proxy, accessor, symbol, hidden, sparse,
subclass, cross-realm and cyclic inputs exercise snapshot-before-semantics; a
successful value is recursively detached and frozen.

## Verification and footprint

Local author verification is intentionally static: `pnpm exec tsc --noEmit`,
the repository planning checks, targeted Prettier verification and
`git diff --check`. No Vitest, contract executable test, native compiler/addon,
module/probe/lock experiment, provider call, network action or hosted workflow
runs locally. The focused test file is executable evidence for ordinary hosted
Linux, macOS and Windows CI only. Root owns board parity and hosted execution;
neither is awaited or claimed here. Exact-head independent source review remains
required before merge, and the candidate never certifies itself.

The predicted and actual source footprint is exactly seven files:

- new `packages/contracts/src/repository-protection.ts`
- new `test/contracts/repository-protection.test.ts`
- existing `packages/contracts/src/index.ts`
- existing `packages/contracts/src/registry.ts`
- existing `packages/conformance/src/iss002-bundle-paths.mts`
- existing `planning/drafts/ISS-054.md`
- new `planning/pressure-tests/2026-09-04-round-447.md`

## Review attack surface

Attack reflection before snapshotting; a widened record, array or enum; UTF-16
instead of UTF-8 bounds; unsafe or zero numeric IDs; malformed branch refs,
paths, names, routes, ETags or timestamps; missing permission rows; mutable
permission documentation silently widening the namespace; BUILD/REVIEW role,
trigger or source inversion; producer bound to BUILD or given a self-terminal
claim; BUILD/REVIEW run aliasing; future environment publication; weakened
issue/expiry or capture chronology; missing or duplicate purpose rows; REST and
GraphQL branch crossing; opaque leaf claims described as authenticated proof;
partial or cyclic page chains; duplicate relation names hidden by distinct
targets; wrong FIRST/PREV/NEXT/LAST indices or boundary links; later REST pages
not joined through both NEXT cells; GraphQL cursors not reframed into request
identity; terminal digests omitting complete handoffs; reduction projections
omitting ordinals, response order or the complete triggering BUILD row; ruleset
semantics including capture metadata or omitting immutable policy; unframed
generic serialization; missing registry/compatibility/census wiring; deferred
fresh/binder protocol smuggled into this slice; and any structural success
presented as live protection, API completeness, operator action, artifact
authenticity or authority.
