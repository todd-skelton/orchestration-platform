# ISS-006 native isolated-walk composer landing review — round 231

## Disposition

`PASS_TO_LAND_NATIVE_COMPOSER`.

Both Round-230 deletion gaps are closed against the exact current tree. The
focused executable fixture now drives the exported production function in
Linux and Win32 builds while replacing only the fixed OS factory imports; the
real unmodified stable coordinator performs all three process cycles. The
stable-bundle test now independently owns the sole exact new
`test/conformance` row. No remaining platform-selection, option-seam,
coordinator, cleanup, result-vocabulary, package-boundary, or proportionality
blocker was found.

This disposition authorizes only the private Windows/Linux-first composer.
Darwin remains an exact fail-closed diagnostic and the missing full macOS
authority/hosted evidence remains required for ISS-006 completion and any
hosted PASS aggregate.

## Exact reviewed tree

```text
HEAD 026ed63bd2d2e2733f66a05b470d87556f08b6e3

79ff32bc5e9235a4be4d77a1c610fdd8446c48bc3683f2892f4927bdcbb7adb1  packages/conformance/src/native-isolated-walk.ts
0e59fb302e18dabae45b7e26d3ca12d55f15b480b39da4c0e9b84d238ea403ef  test/conformance/native-isolated-walk.test.ts
51f034ff278efd4c6f7f271bde88933c93387eba4917fbeaf7b8340b55ad9409  packages/conformance/src/iss002-bundle-paths.mts
3dd9756953ddd455fa46a0dfc89c6eb36062c8400b3bfa60aea38476f2b29b04  test/conformance/stable-bundles.test.ts
b5354442a58acf83eac66479ad7b23f75decbd36d5960bd8a012bbe14cbf9907  planning/pressure-tests/2026-08-27-round-229.md
6a97c2c9209a88ed6977b55e19f0f97709252f0ef0e2ab516664f178a519bff7  planning/pressure-tests/2026-08-27-round-230.md
```

The worktree contained only these composer/test/census changes and immutable
Rounds 229–230 when this record was pinned. I reread the exact implementation,
complete transformed fixture, bundle census assertion, Round-230 findings,
package boundary, coordinator contract, and relevant sole-ledger isolation
requirements. I did not access Chase Sets, mutate an OS principal, run a
hosted diagnostic, or edit implementation, draft, board, roadmap, or sole
authority.

## Round-230 repairs verified

### Exported production binding is executable

`productionTestModule` builds the exact production entry twice with captured
`process.platform` fixed to `linux` and `win32`. Its esbuild resolver replaces
only the two relative OS factory imports. It does not replace
`runIss002IsolatedWalk`, `isolated-walk.ts`, the composer, its result parser, or
its close logic. The test calls the actual exported
`runNativeIss002IsolatedWalk`, not private `runCore`.

For each platform the selected fixed factory returns one opaque authority. The
real coordinator then proves:

1. exactly three distinct principal creations;
2. exactly three preparations and launches;
3. the exact candidate/RPC paths and literal 5,000 ms timeout in every request;
4. each launch receives the same request object as its corresponding prepare;
5. teardown order is exactly `0, 1, 2` before the result is observed; and
6. the stable result contains three canonical duration strings and their exact
   maximum.

The Linux branch additionally proves its one factory is selected, Windows is
not, `close()` runs exactly once after the third teardown/result construction,
and close is the final lifecycle event. The Windows branch proves the inverse
factory selection, no fabricated global close, and teardown 2 as the final
event. Returning a constant success, replacing the real coordinator, swapping
factories/platforms, omitting a cycle/teardown, returning early, or moving Linux
close now changes direct assertions.

The existing private-`runCore` matrix remains useful and independently covers
hostile supported inputs, zero-access Darwin/unknown refusal, exact flat Linux
option reconstruction, asynchronous caller mutation, Windows selection,
factory/coordinator exceptions, malformed results, and all Linux
success/refusal/throw crossed with close success/refusal.

### Test-bundle ownership is exact

`iss002TestBundlePaths` contains
`test/conformance/native-isolated-walk.test.ts`. The stable-bundle test now
independently filters `test/conformance/` and requires exact equality to the
one-element list containing that path. This is not derived from the manifest
under test. Deleting, renaming, duplicating, or adding a conformance-test row
changes the assertion; the manifest test separately proves the surviving exact
list is copied into the stable test bundle.

## Complete trust and proportionality recheck

- The captured production platform remains non-caller-controlled. Darwin and
  unknown platforms return frozen fixed refusals before inspecting hostile
  input/configuration or invoking a factory.
- Supported inputs/configurations accept only own enumerable data descriptors
  on plain/null-prototype records, reject proxy/accessor/symbol/extra/relative
  forms, and copy/freeze path strings before asynchronous use.
- Linux reconstructs account/DAC/execution/process options from nine exact flat
  paths, shares one account state root, and forwards no command runner, profile
  reader, token generator, clock, parser, syscall, or authority seam.
- Linux always attempts its retained factory close after coordinator work;
  close ambiguity overrides success and simultaneous fixed refusals remain
  deterministic. Windows cleanup remains solely the coordinator plus the
  reviewed native authority—there is no misleading no-op close.
- The only return is the existing detached private
  `Iss002IsolatedWalkRunResult`. Three durations, exact maximum, refusal issue
  arrays, extra fields, and canonical decimal form are checked. Candidate
  stdout still enters only the stable coordinator's hostile terminal parser;
  it cannot provide expected values, selected head, timing, PASS, cleanup, or
  authority.
- The module is absent from the package root and GitHub adapter export. Its
  source contains no receipt, provider, workflow, promotion, certification, or
  PASS vocabulary. Harness/test bundle rows are exact.
- The implementation is one small composition owner rather than a duplicated
  OS lifecycle or premature observation/workflow/provider layer. Existing
  lower-layer hosted Linux and Windows diagnostics remain complementary; this
  private composition landing itself creates no hosted evidence claim.

No mechanism in this slice is now unsupported by a current consumer or a
deletion test. A full observation producer, candidate consume/delete/launch
composition, raw serialization, workflow mutation gate, aggregate job, and
provider writer remain ordered downstream work under the sole authority.

## Checks run

```text
pnpm exec vitest run \
  test/conformance/native-isolated-walk.test.ts \
  test/conformance/stable-bundles.test.ts
  PASS — 2 files, 15 tests, 3.97 s

pnpm run typecheck
  PASS
```

The focused suite and full repository TypeScript check were rerun after the
last test-only request-binding assertion. No full suite, privileged/hosted
diagnostic, planning gate, or board check was run for this bounded re-review.

## Planning impact

No ISS-006 draft, sole ledger, roadmap, or board amendment is warranted. This
is the implementation of the already-authorized private composition boundary,
not a changed authority or completed issue. `PASS_TO_LAND_NATIVE_COMPOSER` is
not a hosted PASS, portable receipt, aggregate, provider record,
certification, installation, selection, promotion, issue completion, or Chase
Sets access.
