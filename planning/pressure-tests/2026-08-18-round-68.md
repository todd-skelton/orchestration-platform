# Pressure test — round 68 author commit-tag and packet-nullability repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `f3a4bf2050b5fd6ada3aee78c72a77f418676050`

Implementation of the round-67-authorized commit-evidence union reached two
previously unstated public choices at the next dependency boundary. The user
approved the bounded choices recorded below. Round 67's `PASS` does not apply
to this amendment. This record grants no implementation, merge, release,
promotion, installation, runtime mutation, or self-certification authority; a
fresh independent exact-head `PASS` is required before implementation resumes.

## Bounded repair

- The `Dcommit` outcome byte is exact and branch-local. ORDINARY maps
  `0x00=SELECTED`, `0x01=LOST_CONFLICT`, and `0x02=UNKNOWN_TERMINAL`.
  AUTHORITY_ROTATION independently maps `0x00=RESUMABLE`, `0x01=SELECTED`, and
  `0x02=UNKNOWN`. The framed part is the fixed raw byte, never enum text or a
  caller-selected number.
- `pointer-evidence-packet/v1` requires both outer
  `currentAuthoritySelection` and `authorityHistoryBinding` to be non-null and
  cross-bound for HISTORICAL_READ, every ORDINARY outcome, and rotation
  RESUMABLE/SELECTED. Rotation UNKNOWN requires both exactly null, consistent
  with UNKNOWN/null packet authority and the empty authority registry slot.
  Partial nullability and every UNKNOWN capability attempt refuse.

## Proportionality and trust

The repair assigns fixed bytes to an already-required digest part and fixes the
nullability of two already-required packet members. It adds no field, record,
digest part, path, receipt, pointer kind, state transition, tree, proof,
inventory, coordinator, checkpoint, compaction, serialized capability, or
verdict route. Rotation UNKNOWN remains fail-closed. Structural success grants
no authority; the live ISS-004 context, selected authority, custody, kernel
lock, and stable-predecessor promotion split remain mandatory.

## Required independent attack

Review the exact committed repair head cold. Recompute every ordinary and
rotation `Dcommit` outcome with the declared raw byte and try enum text,
swapped tags, cross-branch meanings, multi-byte encodings, caller-selected
numbers, and every other one-byte value. Exercise both outer packet authority
members for historical reads, all ordinary outcomes, and rotation
RESUMABLE/SELECTED/UNKNOWN; try each one-null combination, stale or mismatched
non-null bindings, non-null rotation UNKNOWN authority, and any capability from
UNKNOWN. Recheck the unchanged outcome-slot matrix, history walk, trust,
vocabulary, proportionality, planning, and board surfaces. Older PASS
dispositions do not apply.
