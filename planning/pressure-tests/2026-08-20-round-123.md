# ISS-002 pressure test — round 123

## Review basis

This is a fresh independent cold removal review of exact initially clean
candidate head `4d49a2dce1259f7ee171b4bba78970d0298b4ae1` on
`iss-002-versioned-contracts`, with exact parent
`0709fd3f5a567e17a3f7100aafd6e2721bdefe17`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
implementation authority in `docs/architecture/contract-decisions.md`, the
registered `ISS-002` draft, round 122 as the finding under retest, the adjacent
pointer registry only to verify the already registered fence-root template,
and the exact parent-to-head diff. No Chase Sets repository, project, issue,
board, backlog, consumer source, or other consumer system was read or touched.

The exact diff changes only `docs/architecture/contract-decisions.md` and
`planning/drafts/ISS-002.md`: 19 insertions and 15 deletions. It adds no
implementation, test, registry, package, runtime, roadmap, dependency, or board-
topology change. `git diff --check` passed. The checkout was clean before this
required pressure record was written.

## Disposition

`PASS` for the bounded recovery-authorization immutable-core planning ledger.

The Round 122 ambiguity and proportionality blocker is removed.
`recoveryFencePath` is deleted from the exact SUCCESSOR member census, its
claimed count, the draft summary, and the compatibility-test census. Later
composition constructs the one exact transaction fence-root path directly as
`installation/activation-recovery-fence-roots/<transactionId>.json` and binds
the core's `recoveryFenceRootDigest` to `Dfr`. The derivable path is explicitly
not a core member. No implementer-selected path or redundant path preimage
remains.

This clean removal round authorizes implementation only of the closed
`recovery-authorization-core/v1` structural parser, its deterministic core-path
constructor, its sole `Dac` computation, public registration/export work, and
the focused compatibility evidence already bounded by the ledger and draft.

## Round 122 blocker removal

### Exact census and count deletion

The repaired authority contains exactly fifteen common members, exactly three
BOOTSTRAP-only members, and exactly twelve SUCCESSOR-only members. BOOTSTRAP
therefore has eighteen members and SUCCESSOR has twenty-seven. The SUCCESSOR
list proceeds directly from `predecessorReleaseDigest` to
`recoveryFenceRootDigest`; no path member, nullable placeholder, alternate
locator, or general relative-path scalar remains. The introductory scalar-
grammar sentence likewise no longer claims a relative-path member.

The `ISS-002` authority summary and acceptance evidence both say twelve
SUCCESSOR members. Compatibility tests must remove, add, rename, reorder,
cross-type, and cross-mode each of those exact members and pin the canonical
SUCCESSOR bytes and `Dac`; they can no longer bless a caller-selected fence
path by fixture choice.

### Exact derived fence-root path

Later SUCCESSOR composition—not the immutable-core parser—must construct
`installation/activation-recovery-fence-roots/<transactionId>.json` from the
same canonical UUIDv7 `transactionId` member and require the corresponding
root digest to equal `Dfr`. This is the exact root template already registered
for `ACTIVATION_RECOVERY_FENCE`; it is distinct from the selected-tip path,
immutable-value/archive path, and every other fence family. The statement
neither authorizes enumeration nor grants lookup or mutation authority.

Deleting the derived-path composition relation would permit a composed proof
to bind the right digest under a redirected fence family. Restoring the path as
a core member would duplicate facts already determined by pointer kind and
transaction without addressing another in-scope threat. Direct construction
at the later equality boundary is the smaller sufficient shape.

### Admission-time boundary

The repair also makes the pre-existing deferral executable and unambiguous.
The structural parser proves only `issuedAt < expiresAt` and consults no clock.
Later composition must supply an authenticated admission time and prove
`issuedAt <= admittedAt < expiresAt`. `admittedAt` is not smuggled into the
immutable core, inferred from local time, or accepted as a caller Boolean. Its
source and the rest of the broker/admission graph remain subject to the later
literal composition ledger and removal round.

## Full census and contract attacks

### Closure, canonical types, and generations

The fifteen common names and both branch lists are closed, non-overlapping, and
internally in ascending canonical JSON member order; global canonical JSON
sorting determines the merged record order. Opposite-branch members are absent
rather than null. Unknown members, raw capability/reference material, selected
authority tuples, candidate verdicts, and the forbidden
`candidateOperationManifestDigest` refuse.

All identity and evidence digests retain the closed lowercase SHA-256 grammar;
installation, transaction, and cycle identifiers retain canonical UUIDv7;
timestamps retain their closed grammar. `nativeGeneration` and every SUCCESSOR
generation are canonical safe-decimal strings bounded before conversion.
`successorBrokerGeneration` is exactly predecessor plus one. Noncanonical,
negative, overflow, non-adjacent, and predecessor-at-safe-maximum successor
pairs refuse, while zero and the safe maximum remain usable wherever no
increment is required. Equality of `expectedActiveGeneration` to authenticated
broker read-back remains deferred rather than trusted from the core.

The core's own path remains exactly
`installation/recovery-authorizations/<transactionId>/core.json`, constructed
from the canonical transaction member. Caller paths, alternate roots,
symlinks, enumeration, and latest-file rules grant nothing. `Dac` remains one
domain-separated frame under literal `recovery-authorization-core/v1` with one
canonical-record part; raw JSON, untagged digesting, field-wise reframing,
cross-domain reuse, and a second identity refuse.

### Digest cycle, opaque identities, and trust

The dependency order is acyclic. A SUCCESSOR recovery-fence root and `Dfr` are
computed without authorization or cleanup-gate facts; `Dac` then binds `Dfr`;
the cleanup-gate root `Dgr` is downstream of `Dac` and the eventual selected
CREATED authorization triple. Neither `Dgr` nor a selected authorization value
enters the core, and BOOTSTRAP carries no fence member. The repair creates no
digest back-edge.

Every engine identity remains opaque. No Git, GitHub, repository, code-host,
provider, platform-specific path, operating-system credential-store, or
consumer vocabulary enters the core. Path construction is portable relative
contract vocabulary, not a host filesystem operation.

Structural parsing and `Dac` calculation do not certify `producerDigest`.
BOOTSTRAP composition must recompute `Dsc` from the exact closed reviewed
`BOOTSTRAP_INSTALL` operation/successor core and authenticated grant/installer/
identity facts. SUCCESSOR composition must recompute stable-promotion `Dsc` and
bind actual selected predecessor/candidate release, broker, admission, and
fence graphs. Copied fields, a supplied expected digest, a candidate verdict,
or this pressure-test outcome cannot grant promotion. Stable release N remains
the producer for N+1, and a worker or candidate release never certifies itself.

### Deferral boundary

The slice still excludes authorization state; native consume/removal;
post-selection observation and receipts; attachment; archive and tombstone;
selected epoch, proposal, tip, and CAS; composed equality; persistent lookup;
credential-broker calls; live capabilities; commands; filesystem mutation; and
runtime promotion. No schema bytes for those surfaces can be inferred from
this PASS. Each remains fail-closed pending its own literal ledger and fresh
independent removal review.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible disposition |
| --- | --- | --- | --- |
| Closed BOOTSTRAP/SUCCESSOR union | Cross-mode, unknown, or future fields could enter the authority preimage. | Deleting closure admits ambiguous or cyclic evidence. | Retain two direct closed branches; no nested proof bag or general map is needed. |
| Strict internal time order and bounded adjacent generations | Equal/reversed issue windows, noncanonical values, or wrap can alias reusable authority evidence. | Deleting a comparison admits invalid windows or generation pairs. | Retain core-only scalar checks; use no clock or broker abstraction in this parser. |
| Domain-separated `Dac` and constructed core path | Untagged bytes or caller-selected placement could substitute another contract or transaction. | Deleting either relation permits semantic cross-use or redirectable evidence. | Retain one framed canonical record and one transaction-derived core path. |
| Later exact fence-root path construction | A composition could pair `Dfr` with a different registered fence path family. | Deleting the later relation admits path substitution at composition. | Derive the exact root path from kind and transaction at composition; do not duplicate it in the core. |
| Authenticated later admission time | Internal issue/expiry order alone cannot prove when external admission occurred. | Deleting later admission timing could admit expired authority. | State the inclusive/exclusive relation now, but defer its authenticated evidence and validator to the composition ledger. |
| Explicit downstream deferral | A structural record could otherwise be mistaken for live mutation or promotion authority. | Deleting the boundary invites caller assertions and candidate self-certification. | Retain the fail-closed boundary; add no coordinator, receipt family, persistence reader, or runtime mutator. |

The repaired shape addresses the current authority, expiry, and path-
substitution threats without restoring the deleted sparse tree, inventory,
coordinator, epoch-split run, arbitrary-precision ordinal, diagnostic,
retention, or compaction mechanisms.

## Draft, board, and dependency assessment

The `ISS-002` draft mirrors the repaired census/count, derived path boundary,
digest order, structural implementation scope, compatibility evidence, and
downstream deferrals. No registered dependency edge changed, so the
transitively reduced roadmap remains intact. The board checker passed against
this repository's own registered delivery project; no consumer board was
consulted.

## Verification evidence

All six required gates ran through their checked-in wrappers strictly
sequentially. The standalone full-suite process fully exited before the
separate bootstrap verifier began. The increased timeout was used for the two
full-suite executions. No wrapper fallback, direct replacement command, or
overlapping test process was used.

1. `pnpm run planning:check` — PASS: `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS: `board contracts verified`.
3. `pnpm run format:check` — PASS: all matched files use Prettier style.
4. `pnpm run typecheck` — PASS: `tsc --noEmit -p tsconfig.json`.
5. Standalone `pnpm test` — PASS: 29 files and 362 tests in 103.00 seconds. The
   process fully exited before step 6 began.
6. Separate `pnpm run verify:bootstrap` — PASS: manifests and registries,
   formatting, typecheck, 29 files and 362 tests in 109.81 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

`PASS` clears exact head `4d49a2dce1259f7ee171b4bba78970d0298b4ae1`
only for the bounded recovery-authorization immutable-core implementation
surface named in this report. It does not authorize recovery-authorization
lifecycle/state, native/post-selection receipts, attachment, archive,
tombstone, composition, persistence, broker operations, capability, CAS,
runtime mutation, promotion, or the next ledger.

This record grants no merge, release, installation, external mutation, or self-
certification authority. A worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-123.md`; no implementation, test,
draft, issue, roadmap, board, or other planning file was edited, and no commit
was created.
