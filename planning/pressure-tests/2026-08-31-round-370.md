# ISS-022 experimental native candidate source — round 370

Static `PASS` at `641084e0a5a04b6aa28e64802a9556e4e527fed9` from independent
reviewer `/root/iss022_native_candidate_review`, Sol high, row 11. Initial
`f905835893750d5ff4db32e5f54010338ae13ae1` was BLOCK_FIXABLE for Darwin's
signed device field widening. The bounded repair preserves unsigned native
device bits, full Linux/inode widths, and tests the actual expressions with
compile-time boundary assertions. Those assertions have not yet compiled.

Six exports, environment-bound handles, fixed OS operations and factual call
records remain unchanged. Candidate and witness implementations were authored
independently. The bounded test-only Worker challenges cross-environment
handles; it adds no binding thread or process-tree claim.

Final source head `cb0a2dbf94a4394ea77a803513c8e68cc5efb90f` only relocates
the unchanged standalone test. Round 369 independently verifies its identical
blob and runtime path equivalence, leaving the original Node-only probe census
intact. No test or native behavior is inferred from this path-only check.

The host found no local C compiler. Official exact-Node headers and reviewed
source bytes were retained; preparation reports UNSUPPORTED with no compiler
argv, output or loaded module. No native compilation, lock observation or
capability grant follows. Hosted build and complete diagnostic observation
remain required before any behavioral acceptance.
