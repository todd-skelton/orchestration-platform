# ISS-003 repaired-packet review — round 242

`BLOCK_REPLAN` on current file head `bd5fd563e45774c4a59e9b729997262857537b79`.

The invocation exposes only six `ORCHESTRATION_*` values and denies ambient environment/platform access, so the config handler cannot select Windows/macOS/Linux defaults or read `LOCALAPPDATA`, `HOME`, or `XDG_STATE_HOME`.
Redaction is contradictory: selected duration environment/config values must appear in provenance, while every read value must be absent; reveal must write canonical paths to stdout while those path canaries must be absent there.
`stateRootDigest` is newly “domain-separated” without a domain/frame, but host-custody defines the same identity as plain SHA-256 of the UTF-8 file URL; downstream equality is therefore not implementable.
UNC state roots are admitted here, yet the required existing `platform-configuration/v1` parser accepts only strings beginning `file:///`, excluding canonical `file://server/share` URLs.
`configuration-paths/v1` never fixes the type/value relation for `revealed`, and realpath-string rechecks do not satisfy the draft's identity-checked read against same-path replacement.
V2 command rows contain only `argv` and `options`, but the runner must validate a “registered result contract”; no result-schema/outcome descriptor exists, forcing a hard-coded second registry.
The closed diagnostic codes omit the existing placeholder owner failure, while `{code,message}` plus non-interpolating messages has no field that can carry its fixed owning issue.
Passing a function an invocation object cannot prevent same-process access to raw argv/environment/stdout/exit; the claimed direct-output/exit fence needs an explicit static or execution boundary before its mutants are executable.
The four shell rows are not literal invocations: bash/zsh omit `-c`, PowerShell omits a command form, and “native escaping” supplies no cross-shell encoder for the required metacharacter argv matrix.
Precedence, zero-write, durable channel bytes, footprint, and adapter separation are otherwise closed; proportional repair is limited to these literals/fences, not wrappers, native helpers, journaling, or state machinery.
