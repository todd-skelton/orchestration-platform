# ISS-022 CREATE_ONCE_32_CONTENDERS slice — round 283

## Disposition

`PASS`.

This is the one bounded cold pressure round for the exact uncommitted diff based
on `adb3a9da299e39123d7b9df1df3546663d27840d`, including the untracked
`probes/portable-primitives/src/create-once.ts`. It authorizes only this
CREATE_ONCE slice, not an ISS-022 capability decision or portable profile.

## Decisive evidence

- The stable executor starts exactly 32 direct child executions, waits for all
  32 terminal `close` observations through `Promise.allSettled`, refuses any
  rejected child, then performs one parent-owned final readback. PASS requires
  32 coherent terminal rows, exactly one `CREATED` row whose bound readback is
  `41`, exactly 31 bound `ERROR/EEXIST` rows, and final readback `41` with no
  read error.
- Candidate text has no verdict authority. Child stdout must be the one
  canonical bound raw event; stderr, timeout, signal, output overflow,
  nonzero/null exit, malformed output, mixed census, wrong final bytes, or a
  literal `PASS` claim can produce only UNKNOWN or refusal.
- UNSUPPORTED cannot elevate to PASS: it requires all 32 coherent rows to carry
  the same admitted unsupported errno, plus parent-observed `ENOENT` and no
  readback. Mixed admitted codes, any unadmitted code, or an extant file is
  UNKNOWN.
- The normalizer snapshots before validation and rejects proxies, accessors,
  exotic prototypes, symbols, typed counts/flags, missing/extra fields, and
  non-32 arrays. Reduction uses only the frozen snapshot, closing mutation and
  repeated-access TOCTOU paths.
- Custody remains the established canonical absolute non-symlink directory
  boundary with source/custody overlap refusal; this slice adds only the fixed
  `create-once` leaf and returns no path or production custody authority.

## Proportionality and verification

The in-scope threat is a partial, forged, or nonterminal contender census being
promoted to the create-once token. Deleting the stable census/readback gate
admits that attack. The smallest credible alternative still needs 32 real child
processes because the literal ledger fixes 32 contenders; a sequencer or
in-process emulation would change the measured primitive. The 412-line source
and test change is one-sitting proportional and adds no helper, retry, cleanup,
PID/age inference, production service, or promotion authority.

- `pnpm exec vitest run test/conformance/iss022-handler.test.ts test/portable-primitives/filesystem-executor.test.ts` — 2 files, 37 tests passed.
- `pnpm run typecheck` — passed.
- `git diff --check` — passed.
