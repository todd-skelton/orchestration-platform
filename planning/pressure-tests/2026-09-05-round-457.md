# Pressure round 457 — ISS-011 split and ISS-055 registration

Status: ISS-011 split planning packet, absolute attempt 1 of the ordinary eight,
at protected base `e6c9d4f1bfeb1347ba4f229d1c965b3c3bb7725e`. This is a
planning-only artifact. It is neither independent review, source, hosted
execution evidence, registry admission, board reconciliation, authority, nor
ISS-011 closure; the controller host creates the ISS-055 board issue and mirrors
bodies after review.

## Authority and declared inputs

Authority is Todd's ruling on Decision #275, option C, posted by the controller
host on 2026-09-05. Its four numbered requirements are binding: (1) register a
successor slice ISS-055 under EPIC-MODULES carrying acceptance items 4, 5, and
the row-dependent half of 7, blocked by ISS-011 and by whichever registered
issue owns the delivery and repair executable consumers, or a stated proposal
when none does; (2) rewrite `planning/drafts/ISS-011.md` to cover only what has
landed, with a criterion-by-criterion closure census, moving the item 4, 5, and
7 narrative to ISS-055; (3) update `planning/drafts/EPIC-MODULES.md` children
and generated DAG and re-derive the blocked_by edges of ISS-012, ISS-039,
ISS-040, ISS-047, and ISS-021 against consuming text; (4) record R2, R3, and R8
as ISS-055 inputs.

Declared inputs: Decision #275 body and its two comments; the read-only ISS-011
frontier audit at protected main
`a41b0b4d1a83cf1b971e0f759bda8d00d0b328a6` (sections 1, 2, and 4 — the census,
the admissibility analysis, and risks R1-R8); `planning/roadmap.json` and
`scripts/planning/check.mjs`; the drafts ISS-011, EPIC-MODULES, EPIC-SELFHOST,
ISS-012, ISS-039, ISS-040, ISS-047, ISS-021, ISS-026, ISS-041, ISS-008, ISS-009,
ISS-013; `docs/architecture/module-abi.md`,
`docs/architecture/bootstrap-manifests.md`, `docs/architecture/routine-cycle.md`;
the ISS-011 pressure rounds 401, 434-439, 443, 444, 446, 448, 450, and 453;
merged PRs #266, #270, and #277; and `package.json` scripts.

## Delivery and repair consumer ownership

No registered issue owns the delivery and repair executable consumers. Every
candidate named in the ruling was checked against its own text:

| Candidate | Text checked | Verdict |
| --- | --- | --- |
| `ISS-026` engine cycle | `planning/drafts/ISS-026.md:96-102` — step 4 admits and invokes an already-composed registry entry whose digests equal the installed release manifest | Not the owner. It consumes the activated registry; it is also downstream of module packaging through `ISS-012`, so making it a prerequisite would invert the ABI's own order |
| `ISS-041` walking skeleton | `planning/drafts/ISS-041.md:21-32` — invokes "one inline trivial plan function" and "a stub review acceptance"; module registry admission is explicitly out; it "blocks nothing" | Not the owner. No delivery or repair invocation; the fixture is frozen and demoted when `ISS-026` lands |
| `ISS-008` worker lifecycle | `planning/drafts/ISS-008.md:20-25` — dispatch plan, ownership, liveness, reclamation; review semantics explicitly out | Not the owner. It invokes no module |
| `ISS-009` review authority | `planning/drafts/ISS-009.md:31-33` — "repair/replan taxonomy belongs to planning modules rather than the engine"; it consumes an opaque module disposition payload | Not the owner. It never invokes a repair entrypoint |
| `ISS-039` review/repair findings | `planning/drafts/ISS-039.md:69-74` — owns future review/repair module *content*, manifest rows, and fixtures, blocked by `ISS-011` | Not the owner. It owns content, not an executable consumer, and it consumes ISS-055; the reverse edge would cycle |

The runtime census agrees with the drafts: the only `REPLAN` producers are
`fixtures/walking-skeleton/src/review-module.ts:126` and
`modules/review/src/index.ts:157`, and both set
`moduleId: descriptor.moduleId` (`review`), never `repair`;
`modules/delivery/` and `modules/repair/` do not exist.

ISS-055 is therefore registered with `blockedBy: ["ISS-011"]` and an explicit
parked state with an un-park condition in its draft: a registered issue lands an
executable consumer that invokes a `delivery` entrypoint and a `repair`
entrypoint with real inputs, at which point ISS-055 gains a direct edge on that
owner. Decision #275 option A — a bounded walking-skeleton fixture-runtime
extension — is named there as the eventual route and was expressly not admitted
by the ruling.

## Edge derivation table

Every edge is kept, added, or dropped only against text that shows the
dependent consuming manifest rows, the generator's row-dependent behavior, or
the emitted bundle. Edges are also reduced: `scripts/planning/check.mjs`
`validateReducedEdges` fails any direct edge that is transitively implied, so a
dependent that gains `ISS-055` must drop its direct `ISS-011` edge, because
`ISS-055 → ISS-011` already carries it.

| Dependent | Before | After | Consuming text | Action |
| --- | --- | --- | --- | --- |
| `ISS-012` | `ISS-010, ISS-011, ISS-025` | unchanged | `planning/drafts/ISS-012.md:74-77` creates `packages/routing/` with "no shared-manifest or glossary edit"; its only manifest references (`:57-58`) are the release manifest, not `modules/manifest.json`; no acceptance criterion consumes a row, the generator, or a bundle | No `ISS-055` edge; `ISS-011` kept |
| `ISS-039` | `ISS-009, ISS-011` | `ISS-009, ISS-055` | `planning/drafts/ISS-039.md:69-70`: "Implementation owns the future review/repair module content, manifest rows, fixtures" | `ISS-055` added for the manifest rows; `ISS-011` dropped as transitively implied |
| `ISS-040` | `ISS-011` | `ISS-055` | `planning/drafts/ISS-040.md:77-78`: "Implementation owns the future planning-module content, its manifest row, fixtures" | `ISS-055` added; `ISS-011` dropped as transitively implied |
| `ISS-047` | `ISS-010, ISS-011` | `ISS-010, ISS-055` | `planning/drafts/ISS-047.md:68-69`: "Implementation owns the future intake module content, its manifest row, fixtures" | `ISS-055` added; `ISS-011` dropped as transitively implied |
| `ISS-021` | `ISS-008, ISS-023` | `ISS-008, ISS-023, ISS-055` | `ISS-011` acceptance item 4 as it stood at base: "One host-neutral module artifact is byte-stable on all targets; concrete host discovery is tested by `ISS-021`", and Decision #275's frontier section: "Blocked: ISS-011 closure and, transitively, ISS-021 host discovery evidence that depends on the four-row bundle" | `ISS-055` added for the emitted bundle; the two existing edges are unaffected and neither reaches `ISS-055` |
| `ISS-055` | — | `ISS-011` | `ISS-011` retains the module ABI, the landed `planning` and `review` entrypoint semantics, the dispatch brief and catalog structures, and the row-independent generator invariants that ISS-055 extends | New row registered |

Weakest link, stated for the reviewer: the `ISS-021` edge. `ISS-021`'s own
acceptance criteria never name `modules/manifest.json` or the emitted bundle;
the edge rests on `ISS-011` item 4's pairing of bundle byte-stability with
`ISS-021` host discovery and on Decision #275's explicit frontier statement.
Dropping it would leave the ruling's re-derivation of `ISS-021` unanswered.

Acyclicity and reduction were re-verified by `pnpm planning:check` at the final
head. `EPIC-SELFHOST.md`'s generated DAG row for `ISS-021` had to change with
the roadmap; that file is outside the ruling's named list but inside what the
checker requires, and it is the only change to it.

## Closure census summary

`planning/drafts/ISS-011.md` gains a `Closure census` section mapping each
retained criterion to its landed round, merged PR, and executable evidence with
file:line citations. Retained: 1, 2, 3, 6, 8, 9 and item 7's row-independent
half. Moved: 4, 5, and item 7's row-dependent half; their numbers are retained
in the criteria list as explicit pointers so the historical numbering used by
Rounds 401 and 434-453 keeps resolving.

- 1 — Rounds 439, 443, 444, 446, 448; PR #266.
  `fixtures/walking-skeleton/test/consumer.test.ts:1274` and `:1422`;
  `fixtures/walking-skeleton/test/review-consumer.test.ts:452` and `:659`.
- 2 — Rounds 439, 448; PR #266. `consumer.test.ts:1275`, `:1425`, `:1824`;
  `modules/planning/src/index.ts:34-39`.
- 3 — Rounds 439, 450; PR #270. `packages/contracts/src/dispatch.ts:14-24`;
  `test/contracts/dispatch.test.ts:148` and `:121`;
  `modules/planning/src/index.ts:122-127`.
- 6 — Rounds 437, 438, 439, 448; PR #266. `consumer.test.ts:1761` and `:1721`,
  fourteen ambient and effect mutants each, all refused on identity.
- 7 (retained half) — Round 453; PR #277 merged as `f311c111`.
  `test/module-registry-placeholder.test.ts:101`, `:114`, `:133`, `:138`,
  `:143`, `:152`, `:168`, `:195`, `:207`, `:230`, `:238`, `:247`, `:258`.
- 8 — Rounds 439, 450; PR #270. `test/contracts/dispatch.test.ts:121`, `:107`,
  `:247`.
- 9 — Round 450; PR #270. `packages/contracts/src/dispatch.ts:348`, `:355-360`,
  `:378-390`; `test/contracts/dispatch.test.ts:186` and `:208`.

The census also records four honest residual fragments rather than claiming
them: criterion 1's "generated denylist scan" has no landed implementation
anywhere in the repository and modules emit no mutation transcript; criterion
2's "manifest validation" reduces to the placeholder `"[]\n"` assertion;
criterion 6's landed mechanism is source-identity denial plus independent byte
review, not an executable effect classifier, because Round 437's classifier
failed on all three hosted runners and was removed; and criterion 9's
release-manifest join is unevidenced. Those fragments belong to the packaging,
manifest, or release surface that moved to ISS-055, ISS-014, or ISS-020, except
criterion 1's mutation-transcript half, which has no successor and is
permanently inapplicable because criterion 6 forbids module mutation, and
criterion 6's own recorded limitation, which stays with the retained criterion.
This record does not resolve them and does not claim ISS-011 closure;
that decision belongs to the controller host and Todd after independent review.

## Scope

Planning registration only:

- register ISS-055 in `planning/roadmap.json` under EPIC-MODULES at milestone M2
  with `blockedBy: ["ISS-011"]`, and re-derive the five dependent rows above;
- author `planning/drafts/ISS-055.md` in the repository's issue-standard form,
  carrying moved acceptance items 4, 5 and the row-dependent half of 7 verbatim
  with their evidence sentences, the Round 434 manifest and ordered-build
  definition verbatim as a quoted block, the definition-then-source pattern,
  R2/R3/R8 as declared inputs, and the parked state with its un-park condition;
- narrow `planning/drafts/ISS-011.md` scope, criteria, verification plan, and
  predicted footprint; add Round 448 and Round 450 narrative and the closure
  census; frontmatter unchanged;
- update `planning/drafts/EPIC-MODULES.md` children, generated DAG, and child
  index, and the `ISS-021` DAG row in `planning/drafts/EPIC-SELFHOST.md`;
- update the frontmatter `blocked_by` of ISS-039, ISS-040, ISS-047, and ISS-021
  to match the roadmap.

## Non-goals

No source, module, manifest, generator, fixture, fixture-runtime, ABI document,
or `package.json` change. `modules/manifest.json`,
`modules/build/generate-registry.mjs`, `modules/.generated/registry.ts`,
`modules/planning/src/index.ts`, `modules/review/src/index.ts`,
`docs/architecture/**`, `packages/**`, `test/**`, `fixtures/**`, `scripts/**`,
`adapters/**`, `bootstrap/**`, `config/**`, and `.github/workflows/**` are
untouched. No board issue is created, no issue is closed, nothing is pushed, no
PR is opened, no provider or hosted run is invoked, and no ABI amendment,
interim manifest row, or fixture-runtime extension is proposed. This record
claims no independent review, no hosted evidence, and no ISS-011 closure;
ISS-011 closure is the controller host's act after this lands. `ISS-012`'s
edges are deliberately unchanged.

## Acceptance evidence

- `pnpm planning:check` passes at the exact final head, which is what proves
  registered-versus-filesystem draft parity, frontmatter/roadmap `blocked_by`
  parity for every changed row, epic child membership, both generated DAG
  blocks, acyclicity, transitive reduction, and the unchanged root command
  census.
- Every retained ISS-011 criterion has a landed citation with a round, a merged
  PR, and a file:line reference, and every residual fragment is named rather
  than implied.
- Every moved criterion appears exactly once in ISS-055: item 4 as ISS-055
  acceptance item 1, item 5 as item 2, and item 7's row-dependent half as item
  3. ISS-011 retains their numbers only as pointers with no assertion.
- Each added or kept edge in the derivation table has quoted consuming text, and
  each dropped edge is dropped only because transitive reduction forbids it.
- The delivery/repair owner search is recorded candidate by candidate with the
  text that excludes each one.

## Verification

Local gates only, at the exact final head: `pnpm typecheck`,
`pnpm format:check`, `pnpm planning:check`, `pnpm planning:board-check`,
`git diff --check`, and `git status --porcelain`. `planning/` is not a
`scripts/format.mjs` target, so this footprint changes no formatter input.
`pnpm planning:board-check` is expected to fail until the controller host
reconciles the board: ISS-055 has no registered board item, and the ISS-011,
EPIC-MODULES, EPIC-SELFHOST, ISS-021, ISS-039, ISS-040, and ISS-047 bodies no
longer match their source drafts, for eight mismatches in total. Mismatches
for keys outside this footprint are pre-existing board drift, not caused by this
change: running the same gate against the clean base at `e6c9d4f` reports
ISS-054 and ISS-048 and nothing else. The board mirror moves independently of
this lane, so a reviewer should compare the base and head mismatch sets rather
than a fixed count. The hosted three-OS bootstrap is the landing gate for this
docs-only change; it executes nothing this record asserts. No local Vitest,
module, native, provider, probe, or lock experiment was run.
Independent semantic review by a different lane is required before the PR.

## Predicted footprint

Exact file list, and the actual footprint:

New:

- `planning/drafts/ISS-055.md`
- `planning/pressure-tests/2026-09-05-round-457.md`

Modified:

- `planning/roadmap.json`
- `planning/drafts/ISS-011.md`
- `planning/drafts/EPIC-MODULES.md`
- `planning/drafts/EPIC-SELFHOST.md`
- `planning/drafts/ISS-021.md`
- `planning/drafts/ISS-039.md`
- `planning/drafts/ISS-040.md`
- `planning/drafts/ISS-047.md`

`planning/drafts/EPIC-SELFHOST.md` is one file beyond the ruling's named list;
it is required because `ISS-021` is an EPIC-SELFHOST child and the checker
validates that epic's generated DAG row against the roadmap. Its only change is
that one row. Any eleventh changed file is footprint growth and forces a
replan.

## Review attack surface

Attack: an edge added or dropped without consuming text, especially the
`ISS-021` edge, whose evidence is `ISS-011` item 4 and the Decision #275
frontier statement rather than `ISS-021`'s own criteria; a dropped `ISS-011`
edge presented as a scope claim rather than as transitive reduction; a retained
criterion lost or silently reworded in the move; a moved criterion appearing
twice or paraphrased instead of verbatim; a closure census citing a round, PR,
or test line that did not land, or a line number that has drifted; residual
fragments buried rather than named; ISS-055 scope widened beyond items 4, 5 and
the row-dependent half of 7, or an interim manifest row, ABI amendment, or
fixture-runtime extension smuggled into it; a verification command that does not
exist in `package.json`, or a new command silently registered; the retained
`4.` and `5.` numbers in ISS-011 read as retained scope; `modules:test` and
`modules:conformance` presented as landed evidence rather than as
`capability-not-implemented` stubs; the parked state read as a schedule; and any
wording implying ISS-011 is closed, ISS-055 is reviewed, or the board is
reconciled by this record.

## Attempt ledger

Rounds 401 and 434-453 remain cumulative ISS-011 lineage and are not reset by
this artifact. For this new artifact, under the ordinary eight-lane ceiling:

1. This planning packet (attempt 1).
2. Independent review BLOCK_FIXABLE (F1, F2) (attempt 2).
3. Bounded correction 6c47ecd (attempt 3).
4. Independent re-review BLOCK_FIXABLE (F3: attribution contradicted at two
   surfaces; parked, Decision #285) (attempt 4).
5. Bounded correction under Decision #285 ruling A (this commit) (attempt 5).
6. Independent exact-head re-review pending by a different lane (attempt 6).

## Routing

model-routing row 8 (decomposition and dependency-edge judgment),
claude-opus-5 at high reasoning effort, placement provisional, budget intent at
most $12, actual spend unknown, no new ceiling requested or authorized. This
lane transcript is the routing record.
