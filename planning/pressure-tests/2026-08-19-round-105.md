# Pressure test — round 105 consumption `Dgb` removal review

**Disposition:** `PASS`

**Reviewed candidate head:** `63d91c5a5ed9d8a06d479d972d0ca82b0af8725b`

**Reviewed parent boundary:** `aa6a34fe51c262f7c5c5f98e20c54be581e3c1b6`

This is a fresh cold independent removal review of the exact initially clean
head above on `iss-002-versioned-contracts`. It is bounded to whether the new
`authority-history-genesis-bootstrap-input/v1` input to
`validateBootstrapAnchorConsumptionBinding` removes `OP-R104-001` without
narrowing either valid origin or widening the consumption, E0, or live-
authority boundary.

The review began with `AGENTS.md`, `docs/planning/work-model.md`, the complete
decision authority in `docs/architecture/contract-decisions.md` and its
incorporated external-bootstrap and simplified-authority ledgers, ISS-002,
rounds 83, 96, 100, 103, and 104, the exact parent-to-candidate delta, and the
current affected implementation and tests cold. Author reasoning was not an
authority source. No Chase Sets repository, issue, project, board, backlog,
consumer system, or runtime was accessed. The board gate read only this
repository and its registered platform delivery project.

## Disposition summary

`PASS`. `OP-R104-001` is removed. The public consumption validator now parses
the actual closed `Dgb`, recomputes it, requires that digest to equal the exact
selected E0 core's `genesisBootstrapInputDigest`, and requires
`Dgb.useIntentDigest` to equal the recomputed supplied use intent. Every other
`Dgb` preimage member is directly equal-bound to the parsed anchor, selected
owner/anchor ACTIVE graph, or selected core's `Dsc`.

Both genesis- and successor-origin valid consumption graphs still return `[]`.
The three Round-104 intent substitutions now refuse in both origins; moving
the intent and final receipt first reaches `genesisInput.useIntentDigest`, and
moving and rehashing `Dgb` as well reaches
`core.genesisBootstrapInputDigest`. Every independent `Dgb` member mutant and
hostile/null `Dgb` input also refuses. An in-memory equality-deletion probe
proved the two new bridge comparisons are discriminating rather than
incidental.

No schema, schema version, digest domain, path, registry row, receipt member,
producer kind, lifecycle transition, persistence rule, runtime writer,
capability, or live-authority surface changed. `Dgse`, owner teardown archive,
owner or anchor `RETIRED`, teardown, reinstall, release cleanup/recovery, and
later promotion remain outside this bounded change and unauthorized.

## Exact delta

The exact
`aa6a34fe51c262f7c5c5f98e20c54be581e3c1b6..63d91c5a5ed9d8a06d479d972d0ca82b0af8725b`
delta has two paths, 98 insertions, and one deletion:

- `packages/contracts/src/consumption.ts`: 54 insertions and one deletion to
  accept and parse the existing `Dgb`, recompute its digest, and add the
  complete consumption-side equality set; and
- `test/contracts/genesis.test.ts`: 45 insertions to pass the fixture's actual
  `Dgb` and exercise the three Round-104 alternate-intent substitutions before
  and after coordinated `Dgb` rehash.

`git diff --check` passes. No authority document, planning topology, public
schema census, registry, index/export, package, command, path constructor,
runtime service, adapter, consumer, or unrelated contract family changed.

## `OP-R104-001` is removed

The selected composition is now:

```text
supplied use intent -> recomputed useIntentDigest
                          |
actual closed Dgb --------+ (Dgb.useIntentDigest equality)
       |
       +-> recomputed Dgb == selected core.genesisBootstrapInputDigest
                                      |
                                      +-> selected Dbg/Dr/Dt/Dgp graph
```

`parseGenesisBootstrapInput` uses the existing closed-record parser and exact
14-member `v1` census. The consumption validator collects its prefixed
findings with every other input before any digest work and returns on any parse
failure. Only its detached parsed value is then passed to
`computeGenesisBootstrapInputDigest`. Null and a throwing `ownKeys` proxy both
returned findings in each origin without throwing:

- null: `genesisInput:record:object-required`; and
- throwing proxy: `genesisInput:value:proxy-refused`.

The recomputed `Dgb` comparison and all direct preimage equalities are exact:

| `Dgb` member | Independently parsed or recomputed source |
| --- | --- |
| `bootstrapAnchorActiveReceiptDigest` | actual ACTIVE anchor proposal |
| `bootstrapAnchorActiveTipDigest` | actual ACTIVE anchor tip |
| `bootstrapAnchorActiveValueDigest` | actual ACTIVE anchor value |
| `bootstrapAnchorDigest` | recomputed parsed anchor `Dba` |
| `bootstrapGrantDigest` | parsed anchor grant |
| `bootstrapTransactionId` | parsed anchor transaction |
| `destinationDigest` | parsed anchor destination |
| `destinationOwnerActiveReceiptDigest` | actual ACTIVE owner proposal |
| `destinationOwnerActiveTipDigest` | actual ACTIVE owner tip |
| `destinationOwnerActiveValueDigest` | actual ACTIVE owner value |
| `globalBootstrapIdentityDigest` | parsed anchor bootstrap identity |
| `schemaVersion` | closed parser literal `authority-history-genesis-bootstrap-input/v1` |
| `successorCoreDigest` | parsed selected E0 core's `successorCoreDigest` |
| `useIntentDigest` | recomputed supplied use intent |

The recomputed complete `Dgb` additionally equals
`core.genesisBootstrapInputDigest`. The core itself remains selected through
the unchanged E0 proposal, tip, `Dgp`, anchor CONSUMED, and owner CONSUMED
composition. There is no caller-supplied digest expectation in place of either
record.

## Adversarial executable evidence

The committed fixture test first preserves valid genesis- and successor-origin
controls. Its new three-row test changes exactly the Round-104 alternatives:

- `intent.destinationOwnerActiveTipDigest`;
- `intent.reviewedInstaller.reviewReceiptDigest`; and
- `intent.proposedGenesisInput.globalIdentityDigest`.

For each row it recomputes the final receipt's `useIntentDigest`; the public
validator refuses at `genesisInput.useIntentDigest:mismatch`. It then also
changes `Dgb.useIntentDigest`, which recomputes a different `Dgb`; the validator
refuses at `core.genesisBootstrapInputDigest:mismatch`.

An independent read-only in-memory bundle of the exact checked-out source and
committed fixture repeated all three rows against both genesis and successor
origins with guaranteed-distinct digest values. All six intent-only attacks
returned only the named `Dgb.useIntentDigest` mismatch. All six coordinated
intent plus rehashed-`Dgb` attacks returned only the named core-to-`Dgb`
mismatch. Moving the core digest and receipt's core digest as well still
refused through the already-selected downstream graph:

- `runtimePostBinding:bootstrapGenesisCoreDigest:mismatch`;
- `runtimePostBinding:proposal.producerDigest:mismatch`; and
- `anchorConsumedValue.bootstrapGenesisCoreDigest:mismatch`.

The same independent probe changed each of all 14 `Dgb` members separately in
both origins. The future schema literal refused in parsing. Every other field
produced `core.genesisBootstrapInputDigest:mismatch` plus its exact direct
`genesisInput.<field>:mismatch`; all 28 origin/member controls refused.

The equality-deletion probe bundled three in-memory source variants without
writing a file:

| Source variant | Intent + receipt moved | Intent + receipt + `Dgb.useIntentDigest` moved |
| --- | --- | --- |
| exact checked-out source | `genesisInput.useIntentDigest:mismatch` | `core.genesisBootstrapInputDigest:mismatch` |
| delete only core-to-`Dgb` equality | `genesisInput.useIntentDigest:mismatch` | `[]` |
| delete only `Dgb`-to-intent equality | `[]` | `core.genesisBootstrapInputDigest:mismatch` |

This is a direct deletion test for both bridge edges. The remaining direct
preimage comparisons are likewise necessary to relate the selected `Dgb`
bytes to the independently parsed ACTIVE and anchor/core siblings; hashing the
siblings without those comparisons would not choose which repeated fact is
authoritative.

## Acceptance and authority boundaries remain exact

There is no valid-origin narrowing: the public validator returns `[]` for the
committed genesis-origin and successor-origin graphs with their actual `Dgb`.
The added input is the already-authorized, already-exported non-persisted
`authority-history-genesis-bootstrap-input/v1` preimage; it adds no record
family or persistence location.

There is no acceptance widening. A different use intent cannot be moved with
only the final receipt, cannot be moved with an arbitrary rehashed `Dgb`, and
cannot be moved through the selected core without reconstructing and selecting
a different complete upstream E0 graph. Every `Dgb` field is independently
closed to its selected sibling.

Candidate and worker self-certification remain impossible in this slice. The
unchanged selected E0 proposal must still be the
`REVIEWED_BOOTSTRAP_GENESIS` producer with null prior and null selected epoch,
and its producer digest remains the recomputed selected `Dbg`. The patch adds
no producer kind, verdict input, selected epoch, runtime handle, writer, or
promotion path. Structural `[]` remains non-authoritative until the separate
ISS-004/ISS-022 live custody boundary grants a private lock-scoped projection.

`authority-history-genesis-selection-evidence/v1` (`Dgse`) semantic binding,
owner teardown archive and owner `RETIRED` CAS, anchor archive/teardown and
anchor `RETIRED` CAS, E0 teardown, exact reinstall context, recovery, runtime
state service, release cleanup/fence/archive, installation, promotion, and all
later consumers remain separately unauthorized. Neither changed production
line introduces or consumes any of those surfaces.

## Proportionality

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible shape |
| --- | --- | --- | --- |
| Actual closed `Dgb` input | A final receipt names an intent other than the one selected upstream. | Supplying no/malformed `Dgb` refuses; without the input the Round-104 attacks were accepted. | Reuse the existing non-persisted record and parser. |
| `Dgb.useIntentDigest` equality | The caller moves the supplied intent and receipt together. | Deleting only this equality makes the intent-only attack return `[]`. | One direct comparison to the recomputed intent. |
| core-to-recomputed-`Dgb` equality | The caller also supplies an arbitrary `Dgb` rehashed around the moved intent. | Deleting only this equality makes the coordinated attack return `[]`. | One recomputed digest comparison to the selected core. |
| Direct `Dgb` preimage equalities | A selected `Dgb` repeats a different owner/anchor ACTIVE, anchor identity, transaction/grant, or `Dsc`. | Each independent member mutant refuses through its named comparison and changed `Dgb`; removing a comparison leaves no authority choosing between repeated sibling facts. | Thirteen comparisons inside the existing validator; no proof wrapper. |
| Structural/live authority split | Pure hashes become a serializable mutation capability. | Treating `[]` as authority bypasses the mandated live custody service. | Keep this module pure and add no capability or runtime mutator. |

The repair is exactly the smallest shape prescribed by Round 104: expose and
validate the existing selected `Dgb` preimage. A second receipt, proof record,
digest domain, path, pointer, history walk, coordinator, capability, or later
lifecycle state would add no discriminating evidence.

## Verification evidence

All complete gates ran strictly sequentially. The standalone full suite exited
before bootstrap verification began; bootstrap exited before the focused
contract slot began; compatibility began only after focused contracts exited.
No full and bootstrap process overlapped.

1. Planning — PASS, `planning contracts verified`, through
   `node scripts/planning/check.mjs`. The `pnpm run planning:check` wrapper was
   silent for the prior documented 120-second package-manager window and was
   stopped before the direct literal checker ran.
2. Board — PASS, `board contracts verified`, against only this repository and
   its registered delivery project. The restricted direct run failed closed at
   its GitHub CLI subprocess with `spawn EPERM`; the identical approved
   read-only rerun passed.
3. Format — PASS, all matched files use Prettier style. The restricted direct
   run failed closed at its pinned Prettier subprocess with `spawn EPERM`; the
   identical approved read-only rerun passed.
4. Typecheck — PASS with pinned TypeScript 7.0.2 through
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`.
5. Standalone full test — PASS, 28 files and 315 tests in 120.25 seconds. The
   named `pnpm test` wrapper was silent for the same 120-second package-manager
   window and was stopped. The direct pinned Vitest command then failed before
   loading configuration at sandboxed `spawn EPERM`; its identical approved
   read-only run passed.
6. Separate bootstrap verification — PASS: manifests and registries, format,
   typecheck, 28 files/315 tests in 117.04 seconds, private compositions,
   planning contracts, and source-tree status invariance. Its restricted run
   failed closed at the first Git subprocess with `spawn EPERM`; the identical
   approved read-only rerun passed.
7. Focused ISS-002 contracts — PASS, 18 files and 118 tests in 7.48 seconds.
   The restricted capability slot failed before tests at sandboxed
   `spawn EPERM`; the identical approved read-only rerun passed.
8. Focused ISS-002 compatibility — PASS, one file and one test in 1.07 seconds.
   The restricted capability slot failed before tests at sandboxed
   `spawn EPERM`; the identical approved read-only rerun passed.

The package-manager stalls and sandbox child-process denials occurred before
semantic checker/test execution and are infrastructure/contention evidence,
not assertion failures. The approved commands were unchanged read-only reruns.
The adversarial and equality-deletion probes bundled exact checked-out sources
and committed fixtures entirely in memory; they wrote no probe, source, test,
fixture, or generated file.

## Final status

`PASS` for exact candidate head
`63d91c5a5ed9d8a06d479d972d0ca82b0af8725b`, with parent boundary
`aa6a34fe51c262f7c5c5f98e20c54be581e3c1b6`.

`OP-R104-001` is removed without acceptance narrowing or widening. The exact
selected `Dgb` now authenticates the supplied use intent through two
independently deletion-tested equalities, every `Dgb` preimage is bound to its
parsed selected sibling, both origin branches remain valid, and candidate
self-certification remains impossible. This PASS is confined to the bounded
consumption composition and grants no merge, runtime, installation,
promotion, teardown, retirement, `Dgse`, or live mutation authority.
