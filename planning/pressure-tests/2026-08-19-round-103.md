# Pressure test — round 103 active-release repair removal review

**Disposition:** `PASS`

**Reviewed candidate head:** `4c69c0995a5dcaab4cb462cf0e22c21a37868c4c`

**Reviewed parent boundary:** `9f8d12a3b19ed237ddb9c1bd616a01412b82732a`

This is a fresh cold independent implementation-removal review of the exact
initially clean head above on `iss-002-versioned-contracts`. It is bounded to
whether the six-line common-proposal repair removes `OP-R102-001` without
regressing the already-confirmed removal of `OP-R97-002`. It began from
`AGENTS.md`, `docs/planning/work-model.md`, the complete decision authority in
`docs/architecture/contract-decisions.md`, the incorporated common-pointer,
external-bootstrap, and simplified-authority ledgers in
`docs/architecture/supervisor-contract.md`, ISS-002,
`docs/contracts/compatibility.md`, rounds 97, 101, and 102, the exact
parent-to-candidate delta, and the current pointer, genesis, authority,
registry, export, compatibility, and focused test surfaces cold.

No Chase Sets repository, issue, project, board, backlog, consumer, or runtime
was accessed. The required board gate read only this repository and its
registered platform delivery project. This review grants no merge,
installation, bootstrap execution, release, promotion, runtime mutation, live
capability, consumption, teardown, receipt production, or self-certification
authority.

## Disposition summary

| Attack surface | Round-103 result | Exact evidence |
| --- | --- | --- |
| `OP-R102-001` selected-epoch/null-prior branch | **Removed** | The public proposal parser now requires all three prior digests to be SHA-256 whenever `pointerKind=ACTIVE_RELEASE` and the producer is not the reviewed-bootstrap arm. The formerly accepted selected-epoch/null-prior proposal refuses with `producerKind:active-release-prior-mismatch`. |
| Initial active-release branch | **Exact** | `ACTIVE_RELEASE + REVIEWED_BOOTSTRAP_GENESIS` accepts only with null prior and authority-epoch triples, `VALUE_PROPOSED`, and `SELECT`. Complete or partial non-null prior/epoch attacks refuse. |
| Later active-release branch | **Exact** | `ACTIVE_RELEASE + SELECTED_EPOCH` accepts with complete non-null prior and authority-epoch triples. Null or partial prior and partial/null epoch attacks refuse. |
| Initial authority bootstrap | **Preserved** | `STATE_MUTATION_AUTHORITY_ROTATION + REVIEWED_BOOTSTRAP_GENESIS` with null prior and epoch remains accepted, and both valid E0 branches still compose. |
| `OP-R97-002` selected-release disconnect | **Still removed** | E0 still parses the actual active-release value/proposal/tip, derives `Dp/Dv/Dr/Dt` and mutation identity, binds the bootstrap producer to recomputed `Dsc`, and equal-binds the value to the reviewed operation, `Dsc`, and selected authority quartet. |
| Canonical value and compatibility | **Exact** | The direct value remains the seven-member `active-release/v1` record. Canonical bytes, record digest, common `Dv`, public dispatch, one-entry version census, exact-current compatibility, and hostile-shape totality all pass. |
| Later lifecycle surface | **Not widened** | The repair adds only the prior-lineage condition. No producer, schema, member, path, receipt, cleanup/fence/archive machine, runtime mutator, capability, or promotion execution surface changed. |

No fresh definite blocker was found. The exact candidate removes
`OP-R102-001`, preserves the Round-102 proof that `OP-R97-002` is removed, and
stays within the Round-101 authority design.

## Public proposal admission is the exact two-branch union

The common parser first closes the proposal record, then proves each prior
triple is either all null or all SHA-256 and proves the producer's authority-
epoch triple is null for reviewed bootstrap or complete for selected epoch.
The new condition is evaluated after the closed producer census, so
`!bootstrap` is exactly `SELECTED_EPOCH` at that point. It requires a complete
prior triple only for `ACTIVE_RELEASE`; it does not alter another pointer
family's decided lifecycle.

An independent read-only in-memory bundle of the exact checked-out source
produced this matrix:

| Proposal shape | Public parser result |
| --- | --- |
| active release, reviewed bootstrap, null prior, null epoch | accepted |
| active release, selected epoch, complete prior, complete epoch | accepted |
| active release, selected epoch, null prior, complete epoch | refused: `producerKind:active-release-prior-mismatch` |
| active release, reviewed bootstrap, complete prior, null epoch | refused: `producerKind:bootstrap-selection-mismatch` |
| active release, reviewed bootstrap, null prior, complete epoch | refused: `producerKind:epoch-mismatch` |
| active release, reviewed bootstrap, partial prior | refused: `priorTriple:partial` |
| active release, selected epoch, partial prior | refused: `priorTriple:partial` |
| active release, selected epoch, partial epoch | refused: `producerKind:epoch-mismatch` |
| cleanup gate, reviewed bootstrap, null prior/epoch | refused: `producerKind:bootstrap-selection-mismatch` |
| active release, reviewed bootstrap with tombstone intent or remove outcome | refused: `producerKind:bootstrap-selection-mismatch` |
| active release, unregistered candidate producer | refused: `producerKind:invalid` |
| authority rotation, reviewed bootstrap, null prior/epoch | accepted |

This proves the initial active-release selection has only the reviewed-
bootstrap/null/null shape, while every later selected-epoch active-release
proposal has both predecessor lineage and a selected stable authority epoch.
It also preserves the separately decided initial authority-bootstrap branch.

The deletion test removed only the new condition from an in-memory normalized
copy of `pointer.ts`, bundled that copy without writing it, and parsed the
same selected-epoch/null-prior proposal. Exact checked-out code refused it;
the guard-deleted code accepted it. The committed pointer test contains that
same rejecting row and the complete-prior acceptance control, so the evidence
discriminates deletion of the repair rather than merely restating it.

## Seven-member value, canonical bytes, and registry remain exact

The literal value census remains, in canonical member order:

1. `independentReviewReceiptDigest`;
2. `installedBytesDigest`;
3. `releaseDigest`;
4. `releaseManifestDigest`;
5. `releaseSubjectDigest`;
6. `reviewedInstallerDigest`; and
7. `schemaVersion`, exactly `active-release/v1`.

All six digests are required non-null lowercase SHA-256 values and
`releaseDigest` must equal `releaseSubjectDigest`. Missing, extra, null,
wrong-type, unequal-subject, unknown, and future records refuse. The exact
test vector serializes as one ascending-key, LF-terminated JSON object. Its
canonical record digest is
`ed208ffe5d4618875aedd957cb0d1f84addf90a98de575e8158bd4c2e4930443`;
under the committed stable pointer identity its common value digest is
`0486d1e2d49f5fcc6758d04d62043fd88d3af5b77e1accee8444176405f56eb1`.
Both were independently reproduced from an in-memory bundle, and the latter
is now a committed focused golden.

`parseContract` still dispatches the value through the pointer graph parser.
The vocabulary registry names its exact fields, `pointerGraphSchemaVersions`
contains exactly one `active-release/v1`, the public schema census registers
that current version once, and compatibility reads only exact current `v1`
while refusing future, missing, and unknown versions. The selected release
root remains exactly `releases/<releaseDigest>/`.

## `OP-R97-002` remains removed

The repair changes no genesis source. Cold inspection reconfirmed that the E0
core validator takes the actual closed active-release value, proposal, and tip
and derives rather than accepts:

- stable `Dp` from literal kind/path, the anchor's installation/project/state
  identities, transaction equal to installation ID, and source `none`;
- the empty VALUE position digest from `{ "mode": "VALUE", "parts": {} }`;
- `Dv` from the parsed seven-member value;
- mutation identity from that `Dp`, empty position, null prior, successor `Dv`,
  and `SELECT`;
- `Dr` from the actual proposal and `Dt` from the actual tip; and
- `Dsc` from the parsed successor core and exact parsed reviewed operation.

The N0 proposal must then be `ACTIVE_RELEASE`,
`REVIEWED_BOOTSTRAP_GENESIS`, `VALUE_PROPOSED`, and `SELECT`, with null prior
and epoch triples and `producerDigest` equal to that recomputed `Dsc`. The tip
is equal-bound to derived `Dp/Dv/Dr`, and the selected E0 authority value's
active-release `Dp/Dt/Dv/Dr` fields equal that actual graph.

The six direct release equalities also remain present: release/subject,
manifest, installed-tree readback, independent review, and reviewed installer
equal the `BOOTSTRAP_INSTALL` operation, while release/subject, manifest,
installed bytes, and review additionally equal `Dsc`. The successor-core
digest routine itself recomputes `Dop`, checks those reviewed operation/core
equalities, and then derives `Dsc`; a caller cannot replace both with an
unrelated digest expectation. No `coreExpectationFields` or four-digest
active-release expectation has returned.

The committed coordinated foreign-value reconstruction changes all six value
digests, rebuilds `Dv`, mutation, proposal/`Dr`, tip/`Dt`, all four authority
members, and the E0 authority `Dv`; it refuses on every operation-owned value
equality. The committed foreign-`Dp` reconstruction rebuilds `Dv/Dr/Dt`, all
four authority members, and the E0 authority `Dv`; it refuses the independently
derived active-release pointer identity.

A further independent in-memory reconstruction supplied a complete prior and
complete selected authority epoch, changed the producer to `SELECTED_EPOCH`,
recomputed the active-release mutation, proposal `Dr`, tip `Dt`, authority
quartet, and E0 authority value digest. Both untouched genesis-anchor and
successor-anchor fixtures returned `[]`; the coordinated self-epoch attack
refused with `activeReleaseProposal.mutationId:mismatch` and
`activeReleaseProposal:not-reviewed-bootstrap-genesis`. Thus the public later
branch is admitted for its later consumer without becoming an N0 substitute.

## Totality, compatibility, and scope boundary

Public proposal parsing catches closed-record or semantic failures and returns
findings. Independent throwing-proxy inputs to both proposal and active-release
value parsing returned `value:proxy-refused` without throwing. The committed E0
tests also pass hostile value/proposal/tip proxies through the composed
validator without an exception, while valid genesis and successor E0 core/post
compositions remain accepted. Focused compatibility proves the sorted current
schema census and rejects legacy, future, missing, and unknown versions.

The repair is local to proposal admission. It does not add a candidate or
worker producer, selected-epoch key, nested proof, standalone digest, second
pointer, directory census, installation behavior, later promotion operation,
cleanup gate, recovery fence, cleanup archive, tombstone producer, runtime
writer, lock authority, or capability. Structural validation remains
non-authoritative until ISS-004 supplies the selected lock-scoped producer
projection.

## Exact delta and proportionality

The exact
`9f8d12a3b19ed237ddb9c1bd616a01412b82732a..4c69c0995a5dcaab4cb462cf0e22c21a37868c4c`
delta contains six paths, 304 insertions, and 14 deletions:

- `packages/contracts/src/pointer.ts`: six insertions for the exact branch
  condition;
- `test/contracts/pointer.test.ts`: 20 insertions and three deletions for the
  valid later prior, null-prior deletion control, canonical JSON, and `Dv`
  golden;
- `test/contracts/commit.test.ts`: six insertions and six deletions to make the
  ordinary active-release fixture carry a real prior and update composed
  goldens;
- `test/contracts/evidence.test.ts`: four insertions and four deletions for the
  same valid active-release predecessor lineage and its evidence golden;
- `test/contracts/genesis.test.ts`: one insertion and one deletion for the
  now-earlier public-parser finding; and
- the 267-line Round-102 independent review record.

`git diff --check` passes. The concrete threat is a selected stable epoch
discarding active-release predecessor lineage and selecting as though no
release had ever been selected. The deletion test is the guard-deleted
acceptance above. The retained mechanism is the smallest credible repair: one
condition in the existing common parser plus negative and positive controls.
Moving the rule into a new schema, proof, receipt, producer, or promotion state
machine would be larger and would duplicate the public admission boundary.

## Verification evidence

The mandated gates ran strictly sequentially. The standalone full suite exited
before the separate bootstrap verifier began; focused contracts began only
after bootstrap completed, and focused compatibility began only after focused
contracts completed.

1. Planning check — PASS, `planning contracts verified`, through
   `node scripts/planning/check.mjs`.
2. Board check — PASS, `board contracts verified`. The sandboxed read-only run
   failed closed at the GitHub CLI subprocess with `spawn EPERM`; the identical
   approved read-only rerun passed.
3. Format check — PASS, all matched files use Prettier style. The sandboxed
   run failed closed at the pinned Prettier subprocess with `spawn EPERM`; the
   identical approved read-only rerun passed.
4. Typecheck — PASS with pinned TypeScript 7.0.2 through
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`.
5. Standalone full test — PASS, 28 files and 307 tests in 102.07 seconds. The
   sandboxed run failed closed during Vite configuration with `spawn EPERM`;
   the identical approved read-only rerun passed.
6. Separate bootstrap verification — PASS: manifests and registries, format,
   typecheck, 28 files/307 tests in 101.55 seconds, private compositions,
   planning contracts, and source-tree status invariance. The sandboxed run
   failed closed at its first Git subprocess with `spawn EPERM`; the identical
   approved rerun passed.
7. Focused contracts — PASS, 18 files and 110 tests in 7.73 seconds. The
   sandboxed capability slot failed closed during Vite configuration with
   `spawn EPERM`; the identical approved read-only rerun passed.
8. Focused compatibility — PASS, one file and one test in 0.961 seconds. The
   sandboxed capability slot failed closed during Vite configuration with
   `spawn EPERM`; the identical approved read-only rerun passed.

The independent admission, golden, guard-deletion, totality, and coordinated
self-epoch probes bundled exact checked-out TypeScript and existing fixture
code entirely in memory. They wrote no source, test, fixture, or probe file.
The first sandboxed esbuild launch failed closed with `spawn EPERM`; approved
read-only executions established the matrix and attacks above.

Immediately before this report was written, the branch still resolved to the
full candidate SHA above, its parent still resolved to the full boundary SHA,
the worktree was clean, and the exact candidate diff passed
`git diff --check`. This review adds only this uncommitted Round-103 record.

## Final status

`PASS` for exact candidate head
`4c69c0995a5dcaab4cb462cf0e22c21a37868c4c`, with parent boundary
`9f8d12a3b19ed237ddb9c1bd616a01412b82732a`.

`OP-R102-001` is removed: public parsing now has only the authorized initial
reviewed-bootstrap/null-prior/null-epoch and later selected-epoch/complete-
prior/complete-epoch active-release branches. The existing initial authority
bootstrap remains valid. `OP-R97-002` also remains removed through the actual
selected graph, rederived pointer identities and digests, recomputed `Dsc`,
and direct reviewed operation/core/authority equalities. All later cleanup,
fence, archive, recovery, installation, promotion execution, runtime
capability, consumption, teardown, and consumer surfaces remain separately
unauthorized.
