# Pressure test — round 20

**Disposition:** `BLOCK_REPLAN`

The twentieth independent review found five edge cases in recovery-launch
reuse, recovery API closure, teardown evidence, repository protection, and
pre-N0 verification. The 39-issue packet remains unregistered pending fresh
review.

| Finding | Repair applied |
| --- | --- |
| `OP-R20-001` — Shim death around child creation was not reconcilable, and a terminal canonical record prevented a later promotion. | Added READY child adoption/proven-absence/ambiguity transitions, LIVE monitor adoption, immutable terminal-chain archive plus CAS cleanup, and a second recovered-promotion test on every OS. |
| `OP-R20-002` — Broker prose implied an undefined fourth successor recovery method. | Recovery now uses only the existing restart-idempotent `activateSuccessorClient`; durable `CONSUMED_BOUND` authorization and current launch-channel identity make sequential attempts exact without widening the closed client. |
| `OP-R20-003` — Unconditional teardown revocation contradicted `retain-for-n0`. | Split retention evidence from removal evidence: retention removes test-only resources and preserves the exact reduced service/key identity; removal revokes and proves complete service/key absence. |
| `OP-R20-004` — ISS-038 depended on repository protections no prerequisite established. | Expanded ISS-036 to configure/probe the exact ruleset, review actor/pagination, workflow trigger/permission, and attestation controls; added its durable protection receipt as an explicit ISS-038 dependency. |
| `OP-R20-005` — Local authenticated API re-query required a credential unavailable before the production broker. | Removed the local API requirement. The run-scoped workflow token performs the attested census inside GitHub Actions; clean target hosts verify the downloaded public bundle with a pinned CLI and no project credential. ISS-036 must prove this on all three OSes or block. |

## Re-review focus

- Kill both shim and recovery child around every READY/LIVE transition and then
  execute a separate later promotion using the same canonical path.
- Confirm the successor client still has exactly three methods and activation is
  safely idempotent across sequential launch generations.
- Verify retained and removed custody outcomes cannot satisfy each other.
- Confirm ISS-036 must pass before ISS-038 and that no target-host GitHub token is
  implied by public attestation verification.
