# ISS-022 GitHub decision-review adapter pressure review — round 317

`PASS` at exact base/HEAD
`44908999129dcb7038d9bc641bae1dcc6df3721a`.

The uncommitted three-file, `+325` adapter slice matches the landed ISS-022
ledger. `github-portable-primitives-independent-review/v1` is a closed exact
15-member record; all five provider/review/actor/PR IDs are positive canonical
decimals, both revisions are lowercase 40-hex, all four digest fields are
SHA-256, the timestamp is canonical, state is exactly `APPROVED`, and the
reviewer differs from the core PR author. The core path is exactly
`planning/decisions/ISS-022/<Dcore>/decision-core.json`. The provider-review
and repository-scoped reviewer-subject digest formulas use the specified
domains and canonical/text frames, with frozen goldens.

The record-only join consumes the provider record, actual portable core and
review records, and exact canonical core bytes. It reserializes the core,
recomputes its domain digest, hashes the supplied bytes, and recomputes the
provider-review and reviewer-subject digests. It closes core bytes/path/digest,
candidate, provider run, review digest/time/disposition, and reviewer subject;
repository, PR, author/reviewer, review ID, merge commit, and reviewed commit
remain sealed in the provider digest for the later live verifier. There is no
caller expectation scalar from which authority can be manufactured.

Extra fields, proxies/accessors, alternate case or traversal paths, malformed
or mismatched digests, self-review, zero/noncanonical IDs, uppercase or
malformed revisions, wrong time/state, substituted bytes, and cross-core,
candidate, run, provider-review, reviewer, and disposition records refuse.
Well-formed alternate GitHub scalars can describe another record but do not
authenticate it: the parser/join documentation explicitly grants no live
review, merge, protection, file-census, or freshness authority. Those facts
belong to the deferred stable terminal writer.

GitHub vocabulary stays in the adapter and its GitHub export surface. This
slice adds no provider-neutral member, live fetch, terminal/publication writer,
command, workflow, helper, or production mechanism.

Proportionality passes. The concrete threat is substitution or self-review
between the landed core and portable review. Deleting the join lets independently
valid records be mixed; a parser-only alternative cannot bind their identities
or exact bytes. The selected single schema plus record-only join is the
smallest credible closure, while live API machinery is correctly deferred.

Focused evidence passed: adapter tests (3), conformance-package TypeScript,
touched-file Prettier, and `git diff --check`.
