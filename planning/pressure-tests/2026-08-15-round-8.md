# Planning pressure test — round 8

Disposition received: `BLOCK_REPLAN`.

The eighth independent reviewer found ten remaining protocol, bootstrap,
self-hosting, host, credential, and cutover gaps.

| Finding | Replan disposition |
|---|---|
| `OP-R8-001` cycle plan and dispatch type flow contradicted | Made `cycle plan` authority-free and added the sole post-preflight `dispatch.plan` producer. |
| `OP-R8-002` early exits lacked exhaustive skip/terminal receipts | Required typed skip receipts for every remaining ordinal and a no-allocation/reclaim receipt before terminal reduction. |
| `OP-R8-003` review was not bound to immutable worker output | Added `worker-result-subject/v1`, materialized once and digest-bound unchanged through review, disposition, mutation plan, and apply. |
| `OP-R8-004` N0-to-N1 had no concrete self-host adapter | Added `ISS-033` and predeclared `@orchestration-platform/adapter-self` with exact frontier, workspace, GitHub/CI, certification, credential, and mutation boundaries. |
| `OP-R8-005` production bootstrap omitted review and confused cloud/target custody | Rewrote `ISS-027` as an actor/host transcript with pinned review, `bootstrap-n0`, grant-only workflow, numeric artifact transfer, and target-host verification/install. |
| `OP-R8-006` bootstrap recovery capability was unrecoverable | Added native-broker raw-capability custody, durable non-secret reference/digest, canonical `recover` argv, restart consumption, and revocation. |
| `OP-R8-007` Codex command/PATH contract was infeasible and canaries incomplete | Declared the actual per-OS command-interpreter contract, absolute hashed verifier path, and executable denial canaries for every claimed capability boundary. |
| `OP-R8-008` cold-host parity relied on reboot simulation | Required a real reboot and next eligible user session on all three designated OS hosts. |
| `OP-R8-009` production credential binding had no owner | Added post-certification, source-frozen `ISS-034` for exact host/user/project/role/capability binding and cold-session proof before install. |
| `OP-R8-010` state import was completed before the live cutover quiescence transaction | Restricted `ISS-024` to implementation/fixtures and moved fresh export/import/commit into `ISS-018` under one uninterrupted quiescence/transfer transaction. |

This is a failed-round record, not a pass. The revised 35-issue packet requires
another fresh independent review before registration.
