# Pressure test — round 99 E0 successor provenance review

**Disposition:** `BLOCK_REPLAN`

**Reviewed candidate head:** `f3ef41d3d1e5654d5eae5704ee65cfe24582b6fd`

**Reviewed parent boundary:** `021866bb0a7641ecb60df26fe897173c402d66d6`

This was a fresh cold independent bounded review of the exact initially clean
head above on `iss-002-versioned-contracts`. It covers only the claimed removal
of the remaining successor portion of `OP-R97-003` and preservation of the
round-98 partial repairs. It does not expect or grant E0 acceptance:
`OP-R97-002`, the selected-active-release ledger, was declared unresolved and
remains so.

This review authorizes no merge, bootstrap execution, installation, release,
promotion, runtime mutation, consumption, teardown, receipt production,
self-certification, or other authority. No Chase Sets repository, issue,
project, board, backlog, consumer, or runtime was accessed. The board gate read
only this repository and its registered platform delivery project.

## Disposition summary

| Item | Round-99 result | Evidence at this exact head |
| --- | --- | --- |
| Actual prior RETIRED owner tip/value/proposal and teardown archive | **Removed** | The successor arm supplies all four records to the existing successor-review validator, and the selected successor mutation validator consumes the same prior triple. Synthetic `Dsrc` and a foreign prior-tip substitution refused through both compositions. |
| Exact `Dsrc` record and review expectation | **Partially removed** | The actual closed review core, teardown graph, candidate, independent-review members, and exact caller expectation are composed. However, the nested `Dsrc.successorAuthority` is not equal-bound to the selected anchor and reviewed bootstrap operation; a coordinated foreign authority reconstruction returned `[]`. |
| Selected successor owner graph and exact `Dsrp` expectation | **Removed** | The actual owner value/proposal/tip, actual `Dsrp`, prior triple, and exact post expectation are supplied to the existing post-selection validator. Its owner mutation composition derives the ACTIVE successor graph from the actual prior RETIRED selection. |
| Current owner observation | **Removed** | E0 recomputes current `Dobs` and directly requires the selected successor owner proposal's `observationDigest` to equal it. A substitution refused with the direct mismatch plus downstream graph findings. |
| Nested intent `Dsrp` and whole persisted readback | **Removed** | E0 requires the parsed actual `Dsrp` to equal the nested intent expectation record and derives the whole-record readback with `canonicalDigest`. Independent record and readback substitutions refused. |
| Internal `Dsrp` value/proposal/tip readbacks | **Removed** | The composed post-selection validator derives all three readbacks from the parsed selected owner records. Coordinated receipt-and-expectation substitutions refused with the three `*:derived:mismatch` findings. |
| Genesis/successor provenance union and totality | **Removed** | Genesis requires all eight successor-provenance inputs to be exactly null; successor requires all eight non-null. Every single-null successor case, a partial expectation, genesis non-null provenance, and hostile proxies refused without throw. Valid genesis and valid successor both returned `[]`. |
| `OP-R97-002` selected active release | **Known remaining blocker** | The unchanged coordinated foreign active-release tuple and matching digest-only expectation still returned `[]`. |

The requested prior-lineage, mutation, post-selection, observation, readback,
nullability, and totality compositions are semantically effective. The claimed
successor portion of `OP-R97-003` is nevertheless not fully removed because one
direct equality from the reviewed successor authority to the selected
successor installation remains absent.

## Removed portions of `OP-R97-003`

### Prior RETIRED lineage and teardown archive are actual inputs

`validateBootstrapGenesisCoreBinding` now accepts the actual prior owner
tip/value/proposal, owner teardown archive, successor review core, and exact
review expectation. On the successor branch it calls
`validateDestinationOwnerSuccessorReviewCoreBinding` with those records. That
existing validator recomputes the prior `Dot/Dov/Dor`, teardown archive,
review candidate, and `Dsrc`; proves the prior value is RETIRED; checks the
retirement transition/source/evidence; binds prior anchor retirement members;
and admits the independent-review expectation.

The same actual prior triple is passed to
`validateDestinationOwnerSuccessorPostSelectionBinding`, whose existing owner
mutation composition proves `RETIRED -> ACTIVE`, exact next owner ordinal,
all-non-null predecessor selection, successor installation/anchor, mutation
identity, `Dsrc` transition evidence, and selected value/proposal/tip graph.

An independently reconstructed `Dsrc` with a changed review receipt but no
corresponding selected anchor/expected reconstruction refused across the
anchor, review, and post-selection bindings. Replacing the actual prior tip's
proposal digest refused through both the review core and selected successor
mutation. This closes the round-98 opaque-`Dsrc` and omitted-prior composition.

### `Dsrp`, current `Dobs`, and readbacks are closed downstream

The successor branch composes the existing post-selection validator with the
actual `Dsrp`, actual `Dsrc`, selected successor owner graph, actual prior
triple, and exact post expectation. E0 also directly requires:

- `anchor.successorReviewCoreDigest` to equal recomputed `Dsrc`;
- successor owner proposal observation to equal current recomputed `Dobs`;
- the intent expectation's nested successor post-selection record to equal the
  parsed actual `Dsrp`; and
- the persisted whole-`Dsrp` readback to equal `canonicalDigest` of that parsed
  record.

The composed helper independently derives the value, proposal, and tip
readbacks from the selected owner records. A coordinated attack changed all
three internal readbacks, changed all three post expectations, rebuilt `Dsrp`,
and changed the nested intent record, receipt digest, and whole-record readback.
It refused with exactly the three derived-readback mismatches. Changing the
actual `Dsrp` while retaining the nested intent record refused both exact
record and whole-readback equality; changing only the whole readback also
refused through the use-intent and E0 comparisons.

### The provenance union fails closed and remains total

The genesis arm admits only eight exact null successor-provenance members. The
successor arm refuses if any one is null and otherwise lets the closed parsers
and composed validators reject malformed or partial members. Independent
probes exercised every single-null position, one partial review expectation,
non-null genesis provenance, and an unreadable proxy in each new position.
Every case returned a nonempty issue list; none threw. The valid genesis and
fully constructed valid successor controls both returned `[]`.

## Remaining blocking findings

### `OP-R99-001` — `Dsrc.successorAuthority` is not equal-bound to the selected successor

The successor review helper correctly proves that the nested closed
`successorAuthority` equals `successorReviewExpected.successorAuthority`.
That expectation is still supplied by the caller. E0 binds only its
`installationId` indirectly, through the selected successor owner mutation.
It never compares the remaining successor-authority members to facts it has
already parsed and derived from the selected anchor and exact reviewed
`BOOTSTRAP_INSTALL` operation.

The independent in-memory reconstruction changed the nested reviewed successor
authority's bootstrap grant, bootstrap transaction, global-bootstrap identity,
project, reviewed installer, release manifest, release subject, and state root,
while retaining the selected installation ID. It rebuilt the review candidate,
`Dsrc`, caller review expectation, anchor and anchor ACTIVE graph, successor
owner ACTIVE graph, `Dsrp` and all readbacks, use intent and expectation,
`Dgb`, GENESIS history, E0 authority value, and `Dbg`. The exact public E0
validator returned `[]`.

Thus every downstream digest agrees, but the `Dsrc` can describe a different
successor authority from the selected anchor and reviewed operation. This
contradicts the normative requirement that a successor anchor's `Dsrc` name the
same successor authority and leaves the successor part of `OP-R97-003`
incomplete.

Smallest repair: after parsing the actual `Dsrc`, equal-bind its nested
`successorAuthority` directly to existing independent facts:

- bootstrap grant, transaction, global-bootstrap identity, installation,
  project, reviewed installer, and state root to the selected anchor; and
- reviewed release manifest and subject to the exact reviewed bootstrap
  operation already used to derive `Dsc`.

Retain the existing review-expectation equality as an additional check, not as
a substitute for those cross-record equalities. Add the coordinated fully
rehashed reconstruction as a committed deletion test. No schema, field, path,
digest domain, lifecycle state, or new validator is required.

### `OP-R97-002` — selected active release remains unresolved as declared

The E0 authority value's active-release `Dp/Dt/Dv/Dr` remains checked only
against four digest members in `coreExpectationFields`. Replacing all four in
the authority value and expectation and recomputing E0 `Dv/Dbg` still returned
`[]`. This is the known round-97 blocker, not a regression introduced by the
reviewed delta. It independently prevents an E0 `PASS` disposition.

## Executable-evidence attack surface

The committed delta adds a valid constructed successor control and a
coordinated internal-readback mutant. Those are valuable deletion evidence for
the new validator composition and canonical readbacks. The ordinary test does
not preserve the single-null provenance matrix, foreign prior, nested-versus-
actual `Dsrp`, whole readback, current observation, hostile-input matrix, or the
accepted coordinated successor-authority reconstruction. The independent
probes establish present behavior, but the next repair should commit the
cross-record equality reconstruction and representative provenance/readback
cases so removal of any new edge makes the suite fail.

## Exact delta and proportionality

The exact `021866b..f3ef41d` delta contains two paths, 340 insertions, and nine
deletions:

- `packages/contracts/src/genesis.ts`: 75 insertions; and
- `test/contracts/genesis.test.ts`: 265 insertions and nine deletions.

The production change is confined to the E0 successor composition and reuses
the existing successor review/post validators and canonical digest machinery.
No schema census, schema version, path, digest framing domain, registry,
compatibility surface, lifecycle machine, runtime capability, consumer,
adapter, or teardown producer changed. `git diff --check 021866b..f3ef41d`
passes. The implementation footprint is bounded to the claimed composition;
the large fixture addition constructs the previously omitted prior and
successor evidence graph.

## Verification evidence

The mandated gates ran in strict sequence. The standalone full suite exited
before the separate bootstrap verifier started; focused tests began only after
bootstrap verification completed.

1. Planning check — PASS, `planning contracts verified`, through
   `node scripts/planning/check.mjs`.
2. Board check — PASS, `board contracts verified`. The sandboxed read-only run
   failed closed at the GitHub CLI subprocess with `spawn EPERM`; the identical
   approved read-only rerun passed.
3. Format check — PASS, all matched files use Prettier style. The sandboxed run
   failed closed at the pinned Prettier subprocess with `spawn EPERM`; the
   identical approved read-only rerun passed.
4. Typecheck — PASS with pinned TypeScript 7.0.2.
5. Standalone full test — PASS, 28 files and 305 tests in 102.19 seconds. Its
   sandboxed direct invocation failed closed during configuration with `spawn
   EPERM`; the identical approved read-only rerun passed.
6. Separate bootstrap verification — PASS: manifests and registries, format,
   typecheck, 28 files/305 tests in 100.65 seconds, private compositions,
   planning contracts, and source-tree status invariance. Its sandboxed direct
   invocation failed closed at the first read-only Git subprocess with `spawn
   EPERM`; the identical approved rerun passed.
7. Focused contracts — PASS, 18 files and 108 tests in 6.13 seconds. The
   sandboxed capability slot failed closed during configuration with `spawn
   EPERM`; the identical approved read-only rerun passed.
8. Focused compatibility — PASS, one file and one test in 1.06 seconds. The
   sandboxed capability slot failed closed during configuration with `spawn
   EPERM`; the identical approved read-only rerun passed.

The adversarial probes bundled the exact checked-out TypeScript and committed
fixtures entirely in memory. No probe, implementation, or test file was
written. Their sandboxed esbuild child was refused with `spawn EPERM`; the
approved read-only rerun established the semantic results recorded above.

## Final status

`BLOCK_REPLAN` for exact candidate head
`f3ef41d3d1e5654d5eae5704ee65cfe24582b6fd`. The remaining successor
prior-lineage, teardown, selected owner, `Dsrp`, observation, readback,
nullability, and totality composition is present and effective. The successor
claim is not complete because `Dsrc.successorAuthority` can still differ from
the selected anchor and reviewed operation when the caller expectation changes
with it. `OP-R97-002` also remains unresolved as declared.

Repair only those two known equality boundaries, preserve the coordinated
reconstructions as committed regression evidence, and return the exact repaired
head to ordinary independent review. All downstream consumption, teardown,
promotion, runtime capability, and consumer surfaces remain outside this
review and unauthorized.
