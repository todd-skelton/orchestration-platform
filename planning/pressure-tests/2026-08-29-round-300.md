# ISS-003 exceptional final minimal-replan review — round 300

`BLOCK_REPLAN` at exact `origin/main`/worktree head
`add69913e8d97c043703f62e7690a95d89e5b337`.

The packet closes the Round 270 loader/result-member, digest-framing,
registration-result-schema, sync/async handler, contract-footprint, and
falsifiable-deferral findings. It also removes `--reveal` from the authoritative
bootstrap command row, the `config` registration, and the verifier census; the
parked architecture sentence is not a current command. Precedence, physical
path admission/redaction, consumer boundaries, engine/adapter separation, and
the predicted footprint are otherwise bounded and proportional.

Two prior closure requirements remain blocking:

1. Round 242 and `ISS-000` require an unimplemented placeholder result to name
   its owning issue. The proposed exact diagnostic is only
   `{code:"COMMAND_NOT_IMPLEMENTED",message:"command is not implemented"}`;
   unlike the existing `{code:"CAPABILITY_NOT_IMPLEMENTED",owner:issue}` result,
   it contains no owner key. The claimed complete ISS-013 ABI would therefore
   break the bootstrap contract it replaces. Close the owner-bearing failure
   bytes and their per-registration golden without adding a second registry.
2. The loader says malformed closed-invocation members, extra/accessor/proxy
   environment values, and missing OS default bases refuse and never throw, but
   none maps to a condition in the exhaustive failure table. These cases cannot
   produce the promised exact `ConfigurationLoadResult` or golden envelope.
   Assign them explicitly to existing rows; no new mechanism is needed.

Focused evidence: `pnpm run planning:check` and
`node scripts/verify/bootstrap-contracts.mjs` pass. The broader bootstrap run
reached 75 passing test files but failed three unrelated `ISS-002` conformance
walk tests on their 5-second timeouts; that does not alter either finding.
