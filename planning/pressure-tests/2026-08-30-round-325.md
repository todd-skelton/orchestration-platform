# ISS-003 native Windows reparse-fact pressure review — round 325

`PASS` at exact base `07444e8f3f647d5399a68256493e1a636e2ac63e`
and head `0a3391c992c9c30791d82832919237aa3ab28b09`.

This one bounded round is authorized only by Round 324's named ACL blocker.
The source-built production addon replaces pathname-spawned PowerShell/fsutil
with one synchronous in-process observation. Its N-API entry accepts exactly
one bounded non-empty UTF-16 string and rejects embedded NUL; the wrapper
further admits only normalized absolute local drive paths, snapshots the exact
addon method, parses an exact plain data-only fact, and returns frozen closed
success/refusal rows without echoing input.

The Windows primitive opens the named object with `FILE_READ_ATTRIBUTES`, all
three share modes, `OPEN_EXISTING`, `FILE_FLAG_BACKUP_SEMANTICS`, and
`FILE_FLAG_OPEN_REPARSE_POINT`. `FileIdInfo` and `FileAttributeTagInfo` are read
from the same retained handle, and production always attempts `CloseHandle`,
refusing either observation or close failure. The shared-core replace/restore
mutant proves that identity remains bound to that handle while the pathname
temporarily names a different object. The ACL mutant kills Round 324's exact
distinction: denial of write-data coexists with allowed rename, yet the native
read-only observation still succeeds. The final component is opened without
following its reparse target.

The addon/wrapper ABI is one `observe(path)` function and one four-field fact;
no handle or input path escapes, and production has no callback, process
global, privilege, broker, isolation boundary, or subprocess. The ACL/rename
authority exists only in a separately named test target and artifact; the
runtime wrapper loads only the production artifact, and neither surface is
exported from the package root. macOS/Linux return the frozen `UNSUPPORTED` row
without reading the manifest, invoking node-gyp, or loading a native module.

Build inputs are bounded by the exact manifest/source census, reviewed gyp and
C++ sources, the running Node distribution's node-gyp, pinned workflow-action
and pnpm inputs, and the exact Node/image versions recorded by the hosted run;
no durable binary is certified. The transient gyp root is removed in `finally`;
intended `.node` outputs live only under ignored `.artifacts`. The reviewed
checkout has no `.artifacts`, tracked compiled binary, or status delta. No root
runtime registry, loader, contract, CLI, installation, or public export is
claimed by this probe.

Hosted run `33325367011` records this exact head (PR merge of the exact
base/head): Windows job `99294434038` passed all 8 focused rows and the complete
81-file/767-test bootstrap; macOS `99294434127` and Ubuntu `99294434152` each
passed 763 tests with only the 4 Windows rows skipped. All three reported no
source-tree status change. `git diff --check` and the compiled-artifact/status
census pass locally; no local bootstrap or native build was rerun.

## Proportionality

The concrete threat is executable substitution during reparse observation.
Deleting the native primitive restores pathname-spawn custody; deleting either
focused mutant leaves the retained-handle or ACL distinction unproved. A
broker, privileged helper, service, FFI dependency, or generic callback seam is
larger than this private read-only primitive. The source/build and hostile-test
scaffolding is therefore proportionate to the named blocker, and this record is
materially shorter than the 1,007-line slice. This pass does not certify the
later loader composition.
