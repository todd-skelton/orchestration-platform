# ISS-003 minimal replan review — round 270
`BLOCK_REPLAN`.
The ISS-041 loader contract is not closed: “resolved in-process value” has no exact members or return/error union, so the consumer cannot obtain exact state-root/config/provenance data without inventing an ABI.
The ISS-013 boundary remains underclosed: registration/handler invocation and sync/async result shapes, throw/reject handling, placeholder-without-handler validity, and result-schema validation are unspecified.
Reveal is contradictory and byte-ambiguous: `revealDisposition` must change `NOT_REQUESTED` to `TERMINAL_ONLY` while AC4 says reveal never changes result bytes; the exact six-name invocation has no CI or TTY inputs, and raw stderr path framing/separators are unfixed.
Failure envelopes are not golden-reproducible because no condition-to-outcome/exit/diagnostic table or fixed message literals exist; `configuration-path/v1` also omits the framed part type for canonical URL bytes.
The three new durable schema families and command envelope are public contracts but the predicted footprint excludes their contracts/compatibility registration; local package types cannot satisfy the repository’s executable compatibility boundary.
`config paths --reveal` adds terminal capability machinery needed by neither named ISS-041 nor ISS-013 consumer; “settled operator row” is not the work model’s required consuming dependency or falsifiable unpark evidence.
Precedence order, source members, local-root/overlap rules, redacted token members, command rows, roadmap edges, and the bounded deferral rule are otherwise materially smaller and preserve engine/adapter separation and non-self-certification.
