# Pressure test — round 73 author rotation-lineage correction

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `507bb38f67b0c071f2f7964c5f186b864c5f39fb`

Implementation preparation exposed one branch conflict in the round-71 repair:
rotation `Dcommit` deliberately contains only checkpoint 5, so a positive
rotation run could not prove the stated checkpoint-zero predecessor without
reintroducing checkpoints 0–4. The user selected the smallest branch-specific
correction. Round 72's PASS does not apply to this amendment. This record grants
no implementation, merge, release, promotion, installation, runtime mutation,
or self-certification authority; a fresh independent exact-head PASS is
required.

## Bounded correction

- ORDINARY retains the reviewed closed prior-run rule: ordinal zero/null;
  positive ordinal/non-null complete immediately prior checkpoint evidence,
  with derived prior `Dcore`, selector tuple, `Dpost`, and checkpoint-zero
  predecessor equality.
- AUTHORITY_ROTATION requires `runOrdinal="0"` and
  `priorCheckpointEvidence=null`. A crash leaves its same selected checkpoint-5
  run CAS-armed. The same transaction may re-drive that run's same final CAS;
  it cannot open a positive rotation run.
- Rotation `Dcommit` remains checkpoint-5-only. No checkpoint 0–4 member,
  additional receipt, handoff, or new-epoch write is added. Target `Dp`
  recomputation remains mandatory in both branches.

## Proportionality and trust

This correction narrows rotation rather than growing its evidence envelope. It
preserves ordinary retry lineage and the written checkpoint-5-only rotation
removal decision. It adds no field beyond round 71's common nullable member, no
path, selector, receipt, pointer kind, coordinator, proof, compaction,
capability, or verdict route. Re-driving the same CAS still requires the old
selected authority, same transaction, kernel lock, and ISS-004 private live
context; structural evidence grants no restart method.

## Required independent attack

Review the exact committed correction cold. Try positive, unsafe, noncanonical,
or copied rotation run ordinals; non-null rotation prior evidence; a second
checkpoint-5 run; ordinary prior evidence smuggled into each rotation outcome;
and checkpoints 0–4, 6–8, resolution, post-CAS, or new-epoch artifacts. Prove
ordinary zero/positive lineage is unchanged and rotation crash recovery can
only re-drive the same transaction/CAS under the old live capability. Recheck
target `Dp` derivation, trust, proportionality, and the explicit continued
block on round 70's three remaining ledger families.
