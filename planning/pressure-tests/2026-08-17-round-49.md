# Pressure test — round 49 independent review

**Disposition:** `PASS`

**Reviewed head:** `0521f270d488d4b975609bba14186e70f6c1cd5c`

This was a fourth fresh independent cold exact-head review under the ordinary
removal-review standard. The worktree was clean before review and the reviewed
head was exactly the hash above. The excluded
`archive/2026-08-17/linear-contracts-wip` tree was not inspected or used.
Round 48 is author-side evidence only, and no earlier disposition was borrowed
as authority for this head.

## Disposition

The latest repairs are clean. `OP-R47-001` and `OP-R47-002` are closed with no
new finding. The reconciled planning/design packet passes its exact-head review
gate. This receipt does not claim that the later ISS-002 contract
implementation or its planned acceptance mutants already exist, and it grants
no worker, candidate, receipt, or review record self-certification authority.

## Reconciliation census

- The historical non-archive base range
  `e77fc77a35d91d35d80cda3887f64c65840182b7..e30ff0508eb8d07ce3ec8d299943bd82bf56f9ef`
  contains exactly 45 unique paths and ten pressure-history/reconciliation
  paths. The category census in ISS-002 accounts for all 45, including the two
  deleted consumer-migration authorities.
- The reviewed non-archive range
  `e77fc77a35d91d35d80cda3887f64c65840182b7..0521f270d488d4b975609bba14186e70f6c1cd5c`
  contains exactly 53 unique paths and sixteen pressure paths.
- The post-base range
  `e30ff0508eb8d07ce3ec8d299943bd82bf56f9ef..0521f270d488d4b975609bba14186e70f6c1cd5c`
  contains exactly eleven paths: rounds 43 through 48 plus exactly five
  non-pressure modifications:
  1. `docs/architecture/supervisor-contract.md` assigns the target one of the
     eleven registry-ordered slots and leaves exactly ten non-target slots.
  2. `docs/contracts/compatibility.md` replaces nonexistent compaction-ordering
     machinery with retention/compaction/degraded-audit export and dispatch-
     reachability negatives.
  3. `planning/drafts/ISS-002.md` adds the eleven/ten/one/no-omission packet
     census mutant and makes later non-pressure content accounting explicit.
  4. `planning/drafts/ISS-042.md` parks richer projections behind a registered
     platform consumer needing an observable that reduced status cannot derive.
  5. `planning/drafts/ISS-043.md` parks optimization, tuning, ensembles, and
     decision autonomy until calibration or delivery evidence shows that the
     current review path misses its published floor.

The full post-base diff was inspected, not merely its path names. These are the
only non-pressure edits after `e30ff05`, and each matches its description.
Calling the older range a historical base-path census rather than a substantive
boundary, then enumerating all later non-pressure paths and their content,
closes the same-path hiding defect from round 47.

## Independently validated evidence

- The ISS-042 and ISS-043 unpark conditions are platform-owned, observable, and
  falsifiable. Neither condition depends on the deleted migration manifest, a
  consumer backlog, or a consumer board.
- Consumer-board decoupling is complete. The registered drafts are the only
  executable platform backlog. The board checker reads the complete issue
  connection for `planning/roadmap.json.repository` and exactly the one
  delivery Project registered by `planning/roadmap.json.project`; it validates
  independent totals, cursor continuity, identities, duplicates, and the
  100/101 provider boundary. The live read-only verification passed.
- The runtime registry and packet are consistently eleven-wide. The target
  occupies exactly one registry-ordered slot, ten non-target slots remain, and
  ISS-002 AC11 requires an executable mutant proving eleven total, ten
  non-target, one target occurrence, and no omitted kind. No target-plus-eleven
  residue remains in the governing packet.
- Deleted retention, compaction, and degraded-audit machinery stays deleted.
  Every record class is `FULL_REQUIRED`; loss is `UNKNOWN`; the only relevant
  negative is public export/dispatch reachability. The O(n) history walk is
  retained, with checkpointing parked behind ISS-006's measured 1,000-record,
  five-seconds-per-supported-OS gate.
- The roadmap has 46 registered issues and 73 direct blocked-by edges. An
  independent traversal found zero transitively implied direct edges and
  closure counts of 544 for ISS-000 through ISS-038, 615 through ISS-043, and
  654 through ISS-045.
- The normative schema ledger independently counts `Dop` 9/15, `Dsc` 17,
  `Dgb` 14, `Dgse` 38, `Drot` 12, `Dh` GENESIS 7/ROTATION 13, selected
  authority value 22, and `Dcommit` common 18 plus ORDINARY 4 or rotation base
  8 plus exactly one RESUMABLE 9, SELECTED 8, or UNKNOWN 1 arm. History records
  serialize only as `authority-history-record/v1`; `authority-history/v1`
  remains the branch-separated digest domain.
- The trust model remains external and fail closed. Records authenticate
  relationships rather than writers. Mutation additionally requires canonical
  custody, the selected current authority, the kernel-exclusive lock, and an
  ISS-004 private live capability. Structural validation, workers, candidates,
  stale bytes, and unsupported host rows cannot authorize themselves; unknown
  or contradictory evidence exposes no mutation capability.
- The engine/adapter boundary is explicit. Engine contracts use opaque
  identities and closed platform enums; repository, branch, worktree, label,
  milestone, queue, CI, deployment, and consumer vocabulary remain adapter
  facts and are covered by the generated denylist mutant.
- Acceptance evidence is executable and discriminating at the appropriate
  stage: the current planning and board mutants execute now, while ISS-002 and
  ISS-006 own the later exact-byte/schema, chain/rotation, outcome-to-slot,
  packet-census, deleted-surface, hostile-reflection, and cross-OS obligations.

## Bounded proportionality check

| Retained mechanism | Concrete in-scope threat | Deletion test and smallest credible alternative | Result |
| --- | --- | --- | --- |
| Fixed eleven-slot evidence packet with outcome-to-slot equality | An omitted, duplicated, substituted, stale, or wrong-outcome slot can make incomplete evidence appear mutation-capable. | Delete the census/equality and the forged packet becomes admissible. A registry-ordered target-plus-ten packet with one census mutant is smaller than any extra slot or second packet representation. | Retain the repaired eleven-slot shape. |
| Complete repository and one registered-Project pagination | A provider cap or cursor/identity defect can yield a locally consistent but incomplete platform-board census. | Delete independent totals/cursor checks and a truncated census passes. Reading this repository and its one registered Project is sufficient; a consumer board discriminates no platform defect. | Retain the current checker; keep consumer reads deleted. |
| FULL_REQUIRED linear authority history and live custody/lock/capability chain | A stale, missing, forged, or ambiguous epoch can otherwise mutate across the single-writer crash boundary. | Delete predecessor/head binding or live capability and stale evidence can authorize mutation. The selected O(n) chain is smaller than the removed sparse tree, inventory, coordinator, epoch-split run, retention, or compaction machinery; the measured ISS-006 gate controls any future checkpoint need. | Retain the linear chain and external authority checks. |
| Explicit post-base non-pressure census | A later substantive edit to an already-counted path can hide behind an immutable unique-path total. | A base-only path count fails this test. Ordinary Git enumeration of all later non-pressure paths and their bounded hunks is the smallest alternative; no checker or runtime mechanism is needed. | Retain the two-part accounting used by this review. |
| Platform-owned park conditions for ISS-042/043 | Hypothetical or consumer-owned demand can silently revive richer projections or model machinery. | Removing a measured gate permits speculative scope. A registered-platform observable gap or evidence of missing the published review floor is cheaper and more discriminating than restoring a migration manifest or consumer-board dependency. | Retain the repaired falsifiable gates. |

## Verification

- `git rev-parse HEAD`, `git status --short` — clean reviewed head
  `0521f270d488d4b975609bba14186e70f6c1cd5c` before this record.
- Three excluded-archive `git diff --name-only`/`--name-status` censuses over
  `e77fc77..e30ff05`, `e77fc77..0521f27`, and `e30ff05..0521f27` — respectively
  45/10, 53/16, and eleven paths split into six pressure plus five
  non-pressure paths.
- Independent PowerShell roadmap traversal — 46 issues, 73 edges, zero implied
  direct edges, closure 544/615/654.
- Independent normative-table row census — schema counts listed above.
- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `pnpm exec vitest run test/planning-contracts.test.ts test/board-contracts.test.ts`
  — PASS (2 files, 48 tests).
- `node scripts/planning/board-check.mjs` — PASS read-only (`board contracts
  verified`). The sandboxed attempt could not spawn `gh` (`EPERM`); the same
  command completed successfully with read-only external access.

## Caveats

This is a planning/design exact-head receipt. It verifies that the packet is
internally consistent and has executable acceptance obligations; it does not
substitute for later ISS-002 implementation evidence, three-OS conformance,
stable-predecessor promotion, or independent release authorization.
