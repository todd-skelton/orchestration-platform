# ISS-022 literal-ledger pressure review — round 245

`BLOCK_REPLAN`.

The candidate table is not literal Markdown: unescaped `O_CREAT|O_EXCL|O_RDWR` splits the create-once row into extra columns, leaving its candidate/token mapping malformed.
The ADMITTED locator arm never fixes `validFrom`, `validUntil`, or their equality/order with locator/custody `observedAt`, so identical evidence can produce different authoritative receipt bytes.
`DcustodyProfile` is defined, but neither decision arm requires `custodyProfileDigest` equality to it or joins each OS helper/ABI slot to that OS observation and consumer custody receipt.
Consumer admission is incomplete: a local consumer must equal-bind its actual Node bytes/ABI and supported filesystem/case/Unicode/statfs profile to the matching OS evidence; verifying only the decision plus a new custody instance permits untested-profile reuse.
`independentReviewReceiptDigest` has no closed receipt schema, producer, subject/candidate equality, or publication join; ISS-009 cannot supply it because its dependency path runs through ISS-005 and therefore ISS-022.
Decision publication is likewise unspecified: no exact terminal output path/artifact, writer identity, reviewed subject, or immutable step binds the locally generated decision bytes to that review digest.
The physical formulas are otherwise reproducible, but “same ... open root handle” must mean one retained handle plus before/after `fstat` identity—not equality of newly reopened handle values.
The diagnostic provider/terminal census, `if:always()` four-job topology, missing-evidence refusal, direct-child limitation, and default non-inheritance boundary are feasible and fail closed.
The known persistent O_EXCL lock and grandchild gap honestly force BLOCK_REPLAN without authorizing a helper; collecting the remaining bounded rows is proportionate evidence for a later mixed profile.
Minimal repair is these literal time/review/publication and per-OS consumer joins; add no native helper, principal isolation, reboot lane, or production service.
