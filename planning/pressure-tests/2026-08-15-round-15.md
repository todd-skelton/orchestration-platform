# Pressure test — round 15

**Disposition:** `BLOCK_REPLAN`

The fifteenth independent review found four executable-bootstrap gaps. The
39-issue packet remains unregistered pending a fresh review of the repairs.

| Finding | Repair applied |
| --- | --- |
| `OP-R15-001` — The cross-principal service had no static downstream operation registry, so broker-side GitHub/CI and canary operations could not be composed after their owners landed. | Added exhaustive `broker-operation-manifest/v1`; made `ISS-032` own the non-executable broker core/factory and synthetic registry tests, `ISS-021`/`ISS-033` own exact operation implementations, and downstream `ISS-020` own the sole final broker-service composition entrypoint and real-provider integration. |
| `OP-R15-002` — A Node `.mjs` alone cannot implement Windows SCM `ServiceMain`. | Fixed the Windows x64 candidate to pinned WinSW 2.12.0 wrapping absolute hashed Node/broker bytes; made `ISS-031` prove the exact asset/config/SCM lifecycle and `ISS-032` package its frozen asset/license/config/lifecycle with unsupported-architecture refusal. |
| `OP-R15-003` — `ISS-000` could not build the self-host placeholder before `ISS-011` created the module generator/manifest. | Added an `ISS-000`-owned empty manifest, deterministic empty-only generator placeholder, and empty generated registry contract; `ISS-011` replaces that predeclared surface with the four-module generator without changing root build configuration. |
| `OP-R15-004` — The bootstrap-authority probe named production workflows owned downstream by `ISS-020`. | Gave `ISS-029` three exact probe-only workflow paths and scope; made `ISS-020` own distinct exact production workflow paths that implement the passed contract; added correspondence and production-inaccessibility evidence. |

## Re-review focus

- Confirm the final broker executable is composed only after every operation
  implementation exists and no secret-bearing callback crosses custody.
- Confirm WinSW supplies the missing Windows service-host contract and is fully
  digest-bound/certified.
- Confirm scaffold builds and probe workflows are executable before downstream
  slices without being mistaken for production capability.
