# ISS-022 swap-observation review — round 271

`PASS`.

The probe retains one original root handle across the exact six-row order and
closes it only after both new rows. A symlink/junction changes the lstat kind
while preserving the followed target kind; a parent replacement keeps the
original handle live while path identity moves, so copied namespace bytes and a
restored lexical realpath cannot make `rootStable` or `locatorStable` true.

Failed link creation restores a direct directory and records only a bounded raw
error code; partial parent renames restore in `catch` and again in `finally`, and
the external swap container is removed. Movement/readback failures throw rather
than becoming absence or PASS. Windows uses a junction to an existing absolute
directory; Linux/macOS use directory symlinks; unsupported parent rename remains
raw non-applied evidence for later closed reduction.

Both swap rows expose fixed-width identities, kinds, stability booleans and the
OS only: no path, `PASS`, derivation, destination identity, or normalized result.
Only the existing/absent rows can derive `Dphys`; order and retained-handle
continuity are asserted across all six rows. Focused tests pass 10/10, root
typecheck passes, and `git diff --check` passes.

Proportionality: the two raw hostile observations directly answer the ledger's
link/parent substitution threat. Removing retained-handle comparison launders a
parent swap; lexical comparison is smaller but insufficient. No helper,
normalizer, receipt, production custody, or authority route was added.
