# ISS-022 nullable-environment follow-up review — round 301

## Disposition

`PASS`.

This is the single permitted post-repair review of Round 299's two blockers on
`codex/iss-022-nullable-environment-replan` at exact base
`add69913e8d97c043703f62e7690a95d89e5b337`. Both blockers are closed without
widening the slice.

## Repaired blockers

1. `conformance-job-receipt/v1.normalizedResult` is now exactly
   `PASS|FAIL|UNSUPPORTED|UNKNOWN`. The landed parser catalog and stable receipt
   producer already use that same four-value union, require null walk timing for
   every non-PASS, and preserve UNKNOWN literally. The reducer refuses every
   non-PASS receipt before aggregate construction, emits only result `PASS`, and
   the hosted archive verifier independently requires every retained receipt to
   be PASS. FAIL, UNSUPPORTED, and UNKNOWN therefore remain diagnostic-only.
2. `DpreCustodyEnvironment` removes the cycle. Its ordered frames cover every
   variable final environment member except `custodyObservationDigest`: ABI,
   architecture, OS profile, helper profile, Node version/OS family, exact raw
   environment-inventory digest, package-manager version, and runner class;
   `schemaVersion` is fixed. The stable chain is now pre-custody evidence,
   root/custody instance and receipt, locator, final environment, then `Denv`.
   The identity is recomputed, never caller input, not a record, and not a
   selectable profile. The diagnostic arm still requires complete ordered 21-row
   evidence, a physical UNKNOWN/UNSUPPORTED, non-PASS observation/receipt,
   exact non-null ABI/helper, null filesystem/custody, and no selection token.

## Scope and evidence

The repair adds no environment member, helper, workflow job, permission, issue,
or roadmap edge. Its predicted implementation footprint is confined to the
existing digest input, ISS-022 profile/environment composition, generic receipt
union/producer/reducer, and focused mutants. `osImageDigest` remains raw-inventory
binding only, other suites retain non-null filesystem digests, and aggregate
authority remains all-PASS. This is the smallest implementable repair of the
two named blockers; no broader mechanism or review round is justified.

Focused evidence passed: `test/conformance/contracts.test.ts` (7 tests), the
conformance TypeScript check, `pnpm run planning:check`, and `git diff --check`.
