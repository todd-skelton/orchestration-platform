# Pressure round 473 — bounded supervised pilot implementation

ISS-071 implementation candidate, based on 794ddd3276a60d260fa306eb03653ff6d787f962.
This record claims no successful live trial, passing independent review,
hosted test result, merge authority or release certification.

## Scope and non-goals

Add a private sequential pilot, installed native Codex CLI adapter and detached
observation wrapper under scripts/dogfood, discovered focused tests under
test/dogfood, and concise invocation documentation. The existing controller
retains issue selection, allowed worktree/footprint, publication and merge.
One author and one distinct exact-head reviewer are the full attempt budget.
No new package, public schema, credential store, production lock, scheduler,
walking-skeleton import, Chase Sets policy or promotion mechanism.

## Acceptance evidence and verification

Exclusive external stage files reserve each attempt. Restart observes existing
identity; uncertain launch intent blocks reconciliation rather than redispatch.
Prompt/config fingerprint and pilot revision stay pinned. Review follows the
exact committed in-scope author diff. Empty/missing/duplicate required checks,
changed heads, malformed results and author-as-reviewer cannot produce readiness.
Publication is an explicit existing-controller handoff, followed by read-only
PR head/check observation. Tests include a fake process whose observer survives
its controller exit; these are not live-run evidence.

Run the four allowed local static/planning gates. All changed assertions require
independent inspection and the intended discovered suites must execute in hosted
macOS/Windows/Linux CI. No local test runner, native build, addon, experiment or
duplicate whole-suite bare-Node execution. The first real candidate is the
already-approved #321A exact bootstrap census repair on PR #320; it must preserve
unknown-directory and unexpected-manifest failures and leave pilot/review/CI
success conditions and authority unchanged.

## Predicted footprint and review attack surface

Four pilot implementation files, two focused test files with two tiny process
fixtures, one plan invocation section and this pressure record. No root script,
registry, public contract or workflow mutation. Attack duplicate dispatch after
crash, false identity/terminal parsing, wrong-head readiness, empty CI success,
reviewer write authority, overbroad author Git directory, partial stage records,
and accidental source-checkout state. The native CLI flags were read from the
installed executable's help; the host adapter is explicitly replaceable.

## Attempt history and horizon

Implementation attempt 1 is the bounded source lane under the existing
controller. Independent exact-head review, hosted suite execution and the real
trial remain pending. Existing target PR #320 author/reviewer history remains
separate and is not reset. Reassess at the first real run: retain actual attempt
IDs, two restart launch-count observations, PR/CI links, time/token observations
and the smallest next change justified by evidence. A captured blocker is useful
but does not complete ISS-071.
