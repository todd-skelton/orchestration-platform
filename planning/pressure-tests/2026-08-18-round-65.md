# Pressure test — round 65 independent composed-evidence review

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `8c11b77cb824311ee4a36ce00fcaad9a8faf52df`

This was a fresh independent cold review of the exact initially clean head above
on `iss-002-versioned-contracts`. The review used `AGENTS.md`,
`docs/planning/work-model.md`, and
`docs/architecture/contract-decisions.md` as decision authority, then read the
relevant supervisor ledger, compatibility contract, ISS-002 draft, round-64
author record, and exact repair diff from
`f05f8e0b64faccefe43a98ef2ba3299bf3dacf12`. No consumer repository, backlog,
issue, board, or project was read.

## Blocking finding

### Outcome evidence is not equal-bound to the new target-slot evidence

The repaired records are individually closed, but their enclosing ORDINARY
composition does not state the cross-record equalities needed to make them one
terminal fact:

- for `SELECTED`, `ordinaryResolution.selectedTargetTipDigest` is not
  explicitly required to equal the recomputed `tip` digest in
  `targetRegistrySlot.selectedEvidence`;
- for `LOST_CONFLICT`, `ordinaryResolution.conflictReceiptDigest` is not
  explicitly required to equal the recomputed digest of
  `targetRegistrySlot.selectedEvidence.conflictReceipt`; and
- the selected-authority producer tuple embedded in that conflict receipt is
  not explicitly required to equal the ORDINARY run's common old/KNOWN packet
  `Dp/Dt/Dv/Dr`, even though the resolution's four producer-authority fields
  are required to equal that tuple.

The ledger separately requires the resolution arm digest, a valid real-winner
conflict composition, and the correct slot kind/outcome. Hashing both values in
`Dcommit` proves that both were presented; it does not prove that the resolution
names the evidence carried by the slot. A coordinated mutant can therefore pair
a valid resolution naming receipt A with a valid target-slot composition naming
receipt B, or pair a conflict receipt created under another selected producer
epoch, while satisfying every equality that is actually written. The analogous
SELECTED mutant pairs one selected-tip digest in the resolution with another
valid selected graph in the slot.

This is a public-contract ambiguity at the exact dependency boundary the repair
was meant to close. It blocks implementation authority. The smallest repair is
pure equality: state the two outcome-to-slot digest equalities above, require the
conflict receipt's named producer tuple to equal the run/resolution/KNOWN packet
tuple, and add independent plus coordinated mutants for each relation. No new
field, record, digest domain, path, receipt, pointer kind, or state transition is
needed.

## Other bounded attack results

- The run-intent common census has exactly thirteen members; the rotation arm
  adds exactly six. Types, branch absence, lexicographic canonical JSON order,
  semantic `Dp/Dt/Dv/Dr` framing, rotation input/derived `Drot`, successor
  value, head-plus-one ordinal/record, and `Dsc` relations are otherwise closed.
  No caller-selected epoch or rotation identity is admitted.
- `pointer-mutation-conflict-evidence/v1` has exactly six members and the
  evidence slot exactly three. The loser, selected real winner, target
  identity, receipt recomputation, nested-arm census, and outcome-to-arm
  kind/null matrix are otherwise closed. The finding above is the missing
  equality between this valid nested composition and its sibling resolution.
- `authority-history-binding/v1` has exactly six lexicographically ordered
  members. Its dense array is fixed to ordinals zero through head, the full
  linear walk recomputes each branch/digest/predecessor/`G`, genesis selection
  is rebound to record zero, and the final `G`/head equals the packet's selected
  current authority value. It adds no alternate path, tree, membership proof,
  directory inventory, checkpoint, compaction arm, or capability.
- Structural success remains non-authoritative. Runtime mutation still needs
  the selected exact authority, canonical custody, kernel-exclusive lock, and
  ISS-004 private live capability; neither an ISS-002 parser, worker, candidate,
  author record, nor this review can certify or promote itself.
- The repair uses only engine-owned pointer, evidence, authority, commit, and
  history vocabulary. It introduces no repository, branch, worktree, label,
  milestone, queue, deployment, consumer, provider, or project-policy field.
- Ownership remains bounded: ISS-002 owns schemas/canonicalizers/pure
  validators; ISS-004 owns live capabilities, runtime proposal/conflict/run and
  rotation production; ISS-020 owns GENESIS and downstream selection evidence;
  runtime consumers must use the package plus ISS-004 and may not reinterpret
  the fields.

## Bounded proportionality and deletion test

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Closed run-intent union | A run can move across target, selected old epoch, or rotation operation. | Deleting the common tuple or rotation-only equality leaves that boundary caller-selected. | The existing create-once record plus pure recomputation is smaller than an epoch key or rotation receipt. Retain. |
| Conflict composition in the slot | A loser can assert a winner or combine a stale receipt with another target. | Deleting loser/winner/receipt composition makes LOST_CONFLICT a digest-only assertion. | One nested closed composition is smaller than adding nullable conflict fields to all eleven slots. Retain, with the missing sibling equalities. |
| Complete authority-history binding | A packet can select a substituted, truncated, forked, or reordered producer history. | Deleting any ordinal record or the genesis/head equality makes complete-chain authentication impossible. | Reusing the required O(n) walk is smaller than a tree, proof, inventory, checkpoint, or compaction protocol. Retain. |
| Outcome-to-slot equality | A valid resolution and a different valid slot can coexist in one hashed commit. | Deleting the equality is the demonstrated coordinated substitution. | Three comparisons against existing values add no public mechanism. Add them. |

The three repaired records close already-required nested values rather than add
new runtime machinery. Their shapes are proportional. The blocker is solved by
declarative equality and executable mutants, so no larger alternative is
authorized.

## Footprint and verification evidence

The exact repair range
`f05f8e0b64faccefe43a98ef2ba3299bf3dacf12..8c11b77cb824311ee4a36ce00fcaad9a8faf52df`
changes exactly five paths and 282 inserted lines: the decision authority,
supervisor ledger, compatibility contract, ISS-002 draft, and round-64 author
record. It changes no implementation, package, test, script, topology, board,
wrapper, glossary, runtime state, or consumer material. This review adds only
this round-65 record.

- Initial `git status --short --branch`, `git rev-parse HEAD`, and
  `git rev-parse 8c11b77` — PASS; exact clean reviewed head resolved to the SHA
  above.
- Exact merge-base/name-status/stat/scoped diff and
  `git diff --check f05f8e0b64faccefe43a98ef2ba3299bf3dacf12..8c11b77cb824311ee4a36ce00fcaad9a8faf52df`
  — PASS; five expected repair paths, no whitespace error.
- Cold authority, supervisor, compatibility, ISS-002, round-64, schema,
  framing, deleted-surface, ownership, vocabulary, and footprint reads/searches
  — complete; one blocking cross-record equality family found above.
- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `node scripts/planning/board-check.mjs` — sandboxed `gh` spawn refused
  `EPERM`; approved read-only rerun PASS (`board contracts verified`).
- `node scripts/format.mjs check` — sandboxed formatter spawn refused `EPERM`;
  approved read-only rerun PASS.
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
- `node node_modules/vitest/vitest.mjs run` — sandboxed helper spawn refused
  `EPERM`; approved read-only rerun PASS (22 files, 252 tests).
- `node scripts/capability-not-implemented.mjs ISS-002 contracts:compatibility-check`
  — sandboxed helper spawn refused `EPERM`; approved read-only rerun PASS (1
  file, 1 test). Current executable checks do not yet implement this repair and
  are regression evidence only, not authority over the written ledger.

During those checks the shared branch advanced to unrelated planning-only head
`b06a09a2316fb6d0274cf044c6e26e4892eaa063`. The five reviewed paths, package,
tests, and checker scripts did not change; the exact reviewed object remained
`8c11b77cb824311ee4a36ce00fcaad9a8faf52df`. The branch movement grants no
authority to or over this review.

## Gate

`BLOCK_REPLAN` authorizes no implementation, merge, release, promotion,
installation, runtime mutation, or self-certification. Repair only the explicit
outcome-to-slot and conflict-producer equalities above, add their mutants, and
return the exact committed head to a fresh independent review.
