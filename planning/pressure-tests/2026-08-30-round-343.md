# ISS-041 partial contract consumer — round 343

Disposition: `BLOCK_FIXABLE` at `c4db387ed3f78355a25e423bb99f7d7c4a7b46b0`,
base `74633faf9518df0f22dd32717a5ebdf61b89b8d1`.
Independent cold reviewer: `/root/iss041_consumer_review`, Sol high, row 11.

The consumer planned from a parsed action snapshot but persisted the original
caller-owned action after awaiting planning. Host execution wrapped the real
plan and mutated the original subject digest at that boundary. Consumption
reported success and both output records parsed, but their public binding
validator returned `binding:actionCoreDigest` and
`binding:immutableSubjectDigest`. The added regression failed; the prior
932-test full suite had passed with 12 expected platform skips.

Retain one parsed immutable action for both planning and persistence, then
prove the persisted binding across the demonstrated mutation. No other
blocker was found in the private fixture, real loader/contracts consumption,
write ordering, bounded filesystem evidence, or coordinated workspace census.

Proportionality: inconsistent integration evidence is the concrete threat.
One retained parsed value is smaller than another validation framework.
The README, ledger, and unchanged root placeholders correctly leave full
cycle, journal, review, lease, and crash/resume acceptance open.
