# ISS-013 read-only snapshot SDK — round 349

Disposition: `BLOCK_FIXABLE` at `3836620b512cab29b361eb97f9e84fdb70171bff`,
base `27f2fde2575c95369404bab8f66383cec41d8f56`.
Independent cold reviewer: `/root/iss013_sdk_review`, Sol high, row 11.

The timeout's initial synchronous check catches a clock exception and resolves
an INTERNAL settlement, but the observer calls readPage before consuming it.
Host execution supplied a monotonic clock returning zero, throwing on its
second call, then returning zero; the adapter threw. The regression observed
one read and UNAVAILABLE/SOURCE_UNAVAILABLE instead of INTERNAL_ERROR with
zero reads. The original full suite passed 965 tests with 12 platform skips.

Propagate the initial deadline failure synchronously, retaining settlement
handling for later timer callbacks. No other blocker was found in pre-call
compatibility, source-bound constants, fresh UUIDv7, closed page aggregation,
fault precedence, bounds, or the two distinct fixtures.

Proportionality: admission prevents incompatible reads; pagination and digest
checks prevent partial or mixed success; the deadline bounds pending work.
Deleting each loses its explicit property. Direct comparisons, local sets,
and one timer remain smaller than registries, receipts, or retry machinery.
CLI and conformance placeholders correctly remain unchanged.
