# ISS-022 Node-probe follow-up — round 244

`BLOCK_REPLAN`.

The 21-row Node matrix is finite, but the physical candidate does not define the four component-digest formulas, absent-leaf ancestor algorithm, filesystem-root identity, or canonical input framing needed to reproduce `Dphys`.
The PASS profile omits helper/ABI/custody selection even though ISS-004/ISS-020 consume them and an ADMITTED locator receipt requires non-null custody instance and receipt digests; deferring persistent custody cannot authorize that arm.
`vectorDigest` and each portable-observation digest lack exact domain tags and ordered frames, so the decision's ordered 63-digest join has no independently recomputable identity.
The BLOCK_REPLAN arm requires no aggregate/provider record, while the landed terminal verifier authenticates only a successful provider-record chain; no closed diagnostic verifier fixes live run/job/artifact/protection/retention joins or the writer identity.
Likewise, synthesizing missing rows as UNKNOWN cannot produce authenticated `environmentDigest`, `observedAt`, and details identities when a required job/artifact is absent under the unchanged workflow topology.
The process row does not fix how the stable parent obtains and proves a grandchild handle/start identity or forces PID-reuse and surviving-descendant negatives; candidate IPC/output cannot supply that authority.
`LOCK_INHERITED_DUPLICATED_HANDLE` must test default non-inheritance only: intentional stable-holder transfer is outside the reviewed-candidate threat model and would manufacture demand for the same-host isolation machinery Round 234 removed.
The O_EXCL owner-death candidate predictably leaves a persistent path and may validly yield UNSUPPORTED; retaining that discriminating Node-first failure is proportional and grants no helper.
Crash barriers, errno reduction, disposable-root writes, four consumers, capability slots, and the stated conformance footprint are otherwise bounded.
Minimal repair is literal physical/custody and digest ledgers plus an authenticated diagnostic terminal chain; add no native helper, principal isolation, reboot lane, or production service before measured evidence.
