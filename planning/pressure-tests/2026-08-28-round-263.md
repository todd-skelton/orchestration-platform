# ISS-022 deterministic multiple-message review — round 263
`PASS`.
The replacement fixture drives the production collector itself through its actual `send`, `message`, `kill`, `exit`, `close`, and stderr surfaces; it does not bypass parsing or response reduction.
Both IPC messages are synchronously provider-observed before terminal events, deterministically proving `messageCount:2`, forced-kill acceptance, provider close, and `responseAccepted:false`.
Removing the real-child race is truthful: a second message never observed before `SIGTERM` cannot be evidence, while any two messages the provider does observe remain refusing.
No production code changed, so private nonce, direct-handle/SIGTERM authority, timeout bounds, grandchild non-authority, and the `dirname(process.execPath)` cwd cleanup remain intact.
Focused tests pass 10/10 and root typecheck passes; the test-only repair is proportional.
