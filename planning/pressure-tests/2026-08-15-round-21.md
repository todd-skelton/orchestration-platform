# Pressure test — round 21

**Disposition:** `BLOCK_REPLAN`

The twenty-first independent review found six remaining durability and trust-
anchor gaps. The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R21-001` — A custom trusted root described only by the kit inventory made verifier/root/bundle substitution circular. | Removed custom-root authority. Added an operator-retained pre-candidate verifier anchor and the pinned CLI's independent default online Sigstore/TUF bootstrap; added combined substitution tests. |
| `OP-R21-002` — Overwriting one launch record lost generations before terminal archival. | Replaced it with create-once transaction/generation/ordinal transition records and a CAS current pointer; terminal archive now manifests the already-durable full chain. |
| `OP-R21-003` — A dead monitor could leave `LIVE` after the child cleared the fence, which was unrecoverable. | Ordered step 15 as terminal receipt/read-back → immutable launch `TERMINAL_COMPLETE` → fence clear → archive/pointer cleanup, with crash recovery at every boundary. |
| `OP-R21-004` — `CONSUMED_BOUND` recovery authority had no schema, owner, path, or cleanup. | Defined canonical `recovery-authorization/v1`, credential-package ownership, exact bindings/CAS lifecycle, launch-generation proof, restart semantics, and terminal native-item revocation. |
| `OP-R21-005` — Removal from pre-key states incorrectly required key revocation. | Defined state-specific removal: cancel/absence from planned, service removal plus key-absence from installed, and revoke-before-removal only for key-bearing states. |
| `OP-R21-006` — The repository-protection receipt lacked a schema and exact transport into production workflows. | Assigned schemas/parsers to ISS-002, a numeric run/artifact producer to ISS-036, protected-environment ID/digest transport, seven-day expiry/current ruleset equality, and exact ISS-038 retrieval/refusal rules. |

## Re-review focus

- Recover and archive three-plus launch attempts, then execute a second promotion.
- Crash through terminal receipt, launch completion, fence clear, and pointer
  cleanup in every order.
- Restart the broker around consumed-bound authorization and terminal cleanup.
- Substitute verifier/root/bundle plus protection receipt/artifact/environment
  variables together and confirm the independent anchors still refuse them.
