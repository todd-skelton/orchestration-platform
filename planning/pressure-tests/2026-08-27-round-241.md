# ISS-003 cold pressure review — round 241

`BLOCK_REPLAN` at exact main `bd5fd563e45774c4a59e9b729997262857537b79`.

`platform-configuration/v1` fixes the project record fields, but `configuration-provenance/v1` and `configuration-paths/v1` have no closed members, canonical bytes, or provenance vocabulary.
Precedence is not executable: exact `ORCHESTRATION_*` names, field/value encodings, unset/empty handling, defaults, `--config` semantics, conflicts, and per-field provenance are unspecified.
Root discovery does not define explicit-root versus ancestor search, “selected checkout” authority, same/different-ID nesting, absent state roots, overlap, or adapter ownership.
Path admission is underclosed across symlinks/junctions, case and Unicode aliases, volumes/UNC, relative XDG/HOME variables, nonexistent tails, canonical file URLs, and zero-write/delta timing.
The exit table is fixed, but stdout/stderr ownership, one-envelope bytes/newline, diagnostic schema/order/codes, text projection, color, partial-write, and internal-error behavior are not.
Redaction lacks a secret/home census and replacement form; `--reveal` lacks TTY/channel/refusal rules and a type boundary that keeps exact paths out of durable envelopes and journals.
The static registry carries only flag names: it has neither typed file/UUID/hex descriptors nor an executable handler ABI, so pre-handler schema rejection and config-only placeholder replacement cannot both hold.
The shell gate lacks an exact OS/shell row census and invocation/quoting/encoding rules, and ISS-006 has no CLI suite row; default bootstrap shells cannot prove cmd, PowerShell, bash, and zsh parity.
The footprint is false: both package test placeholders, root `test:cli-shell-matrix`, the shared runner/registry types, bootstrap verifier/censuses, and likely conformance registration must change under an ISS-000 scaffold amendment.
Proportionality: close these literal tables and authorize the minimal census edits; do not add shell wrappers, Git-specific discovery, journaling, native helpers, or state creation to ISS-003.
