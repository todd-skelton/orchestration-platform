# Pressure test — round 86 destination-owner conflict repair review

**Disposition:** `PASS`

**Reviewed candidate head:** `3459783df76aedf729c5fb92e3857e5c5ffe0905`

**Reviewed parent:** `ddc6298b08ee92a3523a137e28c05ae7c7dafe85`

This was a cold independent removal review of the exact initially clean head
above on `iss-002-versioned-contracts`. It is bounded to removal of
`OP-R85-001`, the coordinated impossible destination-owner conflict winner
admitted at round 85. It authorizes no successor-review, anchor, E0,
installation, promotion, mutation capability, or self-certification surface.

The review began with `AGENTS.md`, `docs/planning/work-model.md`, the sole
decision authority in `docs/architecture/contract-decisions.md`, its
incorporated normative external-bootstrap ledger in
`docs/architecture/supervisor-contract.md`,
`docs/contracts/compatibility.md`, ISS-002, ISS-020, ISS-022, and rounds 84 and
85. The exact parent-to-candidate diff was then read cold. No Chase Sets
repository, issue, project, board, backlog, or consumer system was accessed.
The board gate read only this repository and its registered delivery project.

## Finding removal

`OP-R85-001` is removed. The candidate adds one private bounded
`proposalValueSemanticIssues` composition and invokes it for both the losing
and winning proposal/value graphs before a conflict can validate. The helper
recomputes the successor value digest, stable owner-current position digest,
and derived mutation ID, then enforces the exhaustive transition/source,
prior-nullability, successor branch, and transition-evidence relationships.
The ordinary mutation validator reuses the same helper and retains its fuller
selected-prior/readback checks.

The original coordinated winning mutant was reproduced from a valid genesis
winner. Its derived mutation ID
`4bf109ecbecfdc7f1474187955f4ff8762ab8f0680b2c5bf6495b66e4db039f4`
was replaced by 64 `f` characters, its derived position
`f832384c76d270c692d458d857a6fa82549aa17f46aa3b84017ed5ef078d5c6a`
was replaced by 64 `e` characters, and `source=SUCCESSOR_REVIEW` was paired
with `transition=ACTIVATE_GENESIS`. The winning proposal digest, selected tip,
and conflict receipt fields were rebuilt from the forged graph. The repaired
validator returned a non-empty refusal.

Executable attacks then isolated every repaired equality:

- a forged winning mutation ID with proposal, tip, and conflict digests
  rebuilt refused;
- a forged winning position with all downstream digests rebuilt refused;
- a forged winning source with its mutation ID freshly rederived from that
  source, then proposal, tip, and conflict digests rebuilt, refused on the
  source/transition branch rather than relying on stale sibling disagreement;
- the coordinated three-field winning forgery refused; and
- the same three independent attacks and coordinated attack on the losing
  graph, with its proposal and conflict fields rebuilt, all refused.

Branch attacks independently replaced a genesis null prior triple with a fully
non-null triple, replaced a non-genesis prior triple with all null, supplied a
genesis ACTIVE successor with ordinal one and successor-review evidence,
supplied an ACTIVE successor to `CONSUME`, and substituted consume evidence
while rederiving the mutation. Each forged graph was tested as both loser and
winner and refused. Mixed-null triples remain parser failures before semantic
composition; lifecycle-specific anchor/review/archive nullability remains
closed by the value parser.

The positive control rebuilt all five valid transitions through the ordinary
mutation validator: `ACTIVATE_GENESIS`, `CONSUME`, `RETIRE_UNUSED`,
`RETIRE_CONSUMED`, and `ACTIVATE_SUCCESSOR` all returned `[]`. For every
transition, two otherwise identical legal proposals differed only in canonical
`proposedAt` bytes. Because timestamps are deliberately excluded from mutation
identity, they retained the same legal mutation while producing distinct
proposal receipts; each of the five resulting real conflict graphs returned
`[]`. The repair therefore closes the impossible graphs without narrowing any
valid transition or real conflict branch.

## Exact delta and public surface

The exact `ddc6298..3459783` candidate delta has three paths and 298 insertions:

- `packages/contracts/src/owner.ts`: 57 insertions for the private shared
  semantic composition and its two call sites;
- `test/contracts/owner.test.ts`: 29 insertions for coordinated winning
  mutation, position, and source regressions; and
- `planning/pressure-tests/2026-08-18-round-85.md`: the 212-line incorporated
  blocking review record.

`git diff --check` passes. No parser field census, schema version, digest
domain/order/type, path, registry row, public export, compatibility row,
package manifest, wrapper, runtime service, planning authority, adapter,
consumer, or unrelated contract family changed. The repair creates no record,
receipt, pointer, coordinator, index, retention rule, migration, diagnostic,
or capability.

The helper receives only detached records after all six public conflict inputs
have passed their specialized closed parsers. Proxies, accessors, inherited or
non-enumerable fields, symbols, exotic instances, wrong scalars, mixed
nullability, and unknown/future enum values therefore still refuse before the
helper performs digest or branch work. Its digest functions operate only on
those closed snapshots, and public validators continue to return bounded,
sorted issues rather than executing attacker code.

The helper is private and structural. It does not consume a lock, helper
admission, custody observation, selected runtime authority, private ISS-004
handle, independent review, stable promotion, or operator grant, and it cannot
widen validation success into live authority. FULL_REQUIRED owner history,
current observation, selected prior graph, and later anchor/successor
composition retain their separate authority requirements.

The focused contract/public-surface and compatibility executions retain the
non-resurrection guards for sparse history, inventories, coordinators,
retention/compaction, diagnostics, migrations, v2/v3, and superseded symbols.
The exact candidate diff itself adds none of those deleted surfaces.

## Bounded proportionality

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| One private proposal/value semantic helper called on both conflict arms | An internally rehashed but impossible loser or winner manufactures conflict evidence. | Delete either call or one position/mutation/source/branch equality and the corresponding independent or coordinated executable mutant becomes admissible. | Retain this one shared helper; duplicating the ordinary validator or adding a record/coordinator is larger and supplies no additional evidence for this bounded repair. |
| Existing closed owner parsers before semantic composition | Reflective or malformed inputs execute attacker behavior or reach digest work with open shape. | Bypass one parser and its hostile-shape or missing/extra-field mutants can reach semantic use. | Retain the existing parser boundary unchanged; no new snapshot or wrapper is needed. |
| Exact branch/nullability/evidence checks | A legal digest graph gives a proposal a lifecycle meaning excluded by the transition matrix. | Remove one branch check and the named all-null/all-non-null, wrong-successor, or rederived wrong-evidence mutant survives. | Retain the small exhaustive branch block; a new generic state machine or history structure is not justified. |

Nothing supports an additional owner record, transition, lifecycle state,
proof tree, inventory, coordinator, migration, candidate verdict, or live
capability. The repair is the smallest credible shape that closes the confirmed
round-85 threat on both conflict arms.

## Verification evidence

The required gates ran sequentially in the requested order:

1. `pnpm run planning:check` — PASS, `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS, `board contracts verified`, against
   only this repository and its registered delivery project.
3. `pnpm run format:check` — PASS, all matched files use Prettier style.
4. `pnpm run typecheck` — PASS.
5. Standalone `pnpm test`, once only — PASS, 23 files and 276 tests in 134.89
   seconds.
6. Separate `pnpm run verify:bootstrap`, once only — PASS: manifests and
   registries, formatting, typecheck, 23 files/276 tests in 150.64 seconds,
   private compositions, planning contracts, and source-tree status
   invariance.
7. Focused `pnpm --filter @orchestration-platform/contracts test` — PASS, 13
   files and 79 tests.
8. Focused `pnpm run contracts:compatibility-check` — PASS, one file and one
   test.

The independent executable removal matrix additionally passed one temporary
file and four tests. Its first sandboxed launch was refused before config load
because the local build helper could not spawn; after normal reviewer approval,
the matrix ran once in the configured test path and passed. The temporary file
was removed before repository gates. There was no assertion failure,
repository-test retry, isolated failing-test rerun, timeout override,
implementation edit, or test edit. The existing 30-second ordinary-test
default and explicit 300-second inventory/command census ceilings were
unchanged.

Immediately before this report, the branch and both full SHAs still matched,
the source worktree was clean, and the exact candidate diff still passed
`git diff --check`. This review adds only this uncommitted round-86 record and
does not change the reviewed candidate head.

## Final status

`PASS` for removal of `OP-R85-001` at exact candidate head
`3459783df76aedf729c5fb92e3857e5c5ffe0905`. The destination-owner contract
slice is ready to return to its ordinary external-ledger delivery path, subject
to every later slice's separate exact-head review. Green validation grants no
merge, release, promotion, installation, runtime mutation, live capability, or
self-certification authority.
