# ISS-022 physical normalization follow-up — round 279

`PASS` for the round-278 repair and the complete physical-normalization slice
based on `ccadacfd106a08ef25ffb9a9c8739b88a29ba040`.

Every row is now recursively copied into a frozen, proxy/accessor-free data
snapshot before validation or disposition. The normalizer requires exact
booleans, the closed OS enum, closed relation/kind values, bounded operation
error spelling, and valid closed root, leaf, locator, derivation, and nested
physical-identity records. A failed operation reaches UNSUPPORTED only when
its root and locator facts are coherent and unchanged; movement,
operation/error contradiction, or a missing/unregistered errno remains
UNKNOWN. Wrong-type, invalid-OS, nested proxy, accessor, malformed, and moving
UNSUPPORTED mutants are decisive.

The exact six-row order, base `Dphys`/`Ddest` recomputation, OS-specific alias
expectations, retained-root-handle symlink/parent-swap checks, stable package
dependency and bundle census remain intact. Focused handler and stable-bundle
tests pass 14/14, root typecheck passes, and the slice adds no registry,
workflow, receipt, writer, publication, decision, or self-authority surface.

Proportionality: the repair is confined to validation and mutants in the one
physical handler. The total slice remains one normalizer, one focused test
file, and mechanical package/census wiring, reviewable in one sitting; a new
parser or authority layer would be larger and out of scope.
