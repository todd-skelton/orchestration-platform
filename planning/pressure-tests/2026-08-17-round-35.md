# Pressure test — round 35

**Disposition:** `BLOCK_REPLAN`

The first follow-up attacked the linear replacement protocol rather than the
deleted machinery. It found four correctness gaps; the planning amendment now
contains their repairs.

| Finding                                                                                                                                                              | Repair applied                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OP-R35-001` — Writing an old rotation run's terminal resolution from a fresh successor epoch violates the no-post-CAS-write and single-epoch boundaries.            | Remove the write. Derive truth: prior selected plus exact matching head+1 is resumable under the old epoch; successor selected plus exact record and old CAS-armed checkpoint is `SELECTED`; otherwise `UNKNOWN`.                                                                      |
| `OP-R35-002` — A uniform record shape would force GENESIS to fabricate a retiring epoch.                                                                             | Close `authority-history/v1` over GENESIS ordinal 0/genesis predecessor/`Dgb`/shared `Dsc`/no retiring epoch and ROTATION ordinal >0/prior head/retiring `Dp/Dt/Dv/Dr`/derived `Drot`/shared `Dsc`; downstream genesis-selection evidence binds E0 and CONSUMED facts without a cycle. |
| `OP-R35-003` — A structurally valid `MUTATION_COMMIT` packet could report an outcome inconsistent with the registry slot or smuggle unbounded native failure detail. | Cross-bind every identity and top authority; require selected target, real winner, or empty slot. UNKNOWN carries only closed category/reason, digest, and safe length.                                                                                                                |
| `OP-R35-004` — Linear history cost was described as bounded without an executable threshold, inviting speculative checkpoints.                                       | State O(n) honestly. `ISS-006` owns a 1,000-record walk at no more than five seconds on each supported OS; checkpointing stays parked until measured failure.                                                                                                                          |

Re-review must attack the exact selected unions and derived observations; no
new receipt, handoff, checkpoint, or successor-epoch mutation may be added as
a convenience.
