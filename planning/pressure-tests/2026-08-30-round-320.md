# ISS-022 decision-core writer follow-up — round 320

`PASS` at exact base/HEAD
`78004217f75b28f1ae0e4d0ce0e19487ea289f49`.

Round 318's two output-custody blockers are closed. The canonical output root
now walks every ancestor and refuses any `.git` directory, gitfile, symlink, or
unreadable marker. The existing repository worktree census still refuses both
ancestor and descendant overlap after realpath, while a requested symlink alias
cannot equal its canonical target. The former empty directory inside the
foreign `130-point-proxy` checkout is therefore refused; no arbitrary checkout
can satisfy custody.

The writer now creates only a non-authority pending name with `wx`, retains its
exact file identity and bytes, hard-links without overwrite to the
attempt-qualified final name, removes the pending link, and accepts only the
unchanged output directory with exactly that one final file and exact core
bytes. On every post-create failure, cleanup touches only retained pending/final
paths with the original file identity, then proves the root unchanged, both
owned names absent, and the directory empty. Clean removal returns ordinary
refusal; movement, collision, or foreign residue returns `cleanup-refused` and
never deletes the foreign entry.

Focused mutants cover successful one-file output, foreign `.git` directory and
gitfile ancestors, current-checkout ancestor overlap, canonical alias refusal,
changed final bytes, extra entry, non-overwriting final collision, pending-root
movement, and finalized-root movement. The existing composer, terminal
authentication, four bounded arguments, router closure, and publication scope
are unchanged.

Proportionality passes. The repair adds one private custody transaction and a
fixed non-authority test seam rather than another writer or authority record;
the test growth is justified by the two filesystem-race boundaries and remains
reviewable in one sitting.

Focused evidence passed: hosted-terminal API tests (10), repository TypeScript,
touched-file Prettier, and `git diff --check`.
