# ISS-003 pure resolver implementation pressure review — round 321

`BLOCK_REPLAN` at exact base/HEAD
`78004217f75b28f1ae0e4d0ce0e19487ea289f49` with the resolver slice
uncommitted.

The 497-line resolver and 473-line focused test are reviewable in one sitting.
They stay inside pure resolution and durable projection: no filesystem,
discovery, admission, process/argv, CLI, dispatcher, `loadConfiguration`,
contract, board, authority, or GitHub behavior is added. Closed invocation,
flags, environment, source, admitted-path and success shapes; the four-way
state-root precedence and exact three-OS defaults; six project-only fields;
`platform-configuration/v1`; frozen fixed failures; no-throw handling; and
private contract-validated redacted projections otherwise hold. The package
dependency, lock entry, and resolver public surface are bounded; the two
projection functions remain absent from the package index.

Two focused blockers remain:

1. Project/config precedence is not bound to the admitted result.
   `resolver.ts` validates selected environment paths only for absoluteness and
   canonicalizes the admitted `configPath`/`projectRoot`, but—unlike
   `stateRoot`—never compares them. A no-file pressure probe supplied explicit
   `/expected/project` and `/expected/project.json` values with admitted
   `/substituted/project` and `/substituted/project.json`; it returned
   `ok:true` and emitted both substituted paths. With CLI/environment values
   null, the same substituted config path also returned `ok:true` instead of
   requiring `<admitted projectRoot>/.orchestration/project.json`. This lets a
   structurally valid admitted-path record substitute selection provenance.
   Bind CLI then environment project/config selections by exact canonical host
   equality and bind the default config path by the selected OS join; add CLI,
   environment, default and lower-precedence substitution mutants. This is
   lexical consistency only and must not add discovery or grant path authority.
2. The issue's named package verification is still a placeholder.
   `pnpm --filter @orchestration-platform/config test` runs
   `capability-not-implemented.mjs` and exits 5, while the new resolver tests
   are reachable only through the root Vitest census/direct invocation. Wire
   the package test script to the focused config suite so the declared
   verification command executes this slice; no new dependency or broader
   test mechanism is needed.

Pressure coverage included proxy/accessor/symbol/non-enumerable/extra/empty
closed-shape paths, exact failure rows and canaries; Windows drive-qualified,
drive-relative and UNC forms; POSIX/macOS/Linux bases and file-URL round trips;
all state-root precedence arms including source-null/default-missing cases;
projection contract validation, mixed-family/state-URL substitution and raw
path nonleakage. The focused suite passes 28/28, repository TypeScript passes,
and `git diff --check` passes. The package command exits 5 as described.

## Proportionality

The in-scope threat is selection substitution after physical admission.
Deleting exact selection equality makes the probe survive. A few OS-aware
comparisons plus focused mutants are smaller than discovery, branding, or a
second authority type. Test-script wiring reuses the existing Vitest setup.
No other mechanism or scope expansion is warranted.
