# Pressure test — round 18

**Disposition:** `BLOCK_REPLAN`

The eighteenth independent review found three bootstrap/recovery blockers. The
39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R18-001` — The release state table still applied successor-only fence/broker/follow-up requirements to N0 and sent every interruption toward active state. | Split N0 into `BOOTSTRAP_APPLYING`/`BOOTSTRAP_ACTIVE`/`BOOTSTRAP_RECOVERY_REQUIRED`; split N1+ into staged, active-handoff, active, pre-activation recovery/abort, and post-activation forward-recovery states with distinct next operations. |
| `OP-R18-002` — A post-pointer predecessor crash destroyed the only IPC channel allowed to recover while the shim refused all child starts. | Defined one fence-derived create-once shim recovery launch using the verified predecessor executable and exact `cycle resume --cycle` argv. It may reconnect under the sole broker mismatch exception, resumes steps 13–15, and prevents duplicate recovery processes or ordinary ticks. |
| `OP-R18-003` — Real host custody was required before N0, but its only permitted broker/client compositions were downstream of that evidence. | Added two `ISS-038`-owned, independently reviewed pre-N0 targets: a host-evidence-only client kit and reduced broker-service composition, with exact seven-command private surface, three-OS lifecycle, no project/release/downstream operations, and later digest-bound in-place upgrade to the final `ISS-020` broker without key export. |

## Re-review focus

- Confirm N0 and N1+ states have distinct recovery/next-cycle semantics.
- Confirm a fenced crash can launch exactly one predecessor recovery and no
  ordinary cycle.
- Confirm `ISS-038` can produce signed host evidence before `ISS-029` without
  borrowing authority from `ISS-020` or `ISS-033`.
