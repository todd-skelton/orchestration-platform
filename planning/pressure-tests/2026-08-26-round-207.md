# ISS-006 Windows admission final formatted-tree review — round 207

## Disposition

`PASS_TO_LAND_WINDOWS_SLICE`.

This is the final fresh independent exact-tree removal review after the
repository formatter normalized only `test/conformance/walk.test.ts` line
endings. The current private Windows refusal-only admission slice remains clean
to land against pushed HEAD
`c3de3212ddb616ceafb7508917a43bf62c414e96`, the sole ISS-006 authority, and
Rounds 175 and 197–206.

Git's exact file census remains one insertion and one deletion. The sole
semantic change remains the bounded test-local timeout approved in Round 206:

```diff
-  });
+  }, 120_000);
```

All Windows authority-bearing implementation, PE, build, fixture, and test
bytes are identical to the clean Round-205/206 tree. No assertion, workload,
trust behavior, runtime bound, or authority surface changed.

## Exact reviewed tree

```text
HEAD c3de3212ddb616ceafb7508917a43bf62c414e96

9abc0df3a4d33ccd09946e9c00d188b12d1cd65c8afc1d8eac498a9e328dfb61  test/conformance/walk.test.ts
8242ee55e2c4f3d66032a61e4a7cd28bd97755eb7e72385d82ad30a1031c22e7  planning/pressure-tests/2026-08-26-round-206.md
ba288f80ce01d0064a0810dfa02cce72e51a3a0a603ed213ac13b32cbef04050  planning/pressure-tests/2026-08-26-round-205.md
215b011af27cd6162644b56a558624368e929e8701fd87c2bfe8c2865b5c95be  packages/conformance/src/windows-isolation-broker.c
712f0280cfbda59582849efc441bc458fcac33ee69857ae764efd969d75fa504  packages/conformance/src/windows-isolation-broker-x64.exe
066353a70969ff152959adf4e8c731e38c7b8d8dc7fcb3b7a5957dc198469a31  scripts/build/windows-isolation-broker.mjs
54e3108b5994d5dffe81f52af69d290db2c5c31c13b577c2608cf88abd38d652  test/conformance/windows-isolation-broker-fixture.c
7ee37ec526a208a925e35883ec680b7691108090c3f755ab66cce5b9b08ae038  test/conformance/windows-isolation-broker.test.ts
3c0e366611a62257fab3f678464f2201d9174ef7f4f5c42ed29d01023e004426  test/conformance/windows-appcontainer-profile.test.ts
```

The tracked PE remains exactly 90,112 bytes. I read the repository
instructions, work model, current status/diff/numstat, complete changed walk
test, Rounds 205–206, current planning state, unchanged Windows hashes, package
boundary, and relevant ISS-006 trust constraints. I did not edit
implementation, run the opt-in mutating native diagnostic, or access Chase
Sets.

## Exact delta verification

`git diff --numstat -- test/conformance/walk.test.ts` returns exactly:

```text
1  1  test/conformance/walk.test.ts
```

`git diff --ignore-space-at-eol -- test/conformance/walk.test.ts` returns only
the one timeout hunk above. There is no whole-file or line-ending semantic
noise in Git's patch.

The test still performs the same 1,000-record cross-root walk from separate
stable and candidate roots into an external execution root, throws on every
issue, requires `ok === true`, and requires exact empty execution-parent
cleanup. The new finite 120-second ceiling is a Vitest scheduling allowance,
not a candidate duration, production timeout, protocol value, or evidence
field. Stable-owned per-walk timing semantics remain unchanged.

## Round-205/206 landing result remains intact

Because every Windows authority-bearing byte is identical, all clean removal
evidence remains unchanged: exact Dgrant/Dlaunch, hostile token/Job/access
proofs, durable OPWL branches and retry, partial runtime cleanup, root-first
restoration, exact admission 0/1/-1 reducer, and complete absence before
downstream cleanup.

Production still contains no `ResumeThread`; candidate and RPC bytes do not
execute in this slice. Candidate output supplies no verdict. The private broker
exports no receipt, aggregate, authority, certification, installation,
selection, or promotion route and remains absent from package-root exports.

No ISS-006 draft, sole ledger, roadmap, or board amendment is warranted. macOS
remains required for ISS-006 completion and hosted PASS.

## Checks actually run

```text
git diff --numstat -- test/conformance/walk.test.ts
  PASS — exactly 1 insertion / 1 deletion

git diff --ignore-space-at-eol -- test/conformance/walk.test.ts
  PASS — sole semantic delta is `});` -> `}, 120_000);`

exact hash/status census
  PASS — every Windows authority-bearing hash equals Rounds 205–206

git diff --check
  PASS — no errors; unchanged Windows implementation/test files retain their
         existing LF-to-CRLF notices

pnpm exec vitest run test/conformance/walk.test.ts
  PASS — 1 file, 14 tests
  Duration 16.32 s; tests 15.15 s

pnpm exec tsc --noEmit -p tsconfig.json
  PASS

node scripts/planning/check.mjs
  PASS — planning contracts verified

node scripts/planning/board-check.mjs
  PASS — board contracts verified

pnpm exec prettier --check planning/pressure-tests/2026-08-26-round-207.md
  PASS — all matched files use Prettier code style
```

## Landing authorization

`PASS_TO_LAND_WINDOWS_SLICE` is the clean latest removal disposition for the
exact formatted tree above. It authorizes landing this private, suspended,
refusal-only Windows admission slice and bounded test-local timeout. It grants
no runtime authority, public contract, ISS-006 PASS, receipt, or promotion
capability.
