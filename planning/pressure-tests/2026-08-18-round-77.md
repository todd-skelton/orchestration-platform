# Pressure test — round 77 run-lineage removal review

**Disposition:** `PASS`

**Reviewed head:** `42ae3de8aead265d7a7a70e42721e01cb452f614`

This was a fresh independent cold review of the exact initially clean head
above on `iss-002-versioned-contracts`. The review began with `AGENTS.md`, the
repository work model, and `contract-decisions.md` as the sole decision
authority, then read the relevant supervisor and compatibility contracts,
ISS-002, rounds 70 through 76, the implementation and tests, and the exact
`557452256f6ee305804db122cebb34ab81ab4614..42ae3de8aead265d7a7a70e42721e01cb452f614`
delta. No Chase Sets repository, project, issue, board, backlog, or other
consumer system was read or touched.

## Disposition

`PASS`. `OP-R76-001` is removed by executable evidence. The bounded delta
changes only `test/contracts/commit.test.ts` and
`test/contracts/pointer.test.ts` (376 insertions, 18 deletions). It adds the
missing coordinated positive-lineage, complete-registry target-identity,
canonical-path alias/mismatch, dynamic-path, and hostile-input mutants without
changing a schema, implementation, authority path, capability, or planning
decision.

No new definite blocker was found. The round-75 implementation routes remain
closed, and the new tests discriminate their removal rather than merely pinning
happy-path bytes. This pass is bounded to the target-`Dp` and run-lineage repair
at the exact reviewed head. It grants no merge, release, promotion,
installation, runtime mutation, live capability, or self-certification
authority.

## `OP-R76-001` removal evidence

### Coordinated positive-lineage evidence now executes

The new positive-lineage matrix constructs complete, internally selected prior
checkpoint evidence and then asks `ordinaryCommitEvidence` to rebuild the
positive current run from it. That reconstruction recomputes the prior
`Dcore`, selector `Dp/Dt/Dv/Dr`, `Dpost`, current checkpoint-zero predecessor,
run ID, all nine current selector graphs and observations, and `Dcommit`.
Therefore the tests are whole-graph substitutions, not shallow mutations whose
failure could be caused only by stale sibling hashes.

The executable cases substitute each selected old-authority `Dt/Dv/Dr` input,
`G`, target kind and canonical path, installation, project, state root,
transaction, source, target mutation, and target `Dp`. Each rebuilt graph is
refused by the named cross-run equality. A separate observation mutation proves
that changed prior `Dpost` cannot initialize an unchanged checkpoint zero.
Existing selector-`Dp` cases continue to refuse independently moved prior and
current selector graphs, and the focused suite exercises the accepted positive
retry plus zero/null, predecessor-ordinal, copied-run-ID, and rotation-zero
boundaries.

This closes the round-76 concern that mutually recomputed sibling hashes could
make a forged lineage appear valid. The validator must preserve both the
internal selected graph and the independently checked prior/current semantic
equalities for the rebuilt fixture to pass; every substituted case fails at the
intended equality.

### Complete registry and dynamic target paths now have negative evidence

The ordinary checkpoint test iterates all ten non-rotation registry kinds. For
each row it accepts the canonical derived target `Dp` and then carries a copied
target digest through the complete checkpoint fixture; every row refuses with
`targetPathInstanceDigest:derived-mismatch`. Commit-level cases separately
exercise the two path shapes that round 76 identified as most discriminating:
`RECOVERY_ATTEMPT_RESERVATION` with `<predecessor-key>` and nested
`POINTER_MUTATION_RUN_CURRENT` with `<target-instance-digest>` plus
`<target-mutation-id>`.

The canonical-path suite iterates all eleven registry rows for fixed-segment
and kind/path substitution, then attacks the reservation path's token boundary,
embedded prefix, suffix, missing placeholder value, duplicate separator, dot
segment, backslash, drive prefix, URI scheme, percent spelling, Unicode
lookalike, digest case, mismatched transaction, alternate valid source, and
unsupported kind. Existing position-derived versus path-derived equivalence
still covers all eleven rows. Together these cases discriminate exact template
reconstruction from prefix/suffix matching, caller-selected `Dp`, or a parser
that ignores dynamic identity fields.

The matrix is proportional rather than a Cartesian repetition of every scalar
against every row. Installation, project, and state are uniform framed inputs
and are attacked in the coordinated lineage fixture; kind/path is attacked
across the full census; transaction/source are attacked on a family that uses
both; and both exceptional digest-placeholder families receive commit-level
negative cases. Repeating every uniform scalar for every row would not test a
new branch.

### Totality and the legacy helper remain closed

The previously added public-helper test still proves that
`validateCommitResolutionBinding` returns
`positive-lineage:full-commit-required` and cannot authenticate positive
lineage from a prior core alone. The new outer and nested hostile-proxy cases
prove `parseOrdinaryCommitEvidence` refuses rather than throws while processing
positive prior evidence. Malformed canonical-path inputs reach commit
derivation through caught failures, while the lower-level pure digest
constructor continues to throw on invalid construction input as designed.

The focused contracts slot now passes 70 tests, up from round 76's 66, and the
full repository suite passes 267 tests, up from 263. The four added tests are
the coordinated lineage matrix, dynamic target commit negatives, full registry
path/alias matrix, and hostile positive-lineage totality case.

## Round-75 removal audit

- `OP-R75-001` remains removed. Prior selector `Dp/Dt/Dv/Dr` and `Dpost`
  initialize checkpoint zero; every current selector `Dp` is independently
  derived; the new rebuilt positive-lineage matrix exercises the remaining
  coordinated substitutions.
- `OP-R75-002` remains removed. Canonical-path target derivation admits the
  complete registry, including both dynamic digest-placeholder families, and
  the new registry-wide copied-`Dp` and path-alias matrices provide negative
  evidence.
- `OP-R75-003` remains removed. The partial helper cannot return success for a
  positive run; the full ordinary commit parser is the only positive-lineage
  route and consumes the complete prior evidence.

## Bounded proportionality and trust analysis

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Coordinated prior/current mutant fixture | A caller recomputes every sibling around a substituted prior graph, so local digest checks stay green. | Delete the rebuilt-current cases and the suite again proves only stale-field refusal. | Reuse the existing fixture constructors to rebuild one complete current graph per semantic equality; retained with no production mechanism. |
| Registry-wide copied-`Dp` rejection | One registered target accepts caller-selected identity, or a dynamic target is silently unrepresentable. | Delete the row loop or its two commit-level dynamic cases and a registry branch loses negative evidence. | One parameterized census plus focused exceptional paths is smaller than a Cartesian matrix; retained. |
| Canonical-path alias matrix | Prefix, suffix, separator, encoding, transaction, or source aliases derive the same apparent target. | Delete the boundary cases and a permissive template matcher could pass while happy paths stay green. | Exercise one case per parser boundary and every row's fixed kind/path relation; retained. |
| Hostile positive-lineage totality | Reflective input throws during authority parsing instead of refusing. | Delete the outer/nested hostile cases and the amended nested path has no direct totality evidence. | Two proxies cover both snapshot boundaries; retained. |
| Full-parser-only positive lineage | A partial helper authenticates an unrelated prior core without checkpoint zero. | Remove its unconditional positive refusal and the round-75 escape reopens. | Keep the existing explicit issue and route positive callers through one validator; retained. |

The repair evidence adds tests only. It adds no record member, persistence
path, pointer kind, receipt, selector, proof, tree, inventory, coordinator,
compaction rule, epoch key, capability, migration, or verdict route. The
smallest credible alternative is exactly the submitted discriminating fixture
matrix; static prose or more happy paths would not prove coordinated-copy
defenses.

Structural validation remains relative evidence only. It cannot issue an
ISS-004 private handle, acquire the kernel lock, select an authority epoch,
mutate state, or certify the candidate that contains these tests. Runtime
authority still requires the selected external trust chain and independently
reviewed stable promotion path.

## Remaining round-70 unauthorized blocks

This review does not reopen or cure round 70's three remaining schema-ledger
findings. The following blocks remain unauthorized until the sole decision
authority supplies literal schemas and branch/equality matrices and a later
fresh independent exact-head review passes:

1. external physical destination identity/observation, destination owner,
   bootstrap anchor, and E0 records and branch matrices;
2. active release, cleanup gate, recovery fence, and cleanup archive records;
   and
3. recovery authorization, reservation, descriptor, launch, attachment, and
   attempt-log records and branch matrices.

Registry names, conceptual digest tables, lifecycle prose, current partial
implementation, or this run-lineage `PASS` do not authorize implementation of
those families from an implementer-selected field census.

## Verification evidence

- Initial `git status --short --branch`, branch read, and `git rev-parse HEAD`
  — PASS; the worktree was clean, the branch was exact, and the reviewed full
  SHA was
  `42ae3de8aead265d7a7a70e42721e01cb452f614`.
- Exact `557452256f6ee305804db122cebb34ab81ab4614..42ae3de8aead265d7a7a70e42721e01cb452f614`
  diff — two test paths only, 376 insertions and 18 deletions; `git diff
  --check` passed.
- `pnpm run planning:check` — the package-manager wrapper produced no output
  for 120 seconds and was boundedly terminated; direct
  `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `pnpm run planning:board-check` — the wrapper produced no output for 120
  seconds and was boundedly terminated; direct sandboxed execution reached the
  known `gh` spawn `EPERM`; approved read-only rerun — PASS (`board contracts
  verified`) against only this repository and its registered delivery project.
- `pnpm run format:check` — the wrapper produced no output for 30 seconds and
  was boundedly terminated; direct sandboxed execution reached the known
  formatter spawn `EPERM`; approved read-only rerun — PASS (all matched files
  use Prettier style).
- `pnpm run typecheck` — PASS.
- `pnpm --filter @orchestration-platform/contracts test` — PASS (12 files, 70
  tests).
- `pnpm run contracts:compatibility-check` — the wrapper produced no output for
  60 seconds and was boundedly terminated; the direct capability slot reached
  sandbox `EPERM`; approved read-only rerun — PASS (1 file, 1 test).
- Full `pnpm test` — the wrapper produced no output for 30 seconds and was
  boundedly terminated; direct Vitest reached sandbox `EPERM`; approved
  read-only rerun, after both focused slots — PASS (22 files, 267 tests).
- Separate `pnpm run verify:bootstrap` — the wrapper produced no output for 30
  seconds and was boundedly terminated; direct execution reached sandbox
  `EPERM`; approved read-only rerun after the completed full suite — PASS:
  manifests/registries, formatting, typecheck, 22 files/267 tests, private
  compositions, planning contracts, and source-tree status invariance all
  verified.

## Final status

`PASS` for exact clean implementation head
`42ae3de8aead265d7a7a70e42721e01cb452f614`. `OP-R76-001` is removed by the
executable evidence above, no new definite blocker exists, and round 70's three
other ledger blocks remain open and unauthorized.
