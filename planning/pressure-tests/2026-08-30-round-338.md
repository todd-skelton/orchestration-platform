# ISS-022 byte-brand repair — round 338

Disposition: `PASS` for the single independent code-review follow-up.
Head: `7e8d445d8b69b331d5f76bae1812894b96b6bc1c`;
reviewer: `/root/iss022_wrappers_review`, Sol high, routing row 11.

Proxy rejection now precedes inspection, and intrinsic `isUint8Array` validation
rejects the host-reproduced forged Uint16Array inputs. Exact-prototype checking
and detached copying remain. Six regressions cover both defects in each of the
three publication files; layout, canonical bytes, digest joins, disposition,
and advisory boundaries are unchanged.

The intrinsic check is the smallest repair; deleting it restores the observed
acceptance defect. No scope or mechanism expansion. Diff checks passed and the
reviewed worktree was clean. Repaired-head execution, full/bootstrap gates and
hosted three-OS checks remain required before landing. No provider action or
capability authority is granted by this review.
