# Pressure round 474 — native worker context from the first real run

ISS-071 source attempt 5, based on merged main
61e030376d9ef568be865a2f0ae8a77e568b95fd. No successful real work/review/CI cycle
or corrected-run result is claimed.

## Observed pressure and attempt history

Source attempt 1 produced 119f2ed; independent review attempt 2 returned BLOCK
for writable-root/Git-metadata isolation. Source attempt 3 produced 56b132d;
independent review attempt 4 returned PASS. PR #326 merged after actual hosted
execution of the 35 dogfood tests on all three operating systems. That evidence
did not certify the native provider's live configuration.

The first real census author session was
01a07cb7-633e-7ac1-b025-31f24dcd539b, native PID 38244, using pinned pilot 56b132d.
The worker received `-s workspace-write` but recorded a read-only effective
policy. Basic shell reads were rejected by policy; the author returned FAIL,
made no source edits, and the pilot stopped at `author-failed`. Native exit 0
did not substitute for a successful worker verdict.

Active restart succeeded as an observation boundary: pilot PID 15200 was
replaced by 100996 at 2026-09-07T16:33:57Z, while author PID 38244 stayed alive.
The before/after attempt-record hashes matched and the second pilot log resumed
the same attempt identity. This proves the observed active restart, not terminal
readiness or a complete useful loop. External evidence is retained under the
controller's `dogfood/census-20260907` directory, including
`active-restart-observation.json`, both pilot logs, author trace and rollout.

The native launch omitted the existing Windows `unelevated` backend selection
because user config was ignored. It also inherited Desktop control-plane/session
variables and exposed parent-app tools. The captured native worktree path was
already normalized. Individual causal attribution for the read-only downgrade
remains unproved; do not relabel this diagnosis as a corrected live success.

## Scope, non-goals and predicted footprint

Add the explicit Windows-only native backend flag and filter seven observed
parent context variables inside the child-process boundary. Preserve auth/home
location, source-only author, read-only reviewer, temporary exclusions, no extra
Git write grant, strict terminal/CI rules and controller mutation authority.
Do not serialize environment values or secrets into request files or logs.

Six files: native adapter, observer wrapper, focused adapter test and process
fixture, concise invocation note and this record. No core-flow, schema, package,
credential setup, sandbox bypass, policy framework or provider mutation.

## Acceptance evidence, verification and review attack surface

Tests inspect Windows versus other-host arguments, unchanged role limits,
synthetic parent-context filtering with preserved auth location and parent
environment, and the actual fake child receiving the filtered environment after
controller exit. Synthetic platform arguments are boundary checks, not evidence
of native isolation on those platforms. No real credentials enter test fixtures.

Run the four allowed local static/planning gates with external logs; independently
review every changed assertion and execute the intended tests in hosted
macOS/Windows/Linux CI. No local runner or native sandbox experiment. Then run
the separately authorized corrected real attempt and retain its actual effective
policy, attempt identities, outcome and restart evidence. Do not overwrite the
first failed attempt or reset its history.

Attack inherited parent permission/pipe authority, auth-location changes,
environment serialization, Windows flags leaking into other hosts, accidental
role broadening and unsupported success claims. Reassess at the next real run;
the smallest justified next change must come from its evidence. ISS-071 remains
open until a useful independent-review/hosted-CI loop succeeds.
