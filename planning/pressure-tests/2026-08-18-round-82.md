# Pressure test — round 82 external-ledger final authorization review

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `35455b92603b0906dbb0463f7f3862e2ba48ba04`

This was a fresh independent cold final authorization review of the exact
initially clean head above on `iss-002-versioned-contracts`. There was no
authoring role or authoring context. The review began with `AGENTS.md`,
`docs/architecture/contract-decisions.md`, and
`docs/planning/work-model.md` as the sole decision authority, then read the
incorporated normative external-bootstrap ledger in
`docs/architecture/supervisor-contract.md`, compatibility, ISS-002/006/020/022,
rounds 70/78/79/80/81, the exact
`d5c9a42416d4b83173a21773cd7f13bdb47b0ac9..35455b92603b0906dbb0463f7f3862e2ba48ba04`
delta, the current external implementation/tests, and both bounded timeout
repairs. No Chase Sets repository, issue, board, project, backlog, or other
consumer system was read or touched. The board check read only
`todd-skelton/orchestration-platform` and its registered delivery project.

## Disposition

`BLOCK_REPLAN`, solely because the required once-only actual named standalone
`pnpm test` gate was not green. It completed with three tests exceeding their
unchanged five-second local ceilings: the LF formatter fixture, the ordinary
registry-kind commit identity census, and the initial reference-inventory
binding test. The run reported 19 files and 264 tests passed, with three files
and three tests failed. None was an assertion mismatch, and neither of the two
repaired long-running tests failed.

The separate once-only `pnpm run verify:bootstrap` then passed its full
22-file/267-test phase in 184.01 seconds and completed manifests, registries,
formatting, typecheck, private compositions, planning contracts, and source-
status invariance. That later green run confirms the assertions and bootstrap
composition remain sound, but it does not retroactively make the required
standalone invocation green. The once-only rule forbids retrying until a pass
appears.

The contract authorization audit otherwise passes. The inventory
`120_000→300_000` and planned-command `240_000→300_000` changes are each
limited to one local test ceiling and preserve the complete executable bodies
and every assertion. `OP-R80-001` and `OP-R81-001` are removed for their named
tests. `OP-R79-001` and `OP-R78-001` through `OP-R78-006` remain removed, no
new external-ledger semantic blocker exists, and round 70 block 1 is
contractually implementation-ready. Authorization remains withheld at this
exact head only because the actual standalone full gate is not reproducibly
green. Round 70 blocks 2 and 3 remain independently unauthorized. This record
grants no merge, release, promotion, installation, runtime mutation, live
capability, or self-certification authority.

## Bounded timeout-removal audit

### `OP-R80-001` — removed

Across the repair sequence from round 80's reviewed head to this head,
`test/reference-inventory/inventory.test.ts` changes only the local timeout of
the key-independent relationship test from `120_000` to `300_000`. Its
relationship families, stable normalization, renamed/reordered/wrapped and
one-field-deleted cases, excluded/accepted helpers, production calls, loops,
and numeric assertions are byte-for-byte unchanged.

The actual standalone run did not report that repaired relationship test as a
failure. The separate bootstrap run passed all 59 inventory tests as part of
all 267 tests. The standalone inventory failure was instead the independent
first test at line 20, whose unchanged five-second ceiling expired after
5.891 seconds. The bounded 300-second relationship ceiling therefore removes
the exact round-80 defect without weakening its assertions or introducing a
global timeout.

### `OP-R81-001` — removed for the repaired planned-command census

The exact reviewed delta has two paths only:

- `test/planned-command-execution.test.ts` changes the one local timeout on
  the planned verification-command execution census from `240_000` to
  `300_000`; and
- `planning/pressure-tests/2026-08-18-round-81.md` records the prior
  independent block and proposed bounded repair.

The test still loads the registered drafts, extracts every root and filtered
verification command, distinguishes implemented capability slots, invokes
every unimplemented wrapper, verifies nonzero exit, parses the fixed result
record, equal-checks schema/outcome/capability/owner/forwarded argv, counts
every declared command, and proves `executed + implemented == declared`.
Every command, owner, argv, result-record, and census assertion is unchanged.
No production file, wrapper, registry, parser, schema, path, or global timeout
changes.

The actual standalone run did not report the planned-command test as a failure,
and bootstrap passed all 267 tests. The previous 240-second planned-command
ceiling is therefore no longer the blocking gate. The fresh failure is in
three separate unchanged five-second tests and does not reopen the bounded
repair.

## External-ledger semantic authorization audit

### `OP-R79-001` — remains removed

E0 alone selects `RUNTIME_AUTHORITY_ABSENT`. Both `RETIRE_UNUSED` and
`RETIRE_CONSUMED` select `DESTINATION_STATE_ROOT_ABSENT`; consumed retirement
observes it after terminal cleanup and before the lifecycle archive while the
applicable destination and anchor locks remain held. The receipt equal-binds
the same-lock `Dabs`, current admitted `Dobs`, helper/custody/state root, and
the exact selected owner/anchor CONSUMED graph. A runtime-only, opaque,
wrong-root, wrong-observation, wrong-custody, or different-lock absence cannot
substitute.

### `OP-R78-001` through `OP-R78-006` — remain removed

- The use intent and proposed-genesis input contain only independent bootstrap
  preimages. `Dgb`, GENESIS `Dh`, E0 `Dv`, `Dbg`, and all downstream selection
  evidence are derived later, leaving a constructible one-way graph.
- Anchor consumption keeps distinct
  `runtimePostSelectionReceiptDigest` and
  `runtimePostSelectionReceiptReadbackDigest` members and equalities.
- `external-destination-absence-observation/v1` has one closed census, exact
  domain/order/path, ISS-022-admitted producer, same-lock create-once/readback
  rule, and exhaustive E0/retirement reason matrix.
- The successor-review candidate has exact framing over destination, selected
  prior RETIRED triple, teardown archive, prior installation, and successor
  authority; external authentication and distinct author/reviewer identities
  prevent self-review and coordinated substitution.
- Retirement remains forward-only and crash-resumable:
  `Dabs → lifecycle archive → teardown receipt → anchor RETIRED → owner archive
  → owner RETIRED`. No archive or receipt contains a future value, proposal,
  or tip.
- Physical leaf identity retains one portable pinned algorithm: canonical
  unpadded base64url, shortest-form UTF-8 without BOM/surrogates/noncharacters,
  Unicode 15.1.0 normalization and lowercase data, exact Darwin/Windows/Linux
  profiles, and the complete one-component and reserved-alias refusal rules.
  Helper/profile/custody rotation cannot move `Dphys/Ddest`.

The literal member censuses remain closed and canonically ordered. Scalars,
nested schemas, nullability groups, lifecycle ordinals, enums, persistence,
framed digest order, path derivations, exclusions, and cross-record equalities
remain exact. The accepted graph cross-binds stable `Dphys/Ddest`, current
admitted `Dobs`, owner/anchor ACTIVE, use intent, `Dsc`, `Dgb`, GENESIS `Dh`,
authority `Dp/Dv`, `Dbg`, proposal `Dr`, tip `Dt`, `Dgp`, owner/anchor
CONSUMED, final consumption, and downstream `Dgse`. The full history walk
rejects fork, gap, reorder, truncation, substituted head, or unarmed excess.

Structural validation grants no mutation authority. External independent
review, ISS-022-admitted physical/helper/profile/custody facts,
destination→anchor→runtime kernel lock order, the selected runtime authority,
ISS-004's private revocable handle, and stable promotion remain separate trust
boundaries. A worker or candidate cannot author its own review, absence
premise, live capability, or promotion.

Current `packages/contracts/src/external.ts` and
`test/contracts/external.test.ts` remain the deliberately partial pre-ledger
implementation: raw-`Dphys` `Ddest` derivation and constructed authority paths
only. Passing focused and bootstrap evidence is regression evidence for that
existing surface, not a claim that the planned ledger is already implemented.

## New definite blocker

### `OP-R82-001` — the actual named standalone gate still has cold/default local-timeout failures

In the parent-confirmed quiet window, after the direct gates had completed, the
single allowed `pnpm test` invocation reached unchanged Vitest and failed only
these three tests:

- `test/format-eol.test.ts:39`, formatted LF source acceptance: 5.169 seconds
  against the unchanged five-second ceiling;
- `test/contracts/commit.test.ts:1366`, target/selector identity derivation for
  every ordinary registry kind: 5.477 seconds against five seconds; and
- `test/reference-inventory/inventory.test.ts:20`, complete initial inventory
  binding: 5.891 seconds against five seconds.

Vitest reported 19 passed files, 264 passed tests, and a 289.44-second wall
duration. These are timeouts rather than failed equalities. The repaired
inventory relationship test and planned-command census both completed without
failure under their bounded 300-second ceilings.

The separate bootstrap verifier subsequently ran the same 22-file/267-test
suite green in 184.01 seconds and completed every later verification phase.
That divergence proves the remaining problem is cold/default full-gate timing
reproducibility, not an external-ledger schema or assertion defect. Release
evidence cannot reinterpret a red first named invocation as success because a
later enclosing gate passed after launcher/cache state changed.

The smallest replan is to preserve every body and assertion while making only
these observed cold tests deterministic. Measure their quiet-window cold
paths, then either remove redundant cold work or give exactly these local tests
a bounded evidence-based ceiling with headroom. Do not weaken a census,
change production behavior, introduce a global timeout, or retry until green.
Return the exact clean repair head through fresh independent review and run
the actual named full and separate bootstrap gates once each.

## Bounded proportionality and trust analysis

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Closed same-lock `Dabs` branches | Runtime-only or opaque absence authorizes E0 or destructive retirement. | Remove one reason/equality and the round-79 implementer choice returns. | Retain the existing two-reason observation and branch matrix; no new record. |
| Upstream-only use intent | Bootstrap requires a digest fixed point or authenticates itself downstream. | Insert `Dgb`, `Dh`, `Dv`, or `Dbg` into the intent and the graph cycles. | Retain independent preimages and downstream checks; no coordinator. |
| Distinct `Dgp` and readback | Receipt identity substitutes for verified persisted bytes. | Collapse them and missing/substituted persistence loses its named equality. | Retain the two existing digests; no new receipt family. |
| Exact review and retirement order | Self-review, coordinated substitution, or future-digest cycles authorize a successor. | Remove candidate identity separation or reverse an artifact edge and the exact defect returns. | Retain the reviewed candidate and `Dabs→archive→receipt→anchor→owner`; no sequencer. |
| Pinned portable leaf algorithm | Helper/profile rotation aliases or moves a physical destination. | Unpin decoding, Unicode data, profile, or reserved names and conforming helpers may disagree. | Retain the fixed cross-OS algorithm; no alias inventory. |
| Bounded local long-test ceilings | Valid exhaustive inventory and command censuses cannot complete under the named gate. | Restore 120/240 seconds and rounds 80/81 reproduce the red gate. | Retain only the two local 300-second ceilings with all assertions unchanged. |
| Deterministic remaining five-second tests | The required named gate is red on first invocation even though the enclosing later gate is green. | Keep the current cold work/ceiling relation and this exact reviewed invocation remains red. | Bound or remove cold overhead only for the three observed tests; no global timeout or assertion weakening. |

The first five mechanisms protect current public bytes and the one actual
multi-writer destination boundary. The last two protect reproducible release
evidence only. Nothing supports a third absence reason, proof tree, inventory,
coordinator, migration, verdict route, global timeout, or candidate-owned
authority.

## Round 70 blocks 2 and 3 remain unauthorized

This review authorizes no implementation of either remaining round-70 block:

2. active release, cleanup gate, recovery fence, and cleanup archive records;
3. recovery authorization, reservation, descriptor, launch, attachment, and
   attempt-log records.

Their registry names, lifecycle prose, partial implementation, green tests, or
the block-1 ledger do not provide their missing literal schema ledgers. Each
requires its own decision amendment and fresh independent exact-head review.

## Verification evidence

- Initial branch/status/full-SHA checks — PASS; the worktree was clean,
  `iss-002-versioned-contracts` was exact, and the reviewed full SHA was
  `35455b92603b0906dbb0463f7f3862e2ba48ba04`.
- Exact
  `d5c9a42416d4b83173a21773cd7f13bdb47b0ac9..35455b92603b0906dbb0463f7f3862e2ba48ba04`
  diff — two paths, 284 insertions and one deletion; only the round-81 record
  and one `240_000→300_000` planned-command local timeout; `git diff --check`
  passed.
- The earlier inventory repair was separately re-audited from
  `8b94edde6217361406ca18f93628b9d78182925a`: one
  `120_000→300_000` local timeout only. Both test bodies and all assertions
  remain unchanged.
- Direct `node scripts/planning/check.mjs` — PASS (`planning contracts
  verified`).
- Direct `node scripts/format.mjs check` — PASS (all matched files use Prettier
  style).
- Direct `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` —
  PASS.
- Direct ISS-002 focused contracts slot — PASS (12 files, 70 tests).
- Direct ISS-002 compatibility slot — PASS (1 file, 1 test).
- Direct repository-only `node scripts/planning/board-check.mjs` — PASS
  (`board contracts verified`) against only this repository and its registered
  delivery project.
- Actual named standalone `pnpm test`, once only — BLOCKED by the three
  unchanged five-second local timeouts above; 19 files/264 tests passed and no
  assertion failure was reported. Neither repaired long-running test failed.
- Separate actual named `pnpm run verify:bootstrap`, once only — PASS:
  manifests/registries, formatting, typecheck, 22 files/267 tests in 184.01
  seconds, private compositions, planning contracts, and source-tree status
  invariance.
- Source status immediately before writing this report remained clean. This
  review adds only this round-82 pressure record and makes no implementation,
  test, or commit change.

## Final status

`BLOCK_REPLAN` for exact clean planning head
`35455b92603b0906dbb0463f7f3862e2ba48ba04`. Both bounded long-test ceiling
repairs preserve every assertion and remove their exact prior blockers;
`OP-R79-001` and `OP-R78-001` through `OP-R78-006` remain removed; no new
external-ledger semantic blocker exists; and round 70 block 1 is contractually
implementation-ready. Authorization remains withheld only because the
required once-only actual standalone full gate failed three other unchanged
five-second local ceilings even though the separate bootstrap verifier passed
all 267 tests. Make only the bounded remaining cold/default gate deterministic,
then return the exact clean head for fresh independent review. Round 70 blocks
2 and 3 remain unauthorized.
