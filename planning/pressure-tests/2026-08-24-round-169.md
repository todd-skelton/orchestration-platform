# ISS-006 pressure test — round 169

## Review basis

This is a fresh independent cold exact-head planning/removal review of
`ISS-006` at initially clean and pushed head
`f8683620f51dc57d40a21c74f6ac17689fe0f504` on
`codex/iss-006-trusted-harness`, with exact parent
`3904be5b5704570e5ef08a18f68f7e87c7f20212`. Local `HEAD`, the local
remote-tracking ref, and a read-only `ls-remote` of
`origin/codex/iss-006-trusted-harness` all resolved to the reviewed digest. The
exact repair changes only `docs/architecture/contract-decisions.md` and
`planning/drafts/ISS-006.md`: 19 insertions and 11 deletions.
`git diff --check HEAD^ HEAD` passed.

I read `AGENTS.md`, the work model, Rounds 166–168, the complete ISS-006 sole
authority ledger and draft, exact repair diff, state-machine/bootstrap mirrors,
ISS-000/ISS-019/ISS-022/ISS-029 ownership boundaries, current conformance
scaffold, root/package/bootstrap/lockfile censuses, and issue #12 cold. Parent
reasoning and previous green results were not accepted as evidence. No Chase
Sets repository, project, issue, board, backlog, source, or consumer system was
read or touched.

Issue #12 is open with the exact ISS-006 title and Portable substrate milestone.
Its body contains the planning-key header, EPIC-RUNTIME parent link,
source-draft link, and verbatim current draft. The board gate independently
proves the same draft/project census below.

The current primary upload-action contract was re-read for the repaired
provider boundary. It confirms that `archive:false` uploads exactly one file
without ZIP wrapping, derives the artifact name from that file, and ignores the
`name` input. Sources:
[upload action input](https://github.com/actions/upload-artifact/blob/main/action.yml)
and [v7 release notes](https://github.com/actions/upload-artifact/releases).

## Disposition

`PASS` for `ISS-006` at exact reviewed head
`f8683620f51dc57d40a21c74f6ac17689fe0f504`.

The sole Round 168 blocker is removed unambiguously. The fresh full removal and
trust sweep found no remaining definite planning blocker, unresolved human
decision, or disproportionate retained mechanism. This latest removal round is
clean and clears this exact packet for ISS-006 implementation, subject to the
ordinary implementation review and gates.

## Round 168 repair verified

The provider-record source basename and API artifact name are now the same
exact value:

```text
conformance-<runId>-<runAttempt>-provider-record.json
```

`runId` and `runAttempt` are already closed positive canonical safe-integer
decimal provider IDs. The record job writes canonical
`github-conformance-provider-record/v1` bytes to that sole ASCII basename and
uses the pinned upload action with explicit `archive:false` and no `name`
input. The provider therefore exposes that same exact attempt-qualified name;
its API SHA-256 and byte length bind the canonical record bytes directly. The
post-terminal verifier requires the identical name, current run/attempt
association, downloaded bytes, digest, length, non-expiry, and provider time
relations.

There is no alternate inner filename or competing name authority. A fixed
`provider-record.json`, a supplied/overridden `name`, any other source basename,
or ZIP mode refuses. The draft's acceptance evidence and independent-review
seed require exact source/artifact naming and explicitly attack the ignored
name input; the sole ledger's deletion matrix already kills archive/mode/layout/
attempt-join substitutions. Reruns remain disjoint because the filename binds
both run ID and attempt. The provider record still does not attest its own
writer job or artifact, so the repaired name adds no digest cycle or
self-certification route.

## Complete schema and reducer sweep

The portable core still owns exactly nine closed `v1` records: bundle manifest,
candidate subject, contract-version census, vector census, required-job
registry, environment, raw-artifact manifest, job receipt, and aggregate. The
GitHub adapter owns exactly two closed records, protection snapshot and
provider record. `DproviderRun` is one framed digest, not a third adapter record;
local mode creates no record.

Every record has a closed member census, enum/nullability arms, canonical JSON
order, distinct digest domain, hostile/reflection refusal, and bounded arrays.
The bundle purpose is exactly `HARNESS|TEST_BUNDLE`. The candidate subject is
the complete regular-file tree and admits only the adapter projection
`type=blob,mode=100644 -> executable:false` or
`type=blob,mode=100755 -> executable:true`; symlink/submodule/other mode or type,
truncated tree, duplicate/unlisted row, missing blob, checkout-only file, and
nonportable path refuse. Git remains adapter vocabulary.

`Dcontracts`, BYTES versus GENERATOR fixture domains and parameter framing,
`Dvector`, and `Dregistry` remain literal and bounded. The initial census is
exactly 22 ISS-002 vectors, one `iss002-contracts` suite, and Linux/macOS/Windows
required jobs. Future suites compose through package-local stable handler,
vector, helper/custody, and walk-requirement rows. Each receipt binds its own
suite's `Dvector`; `Dregistry` authenticates the mapping and the aggregate does
not assert a false global vector digest. Walk duration is non-null only for a
PASS `WALK_1000` suite and null for `NONE` or non-PASS.

The raw manifest remains exactly environment/report JSON and stderr/stdout text
rows. Length/digest/media/name are exact, environment raw bytes equal
`osImageDigest`, and no newline/encoding/locale/path/order/diagnostic
normalization is hidden. Observation and aggregate artifact identities are the
provider's exact outer ZIP digest/length plus safe exact inner layouts. The
observation archive contains only its six fixed root files; the aggregate
contains only `aggregate.json` and one canonical receipt path per registry job.
Traversal, absolute/backslash paths, symlinks, duplicates, encryption,
unsupported compression, over-bound entries, extra paths, cross-role layouts,
or inner/outer digest substitution refuse.

The reducer requires one receipt for every required row, no extras/duplicates,
the exact suite/job/environment mapping, suite requirement arms, all shared
candidate/stable/provider identities, recomputed environment/raw/receipt
digests, PASS for every row, and the selected timing arm. Aggregate result is
derived sole `PASS`; an aggregate alone is never hosted authority.

## Provider topology, time, retention, and trust sweep

The sole dispatch payload is the closed member `candidateRevision`, naming one
lowercase 40-hex commit resolved in this repository. The plan job requires
protected `refs/heads/main`, exact workflow path/ref, and
`GITHUB_WORKFLOW_SHA = GITHUB_SHA = workflowRevision`. Stable and candidate
bytes use separate credential-free roots. Permissions are only
`contents:read` and `actions:read`; there are no secrets, environments, caches,
OIDC, self-hosted runners, status writes, or promotion authority. Candidate
children receive an allowlist without GitHub/Actions tokens or provider
metadata.

Every remote `uses:` dependency is a reviewed full 40-hex SHA. Tags, branches,
short hashes, unpinned remote executable downloads, and installs outside the
reviewed lockfile/integrity graph refuse. The protected workflow revision binds
those exact action references. This is the smallest frozen executable boundary;
it adds no engine record or external dependency inventory.

Candidate code emits only untrusted observation data. A fresh stable-only job
recomputes manifests/provider facts and writes receipts plus at most one
aggregate; a later stable-only record job re-queries jobs, artifacts, retention,
and protection before writing the provider record. No later job executes
candidate output. Same candidate subject, repository, workflow revision/ref,
run, attempt, protection, registry, suite/jobs, artifact names/roles/bytes, and
expiry are cross-bound. Missing, duplicate, skipped, cancelled, timed-out,
unreadable, expired, local, prior-attempt, mixed-run, unsupported, or non-PASS
evidence cannot produce hosted PASS.

`recordedAt` is exactly the canonical millisecond projection of the provider
`Date` header on the final current-attempt jobs API response immediately before
record serialization. Missing, repeated, malformed, or non-UTC headers refuse.
The post-terminal verifier waits for the whole run, then requires the unique
successful record job and exact provider artifact with the inclusive relation

```text
recordJob.startedAt <= recordedAt <= artifact.createdAt <= recordJob.completedAt
```

and expiry at least 30 complete days after `recordedAt`. Listed observation and
aggregate artifacts have the same remaining-retention requirement. Requested
retention is never trusted, use stops at the earliest live provider expiry, and
durable certification remains ISS-019's responsibility.

The digest direction remains acyclic:

```text
provider context -> DproviderRun -> receipts -> Daggregate
  -> provider record -> independent post-terminal PASS/refusal
```

The verifier creates no authority record. Status-check names are presentation
only. No ISS-006 API can certify, review, promote, select, install, mutate, or
issue a live capability, and no candidate verdict is routed into its own
promotion. ISS-022 owns the later independently reviewed capability decision;
ISS-019 and ISS-029 retain certification and authenticated-provenance ownership.

## Timing, local mode, footprint, and proportionality

The 1,000-record vector runs in three fresh Node processes per OS. Bytes are
read before measurement, and `process.hrtime.bigint()` surrounds JSON parse,
full genesis-to-head validation, and selected-head equality. Every interval is
independently at most five seconds; startup/install/file IO, warmup, averaging,
best-of, retry, checkpoint, and index are excluded. A timeout, overflow,
refusal, or single slow interval is non-PASS.

Local `harness:test` emits no schema, begins with exact
`ADVISORY CONFORMANCE RESULT`, exits 0 only for all-PASS and 1 otherwise, cannot
call hosted serializers/verifiers, and is rejected by every hosted parser. It
adds no duplicate advisory contract.

The predicted footprint remains exactly `packages/conformance/`, one protected
workflow, environment/vector/workflow-mutation fixtures, conformance tests and
capability slots, two root ISS-006 commands, the package-local test command,
every ISS-000 bootstrap census restating those slots,
`scripts/verify/bootstrap-contracts.mjs`, and the exact conformance lockfile
importer/dependency row. Current placeholders and mirrors make that boundary
constructible. `packages/contracts`, runtime primitives, scheduler, database,
signature/OIDC, checkpoint, promotion, and consumer adapters remain forbidden.

| Retained mechanism | Concrete in-scope threat | Deletion result | Smallest credible shape |
| --- | --- | --- | --- |
| Complete candidate subject | Revision locator is paired with partial/moved/mode-changed bytes. | Deletion admits subject substitution. | One bounded portable file census and adapter-only two-mode map. |
| Stable vector/registry records | Candidate selects tests or required jobs. | Deletion admits omission/downgrade. | Three bounded semantic censuses; no candidate discovery. |
| Separate observation/aggregate/record roles | Candidate writes its own trusted result. | Deletion admits self-certification. | Three stable workflow roles; no certifier or promotion job. |
| `DproviderRun`, protection, provider record | Another repository/workflow/run/attempt/protection is joined. | Deletion admits mixed provenance. | One opaque core digest and two adapter records. |
| Exact outer/inner artifact bytes | ZIP identity is mistaken for canonical evidence. | Deletion admits archive/layout substitution. | Provider digest/length plus role-specific paths; no general archive system. |
| Direct attempt-qualified provider file | Provider-record bytes are replayed or ambiguously named. | Deletion admits prior-attempt/cross-mode evidence. | One filename equal to one API artifact; no alias or receipt. |
| Provider time and live expiry | Runner time or unavailable evidence is trusted. | Deletion admits out-of-job or expired evidence. | One response header, job/artifact timestamps, and live readback. |
| Three fresh walk intervals | Warmup/retry hides an unbounded walk. | Deletion admits best-of timing. | Three direct intervals; no optimization. |
| Fixed local marker | Local/candidate JSON masquerades as hosted evidence. | Deletion admits local-hosted substitution. | One non-record stream and closed exit mapping. |

Every retained mechanism has a current ISS-006 threat and executable deletion
case. Deleting the Round 168 fixed-name alias made the repaired provider boundary
strictly smaller. No signature, attestation, actor allowlist, general artifact
system, scheduler, database, checkpoint, or promotion surface is justified.

## Human-decision boundary

No unresolved human decision remains. The approved protected-dispatch and
zero-bypass topology, non-archive provider byte boundary, and non-promotional
scope are unchanged. A human decision would be required only to replace that
provider topology or add actor, status, signature/attestation, or promotion
authority; this clean round recommends none of those expansions.

## Verification

All required gates were run strictly one at a time with 600,000 ms suite
ceilings. The standalone full suite exited before bootstrap began. The signed
package-manager pre-script launcher remains unavailable in this environment, so
the permitted same-lockfile direct equivalents were used. Read-only board
access and bundled tool subprocesses received only bounded spawn permission.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS:
   `board contracts verified`; it proves issue #12's body is draft-derived.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 39 files and 422 tests in 81.13 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests/registries,
   formatting, typecheck, 39 files and 422 tests in 78.78 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This clean latest removal round clears exact head
`f8683620f51dc57d40a21c74f6ac17689fe0f504` for ISS-006 implementation.
Implementation must remain within the sole ledger, predicted footprint, and
non-promotional topology and must land in reviewable slices with all gates
green. First authoritative hosted evidence still requires the reviewed
workflow to land on protected `main`; a candidate-branch run cannot certify
itself.

This round does not authorize a workflow dispatch, ruleset mutation,
certification, capability grant, promotion, installation/state mutation,
consumer access, or any out-of-scope machinery.
