# ISS-022 absence-row pressure review — round 292

## Disposition

`PASS` for the exact `+321/-12` uncommitted diff over
`dad3e5af5c78d30ef476854abfcef9c50e8b28fd`. This authorizes only
`ABSENCE_HEAD_PLUS_ONE_TWO`, not an ISS-022 profile or capability decision.

## Pressure result

The stable worker opens one fixed `absence-lock`, performs `lstat` on exactly
`authority-head-plus-one` and `authority-head-plus-two`, and closes the same
handle only after both calls. No caller path arguments enter that closure.
The stable provider then rechecks those same two literal leaves after the child
has closed. PASS requires one coherent natural child terminal record, exact
canonical stdout, empty stderr, no timeout/signal/overflow, child `ENOENT` for
both paths, and provider `ENOENT` for both paths. Presence at either observation,
unexpected errno, close/execution failure, malformed or contradictory output,
and partial evidence remain UNKNOWN or refusal.

The unsupported arm admits only the ledger's exact unsupported errno set for
one complete attempted-operation side while the other exact side still proves
both `ENOENT`; it never elevates to PASS. The normalizer snapshots the closed
row before reduction and rejects proxies, accessors, exotic prototypes,
missing/extra fields, path-name substitution, and widened censuses. Child text
has no verdict authority. `absence.ts` is now in the stable harness source
census beside the already-bound worker, handler, and tests.

Round 234's protected-main, provider-fresh VM, stable-parent exclusive-root
boundary excludes an unadmitted same-host actor that creates and removes a leaf
entirely between observations. For admitted mutations, the held lock and exact
post-child recheck are the smallest credible same-lock/later-presence evidence;
the row claims neither global filesystem atomicity nor production custody.

## Proportionality and verification

The in-scope threats are forged child absence, path substitution, presence
after child exit, and unsupported/error promotion. Removing the stable literal
worker, terminal gate, or exact provider rechecks reopens one of them. The
single raw executor, one closed normalizer row, and focused mutants add no
helper, retry, deletion, watcher, receipt, privilege, or promotion machinery.

- Focused handler and filesystem executor suites: 53/53 passed.
- Stable-bundle census: 6/6 passed.
- Portable-primitives and conformance TypeScript checks passed.
- `git diff --check` passed.
