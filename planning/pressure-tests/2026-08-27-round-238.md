# ISS-006 stable-esbuild review — round 238

`PASS`.

Run `33137976273` confined the new blocker to all-OS `walk-1000-records` failure after the other 21 vectors passed.
The absolute file URL resolves only the pinned stable install from the workflow-enforced stable cwd; no candidate-controlled resolver, dependency, environment, or path reaches the child.
The hosted wrapper remains in runner temp, candidate modules remain confined and consumed into the separate execution root, and no runtime state is written to either checkout.
Portability is explicit through `createRequire` plus `pathToFileURL`; exact `5607378`/`56632d` reproduction passed three walks at 416941600, 451132500, and 459545800 ns; focused tests pass 50/50.
The two-file patch adds no schema, receipt, authority, isolation mechanism, privileged operation, or tracked binary; candidate results remain non-self-certifying.
Proportionality: externalizing the already-installed frozen esbuild provider is the smallest repair; copying dependencies or adding a loader/runtime surface would be larger.
