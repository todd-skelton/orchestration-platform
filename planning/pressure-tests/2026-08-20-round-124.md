# ISS-002 pressure test — round 124

## Review basis

This is a fresh independent cold removal review of exact initially clean
candidate head `0b1e722b89415652068f972431879599933ad824` on
`iss-002-versioned-contracts`, with exact parent
`626334863df324e25d03a81410d4e25d739b8da8`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
implementation authority in `docs/architecture/contract-decisions.md`, the
registered `ISS-002` draft, rounds 122 and 123 as the finding and prior review
under retest, and the exact parent-to-head diff. No Chase Sets repository,
project, issue, board, backlog, consumer source, or other consumer system was
read or touched.

The exact diff changes only `docs/architecture/contract-decisions.md` and
`planning/drafts/ISS-002.md`: seven insertions and five deletions. It corrects
the common-member canonical order in the sole authority and repeats that
literal ordering constraint in the draft, whose existing exact-byte evidence
obligation exercises each full merged branch. It adds no
implementation, test, registry, package, runtime, roadmap, dependency, or board-
topology change. `git diff --check` passed. The checkout was clean before this
required pressure record was written.

## Disposition

`PASS` for the bounded recovery-authorization immutable-core planning ledger.

The implementation-attempt finding is removed. The sole authority now orders
`candidateDigest` before `capabilityDigest` and
`capabilityReferenceDigest`, exactly as canonical JSON lexicographic ordering
requires. The draft repeats that ordering constraint and requires exact
canonical bytes for each complete merged branch, so an implementer cannot
preserve the formerly incorrect list order as a wire contract.

This clean removal round authorizes implementation only of the closed
`recovery-authorization-core/v1` structural parser, deterministic core-path
constructor, sole `Dac` computation, public registration/export work, and the
focused compatibility evidence bounded by the ledger and draft.

## Canonical-order and census attacks

### Independent executable check

An independent Node check encoded the literal fifteen common, three
BOOTSTRAP-only, and twelve SUCCESSOR-only names from the authority, verified
each list was unique and internally ascending, checked all three pairwise
intersections were empty, and sorted each complete branch with JavaScript's
ordinal string comparison. A second executable check deliberately created both
objects in reverse insertion order and passed them through the checked-in
`canonicalJson` implementation. The emitted keys exactly equaled the
independently sorted sequences:

- BOOTSTRAP, 18 unique members:
  `candidateDigest`, `capabilityDigest`, `capabilityReferenceDigest`,
  `destinationDigest`, `expiresAt`, `grantDigest`, `hostIdentityDigest`,
  `installationId`, `installerDigest`, `issuedAt`, `mode`, `nativeGeneration`,
  `producerDigest`, `projectId`, `schemaVersion`, `stateRootDigest`,
  `transactionId`, `userIdentityDigest`.
- SUCCESSOR, 27 unique members:
  `admissionDigest`, `candidateDigest`, `capabilityDigest`,
  `capabilityReferenceDigest`, `cycleId`, `expectedActiveGeneration`,
  `expiresAt`, `hostIdentityDigest`, `installationId`, `issuedAt`, `mode`,
  `nativeGeneration`, `predecessorBrokerGeneration`,
  `predecessorExecutableDigest`, `predecessorOperationManifestDigest`,
  `predecessorReleaseDigest`, `producerDigest`, `projectId`,
  `recoveryFenceRootDigest`, `schemaVersion`, `stateRootDigest`,
  `successorBrokerGeneration`, `successorExecutableDigest`,
  `successorOperationManifestDigest`, `successorReleaseDigest`,
  `transactionId`, `userIdentityDigest`.

The census therefore remains exactly `15 + 3` for BOOTSTRAP and `15 + 12` for
SUCCESSOR. The full merged order is determined without relying on declaration
insertion order. Opposite-branch names remain absent rather than null; the
lists neither overlap nor leave an implementer-selected branch member.

### Type, closure, and scalar rules

Every member retains one exact scalar or literal type. The fifteen common names
and both branch-only lists are closed. Unknown, renamed, null, cross-type,
cross-mode, `candidateOperationManifestDigest`, candidate-verdict, raw
capability/reference, and selected-authority-tuple members refuse. All engine
identities remain opaque digests; no repository, code-host, provider, consumer,
or operating-system credential-store vocabulary enters the record.

The global timestamp, lowercase SHA-256, lowercase UUIDv7, and canonical safe-
decimal grammars supply the named scalar rules. The core proves only the strict
`issuedAt < expiresAt` relation. Later composition requires authenticated
admission evidence satisfying `issuedAt <= admittedAt < expiresAt`; it does not
trust a clock reading or caller Boolean in this parser. SUCCESSOR broker
generations remain exactly adjacent, with grammar and safe-integer bounds
checked before conversion, so equal, skipped, noncanonical, and maximum-plus-one
generations refuse.

### Path, identity, cycle, and trust

The core's sole path is still constructed exactly as
`installation/recovery-authorizations/<transactionId>/core.json`. The later
SUCCESSOR composition boundary derives, rather than stores, the distinct exact
fence-root path
`installation/activation-recovery-fence-roots/<transactionId>.json` and binds
its digest to `Dfr`. Caller paths, enumeration, latest-file conventions, and
other registered fence families grant nothing.

`Dac` remains the one domain-separated digest over one canonical-record frame
part under literal `recovery-authorization-core/v1`. Raw JSON, untagged
canonical digesting, member-wise reframing, and a second core identity refuse.
The dependency order remains acyclic: the SUCCESSOR fence root yields `Dfr`
before `Dac`; `Dac` and the later selected CREATED authorization triple precede
the cleanup-gate root `Dgr`. Neither `Dgr` nor a selected authorization value
enters the core, and BOOTSTRAP has no fence member.

Parsing and hashing do not certify `producerDigest`. BOOTSTRAP composition must
recompute `Dsc` from the exact reviewed `BOOTSTRAP_INSTALL` graph. SUCCESSOR
composition must recompute stable-promotion `Dsc` and bind authenticated
predecessor/candidate release, broker, admission, and fence evidence. Copied
fields, a supplied expected digest, a candidate verdict, or this pressure-test
outcome cannot grant promotion. Stable release N remains the producer for N+1,
external review remains distinct, and a worker or candidate release never
certifies itself.

### Scope boundary

The slice still excludes authorization state; native consumption/removal;
post-selection observation and receipts; attachment; archive and tombstone;
selected epoch, proposal, tip, and CAS; composed equality; persistent lookup;
credential-broker operations; live capabilities; commands; filesystem
mutation; and runtime promotion. No bytes or authority for those surfaces can
be inferred from this PASS.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible disposition |
| --- | --- | --- | --- |
| Literal common and branch censuses with canonical full-record sorting | An insertion-order or cross-mode implementation can hash different authority bytes for the same claimed record. | Deleting the exact names, closure, or ordering evidence admits ambiguous preimages and the observed `capability*`/`candidateDigest` defect. | Retain the direct `15 + 3` and `15 + 12` union and exact-byte tests; no field-order table, general schema language, or proof bag is needed. |
| Strict time order and bounded adjacent generations | Equal/reversed issue windows, unsafe integers, or generation wrap can alias reusable authority evidence. | Deleting a comparison admits invalid lifetime or broker-generation pairs. | Retain direct scalar comparisons in the parser; defer clocks and broker reads. |
| One domain-separated `Dac` and one constructed core path | Untagged bytes or caller-selected placement can substitute another contract or transaction. | Deleting either relation permits semantic cross-use or redirected evidence. | Retain one framed canonical record and one transaction-derived path. |
| Derived later fence-root path and authenticated admission time | Later composition could bind `Dfr` to another fence family or admit expired evidence. | Deleting either later relation permits path substitution or stale admission. | State the exact later relations, but store neither path nor admission time in this immutable core. |
| Explicit composition and runtime deferral | A structural record could otherwise be mistaken for live mutation or promotion authority. | Deleting the boundary invites caller assertions and candidate self-certification. | Retain the fail-closed boundary; add no coordinator, capability, persistence reader, or runtime mutator. |

The shape addresses the concrete ordering, closure, lifetime, substitution, and
reviewed-promotion threats without restoring sparse trees, inventories,
materialization coordinators, epoch-split runs, arbitrary-precision ordinals,
diagnostics, retention, or compaction.

## Draft, board, and dependency assessment

The `ISS-002` draft mirrors the corrected canonical-order authority, exact
counts, full-branch golden obligation, path/digest boundary, structural scope,
and downstream deferrals. No registered dependency edge changed, so the
transitively reduced roadmap remains intact. The board checker passed against
this repository's own registered delivery project; no consumer board was
consulted.

## Verification evidence

All six required gates ran through their checked-in wrappers strictly
sequentially. The standalone full-suite process fully exited before the
separate bootstrap verifier began. The increased timeout was used for both
full-suite executions. No wrapper fallback, direct replacement command, or
overlapping test process was used.

1. `pnpm run planning:check` — PASS: `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS: `board contracts verified`.
3. `pnpm run format:check` — PASS: all matched files use Prettier style.
4. `pnpm run typecheck` — PASS: `tsc --noEmit -p tsconfig.json`.
5. Standalone `pnpm test` — PASS: 29 files and 362 tests in 132.68 seconds. The
   process fully exited before step 6 began.
6. Separate `pnpm run verify:bootstrap` — PASS: manifests and registries,
   formatting, typecheck, 29 files and 362 tests in 131.40 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

`PASS` clears exact head `0b1e722b89415652068f972431879599933ad824`
only for the bounded recovery-authorization immutable-core implementation
surface named in this report. It does not authorize recovery-authorization
lifecycle/state, native/post-selection receipts, attachment, archive,
tombstone, composition, persistence, broker operations, capability, CAS,
runtime mutation, promotion, or the next ledger.

This record grants no merge, release, installation, external mutation, or self-
certification authority. A worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-124.md`; no implementation, test,
draft, issue, roadmap, board, or other planning file was edited, and no commit
was created.
