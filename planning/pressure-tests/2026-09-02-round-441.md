# Pressure round 441 — protection receipt and verifier anchor literals

Status: docs-only contract definition for ISS-054. Protected base is
`6848bf2eb4cefb28f25dbb486ef61c27146d24b3`.

## Declared inputs and scope

Pressure Rounds 21, 24, and 440 plus the current ISS-036 and ISS-038 consumer
drafts are the complete planning inputs. The live census in Round 440 found no
implementation. This round changes only the ISS-054 draft, the bootstrap
contract decisions, and this record. It defines no source, test, registry,
compatibility entry, workflow, provider configuration, ruleset, environment,
anchor file, artifact, credential, or authority.

## Literal closure

The architecture ledger now fixes a fourteen-member
`repository-protection-receipt/v1` and a ten-member
`bootstrap-verifier-anchor/v1`, every nested census and canonical order,
decimal/path/ref/name/ETag/route/timestamp/hash grammars, all enums and
nullability, every array bound/order/uniqueness rule, maximum seven-day receipt
life, three-OS anchor census, and fixed GitHub CLI/release/OIDC/trust/operator
literals. It also fixes domain-separated family identities and the exact
ruleset semantic projection.

The receipt carries exactly one ISS-036 synthetic BUILD and one REVIEW workflow
identity and complete effective five-permission rows. A provider permission key
outside that closed census refuses. It never carries the later ISS-038 workflow
bytes. It carries the intended artifact name and producer run/attempt but not
its enclosing artifact ID, archive digest, upload URL, or a latest selector.
The anchor carries official archive/checksum-manifest/executable identities for
one target architecture on each OS, but no download URL, executable path,
custom root, candidate/kit identity, credential, token, or fallback.

## Pure relation and authority fence

The sole cross-family operation is a four-input pure binder over a receipt,
anchor, closed supplied protection observation, and evaluation time. It
requires `ACCEPTED`, inclusive issue/current and exclusive current/expiry time,
recomputes family and semantic digests, and checks every repository, ruleset,
policy, workflow, permission, API, producer, environment, and anchor equality.
It returns detached parsed values and digests only. Structural success cannot
prove API freshness, page completeness, operator action, artifact authenticity,
public attestation verification, ruleset existence, or authority.

ISS-036 remains the first live producer and anchor writer. ISS-038 remains the
first authority consumer. All GitHub/API access, environment/ruleset mutation,
artifact transport, CLI execution, Sigstore/TUF behavior, filesystem IO,
credentials, installation, and release behavior remain outside ISS-054.

## Acceptance, verification, predicted footprint, and review attack

Implementation acceptance is exact canonical byte/digest goldens, public
schema/registry/compatibility census, total parsing, the complete closed-
snapshot corpus, each boundary and enum, every nested array minimum/maximum,
unsorted/duplicate/incomplete censuses, one-axis field and relation mutation,
combined anchor/verifier/bundle substitution, and committed deletion mutants
for every semantic or cross-record equality. Local verification for this docs
packet is planning checks, formatting, and exact three-file diff inspection;
implementation later adds the two focused contract suites and typecheck.

The predicted implementation footprint is one or two contract source files,
public export/registry/compatibility/serializer wiring, two focused tests, and
the inseparable ISS-036 edge reconciliation. No root script, dependency,
workflow, provider, adapter, probe, filesystem, credential, or runtime file is
predicted.

Independent review should attack field-count mistakes; inconsistent canonical
order; unsafe numeric conversion; incomplete permissions/pages/OS assets;
trigger and ref ambiguity; post-upload self-reference; ETag or caller freshness
presented as fact; stale or overlong receipts; semantic-projection drift;
workflow/producer/repository/signer mismatch; movable verifier or trust roots;
reflection before snapshot; missing registry/serializer routes; and any parser
or binder result presented as operator action, verification, or authority. The
author claims no implementation PASS and no live protection or verifier status.
