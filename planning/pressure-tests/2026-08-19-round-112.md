# ISS-002 pressure test — round 112

## Review basis

This is a fresh independent cold removal review of exact candidate head
`7a214eadb68a396c117fe6d7e7c9ce953456df35` on
`iss-002-versioned-contracts`, with parent boundary
`c6f593349705f35ecaafa80c9cbe91e172fe30f7`. The checkout was clean before
this required pressure record was written.

The review used only the repository instructions, work model, contract
authority, registered `ISS-002` draft, round 111 finding, and the exact
parent-to-head delta. No consumer repository, milestone delivery mechanism,
or external certification path was consulted.

The exact delta has one path, 32 insertions, and 23 deletions:

- `test/contracts/anchor-teardown.test.ts`: 32 insertions and 23 deletions.

`git diff --check` passes. The change adds no source contract, schema member,
digest domain, persistence path, registry entry, runtime state, lock, handle,
mutation capability, producer, promotion path, or compatibility version. It
only parameterizes four timestamps in the already complete successor-retirement
fixture and adds public-wrapper chronology tests.

## Complete graph reconstruction and chronology evidence

The committed builder applies each timestamp before constructing the affected
record and then recomputes its descendants. A read-only in-memory fingerprint
probe confirmed these exact effects:

| Override | Recomputed graph whose digest changed |
| --- | --- |
| `reviewedAt` | `Dsrc`, successor anchor/current-owner value, proposal and tip, `Dsrp`, and the anchor/owner RETIRED local tail |
| `activeProposedAt` | current-owner proposal and tip, `Dsrp`, and the local retirement tail; `Dsrc` and the current-owner value remain correctly stable |
| `postObservedAt` | `Dsrp` only; upstream selection and the local retirement tail remain correctly stable |
| `absenceObservedAt` | retirement `Dabs` and the anchor/owner RETIRED local tail; `Dsrc`, current owner, and `Dsrp` remain correctly stable |

The baseline full graph returns `[]` through
`validateBootstrapSuccessorActiveRetirementBinding`. Each fully rebuilt
reverse graph returns exactly its one intended public-wrapper finding:

```text
prior owner RETIRED 12:04 > Dsrc review 12:03
  successorChronology:review-before-prior-retired

Dsrc review 12:05 > current owner ACTIVE proposal 12:04:30
  successorChronology:active-before-review

current owner ACTIVE proposal 12:06 > Dsrp 12:05:30
  successorChronology:post-before-active

Dsrp 12:07 > retirement Dabs 12:06:30
  successorChronology:absence-before-post
```

The four independently rebuilt inclusive boundaries also return `[]`:

```text
prior owner RETIRED = Dsrc review = 12:04
Dsrc review = current owner ACTIVE proposal = 12:05
current owner ACTIVE proposal = Dsrp = 12:06
Dsrp = retirement Dabs = 12:07
```

The builder retains the parsed local tail after `Dabs`: lifecycle archive
12:09, anchor RETIRED proposal 12:10, and owner RETIRED proposal 12:11. The
existing local validator therefore continues to enforce
`Dabs <= archive <= anchor RETIRED <= owner RETIRED` in the same complete
graph.

## Executable mutation controls

A read-only in-memory Vitest transform ran the exact committed
`test/contracts/anchor-teardown.test.ts` against one source mutant at a time.
It wrote no source, test, fixture, generated artifact, or probe file. The
baseline passed. Every mutant was killed by exactly its corresponding
committed public-wrapper case; the other 27 tests passed in each run.

| Mutant | Exact committed failure |
| --- | --- |
| Delete prior-RETIRED-to-review comparison | Reverse `reviewedAt` case failed because the expected `review-before-prior-retired` finding disappeared |
| Delete review-to-ACTIVE comparison | Reverse `activeProposedAt` case failed because the expected `active-before-review` finding disappeared |
| Delete ACTIVE-to-Dsrp comparison | Reverse `postObservedAt` case failed because the expected `post-before-active` finding disappeared |
| Delete Dsrp-to-Dabs comparison | Reverse `absenceObservedAt` case failed because the expected `absence-before-post` finding disappeared |
| Change edge 1 `<` to `<=` | Equality `reviewedAt` case failed with only `review-before-prior-retired` |
| Change edge 2 `<` to `<=` | Equality `activeProposedAt` case failed with only `active-before-review` |
| Change edge 3 `<` to `<=` | Equality `postObservedAt` case failed with only `post-before-active` |
| Change edge 4 `<` to `<=` | Equality `absenceObservedAt` case failed with only `absence-before-post` |

This removes `OP-R111-001`: neither half of the four-edge chronology nor any
inclusive boundary can now be deleted or made strict while the committed test
passes.

## Structural provenance and branch guard

The committed public-wrapper composition still accepts the exact successor
ACTIVE graph only with its actual prior RETIRED selection, recomputed `Dsrc`,
current owner ACTIVE selection, and recomputed `Dsrp`. Its structural probes
still refuse a substituted `Dsrc.priorRetiredTipDigest` at
`successorReviewBinding:priorRetiredTipDigest:mismatch` and a substituted
`Dsrp.reviewCoreDigest` at
`successorPostBinding:reviewCoreDigest:mismatch`.

A separate read-only in-memory branch pressure case reconfirmed that the valid
successor ACTIVE graph returns `[]`, while replacing its local branch with a
fully constructed CONSUMED retirement retains
`successorBranch:not-active-successor`. CONSUMED therefore remains incomplete,
fail closed, and unauthorized. These pure validators return findings only and
grant no live authority.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion/attack result | Smallest credible shape |
| --- | --- | --- | --- |
| Four optional time inputs on the existing complete fixture | Direct timestamp edits leave stale `Dsrc`, selected-owner, `Dsrp`, or local-tail descendants and test parser noise instead of chronology. | Digest fingerprints prove each override rehashes exactly its affected graph; every reverse case reaches one chronology finding. | Retain four optional scalar overrides on the existing builder; no second fixture, production helper, or new record is warranted. |
| Four reverse public-wrapper cases | A missing edge admits evidence dated before the predecessor it claims to consume. | Deleting each comparison fails only its corresponding reverse case. | Retain one table with four rows and the shared builder. |
| Four equality public-wrapper cases | Accidental strict ordering rejects valid simultaneous boundary evidence. | Every independent `<` to `<=` mutation fails only its corresponding equality case. | Retain one table with four rows; no timestamp policy object or tolerance mechanism is needed. |
| Existing `Dsrc`/current-owner/`Dsrp` composition | Caller-selected digest copies substitute for actual selected provenance. | Exact provenance substitutions remain nonempty at the independently derived bindings. | Retain the existing pure validators and actual parsed record inputs. |
| Successor-only ACTIVE branch guard | A complete CONSUMED local retirement is mistaken for successor ACTIVE authority. | The complete local CONSUMED pressure graph retains `successorBranch:not-active-successor`. | Retain the existing branch predicate; add no lifecycle state or capability. |

The round 111 bounded repair is therefore complete at one test path. Deleting
the parameterized builder/tests reopens the executable-evidence blocker;
expanding the repair into source machinery has no evidenced threat.

## Verification evidence

All gates ran in the mandated strict sequence. The standalone full suite
exited before bootstrap began; bootstrap exited before focused contracts;
focused contracts exited before compatibility. No full and bootstrap process
overlapped.

1. Planning — PASS, `planning contracts verified`, through the checked-in
   direct checker. The restricted `pnpm run planning:check` failed closed
   before its script because signed pnpm 11.22.0 registry metadata could not be
   fetched.
2. Board — PASS, `board contracts verified`, against this repository and its
   registered platform delivery project. The restricted direct checker failed
   closed at the read-only `gh` subprocess with `spawn EPERM`; the approved
   identical direct rerun passed.
3. Format — PASS, all matched files use Prettier style. The restricted direct
   checker failed closed at the pinned formatter subprocess with `spawn
   EPERM`; the approved identical direct rerun passed.
4. Typecheck — PASS with the installed pinned TypeScript 7.0.2 compiler.
5. Standalone full test gate — PASS through the installed lockfile-pinned
   Vitest runner after `pnpm test` failed closed before its script on the same
   signed-registry reachability condition: 28 files and 337 tests in 122.43
   seconds.
6. Separate bootstrap verification — PASS: manifests and registries,
   formatting, typecheck, 28 files/337 tests in 137.60 seconds, private
   compositions, planning contracts, and source-tree status invariance.
7. Focused ISS-002 contracts — PASS, 18 files and 140 tests in 10.48 seconds.
8. Focused ISS-002 compatibility — PASS, one file and one test in 1.46
   seconds.

The approved in-memory pressure runs were serialized outside the gate sequence
and wrote no repository artifact. Before this required pressure record was
written, every gate and probe left the source tree clean and exact HEAD
remained `7a214eadb68a396c117fe6d7e7c9ce953456df35`.

## Final status

`PASS` for exact candidate head
`7a214eadb68a396c117fe6d7e7c9ce953456df35`, with parent boundary
`c6f593349705f35ecaafa80c9cbe91e172fe30f7`.

The round 111 executable-evidence blocker is removed. The committed complete
builder fully rehashes the affected `Dsrc`/current-owner/`Dsrp`/local-tail
graph, all four reverse chronologies refuse only their intended issue, all
four equality boundaries accept, and deletion or strictness corruption of any
edge fails the corresponding committed public-wrapper test. Structural
provenance and the valid-successor/CONSUMED branch boundary remain fail closed.
