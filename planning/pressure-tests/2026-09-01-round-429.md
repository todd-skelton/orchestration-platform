# Pressure round 429 — ISS-048 hosted native-lock delivery boundary

Status: bounded replan plus stage-one hosted plan registration. No workflow
trigger, native build/run, artifact upload, provider record, selection, or
operator authority is activated.

## Measured blocker and disposition

The registration predicted one implementation sitting across dispatch,
toolchain/distribution acquisition, native process coordination, retained
artifacts, three-OS aggregation, and terminal provider verification. The landed
code proves those are four independent attack surfaces. The generic observation
runner accepts only `ISS022_PORTABLE_PRIMITIVES` and a fixed six-file archive;
the generic aggregate semantically accepts only the existing portable-primitives
suite. Native build preparation requires already-produced official Node and
toolchain evidence, while the fixture foundation explicitly leaves chronology,
controls, archive collection, and hosted execution to a missing coordinator.

Combining these surfaces would span the workflow and every hosted stage plus
native acquisition/coordinator/collector code and could not receive meaningful
independent removal review. The issue is amended to three ordered stages: exact
hosted plan registration; complete single-OS observation ownership; then
three-OS workflow/aggregate/record/terminal activation.

## Stage-one scope and evidence

Stage one adds only the literal `iss022_native_lock_experiment` action, closed
`ISS022_NATIVE_LOCK_EXPERIMENT` runner token, exact Linux/macOS/Windows required
job registry and fixed runner matrix, distinct private selection/context, and
digests over the actual stable harness/test files plus the frozen four-case,
twelve-control, and prerequisite censuses. The existing candidate revision and
protected-main provider bindings remain mandatory. Ordinary
`conformance_candidate` selection/context bytes and matrix remain unchanged.

Focused tests must refuse changed action, payload-selected matrix, runner token,
job ID, OS, requirement, row order, missing/extra row, moved candidate, empty
provider token, or stable bundle/census substitution. Planning, typecheck,
format, bootstrap, diff, and three-OS hosted CI remain required.

## Non-goals and review attack surface

No `.github/workflows/conformance.yml` change is permitted in stage one: a live
trigger would dispatch into an observation runner that cannot own the native
experiment and would create only a failure or misleading diagnostic. No #182
parked source-repair file, local compiler, native source, build script,
selection/profile/capability writer, release path, or provider mutation is in
scope. Review should attack ordinary-action drift, action/context aliasing,
caller-selected matrices, an optional OS, incomplete stable census, candidate
self-certification, premature scheduling, and a plan digest presented as native
or capability evidence.

Prediction: after this stage, protected stable N can authenticate and reproduce
the exact future native-lock plan, but cannot schedule or execute it. Stage two
must supply the complete single-OS observation owner before stage three exposes
the repository-dispatch trigger.
