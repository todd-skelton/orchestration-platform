# ISS-022 CREATE_ONCE_32_CONTENDERS repair — round 284

## Disposition

`PASS`.

This is the one permitted post-repair review. It closes gate 5 for the exact
current uncommitted diff; it does not authorize an ISS-022 profile or widen
Round 283's slice.

## Decisive evidence

- The repair is exactly one alphabetically placed stable-harness path:
  `probes/portable-primitives/src/create-once.ts`. The closed source census now
  equals all 12 files physically present below `probes/portable-primitives/src`;
  no other new source is omitted and no test or candidate path was added to the
  harness arm.
- Stable manifest construction reads that exact path from `stableRoot`, binds
  its byte length and SHA-256 in the ordered `conformance-bundle-manifest/v1`,
  and the hosted plan frames that complete manifest as `harnessBundleDigest`.
  Changed, missing, moved, symlinked, reordered, duplicated, or escaping bytes
  fail closed.
- The row changes no execution, custody, or mutation surface. Stable/candidate
  roots remain disjoint, and the create-once executor retains the canonical
  non-symlink custody-root and source-overlap refusal reviewed in Round 283.
- Round 283's 32-child terminal census, one-winner/31-`EEXIST` PASS gate,
  parent readback, frozen hostile-input snapshot, candidate-verdict exclusion,
  and UNSUPPORTED-to-PASS protections are unchanged and their focused tests
  remain green.

## Verification

- `pnpm exec vitest run test/conformance/stable-bundles.test.ts test/conformance/manifest.test.ts test/conformance/iss022-handler.test.ts test/portable-primitives/filesystem-executor.test.ts` — 4 files, 51 tests passed.
- `pnpm run typecheck` — passed.
- `git diff --check` — passed.

The one-row repair is the smallest credible closure for the omitted executed
source and remains one-sitting proportional.
