# Pressure test — round 38

**Disposition:** `BLOCK_REPLAN` (removal round; three residual removals found and applied)

The thirty-eighth review verified all round-37 repairs consistent across the
packet — the armed-rotation-intent definition and equality set, the
each-fresh-run-until-selected resumability, the successor path derivation, the
`SELECTED` rotation exception, the eleven-kind/four-classification census, the
v0-fixture deletion, and the dispatch-brief cycle rows — and examined the
round-37 machinery itself for over-weight (found none: the equality set's
three clauses each close a distinct forgery, and the definition composes two
artifacts every commit run already has). It found three residual removals.

| Finding | Repair applied |
| --- | --- |
| `OP-R38-A` — `state-mutation-destination-owner-retention/v1` survived the retention deletion on the external-bootstrap side: a per-destination record whose entire payload re-attests the now-global FULL_REQUIRED constant, listed in the registry with an undomained storage slot and no producer, consumer, or acceptance criterion. | Deleted from the registry list and the destination-owner storage census. The FULL_REQUIRED rule for owner lineage remains stated in the retention section. |
| `OP-R38-B` — The FULL_REQUIRED rule inherited the five restatement slots the deleted `AUDIT_DEGRADED` matrix occupied (three in `supervisor-contract.md`, two in `compatibility.md`). | Folded to one canonical statement per document (`supervisor-contract.md` §Retention; `compatibility.md`'s semantic-validator section); other occurrences reduced to cross-references. |
| `OP-R38-C` — The round-37 repairs re-seeded full rotation-protocol restatements in three non-canonical places: the `SELECTED` resolution row, `ISS-014`'s decisions/AC6 (an issue whose ownership section disclaims executing rotation), and `EPIC-SELFHOST`'s decision registry. | Reduced to cross-references: the resolution row cites `Authority rotation`; `ISS-014` proves only its request bindings and that N+1 cannot self-rotate, deferring execution to `ISS-004`'s protocol; the epic bullet names where the decision is settled. |

A general (non-removal) repair applied in the same pass: `compatibility.md`'s
authorization registry list was missing `recovery-authorization-archive/v1`
and `recovery-authorization-attachment-archive/v1`, both declared closed
schemas by `credential-broker.md`; the two names were added.

## Examined and found not further removable

The eleven pointer kinds and eleven-slot packet; the four-value
classification; the armed-intent equality set; the successor path derivation;
the `SELECTED` exception; the destination-owner/anchor CAS chain; the
attempt-log with folded terminal fields; the dispatch-brief three-way
producer split; `ISS-039/040/041`; the reduced dependency graph; and the
negative-assertion guardrails.

## Re-review focus

- The three folds must leave exactly one normative rotation statement per
  document and no protocol content in `ISS-014`/`EPIC-SELFHOST` beyond
  request-side facts.
- The next removal round is the closing round: it must record an
  evidence-backed nothing-further-removable finding for the packet to reach
  `PASS`.
