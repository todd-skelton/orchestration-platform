# Pressure test — round 26

**Disposition:** `BLOCK_REPLAN`

The twenty-sixth independent review found three abort/order/history omissions.
The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R26-001` — A successor caller could die after removing its fence but before revocation, leaving neither an ordinary tick nor recovery launch able to finish abort. | Added durable gate `ABORTING`; staging/pending are removed while the fence remains, broker internally revokes, then shim clears fence and commits cleanup. Caller death is recoverable at every boundary. |
| `OP-R26-002` — Producer issues did not require the shared authorization-created → gate → consume → irreversible ordering and conflated pre-/post-gate abort. | Added exact read-back barriers to ISS-014/020/027 and split pre-gate client revoke from post-gate durable abort intent/internal revoke/archive-head cleanup. |
| `OP-R26-003` — Gate absence had no canonical reverse link to prove prior cleanup rather than deletion. | Added deterministic transaction archive, canonical cleanup head, active-release archive-path identity, exact linked validation, and deletion/substitution/ambiguity/second-transaction tests. |

## Re-review focus

- Kill the abort caller after every staging/pending/fence/revoke write and prove
  fence-backed or shim-only cleanup remains reachable.
- Verify producer issue evidence cannot skip or reorder create, gate, consume,
  fence/mutation barriers.
- Delete or substitute gate archives and cleanup head across abort and a second
  promotion; ordinary launch must refuse without scanning.
