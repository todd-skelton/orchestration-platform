# Pressure test — round 89 successor evidence removal review

**Disposition:** `PASS`

**Reviewed candidate head:** `8e69404b7d180f271bcbf485586cc60ccad42eec`

**Reviewed parent:** `b8e6f0df0894b355348a1dcbfe9d24536602fcdc`

This was a cold independent local removal review of the exact initially clean
head above on `iss-002-versioned-contracts`. It is bounded to removal of
`OP-R88-001` and reconfirmation of the `OP-R87-001` / `OP-R87-002` repairs. It
grants no merge, release, promotion, installation, runtime mutation, live
capability, or self-certification authority.

The review read `AGENTS.md`, `docs/planning/work-model.md`, the decision
authority in `docs/architecture/contract-decisions.md`, its incorporated
normative external-bootstrap and simplified-authority ledgers in
`docs/architecture/supervisor-contract.md`,
`docs/contracts/compatibility.md`, ISS-002, ISS-020, ISS-022, rounds 87–88,
and the exact parent-to-candidate diff. No Chase Sets repository, issue,
project, board, backlog, or consumer system was accessed. The board gate read
only this repository and its registered platform delivery project.

## Finding removal

`OP-R88-001` is removed. The candidate adds both independently discriminating
regressions required by round 88.

The first regression changes only the non-persisted trusted
`reviewExpected.candidateDigest` from the valid candidate digest to 64 `f`
characters. The core, prior selected value/proposal/tip, teardown archive,
nested independent review, intended successor authority, and every other
trusted expected value remain unchanged. The validator returns exactly:

```text
["expected.candidateDigest:mismatch"]
```

The second regression performs the legal coordinated reconstruction from
round 87. It changes only the selected retirement proposal's canonical
`proposedAt` from `2026-08-18T12:00:00.000Z` to
`2026-08-18T12:00:01.000Z`, then recomputes the proposal receipt digest,
rebuilds the selected tip's proposal linkage, rebuilds the core's prior receipt
and tip linkages, and recomputes the nested candidate digest from that rebuilt
core. It retains the original selected prior value, archive, successor
authority, and the entire original trusted `reviewExpected` object. Thus the
trusted author, reviewer, review receipt, destination, project, state root,
teardown absence, successor authority, and expected candidate are unchanged.
The validator again returns exactly:

```text
["expected.candidateDigest:mismatch"]
```

An independent in-memory deletion probe bundled the candidate's own fixture
and canonical constructors while removing only the
`expected.candidateDigest` comparison from the loaded validator. No source,
test, planning, or temporary file was written. With that equality disabled,
the valid core, independent expected-candidate mutant, and coordinated legal
rebuild all returned `[]`. Consequently both new committed assertions would
fail if the trusted equality were deleted or made non-enforcing. Evaluating
both inputs directly also avoids the first assertion's failure masking the
second inside their shared test case.

An initial read-only Vitest configuration-hook experiment did not alter the
transformed module and merely passed the existing named test. It was not used
as mutation evidence. The subsequent esbuild on-load deletion was checked for
the exact comparison before replacement and produced the discriminating
three-result evidence above.

## Retained round-87 semantic closure

`OP-R87-001` remains semantically closed. The private closed review expectation
contains a SHA-256 `candidateDigest`; the composition recomputes the candidate
from the supplied core and requires both the nested independent review and the
trusted expectation to equal it. The two new regressions now prove that this
trusted equality, rather than a successor-authority mismatch or another
boundary, is what refuses both the independent and legal coordinated
substitutions.

`OP-R87-002` remains semantically closed. The private closed post expectation
contains a SHA-256 `reviewCoreDigest`; the composition requires both the
recomputed `Dsrc` and the receipt's `reviewCoreDigest` to equal it and supplies
that trusted digest as the ordinary `ACTIVATE_SUCCESSOR` transition evidence.
The existing full core/owner/proposal/tip/post reconstruction remains
discriminating for the trusted post boundary.

The original valid review-core composition and valid downstream post-selection
composition both return `[]`. The focused and full suites retain the exact
candidate/core/post goldens, canonical paths, public dispatch, closed field
censuses, author/reviewer separation, prior RETIRED lineage, ordinal increment,
different successor installation/anchor, `SUCCESSOR_REVIEW` source,
`ACTIVATE_SUCCESSOR` transition, readbacks, and custody observation.

## Exact delta and compatibility

The exact `b8e6f0d..8e69404` delta has two paths and 237 insertions:

- `planning/pressure-tests/2026-08-18-round-88.md`: 195 insertions; and
- `test/contracts/successor.test.ts`: 42 insertions.

`git diff --check` passes. No implementation, public schema, registry row,
export, digest domain/part order, canonical path, package manifest, wrapper,
runtime state service, adapter, consumer, or unrelated contract family changed.

The candidate therefore leaves the five current successor schemas and their
acyclic candidate→core→anchor/owner→post order unchanged. Exact current-version
equality remains accepted; missing, legacy, unknown, malformed, and future
versions remain refused. No superseded symbol, migration, diagnostic,
coordinator, inventory, retention/compaction surface, v2/v3 schema, or
downstream field enters an upstream digest.

## Bounded proportionality

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Trusted expected candidate digest and equality | A caller replaces the exact reviewed prior selection/core preimage while reusing an opaque review receipt. | Removing only the equality makes both the independent expected-candidate mutant and legal proposal/tip/core/candidate rebuild return `[]`; both committed regressions fail. | Retain the private expectation member/equality and these two bounded tests; no public record, coordinator, or capability is needed. |
| Trusted expected `Dsrc` used by post and owner transition | A caller rebuilds a complete successor owner/post graph around an unreviewed core. | The existing full-chain regression becomes admissible if the trusted equality or trusted transition-evidence use is removed. | Retain the two private equalities and existing regression; no anchor implementation or new record is needed. |
| Existing five parsers, framing, and acyclic split | Open or cyclic records alter public bytes or make upstream review depend on downstream selection. | Existing schema, golden, path, exclusion, and compatibility mutants fail when their corresponding boundary is weakened. | Retain the bounded public surface unchanged. |

The candidate's 42 test-only lines are the smallest credible removal of the
confirmed evidence gap. Nothing supports additional machinery.

## Verification evidence

The required repository gates ran sequentially in the requested order:

1. `pnpm run planning:check` — PASS, `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS, `board contracts verified`, against
   only this repository and its registered platform delivery project.
3. `pnpm run format:check` — PASS, all matched files use Prettier style.
4. `pnpm run typecheck` — PASS.
5. Standalone `pnpm test`, once only — PASS, 24 files and 280 tests in 115.12
   seconds.
6. Separate `pnpm run verify:bootstrap`, once only — PASS: manifests and
   registries, formatting, typecheck, 24 files/280 tests in 114.40 seconds,
   private compositions, planning contracts, and source-tree status
   invariance.
7. Focused `pnpm --filter @orchestration-platform/contracts test` — PASS, 14
   files and 83 tests.
8. Focused `pnpm run contracts:compatibility-check` — PASS, one file and one
   test.

The first sandboxed planning invocation stalled before the repository script
emitted output because the sandbox blocked pnpm's signed-release/spawn
preflight. The reviewer-owned process was terminated, and the identical named
read-only command then ran once through normal approval and passed. All later
gates used that approved read-only path. There was no assertion failure,
repository-test retry, isolated failing-test rerun, timeout override,
implementation edit, or test edit.

Immediately before this report, the branch and both full SHAs still matched,
the source worktree was clean, and the exact candidate diff still passed
`git diff --check`. This review adds only this uncommitted round-89 record.

## Final status

`PASS` for exact candidate head
`8e69404b7d180f271bcbf485586cc60ccad42eec`. `OP-R88-001` is removed,
`OP-R87-001` and `OP-R87-002` remain semantically closed, their valid core and
post compositions remain accepted, and all required gates are green. This
review records local contract readiness only and grants no merge, release,
promotion, installation, runtime mutation, live capability, or
self-certification authority.
