# ISS-002 pressure test — round 122

## Review basis

This is a fresh independent cold removal review of exact initially clean
candidate head `aa6bc6be3bb0a22d35d7d5e4a0f431c347ea4b3a` on
`iss-002-versioned-contracts`, with exact parent
`2dbbb558acbb29eadbaa7eff687a8b9f2f427935`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
implementation authority in `docs/architecture/contract-decisions.md`, the
registered `ISS-002` draft, rounds 115 and 121 as bounded prior review context,
the credential-broker and public-compatibility documents only as attack context,
and the exact parent-to-head diff. No Chase Sets repository, project, issue,
board, backlog, source, or other consumer system was read or touched.

The exact diff changes only `docs/architecture/contract-decisions.md` and
`planning/drafts/ISS-002.md`: respectively 144 insertions/9 deletions and 53
insertions/23 deletions. It adds no implementation, test, registry, package,
runtime, roadmap, dependency, or board-topology change. `git diff --check`
passed. The checkout was clean before this required pressure record was
written.

## Disposition

`BLOCK_REPLAN` for the planning-only recovery-authorization immutable-core
ledger.

The closed record census, digest order, producer boundary, and implementation
deferrals are otherwise bounded and coherent. One SUCCESSOR member remains
implementer-selected: `recoveryFencePath` is typed only as an arbitrary
canonical relative path, while the ledger later requires an unnamed “canonical
recovery-fence path.” The existing authority surface has several distinct
canonical fence path families, so that phrase does not determine exact bytes.

No implementation is authorized from this disposition.

## Blocking finding: recovery-fence path is neither exact nor proportionate

The SUCCESSOR core includes `recoveryFencePath:relative-path`. The structural
parser authorized by this slice can therefore admit any syntactically canonical
relative path. Later prose says composition must “require the canonical
recovery-fence path,” but does not select among at least these already registered
families:

- the selected tip at `installation/activation-recovery-fence.json`;
- the transaction root at
  `installation/activation-recovery-fence-roots/<transaction>.json`;
- the transaction immutable-value family at
  `installation/activation-recovery-fences/<transaction>.json`; and
- the separately registered fence archive family.

`recoveryFenceRootDigest` is explicitly `Dfr`, which suggests the transaction
root path, while the operational recovery capability may instead intend the
selected tip path. Neither inference is authority. The transaction identifier
and every candidate path are constructible without enumeration, so exact-byte
goldens cannot resolve the choice: whichever path an implementer places in the
first SUCCESSOR fixture would become an unauthorized contract decision. The
required “path substitution” mutant is likewise non-discriminating while all
canonical relative paths parse.

The member also fails the current proportionality test. Once the intended path
is an exact function of pointer kind and `transactionId`, storing it appears to
duplicate already constructible evidence. The packet names no threat that
requires both the transaction and its derived path in the immutable core, and
does not explain why later composition cannot derive the path directly.

### Smallest bounded repair

Choose one of these two planning-only repairs in both the authority document and
the ISS-002 draft, then return the exact repaired head for a fresh independent
removal round:

1. Delete `recoveryFencePath` from the SUCCESSOR census and claimed count, and
   state that later composition constructs the exact registered fence target
   path from the pointer kind and `transactionId`; or
2. Retain it only if direct duplication has an in-scope threat, name the exact
   path template byte-for-byte, state whether the structural parser or only the
   later composed validator enforces equality, and record the deletion test that
   justifies the redundant member.

If the intended value is the root path corresponding to `Dfr`, the smallest
retention repair is to require exact equality to
`installation/activation-recovery-fence-roots/<transactionId>.json`, constructed
from the same canonical UUIDv7 member, and make the implementation/test boundary
explicit. This review does not choose that meaning on the author’s behalf.

The adjacent sentence “Admission must occur before expiry” does not create a
second blocker for this structural slice: `issuedAt < expiresAt` is the only
executable core-only comparison, and actual admission/state timing necessarily
uses deferred authenticated composition evidence. The repair should preserve
that boundary and must not invent a clock, caller Boolean, or admission timestamp
inside the structural parser.

## Attacks that passed

### Census, closure, and scalar rules

The literal census is otherwise exact and non-overlapping: fifteen common
members, three BOOTSTRAP-only members, and thirteen SUCCESSOR-only members, for
eighteen and twenty-eight total members before any repair. Common and branch
lists are internally canonical, and global canonical JSON sorting determines
their merged order. Opposite-branch members are absent rather than null;
`candidateOperationManifestDigest`, candidate verdicts, selected authority
tuples, raw capability material, and unknown members explicitly refuse.

All identities remain opaque digests. Host, user, candidate, producer,
installation, project, state-root, release, executable, manifest, grant,
destination, admission, fence, capability, and reference vocabulary introduces
no Git, GitHub, repository, code-host, provider, or operating-system credential
store term into the engine contract.

`issuedAt < expiresAt` is strict and constructible from the core alone. Every
generation is a canonical safe-decimal string bounded before conversion.
SUCCESSOR broker generations require exact adjacency; predecessor maximum
cannot be incremented and therefore refuses, while zero and the safe maximum
remain usable in the fields/adjacent pairs where they do not require overflow.
`expectedActiveGeneration` equality to actual broker read-back is correctly
deferred.

The core’s own canonical path is exact and constructible as
`installation/recovery-authorizations/<transactionId>/core.json`; it grants no
lookup or mutation authority. `Dac` has one unambiguous framed canonical-record
part under literal domain `recovery-authorization-core/v1`, with no untagged,
member-wise, raw-text, or second identity alternative.

### Cycle and trust analysis

The declared dependency order is acyclic. A SUCCESSOR fence root `Dfr` is
computed without authorization or gate facts; `Dac` then binds `Dfr`; the
cleanup-gate root `Dgr` is downstream of `Dac` and the eventual selected CREATED
authorization triple. Neither a selected authorization value nor `Dgr` enters
the core. BOOTSTRAP carries no fence member.

Structural parsing and `Dac` calculation do not certify `producerDigest`.
BOOTSTRAP composition must recompute `Dsc` from the closed reviewed
`BOOTSTRAP_INSTALL` operation/successor core, while SUCCESSOR composition must
recompute stable-promotion `Dsc` and bind actual predecessor/candidate release,
broker, admission, and fence graphs. Consistently copied fields and caller
expectations remain insufficient. The record is evidence, not a live
capability, proposal, receipt, selected tip, or promotion verdict.

Consequently candidate N0 and N+1 cannot certify themselves. Stable N remains
the producer of N+1 staging/promotion authority, external review remains
distinct, and neither a candidate verdict nor this review’s outcome can be
routed into the candidate’s own promotion.

### Scope and deferral

The slice clearly defers authorization state, native consume/removal,
post-selection receipts, attachment, archive, tombstone, selected epoch,
proposal/tip, composed equality, persistence lookup, credential-broker
operations, live capabilities, CAS, commands, and runtime mutation. It adds no
state/native/post/archive schema by implication. Those surfaces require their
own literal ledgers and independent removal rounds.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible disposition |
| --- | --- | --- | --- |
| Closed BOOTSTRAP/SUCCESSOR union | Cross-mode fields or future evidence can be smuggled into the authority preimage. | Deleting branch closure admits ambiguous or cyclic evidence. | Retain the two direct closed branches; no nested proof bag or general map is needed. |
| Strict issue/expiry and bounded generations | Invalid time windows or generation wrap can create reusable or aliased authorization evidence. | Deleting either check admits equal/reversed times, noncanonical values, or unsafe increment. | Retain scalar comparisons in the structural parser; defer actual admission/read-back timing. |
| Domain-separated `Dac` | Untagged or cross-family canonical bytes can collide semantically. | Deleting the domain permits cross-contract identity reuse. | Retain one framed canonical-record part; no second core digest is needed. |
| Constructed core path | Caller paths, enumeration, and latest-file rules can redirect evidence. | Deleting the relation makes canonical placement ambiguous. | Retain the one transaction-derived core path constructor; it grants no lookup. |
| `recoveryFencePath` member | No distinct threat is stated beyond facts already bound by kind, transaction, and `Dfr`. | Deletion appears safe if later composition constructs the exact path; retention is currently ambiguous among multiple registered paths. | Delete it, or pin one exact template plus enforcement stage and a concrete deletion mutant. Block until decided. |
| Deferred state/broker/runtime surfaces | A structural digest could otherwise be mistaken for live mutation or promotion authority. | Deleting the boundary invites caller assertions and candidate self-certification. | Retain explicit deferral; add no coordinator, proof bag, persistence reader, or runtime mutator. |

The remaining shape addresses the repository’s single-writer and reviewed-
promotion threats without restoring sparse trees, inventories, coordinators,
epoch-split runs, arbitrary-precision ordinals, diagnostics, retention, or
compaction.

## Draft, board, and dependency assessment

The ISS-002 draft mirrors the new census, timing/generation rules, digest order,
trust boundary, test obligations, and deferred implementation surface, including
the same unresolved recovery-fence path member. No registered dependency edge
changed, so the transitively reduced roadmap remains intact. The board checker
passed against this repository’s own registered delivery project; no consumer
board was consulted.

## Verification evidence

All required gate meanings were executed strictly sequentially. The standalone
full suite terminated before the separate bootstrap verifier began. The
package-manager version switch itself failed closed because the registry
signature endpoints were unreachable. Following the bounded infrastructure
rule, each remaining gate used only its exact checked-in wrapper or lockfile-
installed binary; sandbox `spawn EPERM` failures were rerun unchanged with the
required process authority.

1. `pnpm run planning:check` — the pnpm registry-signature switch failed closed;
   the exact checked-in `node scripts/planning/check.mjs` passed with `planning
   contracts verified`.
2. `pnpm run planning:board-check` — the exact checked-in board checker first
   encountered restricted `spawn EPERM`; its approved unchanged rerun passed
   with `board contracts verified`.
3. `pnpm run format:check` — the exact checked-in format wrapper first
   encountered restricted `spawn EPERM`; its approved unchanged rerun passed:
   all matched files use Prettier style.
4. `pnpm run typecheck` — the lockfile-installed TypeScript compiler passed
   `--noEmit -p tsconfig.json`.
5. Standalone `pnpm test` — the lockfile-installed Vitest startup first
   encountered restricted `spawn EPERM`; its approved unchanged rerun passed 29
   files and 362 tests in 88.22 seconds. The process fully exited before step 6.
6. Separate `pnpm run verify:bootstrap` — the exact checked-in verifier first
   encountered restricted `spawn EPERM`; its approved unchanged rerun passed
   manifests/registries, formatting, typecheck, 29 files and 362 tests in 79.79
   seconds, private compositions, planning contracts, and source-tree status
   invariance.

## Authorization boundary

`BLOCK_REPLAN` applies to exact candidate head
`aa6bc6be3bb0a22d35d7d5e4a0f431c347ea4b3a`. Repair only the planning packet,
then obtain a fresh independent exact-head removal review. Do not implement the
core from this disposition.

This record grants no merge, release, promotion, installation, parser, digest,
path, persistence, broker, capability, CAS, runtime mutation, or self-
certification authority. A worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-122.md`; no implementation, test,
draft, issue, roadmap, board, or other planning file was edited, and no commit
was created.
