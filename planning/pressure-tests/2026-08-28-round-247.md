# ISS-022 stable-harness-writer pressure review — round 247
`PASS`.
The recovered ledger is complete: 508 lines, 30 balanced fences, three expected subsections, the full physical/vector/decision chain, and zero truncation or tool-output artifacts; its +17 lines and the draft's +8 lines match the writer-only repair over the Round 246 packet.
`DstableHarnessSubject` exactly binds the authenticated harness, test, registry, and contract digests, is equality-bound into the core/writer, and removes every installed-release/N0 prerequisite.
The landed terminal already requires a clean checkout at exact `workflowRevision` and recomputes those stable inputs; `run-bundled.mts` and `hosted-terminal.mts` are harness-manifest members, so the two closed submodes are executable within the predicted command-arm extension.
Both modes have four bounded payload arguments, preserve the administrator-authenticated provider joins, write only the exact external-root census, and never execute or accept writer inputs from the candidate checkout.
The core-only reviewed PR and second two-file PR remain acyclic, candidate/provider/core equal-bound, distinct-actor reviewed, and protected-main published; malformed, moved, stale, extra, or self-reviewed evidence refuses.
Proportionality: one content-addressed stable-harness subject plus two terminal submodes is the smallest available writer repair and adds no release bootstrap, helper, job, permission, isolation, reboot, binary, or production authority.
