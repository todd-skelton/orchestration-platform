# SDLC feedback replan — round 373

Operator replan measuring the delivery program against the SDLC it must serve
(discovery, planning, prioritizing, implementation, test, review, release plus
feedback). Findings: the retained corpus holds 166 round records against 46
registered issues while 14 of 19 packages remain placeholders, so verification
and process prose outrun runtime evidence; recurring finding classes are
re-litigated in prose per slice; release receipts have no discovery consumer,
leaving intake permanently operator-bound; and full-standard drafting is spent
before scheduling. The executable-cycle path (`ISS-041` into the kernel
slices) is the critical path.

Actions taken in this packet: register `ISS-046` (finding-class census and
codification, M2, `EPIC-RUNTIME`, blocked by `ISS-000`) and `ISS-047`
(telemetry-driven discovery intake, M3, `EPIC-MODULES`, blocked by `ISS-010`
and `ISS-011`); amend the work model with four Round-373 bounds
(runtime-evidence bound on process output, codify-once per finding class,
registration precedes elaboration, feedback as a named phase); raise `ISS-041`
to `priority:p0` under the contention rule; register `finding class` and
`discovery intake` in the glossary; add both root placeholders through the
approved shared-scaffold amendment.

Proportionality check. `ISS-046`: in-scope threat is unbounded recurring prose
review cost (measured ~8 rounds per issue); deletion test keeps prose-only
review and the recurrence continues at the measured rate; smallest credible
alternative — a manual checklist in review seeds — still spends prose per
round and drifts; the selected shape extends the existing checker family with
no new service. `ISS-047`: threat is a delivery loop whose release evidence
feeds nothing; deletion test leaves self-host successor selection on prose
intake indefinitely; smallest credible alternative — the operator reads the
journal manually — is retained as the interim state, and machinery stays
behind the `ISS-010`/`ISS-011` edges, advisory-only, with a prediction and an
`ISS-015` review-by horizon. Work-model bounds: the existing proportionality
paragraph governs mechanisms, not sequencing, drafting depth, or feedback, so
a smaller alternative does not exist; each bound is one sentence of policy
with no new machinery. Explicitly not added: no priority scoring model, no
meta-freeze checker, no process dashboard, no new epic; deterministic
enforcement of the new bounds waits for a measured recurrence, consistent
with earning determinism from evidence.

This packet is planning registration only: no runtime code, no tests run
beyond `planning:check`, `planning:board-check`, and format verification, and
no authority change. The registered board mirrors the drafts in the same
reviewed change.
