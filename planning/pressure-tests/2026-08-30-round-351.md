# ISS-013 native metadata loading repair — round 351

Disposition: `PASS` at `49e75f917a4947469fd8ad5103e28ce951528c56`,
base `1cd9f44b3ef3ad0db7953d8eb44f13046bdc9753`.
Independent reviewer: `/root/iss013_sdk_review`, Sol high, row 11.

This additional bounded round answers a new concrete bootstrap blocker after
Round 350: native Node imports the static CLI registry before building, but
the SDK root's eager runtime exports referenced unavailable JavaScript paths.
The separate bootstrap failed with ERR_MODULE_NOT_FOUND despite 966 passing
Vitest tests. Host execution also proved the new native registry regression
failed on that unrepaired integration.

The root now exposes registration metadata and types only; actual SDK tests
import the fixed runtime sources. No current runtime consumer loses a required
root export. The new native Node regression clears NODE_OPTIONS and checks
the real registry. All 34 focused SDK, native-import, and contract tests pass.
The PR records subsequent full/bootstrap and hosted verification.

Proportionality: deleting this change restores the observed bootstrap failure.
Removing unused eager exports is smaller than adding a loader, build
prerequisite, runtime facade, or package subpath. No verifier was weakened and
no SDK observation behavior or authority changed.
