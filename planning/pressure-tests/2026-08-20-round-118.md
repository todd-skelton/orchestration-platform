# ISS-002 pressure test — round 118

## Review basis

This is a fresh independent cold removal review of exact initially clean head
`8059a0593dfa26425eef143d9a2344e0a2283e89` on
`iss-002-versioned-contracts`, with exact parent
`0a8ada238abc8b84fd3b145db4db12416c6180d4`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was only `AGENTS.md`, `docs/planning/work-model.md`,
`docs/architecture/contract-decisions.md`, the registered `ISS-002` draft,
the relevant supervisor/common pointer contracts, rounds 116–117 as the
findings under retest, and the exact parent-to-head diff. The exact diff changes
only `docs/architecture/contract-decisions.md` and
`planning/drafts/ISS-002.md`: 13 insertions and 7 deletions. No Chase Sets
repository, project, issue, board, backlog, consumer source, or other consumer
system was read or touched. The checkout was clean before this required
pressure record was written.

## Disposition

`PASS` for the bounded structural cleanup-gate/recovery-fence implementation
slice, and no more.

Exact head `8059a0593dfa26425eef143d9a2344e0a2283e89` authorizes only the four
closed root/head parsers, the two domain-separated root digests, the two exact
VALUE-position parsers/digests, common `Dv` computation over explicitly
supplied head records, and bounded relative history validation over the
supplied parsed root plus a supplied dense valid prefix. It does not authorize
suffix completeness/currentness, authenticated persistence discovery,
composed upstream equality, first-ever archive lineage, tombstones, archives,
filesystem mutation, broker calls, capabilities, release selection,
promotion, or a runtime state transition.

Round 117's sole residual blocker is removed. The repaired decision now says
that a valid shorter prefix is a valid earlier history state, denies that the
pure structural validator can claim a current complete suffix, and defers tail
completeness/currentness to authenticated common-CAS selected evidence. The
ISS-002 acceptance text mirrors that boundary and no longer requires an
unqualified gate/fence truncation mutant.

## Round 117 completeness blocker retest

The validator's entire input is the parsed immutable root and an explicitly
supplied dense array of parsed heads. From those values it can recompute `Dgr`
or `Dfr`, each supplied common head `Dv`, and the relationships among supplied
records. It has no authenticated selected-current head, mutation locator, or
persistence source and therefore cannot distinguish a genuine earlier valid
prefix from the same prefix copied out of a later history.

The repaired rules now make precisely that epistemic boundary executable:

- cleanup accepts a valid supplied prefix of one through six records, and the
  fence accepts a valid supplied prefix of one or two records;
- ordinal zero must be present at the start with null prior `Dv`, so omission
  of the start is detectable;
- every later supplied record must increment the ordinal by one, bind the
  immediately prior supplied record's recomputed common `Dv`, keep the same
  recomputed root digest, follow a permitted non-self edge, and have monotone
  inclusive time, so an unmodified interior omission, gap, fork, or reorder is
  detectable within the supplied sequence;
- an extension beyond the graph bound, wrong root, wrong prior `Dv`, unsafe
  ordinal, illegal edge, or attempted write self-loop refuses; and
- absence of a later valid suffix is expressly not detectable or rejected.

The draft's compatibility claim now says
`fork/gap/reorder/start-or-interior omission` and immediately states that a
valid shorter prefix remains a valid earlier state. Tests therefore have a
discriminating oracle: mutate relationships inside the supplied prefix and
expect refusal; remove only a valid tail and continue to accept the resulting
earlier prefix. No unauthenticated expected-head digest, caller Boolean,
directory scan, latest file, alternate tip, or second head was added.

The remaining unqualified `truncation` requirements in the authority documents
apply to the distinct `authority-history/v1` full-chain walk, whose input is
cross-bound to an authenticated selected head ordinal and digest. They do not
apply to this cleanup-gate/recovery-fence supplied-prefix validator and do not
restore its deleted currentness claim.

## Round 116 blocker retest

### 1. Durable lookup, archive lineage, and deletion/loss

This block remains removed. The decision still states that common immutable
values and proposals are mutation-ID-addressed while
`pointer-current-tip/v1` exposes neither that mutation ID nor the transaction
needed by the root path. It forbids claiming that selected `Dp/Dv/Dr` fetches
the root, head, proposal, or full history. The registered gate/fence strings
remain future paths, not a lookup protocol.

The BOOTSTRAP cleanup-archive triple remains structural nullability only.
All-null does not prove first-ever selection, all-non-null does not select an
archive, and bare absence or copied caller expectations grant no authority.
Authenticated discovery, selected archive/tombstone versus missing/corrupt
FULL_REQUIRED handling, first-ever lineage, and deletion/loss remain deferred
to later independently reviewed common-CAS locator and cleanup-archive
decisions. The tail-currentness repair adds no persistence or archive claim.

### 2. Composed validation and upstream proof selection

This block also remains removed. The four parsers accept closed typed bytes for
structural identity only. They grant no equality to authorization,
active-release, cleanup-archive, fence, broker admission/generation, `Dsc`, or
create-once upstream time. A coordinated copied digest remains explicitly
insufficient.

Every composed root/gate/fence validator is still withheld until the
recovery-authorization and cleanup-archive ledgers, a closed non-persisted
composition input census, and exhaustive required/forbidden proof matrices for
all ten gate cells, twelve gate edges, and two fence cells independently pass.
Future proof time comparisons remain explicitly inclusive, including selected
PREPARED fence `recordedAt <=` PUBLISHED gate-head `recordedAt`. The exact diff
does not alter any of these deferrals.

## Retained structural surface

The retained contract remains coherent and acyclic:

- The four flat v1 censuses fix exact canonical members, schema literals,
  scalar grammars, enums, and BOOTSTRAP/SUCCESSOR structural nullability. The
  successor fence alone checks the internal safe-decimal generation increment.
- `Dgr` and `Dfr` remain distinct one-canonical-record framed digests. Roots
  exclude their heads, selection graphs, proof bags, and downstream archives.
- Heads have no competing standalone identity. Common
  `Dv = pointer-value/v1(kind, Dp, canonical supplied head bytes)` is their sole
  identity, and positive heads bind the immediately prior supplied common
  `Dv`.
- Each VALUE position is exactly
  `{mode:"VALUE",parts:{ordinal,rootDigest}}` under the already registered
  family domain. It adds neither a head digest nor mutation identity;
  tombstone position remains deferred.
- The gate graph still has exactly ten admitted cells and twelve non-self
  edges, with a longest legal path of six records. The fence graph still has
  only `PREPARED -> POST_ACTIVATION`, so its valid supplied histories contain
  one or two records. Replay remains `NO_APPEND`, never a write self-loop.
- Structural success authenticates no persistence source, upstream proof,
  selected current head, or capability.

No digest cycle or self-certification has been reintroduced. A candidate or a
caller that consistently substitutes every expectation can at most supply
another structurally valid byte graph; this slice neither authenticates it nor
uses it to authorize a mutation.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and disposition |
| --- | --- | --- | --- |
| Four closed flat parsers | Open or inferred records let implementations choose members, nullability, enums, or canonical bytes. | Deleting a census or scalar/nullability check admits malformed or cross-family bytes. | Four flat records are smaller than nested proof bags. Retain. |
| Separate `Dgr`/`Dfr` | An untagged root permits cross-family substitution or implementation-selected identity. | Deleting or swapping a domain lets the wrong family root retain an accepted digest. | One canonical-record part under each of two domains is minimal. Retain. |
| Common supplied-head `Dv` | A second head digest creates divergent identity and lineage rules. | Replacing common `Dv` with an untagged canonical digest loses kind/`Dp` binding. | Reuse the existing pointer value identity. Retain. |
| Exact VALUE positions | Open `parts` or a duplicate head digest permits caller-selected position identity. | Removing member closure or the family domain admits an extra, missing, or cross-family position. | Ordinal plus root digest is the smallest exact position. Retain. |
| Bounded relative prefix walks | A supplied start/interior omission, gap, fork, reorder, illegal edge, wrong root/prior `Dv`, ordinal, or time can fake structural progress. | Deleting a relationship check admits a malformed supplied prefix; deleting the bound admits an unbounded scan. | Dense arrays of at most six and two are smaller than a tree, membership proof, inventory, or checkpoint. Retain. |
| Explicit suffix-currentness deferral | Currentness requires authenticated selected evidence not present in this slice. | Deleting the deferral revives an unimplementable tail-truncation claim; adding a caller expectation invents authority. | Accept valid earlier prefixes and defer selected-tail proof. Retain. |
| Deferred locator/archive/composition | Durable discovery, first-ever/loss classification, and coordinated upstream substitution need undecided authenticated inputs. | Implementing them now requires caller trust or invented schemas. | Preserve the existing later decisions; add no tip, index, scan, proof tree, or proof bundle. Retain deferral. |

The resulting implementation footprint is exact and proportionate: existing
pointer/definition contract modules, public export/compatibility registries,
and focused tests for the retained structural surface only. It adds no package,
command, pointer kind, coordinator, migration, runtime mutator, archive inside
a head, broker receipt, capability, or consumer field.

## Verification evidence

All required gates were run strictly sequentially. The standalone test process
fully exited before the separate bootstrap verifier started. Only identical
direct underlying commands were rerun for signed-registry availability or
sandbox subprocess failures.

1. `pnpm run planning:check` — the package-manager wrapper failed closed
   because it could not verify the `pnpm@11.22.0` registry signature while the
   registry was unreachable; identical `node scripts/planning/check.mjs`
   passed with `planning contracts verified`.
2. `pnpm run planning:board-check` — the wrapper failed at the same registry
   check; direct execution hit sandbox `spawn EPERM`, and the approved
   identical `node scripts/planning/board-check.mjs` rerun passed with
   `board contracts verified`.
3. `pnpm run format:check` — the wrapper failed at the same registry check;
   direct execution hit sandbox `spawn EPERM`, and the approved identical
   `node scripts/format.mjs check` rerun passed: all matched files use Prettier
   style.
4. `pnpm run typecheck` — PASS (`tsc --noEmit -p tsconfig.json`).
5. Standalone `pnpm test` — the wrapper failed at the registry check; direct
   Vitest hit sandbox `spawn EPERM`, and the approved identical
   `node node_modules/vitest/vitest.mjs run` rerun passed: 28 files and 349
   tests in 79.11 seconds. The process fully exited before step 6 started.
6. Separate `pnpm run verify:bootstrap` — the wrapper failed at the registry
   check; direct verification hit sandbox `spawn EPERM`, and the approved
   identical `node scripts/verify/bootstrap.mjs` rerun passed: manifests and
   registries, formatting, typecheck, 28 files and 349 tests in 84.21 seconds,
   private compositions, planning contracts, and source-tree status
   invariance.

The green regression suites validate current repository and planning
compatibility. They do not widen the structural validator into persistence,
composition, or suffix authority.

## Implementation authorization boundary

This `PASS` is the independent exact-head removal gate authorizing
implementation of only the bounded structural subset named above. The
implementation must return through its own ordinary verification and
independent exact-head review; this planning candidate does not certify its
future implementation.

This record grants no merge, release, promotion, installation, persistence
lookup, selected-tail completeness/currentness proof, composed validation,
first-ever archive fact, filesystem mutation, tombstone, archive, broker call,
live capability, runtime transition, or self-certification authority. A worker
or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-20-round-118.md`; no source, test, draft,
issue, roadmap, board, or other planning file was edited, and no commit was
created.
