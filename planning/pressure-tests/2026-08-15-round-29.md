# Pressure test — round 29

**Disposition:** `BLOCK_REPLAN`

The twenty-ninth independent review found one publication atomicity gap. The
39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R29-001` — Fence create/read-back followed by gate `PUBLISHED` CAS left an unrepresented crash window where an exact fence existed while the gate still said `NOT_PUBLISHED`. | Added durable `PUBLISHING` before fence creation. Pending recovery resumes absent/exact-present fence variants to `PUBLISHED`; abort at `NOT_PUBLISHED` creates no fence, while abort after `PUBLISHING` idempotently completes and retains the exact fence through revocation. Added pre-fence `TERMINAL_ABORTED` archive/pointer cleanup when abort wins. |

## Re-review focus

- Crash before/after `PUBLISHING`, fence create/read-back, and `PUBLISHED`; the
  same source-qualified pre-fence chain must resume without a second fence.
- Race abort against both publication CAS operations with fence absent and exact
  fence present; wrong/ambiguous fence state must refuse.
- Prove an aborted pre-fence child receives no client, reaches
  `TERMINAL_ABORTED`, archives/exits, and cannot block final gate cleanup.
