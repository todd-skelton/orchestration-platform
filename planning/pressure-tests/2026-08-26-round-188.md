# ISS-006 Windows profile-custody test-timeout review — round 188

## Disposition

`PASS_TO_LAND`.

This is a fresh independent exact-delta landing review after Round 187. The
only post-review change is one test-local Vitest timeout on the already reviewed
compiled Windows broker matrix. The timeout prevents the repository's ordinary
`pnpm run test` invocation from applying its 30-second default to a matrix that
completed in approximately 30.1–32.6 seconds on the current Windows host.

The change is proportional and does not alter production, fixture behavior,
the tracked PE, real diagnostic, authority semantics, or package surface. It
does not make a failing or hanging matrix pass: the same assertions and child
processes must complete, and the test still has a finite 600-second terminal
bound. No global Vitest configuration or unrelated test receives the wider
bound.

## Exact reviewed state

```text
HEAD 7e06766b389c24f2d878480f996633bb2525d8ce

578e02184cfb0a3d16d8a1f48633be18df34bdc5673aaf28c8ce366b81f0e8ac  packages/conformance/src/windows-isolation-broker.c
0b65284268d576df4acc6b15f440586cd245c42d8ec2f242a2dcb01d1b2ee965  packages/conformance/src/windows-isolation-broker-x64.exe
2f2a179d89de24641b5ea69d5f5567f5bf8b0c0e7ec5362d4a5fc6ed2dfe9901  scripts/build/windows-isolation-broker.mjs
6d76790c5841162776770542ca62bb00bef76b0958241737f653608071ed410d  test/conformance/windows-isolation-broker-fixture.c
30ef3c90ac70a10b09c81c13a381b02ce169b79bd391b41ae4376eb2f08e7302  test/conformance/windows-appcontainer-profile.test.ts
35f60b38ddf8aea68ec691e48e446123efb58e8318ded0721a132ce0b8edfc4b  test/conformance/windows-isolation-broker.test.ts
```

All files except `windows-isolation-broker.test.ts` are byte-identical to the
Round-187 landing review. Inspection finds exactly one `600_000` or other test-
timeout literal in that file. It is the third argument to only the Windows-only
test named `executes raw target census cardinality and unrelated duplicate
mutants`; it is not a CLI option, shared helper, suite setting, or production
constant.

## Removal review

The smaller alternatives do not preserve the reviewed evidence as well:

- deleting matrix arms would weaken the Round-184 through Round-187 deletion
  evidence;
- splitting or parallelizing the native fixture could introduce additional
  build/state-root concurrency and cleanup behavior merely to avoid a framework
  default; and
- widening global test configuration would affect unrelated tests.

One explicit finite timeout on the single serial compile-and-execute matrix is
the smallest safe repair. Its authority-bearing assertions, durable state
cleanup, after-each root removal, and residue checks are unchanged.

## Checks actually run

```text
pnpm exec vitest run test/conformance/windows-isolation-broker.test.ts test/conformance/windows-appcontainer-profile.test.ts
  PASS — 1 file passed, 1 opt-in file skipped; 10 passed, 1 skipped
  Duration 30.42 s; tests 30.01 s; no CLI timeout override

hash and literal census
  PASS — all non-test hashes equal Round 187; the changed test hash is exact;
         one test-local 600_000 occurrence only
```

The author-reported full verification prefix failed only because the prior
30-second default expired at 30.097 seconds after 630 other tests passed. The
new ordinary focused run crosses that same default boundary and completes under
the local bound. No implementation failure or residue is being waived.

```text
node scripts/planning/check.mjs
  PASS — planning contracts verified

pnpm exec prettier --check planning/pressure-tests/2026-08-26-round-188.md
  PASS — all matched files use Prettier code style

git diff --check
  PASS — no errors; only the existing Windows LF-to-CRLF notices
```

## Landing and unchanged authority status

Round 187's `PASS_TO_LAND` remains valid for the exact private native Windows
profile-custody slice with this test-only timeout. This record does not amend
Round 175, the ISS-006 draft, ledger, or board. It authorizes no candidate
launch, AppContainer execution DACL, Job Object, hosted Windows PASS, receipt,
aggregate, provider result, certification, promotion, public export, or Chase
Sets access. ISS-006 remains incomplete, and a candidate never certifies
itself.
