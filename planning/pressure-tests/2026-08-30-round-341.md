# ISS-003 command token repair — round 341

Disposition: `PASS` at `33dd3bd4c8ec49d0d571aa3f8e2839d523e5163d`,
base `f181721d4bd6c86a4c84785c8f7ad47b9920c4e0`.
Independent reviewer: `/root/iss003_dispatcher_review`, Sol high, row 11.

The two-file repair compares token count and each token before assigning
command identity or loading configuration. Both fused-token regressions
require the empty-command argv refusal and zero loader and handler calls.
Configuration-before-placeholder ordering, owner/census checks, schema
enforcement, and the handler-free project boundary remain intact.

Host execution on the source integrated with main passed all 59 CLI tests,
including the two previously failing controls. Full and hosted verification
are recorded with the implementation PR; this review does not certify live
wiring or three-OS acceptance.

Proportionality: deleting the exact-token comparison restores the reproduced
defect. It is the smallest adequate repair and adds no new mechanism.
