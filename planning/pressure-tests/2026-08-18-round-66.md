# Pressure test — round 66 author sibling-equality repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `8e79d012fd208aeb6ad3d7111509f83ae0a54928`

This bounded author repair answers only round 65's `BLOCK_REPLAN`. It grants no
implementation, merge, release, promotion, installation, runtime mutation, or
self-certification authority. A fresh independent exact-head `PASS` is required
before implementation resumes.

## Bounded repair

- ORDINARY SELECTED now requires the target-slot proposal mutation to equal the
  common target mutation and its recomputed selected tip to equal
  `ordinaryResolution.selectedTargetTipDigest`.
- ORDINARY LOST_CONFLICT now requires the conflict composition's losing
  proposal mutation to equal the common target mutation and its recomputed
  conflict-receipt digest to equal
  `ordinaryResolution.conflictReceiptDigest`.
- SELECTED's target proposal and LOST_CONFLICT's losing proposal plus conflict
  receipt each carry named producer `Dt/Dv/Dr`; every named component must equal
  the corresponding old/KNOWN packet and resolution producer field. Generic
  proposal and conflict-receipt schemas do not contain producer `Dp`, so no
  duplicate field is invented: `Dp` remains equal-bound by the resolution and
  common old/KNOWN packet fields.

## Proportionality and trust

The repair adds only equality requirements and executable mutants over existing
members. It adds no field, record, digest part, path, receipt, pointer kind,
state transition, tree, proof, inventory, coordinator, checkpoint, compaction,
capability, or verdict route. Structural success still grants no authority;
the live ISS-004 context, selected authority, custody, kernel lock, and stable-
predecessor promotion trust split remain mandatory.

## Required independent attack

Review the exact committed repair head cold. Pair a valid SELECTED resolution
with a different valid selected target graph; pair a valid LOST_CONFLICT
resolution with another valid conflict composition; change target/loser
mutation; independently and coordinately substitute proposal and conflict
authority-epoch tip/value/receipt; swap semantic `Dt/Dv/Dr` order; attempt to
require or smuggle a duplicate producer `Dp`; and verify every mismatch refuses
before `Dcommit` or packet capability use. Recheck the unchanged run-intent,
history binding, trust, vocabulary, proportionality, planning, and board
surfaces. Older PASS dispositions do not apply.
