# ISS-022 runtime normalizer review — round 280

`PASS` for the exact two-file worktree diff.

The normalizer snapshots the two-row census recursively through data descriptors, rejects proxies, accessors, sparse or extended arrays, non-plain records, unsafe numbers, extra fields, and values outside the closed signal/request enums. The retained raw snapshots are recursively frozen and cannot be changed after reduction.

The process row recomputes the one-message nonce-bound response and exact `exit` then `close` terminal tuple. It requires direct-handle ownership, accepted `SIGTERM`, matching null-code/`SIGTERM` events, no forced kill, timeout, or overflow, and never admits the grandchild self-report as authority. That one coherent observation deterministically remains the measured `UNSUPPORTED` gap; any contradiction is `UNKNOWN`, and malformed input is refused.

Handle `PASS` requires exactly one direct callback, the 32-byte nonce, and every clone, worker-send, serialization, wrapped-function, cross-process replay, and post-release rejection. Mutants remove each confinement fact and attack census, descriptors, scalar types, response count, event ordering, observed flags, signals, and grandchild authority. Focused runtime/handler tests pass 24/24 and root typecheck passes.

Proportionality: this is a bounded reducer and test slice over already-landed raw observations. Deleting either reducer leaves its registered vector without stable-parent normalization; a registry, receipt, writer, decision, helper, or isolation mechanism would be larger and is absent from the diff. Exact-head full-suite and bootstrap gates remain a separate landing condition.
