# Pressure test — round 25

**Disposition:** `BLOCK_REPLAN`

The twenty-fifth independent review found three cleanup/provenance omissions.
The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R25-001` — A null-launch promotion could clear its fence with no durable blocker while authorization revocation was pending. | Added canonical `activation-cleanup-gate/v1` for N0 and every promotion; a present gate blocks all ordinary ticks until mode-specific revocation, archive, and removal. |
| `OP-R25-002` — N0 pre-mutation cancellation had no revocation owner or transition. | The existing narrowed recovery-admin revoke method now handles only pre-bound abort after authoritative destination absence for unconsumed/consumed state; post-mutation client revoke refuses and terminal cleanup stays broker-internal. |
| `OP-R25-003` — The closed protection schema omitted its producer's pre-run anchor-variable provenance. | Added protected-environment identity and exact anchor variable name/value/API ETag/update time to ISS-002's receipt schema and mutation fixtures; post-upload artifact identity stays external. |

## Re-review focus

- Crash an uninterrupted null-launch promotion after fence clear and prove N1
  cannot start before revocation and cleanup-gate removal.
- Abort N0 on both sides of consumption but before first mutation, then attempt
  revoke after mutation.
- Reorder or substitute the protected anchor variable and producer run while
  preserving post-upload artifact values.
