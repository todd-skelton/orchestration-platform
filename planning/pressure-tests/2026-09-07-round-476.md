# Pressure round 476 - allowlist both native child environments

Source 1 and review 2 passed the now-superseded five-name ISS-073 candidate.
Source reservations 3, 5 and 7 stopped on controller preflight defects without
source edits and count as actual attempts 3, 4 and 5. Reviewer reservations 4,
6 and 8 were skipped, not launched. Corrective source 6 preserves exact base
`d163642b150754710d9fea89fe8d263f8cba6f5b` under the same ordinary eight-attempt
ceiling; there is no attempt reset or larger cap.

Source 6 and independent review 7 passed exact candidate `0fccf642`. All four
controller gates passed and draft PR #331 was opened, but hosted bootstrap
34160934665 failed the two new subprocess assertions on Windows and macOS;
Ubuntu passed. Read-only failure projections identified fixture timeout and
child assertion failure without emitting raw logs. This is failed acceptance,
not a reason to waive either host or the environment boundary.

Upstream Node/libuv source shows four additional Windows startup variables are
supplied implicitly: `LOGONSERVER`, `SYSTEMDRIVE`, `USERDOMAIN`, and `USERNAME`.
They now join the explicit Windows-only list. Node also automatically propagates
ambient `NODE_V8_COVERAGE`; an own undefined non-enumerable property suppresses
that propagation without passing the variable or changing the parent. A frozen
empty-prototype result refuses additional JavaScript runtime mutation. Synthetic
coverage directories stay outside the checkout. CoreFoundation's initialization
can generate `__CF_USER_TEXT_ENCODING` after exec; the fixtures distinguish only
that exact Darwin name, with fixed boolean reports required from the next hosted
run. This hypothesis is not treated as completed hosted evidence.

This bounded controller repair retains all seven native attempt identities and
uses no further author worker. Independent exact-head review 8 is the final
reserved native attempt under the same eight-attempt ceiling. The corrected head
must rerun all four gates and actual hosted Windows/macOS/Linux tests before
acceptance. No earlier PASS certifies the repaired head.

Primary implementation references:
- [Node child-process environment normalization](https://github.com/nodejs/node/blob/v24.x/lib/child_process.js)
- [libuv Windows environment construction](https://github.com/nodejs/node/blob/v24.x/deps/uv/src/win/process.c)
- [CoreFoundation initialization](https://github.com/apple-oss-distributions/CF/blob/main/CFRuntime.c)

## Actual observed need and source plan

Five-name exclusion does not prevent unknown credentials. Build from empty at
both production hops and copy only the documented OS/process startup and native
provider-auth location allowlist. POSIX uses exact uppercase spellings; Windows
matches case-insensitively and emits one canonical spelling, preferring an exact
entry. Unknown names are omitted without logging. Provider-auth filesystem and
keychain locations remain usable; no isolation of those stores is claimed.

Credential material reached retained logs in the observed run. Validity, scopes
and unauthorized operations remain unverified, and invalidation was not
performed. Do not claim controller containment, unrelated-value preservation or
proof from the superseded candidate. Brokers, scheduler, public contracts,
arbitrary overrides, controller environment changes, network checks, sandbox
changes, publication, promotion and activation remain out of scope.

## Source evidence and independent attack

Hosted synthetic tests must independently fail a regression at either explicit
spawn, verify actual observer/worker child environments, exact required-location
values, known delivery/cloud names, arbitrary secrets and aliases, POSIX/Windows
case rules, unchanged parents and no values in output. The production controller
spawn helper is exercised rather than replaced by a fake implementation.
Direct-Node imports remain covered.

Attempt 6 may use only installed static TypeScript, pinned Prettier and planning
entrypoints. It does not run local tests, native builds, addon/probe/lock
experiments or network checks. Review 7 and four gates passed only the pre-CI
candidate. Review 8, four new exact-head gates and actual hosted three-OS execution
remain required for the controller correction; none is claimed here.

Falsifiable prediction: both spawn environments contain only canonical allowlist
values, without coverage propagation, and their parents remain unchanged. Actual
children preserve required locations and admit only the documented Darwin
runtime-generated metadata name beyond the list. Unknown parent propagation,
any other child name, absent explicit `env`, location change or platform case
mismatch falsifies the repair and requires evidence-led replanning.
