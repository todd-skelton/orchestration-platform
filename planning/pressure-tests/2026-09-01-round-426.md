# Pressure round 426 — hosted native-lock path registration

Status: planning-only registration; implementation, independent source review,
hosted execution, and evidence review pending. No native build, probe, selection,
release, or authority claim.

## Measured blocker and proportionality

The authenticated ISS-022 result is `BLOCK_REPLAN` with null OS-profile and
selectable lock/custody slots. PR #192 landed the reviewed source inputs for the
already selected bounded native-lock experiment, but no registered work item
schedules their source compilation on the required hosted macOS, Windows, and
Linux runners. The owner audit therefore selected Option A: register the hosted
native-compilation path before admitting the final two parked source-repair
attempts.

One p0 probe issue between ISS-006 and ISS-022 is the smallest honest change.
It reuses the complete landed ledger and harness contracts rather than adding a
second experiment, mechanism, or authority path. Replacing the direct
`ISS-006 → ISS-022` edge with `ISS-006 → ISS-048 → ISS-022` preserves the
transitive reduction and makes the measured prerequisite executable.

## Scope and non-goals

The future slice adds only the literal experiment dispatch, source-only hosted
build path, exact four cases, twelve controls, stable aggregate/record/verifier,
and retained build/provider census already frozen by the ISS-022 ledger. This
packet changes only roadmap and issue prose.

There is no local compiler discovery, installation, native build, probe
dispatch, binary, production selection, existing receipt mutation, unparked
source repair, credential, privilege, spend, administration, release, or
promotion. Existing capability/profile slots remain null.

## Acceptance evidence and review attack surface

The implementation must prove exact protected-hosted topology and revisions,
official Node input and compiler argv binding, stable/candidate separation,
same-attempt three-OS build/artifact/provider census, all four cases and twelve
controls, and one immediate post-death acquisition attempt. Diagnostic
collection can report `PASS|FAIL|UNSUPPORTED|UNKNOWN` but cannot write or imply
capability, profile, production, release, or promotion authority.

Review should attack dependency redundancy or cycles, a broader-than-ledger
build surface, local/self-hosted execution, candidate build or verdict code,
unstable input provenance, shell/environment injection, missing controls,
retry-until-green semantics, partial-OS inference, mixed attempts, artifact or
retention substitution, and any diagnostic value gaining authority.

## Verification and prediction

`pnpm run planning:check` and live `pnpm run planning:board-check` must pass on
the registered issue and exact affected issue bodies. Independent exact-head
review and three-OS hosted CI are required before merge. The next slice may
implement only this registered path; its first complete protected-main run is
expected to distinguish a usable bounded native lock from a remaining named
premise without weakening the prior `BLOCK_REPLAN` result.
