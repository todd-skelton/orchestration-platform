# Pressure test — round 71 author run-lineage repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `9f0f18b8d677c9d230d88a0be85c37f2ba734961`

This bounded author repair answers only round 70's deterministic target/run
identity finding. The user selected closed prior evidence rather than a
digest-only reference or a single-run restriction. Round 70's three remaining-
surface ledger blockers remain open and those families remain unauthorized.
This record grants no implementation, merge, release, promotion, installation,
runtime mutation, or self-certification authority; a fresh independent exact-
head `PASS` is required before this identity repair is implemented.

## Bounded repair

- Every `pointer-mutation-commit-evidence/v1` arm adds one nullable
  `priorCheckpointEvidence` member containing a recursively closed
  `pointer-mutation-run-checkpoint-evidence/v1` record. It is null exactly for
  run ordinal zero and non-null exactly for a positive run ordinal.
- For a positive ordinal, the prior core ordinal is exactly one less and the
  prior evidence repeats the exact `G`, target kind/path/`Dp`/mutation,
  installation/project/state/transaction/source identity, and selected old-
  authority epoch. Its recomputed `Dcore` is the sole prior-checkpoint part in
  `pointer-mutation-run-id/v1` and `Dcommit`.
- The recomputed prior selector `Dp/Dt/Dv/Dr` and `Dpost` initialize the current
  checkpoint-zero predecessor fields. Every current checkpoint target `Dp` is
  independently recomputed from its already-required identity tuple before
  checkpoint-sequence, `Dcommit`, or packet acceptance.

## Proportionality and trust

The repair adds one nested evidence member to the existing non-persisted commit
composition. It reuses a public record already required and retained by the
run-current protocol; it adds no storage path, pointer kind, receipt, selector,
tree, proof, inventory, coordinator, compaction, epoch key, capability, or
verdict route. A digest-only reference would leave coordinated copying
undetectable, while single-run restriction would delete the reviewed retry
lineage. Structural validation still grants no live authority.

## Required independent attack

Review the exact committed repair head cold. Recompute target `Dp` from each
checkpoint identity tuple and independently/coordinately substitute kind,
path, installation, project, state, transaction, source, and every copied
target `Dp`. Exercise zero/null and every positive/non-null run-lineage rule;
skip, reuse, or reorder the prior run ordinal; substitute prior core, selector
tuple, observation/`Dpost`, target, `G`, or old authority; copy a forged prior
digest and run ID through every sibling; and prove checkpoint zero refuses each
mismatch. Recheck branch absence, packet composition, trust, vocabulary, and
proportionality. Do not treat this repair as authorization for the three
remaining ledger blocks or apply an older PASS to this amendment.
