# ISS-022 process/handle repair follow-up — round 259
`PASS`.
Both workers now receive the 32-byte challenge only through their direct IPC channel; neither argv carries nonce bytes, and the closed response parser binds the exact challenge.
Process and handle-replay collection race provider `close` against an absolute deadline after forced termination, so false/ineffective kills cannot hang the probe.
Overflow is retained as `outputLimitExceeded` and gates `responseAccepted`; nonce mismatch, malformed/multiple messages, overflow, timeout, and kill refusal all have hostile tests and remain non-PASS raw evidence.
The successful row still binds the direct handle, exact `SIGTERM`, and provider `exit` then `close`; grandchild text remains explicitly non-authoritative.
WeakMap/function identity, nonce, clone/worker/serialization/cross-process/release refusal and canonical external custody remain intact; no verdict, helper, parser-equivalence, physical, or production authority was added.
Focused tests pass 10/10 and root typecheck passes; the repair is proportional to Round 258.
