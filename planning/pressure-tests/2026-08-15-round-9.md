# Planning pressure test — round 9

Disposition received: `BLOCK_REPLAN`.

The ninth independent reviewer found seven remaining public-contract and
authority-owner gaps.

| Finding | Replan disposition |
|---|---|
| `OP-R9-001` cycle plan names and launch-refusal skip flow contradicted | Standardized `cycle-plan/v1` and added an exhaustive per-source terminal/`routine-step-skip/v1` routing table. |
| `OP-R9-002` review issue still authorized a revision rather than worker result | Recast `ISS-009` around immutable `worker-result-subject/v1` plus optional exact revision/tree materialization proof. |
| `OP-R9-003` broker had no recovery-secret creation authority | Added closed internal OS-CSPRNG create/consume/revoke APIs guarded by `release.recovery-admin`, with atomic restart semantics and no raw-byte egress. |
| `OP-R9-004` no authorized executable could bind production credentials before install | Split independent review into `ISS-037` and added reviewed bootstrap `bind-credentials`; `ISS-034` now executes only that attested reviewed binary before authorization/install. |
| `OP-R9-005` self-host adapter invented GitHub work/API authority and exports | Published exact exports and work-record/API lifecycle contract, plus live evidence probe `ISS-036` gating `ISS-033`. |
| `OP-R9-006` Codex denial canaries were categories rather than fixtures | Published literal per-OS commands, sentinel setup, exact schema-escalation call, required raw denial surfaces, and pre/post censuses. |
| `OP-R9-007` real reboot evidence lacked host identity/custody | Published canonical host/user/state/key/re-entry contract, added `ISS-035` probe and `ISS-038` production implementation, and blocked bootstrap/supervisor/review/credentials on it. |

This is a failed-round record, not a pass. The revised 39-issue packet requires
another fresh independent review before registration.
