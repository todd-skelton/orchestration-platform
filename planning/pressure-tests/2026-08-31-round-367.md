# ISS-022 private native build helper — round 367

`PASS` at `229c0383b777b7a79dcad3308ac25ddde4769298`, base
`2020d9429998de5b53fd7eeb5c2f3a54a158cd48`. Independent cold reviewer:
`/root/iss022_native_build_review`, Sol high, row 11. No blocker was found.

The private helper owns fixed platform recipes, environment allowlisting,
fresh separate outputs, retained-byte checks, custody and output censuses,
and unavailable-toolchain versus failed-compilation reduction. It loads no
native code and invokes no candidate build hook. Synthetic invocation tests
do not prove native behavior.

Archive-to-header derivation, complete stable fixture capture, hosted compiler
identity, ABI checks and actual pre/post-load checks remain explicit stable
coordinator prerequisites. `loaded:null` is an intermediate build result,
never a complete diagnostic report. No new public schema or trust framework
is introduced. Removing fixed argv or byte checks admits build substitution;
keeping this helper private avoids a general compiler service. Review ran no
tests or builds; host verification remains separate.
