# Pressure test — round 67 independent sibling-equality review

**Disposition:** `PASS`

**Reviewed head:** `f51fae1ad56ab3b9bb2a32cb08bee7b234b1758e`

**Repair base:** `8e79d012fd208aeb6ad3d7111509f83ae0a54928`

This was a fresh independent cold review of the exact initially clean head
above on `iss-002-versioned-contracts`. The review began with `AGENTS.md`,
`docs/planning/work-model.md`, and
`docs/architecture/contract-decisions.md` as the sole decision authority, then
read the relevant supervisor ledger, compatibility contract, ISS-002 draft,
rounds 64–66, the round-65 blocker, and the exact round-66 diff from the named
base. No consumer repository, Chase Sets repository, issue, board, project, or
backlog was read or touched.

## Disposition

The round-65 outcome-to-slot and conflict-producer substitution blocker is
completely removed. The repair adds only mandatory equality checks over
existing fields and requirements for their implementation mutants. It adds no
schema member, record, digest part or domain, path, receipt, pointer kind,
state transition, selector, capability, tree, proof, inventory, coordinator,
checkpoint, or compaction surface. No blocking ambiguity remains.

This exact-head `PASS` authorizes implementation of the round-66 sibling-
equality repair only within ISS-002's stated scope and predicted implementation
footprint. It does not authorize merge, release, promotion, installation,
runtime mutation, or self-certification. A substantive contract or planning
change requires another independent exact-head review.

## Direct attack results

- **SELECTED target and outcome equality:**
  `targetRegistrySlot.selectedEvidence.proposal.mutationId` must equal the
  common `targetMutationId`. The selected graph is recomputed, and its `Dt`
  must equal `ordinaryResolution.selectedTargetTipDigest`. That resolution
  member remains the sole non-null SELECTED arm and equals
  `outcomeEvidenceDigest`. A valid resolution naming tip A paired with a valid
  selected graph for tip B therefore refuses before `Dcommit` is accepted.
- **SELECTED producer equality:** the selected proposal's
  `authorityEpochTipDigest`, `authorityEpochValueDigest`, and
  `authorityEpochReceiptDigest` map respectively to semantic `Dt/Dv/Dr` and
  must equal both the common old/KNOWN packet authority and the correspondingly
  named resolution producer fields. Those resolution fields already equal the
  run's single selected epoch. Independent, reordered, partial, stale, and
  coordinated proposal-producer substitutions cannot preserve all named
  equalities.
- **LOST_CONFLICT target and outcome equality:** the conflict composition's
  losing proposal mutation must equal the common target mutation. The
  conflict receipt is recomputed from the losing `Dr/Dv`, selected real winner
  `Dt/Dv/Dr`, target identity, conflict facts, and producer epoch; that
  recomputed receipt digest must equal
  `ordinaryResolution.conflictReceiptDigest`. The resolution member remains
  the sole non-null LOST_CONFLICT arm and equals `outcomeEvidenceDigest`.
  Pairing a resolution naming receipt A with a valid composition carrying
  receipt B therefore refuses.
- **LOST_CONFLICT producer equality:** both the losing proposal and conflict
  receipt carry named `authorityEpochTipDigest`,
  `authorityEpochValueDigest`, and `authorityEpochReceiptDigest`. Each maps to
  `Dt/Dv/Dr` and must equal the same common old/KNOWN packet and resolution
  tuple. Substituting either sibling independently fails its equality;
  substituting both together still fails the independently selected run,
  packet, resolution, and current-authority bindings.
- **No duplicate producer `Dp`:** the existing generic proposal and
  `pointer-conflict-receipt/v1` schemas contain producer `Dt/Dv/Dr` but no
  producer-authority path-instance member. Their `pathInstanceDigest` is the
  target pointer `Dp`, not a duplicate producer `Dp`. Producer `Dp` remains
  represented once by the resolution and common old/KNOWN packet fields. The
  repair changes no schema table or implementation field census and invents no
  `authorityEpochPathInstanceDigest` or equivalent alias.
- **Coordinated substitution closure:** moving target mutation, selected tip or
  conflict receipt, proposal producer, conflict producer, resolution producer,
  common old authority, and KNOWN packet authority together would no longer be
  the reviewed run. Existing intent, run-ID/checkpoint, selected current-
  authority, complete history, packet identity, and `Dcommit` recomputation
  bind those facts independently. Co-hashing cannot replace the new named
  equalities, and the repaired ledger explicitly refuses that construction.
- **Unchanged censuses and framing:** the run intent remains exactly thirteen
  common members plus six rotation-only members; conflict evidence remains six
  members; the evidence slot remains three; authority-history binding remains
  six; the registry remains eleven ordered kinds. The `Dcommit`, run-intent,
  conflict, evidence-slot, and history-binding framed formulas and canonical
  orders are unchanged. The exact diff adds narrative semantic relations and
  acceptance mutants only.
- **Unchanged conflict slot and history binding:** SELECTED still requires the
  generic target selection, LOST_CONFLICT the closed real-winner conflict
  composition, and unknown the null slot. The dense complete history walk still
  binds GENESIS zero through the selected head, every predecessor and record
  digest, `G`, genesis selection, and selected current-authority head. There is
  no alternate history head, sparse proof, directory census, or compaction arm.
- **Trust, ownership, and authority:** structural parsing and `Dcommit` success
  still grant no live authority. Runtime mutation requires the selected exact
  authority, canonical custody, kernel-exclusive lock, and ISS-004 private live
  context. ISS-002 owns schemas, canonicalizers, and pure validators; ISS-004
  remains the runtime producer; ISS-020 remains the GENESIS/downstream-
  selection producer. No worker, candidate, repair record, or this review can
  certify or promote itself.
- **Vocabulary and scope:** the additions use only engine-owned target,
  pointer, proposal, conflict, outcome, packet, and authority vocabulary. They
  add no adapter, repository-policy, consumer, provider, branch, worktree,
  label, milestone, queue, or deployment field. Run framing, FULL_REQUIRED
  history, release trust, portability, and consumer separation are unchanged.

## Bounded proportionality check

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Closed run-intent union | A run can move across target, selected old epoch, or rotation operation. | Deleting the common tuple or rotation equality leaves that boundary caller-selected. | The existing create-once record and pure recomputation remain smaller than an epoch key or rotation receipt. Retain unchanged. |
| Conflict composition in the target slot | A loser can assert a winner or combine evidence from another target. | Deleting loser/winner/receipt composition makes LOST_CONFLICT a digest-only assertion. | The existing six-member non-persisted composition is smaller than widening every registry slot. Retain unchanged. |
| Complete authority-history binding | A packet can select a substituted, forked, truncated, or reordered producer history. | Deleting a record or the genesis/head equality makes complete-chain authentication impossible. | Reusing the required O(n) walk is smaller than a tree, proof, inventory, checkpoint, or compaction protocol. Retain unchanged. |
| Outcome-to-slot digest and target-mutation equality | A valid resolution can be paired with a different valid selected graph or conflict composition. | Deleting either equality recreates the exact round-65 sibling substitution. | Comparisons against already-present values are the smallest credible repair and add no public mechanism. Retain repaired shape. |
| Proposal/conflict producer `Dt/Dv/Dr` equality | Valid sibling records can name another selected producer epoch independently or coordinately. | Deleting any component admits a stale or cross-epoch sibling at that component. | Three named comparisons per existing producer-bearing sibling are smaller than adding producer `Dp`, an epoch key, receipt, or membership proof. Retain repaired shape. |

The equality repair is directly evidenced by the round-65 substitution and has
an immediate deletion test. Adding a duplicate producer `Dp` would not close an
additional in-scope threat and would widen the public schema, so its absence is
the proportional result.

## Footprint and verification evidence

The exact repair range
`8e79d012fd208aeb6ad3d7111509f83ae0a54928..f51fae1ad56ab3b9bb2a32cb08bee7b234b1758e`
is one commit and changes exactly five paths: the decision authority,
supervisor ledger, compatibility contract, ISS-002 draft, and round-66 author
record. It has 101 insertions and 4 deletions and changes no implementation,
test, schema table, digest formula, topology, board, wrapper, shared script,
glossary, unrelated package, or runtime state. This review adds only this
round-67 pressure record.

- Initial `git rev-parse HEAD` and `git status --short --branch` — PASS; the
  exact clean reviewed head resolved to the full SHA above.
- Merge-base, one-commit rev-list, name-status, stat, numstat, full scoped diff,
  and `git diff --check` for the exact repair range — PASS; only the five
  expected paths changed and no whitespace error was found.
- Cold authority, supervisor, compatibility, ISS-002, rounds 64–66,
  proposal/conflict schema, deleted-surface, census, framing, history, trust,
  vocabulary, ownership, and footprint reads/searches — complete; no blocker
  or schema growth found.
- `pnpm run planning:check` — the package-manager shim could not verify/fetch
  its pinned registry signature in the restricted environment, so the script
  did not execute through pnpm. Direct `node scripts/planning/check.mjs` — PASS
  (`planning contracts verified`).
- `node scripts/planning/board-check.mjs` — sandboxed `gh` spawn refused
  `EPERM`; the approved read-only rerun against only this repository's issue and
  delivery-project census passed (`board contracts verified`). No consumer or
  Chase Sets system was read.
- `node scripts/format.mjs check` — sandboxed formatter spawn refused `EPERM`;
  the approved read-only rerun passed.
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
- `node node_modules/vitest/vitest.mjs run` — sandboxed helper spawn refused
  `EPERM`; the approved read-only rerun passed (22 files, 252 tests).
- ISS-002 `@orchestration-platform/contracts:test` capability slot — sandboxed
  helper spawn refused `EPERM`; the approved read-only rerun passed (12 files,
  55 tests).
- ISS-002 `contracts:compatibility-check` capability slot — sandboxed helper
  spawn refused `EPERM`; the approved read-only rerun passed (1 file, 1 test).
  The current executable checks predate implementation of this repair and are
  regression evidence only; the amended ISS-002 acceptance criteria require
  the independent and coordinated mutants during implementation.
- Final pre-report `git rev-parse HEAD`, `git status --short --branch`, scoped
  path census, and `git diff --check` — PASS; the reviewed commit remained the
  exact clean head before this report was created.

## Final status

`PASS`. The round-65 blocker is removed at exact head
`f51fae1ad56ab3b9bb2a32cb08bee7b234b1758e` without schema growth. ISS-002 may
proceed to bounded implementation of the reviewed equality and mutant matrix;
all later review and stable-promotion trust gates remain required.
