# Pressure test — round 56 author producer-ownership repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `3528c1af8426878f40c3d284b6e57ef81f770e33`

This author-side repair answers only `OP-R55-001` through `OP-R55-003`.
Round 55's `BLOCK_REPLAN` remains governing. This record grants no
implementation, merge, promotion, installation, or release authority; only a
fresh independent exact-head `PASS` may authorize implementation.

## Bounded repair

- The renderer digest is per selected installed worker host, not globally
  Codex-specific. A new closed `worker-host-renderer-artifact/v1` row maps one
  opaque host identity digest to one exact host renderer artifact digest. The
  reviewed release manifest carries a dense, unique 1–16 row array; route and
  step 7 select by opaque digest without provider vocabulary.
- ISS-021 is the first host producer and ISS-045 may produce a distinct reviewed
  host artifact under the same contract. Each owns only deterministic artifact/
  identity digest production and template goldens. No dynamic resolver or
  shared unbound registry is introduced.
- ISS-020 now owns N0 reviewed-manifest/install-read-back mapping and ISS-014
  owns every successor mapping. Their acceptance evidence covers exact-byte
  recomputation, selected active-release equality, and missing/duplicate/wrong-
  package/stale/candidate/self-reported/moved/manifest/read-back substitutions.
- ISS-011 now has producer acceptance for the dense catalog, exact four-part
  resolver key, one accessor/template result, exact descriptor pair projection,
  and descriptor/release-manifest digest binding.

## Proportionality and ownership

One three-member host-artifact mapping row and one bounded release-manifest
array are the smallest portable authority that distinguishes multiple already-
registered worker hosts. It reuses the single existing renderer artifact digest
per host and active-release equality. It adds no second registry digest,
provider name, path, filesystem/network lookup, dependency edge, or mutation
owner.

ISS-002 owns the closed schema and pure relations; ISS-011 owns catalog
production; each host issue owns its package artifact; ISS-020/014 own N0/N1+
reviewed manifest and install read-back; ISS-008 binds the dispatch plan; and
ISS-026 composes the equality. The affected drafts now carry those exact
acceptance obligations inside their existing footprints.

## Required independent attack

Review the exact committed repair head cold. Attack duplicate/missing/extra
host rows, cross-host identity/artifact substitution, provider vocabulary,
route-to-manifest mismatch, stale/candidate/self-reported artifacts, manifest/
install read-back drift, ISS-021/045 ownership collision, catalog duplicate-key
and pair-census production omissions, dependency/footprint drift, removed
reference/resource-kind resurrection, and self-certification. Older PASS
dispositions do not apply.
