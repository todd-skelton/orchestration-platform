# Pressure test — round 42 reconciliation packet

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

This author-side record reconciles branch B
`e77fc77a35d91d35d80cda3887f64c65840182b7` with the settled deletions and
tooling from branch A commits `5249c539ccb52c0b750716e5e71f3b3c248cc8b9`
and `cebe7249bc4ad9c5be99c796b96e9399356ed4cb`. It incorporates the schema pin
from `244911b999151ca48369f4a00eb7a616d8378fa0` only where it agrees with this
reconciliation. It is not an independent receipt and grants no implementation,
merge, board-mutation, or release authority.

## Superseded histories and governing review

Branches A and B reused round identities 34–36 for divergent packets. B's
original round-34/35/36 files remain at their canonical paths; A's exact
34/35/36 texts are preserved separately as branch-A historical records. A
rounds 37–41 are retained and explicitly marked historical/superseded. A's
round-41 PASS and B's earlier terminal dispositions do not apply to the
reconciled head. A fresh independent exact-head review must issue the next
governing disposition.

## Selected protocol and retained deletions

- B owns the closed GENESIS/ROTATION `authority-history-record/v1` union and
  shared `Dop`/`Dsc`/`Dgb`/`Dgse`/`Drot` composition. Serialized records use
  `authority-history-record/v1`; `authority-history/v1` is only the log/`Dh`
  digest domain.
- B owns the single-epoch ORDINARY/AUTHORITY_ROTATION commit union. Rotation
  stops at selected checkpoint 5, appends the record, performs authority CAS as
  its final write, and derives only RESUMABLE/SELECTED/UNKNOWN. Checkpoints
  6–8, ordinary resolution, later selector evidence, or successor-epoch writes
  are contradictory and reduce to UNKNOWN.
- Canonical decimal ordinals are grammar/length/lexicographically compared to
  `Number.MAX_SAFE_INTEGER` before conversion. Overflow never reaches Number.
- A's retention deletion is retained: eleven pointer kinds, eleven packet
  slots, ten tombstone-enabled families (all except the never-removed authority
  rotation pointer), four proposal classifications, and FULL_REQUIRED records
  only. No retention pointer, compaction transition, or degraded-audit mode
  exists.
- A's exact armed-intent equality set, missing authorization archive schemas,
  LIVE-only descriptor, folded TERMINAL attempt log, constructed-path
  existence/absence probe, bounded coordinated mutants, and dispatch-brief
  ownership split are retained.
- A's board reconciliation tooling, shared-scaffold amendment rule, and
  ISS-044/045 second-host probe/adapter are retained. B's ISS-039–043 identities
  and transitive-reduced prose conventions govern.
- B's bounded proportionality rule governs: one deletion test and
  smallest-alternative justification per replan/review, followed by ordinary
  independent exact-head review. A's historical repeat-until-clean protocol is
  not current policy.

## Deterministic census

- 46 registered issues, 73 direct dependency edges, and zero transitively
  implied direct edges.
- The 39-issue original closure remains 544 pairs; closure through ISS-043 is
  615; closure through ISS-045 is 654.
- The immutable 183-record migration base is 7,126 canonical UTF-8 bytes with
  SHA-256
  `a8d168d55fdb2ee12da28988009d8e93c25675117e0f2b903c3c1f8edc832b7a`.
  ISS-039/040 remain a separate two-record addendum, for 185 provenance
  identities total.
- The desired addendum terminal state is exact: Chase Sets #6999 and #7000 are
  both CLOSED/NOT_PLANNED and absent from Chase Sets Delivery after the
  authorized scripted sync. Live external audit still reports both open and
  present; this author lane performs no GitHub mutation, so that drift remains
  an operator/sync obligation rather than being coerced to success.
- The source-project verifier checks absence for all 185 tracked identities
  against the complete paginated 1,124-item project census while allowing
  unrelated and explicitly excluded Chase Sets items. The immutable 183 are
  already absent; only #6999/#7000 currently fail this terminal check.
- The author-side live `planning:board-check` completed all repository and
  Project pagination and returned 31 external mismatches: 20 draft-body sync
  rows, missing destination Project membership for ISS-041 through ISS-045,
  #6999/#7000 still present on Chase Sets Delivery, and four independent
  source state/reason mismatches. This is expected pending the prohibited-in-
  this-lane scripted sync and is not reported as green.
- Schema field counts are Dop 9/15, Dsc 17, Dgb 14, Dgse 38, Drot 12, Dh 7/13,
  selected authority value 22, and Dcommit common 18 plus ORDINARY 4 or rotation
  base 8 plus exactly one RESUMABLE 9, SELECTED 8, or UNKNOWN 1 arm.

## Bounded proportionality check

The retained closed record and commit unions defend the actual single-writer
crash and external-destination race boundaries. Deleting their branch tags,
head binding, or outcome-slot equality admits ambiguous authority. The smaller
credible alternative is the selected linear chain plus derived rotation truth;
that alternative is already used. Sparse trees, node inventory, coordinator,
epoch-split runs, retention/compaction, diagnostics, and arbitrary-precision
ordinals remain deleted because they defend excluded or unobserved threats.

Board pagination needs one additional completeness guard because board state is
external authority. The reconciled checker uses the issue connection's
independent `totalCount`, the two Project item connections' independent totals,
validates every page/cursor and issue/item identity, and has positive and
negative 100/101 cap-boundary cases. It checks Project membership but not the
unmodeled presentation-only Status field. Merely trusting a locally consistent
first page is not an acceptable smaller alternative.

## Required independent attack

Attack every selected B branch/schema formula, A deletion residue, ordinal
conversion before bounds checking, armed-intent substitution, wrong/missing
registry slots, pressure-history identity, graph counts/closure, immutable-base
hash, addendum state/reason/board presence independently, incomplete board
pagination at exactly 100/101 rows, ISS-044/045 unsupported-OS certification,
and direct-versus-transitive blocker prose. The independent reviewer must bind
its result to the final clean reconciliation head.
