# ISS-022 diagnostic aggregate artifact pressure review — round 309

`BLOCK_REPLAN` at exact base/HEAD
`f80c12ba5cc6bb41cc60a77dbc96391c6d55431d`.

The uncommitted seven-file, `+143/-3` Slice A stays within the existing
four-job workflow and aggregate artifact coordinate. The stable aggregate
rereads the exact current-attempt observation directories, environment,
report, raw manifest, and receipt identities; on an authenticated ISS-022
non-PASS it writes only the three registry-named receipt files, returns refusal,
and emits no `aggregate.json`. The upload step's sole `if:always()` widening is
minimal. Candidate writing, non-current downloads, provider/record/terminal/
publication behavior, schemas, commands, helpers, issues, and edges remain
unchanged.

One verifier blocker prevents landing. `verifyGithubDiagnosticAggregateArchive`
is not bound to the exact ISS-022 diagnostic arm or to the evidence it claims
to authenticate. It checks only ZIP/file census, receipt syntax, job/suite/
vector names, null walk timing, and that some receipt is non-PASS. It does not
require the ISS-022 registry, recompute/equal-bind `requiredJobRegistryDigest`,
or require common/current candidate, contracts, harness, test, and provider-run
identities. It receives no observation environment/report/raw evidence or
expected receipt digests, so a stale receipt, cross-run mixture, substituted
environment/raw identity, or one UNKNOWN changed to PASS while another receipt
remains non-PASS is accepted. The new positive test proves the scope widening
by deliberately accepting a synthetic receipts-only `iss002-contracts`
archive, despite the authority closing nullable diagnostic composition to
ISS-022.

The smallest repair is to make the diagnostic verifier consume the exact
ISS-022/current-context and recomputed receipt expectations already established
by the stable path, refuse every global or per-job mismatch, and replace the
ISS-002 happy path with exact three-job ISS-022 mutants for mixed, missing,
extra, reordered, substituted, stale, and UNKNOWN-laundered archives. No
provider record, terminal, publication, job, schema, or helper mechanism is
needed for this slice.

Proportionality: the retained 146 changed lines address the in-scope threat of
preserving non-PASS evidence without granting aggregate authority; deleting
the receipts-only artifact loses that evidence, while deleting its semantic
bindings makes the counterexamples pass. Binding the existing verifier is
smaller than adding another record or workflow surface.

Focused evidence: the three touched test files pass (44 tests), `pnpm run
typecheck` passes, and `git diff --check` passes. Those checks currently encode
rather than reject the blocker above.
