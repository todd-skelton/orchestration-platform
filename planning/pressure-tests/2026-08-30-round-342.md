# ISS-003 live CLI composition — round 342

Disposition: `PASS` at `37cad869cad636645875a368e03155750a87a56b`,
base `38071f13394d207fe34ffbb308d96e401b1687ea`.
Independent cold reviewer: `/root/iss003_composition_review`, Sol high, row 11.

The fixed bundle captures the closed process inputs and composes the existing
dispatcher, loader, config handler, and canonical output boundary. Only config
is implemented; project and the other owner placeholders remain handler-free.
The fixed deferred import keeps the source registry inspectable before builds
and closes during bundling.

The sixth target preserves the original outputs, broker allowlist, options,
and private-path inspection. Root build runs the existing native builder
before JavaScript bundling, after test teardown; the JavaScript builder never
rebuilds an addon held by test workers. Equal source/bundle directory depths
preserve the native artifact path without rewriting.

Proportionality: esbuild lazy initializer labels exposed private source paths.
Deleting cleanup rejects the legitimate bundle; deleting inspection admits
runtime leaks. Cleanup constrained to a source-path method label immediately
after an esbuild initializer declaration is the smaller repair. Executed
controls preserve runtime methods for rejection and legitimate runtime values.

ISS-000/003 drafts, manifests, builder, verifier, and affected census tests
change together. No native source, lockfile, export, workspace, or shell-matrix
expansion occurs. Host targeted execution passed 182 tests with 12 expected
platform skips. Full/bootstrap and hosted evidence belong to the PR; this
review does not certify unrun OS checks. Generated board bodies must be
reconciled adjacent to the reviewed merge.
