# Pressure round 443 — neutral worker-result review planning definition

Status: bounded docs-only ISS-011 definition. Protected base is
`2cf49627a37dcd7d38a86d300384f9496c35c0c5`.

Absolute attempt 1 was the read-only next-slice audit at that base. It identified
the existing review consumer and the missing neutral descriptor/policy literals.
Absolute attempt 2 authors this definition in the predicted three-file scope.
No review PASS or source execution is claimed. Attempts remain cumulative for
this planning artifact; changing its name or branch does not reset them.

## Declared inputs and scope

Rounds 434 through 439 are mandatory lineage, including both abandoned source
rounds 435/437, their exact heads, independent reviews, hosted failures, and the
438 exact-byte replan. The landed 439 planning source/pin remains byte-stable.
Round 401 supplies the existing optional-disposition and plan-only ABI boundary.
Current `review-module.ts`, `consume.ts`, `review-consumer.test.ts`, and
the two-adapter preparation in `consumer.test.ts` are concrete consumer inputs;
the closed public module, review-subject, and dispatch contracts remain owners
of parsing and binding.

This packet defines only the neutral review descriptor and its pure step-4
worker-result action policy in the module ABI, adds the adjacent ISS-011 note,
and records this round. It does not copy fixture source into production or
admit either quarantined module to the registry. The live issue is reconciled
by the root after independent review.

## Literal closure and non-goals

The descriptor is `review@0.0.0`, with the exact two existing fixture adapter
tuples, one `review.worker-result/work.read` action, review role, required
worker, no recursive review requirement, eight fixed catalog rows, and empty
disposition codes. The ABI/export/schema census is unchanged. A parsed prior
worker-result subject produces one complete review brief with null work ID and
the whole immutable subject digest; alternate descriptor/null/candidate input
refuses with a bound INPUT_REFUSED, while parser refusal produces no result.
Both policy arms preserve the planned action. No frontier row, policy fact,
parser success, source pin, or planned review is execution/review authority.

Non-goals are disposition and findings, accepted/rejected authority, worker
launch, routing/host/model choice, rendering, provider reads, credentials,
mutation, journal/reclaim, release-candidate review, delivery/repair semantics,
new public families, runtime replacement, manifest/build activation, and
ISS-011 closure. Full-routine admission still needs the actual source-owned
disposition handler before effects. Seeded author history remains input rather
than a claim that an author cycle ran.

## Proportionality and removal evidence

| Mechanism                       | Concrete threat and discriminating removal vector                                                                                                                                                | Smallest alternative                                                                       |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| Exact descriptor/catalog        | a changed role, action pair, directive or catalog binds an unintended worker request; remove one complete golden equality                                                                        | one closed descriptor and eight existing accessor rows, no renderer or policy language     |
| Complete worker-result target   | artifact/terminal/frontier digest replaces the exact reviewed result; substitute one subject component while retaining the old result                                                            | reuse the existing whole-subject digest and post-await binder, no new target schema        |
| Policy-arm preservation         | a fixture plan is mistaken for breaker clearance, or a copied frontier planner silently suppresses TRIP review planning; pair actual TRIP/NO_TRIP inputs and inspect absence of authority output | preserve observations and fixed review planning; later engine gates retain authority       |
| Structural versus bound refusal | rejected bytes gain a fabricated bound result, or valid null/candidate input falls into a review action; remove one refusal boundary                                                             | existing public parser plus one closed INPUT_REFUSED cell                                  |
| Quarantine byte identity        | changed production bytes inherit review; remove the pre-call pin or change an unlisted byte                                                                                                      | normalized SHA-256 plus independent exact-byte review, no semantic source scanner          |
| Actual preparation seam         | hand-built facts hide adapter/intent/policy/cycle substitutions; remove retained input or post-await validation                                                                                  | reuse loader/snapshot/policy/composition with both landed adapters, no runtime integration |

These are required later source vectors, not executed deletion evidence from
this definition. The pin proves identity continuity only. Every production
byte and sole public-contract dependency still require independent inspection.

## Acceptance, verification, footprint, and attack surface

Definition acceptance is the literal census and complete action/refusal matrix,
actual consumer grounding, explicit policy/authority distinction, unchanged
plan-only phase boundary, and source footprint below. Source acceptance requires
exact descriptor/result bytes and digests, each core/brief/directive/resource
join, structural and valid substitution refusals, same-cycle refusal, actual
two-adapter policy arms, empty/reordered/changed-frontier noninterference, target
substitution, repeated equality, identity mutants, and byte-stable placeholder
and planning source evidence. Any new family, changed public binding, third
adapter, phase, runtime consumer, manifest row, dependency or authority claim is
a replan boundary.

Definition footprint is exactly `docs/architecture/module-abi.md`,
`planning/drafts/ISS-011.md`, and this file. The following source footprint is
exactly `modules/review/src/index.ts`, focused additions to
`fixtures/walking-skeleton/test/consumer.test.ts`, and one source pressure
record. Existing planning vectors/pin, fixture runtime, manifest, generator,
generated stub, public contracts, root configuration and dependencies stay
unchanged.

Local definition verification is planning, targeted formatting, and diff
checks, with the root reconciling the issue before live board verification.
No tests, compiler, native probe, or provider experiment run locally. Source
verification adds local typecheck, then hosted focused/full suites on all three
operating systems. Both packets require independent exact-head review before
merge; no current author receipt supplies its own PASS.

Review should attack fixture certification leaking into production claims,
same-cycle self-review, changed/mutable/incomplete targets, descriptor/catalog
substitution, policy or frontier confused with permission or target identity,
missing structural refusal, hidden ambient effects, an unguarded plan call,
reintroduction of the abandoned classifier, or a plan-only module entering a
full routine. Also verify the exact three-file scope and unchanged lineage,
planning pins, and activation surfaces.
