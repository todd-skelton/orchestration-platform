# ISS-022 stable-bundler delta review — round 277

`PASS` for exact delta `05b5d38cf98fdcac79ef278cffe651aacccd272e`. Root `package.json` and its lock importer pin `esbuild@0.28.2`; the harness manifest binds both, the workspace contract, probe manifest/source, and stable wrapper. Resolution from the stable root and parser source reaches the same root-installed bytes, and the wrapper externalizes nested `esbuild` imports to that absolute stable URL, outside candidate selection.

The probe is private/source-only: missing root install or package relocation fails resolution rather than substituting authority. Bootstrap requires the root pin and rejects package-local build scripts or bundler dependencies (apart from the pre-existing conformance pin); retaining no second package pin is the smallest shape and closes the named blocker.
