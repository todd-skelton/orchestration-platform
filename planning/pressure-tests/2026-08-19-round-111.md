# ISS-002 pressure test — round 111

## Review basis

This is a fresh independent cold removal review of exact candidate head
`2486a2d031ddbbff4caa0aec2fdecd909e38129a` on
`iss-002-versioned-contracts`, with parent boundary
`c6b8dc1f6837f130496c1fe850ebf1c249daf627`. The checkout was clean before
this required pressure record was written.

The review used only the repository instructions, work model, contract
authority, registered `ISS-002` draft, round 110 finding, and the exact
parent-to-head delta. No consumer repository, milestone delivery mechanism,
or external certification path was consulted.

The exact delta has two paths, 60 insertions, and eight deletions:

- `packages/contracts/src/retirement.ts`: 30 insertions and one deletion; and
- `test/contracts/anchor-teardown.test.ts`: 30 insertions and seven deletions.

`git diff --check` passes. The change adds no schema member, digest domain,
persistence path, registry entry, runtime state, lock, handle, mutation
capability, producer, promotion path, or compatibility version. It composes
five already-closed parsed records and four timestamp comparisons in the pure
`validateBootstrapSuccessorActiveRetirementBinding` result.

## Complete chronology independently reconstructed

The repaired complete fixture now represents this actual selected lineage:

```text
prior Dabs 12:01
<= prior lifecycle archive 12:02
<= prior anchor RETIRED proposal 12:03
<= prior owner RETIRED proposal 12:04
<= Dsrc independentReview.reviewedAt 12:05
<= successor owner ACTIVE proposal 12:06
<= Dsrp observedAt 12:07
<= successor-retirement Dabs observedAt 12:08
<= successor lifecycle archive 12:09
<= successor anchor RETIRED proposal 12:10
<= successor owner RETIRED proposal 12:11
```

The exact committed complete graph and an independently rebuilt complete graph
both returned `[]` through the public wrapper. The new comparisons are the
four missing cross-composition edges:

1. actual prior owner RETIRED proposal `proposedAt <= Dsrc reviewedAt`;
2. `Dsrc reviewedAt <=` selected successor owner ACTIVE proposal `proposedAt`;
3. selected successor owner ACTIVE proposal `proposedAt <= Dsrp observedAt`;
4. `Dsrp observedAt <=` successor-retirement `Dabs.observedAt`.

All inputs reach closed parsers before these comparisons. Canonical timestamp
text therefore has the same order as time, and the implementation's strict
`<` rejection admits equality as required.

## Fully rehashed reverse-time and equality probes

A read-only in-memory probe rebuilt every digest descendant affected by each
timestamp mutation: the actual prior RETIRED proposal/tip, `Dsrc` review
candidate/core, successor anchor, owner ACTIVE value/proposal/tip, `Dsrp`
readbacks, successor absence/archive/teardown receipt, and the anchor/owner
RETIRED tail. It wrote no source, test, fixture, generated artifact, or probe
file.

Each isolated reverse-time graph refused at only its intended edge:

```text
prior RETIRED 12:05:30 > review 12:05
  successorChronology:review-before-prior-retired

review 12:06:30 > ACTIVE proposal 12:06
  successorChronology:active-before-review

ACTIVE proposal 12:07:30 > Dsrp 12:07
  successorChronology:post-before-active

Dsrp 12:08:30 > retirement Dabs 12:08
  successorChronology:absence-before-post
```

Four independently rebuilt boundary graphs equating, in turn, prior
RETIRED/review, review/ACTIVE, ACTIVE/Dsrp, and Dsrp/Dabs each returned `[]`.
The existing parsed local tail still independently enforces
`Dabs <= archive <= anchor RETIRED <= owner RETIRED`.

## Structural provenance, branch guard, and totality

A coordinated provenance attack replaced `Dsrc.priorRetiredTipDigest`, then
recomputed its independent-review candidate, `Dsrc`, successor anchor, current
owner ACTIVE graph, `Dsrp`, and the complete local retirement tail while
leaving the actual parsed prior selected tip unchanged. It refused at:

```text
successorReviewBinding:priorRetiredTipDigest:mismatch
```

Replacing both `Dsrp.valueReadbackDigest` and the caller expected copy refused
at the independently derived selected-owner bytes:

```text
successorPostBinding:valueReadbackDigest:derived:mismatch
```

A complete genesis ACTIVE local retirement, a complete local CONSUMED
retirement, and an ACTIVE graph with the wrong successor installation identity
all remained nonempty through the successor wrapper. Genesis and CONSUMED both
retained `successorBranch:not-active-successor`; the wrong identity retained
owner-mutation, selected-value, readback, teardown, and local-retirement
findings. CONSUMED therefore remains incomplete, fail closed, and unauthorized.
Structural validation still returns findings only and grants no live authority.

The five inputs newly parsed by the wrapper for chronology
(`selectedOwnerProposal`, retirement `Dabs`, prior owner RETIRED proposal,
`Dsrc`, and `Dsrp`) were each independently replaced by `null` and by an
`ownKeys`-throwing proxy. All ten calls returned nonempty findings; zero threw
and zero returned `[]`.

## `OP-R111-001` — committed tests do not discriminate half the chronology or any equality boundary

The implementation removes both round 110 chronology holes, but the committed
test does not make the four-edge contract executable. Its two new negative
assertions cover only edge 1 and edge 4, and neither is accompanied by an
equality control.

A read-only in-memory source-deletion probe bundled the exact committed focused
test against one mutant at a time. It made no filesystem edit. The baseline
passed. The results were:

| Mutant | Exact committed focused test |
| --- | --- |
| Delete prior-RETIRED-to-review comparison | FAIL — expected chronology finding disappears |
| Delete review-to-ACTIVE comparison | **PASS** |
| Delete ACTIVE-to-Dsrp comparison | **PASS** |
| Delete Dsrp-to-Dabs comparison | FAIL — expected chronology finding disappears |

A second in-memory series changed each comparison independently from `<` to
`<=`, which wrongly rejects its equality boundary. All four mutants passed the
exact committed focused test. Repository search confirms these two chronology
finding strings have no other executable assertion, and no public-wrapper test
constructs any of the four equality boundaries.

The two surviving deletion mutants are material: deleting edge 2 admits an
ACTIVE selection proposed before its independent review, and deleting edge 3
admits a claimed post-selection observation recorded before the selected
proposal. The four surviving strictness mutants turn valid simultaneous
boundary evidence into false refusal. Passing broad gates cannot substitute
for executable compatibility evidence of these public state-transition
boundaries.

### Bounded repair

Keep the four implementation comparisons unchanged. In
`test/contracts/anchor-teardown.test.ts`, add one small chronology rebuild
helper and public-wrapper cases that:

- fully rehash and refuse each of the four isolated reverse-order graphs; and
- fully rehash and accept each of the four isolated equality-boundary graphs.

At minimum, deletion of edge 2 and edge 3 and strictness change of every edge
must fail committed tests. No source contract, schema, receipt, digest,
timestamp member, sequence record, proof object, coordinator, capability, or
runtime writer is needed.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Deletion/attack result | Smallest credible shape |
| --- | --- | --- | --- |
| Four direct parsed timestamp comparisons | Review/activation/post evidence can be dated before the actual predecessor it claims to consume. | Fully rehashed reverse graphs now refuse at four unique findings; deleting edge 2 or 3 remains invisible to committed tests. | Retain the four comparisons; add discriminating tests, not a new record or digest. |
| Equality admission | A valid same-instant boundary is rejected by accidental strict ordering. | All four equality graphs return `[]`, but every `<` to `<=` mutant survives committed tests. | Add four public-wrapper equality controls using the same rebuild helper. |
| Existing Dsrc/current-owner/Dsrp composition | Caller-selected digest copies can substitute for actual selected provenance. | Coordinated forged Dsrc and Dsrp-copy attacks refuse at independently derived equalities. | Retain the existing two pure validators and actual record inputs. |
| Successor-only branch guard | Removing one local incomplete-provenance finding admits genesis or CONSUMED as a successor. | Genesis and CONSUMED remain nonempty with `successorBranch:not-active-successor`; wrong identity also refuses. | Retain the three closed branch predicates; add no new lifecycle state. |
| Closed parsing of the five new chronology inputs | Hostile input can throw during the new comparison path. | Ten null/throwing-proxy probes are total and nonempty. | Retain the five local parses before comparing their snapshots. |

The bounded repair is test-only and confined to the already changed contract
test path. This is smaller than any new contract machinery and directly
exercises the public compatibility boundary.

## Verification evidence

All gates ran in the mandated strict sequence. The standalone full suite
exited before bootstrap began; bootstrap exited before focused contracts;
focused contracts exited before the separate compatibility gate. No full and
bootstrap process overlapped.

1. Planning — PASS, `planning contracts verified`, through the checked-in
   direct checker. The restricted `pnpm run planning:check` failed closed
   before its script because signed pnpm 11.22.0 registry metadata could not
   be fetched.
2. Board — PASS, `board contracts verified`, against this repository and its
   registered platform delivery project. The restricted direct checker failed
   closed at the read-only `gh` subprocess with `spawn EPERM`; the approved
   identical direct rerun passed.
3. Format — PASS, all matched files use Prettier style. The restricted direct
   checker failed closed at the pinned formatter subprocess with `spawn
   EPERM`; the approved identical direct rerun passed.
4. Typecheck — PASS with the installed pinned TypeScript compiler.
5. Standalone full test gate — PASS through `pnpm test`: 28 files and 329
   tests in 162.75 seconds.
6. Separate bootstrap verification — PASS: manifests and registries,
   formatting, typecheck, 28 files/329 tests in 175.13 seconds, private
   compositions, planning contracts, and source-tree status invariance.
7. Focused ISS-002 contracts — PASS, 18 files and 132 tests in 8.00 seconds.
   A preliminary excluded-compatibility census also passed 17 files/131 tests
   before the exact focused census.
8. Focused ISS-002 compatibility — PASS, one file and one test in 1.35
   seconds.

The restricted in-memory probe startup failed closed at esbuild child-process
creation with `spawn EPERM`; the approved identical read-only reruns produced
the graph and mutation evidence above. They wrote no source, test, fixture,
generated artifact, or probe file. Before this required pressure record was
written, every gate and probe left the source tree clean and exact HEAD
remained `2486a2d031ddbbff4caa0aec2fdecd909e38129a`.

## Final status

`BLOCK_REPLAN` for exact candidate head
`2486a2d031ddbbff4caa0aec2fdecd909e38129a`, with parent boundary
`c6b8dc1f6837f130496c1fe850ebf1c249daf627`.

The four-comparison implementation now accepts the valid complete graph,
admits all four equality boundaries, and refuses fully rehashed reverse-time,
provenance-substitution, wrong-branch, and hostile-input attacks. It cannot be
removed as a chronology blocker until committed public-wrapper tests fail on
deletion of edges 2 and 3 and on strictness corruption of all four boundaries.
Add only those discriminating test controls, then return the exact repaired
head to fresh independent review.
