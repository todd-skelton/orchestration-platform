# Pressure test — round 32

**Disposition:** `BLOCK_REPLAN`

The thirty-second independent review found one initialization-prefix omission.
The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R32-001` — Gate/fence root → initial head → current-pointer construction had no crash reducer, and ordinal-zero head predecessor encoding was undefined. | Defined ordinal-zero canonical-null predecessor, current-pointer CAS-from-absence, and a shared `ABSENT` → `ROOT_ONLY` → `ROOT_AND_HEAD` → `CURRENT` reducer. The same transaction resumes only the missing suffix; malformed, multiple, reordered, or mixed prefixes become `UNKNOWN`. |

## Re-review focus

- Kill gate and fence initialization after root creation, ordinal-zero head
  creation/read-back, and current-pointer CAS/read-back.
- Replay exact same-transaction root-only and root-plus-head prefixes; no byte
  may be rewritten.
- Supply head-without-root, current-without-both, non-null initial predecessor,
  duplicate/extra head, and mixed-transaction prefixes; all must refuse.
