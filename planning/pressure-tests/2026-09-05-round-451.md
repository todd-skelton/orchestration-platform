# Pressure round 451 — repository-protection receipt phase-gate plan

Status: fresh ISS-054 historical-receipt sub-slice, absolute plan correction
attempt 3. Protected base is `73ab243ee83d75dbbfb2269b5e629f0dbb4e06f4`
on `codex/iss054-phase-gate-plan`; this correction starts from exact attempt-1
head `94a857b128a46ca39fcc254a333dc189f32d4966`. This one-file packet defines
the source boundary; it implements no parser, test, schema, registry entry,
provider action or authority. Source work is forbidden until this exact
corrected plan receives independent semantic review attempt 4 by a lane that
did not author attempt 3, lands through an ordinary PR on protected `main`, and
the Packet A source lane starts from that landed main.

Decision #268 comment `5552688364` admitted the fresh A/B sub-slice. Decision
#272 OWNER comment `5552876743` at `2026-09-05T15:37:01Z` admits this bounded
same-file correction and extends the cumulative ceiling to eight without a
spend increase. The absolute ledger is:

1. attempt 1 consumed: plan authored at exact
   `94a857b128a46ca39fcc254a333dc189f32d4966`;
2. attempt 2 consumed: independent semantic review by nonauthor lane
   `iss011_disposition_source` BLOCKED that exact head because the Packet-A
   census omitted accepted standalone `completeReductionPage` and
   `rulesetSemantics` helper inputs;
3. attempt 3: this same-file parser-census and matrix correction;
4. attempt 4: independent semantic review pending by a lane that did not author
   attempt 3;
5. attempt 5: Packet A source, only after attempt 4 PASS and plan landing;
6. attempt 6: independent exact-head Packet A review by a different lane;
7. attempt 7: Packet B source, only after Packet A lands on protected `main`;
8. attempt 8: independent exact-head Packet B review by a different lane.

Decision #272's standing rule governs this fresh sub-slice: one bounded
same-file correction and one independent re-review follow the first plan-review
BLOCK; a second consecutive plan-review BLOCK parks the sub-slice and files a
Decision. A surviving source counterexample or footprint growth also parks.
Hosted infrastructure failure is governed by Decision #271 and is not an
artifact attempt; hosted canonicality or integration-census repair remains
bounded lane hygiene and requires independent exact-head re-confirmation.

Routing record: task `ISS-054/Round-451/attempt-3`; exact reported model
`gpt-5.6-sol`; effort `high`; `row: 8` decomposition/judgment;
`placement: provisional` Sol-high Codex fallback, with Opus 5 reserved for the
independent attempt-4 review; transcript is this planning-lane artifact; budget
intent is at most $10, actual spend is unknown, and no new artifact spend
ceiling is requested or inferred.

## Declared evidence and prevention criteria

The input set is closed and historical evidence is never erased. The latest
operator handoff at
`C:\Users\ToddS\.codex\attachments\44c01c13-641d-4f39-b3f4-0585d1aebfd8\pasted-text.txt`,
the OWNER ruling, protected main, the named local archive, the owning PR/run
records, the accepted contract ledger and Rounds 440 through 447 are inputs.
The archive is read only and must never be pushed.

| Input | Preserved result | Discriminating prevention criterion |
| --- | --- | --- |
| Round 440 | Registered ISS-054 and the ISS-036/038 ownership edge after the missing-family census | Source remains a portable contract implementation; no provider or runtime behavior enters either packet. |
| Round 441 | Literal definition lineage includes independent review attempt 2 BLOCK at `0608bfc7702f4bf4fb9327270ccbc74a8fe2dbc` and attempt 4 BLOCK at `2674fe7759fe39b1b27bd4d30ec7315c49740839` before chronology, permission, pagination and producer/source repairs | Packet B retains the final fourteen-member family, complete permission namespace, distinct BUILD/REVIEW producer topology, and all repaired time relations; deletion vectors pin each relation independently. |
| Round 442 and the landed verifier anchor | Anchor source attempt 1 was `aca17ca7bb9ab950a809f97220d624beed247751`, independent review attempt 2 PASS, hosted run `33690404922` found the source/test bundle-census omission, and bounded attempt 3 repaired it before the anchor landed | The anchor is unchanged. Packet A adds its new source/test paths to the stable bundle census in the same packet; Packet B adds the relations-module path before its hosted run. |
| Rounds 443, 444 and 446 | Unrelated ISS-011 artifacts interleaved chronologically | They contribute no ISS-054 semantics and cannot reset, replace or certify any ISS-054 attempt. Their files and active PR lineage remain untouched. |
| Round 445 | Definition planning attempt 1 BLOCKED missing exact digest protocol; attempt 2 authored `b5f6f8d5112a30f8a4e58283e88d935938a01039`; review attempt 3 BLOCKED duplicate relation names, cyclic chains and the fresh-update chronology; repair attempt 4 supplied the accepted recipes | Packet B copies the accepted frame tags, part order, closed projections, opaque-leaf boundary, page-chain rules and terminal evidence without invention. Independent goldens and deletion mutants distinguish every repaired relation. |
| Round 447 and old source attempt 1 | `146381c5cb7896f19e510e8e45b277db10c79fc1` authored the standalone receipt | It is source material only. The new implementation may reuse validators, censuses, goldens and vectors verbatim where they fit the phase gate, but does not cherry-pick or accept that parser. |
| Old independent review attempt 2 | BLOCK at exact `146381c5cb7896f19e510e8e45b277db10c79fc1`: the request helper read `apiKind` before snapshot closure | Every record parser snapshots and closes its own input before reading a discriminator; the no-throw matrix includes request proxy, getter and malformed-branch rows with trap non-execution. |
| Old source repair attempt 3 | `a2d4d22746a117d5c4d8a7652b0a8fef2d618608` moved request branch selection after detached snapshotting | That behavior may be reused inside Packet A's typed request parser; it is generalized to every record rather than treated as a sufficient per-site repair. |
| Old independent review attempt 4 | PASS at exact `a2d4d22746a117d5c4d8a7652b0a8fef2d618608` | The PASS is preserved as review evidence for that head only. It neither certifies the fresh sub-slice nor excuses the later hosted counterexamples. |
| Owning bootstrap run `33941789720` | All three jobs failed at head `a2d4d22`: Windows job `101240538486`, Ubuntu `101240538613`, macOS `101240538733`. Each reported the same three focused failures. | Packet A/B must pass ordinary hosted bootstrap on Linux, macOS and Windows after their own review PASS. No local test substitutes for those jobs. |
| Hosted null environment failure | `environmentBinding:null` reached `projectionFor` and threw while reading `environmentName` | Packet A's path matrix covers `$.environmentBinding` with null, wrong-kind and malformed-record substitutions. Packet B can receive only a successful typed tree and never dereferences the raw field. |
| Hosted workflow-census failure | A missing REVIEW role, including the `[BUILD, BUILD]` workflow census mutant, reached reduced-value framing as an undefined `workflow` and threw `workflow:value:non-json` | Packet A parses every workflow row, then applies the two-role census only after the tree gate. It returns refusal before Packet B can select either typed workflow. |
| Hosted observation-field census failure | The implementation listed `completedAt` before codepoint-earlier `completeReductionDigest` | Packet A publishes one test-local census derived from the accepted ledger and compares every source field array to codepoint-sorted fixture keys; source, registry and tests use the same accepted order without changing canonical bytes. |
| Old source repair attempt 5 | Parked exact head `6e33ec334201e16f565765e23cebef9e038f906b` added a top-level early return for the hosted failures | The archive remains read only. A top-level issue-count check is not reused as the phase architecture because it leaves invalid descendants inside nominal records. |
| Old independent review attempt 6 | BLOCK at exact `6e33ec334201e16f565765e23cebef9e038f906b`; Decision #268 preserves the two counterexamples at archived source lines 411 and 511 | Packet A includes both exact paths in the no-throw matrix and requires typed child results before any parent relation. The old artifact is parked with no attempt 7. |
| Review-6 null-trigger counterexample | A null workflow `trigger` accumulated an issue at archived line 411, but later code cast `review.trigger` and dereferenced its source fields | `$.workflows[*].trigger` must produce `ParseResult<WorkflowTrigger>`; a failed child prevents construction of the workflow tree and therefore prevents every BUILD/REVIEW source comparison. |
| Review-6 null-Link-relations counterexample | A null REST `linkRelations` accumulated an issue at archived line 511, but later code cast it to an array and called `.find` | `$.apiObservations[*].pages[*].linkRelations` must produce a typed dense array result; failed array or relation rows prevent construction of the REST page tree and every pagination traversal. |
| The accepted `Pre-N0 repository-protection and verifier-anchor ledger` at protected main | Sole schema, scalar, ordering, nullability, framing, semantic-projection and binding authority | Public fields and identities stay byte-for-byte compatible. A typed field proves only its local grammar; only Packet B's independently tested equality, chronology, pagination and digest relations pin the receipt semantics. |
| Decision #268 comment `5552688364` | OWNER admission of the original fresh six-attempt A/B sequence, extended only by Decision #272 after attempt-2 BLOCK | Phase order, footprint, review independence, hosted evidence and park conditions remain executable delivery gates; only #272 changes the cumulative ledger. |
| Fresh plan attempt 1 at `94a857b128a46ca39fcc254a333dc189f32d4966` and independent semantic review attempt 2 BLOCK | The receipt-tree census omitted the accepted standalone `completeReductionPage` and `rulesetSemantics` records, leaving Packet B no permitted way to validate those raw helper inputs | The corrected census is the cited union of receipt-tree and every public-helper input kind; each kind names exactly one private-A typed parser, every helper invokes its mapped parser, standalone roots receive the no-throw matrix, and B contains neither raw casts nor fragment parsers. |
| Decision #272 body and OWNER comment `5552876743` | The issue records attempt-2 BLOCK at exact `94a857b`; the ruling admits bounded same-file attempt 3, nonauthor review attempt 4, Packet A attempts 5/6 and Packet B attempts 7/8; cumulative ceiling eight, B waits for A, no spend increase | The status, absolute ledger, phase order and review ownership must match that sequence. A second consecutive plan-review BLOCK parks and files a Decision under #272's standing rule; no rename, rebase, correction or hosted infrastructure event resets the ledger. |

The owning run is cited by run and job identity rather than a copied aggregate.
The archived source and Round 447 are inspected through
`archive/iss054-historical-receipt-6e33ec3`/`git show`; that local branch remains
unmodified and unpushed.

## Scope and non-goals

This fresh sub-slice implements only the historical
`repository-protection-receipt/v1` family already fixed by the accepted ledger.
It retains the exact public record shapes, schema version, compatibility rule,
canonical bytes, request/page/reduction/terminal/ruleset/family digest recipes,
permission namespace, protected paths, workflow roles and triggers, producer,
environment, chronology, disposition and stable-bundle ownership. Unknown or
malformed authority input fails closed.

The sub-slice does not accept the archived parser, add or remove a public field,
change a frame domain/tag/order/projection, weaken a scalar/census/relation,
authenticate an opaque digest leaf, prove pagination completeness, observe a
provider, produce a receipt, mutate a ruleset/environment, access credentials,
perform filesystem or network operations, change runtime primitives, grant
authority, select a release, or close ISS-054. Fresh `observedProtection` and the
four-input receipt/anchor binder remain separate later slices with their own
records after Packet B lands. PRs #269/#270 and their eventual disjoint merges
are host-owned; neither packet rebases or merges main without coordination.

## Mandatory parse/relations split

The plan uses the OWNER-permitted one-file module split. Packet A places closed
parsing in new private module
`packages/contracts/src/repository-protection-parse.ts`. Packet B later adds
`packages/contracts/src/repository-protection.ts` for pure relations, digest
composition and the reviewed public API. This is one additional source file and
the only footprint growth from the earlier seven-file source artifact.

The split is required for a sound standalone Packet A. The contracts package
exports only `.` through `src/index.ts`; Packet A does not edit that index or the
registry. Its direct-module structural function is therefore available to the
focused in-repository test and future relations module, but it cannot appear as
generic `parseContract`, serialization, compatibility, schema registration or a
consumer-facing receipt acceptance. Packet A success means only that a detached
tree is locally closed and typed. It makes no full-receipt, authentication,
completeness, freshness or authority claim.

Packet B imports the private typed result and exposes exactly the previously
reviewed public names and no others:

```text
computeGitHubApiCompleteReductionDigest
computeGitHubApiPageRequestDigest
computeGitHubApiRequestIdentityDigest
computeGitHubApiTerminalPaginationDigest
computeRepositoryProtectionReceiptDigest
computeRepositoryProtectionRulesetSemanticDigest
parseRepositoryProtectionContract
parseRepositoryProtectionReceipt
repositoryProtectionPermissionNames
repositoryProtectionPurposes
repositoryProtectionSchemaFields
repositoryProtectionSchemaVersions
```

The public parser always invokes Packet A itself. No exported relation or digest
function accepts a caller assertion that a raw object is already typed. Public
fragment helpers independently invoke the applicable private structural parser
before their pure computation. `index.ts` re-exports only the relations module;
the parse module and its structure-only entry point remain package-private.

## Packet A — closed typed structure

Packet A source is artifact attempt 5 and cannot start until this corrected plan
lands after independent semantic review attempt 4 PASS. Its parser has two
internal stages, both inside the structure-only module:

1. **Closure/type stage.** Snapshot the root once using the repository hostile-
   reflection primitive. Each child record has its own exact parser returning
   `ParseResult<SpecificRecord & ContractRecord>`; each array parser returns a
   typed success/refusal result and invokes the child parser for every dense
   element. The stage may read a successfully closed parent field only to pass
   that value to its child parser. It performs no comparison with another
   record, no `.find`, no projection and no digest. It never casts a failed child
   or inserts `undefined` into a nominal typed row. Any child issue prevents the
   typed receipt tree from being constructed and returns a sorted, frozen
   refusal.
2. **Local structure stage.** Only after every record and array in the complete
   tree has a successful typed result, validate scalar grammars, literal and
   nullable cells, dense bounds, codepoint/UTF-8 order, uniqueness and structural
   censuses over those typed values. Local time comparisons are limited to
   fields in the same typed record, such as receipt issue/expiry and observation
   start/completion. Request and page union branches are classified from each
   record's own detached field census; a raw request discriminator never selects
   another raw record's parser. Any issue returns refusal. Cross-record equality,
   chronology, pagination handoff, semantic projection and digest work remain
   absent.

The Packet-A census is the set union of every accepted receipt-tree record kind
and every accepted public-helper input kind. It is derived from the accepted
ledger at `docs/architecture/contract-decisions.md:6449-6804`, the complete
`repositoryProtectionSchemaFields` object at archive
`6e33ec334201e16f565765e23cebef9e038f906b` in
`packages/contracts/src/repository-protection.ts:52-172`, and that archive's
exported helper signatures and bodies at lines `664-721` and `785-1023`.

The fifteen receipt-tree record kinds map one-to-one to these typed private-A
parsers:

| Accepted record kind | Accepted citation | Sole private-A parser |
| --- | --- | --- |
| `RepositoryProtectionReceiptStructure` | archive `schemaFields.receipt`, lines 53-68; ledger lines 6472-6498 | `parseRepositoryProtectionStructure` |
| `ReviewPolicy` | archive `schemaFields.reviewPolicy`, lines 69-75; ledger lines 6500-6508 | `parseReviewPolicy` |
| `ProtectedPathPolicy` | archive `schemaFields.protectedPathPolicy`, line 76; ledger lines 6510-6514 | `parseProtectedPathPolicy` |
| `Workflow` | archive `schemaFields.workflow`, lines 77-86; ledger lines 6516-6528 | `parseWorkflow` |
| `WorkflowPermission` | archive `schemaFields.permission`, line 87; ledger lines 6540-6569 | `parseWorkflowPermission` |
| `WorkflowTrigger` | archive `schemaFields.trigger`, lines 88-95; ledger lines 6571-6579 | `parseWorkflowTrigger` |
| `HistoricalApiObservation` | archive `schemaFields.observation`, lines 96-107; ledger lines 6581-6598 | `parseHistoricalApiObservation` |
| `RestRequest` | archive `schemaFields.restRequest`, line 108; ledger lines 6600-6609 | `parseRestRequest` |
| `GraphqlRequest` | archive `schemaFields.graphqlRequest`, lines 109-115; ledger lines 6600-6609 | `parseGraphqlRequest` |
| `RestPage` | archive `schemaFields.restPage`, lines 116-126; ledger lines 6611-6647 | `parseRestPage` |
| `GraphqlPage` | archive `schemaFields.graphqlPage`, lines 127-137; ledger lines 6649-6657 | `parseGraphqlPage` |
| `RestLinkRelation` | archive `schemaFields.linkRelation`, line 138; ledger lines 6619-6647 | `parseRestLinkRelation` |
| `TriggeringBuild` | archive `schemaFields.triggeringBuild`, lines 139-147; ledger lines 6744-6751 | `parseTriggeringBuild` |
| `ReceiptProducer` | archive `schemaFields.producer`, lines 148-156; ledger lines 6776-6786 | `parseReceiptProducer` |
| `EnvironmentBinding` | archive `schemaFields.environmentBinding`, lines 157-163; ledger lines 6788-6795 | `parseEnvironmentBinding` |

The schema object then adds the two record kinds that are accepted public-helper
inputs but are not paths in `RepositoryProtectionReceiptStructure`:

| Accepted standalone record kind | Accepted citation | Sole private-A parser |
| --- | --- | --- |
| `CompleteReductionPage` (`ordinal`, `responseDigest`) | archive `schemaFields.completeReductionPage`, line 164, and helper lines 682-706; ledger lines 6710-6719 | `parseCompleteReductionPage` |
| `RulesetSemantics` (`protectedPathPolicies`, `repositoryId`, `reviewPolicy`, `rulesetId`, `workflows`) | archive `schemaFields.rulesetSemantics`, lines 165-171, and helper lines 999-1011; ledger lines 6797-6804 | `parseRulesetSemantics` |

The remaining public-helper argument kinds are also members of the union even
when they reuse a receipt leaf or collection. Each has exactly one private-A
entry parser:

| Accepted helper-input kind | Accepted citation | Sole private-A parser |
| --- | --- | --- |
| `RepositoryProtectionPurpose` | archive purpose enum lines 20-30 and helper lines 665-671; ledger lines 6581-6584 and 6672-6679 | `parseRepositoryProtectionPurpose` |
| `RepositoryProtectionRequest` (`RestRequest | GraphqlRequest`) | archive helper lines 665-671 and branch fields lines 108-115; ledger lines 6600-6609 | `parseRepositoryProtectionRequest`, which dispatches only after detached closure to the one concrete branch parser above |
| `Sha256Digest` | archive helpers lines 672-721; ledger lines 6469-6470 and 6672-6735 | `parseSha256Digest` |
| `NullableRequestCursor` | archive helper lines 672-680; ledger lines 6651-6657 and 6681-6695 | `parseNullableRequestCursor` |
| `CompleteReductionPages` (`CompleteReductionPage[]`) | archive helper lines 682-706; ledger lines 6710-6719 | `parseCompleteReductionPages`, whose dense-array parser invokes `parseCompleteReductionPage` for every row |
| `GitHubApiKind` (`REST | GRAPHQL`) | archive helper lines 707-721 and request fields lines 108-115; ledger lines 6600-6609 and 6721-6735 | `parseGitHubApiKind` |
| `TerminalPaginationPages` (`RestPage[] | GraphqlPage[]`) | archive helper lines 707-721 and page fields lines 116-137; ledger lines 6611-6657 and 6721-6735 | `parseTerminalPaginationPages`, which receives a parsed `GitHubApiKind` and invokes the applicable dense branch-array parser |
| `RepositoryProtectionSchemaVersion` | archive schema-version enum lines 16-18 and contract helper lines 1017-1023; ledger line 6492 | `parseRepositoryProtectionSchemaVersion` |

This exhausts the exported function surface at the archive head. Constants have
no input parameters. Every function parameter is mapped as follows; repeated
semantic kinds deliberately reuse the same parser rather than creating a
helper-specific validator:

| Public helper and archive citation | Input parameters -> private-A parsers |
| --- | --- |
| `computeGitHubApiRequestIdentityDigest` (lines 664-671) | `purpose` -> `parseRepositoryProtectionPurpose`; `request` -> `parseRepositoryProtectionRequest` |
| `computeGitHubApiPageRequestDigest` (lines 672-681) | `requestIdentityDigest` -> `parseSha256Digest`; `requestCursor` -> `parseNullableRequestCursor` |
| `computeGitHubApiCompleteReductionDigest` (lines 682-706) | `requestIdentityDigest` -> `parseSha256Digest`; `pages` -> `parseCompleteReductionPages`; `reducedValueDigest` -> `parseSha256Digest` |
| `computeGitHubApiTerminalPaginationDigest` (lines 707-721) | `requestIdentityDigest` -> `parseSha256Digest`; `apiKind` -> `parseGitHubApiKind`; `pages` -> `parseTerminalPaginationPages` after `apiKind` succeeds |
| `parseRepositoryProtectionReceipt` (lines 785-997) | `input` -> `parseRepositoryProtectionStructure` |
| `computeRepositoryProtectionRulesetSemanticDigest` (lines 999-1011) | `input` -> `parseRulesetSemantics` |
| `computeRepositoryProtectionReceiptDigest` (lines 1012-1016) | `input` -> `parseRepositoryProtectionStructure` |
| `parseRepositoryProtectionContract` (lines 1017-1023) | `schemaVersion` -> `parseRepositoryProtectionSchemaVersion`; for the accepted literal, `input` -> `parseRepositoryProtectionStructure`; unsupported versions retain the public `null` dispatch result |

Array results additionally cover protected-path rows, two workflows, seventeen
permissions, trigger activities, nine observations, 1..64 branch-specific pages
and 0..4 REST Link relations. The implementation may use the archived validators
and censuses as source text, but every returned parent contains only successful
typed children. No `as ContractRecord`, raw-object cast or non-null assertion may
bridge a failed child result into a parent.

Packet A does not export `parseRepositoryProtectionReceipt`, register the schema,
serialize it, or compute any family/relation digest. Its tests assert that the
structure-only fixture succeeds through the private module while the public
index still reports `repository-protection-receipt/v1` unsupported. That is the
standalone soundness boundary: Packet A supplies a safe prerequisite, never an
accepted incomplete receipt.

## Complete structural-input derivation and no-throw matrix

Packet A publishes the renamed test-local constant
`repositoryProtectionStructuralInputCensus20260905`; this census correction is
the only reason for replacing the attempt-1
`repositoryProtectionNestedPathCensus20260905` name. It is derived mechanically
from the exact record and array members under the accepted ledger heading at
main `73ab243ee83d75dbbfb2269b5e629f0dbb4e06f4`, cross-checked against every key
in `repositoryProtectionSchemaFields` and every exported function parameter at
archived head `6e33ec334201e16f565765e23cebef9e038f906b`. It is not a new schema version
or public contract. Derivation starts once at the receipt root and once at every
standalone helper-parameter root, recursively follows every record-valued field,
expands both request/page union branches, adds every array container and element
record, and stops at scalar leaves after adding their reusable helper-input
kind. Future fields or helper parameters cannot silently enter: the test compares
the census to the codepoint-sorted schema field arrays and the explicit
helper-parameter census above and requires a reviewed census-version change.

The complete record-path patterns are:

```text
$
$.reviewPolicy
$.protectedPathPolicies[*]
$.workflows[*]
$.workflows[*].permissions[*]
$.workflows[*].trigger
$.producer
$.environmentBinding
$.apiObservations[*]
$.apiObservations[*].request{REST|GRAPHQL}
$.apiObservations[*].pages[*]{REST|GRAPHQL}
$.apiObservations[*].pages[*].linkRelations[*]       # REST
$.apiObservations[WORKFLOW_RUN].triggeringBuild
```

The complete nested-array patterns are:

```text
$.protectedPathPolicies
$.workflows
$.workflows[*].permissions
$.workflows[*].trigger.activities
$.apiObservations
$.apiObservations[*].pages
$.apiObservations[*].pages[*].linkRelations          # REST
```

The standalone public-helper parameter roots are all present independently, even
when several map to the same reusable parser:

```text
$computeGitHubApiRequestIdentityDigest.purpose
$computeGitHubApiRequestIdentityDigest.request{REST|GRAPHQL}
$computeGitHubApiPageRequestDigest.requestIdentityDigest
$computeGitHubApiPageRequestDigest.requestCursor
$computeGitHubApiCompleteReductionDigest.requestIdentityDigest
$computeGitHubApiCompleteReductionDigest.pages
$computeGitHubApiCompleteReductionDigest.pages[*]
$computeGitHubApiCompleteReductionDigest.reducedValueDigest
$computeGitHubApiTerminalPaginationDigest.requestIdentityDigest
$computeGitHubApiTerminalPaginationDigest.apiKind
$computeGitHubApiTerminalPaginationDigest.pages{REST|GRAPHQL}
$computeGitHubApiTerminalPaginationDigest.pages[*]{REST|GRAPHQL}
$computeGitHubApiTerminalPaginationDigest.pages[*].linkRelations          # REST
$computeGitHubApiTerminalPaginationDigest.pages[*].linkRelations[*]       # REST
$parseRepositoryProtectionReceipt.input
$computeRepositoryProtectionRulesetSemanticDigest.input
$computeRepositoryProtectionRulesetSemanticDigest.input.reviewPolicy
$computeRepositoryProtectionRulesetSemanticDigest.input.protectedPathPolicies
$computeRepositoryProtectionRulesetSemanticDigest.input.protectedPathPolicies[*]
$computeRepositoryProtectionRulesetSemanticDigest.input.workflows
$computeRepositoryProtectionRulesetSemanticDigest.input.workflows[*]
$computeRepositoryProtectionRulesetSemanticDigest.input.workflows[*].permissions
$computeRepositoryProtectionRulesetSemanticDigest.input.workflows[*].permissions[*]
$computeRepositoryProtectionRulesetSemanticDigest.input.workflows[*].trigger
$computeRepositoryProtectionRulesetSemanticDigest.input.workflows[*].trigger.activities
$computeRepositoryProtectionReceiptDigest.input
$parseRepositoryProtectionContract.schemaVersion
$parseRepositoryProtectionContract.input
```

The test expands every receipt occurrence in a canonical nine-purpose fixture,
plus two-page REST and GraphQL fixtures so nonterminal/final page and Link-
relation rows exist. It separately instantiates every standalone root above; the
ruleset-semantic root recursively expands its shared policy/workflow descendants.
At every record occurrence it independently substitutes null, a wrong JSON kind,
a record missing each required member in turn, and an unknown-member record. At
every array occurrence it substitutes null, a wrong JSON kind, a sparse array, a
wrong element kind and a malformed element record. At each non-null scalar root
it substitutes null, a wrong JSON kind and a malformed scalar. Every applicable
case asserts the mapped private-A parser does not throw and returns `ok:false`.
The accepted null `requestCursor` is necessarily a positive; its missing/wrong-
kind/malformed non-null cases refuse without throwing. The WORKFLOW_RUN non-null
triggering-build path is exercised; literal null triggering-build cells for the
other eight purposes remain positive nullability cases rather than false
negatives. Record-only missing/unknown-member mutations and array-only sparse/
element mutations are applied only to those JSON kinds.

Packet B adds a call-path discriminator for every public helper: a malformed
argument must reach its mapped A parser and prevent the pure digest/relation body
from running. The public helper retains its accepted signature and failure
contract, but there is no caller-asserted typed fast path.

The matrix explicitly names the five prior failure instances:

```text
$.environmentBinding = null
$.workflows = [BUILD, BUILD]                         # missing REVIEW census
observation field census completedAt/completeReductionDigest order drift
$.workflows[*].trigger = null                        # archived line 411 path
$.apiObservations[*].pages[*].linkRelations = null   # archived line 511 path
```

The first, second, fourth and fifth rows must return refusal without throwing.
The third is pinned by exact codepoint-sorted source/fixture/registry census
comparison. Committed deletion mutants remove each typed-child gate in turn;
the matching path mutation must then throw or pass and therefore fail the suite.

Packet A also retains the archived mutable/sealed/frozen positives and hostile
proxy, getter, symbol, hidden field, subclass, cross-realm, hole, extra index,
custom iterator, descriptor trap and cycle negatives. Trap counters must remain
zero. Scalar/local vectors retain the exact safe decimal, SHA-256, timestamp,
UTF-8 name/path/ref/cursor, ASCII ETag/route, literal/nullability, array bound,
permission namespace/access, disposition and local time boundaries. Structural
census vectors cover 1/64 and 0/65 path/page bounds, exactly two roles, seventeen
permission rows, nine purposes, request/page branch field censuses, relation-name
order/uniqueness and the exact observation member order. These tests prove
closure and typed structure only.

After author static gates, a nonauthor performs exact-head review attempt 6.
A BLOCK, a surviving malformed-path counterexample, or any footprint growth
parks the new sub-slice. A PASS permits an ordinary Packet A PR and hosted
three-OS bootstrap. Packet B remains forbidden until that PR is merged and
protected `main` is green.

## Packet B — relations, pagination and identities

Packet B source is artifact attempt 7 and starts only from protected main after
Packet A lands. It adds the public relations module and consumes only
private-A parsed values returned by the exact parser census above, including
`ParsedRepositoryProtectionStructure` and the typed standalone helper inputs.
Packet B contains no fragment parser, raw-object cast, non-null assertion over
raw input, or caller-asserted typed fast path. Every public helper invokes all
applicable A parsers before its pure computation; a branch parser receives only
a successfully parsed discriminator. Type correctness is necessary but never
treated as relational proof. Packet B independently checks:

- protected-path and workflow ordering/uniqueness; exact BUILD and REVIEW role
  selection; trigger event/activity/conclusion/source equality; producer bound
  to REVIEW; completed successful BUILD bound to WORKFLOW_RUN; and distinct run
  identities;
- repository/ruleset/anchor/environment equalities and all cross-record
  chronology, including variable publication before producer, producer/page/
  observation/issue order, strict expiry and triggering BUILD completion;
- nine-purpose uniqueness and request-identity uniqueness; REST/GraphQL request
  branch agreement; 1, 2 and 64-page chains; ordinal adjacency; REST relation-
  name and global request/NEXT uniqueness; exact optional FIRST/PREV/LAST
  targets and boundaries; mandatory nonterminal NEXT; no `A -> B -> A`; and
  GraphQL cursor acquisition, non-repetition and terminal state;
- exact request, first/GraphQL page, complete-reduction, terminal-pagination,
  seven recomputable reduced-value, ruleset-semantic and family digest recipes;
  response, raw Link, later REST-request and historical PR/review reductions
  remain explicitly opaque claims.

Relations receive only parsed records, scalars and dense arrays and return a
typed issue list. No raw input reaches a relation, projection, pagination walk or
digest frame. Only a zero-issue relation result produces public parser success.
Generic parse, canonical-byte parse, framed serialization, registry vocabulary,
compatibility and package export are added together in Packet B, so there is no
interval where the public family accepts a structure-only value.

Packet B reuses the archived independent literal goldens and one-axis mutation
vectors where the accepted bytes are unchanged. It adds deletion mutants for
every cross-record equality, chronology, branch join, page handoff, terminal
cell, projection member, frame tag/order and opaque-leaf enclosure. Positives
cover all three dispositions structurally while only `ACCEPTED` remains eligible
for the later binder. No positive claims API origin, completeness,
authentication, operator action or authority.

After all local static gates, a different nonauthor performs exact-head review
attempt 8. A BLOCK, surviving relation counterexample or footprint growth parks
the sub-slice with no implicit further attempt. A PASS permits the ordinary
Packet B PR and hosted Linux/macOS/Windows bootstrap. Source authors, reviewers,
parsers, fixtures and CI never certify themselves or grant authority.

## Exact source footprint

The parse/relations split is the sole justified growth from the prior seven-file
source footprint. Round 451 is the single cumulative attempt record for this
fresh sub-slice and is updated by Packets A and B; neither packet creates another
pressure file.

Packet A changes exactly five files:

```text
packages/contracts/src/repository-protection-parse.ts       NEW
test/contracts/repository-protection.test.ts                NEW
packages/conformance/src/iss002-bundle-paths.mts            EXISTING
planning/drafts/ISS-054.md                                  EXISTING
planning/pressure-tests/2026-09-05-round-451.md             EXISTING
```

Packet B changes exactly seven files:

```text
packages/contracts/src/repository-protection.ts             NEW
packages/contracts/src/repository-protection-parse.ts       EXISTING, unchanged unless review finds an A defect (which parks)
test/contracts/repository-protection.test.ts                EXISTING
packages/contracts/src/index.ts                             EXISTING
packages/contracts/src/registry.ts                          EXISTING
packages/conformance/src/iss002-bundle-paths.mts            EXISTING
planning/drafts/ISS-054.md                                  EXISTING
planning/pressure-tests/2026-09-05-round-451.md             EXISTING
```

The Packet B list contains eight entries because it shows the unchanged typed
input module explicitly. Its actual changed-file census is seven: the unchanged
parse module is read-only input. Across both source packets the cumulative
distinct implementation footprint is exactly eight files: the earlier seven
roles plus the one permitted parse module. Any need for a second test, another
source module, runtime/package/workflow change, or another pressure record is
footprint growth and parks before source.

## Acceptance evidence and verification

Plan correction attempt 3 changes only this file. Before independent semantic
review attempt 4 and before its ordinary PR, its exact head must pass:

```text
pnpm typecheck
pnpm format:check
pnpm planning:check
pnpm planning:board-check
git diff --check
```

The same four pnpm gates and diff check run at the exact Packet A and Packet B
source heads before their respective independent reviews and PRs. If dependency
materialization is required, the only admitted command is
`pnpm install --offline --frozen-lockfile --ignore-scripts`. No local Vitest,
test, module, native compiler/addon, probe, lock experiment, provider operation
or admin mutation runs. Executable contract evidence comes only from the
ordinary hosted bootstrap on Linux, macOS and Windows after independent review
PASS.

Packet A acceptance evidence is the complete versioned receipt-tree plus
standalone-helper union derivation, typed result census, no-throw mutation
matrix, trap non-execution, detached/frozen success, exact local literals/
scalars/nullability, local chronology and structural censuses, plus proof that
the public schema remains unsupported.
Packet B acceptance evidence is the public surface/registry/compatibility census,
canonical bytes and independent frame goldens, all pure relation and chronology
joins, 1/2/64 pagination with 0/65 and cycle refusals, opaque-leaf enclosure,
and meaningful deletion mutants. Hosted success does not replace either
independent exact-head review.

## Review attack surface

Attack an issue accumulator that continues into another record; a nullable or
malformed child cast as typed; `.find` on a failed array; a discriminator read
before the record's own detached closure; array elements partially accepted;
unknown fields or reflection traps reached before snapshotting; a path omitted
from the versioned derivation; a public-helper input omitted because it is not a
receipt path; a nullable positive incorrectly turned into a blanket null
refusal; codepoint and UTF-8 order conflated; or census checks made against
hand-written recall rather than the accepted fields and helper signatures.

Attack Packet A as an incomplete public receipt parser; any index, registry,
compatibility, serializer, digest or authority exposure in A is a stop condition.
Attack Packet B with raw casts, a fragment parser, caller-asserted typed values,
a public helper that bypasses its A parser, typed fields treated as equality
proof, partial pagination, repeated/cyclic requests, wrong Link or cursor
handoffs, caller page counts as terminal evidence, weakened
BUILD/REVIEW/producer chronology, semantic projection drift, frame tag/order
drift, opaque leaves described as authenticated, or parser success described as
freshness, completeness, operator action or authority.

Attack the delivery record through stale-head review, author self-review,
starting B before A lands, source before plan review/landing, treating hosted
failure as a free semantic repair, adding an implicit attempt, rebasing onto
host-owned PR work, changing the archive, pushing an archive branch, expanding
the exact footprint, weakening existing guards/tests, or closing ISS-054 before
the later fresh-observation and binder records land.
