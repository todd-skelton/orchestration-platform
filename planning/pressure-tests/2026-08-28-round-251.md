# ISS-022 portable-probe-core final repair review — round 251
`PASS`.
The authority-row test now normalizes tracked text, requires exactly one uniquely introduced fence, and compares its complete capture to all 21 canonical records joined in registry order with the fixed final LF.
Reorder, duplicate, extra, missing, delimiter/fence, row-byte, and final-LF mutations therefore fail equality rather than surviving independent substring checks.
The repaired vector test passes 5/5; corpus, `predecessorHex`, parser mutants, stable-only trust boundary, and scaffold scope are unchanged from the otherwise-clean Round 250 review.
Proportionality: this is one bounded test correction with no executor, helper, workflow, isolation, or production authority.
