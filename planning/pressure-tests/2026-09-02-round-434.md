# Pressure round 434 — ISS-011 manifest and planning definition

Status: bounded consumer-backed definition candidate. No module source,
registry, build output, runtime invocation, route, dispatch, mutation, release,
or authority action is claimed.

## Frontier and scope

Protected base is `a92c07b65f0553a20fca9cd6ee6f29a42c03631a`. ISS-013 is
closed and its two behaviorally distinct fixture adapters plus the public module
records and walking-skeleton consumer are executable. ISS-011 is runnable, but
source would still have to invent its private manifest row/build order and the
first module's literal action policy. This packet defines only those two inputs
before implementation.

Delivery, review, and repair semantics remain undefined until their own source
consumer tranches. The `planning` definition reuses the exact branch/queue
adapter tuples and `work.read` facts already exercised by the walking skeleton.
It adds no hypothetical adapter, capability, schema, result arm, or authority.

## Proportionality and removal evidence

| Mechanism | Concrete threat and deletion test | Smallest alternative |
| --- | --- | --- |
| Closed manifest row | a moved/substituted source or emitted module keeps a copied descriptor digest; delete one path/digest/projection equality | one private canonical row with direct projections; no public registry schema |
| Normalized source census | checkout line endings or an unlisted helper change installed bytes; omit one tracked file or normalized-byte hash | reuse the repository tracked-text normalizer and canonical census; no archive format |
| Sealed serial build | a source changes during emission or a partial output set becomes build input; mutate an earlier row while a later row builds or fail during publication | one generator, retained source bytes, fixed order, per-file atomic writes, registry-last commit marker and final census; no daemon or transaction service |
| Exact planning policy | consumer fields, locale, clock, routing, or arbitrary scoring chooses work; reorder equivalent neutral facts or remove one eligibility comparison | first canonical READY `work.read` row under exact NO_TRIP, using landed inputs only; no expression language or priority framework |
| Closed descriptor/catalog | a worker action lacks renderer inputs or review requirement; remove/change one action, tuple, directive, role, or flag | one action and existing directive catalog/accessor vocabulary; no new schema or template payload |

## Acceptance, predicted footprint, and review

The definition is falsified if the source packet needs a new public family,
manifest-provided build option, dynamic path/import, extra capability, consumer
field, host/model/route choice, time/random input, or authority Boolean. The
cheapest later evidence is exact canonical manifest/descriptor/action goldens;
single-field and path/digest/projection mutants; late source-change and partial-
publication controls; repeated byte equality; forbidden import/effect mutants;
and equivalent branch/queue facts producing identical output on three OSes.

This packet touches only the ABI document, ISS-011 draft, and this record.
Verification is focused formatting, `planning:check`, live
`planning:board-check` after issue reconciliation, `git diff --check`,
independent exact-head review, and hosted three-OS bootstrap. Source predicted
for the next packet is one quarantined planning entrypoint and focused tests;
the empty manifest/generator/generated stub remain unchanged until all four
baseline entrypoints exist. A wider first source packet is replan evidence.

Review should attack circular/self-authored digests, source normalization that
misses executable bytes, path aliases, build TOCTOU/partial replacement,
descriptor/manifest projection disagreement, a planning rule that treats
NO_TRIP or parsed facts as runtime authority, unstable first-row selection,
missing required directive/catalog rows, consumer vocabulary, and any claim
that definition or build output makes a module installed or authorized.

## Exact-head review follow-up

The first independent pass accepted the manifest closure and proportionality
but found two claim gaps. The repair now makes every public structural or
relational input refusal—including missing compatibility, incomplete policy
census, and invalid module intent—a caller failure with no fabricated digest or
result; bound INPUT_REFUSED is reserved for parse-valid but unusable inputs.
It also requires the first source tests to use the actual exported
walking-skeleton preparation functions, ISS-013 branch/queue readers, retained
parsed input, and post-await result binding. The concrete matrix pins branch
TRIP to no action, queue NO_TRIP to implementation action, and the common
all-READY NO_TRIP decision projection across both adapters. No source,
manifest, generator, schema, or authority surface changed.
