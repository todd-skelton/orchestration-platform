# ISS-006 checkout-EOL review — round 237

`PASS`.

Run `33136364412` isolates the blocker to Windows stable-byte refusal; Linux and macOS passed and downstream authority remained skipped.
All 96 harness/test census paths now resolve to `text eol=lf`; the ten new rows are the only previously uncovered paths and their Git blobs are already LF.
Those rows are textual package, lock, workspace, TypeScript, and config inputs; no binary or out-of-census generated artifact is classified.
The change fixes checkout bytes only: schemas, manifests, reducers, provider records, review gates, and promotion authority are unchanged.
Focused evidence: `stable-bundles.test.ts` passes 6/6 and `git diff --check` passes.
Proportionality: ten explicit attributes plus one census-wide regression test are the smallest durable cross-OS repair; no runtime normalizer or authority surface is added.
