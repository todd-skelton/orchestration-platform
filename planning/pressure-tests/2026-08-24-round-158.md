# ISS-002 pressure test — round 158

## Review basis

This is a fresh independent repair/removal review of the recovery-attempt LIVE
descriptor evidence at exact initially clean and pushed head
`060b60bef80af62af48ac42e6e85d0e72988a353` on
`iss-002-versioned-contracts`, with exact parent `64178fc`. Local `HEAD` and the
read-only `ls-remote` result for `origin/iss-002-versioned-contracts` both
resolved to that exact commit before review and again after verification.

The authority basis was `AGENTS.md`, `docs/planning/work-model.md`, the sole
ISS-002 authority in `docs/architecture/contract-decisions.md`, the registered
ISS-002 draft, Round 157, the exact `64178fc..060b60b` delta, and the unchanged
descriptor/reservation implementation. Parent reasoning and prior green runs
were not treated as evidence. No Chase Sets repository, project, issue, board,
backlog, source, or other consumer system was read or touched.

The exact repair changes only
`test/contracts/attempt-descriptor.test.ts`: 46 insertions and no deletions. It
adds no production, authority, schema, digest, registry, package, dependency,
command, runtime, roadmap, or board-topology change. `git diff --check
64178fc..060b60b` passed.

## Disposition

`PASS` for the repaired recovery-attempt LIVE descriptor implementation
evidence at exact reviewed head
`060b60bef80af62af48ac42e6e85d0e72988a353`.

All five Round 157 evidence blockers are removed by the prescribed test-only
repair. The exact formerly surviving source mutants were rerun unchanged and
were killed for independent, intended reasons:

1. descriptor unknown-field closure deletion fails the new pure-extra
   descriptor assertion;
2. predecessor-key locator unknown-field closure deletion fails the new
   pure-extra three-field locator assertion;
3. hard-coded locator source fails the exact alternate-source path assertion;
4. hard-coded locator transaction fails the exact alternate-transaction path
   assertion; and
5. descriptor vocabulary registry deletion fails the exact registry census
   assertion.

No definite semantic, trust-model, proportionality, dependency, or remaining
deletion-grade evidence blocker was found. The LIVE descriptor production
implementation remains the exact surface cleared by Round 156.

## Round 157 blocker removal

### Descriptor pure-added closure

The repaired test now supplies all eight valid descriptor fields plus
`unknown: true` and requires parser refusal. This isolates the unknown-member
comparison from missing, scalar, and renamed-member failures.

Deleting only the parser's unknown-field issue now causes that input to parse
successfully and fails the exact assertion. The mutant is killed without any
confounding missing member or malformed scalar. Existing remove, null,
cross-type, rename, schema, lifecycle, digest, UUID, and source cases remain
unchanged.

### Exact three-field predecessor-key locator closure

The repaired test defines the exact valid locator record:

```text
reservationPredecessorKey
sourceToken
transactionId
```

It now proves:

- a fourth `extra` field refuses;
- deleting each required field refuses; and
- null, number, boolean, and array substitutions for each field refuse.

The exact Round 157 projection mutant—which copied only the expected
properties into a new object before closed parsing—was rerun unchanged. It
silently discarded the pure extra field and therefore failed the new
`toThrow()` assertion. The closure mutant is killed for unknown-member
admission, not by a scalar or missing-field check.

### Locator source and transaction sensitivity

The new source case keeps transaction and predecessor key fixed, changes only
the source to `cleanup-gate-pre-fence`, and pins the exact whole reservation
path. Hard-coding the implementation's source parent to `recovery-fence`
therefore fails only the changed source segment; key and transaction remain
identical.

The new transaction case keeps source and predecessor key fixed, changes only
the UUID, and pins the exact whole reservation path. Hard-coding the parent
transaction to the original fixture UUID therefore fails only that segment.

Both exact Round 157 mutants were rerun unchanged and killed. These assertions
use exact equality rather than broad inequality, so no sibling path component
can hide deletion of the target input. Existing malformed key/source/
transaction validation, key sensitivity, and byte-identical delegation from
predecessor evidence remain intact.

### Vocabulary registry census

The repaired test now asserts that
`schemaVocabularyDefinitions['recovery-attempt-descriptor/v1']` equals exactly:

```text
schemaVersion: recovery-attempt-descriptor/v1
fields: recoveryAttemptDescriptorSchemaFields
closedValues: [LIVE]
```

Deleting the vocabulary registry definition now returns `undefined` and fails
that assertion. The same exact mutant that previously left descriptor plus
vocabulary tests green is killed. The pre-existing `schemaVersions` assertion
continues to distinguish compatibility registration separately.

## Mutation evidence

All five source mutants were applied only through isolated per-process
pre-transform Vite plugins. Every plugin matched the same exact production
anchor used in Round 157, ran the named focused tests, and disappeared when its
Node process exited. No repository file, configuration, generated artifact,
dependency, or cache registration was edited.

| Mutant | Repaired result | Independent failure reason |
| --- | --- | --- |
| Descriptor unknown-field check deleted | KILLED | Pure-added ninth member parses |
| Locator unknown fields projected away | KILLED | Pure-added fourth locator member is ignored |
| Locator source fixed to `recovery-fence` | KILLED | Exact alternate source parent differs; transaction/key unchanged |
| Locator transaction fixed to original UUID | KILLED | Exact alternate transaction parent differs; source/key unchanged |
| Descriptor vocabulary definition deleted | KILLED | Exact registry entry is absent |

No transform-anchor or harness failure was counted as evidence.

## Unchanged descriptor semantics and trust boundary

The production implementation is unchanged. The cold review reconfirmed:

- one exact eight-member `recovery-attempt-descriptor/v1` record;
- exact LIVE-only runtime and TypeScript lifecycle surfaces;
- canonical UUIDv7 attempt/transaction identities;
- exact two-source enum;
- SHA-256 process observation, reservation predecessor key, and reservation tip
  identities;
- exact transaction/source/attempt descriptor path with movement resistance;
- one canonical framed `Ddescriptor` domain and tagged golden;
- exact generic canonical serialization and reordered-byte refusal;
- descriptor schema-version and vocabulary registration;
- hostile parser totality;
- exact three-field crash-recovery reservation locator;
- byte-identical delegation from selected predecessor evidence; and
- public absence of READY_ONLY, copied proof triples, descriptor-input/
  launch-definition digests, raw launch/process vocabulary, observation/
  launch/log/attachment/archive schemas, and runtime authority.

The descriptor remains structurally sufficient to reconstruct the selected
reservation after a crash without directory enumeration:

```text
transaction + source + predecessor key -> reservation path/Dp
reservation tip digest                 -> exact selected RESERVED snapshot
```

Later family composition must still authenticate the selected reservation,
attempt ID, predecessor lineage/key, process observation, and chronology. A
well-formed descriptor, key, tip digest, observation digest, or parser result
grants no process, capability, mutation, currentness, review, or promotion
authority.

The graph remains acyclic: prior terminal/genesis precedes selected RESERVED,
which precedes process observation and immutable LIVE descriptor, which
precedes any future selected IN_PROGRESS log. Neither current descriptor nor
current log enters its reservation key.

No observation producer, launch pointer, attempt log, attachment, archive,
tombstone, persistence, selected-family/currentness implementation,
proposal/CAS, capability, process operation/adoption, broker call, command,
package, runtime service, release review, or self-certification path is added.
Only stable external authority and later authenticated composition may grant
use. Candidate output is never routed into its own promotion.

The one-line older reservation-test removal remains narrow: the descriptor
field-census export is now positively pinned by the dedicated descriptor test,
while all unrelated reservation and descriptor exclusions remain.

## Proportionality and deletion table

| Mechanism | Concrete threat | Exact-head evidence | Smallest disposition |
| --- | --- | --- | --- |
| Closed eight-field descriptor | A proof-bag ninth member alters detached meaning. | Pure-extra case kills unknown-field deletion. | Retain one added assertion. |
| Closed three-field locator | Caller supplies ignored locator proof fields. | Pure-extra case kills projection/widening mutant; missing/type matrix pins input. | Retain focused helper matrix. |
| Locator source relation | Crash lookup uses a fixed wrong recovery source. | Fixed-source mutant killed with transaction/key constant. | Retain one exact moved-source path. |
| Locator transaction relation | Crash lookup uses a fixed wrong transaction. | Fixed-transaction mutant killed with source/key constant. | Retain one exact moved-transaction path. |
| Vocabulary registry census | Descriptor fields evade denylist lint by registry omission. | Definition deletion killed by exact entry/fields/LIVE assertion. | Retain one registry assertion. |
| Descriptor/key/tip design | Crash cannot locate identity and selected RESERVED snapshot. | Prior path, key, delegation, digest, and scalar evidence stays green. | Retain unchanged; no index or copied triple. |
| Stable external authority | Structural bytes self-certify or adopt a process. | Test-only delta adds no production or runtime surface. | Retain later composition/ISS-004 authority. |

The repair adds only the cheapest discriminating evidence and no mechanism.

## Verification evidence

All processes ran strictly one at a time. The five mutation processes fully
exited before the unmodified focused baseline. Focused and compatibility
processes fully exited before required gates. The standalone full suite fully
exited before the separate bootstrap verifier began. Direct checked-in scripts
or lockfile-pinned binaries avoided the known signed package-manager launcher
infrastructure dependency; no semantic gate was substituted. Test processes
used the increased 600,000 ms test and hook ceilings.

Mutation evidence:

1. descriptor unknown-field closure deletion — KILLED;
2. locator unknown-field closure deletion — KILLED;
3. locator hard-coded source — KILLED;
4. locator hard-coded transaction — KILLED; and
5. descriptor vocabulary registry deletion — KILLED.

Focused and compatibility evidence:

1. `node node_modules/vitest/vitest.mjs run
   test/contracts/attempt-descriptor.test.ts
   test/contracts/reservation-identity.test.ts
   test/contracts/reservation-value.test.ts --testTimeout=600000
   --hookTimeout=600000` — PASS: three files and 19 tests in 1.64 seconds.
2. `node test/capability-slots/ISS-002/contracts%3Acompatibility-check.mjs
   --testTimeout=600000 --hookTimeout=600000` — PASS: one file and one test in
   815 milliseconds.

Required gates:

1. Direct equivalent of `pnpm run planning:check`, `node
   scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`, `node
   scripts/planning/board-check.mjs` — PASS: `board contracts verified`; it
   read only this repository and its registered delivery project.
3. Direct equivalent of `pnpm run format:check`, `node scripts/format.mjs
   check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`, `node
   node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`, `node
   node_modules/vitest/vitest.mjs run --testTimeout=600000
   --hookTimeout=600000` — PASS: 38 files and 413 tests in 80.73 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`, `node
   scripts/verify/bootstrap.mjs` — PASS: manifests/registries, formatting,
   typecheck, 38 files and 413 tests in 94.65 seconds, private compositions,
   planning contracts, and source-tree status invariance.

No mutation, focused, compatibility, full-suite, or bootstrap process
overlapped another.

## Authorization boundary

This clean removal round clears the bounded recovery-attempt LIVE descriptor
implementation and evidence at exact reviewed head
`060b60bef80af62af48ac42e6e85d0e72988a353`.

It does not clear process-observation semantics/production, selected
reservation family composition, launch pointer, attempt log, attachment,
archive/tombstone, persistence, proposal/CAS runtime, capability issuance,
process operation/adoption, broker calls, release promotion, or candidate self-
certification. Any such widening requires its own reviewed authority.

After this report is written, the sole intended worktree change is
`planning/pressure-tests/2026-08-24-round-158.md`; no source, test, authority
document, draft, issue, roadmap, board, or other planning file is edited, and
no commit is created.
