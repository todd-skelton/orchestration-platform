# ISS-006 Windows admission post-timeout landing review — round 206

## Disposition

`PASS_TO_LAND_WINDOWS_SLICE`.

This is a fresh independent exact-tree removal review after the sole
post-Round-205 change. The current private Windows refusal-only admission slice
remains clean to land against pushed HEAD
`c3de3212ddb616ceafb7508917a43bf62c414e96`, the sole ISS-006 authority, and
Rounds 175 and 197–205.

The only later delta adds a test-local `120_000` millisecond Vitest timeout to
the existing ISS-002 1,000-record cross-root walk case. It changes no workload,
assertion, implementation, authority behavior, failure policy, retry policy, or
runtime timeout. The exact Round-205 Windows implementation, PE, fixture, and
landing evidence are unchanged.

This disposition remains narrow. It authorizes landing the private, suspended,
refusal-only Windows slice and this bounded test scheduling adjustment. It does
not resume candidate code, grant runtime authority, complete ISS-006, or relax
the required macOS evidence.

## Exact reviewed tree

```text
HEAD c3de3212ddb616ceafb7508917a43bf62c414e96

d5cc13c55c45df92adc3a62d841c54657c3b54ba07c4e944bb134a5639d0eb9f  test/conformance/walk.test.ts
ba288f80ce01d0064a0810dfa02cce72e51a3a0a603ed213ac13b32cbef04050  planning/pressure-tests/2026-08-26-round-205.md
215b011af27cd6162644b56a558624368e929e8701fd87c2bfe8c2865b5c95be  packages/conformance/src/windows-isolation-broker.c
712f0280cfbda59582849efc441bc458fcac33ee69857ae764efd969d75fa504  packages/conformance/src/windows-isolation-broker-x64.exe
066353a70969ff152959adf4e8c731e38c7b8d8dc7fcb3b7a5957dc198469a31  scripts/build/windows-isolation-broker.mjs
54e3108b5994d5dffe81f52af69d290db2c5c31c13b577c2608cf88abd38d652  test/conformance/windows-isolation-broker-fixture.c
7ee37ec526a208a925e35883ec680b7691108090c3f755ab66cce5b9b08ae038  test/conformance/windows-isolation-broker.test.ts
3c0e366611a62257fab3f678464f2201d9174ef7f4f5c42ed29d01023e004426  test/conformance/windows-appcontainer-profile.test.ts
```

The tracked PE remains exactly 90,112 bytes. I read the repository
instructions, work model, current diff, the complete changed walk test, Round
205, the unchanged exact Windows implementation/test hashes and package
boundary, and the relevant ISS-006 trust constraints. I did not edit
implementation, run the opt-in mutating native diagnostic, or access Chase
Sets.

## Timeout delta review

The complete post-Round-205 diff is:

```diff
-  });
+  }, 120_000);
```

It applies only to:

```text
ISS-002 1,000-record walk
  builds stable and candidate contracts from separate roots into an external
  execution root
```

The case still:

1. constructs the same candidate root;
2. constructs the same external execution parent;
3. calls `runIss002CrossRootWalk` with the same stable, candidate, and external
   roots;
4. throws on every returned issue;
5. requires `result.ok === true`; and
6. requires the execution parent to be exactly empty afterward.

No assertion was removed, weakened, skipped, retried, made advisory, or wrapped
in `continue-on-error`. The 1,000-record cardinality and all stable/candidate
root separation remain. The timeout is a finite two-minute test-runner ceiling,
not a production walk interval, candidate duration allowance, protocol value,
or evidence field.

The authority's independent per-walk five-second measured interval remains in
production and its dedicated tests. Increasing this integration test's outer
wall-clock allowance under full-suite contention does not change that stable-
owned semantic bound or allow candidate-reported timing.

The adjustment is proportional: it localizes scheduling tolerance to the one
known heavy test rather than widening global suite timeouts. A hang still fails
after a finite bound, while normal failure/refusal and cleanup observations
remain synchronous and exact.

## Round-205 landing disposition remains intact

All authority-bearing Windows files are byte-identical to the clean Round-205
tree. Therefore the accepted removal evidence remains unchanged:

- exact original/grant recovery and third-profile refusal;
- durable OPWL branch/publication/retry semantics;
- exact Dgrant/Dlaunch and suspended atomic process attributes;
- closed token, Job, access, and impersonated-open observations;
- complete partial-runtime cleanup and root-first restoration;
- exact admission result reducer for 0, 1, and -1; and
- final absence before execution/profile cleanup.

Production still contains no `ResumeThread`; candidate/RPC bytes do not execute
in this slice. Candidate output supplies no verdict. There is no receipt,
aggregate, provider result, authority, certification, installation, selection,
or promotion path. The broker remains private and outside package-root exports.

No ISS-006 draft, sole ledger, roadmap, or board amendment is warranted. The
timeout changes test scheduling only. macOS remains required for ISS-006
completion and hosted PASS.

## Checks actually run

```text
git diff -- test/conformance/walk.test.ts
  PASS — exact one-line test-local timeout delta only

exact hash/status and Round-205 equality census
  PASS — all Windows implementation/PE/fixture/test hashes are unchanged

pnpm exec vitest run test/conformance/walk.test.ts
  PASS — 1 file, 14 tests
  Duration 23.39 s; tests 21.96 s

pnpm exec tsc --noEmit -p tsconfig.json
  PASS

node scripts/planning/check.mjs
  PASS — planning contracts verified

node scripts/planning/board-check.mjs
  PASS — board contracts verified

pnpm exec prettier --check planning/pressure-tests/2026-08-26-round-206.md
  PASS — all matched files use Prettier code style

git diff --check
  PASS — no errors; current Windows/walk implementation-test files have
         existing LF-to-CRLF notices
```

The focused walk completed beneath the default 30-second contention threshold
on this run; the new 120-second ceiling was not needed to obtain the pass.

## Landing authorization

`PASS_TO_LAND_WINDOWS_SLICE` remains the clean latest removal disposition for
the exact tree above. It authorizes landing the private refusal-only Windows
admission slice and the bounded local timeout adjustment. It grants no runtime
authority, public contract, ISS-006 PASS, receipt, or promotion capability.
