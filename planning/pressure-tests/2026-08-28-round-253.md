# ISS-022 raw-filesystem-executor pressure review — round 253
`BLOCK_REPLAN`.
The O_EXCL/write/sync/readback, persistent lock/no-retry owner-death, replace barriers/rename/dirsync, locked CAS, and same-lock absence sequences are literal; roots remain external, cleanup is parent-owned, no PASS/profile token exists, and 10/10 focused tests pass.
The hostile raw-event boundary is underclosed: the parser checks field shapes but not mode/event/readback arms, and the parent never equal-binds the parsed `mode` or barrier to its requested mode/arguments.
It therefore accepts impossible evidence such as `ABSENCE+SELECTED`, `CAS+CREATED`, `SELECTED` with null/`41`, `PREDECESSOR_MISMATCH` without readback, or a REPLACE child reporting a different requested barrier.
These records grant no authority yet, but later stable normalization cannot safely distinguish a raw primitive fact from a syntactically valid child fabrication.
Minimal repair: close event arms per mode and require parent equality to requested mode/barrier/CAS inputs; add those mutants while retaining ERROR/timeout/Windows-unsupported as raw non-PASS.
Proportionality: no executor expansion, retry, lock deletion, PID/age logic, helper, workflow, or additional vector family is needed.
