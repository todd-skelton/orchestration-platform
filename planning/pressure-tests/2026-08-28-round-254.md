# ISS-022 raw-event repair follow-up — round 254
`PASS`.
Each mode now admits only its exact raw event arms; `CREATED=41`, `SELECTED=42`, mismatch requires a readback, and every unused readback/error/barrier/absence field is forbidden.
The execution parent enforces exact 0/1/2 argument counts, equal-binds parsed mode and requested replace barrier, and requires CAS mismatch readback differ from the predecessor and selection equal the proposal.
Named hostile mode/event/readback/barrier/CAS mutants refuse; the focused suite passes 10/10 while ERROR, malformed output, timeout, and Windows-unsupported codes remain raw non-PASS evidence.
Trust and proportionality remain intact: no PASS/profile authority, retry, lock deletion, PID/age inference, helper, workflow, or added vector family.
