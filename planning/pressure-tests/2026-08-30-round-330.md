# ISS-022 bundled worker paths — round 330

Disposition: `PASS` for this two-file packaging repair; no blocking findings.
This is not hosted acceptance, capability selection, or promotion authority.

## Exact review coordinates

- Branch: `codex/iss-022-hosted-worker-paths`.
- Baseline/HEAD: `78004217f75b28f1ae0e4d0ce0e19487ea289f49`.
- Reviewed uncommitted `scripts/conformance/run-bundled.mts` SHA-256:
  `1bf89b52a5374c64b894563ba82e31c7bff58fb2561e36e08cbf2fb9eb97f37b`.
- Reviewed uncommitted `test/conformance/workflow-structure.test.ts` SHA-256:
  `367663656f898177b1246d6d63501b5a1e96181f817262ec621d02d20a327e51`.
- Delta: 136 insertions, 36 deletions; no other implementation changes reviewed.
- New blocker supplied with dispatch: run `33333610856`, attempt `1`, stable
  baseline above, candidate `edd01a01684da749cfe66756a27402a71cedb7bf`;
  all three OS observations refused with `observation:suite.suite:execution-refused`
  before artifacts. Provider evidence was not independently refetched here.

## Independent findings and evidence

Read AGENTS.md, work-model.md, Round 234, ISS-022, and the trusted-harness
ledger's stable-census, hosted-topology, exclusions, and ISS-022 scope authority.
Static review and `git diff --check` only; no tests, bootstrap, or hosted run.
The author's reported 44 passing tests are not independent review evidence.

`run-bundled.mts:17-48` limits URL preservation to two exact stable source
paths, not every matching basename or candidate entrypoint. Both executors and
their worker sources already belong to the stable harness census. Hosted CLI
targets remain closed; stable checkout, candidate separation, pre/post bundle
checks, allowlisted child environments, and receipt authority are unchanged.

The original temporary-bundle URL relocates both worker lookup and the parent
source-overlap calculation. Preserving the reviewed modules' source URLs repairs
both without copying worker bytes or deleting the existing canonical guard.
`workflow-structure.test.ts:37-98` uses the production bundle builder, executes
outside the checkout, checks exact filesystem bytes and runtime replay, and
rejects source root, descendant, and ancestor custody paths. Removing either
module's URL repair or deleting the overlap guard is discriminated by those
assertions; this is static test analysis, not an executed mutation claim.

## Bounded proportionality

Threat: relocated worker/source paths prevent the admitted hosted probe from
running or misidentify protected source. Deletion restores that measured path
failure. The smallest credible alternative is injecting explicit paths into
executor APIs; the two-file packaging-only URL binding avoids that wider API
change. The exported builder is the CLI's own path, not a parallel test bundler.
No broker, native binary, same-host isolation, new schema, or policy is added.
