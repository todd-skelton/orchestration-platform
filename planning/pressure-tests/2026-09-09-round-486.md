# Pressure round 486 - repair review-location authority

Date: 2026-09-09. ISS-085/#348 is registered under EPIC-KERNEL/M2 and blocked
by ISS-078/#338. Pilot `iss085-review-location-20260909` begins implementation
1/4 at exact source base `5d25a402f0c76a86df1469c7ff5d4c7a6882ed2f`
through the independently promoted b4 queue. Earlier attempts, failures and
spend lineage remain immutable.

## G0, profile and pressure result

G0: PASS. Retaining the v1 field while correcting it to exact review locations,
enforcing repository categories at closed queue admission, and appending the
complete source-review contract is smaller and safer than a schema fork or
parallel repair engine. The generic repair validator no longer embeds the
repository's scripts-only policy; the explicit queue adapter owns runtime,
test, docs and planning admission. Exact-head Git existence and line bounds
remain one adapter census.

Profile: `contract` under `quality-contract/v2`. All five criteria, scope,
non-goals, verification and attack surface are in `planning/drafts/ISS-085.md`.
AUTHOR1 is `gpt-5.6-sol` high under model-routing row 7 for contract/mutation
authority implementation; REVIEWER2 must be fresh and independent.

Pressure result: PASS for source planning. Hosted focused cases must
discriminate all four repository categories, directories/duplicates,
unsupported and out-of-footprint paths before adapter entry, exact-head Git
existence and one-based line bounds, and the injected G0/profile/all-twelve-pair
contract. Existing real-Git cases keep their finite `30_000` per-test deadline;
global deadlines and assertions do not change. The controller still owes all
four static gates and actual hosted Node 24 Windows/macOS/Linux execution.

Fixture success and source acceptance cannot close #348. Only after external
promotion may a genuinely needed failed review exercise the corrected
repair/DELTA/restart path, preserving its original failure, attempts and spend
as distinct later evidence.
