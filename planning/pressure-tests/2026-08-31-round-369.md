# ISS-022 experiment test placement — round 369

Independent path-only `PASS` from `/root/iss022_native_build_review`, Sol high,
row 11, at build `8b3b24203b5cd8bd7bbbe1a7a588a40e84e709ee`, witness
`2bff8453e752ea4b0f6538aa804d47d045dbbaa8`, and candidate
`cb0a2dbf94a4394ea77a803513c8e68cc5efb90f`.

Full bootstrap at build integration `a8efdaf1c264159c83c5516866bb86523ee081c3`
and all three protected PR jobs failed the exact legacy portable-test census:
the new experiment test had been placed inside the original Node-only suite.
The repair moves experiment tests to `test/native-lock-experiment/` without
changing bytes, assertions, helpers, native sources, registry or old census.
Every rename is 100% identical. Root Vitest still discovers the build test;
relative imports, computed repository roots and standalone self-spawns retain
their original meaning at the same path depth.

Separating the tests preserves the original probe contract; weakening its
census or silently expanding that Node-only suite would hide the mistake.
This review covers placement equivalence only, not native semantics or CI
success. The stale witness usage comment is non-executable. No test, compiler,
probe or provider action was run by the reviewer.
