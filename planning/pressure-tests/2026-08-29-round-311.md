# ISS-022 diagnostic aggregate verifier follow-up — round 311

`PASS` at exact base/HEAD
`f80c12ba5cc6bb41cc60a77dbc96391c6d55431d`.

Round 309's sole verifier blocker is closed. The receipts-only verifier now
admits only the exact ISS-022 registry and exact three-row registry order. For
each authenticated observation evidence row it reparses the canonical stable
report against the expected current provider-run/job/environment coordinates,
recomputes the raw manifest and job receipt under the expected candidate,
contracts, harness, test, registry, and provider-run identities, and requires
the archived canonical receipt's bytes/digest projection to equal that result.
It additionally requires at least one `selection:null` report with a physical
`UNKNOWN|UNSUPPORTED` row. The archive census is receipts-only, so
`aggregate.json`, aggregate bytes, and PASS authority remain absent.

The repaired mutants refuse generic ISS-002, stale/cross-run and changed global
identities, reversed/mixed evidence order, missing/extra archive members,
environment, manifest, raw report, vector, and result substitutions, including
changing one UNKNOWN receipt to PASS while other non-PASS receipts remain.
Canonical report replay and manifest/receipt recomputation also close rebound
raw/report and per-job substitutions rather than trusting archived claims.

The seven-file `+475/-4` working diff remains proportionate. The in-scope
threat is a receipts-only diagnostic archive laundering stale or substituted
evidence when no aggregate digest list exists. Reusing the existing exact
ISS-022 report parser and generic receipt producer is the smallest credible
binding; deleting either replay makes Round 309's counterexamples pass, while a
new provider record, terminal, schema, job, or publication mechanism would be
larger and is absent. Roughly half the repair is focused fixture/mutation
evidence, and the workflow remains four jobs with unchanged trust and writers.

Focused evidence passed: the three touched test files (44 tests), `pnpm run
typecheck`, and `git diff --check`.
