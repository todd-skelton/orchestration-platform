# ISS-078 hosted fixture correction

This bounded v1 source-only correction preserves the original seven acceptance
criteria and the existing F1--F6 authority and usage fixes. It changes no
runtime file and does not create, alter, or certify a release.

## Exact fixture corrections

- H1: queue fixtures now declare each synthetic source stage directory and
  accepted records use that authoritative directory, allowing the completion
  and delivery-identity assertions to reach their intended runtime checks.
- H2: the completed-item fixture's accepted record uses the item's valid
  source directory and its existing matching accepted history before the queue
  completion metric is deliberately corrupted; the zero-effect assertion is
  retained.
- H3: the direct-composition fixture canonicalizes its temporary root before
  deriving the controller and two distinct worktree roots, satisfying the
  delivery overlap guard without changing it.
- H4: supervise command and hook paths are derived from the test module, the
  temporary fixture root is canonical, and the authorized controller root is
  derived from the module location. Fixture cleanup uses the filesystem's
  bounded retry support for transient Windows handle release. Actual command
  invocation, observed wait, completed restart, assertions, and finite
  deadlines remain intact.
- H5: the six-row initial malformed draft-flag registration alone receives the
  established 30 second per-test deadline; its table, body, assertions, and
  global deadline remain unchanged.

## Hosted requirement

No local test runner, build, Git fixture experiment, or native execution was
run. Hosted Windows, macOS, and Linux validation remains required.
