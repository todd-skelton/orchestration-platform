# ISS-003 pure dispatcher — round 340

Disposition: `BLOCK_FIXABLE` at `f181721d4bd6c86a4c84785c8f7ad47b9920c4e0`,
base `e820459d38ee6a8cbd3ce3f49e4a1650b41e7f20`.
Independent cold reviewer: `/root/iss003_dispatcher_review`, Sol high, row 11.

Command matching joined argv tokens and admitted a single `"config paths"`
or `"config validate"` token. Host execution against the exact source with
only two negative test rows added produced two failures: both returned success
and exit 0 instead of empty-command `ARGV_REFUSED`, exit 2, and zero loader or
handler calls. Eleven existing argv negatives passed; 32 other tests were
skipped by the filter. The original targeted suite had passed 57 tests.

The bounded repair compares the selected tokens and count to the existing
registered argv row before command assignment or configuration admission.
No additional blocker emerged in the bounded source review; live bootstrap
wiring remains outside this pure slice.

Proportionality: malformed invocation reaching configuration reads is the
concrete threat. Exact comparison against the existing row is the smallest
repair; no registry, loader, schema, or isolation mechanism is added.
