# Pressure round 401 — disposition and follow-up literals

Status: author proposal; ordinary independent exact-head review pending.
No PASS, implementation, mutation or completed ISS-041 cycle is claimed.

## Scope and evidence

Base is reviewed dispatch definitions/status
`2667d5aa0d199ea47d923a5d8720c05f2648fc6f`. The existing finite census names
action-disposition/v1 and follow-up-cycle-request/v1 for step 11. Current
module input/result bytes are step-4-only; review, immutable subjects, skips
and pending dispatch/terminal records provide actual preimages, not a callable
phase or complete disposition. The consumer seeds an earlier immutable author
result and observes a distinct later review-cycle echo, with no applied
mutation claim and no fiction that seeded history was executed.

This proposal closes only those two public families, a closed inline context/
intent/cause and exact supplied relations. Full project mutation, reclaim and
event/journal/reduced-state/cycle-receipt groups remain required by the census.

## Explicit replans

1. Optional static disposition export is allowed only with nonempty existing
   dispositionCodes. Plan-only ABI/bytes remain valid; inputSchemas/outputSchemas
   describe step 4. Full-cycle handler/code/source admission happens before
   worker effects. No code lookup from strings or new public input schema.
2. Original opaque action target and later worker-result identity are distinct.
   Current review core already targets the exact older immutable subject. The
   actual action, source/cycle/attempt/terminal and target remain joined without
   an impossible pre-worker/result equality or new materialization format.
3. Typed skips bind actual primary output digests: preflight at 6, launch at 8,
   terminal at 9, or preceding skip. Later complete events additionally bind
   dependent terminal/subject/attempt records. No new wrapper hash, fake journal
   prefix or UNKNOWN padding is admitted.
4. COMPLETE explicitly means completed nonmutating observer/review work;
   successful materialized REVIEW_NEEDED is complete-follow-up. Both can reach
   COMPLETED only after required worker/review/reclaim/journal gates. Applied
   completion still binds actual apply. NO_ACTION after eligible work is declined
   known failure, not empty-frontier proof. This resolves the existing routine
   complete-follow-up versus applied-only terminal inconsistency.
5. Disposition carries an inline intent; a later follow-up request references
   Ddisposition. No future cycle/session is assigned. Failure may request no
   retry; rejection must request nonapplying repair/replan/retry. The successor
   verification structural arm grants no promotion/scheduler authority and its
   production cause is deliberately outside the step-11 supplied binder.

## Attack surface and required later evidence

| Mechanism | Discriminating removal attack | Smallest alternative |
| --- | --- | --- |
| Exact module/role/code source | Missing full-cycle handler, empty/foreign code, forbidden export/effect or observer review-required bypass refuses before effects | Optional phase and existing code census; no mandatory rewrite or new descriptor flag |
| Actual input/result/review joins | Substitute any actual action/preflight/route/plan/launch/terminal/stream/request/attempt/authority and rehash downstream records | Reuse complete existing parsers and terminal/review relations; no expected-digest bag |
| Subject-phase distinction | Equate pre-worker target with new result, change reviewed subject, use candidate acceptance for worker apply, or reuse author attempt | Existing concrete subject identities plus original action; no new subject family |
| Real skips and completion | Missing disposition, wrong predecessor output, early UNKNOWN padded with skips, live worker or missing reclaim called complete, NO_ACTION called no-work | Existing skip family and explicit terminal amendment; no parser-omission bypass |
| Follow-up causality | Moved intent/target/source cycle/cause, cyclic digests, rejected review without follow-up or step-11 promotion-cause admission | One inline intent then one cause-bound request; future intake remains owner-gated |
| Full compatibility | Every outcome/operation/cause/kind/null/bound/hostile cell, independent canonical bytes/full frames/digests and inline input hash | Two existing public names and closed reuse; no partial union or generic payload |

## Non-goals, footprint and verification

Eight documentation paths: shared ledger, module ABI, routine cycle, additive
ISS-002/011/009/013 notes and this record. No source, manifests, CLI, new public
family/issue/edge/roadmap, provider/probe, runtime service, release/mutation
implementation, scheduler or ISS-026 work. Closed PROJECT/ASSEMBLE_CERTIFY/
PROMOTE choices do not select or implement their later owners' authority.

Predicted parser packet: two complete parsers/identities, bounded inline input/
intent helpers and two supplied relations plus focused compatibility tests and
inseparable census wiring. Required new authority/family or an open resolver is
a named replan boundary. Host performs planning/format/whitespace checks,
independent exact-head review and adjacent draft/board reconciliation before
code. No tests, compiler, builds, native cases or provider operations ran in this
author packet; passing structural claims cannot supply runtime authority.
