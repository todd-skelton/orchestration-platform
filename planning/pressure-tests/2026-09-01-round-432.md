# Pressure round 432 — first-consumer adapter composition scaffold

Status: bounded ISS-028 source packet. No consumer access, shadow, import,
mutation, credential, provider, or operator-authority action is claimed.

## Frontier and scope

Protected base is `adf042d7e3eb7fdf332ae5240f013ee291124b1c`. ISS-048 cannot cross into
its single-OS observation owner while Decision #182's native-cases artifact is
parked after absolute attempt 10. Recreating that source is forbidden. ISS-028
is independently runnable after ISS-013 and supplies executable composition
evidence without crossing the parked or operator-held boundaries.

This packet fills only the predeclared adapter root with an exact static adapter,
SDK, engine, capability, schema, and extension-owner census. Its admission
function snapshots closed ordinary records and arrays and refuses every version,
capability, schema, prototype, accessor, symbol, or extra-field substitution. It
is pure and has no callback through which rejected input could reach an external
source. Existing import, mutation, and shadow placeholders remain byte-stable.

## Proportionality and removal evidence

| Mechanism | Concrete threat and deletion test | Smallest alternative |
| --- | --- | --- |
| Exact compatibility record | Change one adapter/SDK/engine version, remove/reorder one capability or schema, add a field, or supply an exotic record; admission must refuse | One local closed snapshot; no new public schema, registry, SDK callback, or configuration loader |
| Static extension census | Move, duplicate, or retarget one package export or owner; scaffold verification must refuse | Reuse the three predeclared package exports and existing placeholders; no dynamic discovery |
| Disjoint footprint check | Assign two extensions the same owner path or include the composition root; collision verification must refuse | Compare three literal owner/path rows; no worktree mutation simulator |
| Package boundary | Widen the package files census or add a shared export; scaffold verification must refuse | Inspect the existing package manifest; no pack/install machinery in this slice |

## Acceptance, verification, and predicted footprint

The exact compatibility value passes; every single-axis mutant above refuses;
the package retains exactly root/import/mutation/shadow exports and `src` files;
each exported placeholder retains its registered owner and not-implemented
outcome; and representative extension paths are pairwise disjoint from the
composition root. Root and package test capability slots execute the same
verifier without editing shared manifests or scripts.

Verification is typecheck, focused scaffold tests on a non-Windows host or
hosted CI, both root capability commands plus the package-filter command,
format/planning/diff checks, independent exact-head review, and three-OS
bootstrap. Local Windows does not run Vitest. Predicted footprint is the adapter
root, one shared test verifier, one test entry, three predeclared capability-slot
owners, and this record. A required external callback, credential, consumer
field, package-manifest edit, public schema, or extension implementation is new
replan evidence.

Review should attack prototype/accessor execution, incomplete array closure,
version or capability widening, a dynamic import/path, changed placeholder,
two extensions sharing a file, consumer terminology entering platform
packages, and any claim that scaffold admission proves live compatibility.

## Exact-head review follow-up

The first independent pass found three evidence gaps without requiring a
production redesign. This bounded follow-up statically imports all three
placeholder modules and proves their exact export census and closed frozen data
shape; proves every extension row's exact closed fields plus the ordered
capability projection; and adds refusal mutants for record accessors and
symbols, array proxies and accessors, exotic array prototypes, holes,
additions, duplicates, and reordered schemas. The proxy and accessor mutants
also prove their traps are never invoked. No placeholder source, public
contract, authority boundary, package manifest, or predicted footprint changed.
