# Pressure test — round 93 bootstrap use-intent removal review

**Disposition:** `PASS`

**Reviewed candidate head:** `efbdad20526f458c59dfcb7a2be633fb79226ee2`

**Reviewed parent:** `dd63d9ebf1f834564628fb12da5e1e9922157520`

This was a cold independent local removal review of the exact initially clean
head above on `iss-002-versioned-contracts`. It is bounded to removal of
`OP-R92-001` through `OP-R92-004` in bootstrap use-intent composition. It
grants no merge, bootstrap, anchor creation, E0, installation, release,
promotion, runtime mutation, live capability, receipt, or self-certification
authority.

The review read `AGENTS.md`, `docs/planning/work-model.md`, the decision
authority in `docs/architecture/contract-decisions.md`, its incorporated
normative external-bootstrap ledger, successor digest formulas, and final
cross-record order in `docs/architecture/supervisor-contract.md`,
`docs/contracts/compatibility.md`, ISS-002, ISS-020, ISS-022, rounds 91–92,
and the exact three-file parent-to-candidate diff. No Chase Sets repository,
issue, project, board, backlog, or consumer system was accessed. The board gate
read only this repository and its registered platform delivery project.

## Finding removal

`OP-R92-001` through `OP-R92-004` are removed.

### `OP-R92-001` — authority `G` remains distinct and accepted

The false equality between proposed-genesis `globalIdentityDigest` and the
anchor's `globalBootstrapIdentityDigest` is deleted. The validator no longer
compares the independent authority `G` from domain
`state-mutation-global-identity/v1` with the external anchor projection from
domain `global-bootstrap-identity/v1`.

The proposed-genesis record remains closed and canonically equal to the trusted
private proposed-genesis expectation. Its authority path, bootstrap grant, and
transaction remain equal-bound to the anchor. `G`, genesis position, and
successor core remain independent upstream preimages for later `Dsc` and
genesis-position construction; no anchor-domain digest substitutes for them.

An independent in-memory reconstruction computed a real authority `G` from the
fixture's installation, project, state root, custody instance, canonical
authority path, and authority-path instance. It was distinct from the anchor's
recomputed global-bootstrap identity and the composed genesis intent returned
`[]`.

### `OP-R92-002` — selected anchor and owner graphs are closed

The selected anchor tip now equal-binds its `anchorDigest` to recomputed `Dba`.
The selected owner tip equal-binds its destination to the anchor's `Ddest`.
The owner proposal now equal-binds destination, the recomputed canonical owner
position, successor value, and mutation identity. The existing branch checks
still require exactly genesis `ACTIVATE_GENESIS`/`BOOTSTRAP_GENESIS`/bootstrap
grant with an all-null prior triple or successor
`ACTIVATE_SUCCESSOR`/`SUCCESSOR_REVIEW`/exact `Dsrc` with an all-non-null prior
triple. Owner value still binds installation, destination, anchor, lifecycle,
and nullable/exact successor-review core.

The independent probe rebuilt a foreign anchor tip and recomputed the intent's
selected tip digest. It refused with
`anchorTip.anchorDigest:mismatch`. It then rebuilt a coordinated foreign owner
value/proposal/tip graph, including the foreign destination position, successor
value digest, mutation ID, proposal digest, tip digest, and all three intent
selection digests. It refused with the four destination/position mismatches.
Sibling digest reconstruction therefore cannot select a foreign anchor or
destination.

### `OP-R92-003` — successor eligibility consumes validated `Dsrp` context

The private expectation is now a closed genesis/successor union in practice.
Genesis requires null successor review core, `Dsrp`, `Dsrp` readback, and
destination-lock/custody observation. Successor requires each of them present
and schema-valid.

For a successor, the composition parses the exact
`state-mutation-destination-owner-successor-review-post-selection-receipt/v1`,
recomputes `Dsrp`, and requires the expected receipt digest to equal it. The
receipt's exact `Dsrc` equals the successor anchor's reviewed core; its anchor
equals recomputed `Dba`; its owner tip/value/proposal digests equal the selected
owner graph; its destination-lock/custody observation equals the trusted
context; its three readback members remain closed SHA-256 facts; and a distinct
receipt-readback digest is mandatory. The path identity is consequently pinned
by the already-bound destination and successor owner tip. None of these
downstream facts enters the anchor, owner, intent schema, or intent digest.

The independent probe rebuilt a syntactically valid foreign receipt with a
foreign `Dsrc`, anchor, complete owner triple, and custody observation, then
recomputed `Dsrp` and supplied that recomputed digest. The composition refused
all six substitutions. The valid successor reconstruction returned `[]`; the
valid genesis reconstruction also returned `[]` with the successor context
exactly null.

### `OP-R92-004` — nested installer facts equal the anchor

The nested reviewed installer now equal-binds
`installerArtifactDigest` to the anchor's `reviewedInstallerDigest` and
`reviewReceiptDigest` to the anchor's
`independentReviewReceiptDigest`. Canonical equality to the trusted private
reviewed-installer expectation remains in force. The source digest stays in
the closed canonical nested record and no candidate-produced receipt or new
self-certification surface is introduced.

The independent probe changed each nested digest separately while changing
both copies of the nested installer and retaining the anchor. Artifact
substitution refused with `installer.installerArtifactDigest:mismatch`; review
substitution refused with `installer.reviewReceiptDigest:mismatch`.

## Preserved behavior and exclusions

The four persisted schemas, parser dispatch, registry, compatibility, digest
domain, framed part order, canonical use-intent path, interval, and hostile
input behavior are unchanged by the implementation repair. The private
expectation gains only the successor post-selection composition and readback/
custody facts needed to prove eligibility.

The valid genesis and successor intent compositions both returned `[]` in the
committed suite and independent reconstruction. The proposed authority `G` is
accepted independently of the anchor global-bootstrap projection. Exact
current-version compatibility remains accepted while missing, legacy,
unknown, malformed, and future versions remain refused.

The intent's closed census and digest remain upstream-only. They contain no
`Dsrp`, `Dgb`, GENESIS `Dh`, E0 `Dv`, `Dbg`, proposal/receipt/tip/readback,
CONSUMED fact, or final consumption receipt. The independent census check also
confirmed those exclusions. Successor context is consumed only by the private
composition after `Dsrp` exists and is read back, preserving the normative
one-way order.

## Exact delta

The exact `dd63d9e..efbdad2` delta has three paths, 415 insertions, and nine
deletions:

- `packages/contracts/src/intent.ts`: 94 insertions and four deletions;
- `test/contracts/intent.test.ts`: 96 insertions and five deletions; and
- `planning/pressure-tests/2026-08-18-round-92.md`: 225 insertions.

The implementation adds private expectation fields and bounded equalities; the
tests rebuild the valid fixtures and add focused foreign selection/installer/
successor-post negatives. The blocking round-92 review is retained as history.
`git diff --check` passes. No public schema, registry, export, package manifest,
architecture authority, wrapper, runtime state service, adapter, consumer,
history/E0/consumption implementation, or unrelated contract family changed.

## Bounded proportionality

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Remove the cross-domain global-identity equality | A conforming authority `G` is rejected or the unrelated anchor projection is admitted as `G`. | Restoring the deleted equality rejects the independently computed distinct authority `G`; leaving no trusted proposed-genesis canonical binding would admit a changed upstream input. | Retain canonical equality to the trusted proposed-genesis input and no cross-domain equality; no new identity record is needed. |
| Anchor-tip and owner destination/position equalities | A caller rebuilds sibling digests around a foreign anchor or physical destination. | Omitting the anchor-tip equality admits the rebuilt foreign anchor tip; omitting the owner destination/position equalities admits the rebuilt foreign owner graph after recomputing mutation and selection digests. | Four direct identity equalities reuse existing digest constructors; a coordinator or duplicate owner validator input graph is larger. |
| Private parsed and recomputed successor-post context | A caller supplies an arbitrary SHA as `Dsrp` or rebuilds it around another reviewed core, anchor, owner selection, or custody observation. | Removing the receipt parse/digest or any named graph equality makes the reconstructed foreign receipt admissible while the valid successor still composes. | Reuse the existing receipt parser/digest with a closed private expectation; adding `Dsrp` to the upstream intent would create the forbidden cycle. |
| Two installer-to-anchor equalities | Caller-chosen nested installer bytes or review remain disconnected from the reviewed anchor. | Removing either equality makes its independent nested substitution return `[]` because both nested copies still agree. | Two scalar equalities are sufficient; no additional persisted receipt or candidate certification is warranted. |

Each retained mechanism addresses one demonstrated substitution at an already
existing private composition boundary. The repair introduces no public record,
selector, inventory, coordinator, migration, or downstream edge in the intent.

## Verification evidence

The required repository gates ran sequentially in the requested order:

1. `pnpm run planning:check` — PASS, `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS, `board contracts verified`, against
   only this repository and its registered platform delivery project.
3. `pnpm run format:check` — PASS, all matched files use Prettier style.
4. `pnpm run typecheck` — PASS.
5. Standalone `pnpm test`, once only — PASS, 26 files and 288 tests in 108.29
   seconds.
6. Separate `pnpm run verify:bootstrap`, once only — PASS: manifests and
   registries, formatting, typecheck, 26 files/288 tests in 112.61 seconds,
   private compositions, planning contracts, and source-tree status
   invariance.
7. Focused `pnpm --filter @orchestration-platform/contracts test` — PASS, 16
   files and 91 tests.
8. Focused `pnpm run contracts:compatibility-check` — PASS, one file and one
   test.

The first sandboxed planning invocation could not verify the pinned pnpm
release because registry fetches were blocked and failed closed before the
repository script ran. The identical approved read-only invocation passed; all
later gates used that approved read-only path. The first two in-memory probe
setups did not execute semantic assertions: one was rejected by JavaScript
parsing before esbuild ran and one was blocked by sandbox process-launch policy.
The unchanged probe then ran through the approved read-only path. There was no
assertion failure, repository-test retry, isolated failing-test rerun, timeout
override, implementation edit, or test edit.

Immediately before this report, the branch and both full SHAs still matched,
the source worktree was clean, and the exact candidate diff passed `git diff
--check`. This review adds only this uncommitted round-93 record.

## Final status

`PASS` for exact candidate head
`efbdad20526f458c59dfcb7a2be633fb79226ee2`. `OP-R92-001` through
`OP-R92-004` are removed: distinct authority `G` is accepted without a false
anchor-domain equality; selected anchor and owner identities are closed;
successor eligibility parses, recomputes, reads back, and exact-binds the
downstream `Dsrp` context; and nested installer artifact/review facts equal the
anchor. Valid genesis/successor behavior, upstream-only framing, and strict
compatibility are preserved, all adversarial reconstructions refuse, and every
required gate is green. This review records local contract readiness only and
grants no merge, bootstrap, installation, release, promotion, runtime mutation,
live capability, receipt, or self-certification authority.
