# Pressure test — round 83 external-ledger final exact-head review

**Disposition:** `PASS`

**Reviewed head:** `d88f6938bfec502957da9e0fefb9df39b7c48882`

This was a fresh independent final review of the exact initially clean head
above on `iss-002-versioned-contracts`. There was no authoring role or authoring
context. The review began with `AGENTS.md`,
`docs/architecture/contract-decisions.md`, and the incorporated normative
external-bootstrap schema ledger in `docs/architecture/supervisor-contract.md`,
then read compatibility, ISS-002/006/020/022, rounds 70 and 78 through 82, the
exact
`35455b92603b0906dbb0463f7f3862e2ba48ba04..d88f6938bfec502957da9e0fefb9df39b7c48882`
delta, the current external implementation/tests, and all three bounded timeout
changes. No Chase Sets repository, issue, board, project, backlog, or other
consumer system was read or touched. The board check read only
`todd-skelton/orchestration-platform` and its registered delivery project.

## Disposition

`PASS` for round 70 block 1 at exact planning head
`d88f6938bfec502957da9e0fefb9df39b7c48882`. The only executable change after
round 82 is the Vitest configuration default `testTimeout: 30_000`. It changes
no test body or assertion. The prior inventory relationship and planned-command
census tests retain their explicit local `300_000` ceilings, which take
precedence over the configuration default, and their complete bodies and
assertions remain byte-for-byte unchanged.

The parent-confirmed and independently rechecked repository-specific process
window was quiet before the once-only named gates. The sole actual `pnpm test`
invocation passed all 22 files and 267 tests. The separate sole
`pnpm run verify:bootstrap` invocation also passed all 22 files and 267 tests,
then completed private compositions, planning validation, and source-tree
status invariance. No wrapper probe, retry, isolated rerun, or timeout override
was used.

`OP-R82-001`, `OP-R81-001`, and `OP-R80-001` are removed. `OP-R79-001` and
`OP-R78-001` through `OP-R78-006` remain removed. No new external-ledger
schema, path, digest, lifecycle, equality, portability, or trust blocker exists.
Round 70 block 1 remains contractually implementation-ready. Round 70 blocks 2
and 3 remain independently unauthorized. This planning disposition grants no
merge, release, promotion, installation, runtime mutation, live capability, or
self-certification authority.

## Exact delta and timeout audit

The exact reviewed delta has two paths, 269 insertions, and no deletion:

- `planning/pressure-tests/2026-08-18-round-82.md` is the prior independent
  record; and
- `vitest.config.ts` adds only `testTimeout: 30_000` to the existing `test`
  configuration.

`git diff --check` passes. No production file, contract, schema, registry,
parser, canonicalizer, digest, path, lifecycle validator, wrapper, capability
slot, or test file changes in the exact round-82-to-round-83 delta.

### `OP-R82-001` — removed

Round 82 observed three otherwise unchanged tests exceed Vitest's five-second
default: LF formatter acceptance, ordinary registry-kind commit identity, and
initial reference-inventory binding. The new 30-second default is configuration
only. It preserves every executable body and assertion while giving ordinary
tests a bounded loaded-host ceiling. It does not introduce a command-line
override, an unbounded wait, or a production behavior change.

The sole actual named full gate passed all 22 files and 267 tests in 216.45
seconds with no local timeout or assertion failure. The separate bootstrap
full-suite phase passed the same 22 files and 267 tests in 175.89 seconds and
completed all remaining verifier phases. The exact failure class reported in
round 82 therefore no longer blocks reproducible named-gate evidence.

### `OP-R81-001` — remains removed

Across
`d5c9a42416d4b83173a21773cd7f13bdb47b0ac9..35455b92603b0906dbb0463f7f3862e2ba48ba04`,
`test/planned-command-execution.test.ts` changes only the planned wrapper
census's local ceiling from `240_000` to `300_000`. The test still loads all
registered issue drafts, extracts every root and filtered verification command,
separates implemented slots, invokes every unimplemented wrapper, validates the
fixed result schema/outcome/capability/owner/argv record, and proves the exact
declared census. Every command, loop, result check, and assertion is unchanged.

Its explicit `300_000` third argument remains present at this head and is not
reduced by the 30-second configuration default. Both once-only full-suite
executions passed it. `OP-R81-001` remains removed.

### `OP-R80-001` — remains removed

Across
`8b94edde6217361406ca18f93628b9d78182925a..d5c9a42416d4b83173a21773cd7f13bdb47b0ac9`,
`test/reference-inventory/inventory.test.ts` changes only the
key-independent relationship matrix's local ceiling from `120_000` to
`300_000`. Its relationship families, normalization, renamed/reordered/wrapped
cases, one-field deletions, excluded and accepted paths, production calls,
loops, and all four numeric assertions are unchanged.

Its explicit `300_000` third argument remains present at this head and is not
reduced by the 30-second configuration default. Both once-only full-suite
executions passed it. `OP-R80-001` remains removed.

## External-ledger semantic authorization audit

### `OP-R79-001` — remains removed

E0 alone selects `RUNTIME_AUTHORITY_ABSENT`. Both `RETIRE_UNUSED` and
`RETIRE_CONSUMED` select `DESTINATION_STATE_ROOT_ABSENT`; consumed retirement
observes it after terminal cleanup and before lifecycle archival while the
applicable destination and anchor locks remain held. The receipt equal-binds
the same-lock `Dabs`, current admitted `Dobs`, helper/custody/state root, and
the exact selected owner/anchor CONSUMED graph. Runtime-only, opaque,
wrong-root, stale-observation, wrong-custody, or different-lock absence cannot
substitute. ISS-002/006/020/022 and compatibility preserve the same closed
branch census rather than redefining it.

### `OP-R78-001` through `OP-R78-006` — remain removed

- The use intent and proposed-genesis input contain only independent bootstrap
  preimages. `Dgb`, GENESIS `Dh`, E0 `Dv`, `Dbg`, and downstream selection
  evidence are derived later, so the graph remains acyclic and constructible.
- Anchor consumption retains distinct
  `runtimePostSelectionReceiptDigest` and
  `runtimePostSelectionReceiptReadbackDigest` members and equalities.
- `external-destination-absence-observation/v1` retains one closed census,
  exact domain/order/path, ISS-022-admitted producer, same-lock create-once and
  readback rule, and the exhaustive E0/retirement reason matrix.
- The successor-review candidate retains exact framing over destination,
  selected prior RETIRED facts, teardown archive, prior installation, and
  successor authority. External authentication plus distinct author and
  reviewer identities prevents self-review and coordinated substitution.
- Retirement remains forward-only and crash-resumable:
  `Dabs -> lifecycle archive -> teardown receipt -> anchor RETIRED -> owner
  archive -> owner RETIRED`. No archive or receipt contains a future selected
  value, proposal, or tip.
- Physical leaf identity retains the single pinned portable algorithm:
  canonical unpadded base64url, shortest-form UTF-8 without BOM/surrogates/
  noncharacters, Unicode 15.1.0 normalization and lowercase data, exact
  Darwin/Windows/Linux profiles, and complete component/reserved-alias refusal
  rules. Helper/profile/custody rotation cannot move `Dphys` or `Ddest`.

The normative external ledger's literal member censuses, scalar grammars,
nested schemas, nullability groups, lifecycle ordinals, enums, persistence,
framed digest order, constructed paths, exclusions, and cross-record equalities
remain unchanged and exact. The accepted graph cross-binds stable
`Dphys/Ddest`, current admitted `Dobs`, owner/anchor ACTIVE, use intent, `Dsc`,
`Dgb`, GENESIS `Dh`, authority `Dp/Dv`, `Dbg`, proposal `Dr`, tip `Dt`, `Dgp`,
owner/anchor CONSUMED, final consumption, and downstream `Dgse`. The complete
history walk rejects fork, gap, reorder, truncation, substituted head, and
unarmed excess.

Structural validation grants no mutation authority. External independent
review, ISS-022-admitted helper/profile/custody facts, the
destination-to-anchor-to-runtime kernel lock order, selected runtime authority,
ISS-004's private revocable handle, and stable promotion remain separate trust
boundaries. A worker or candidate cannot author its own review, absence premise,
live capability, or promotion.

Current `packages/contracts/src/external.ts` and
`test/contracts/external.test.ts` remain the intentionally partial pre-ledger
implementation: raw-`Dphys` `Ddest` derivation and constructed authority paths
only. The green focused/full/bootstrap evidence is regression evidence for the
implemented surface, not a claim that the planned external ledger has already
been implemented.

## Bounded proportionality and trust analysis

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Closed same-lock `Dabs` branches | Runtime-only or opaque absence authorizes E0 or destructive retirement. | Remove one reason/equality and the round-79 implementer choice returns. | Retain the two-reason observation and branch matrix; no new record. |
| Upstream-only use intent | Bootstrap requires a digest fixed point or authenticates itself downstream. | Insert `Dgb`, `Dh`, `Dv`, or `Dbg` into the intent and the graph cycles. | Retain independent preimages and downstream checks; no coordinator. |
| Distinct `Dgp` and readback | Receipt identity substitutes for verified persisted bytes. | Collapse them and substituted persistence loses its named equality. | Retain the two existing digests; no new receipt family. |
| Exact review and retirement order | Self-review, coordinated substitution, or future-digest cycles authorize a successor. | Remove candidate identity separation or reverse an artifact edge and the exact defect returns. | Retain the reviewed candidate and `Dabs -> archive -> receipt -> anchor -> owner`; no sequencer. |
| Pinned portable leaf algorithm | Helper/profile rotation aliases or moves a physical destination. | Unpin decoding, Unicode data, profile, or reserved names and conforming helpers may disagree. | Retain the fixed cross-OS algorithm; no alias inventory. |
| Explicit 300-second census ceilings | Exhaustive valid inventory or command census work cannot finish under the named gate. | Restore 120/240 seconds and rounds 80/81 reproduce their red gates. | Retain only the two local 300-second ceilings and every assertion. |
| Bounded 30-second ordinary-test default | Valid loaded-host tests fail the five-second library default before reaching their unchanged assertions. | Restore the prior default and round 82's three exact timeouts return. | Retain the configuration-only 30-second default; no body, assertion, or production change. |

The first five mechanisms protect current public bytes and the actual
multi-writer destination boundary. The last two protect reproducible release
evidence only. Nothing supports a third absence reason, proof tree, inventory,
coordinator, migration, verdict route, unbounded timeout, or candidate-owned
authority.

## Round 70 blocks 2 and 3 remain unauthorized

This review authorizes no implementation of either remaining round-70 block:

2. active release, cleanup gate, recovery fence, and cleanup archive records;
3. recovery authorization, reservation, descriptor, launch, attachment, and
   attempt-log records.

Their registry names, lifecycle prose, partial implementation, green tests, or
the block-1 ledger do not provide their missing literal schema ledgers. Each
requires its own decision amendment and fresh independent exact-head review.

## Verification evidence

- Initial branch/status/full-SHA checks — PASS; the worktree was clean,
  `iss-002-versioned-contracts` was exact, and the reviewed full SHA was
  `d88f6938bfec502957da9e0fefb9df39b7c48882`.
- Exact
  `35455b92603b0906dbb0463f7f3862e2ba48ba04..d88f6938bfec502957da9e0fefb9df39b7c48882`
  diff — two paths, 269 insertions, no deletion; only the round-82 record and
  one `testTimeout: 30_000` configuration line; `git diff --check` passed.
- Direct `node scripts/planning/check.mjs` — PASS (`planning contracts
  verified`).
- Direct `node scripts/format.mjs check` — PASS (all matched files use Prettier
  style).
- Direct `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` —
  PASS.
- Direct ISS-002 focused contract slot — PASS (12 files, 70 tests).
- Direct ISS-002 compatibility slot — PASS (1 file, 1 test).
- Direct repository-only `node scripts/planning/board-check.mjs` — PASS
  (`board contracts verified`) against only this repository and its registered
  delivery project.
- Actual named standalone `pnpm test`, once only — PASS (22 files, 267 tests,
  216.45 seconds). No wrapper probe, retry, isolated rerun, timeout override,
  test timeout, or assertion failure occurred.
- Separate actual named `pnpm run verify:bootstrap`, once only — PASS:
  manifests/registries, formatting, typecheck, 22 files/267 tests in 175.89
  seconds, private compositions, planning contracts, and source-tree status
  invariance. No wrapper probe or retry occurred.
- Source status immediately before writing this report remained clean. This
  review adds only this round-83 pressure record and makes no implementation,
  test, or commit change.

## Final status

`PASS` for exact clean planning head
`d88f6938bfec502957da9e0fefb9df39b7c48882`. The bounded configuration-only
30-second default removes `OP-R82-001` without changing assertions; the two
explicit local 300-second ceilings preserve all assertions and keep
`OP-R81-001` and `OP-R80-001` removed. `OP-R79-001` and `OP-R78-001` through
`OP-R78-006` remain removed, no new external-ledger semantic blocker exists,
and round 70 block 1 is contractually implementation-ready. Round 70 blocks 2
and 3 remain unauthorized.
