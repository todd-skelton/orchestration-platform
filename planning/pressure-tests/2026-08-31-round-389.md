# Route literal prerequisite and workerless amendment — round 389

Status: proposed; independent exact-head review and host verification pending.
Authoring base: `c3531e12b81867cf9caaf8e3bd8d974fcbe9eada`. This authoring
packet neither approves its own work nor claims parser/runtime acceptance.

The active ISS-041 module-input/action observer provides the concrete next
consumer for supplied routing evidence. Existing host identity/mapping parsers
already close artifact/capability identity and the whole 1–16 row census; no
new host probe or model router is needed to connect those public objects.
Installed/current-release and renderer admission still have distinct owners.

Scope: one existing `route-selection/v1` literal family, all selected/refused/
unknown/no-worker arms, designated identities and actual supplied-record joins.
The workerless amendment resolves the unconditional host-choice requirement:
declared workerless actions keep steps 5/6 and then typed skips 7–10, without
a fictional host or new skip. Non-goals: implementation, CLI/registry/graph,
provider/model/pricing/credential machinery, probes, or production authority.

Acceptance evidence is the future all-arm, nullability, canonical/full-frame,
complete-census, capability, substituted-plan/mapping and workerless matrix in
the literal ledger. Verification belongs to the host: planning and board
reconciliation, targeted formatting, whitespace, and independent review.
The author ran no verifier, tests, build, probe or provider operation.

Predicted footprint: five documentation files, followed after review by one
pure parser, one framed route identity, inline mapping hash and supplied
relation. Review attack surface is cross-plan/census substitution, fabricated
selected keys, empty/malformed mapping called known absence, workerless used
to hide a missing host, and structural facts laundered into installed authority.

Proportionality: deleting exact plan and whole-mapping joins loses those
substitution discriminators. A separate request schema or generic proof service
adds no current evidence; retaining existing parsers and one result family is
smaller. `NO_WORKER` avoids imposing a host requirement on work that launches
none. Missing runtime preimages stay explicit; no placeholder proof field is
added. Review the footprint at parser/observer handoff review. ISS-041 remains
incomplete, and no author PASS is claimed.
