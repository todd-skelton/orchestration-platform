# Planning pressure test — round 3

Disposition received: `BLOCK_REPLAN`.

The independent reviewer found nine remaining contract and mechanical-consistency
gaps. Registration stayed frozen while they were repaired.

| Finding | Replan disposition |
|---|---|
| `OP-R3-001` package commands referenced undeclared packages | Aligned every filter with the authoritative package table and made portable modules a root command. |
| `OP-R3-002` CLI placeholders had no handler ownership contract | Added a typed static registry, one owner per command family, dependency edges, command census, and owner acceptance evidence. |
| `OP-R3-003` milestone entry gates contradicted runnable early slices | Replaced blanket entry gates with dependency-driven preparation and explicit activation gates. |
| `OP-R3-004` the Codex probe had no exact artifact or reported-model surface | Pinned `@openai/codex@0.144.4`, npm integrity, install command, native executable hashes, and exact JSONL-field failure semantics. |
| `OP-R3-005` bootstrap and stable-predecessor actors were conflated | Split every release state into explicit N0 and N1+ actors, including authorization, installation, abort, and recovery. |
| `OP-R3-006` comparator timing and retirement were incomplete | Added post-cutover comparator applicability plus a terminal report/credential/process retirement state machine. |
| `OP-R3-007` bounded termination had no known-live failure state | Added `TERMINATION_FAILED_LIVE`; ownership and capacity remain claimed until exact exit. |
| `OP-R3-008` consumer reference evidence leaked toward shipped contracts | Confined inventory and term-map material to test/reference inputs and required package/bundle exclusion evidence. |
| `OP-R3-009` epic DAG prose could drift from the registration graph | Made `planning/roadmap.json` authoritative, rewrote epics as direct-edge mirrors, and assigned validation/generation to `ISS-000`. |

The replanned packet requires a fresh independent review; this document records
the failed round and dispositions, not a pass.
