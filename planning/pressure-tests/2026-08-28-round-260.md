# ISS-022 physical identity derivation review — round 260
`BLOCK_REPLAN`.
All ledger domains, frame order, big-endian u64/u32 bounds, signed `statfs.type` projection, physical OS vocabulary, decoded canonical leaf bytes, root/custody order, and reuse of the landed `Dphys`/`Ddest` formulas are exact.
The custody-instance job ID is not type-closed: `RegExp.test` coerces non-strings and `frame.text` can encode numeric/boolean values, so JSON number `1` aliases admitted text `"1"` instead of refusing an invalid job ID.
Minimal repair: require `typeof jobId === "string"` before the lexical check and add non-string/null mutants; no formula or filesystem behavior needs to change.
Input movement changes the existing physical/destination route, invalid widths/digests/namespace/leaf/OS/kind refuse, and no filesystem claim or alternate physical formula was added.
Focused tests pass 6/6 and root typecheck passes; scope is otherwise proportional.
