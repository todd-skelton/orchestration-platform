# ISS-022 portable-probe-core repair follow-up — round 250
`BLOCK_REPLAN`.
The corpus array, final-LF digest, all 21 canonical rows, `predecessorHex` schema, and mismatch=`42`/two-contender=`41` CAS bindings now match code exactly; the focused suite passes 68/68 and trust/scope remain bounded.
The required byte-equality deletion test is still not closed: `ledger.toContain(canonical)` checks 21 independent substrings, not the exact ordered table bytes.
Reordering or duplicating rows, adding an extra canonical row, or changing the table delimiter/final-LF structure leaves every substring present and the test green despite violating the ledger's exact census.
Minimal repair is one extraction of the authoritative fenced row block and equality to the 21 canonical records joined in registry order with the fixed final LF; retain the existing parser mutants.
Proportionality: no executor, helper, workflow, isolation, or production surface is implicated.
