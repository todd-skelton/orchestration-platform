# Pressure round 435 — quarantined planning module source

Status: blocked and abandoned ISS-011 source candidate. Neither reviewed head
was pushed. The module was never present in the manifest or generated registry
and gained no installed, runtime, dispatch, mutation, release, or authority
claim.

## Frontier and scope

Protected base is `6ba4dbe2c02ad47ec78075baabd0f16cf49e7643`. Round 434
landed the exact planning descriptor/policy and final four-row manifest/build
definition. This packet implements only `modules/planning/src/index.ts` and
exercises it through the existing walking-skeleton preparation test. The empty
manifest, empty generator contract, generated stub, root scripts/configuration,
and other three module entrypoints remain byte-stable.

## Proportionality and removal evidence

| Mechanism | Concrete threat and deletion test | Smallest alternative |
| --- | --- | --- |
| Parsed exported descriptor | source action/tuple/catalog drifts from public contracts; mutate one literal or extra export | one static descriptor parsed by the landed contract; no local descriptor schema |
| Retained input/result binding | asynchronous planning returns an action for another input/work/descriptor; mutate each returned binding after the await | reuse `composeFixtureModuleInput` and `validateModulePlanBinding`; no receipt or wrapper |
| Exact NO_TRIP selection | a TRIP or unrelated capability becomes executable work; remove the policy/capability/readiness check | first canonical eligible row under exact `work.read:NO_TRIP`; no scoring or policy language |
| Complete closed brief | implementation dispatch lacks review/verification/footprint inputs; delete one directive, role, action, or resource equality | reuse the existing catalog and brief families; no rendered prose or host fields |
| Actual two-adapter seam | hand-built unit facts conceal loader/SDK/policy differences; bypass one real preparation stage | reuse the existing branch/queue fixture loader, observations, composition, retained await, and post-await binding |

## Acceptance and verification

The exported namespace is exactly `descriptor,plan`; plan returns a native
Promise and imports only the repository's public contracts source, with no
effect imports or ambient reads. Branch facts with one
NOT_READY row produce TRIP and bound no-action. Equivalent queue facts produce
NO_TRIP and the implementation action for the first canonical READY row. An
all-READY corpus through both adapters selects the same work/action/core/brief
projection while retaining distinct adapter-bound input digests. Descriptor,
review-subject, policy, readiness, capability, order, digest, core, directive,
and footprint mutants discriminate the corresponding check.

Local verification is typecheck, focused formatting, planning and diff checks;
Vitest remains hosted-only on this Windows checkout. Merge gates are focused
tests in the ordinary hosted suite, one independent exact-head review, and
three-OS bootstrap. Predicted footprint is the one entrypoint, focused existing
consumer test, and this record. A manifest/generator/root-config change, second
module, new schema, effect boundary, renderer, route, host/model choice, or
authority field is replan evidence.

Review should attack TRIP becoming permission, noncanonical row choice,
descriptor/input substitution, missing review-required implementation role,
incomplete directives, adapter-specific source fields, hidden ambient imports,
test-only hand-built input, changed placeholder files, and any inference that
quarantined source is installed or runnable in the engine.

## Exact-head follow-up

The pressure review found that the first source scan recognized only
double-quoted static imports and that partial descriptor/result assertions did
not independently pin the complete planning contract. This sole follow-up
replaces the scan with TypeScript token inspection of static exports and
imports, import-equals declarations, dynamic imports, `require` calls, and the
closed ambient-effect census; single-quote, computed, CommonJS, export, clock,
randomness, process, and network mutants now deny the bypasses. The consumer
vector independently fixes the complete descriptor and digest, exact action,
brief, directive, and footprint records, valid descriptor and review-subject
substitutions, readiness and capability negatives, canonical ordering, and
repeated-input byte equality through the actual preparation seam. The
generator-source pin now hashes normalized tracked text so identical repository
content has one assertion on Windows, macOS, and Linux. Production source,
manifest, generator, generated stub, root configuration, and scope are
unchanged by the follow-up.

## Final exact-head disposition

The initial candidate at
`1c8cc9f6c28a279b8be10c8b4279f0f8ab040032` received one fixable block: its
import scan was syntactically bypassable and its consumer vectors did not pin
the full descriptor and result. The sole permitted follow-up produced
`fa0e0d7c60ef8ad11efdc488493ed703b0beb79c`; local typecheck, formatting,
planning, and diff checks passed without local Vitest. The final independent
review blocked that exact head because the finite ambient-name census omitted
Node 24's `navigator` and `WebSocket` globals. Those valid mutations read host
state or open a network surface while returning the same clean scan result.

No second follow-up is admitted. Both exact heads, both reviews, the source and
test diff, and the normalized generator-pin correction are inputs to Round
436. The candidate remains only on its local branch and is abandoned rather
than pushed, merged, or represented as partial authority.
