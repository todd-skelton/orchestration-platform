# Pressure round 439 — exact-byte quarantined planning source

Status: fresh ISS-011 source candidate after the independently reviewed Round
438 definition.

## Declared inputs and scope

Rounds 435 and 437 remain abandoned inputs with their exact heads, reviews, and
hosted evidence. Round 438 supplies this new admission boundary. This candidate
does not repair or reset either failed source round.

The packet adds only `modules/planning/src/index.ts`, focused vectors in the
existing walking-skeleton consumer, and this record. The empty manifest, empty
generator contract, generated stub, public contracts, adapters, runtime, root
configuration, dependencies, and authority surfaces remain byte-stable.

## Admission and behavior

Immediately before every `planningModule.plan` call, the consumer normalizes
the tracked source and requires the fixed independently reviewed SHA-256. There
is no source scanner, parser, ambient-name census, sandbox, or VM. Import,
`require`, clock, randomness, process, network, `navigator`, `WebSocket`, and
deliberately unlisted `structuredClone` mutations are asserted only to change
the admitted identity. Independent review recomputes the digest and inspects
the exact production bytes for the sole public-contract dependency and absence
of ambient or effect access.

Planning behavior remains the Round 434 definition. The complete descriptor and
catalog are fixed. Valid alternate descriptors and review subjects refuse.
TRIP, missing capability, or no canonical READY eligible row yields bound
no-action. Exact NO_TRIP selects the first canonical READY `work.read` row and
emits the complete review-required implementation brief. Every input traverses
the actual ISS-013 branch/queue loader, snapshot, policy, composition, retained
await, and post-await binding seam.

## Acceptance, verification, and review attack

Acceptance fixes the normalized source identity before every plan call;
complete descriptor/digest; exact action/core/brief/directive/footprint; valid
substitution refusals; readiness and capability negatives; canonical order;
repeated-input equality; adapter decision projection; normalized generator
identity; and empty normalized manifest. The source remains absent from the
manifest and registry.

Local verification is typecheck, focused formatting, planning, and diff checks;
local Vitest remains prohibited on this Windows checkout. Hosted bootstrap runs
the focused test and complete suite on all three operating systems. Independent
review should inspect the exact admitted bytes, recompute source/generator/
manifest identities, search every production dependency and ambient/effect
surface, mutate a non-seeded byte, prove every plan call crosses the identity
gate, attack all behavioral joins, and verify no placeholder or authority
surface changed. The digest proves identity continuity only and grants no
semantic, safety, installation, execution, release, or authority status. Any
source edit after review, manifest/generator edit, new dependency or public
contract, second module, or runtime integration forces replan.
