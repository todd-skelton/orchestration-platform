# Pressure round 404 — complete project mutation literals

Status: independent literal review PASS at source head
`4b9a2e2bd0bd059062796a88b6b8fc8679d40028` (bounded round404).
No implementation, authority, successful mutation or ISS041 completion claimed.

## Scope and problem

The finite routine census requires project-mutation-request/v1,
project-mutation-plan/v1 and project-apply-receipt/v1 even when the walking
skeleton legitimately chooses COMPLETE and skips12/13. Readiness found no
literal fields, inspectable effect representation, transaction identity,
pre/post observation or known-refusal/partial-unknown matrix. Installation CAS
and release receipts do not define adapter project effects and cannot be reused
as permission. This packet closes the three existing names as one group.

Base is independently reviewed disposition definitions/status035b962. Its
actual context and captured bytes remain the input to request admission.
Independent readiness is retained in the host's mutation-family note; it is
not itself an accepted representation or evidence of an executed operation.

## Explicit bounded choices

- One finite COMPARE_REPLACE operation over complete small adapter-owned byte
  resources. Closed absent/present pre/post values retain every actual byte,
  with4096-byte per-value and64-effect bounds. This is inspectable data, not a
  generic patch, program, path, executable or opaque effect hash. Admitted adapter
  source must prove complete resource mapping and semantic compatibility; no
  partial projection or omitted effect is allowed. Different semantics replan.
- Request binds actual APPLY/PROJECT context and immutable target, one stable
  owner-issued transaction and canonical configuration/cycle/disposition.
  ASSEMBLE_CERTIFY/PROMOTE remain release-owner routes outside project admission.
- Fresh dry/pre/post observations retain full existing project facts and the
  actual complete resource census. Exact-plan apply conservatively rechecks
  the entire dry frontier and resources; APPLIED needs every planned postvalue.
  Source identity, capture/materialization and actual authority are owner gates.
- BEFORE_WRITE known refusal cannot report uncertain or partial effects.
  WRITING/AFTER_WRITE unknown retains phase/count/observations without retry or
  vacancy authority. Existing committed transactions return identical evidence;
  matching postimage alone never proves issuance, idempotency or history.
- All three framed identities and the unframed inline observation commitment
  are explicit and acyclic. Request never embeds a later plan/receipt identity.

## Attack surface and acceptance evidence

| Boundary | Required discriminating evidence / smallest alternative |
| --- | --- |
| Complete operation data | Missing/extra/hostile fields, opaque digest in place of bytes, uppercase/odd/oversized hex, unchanged/absent-to-absent effect, duplicate/out-of-order/empty/oversized census refuse; all create/remove/replace cells positive |
| Wrong authority owner | Actual nonapplying/rejected/unknown/candidate/release disposition cannot enter project binding; no generic release executor or new approval token |
| Exact immutable preimages | Substitute action/context/raw terminal/review/disposition/target/config/request/plan-id/transaction and rehash downstream; relation rejects rather than trusting expected digest bags |
| Actual observation tuple | Reused observation IDs, wrong project/config, invalid observation hidden as unavailable, mismatched dry/before/after census and wrong planned postvalue refuse; no boolean freshness proof |
| Known versus uncertain effect | Every phase/outcome/null/count cell pinned; partial write cannot be REFUSED/APPLIED, no retry/reclaim/completion inferred from UNKNOWN or matching bytes |
| Canonical compatibility | Independent complete canonical strings, full frames/digests, inline observation hashes and all reused nested contract bounds; no partial public union |
| Runtime exclusions | Source mapping/semantics, real source reads/writes, policy/lease/breaker/credentials/current progress and duplicate-call zero-effects need later owner execution evidence; pure parsers certify none |

## Non-goals, footprint and verification

Exactly three documentation paths: shared contract ledger, additive ISS013
note and this pressure record. No new public family, dependency, issue/edge,
roadmap, service, SDK/CLI handler, engine, journal, provider, native probe,
compiler, release/production mutation implementation or ISS026 work.

Predicted implementation: one complete three-family contract module plus
inseparable registry/generic parser/harness wiring and independently authored
compatibility tests. Literal ambiguity, incomplete projection or a needed new
operation/authority is a named replan boundary, not permission to guess code.
Required host verification is planning/checker/format/whitespace, independent
bounded review and adjacent exact draft/board reconciliation before protected
landing. Source author ran no tests and claims no self-PASS. Complete runtime
cycle acceptance remains separate.

Reclaim-readiness closure before review: plan resourceIntents and apply resources
reuse the complete round398 inline claims, restricted to the actual ADAPTER and
mutation transaction. Every arm retains the full census; APPLIED requires all
allocated, REFUSED a known allocation prefix, UNKNOWN retains uncertainty.
This is separate from changed project resources and adds no public family.
