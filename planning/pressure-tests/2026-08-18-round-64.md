# Pressure test — round 64 author composed-evidence repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `f05f8e0b64faccefe43a98ef2ba3299bf3dacf12`

Implementation of the round-63-authorized checkpoint evidence exposed three
undefined public nested records at the next dependency boundary. The user
authorized the author to supply the smallest exact field tables and formulas.
Round 63's `PASS` does not apply to this amendment. This record grants no
implementation, merge, promotion, installation, release, or runtime authority;
only a fresh independent exact-head `PASS` may authorize implementation.

## Bounded repair

- `pointer-mutation-run-intent/v1` is now one exact create-once
  `ORDINARY|AUTHORITY_ROTATION` union. Its common census binds `SINGLE_EPOCH`,
  target identity, `G`, the selected old-authority `Dp/Dt/Dv/Dr`, and creation
  time. Rotation alone carries the closed rotation input and recomputed `Drot`,
  expected successor `Dv`, expected head ordinal/record digest, and `Dsc`.
- `pointer-mutation-conflict-evidence/v1` now has one exact six-member
  composition of the losing proposal, selected real winner, conflict receipt,
  and target identity. `pointer-evidence-slot/v1` has its exact three members
  and a closed null/generic-selection/conflict-composition nested union.
- `authority-history-binding/v1` now has one exact six-member non-persisted
  composition of `G`, head ordinal/digest, the complete dense linear history
  array, and genesis-selection evidence. It reuses the existing full chain
  walk and cross-binds the packet's selected current authority value.

## Proportionality and trust

The repair names and closes evidence already required by the reconciled
protocol. It adds no pointer kind, storage path, mutation, receipt, selector,
tree, membership proof, directory census, inventory, coordinator, checkpoint,
compaction, serialized capability, or self-certification route. Structural
records remain non-authoritative without ISS-004's private live context,
selected exact authority, kernel lock, and stable-predecessor promotion path.

## Required independent attack

Review the exact committed repair head cold. Attack every census, type,
canonical and semantic order, branch absence, digest part, selected-old tuple,
target-arm equality, rotation-input/`Drot`/record/head/`Dsc` relation, loser and
winner substitution, conflict-receipt recomputation, slot-kind/outcome matrix,
history length/ordinal/predecessor/`G`/head/genesis-selection equality, and
packet-current-authority cross-binding. Try to reintroduce an epoch key,
caller-selected winner, sparse membership/root/proof, directory inventory,
compaction, rotation receipt, adapter vocabulary, candidate self-verdict, or
serialized authority. Older PASS dispositions do not apply.
