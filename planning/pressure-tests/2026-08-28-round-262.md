# ISS-022 Windows custody cleanup review — round 262
`PASS`.
Only the non-authoritative grandchild cwd moves from custody to `dirname(process.execPath)`, an existing reviewed runtime directory; its fixed timer/exit mode has no IPC, output, filesystem mutation, or checkout write.
The direct child still owns the private IPC exchange, custody cwd, exact `SIGTERM`, and provider-observed `exit`/`close`; the grandchild PID claim remains hostile and unavailable as authority.
Immediate recursive custody removal occurs after the process-probe promise has observed direct-child close, so the test detects the Windows cwd-handle leak without claiming grandchild death.
No mutation authority moves outside custody and no helper, process-tree, or filesystem guarantee is added; the two-line runtime change and cleanup assertion are proportional.
Focused tests pass 10/10 and root typecheck passes.
