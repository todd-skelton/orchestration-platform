# ISS-022 native fixture foundation — round 372

Static `PASS` at `306fa80f28d59d6a5309b86a73beb9f4d48939db` from independent
reviewer `/root/iss022_native_driver_review`, Sol high, row 11. Initial
`a990f574067df3bcfd9b28e2aa0ec688cb2b19c2` was BLOCK_FIXABLE because successful
metadata facts could omit identity and recover it from a later record.

The two-file repair rejects missing post-OPEN identities in live admission and
accepted custody, preserving legitimate initial OPEN-null and null-custody
refusals. Focused mutations retain the final identity while removing each
earlier identity. The repair matches actual native call snapshots without
changing the native sources or extending the fixture protocol.

The foundation closes native facts, load identity/hash checks, candidate
commands and stable default-child lifecycle. It is not the complete experiment:
parent case orchestration, native custody/HELD barriers, watchdog/death timing,
all twelve controls, report reduction and provider joins remain prerequisites.
The small guard repair prevents incomplete evidence from becoming accepted
metadata without another framework. Review ran no tests or native code;
host execution and hosted native compilation remain separate gates.
