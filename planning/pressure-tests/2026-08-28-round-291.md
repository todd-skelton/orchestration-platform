# ISS-022 CAS post-repair review — round 291

## Disposition

`PASS` for the exact `+817/-26` aggregate uncommitted CAS diff over
`331c567df94dd6ae5e6077d3f71b535669e6b670`. This is the one permitted
post-repair review and authorizes only the two CAS rows, not an ISS-022 profile.

## Pressure result

The stable parent spawns two direct `CAS_BARRIER` children and awaits each
closed `READY` IPC record binding request `41 -> 42`. Only after both handles
have supplied that fact does source order record the single release barrier and
send the exact release command to each child; both closed `RELEASED`
acknowledgements are awaited before terminal collection. Missing, forged,
reordered, wrong-request, wrong-ID, accessor/proxy, sparse, extra, or mixed
barrier/contender facts refuse. Child IPC never supplies a verdict, and the
reviewed worker cannot enter CAS before receiving the parent release.

The worker performs predecessor read, proposal creation/sync, replace,
directory sync, and selected `42` readback while its lock handle remains open.
The mismatch arm reads the actual byte before close and has no write/rename
path. The structural regression pins both orderings. Exact request binding also
separates the deliberate `42 -> 42` mismatch row from the `41 -> 42`
contention row.

PASS is now limited to two coherent fresh-child terminal envelopes after the
closed barrier census: one `SELECTED(42)`, one
`PREDECESSOR_MISMATCH(actual 42)`, and parent final readback `42`. The live
persistent-`O_EXCL` result, `SELECTED + EEXIST`, is UNSUPPORTED and EEXIST has no
PASS branch. A Windows directory-sync failure after writing `42`, equal
unsupported codes, or one admitted unsupported code plus EEXIST also remains
UNSUPPORTED; unsupported plus selection, wrong final bytes, timeout, signal,
nonzero exit, stderr, output mismatch/overflow, or duplicated terminal arms is
UNKNOWN or refusal. Candidate text has no authority.

The mismatch row independently requires coherent terminal mismatch/readback
`41`, unchanged parent final `41`, predecessor `42`, and proposal `42`; error or
readback contradictions cannot PASS. Fresh per-row directories, canonical
source-disjoint custody, stable execution, and the added `cas.ts` closed bundle
census prevent cross-row/source substitution.

## Proportionality and verification

The concrete threat was promotion of a nonparticipant EEXIST loser or forged
barrier order to CAS PASS. Deleting the IPC barrier, request-bound acknowledgements,
or exact terminal reducer reopens it. The `817/-26` aggregate separates cleanly
into a raw executor/worker half and stable normalizer/mutant half, each an
ordinary one-sitting surface; keeping them together supplies executable evidence
for the same two-row contract. No delete, retry, helper, sequencer, receipt,
production lock, or promotion mechanism was added.

- Focused CAS handler, filesystem executor, and stable-bundle suites: 54/54 passed.
- Portable-primitives and conformance TypeScript checks: passed.
- `git diff --check`: passed.
