# Pressure test — round 12

**Disposition:** `BLOCK_REPLAN`

The twelfth independent review found one implementation-blocking ownership gap.
The 39-issue packet remains unregistered pending a fresh review of the repair.

| Finding | Repair applied |
| --- | --- |
| `OP-R12-001` — Broker-client issuance was non-implementable: the credential package correctly forbade runtime exports and deep imports, while the bootstrap bundle required a composition factory, but no installed runtime composition owner or private build entrypoint existed. | Defined a build-only closure generator in `packages/credentials/build/compose.ts`; assigned exact private composition entrypoints to `ISS-020` (bootstrap) and `ISS-033` (installed self-host adapter); made `ISS-000` own the build-only `#broker-compose` alias and negative export/tarball/source-map tests; added `ISS-038 → ISS-033` so installed composition consumes the production host-custody boundary. |

## Re-review focus

- Confirm the two authorized bundler targets can receive object-capability clients
  without creating a runtime or package export.
- Confirm no third package, deep import, emitted tarball, or source map can recover
  the composition generator.
- Confirm dependency manifests, issue frontmatter, epic DAG prose, and the
  authoritative roadmap agree.
