# ISS-022 diagnostic terminal verifier pressure review — round 313

`BLOCK_REPLAN` at exact base/HEAD
`3787c1d5830ab3005cceee68b509208d9384811d`.

The uncommitted four-file, `+690/-7` slice stays inside the existing terminal
adapter and four bounded arguments. It requires a clean protected-main checkout,
exact stable bundles/registry, overall FAILURE, the current run/attempt, a unique
successful record job and diagnostic record artifact, the live protection floor,
the record's present/missing census, retained artifact bytes, and Round 311 replay
when diagnostic receipts exist. Its closed digest can emit only `BLOCK_REPLAN`;
it adds no decision, publication, promotion, workflow job, helper, root command,
or portable-engine GitHub policy.

Two terminal-boundary blockers remain:

1. `readGithubTerminalJobs` never reads or checks the provider job `status`, and
   `liveJobFields` has no status member. A current-attempt row with
   `status:"in_progress"|"queued"` and a terminal-looking conclusion projects
   identically to a completed row; this includes the supposedly unique successful
   record job. The record writer checked `status:"completed"` for required jobs,
   but the administrator terminal does not reauthenticate that live fact.
2. The final authenticated GMT `Date` is not an upper bound on the diagnostic
   writer chain. Present provider artifacts require `createdAt <= verifiedAt`,
   but the diagnostic record artifact does not, and the inclusive writer check
   ends at `createdAt <= recordJob.completedAt` without requiring
   `recordJob.completedAt <= verifiedAt`. With no present provider artifact, or
   with an earlier observation artifact, a stale but canonical final Date before
   record creation/completion can pass and make the 30-day-remaining calculation
   use contradictory time evidence.

Both threats are in scope and deletion-discriminating: omitting live job status
or the final-time closure admits the counterexamples above. The smallest repair
is confined to the existing job projection/verifier and writer-time comparison;
no new record, schema, API family, or workflow surface is justified. The
697-line review surface remained readable in one sitting, with 251 changed test
lines, so size alone is not a blocker.

Focused evidence passed: the touched hosted-terminal API tests (8), hosted
aggregate tests (3), repository TypeScript check, touched-file Prettier check,
and `git diff --check`. Those tests do not currently mutate live job status or
place final `verifiedAt` before the diagnostic record writer chain.
