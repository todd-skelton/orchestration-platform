# ISS-002 pressure test — round 126

## Review basis

This is a fresh bounded removal review of OP-R125 at exact initially clean head
`f10c7c373d8bdb8d78c2cbe6b8f8a230e37ca0ca` on
`iss-002-versioned-contracts`, with exact parent
`0a297f69969dccdca7b4692b320c6554c67ff526`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The review read `AGENTS.md`, `docs/planning/work-model.md`, the sole authority
in `docs/architecture/contract-decisions.md`, the registered `ISS-002` draft,
round 125 as the blocker under retest, and the exact parent-to-head diff and
affected source/tests. No Chase Sets repository, project, issue, board,
backlog, consumer source, or other consumer system was read or touched.

The exact repair changes only `packages/contracts/src/index.ts` and
`test/contracts/recovery-core.test.ts`: 20 insertions and two deletions. It
routes the already public serializer's digest for exactly
`recovery-authorization-core/v1` through the existing sole `Dac` helper and
adds both-branch focused evidence. It adds no schema, package, command, state,
native/post-selection/archive/tombstone/composition record, broker operation,
persistence lookup, live capability, proposal, CAS, runtime mutator, planning
change, or board topology. `git diff --check` passed. The checkout was clean
before this required pressure record was written.

## Disposition

`PASS` for removal of OP-R125 at exact reviewed head
`f10c7c373d8bdb8d78c2cbe6b8f8a230e37ca0ca`.

The public serializer no longer exposes an untagged second recovery-core
identity. It returns unchanged canonical bytes and the sole authorized
one-part, domain-separated `Dac` for both BOOTSTRAP and SUCCESSOR. The repair is
schema-specific; generic serialization for other schemas remains unchanged.
No fresh blocker or smaller repair is required.

## OP-R125 removal and exact identities

The repaired public route parses first, then chooses
`computeRecoveryAuthorizationCoreDigest(parsed.value)` only when the expected
schema is the exact literal `recovery-authorization-core/v1`. That helper
reparses the detached closed snapshot and computes exactly:

```text
SHA256(frame("recovery-authorization-core/v1", canonical core bytes))
```

The frame still has exactly one canonical-record part. No raw JSON, untagged
canonical hash, member-wise frame, alternate domain, or second core identity is
returned by public recovery-core serialization.

An independent lockfile-pinned in-memory audit observed these exact pairs:

| Mode | Public serializer and dedicated `Dac` | Untagged canonical digest |
| --- | --- | --- |
| BOOTSTRAP | `a8de464e22b7194daf49b52f102e6513b19255ff49d965fe68e4ef8940e67879` | `7a2eea3d0f85f3373410b63171584c1d5a12bd16a9913629f1af1f25a25f4fa3` |
| SUCCESSOR | `d92dd5a77ab011dcc73f90bb3ad28d7444ddd4650306063de64d33d9152dc68a` | `c4385871d7402f133a7e9b4f9c2032d2aa39cfa337c04f91c45079e145efc259` |

For each branch, the public digest equaled the independently recomputed framed
`Dac`, differed from the untagged digest, and accompanied bytes exactly equal
to canonical LF-terminated core bytes. The pre-existing complete-branch byte
and `Dac` goldens remain unchanged.

## Deletion discrimination and generic control

The audit built a second module entirely in memory and deleted only the exact
schema-specific serializer condition. It wrote no source or test file. Under
that single deletion, both otherwise valid cores fell back to their distinct
untagged canonical digests. The committed both-branch assertions then fail
twice per branch: the serializer digest no longer equals `Dac` and instead
equals the explicitly forbidden canonical digest. The evidence therefore
directly discriminates OP-R125's recurrence rather than merely exercising the
dedicated helper.

As a non-recovery control, the audit serialized a valid
`platform-configuration/v1` through both current code and the route-deleted
module. Both produced identical canonical bytes and the identical established
generic digest
`39788e7a712dae252a63c0c4ddd10afbe2f4dd947902f314194f73eb4c4126ff`.
The parent-to-head diff likewise leaves the generic `else` arm as the existing
`canonicalDigest(parsed.value)`. The repair neither changes another schema's
serialization identity nor introduces a general digest registry.

The focused recovery-core plus existing external-serialization tests passed
independently at two files and thirteen tests. The full suite then supplied the
complete compatibility and generic regression evidence.

## Totality, scope, and trust boundary

Public serialization still calls the total public parser before either digest
arm. Independent probes supplied null, an array, a primitive, an exotic
instance, a throwing proxy, and an accessor whose getter must not execute;
each refused without throwing. An exact null-prototype BOOTSTRAP record was
accepted, serialized to canonical bytes, and returned the BOOTSTRAP `Dac`.
The schema-specific route receives only the already detached valid parsed
record, and its dedicated helper remains total for that closed value.

The repair computes structural identity only. It does not certify
`producerDigest`, consult a clock or broker, authenticate admission, construct
the later fence composition, read persistence, select a pointer, or grant a
capability. The acyclic order remains `Dfr -> Dac -> Dgr`. BOOTSTRAP and
SUCCESSOR composition must still independently recompute their reviewed `Dsc`
and bind all authenticated grant/review/release/broker/fence facts. A candidate,
worker, host assertion, core, serializer result, or this review cannot certify
its producer.

Recovery-authorization lifecycle/state, native consume/removal,
post-selection observation/receipts, attachment, archive, tombstone, composed
equality, selected epoch, proposal, tip, CAS, persistence, broker operations,
live capability, command, filesystem mutation, and runtime promotion remain
outside this implementation and unauthorized.

## Proportionality and deletion bound

| Mechanism | Concrete in-scope threat | Deletion result | Smallest credible disposition |
| --- | --- | --- | --- |
| Exact schema-specific serializer digest arm | Generic serialization otherwise exposes the untagged OP-R125 identity. | Deleting only the condition makes both public core digests untagged and fails the committed negatives. | Retain the four-line branch; a digest registry or serializer redesign is larger and unevidenced. |
| Both-mode serializer assertions | A repair might cover BOOTSTRAP but leave SUCCESSOR with a second identity, or preserve digest while changing bytes. | Each mode independently falls back under route deletion; canonical-byte equality is separately pinned. | Retain one compact loop over the two existing fixtures. |
| Unchanged generic `else` arm | A broad repair could silently change unrelated public serialization contracts. | The platform-configuration control would change. | Retain the existing canonical-digest arm for every other schema; add no abstraction. |
| Downstream deferral | Treating structural serialization as live authority would widen trust beyond the core ledger. | Deleting the boundary invites copied equality and candidate self-certification. | Preserve fail-closed deferral; add no state, broker, persistence, capability, CAS, or runtime surface. |

The repair is the smallest shape that removes the observed public identity leak
while preserving all unrelated serializer behavior. Its single literal branch
is smaller and more directly evidenced than a new registry, callback map,
serializer family, or contract-wide digest redesign.

## Verification evidence

The local `pnpm` launcher produced no output and did not invoke the first
checked-in script within 30 seconds, so it was interrupted. Following the
approved infrastructure-only fallback, each required gate used the identical
checked-in script or lockfile-pinned binary directly under read-only
escalation. No source, test, dependency, or semantic command was substituted.
All gates ran strictly one at a time. The standalone full suite fully exited
before the separate bootstrap verifier began.

1. Direct equivalent of `pnpm run planning:check`,
   `node scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`,
   `node scripts/planning/board-check.mjs` — PASS: `board contracts verified`.
3. Direct equivalent of `pnpm run format:check`,
   `node scripts/format.mjs check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`,
   `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`,
   `node node_modules/vitest/vitest.mjs run` — PASS: 30 files and 367 tests in
   86.78 seconds. The process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`,
   `node scripts/verify/bootstrap.mjs` — PASS: manifests and registries,
   formatting, typecheck, 30 files and 367 tests in 81.20 seconds, private
   compositions, planning contracts, and source-tree status invariance.

## Authorization boundary

This `PASS` clears exact head `f10c7c373d8bdb8d78c2cbe6b8f8a230e37ca0ca`
only for OP-R125 removal and the bounded recovery-authorization immutable-core
parser/path/`Dac`/public-registry/serialization surface previously authorized
by the clean planning removal round. It does not broaden implementation or
runtime authority to any deferred recovery, composition, persistence, broker,
capability, CAS, or promotion surface.

This record grants no merge, release, installation, external mutation,
promotion, or self-certification authority. Stable release N may build and test
N+1, but only external review and the stable promotion path may grant N+1
authority; a worker or candidate release never certifies itself.

After this record was written, the sole worktree change was
`planning/pressure-tests/2026-08-21-round-126.md`; no source, test, draft, issue,
roadmap, board, or other planning file was edited, and no commit was created.
