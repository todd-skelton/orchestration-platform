# ISS-022 alias-observation repair follow-up — round 267
`PASS`.
Absence is now authorized only when the first awaited lstat returns `ENOENT`; after lstat observes an entry, stat/realpath `ENOENT` propagates as movement/refusal and cannot become `DISTINCT_ABSENT`.
The disappearance mutant proves that later ENOENT is preserved, and two stable real entries prove the `DISTINCT_EXISTING` relation without forging identity.
The exact alias corpus, OS expectations, repeated relation/leaf/root stability facts, four-row order, retained namespace/root handle, path omission, cleanup, and no-Dphys/no-PASS boundary remain unchanged.
Provider injection is confined to raw leaf observation tests and is not used by the public session to grant authority; no normalization or destination route was added.
Focused tests pass 7/7 and root typecheck passes; the repair is proportional to Round 266.
