# ISS-006 Windows admission clean landing review — round 205

## Disposition

`PASS_TO_LAND_WINDOWS_SLICE`.

This is the fresh independent exact-tree removal review required after Round
204. The current uncommitted private Windows pre-execution admission slice is
clean to land against pushed HEAD
`c3de3212ddb616ceafb7508917a43bf62c414e96`, the sole ISS-006 authority, and
Rounds 175 and 197–204.

The last two removal gaps are closed. The canonical-plan fixture now constructs
independent expected application, command, cwd, environment, environment-unit,
and flag values before real suspended creation. The outer lifecycle fixture
now distinguishes admission outcomes 0, 1, and -1 with exact status, cleanup,
and release observations. I found no surviving deletion mutant in the latest
reviewed packet.

This disposition authorizes landing only this private, refusal-only Windows
slice. It does not resume candidate code, emit authority or evidence, complete
the Windows launcher, complete ISS-006, relax the macOS requirement, or
authorize a draft/ledger/board change.

## Exact reviewed tree

```text
HEAD c3de3212ddb616ceafb7508917a43bf62c414e96

215b011af27cd6162644b56a558624368e929e8701fd87c2bfe8c2865b5c95be  packages/conformance/src/windows-isolation-broker.c
712f0280cfbda59582849efc441bc458fcac33ee69857ae764efd969d75fa504  packages/conformance/src/windows-isolation-broker-x64.exe
066353a70969ff152959adf4e8c731e38c7b8d8dc7fcb3b7a5957dc198469a31  scripts/build/windows-isolation-broker.mjs
54e3108b5994d5dffe81f52af69d290db2c5c31c13b577c2608cf88abd38d652  test/conformance/windows-isolation-broker-fixture.c
7ee37ec526a208a925e35883ec680b7691108090c3f755ab66cce5b9b08ae038  test/conformance/windows-isolation-broker.test.ts
3c0e366611a62257fab3f678464f2201d9174ef7f4f5c42ed29d01023e004426  test/conformance/windows-appcontainer-profile.test.ts
b2e6976325f35bf6a2335711658c959fa76b236cfc98188a2cd37a3ae9f12734  planning/pressure-tests/2026-08-26-round-204.md
```

The tracked PE is exactly 90,112 bytes. Its SHA-256 matches the focused census
and the reported real diagnostic. The pinned production build never defines a
fake-OS fixture macro, and the focused test rebuilds the PE byte-identically.
Fixture seams are absent from production bytes.

I read the repository instructions, work model, complete ISS-006 draft and sole
ledger, Rounds 175 and 197–204, complete current diff, production broker,
latest exact-source and lifecycle fixtures, TypeScript driver, OPWL publication
matrix, real diagnostic, build/PE census, stable bundle census, and package
exports. I did not edit implementation, run the opt-in mutating native
diagnostic, or access Chase Sets.

## Final Round-204 repair verification

### Independent canonical Dlaunch projection — clean

`fixture_admission_canonical_plan_matrix` invokes real
`admission_plan_digest` over modeled stable, integrity, and package identities;
valid retained original descriptors; exact profile, parent, execution-root,
runtime, RPC, and candidate paths; and the fixed target census.

It then independently constructs and compares:

- application equal to exact target 0 (`node.exe`);
- exactly three quoted arguments: target 0, target 1, and the independently
  literal expected candidate `file:///C:/.../candidate.mjs` URL;
- cwd equal to the exact profile folder;
- environment in exact order:
  `LOCALAPPDATA`, `SystemRoot`, `TEMP`, `TMP`, `WINDIR`;
- exact profile/Windows-directory values, separators, embedded NULs, terminal
  double NUL, and `environment_units`; and
- exactly `CREATE_SUSPENDED | EXTENDED_STARTUPINFO_PRESENT |
  CREATE_UNICODE_ENVIRONMENT | CREATE_NO_WINDOW`.

The expected command uses fixed literals and generic concatenation, not
production quoting or candidate-file-URL helpers. The expected environment
uses a fixture-local encoder, not production `append_environment`. Formula
deletions or substitutions therefore change one side only.

Real `create_suspended_admission` then receives the plan. Fake
`CreateProcessW` requires its mutable command pointer to differ from the stable
plan buffer while remaining byte-equal. Real cleanup proves the one attribute
delete, two exact pipe drains, terminal Job/process state, and handle absence.

The Round-204 application/argument/cwd/environment/copy mutants no longer
survive.

### Closed outer admission result reducer — clean

The compiled lifecycle matrix now has three distinct launch outcomes:

| Admission result | Required cleanup | Required status/exit |
| --- | --- | --- |
| `1` | execution, profile, release | NOT_IMPLEMENTED / 78 |
| `0` | execution, profile, release | REFUSED / 65 |
| `-1` | release only | RECOVERY_REQUIRED / 70 |

The new `lifecycle-admission-refused` scenario returns zero and requires exact
admission, execution cleanup, profile cleanup, and root release diagnostics,
plus response and exit 65. The ambiguous scenario requires admission then root
release only and status/exit 70. The successful refusal-only admission remains
status/exit 78. Comparison widening, status substitution, or cleanup-guard
deletion now fails the compiled test.

## Complete accepted removal evidence

The clean packet now retains all earlier accepted evidence:

- all 16 original/grant descriptor combinations and every individual third
  object across phases 1–6;
- children-before-root grant, root-before-children best-effort restore, every
  partial failure, exact final-original proof, and clean retry;
- exact ordered 30-row AccessCheck and 34-row impersonated-open oracles,
  including the fixed broker-module path and every per-call failure;
- bounded scalar and variable token queries, zero/no-allocation/over-limit/
  wrong-error/second-call/returned-length cases for all five variable classes;
- closed SID pointer/revision/subauthority/extent checks; complete group count,
  row, attribute, forbidden-enabled-group, duplicate, hole, and trailing cases;
- exact zero capability relation, with a complete nonzero-capability mutant;
- Job create/preexist/security/limit/accounting/PID/terminate/close/reopen
  cases, including a fully canonical 65-PID over-bound input;
- all 22 partial suspended-launch prefixes through real cleanup, exact stdio/
  sentinel/security-capability/Job/handle-list/application/argv/cwd/environment/
  flag postconditions, each remaining handle close, free, and simultaneous
  ambiguity;
- exact Round-198 OPWL serializer/parser/publication branches and faults;
- exact lifecycle component and phase-failure sequences; and
- recovery phases 1–6 in one durable order: authentication, kind 6, Job
  terminality, root-first restore, final custody/census/scratch proof, kind 7,
  release, and retained kind-6/kind-7 failure retry.

The real tracked-PE diagnostic remains complementary native API/ABI evidence.
The reported repinned run passed 1/1 in 86.37 seconds. It does not replace the
compiled deletion matrix and does not emit authority.

## Trust, scope, and proportionality

The landed slice preserves the stable-N/candidate boundary:

- stable N alone owns Dgrant, Dlaunch, expected token/Job/access values,
  durable OPWL, reversal, and status;
- the exact package SID receives only read/execute while stable and SYSTEM
  retain custody;
- one non-breakaway kill-on-close Job and the closed stdio handle list are
  atomically attached with exact security capabilities;
- production contains no `ResumeThread`, so candidate and RPC bytes never
  execute in this slice;
- child stdout/stderr must be empty and never carry a verdict;
- unresolved OPWL state bars OPWX and profile cleanup; and
- successful pre-execution proof still returns private NOT_IMPLEMENTED after
  complete reversal.

There is no receipt, aggregate, provider result, authority, certification,
selection, installation, consumer mutation, or promotion path. The engine sees
no Git or GitHub vocabulary. The broker and PE remain outside package-root
exports and inside only the exact stable bundle census.

Every retained mechanism answers a concrete principal, mutation, crash,
terminality, hostile-observation, or deletion threat. Adding a service,
TypeScript privilege fallback, same-principal launch, candidate self-probe,
wider ACL, public schema, or candidate-visible stable/source path remains
disproportionate and unauthorized.

No ISS-006 draft, sole ledger, roadmap, or board amendment is warranted. This
slice materially advances Windows/Linux-first private infrastructure but does
not complete the Windows runtime: candidate resume, terminal response/timing,
and final hosted composition remain future independently reviewed work. macOS
remains required for ISS-006 completion and hosted PASS.

## Checks actually run

```text
exact status/hash/diff and call-path census
  PASS — reviewed the latest six-file implementation/test slice and Rounds
         197–204 against HEAD c3de3212

pnpm exec vitest run test/conformance/windows-isolation-broker.test.ts --testTimeout=600000
  PASS — 1 file, 12 tests
  Duration 273.80 s; tests 272.67 s

pnpm exec tsc --noEmit -p tsconfig.json
  PASS

compiled removal census
  PASS — no surviving mutant identified in the latest bounded slice

node scripts/planning/check.mjs
  PASS — planning contracts verified

node scripts/planning/board-check.mjs
  PASS — board contracts verified

pnpm exec prettier --check planning/pressure-tests/2026-08-26-round-205.md
  PASS — all matched files use Prettier code style

git diff --check
  PASS — no errors; current Windows implementation/test files have existing
         LF-to-CRLF notices
```

The focused run was slower than the reported 119.78-second run but completed
well inside its explicit 600-second test timeout.

## Landing authorization

`PASS_TO_LAND_WINDOWS_SLICE` is the clean latest removal disposition for the
exact tree above. It authorizes landing this private refusal-only Windows
pre-execution admission slice and its deterministic PE/tests/planning records.
It grants no runtime authority, public contract, ISS-006 PASS, receipt, or
promotion capability. Subsequent resume/protocol/timing/composition work
requires a new bounded planning and removal round.
