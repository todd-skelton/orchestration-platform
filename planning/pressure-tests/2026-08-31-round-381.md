# Module literal prerequisite and explicit ABI replan — round 381

Status: PROPOSED; independent exact-head review and host verification pending.
Authoring base: `70b18877cc26f4e323cfd3469a72c7cc1e7b21de`. This packet is
documentation only and grants no implementation or runtime acceptance.

The existing fixture has a real configuration/snapshot/current-policy to
action-core/brief consumer but a private descriptor and no admitted module
input/result. A breaker-receipt shortcut would require undefined durable
history/open/probe/recovery evidence and could mistake NO_TRIP for permission.
The smaller useful next boundary is the five already enumerated module names,
reusing actual observations and existing dispatch/subject contracts.

The ABI currently requests undefined reduced facts and a policy digest that
ISS-013 explicitly does not define. The proposal names both contradictions:
use the actual COMPLETE observations, configuration/provenance and exact
policy tuple; do not call them reduced state or hash policy claims. Finite
exact compatibility tuples replace the undefined range interpreter, and
host compatibility keeps the existing capability/renderer contracts.
These are explicit replans for review, not silent requirement relaxations.

The literal packet closes descriptor/action/catalog/code declarations,
ordinary and review-target inputs, worker/direct action plans, normal no
action and typed refusal. Refusal is a closed arm of the existing no-action
family; the result union has no persisted wrapper or fifth identity. Existing
catalog bounds remain unchanged, including the exclusion of workerless-only
empty-catalog descriptors. No source loader, registry generator or arbitrary
schema/lookup payload is added.

The deletion tests are narrow: dropping actual input joins permits cross-
configuration/policy/cycle substitution; dropping descriptor/core/brief/target
joins permits a changed action; dropping catalog/census checks permits hidden
capabilities or ambiguous rendering. Required future vectors pin all concrete
bytes/frames, bounds, null/enum cells, hostile input refusal and independent
equality removal mutants. Parser success must never supply breaker, installed
registry, reviewer or mutation authority.

Prediction: four concrete parsers, one union dispatcher, four framed identities
and one supplied-record relation, followed by a bounded observer-fixture
continuation. The packet changes only the architecture subsection, ISS-002
additive note, explicit ABI correction and this pressure record. A needed new
family, authority source or owner change is a concrete replan boundary.
No verifier, test, build, probe, provider action or ISS-026 work was run; no
ISS-041 completion or independent-review PASS is claimed.
