# Pressure round 467 - ISS-036 planning repair and credential-free decomposition (CF-0)

Status: attempt 3 of 8, a bounded same-file correction. Planning artifact
only. Base `60269e3218f1d691da276da3a59b63cd8f839bf3` on protected `main`.
This record claims no review, no independent confirmation, and no
acceptance. It does not claim ISS-036 is ready, that any live provider work
is authorized, or that `ISS-054`/`ISS-013` closure alone unblocks the live
window. Attempt 2, an independent review at exact head
`5af55932931c25efa53ab7cfb060731cc6846add` (Opus 5 high, routing row 8),
returned `BLOCK_FIXABLE` with two blocking findings (F1, F2); this attempt is
the bounded repair Decision #302 ruling 1A and Decision #272 admit — see
"Attempt 3 correction" below. Attempt 4 is the independent re-review,
pending, not run here.

## Authority

Decision #302 ("ISS-036 readiness, credential-free decomposition, and four
contract gaps before any live GitHub window"), ruling **1A/2A/3A** posted by
the controller host on Todd's instruction: 1A dispatches this CF-0 planning
repair (Sonnet 5 medium row 13 source; Opus 5 high row 8 independent review;
ordinary eight-attempt ceiling); 2A gives `ISS-063` (the four post-upload
environment variables) its own Decision, filed by the host once CF-0 lands;
3A defers the first live-window Decision until after CF-0 lands. The
readiness audit at
`D:\Users\ToddS\Source\Repos\orchestration-platform-handoff\lanes\iss036-readiness.report.md`
is the decomposition this repair registers; every citation this record and
the amended drafts make against that audit was re-verified against this
worktree at the stated base (`docs/planning/work-model.md`,
`scripts/planning/check.mjs`, `docs/architecture/host-custody.md`,
`docs/architecture/self-host-github-contract.md`,
`docs/architecture/contract-decisions.md`, `planning/roadmap.json`,
`planning/drafts/ISS-013.md:72,142`, `planning/drafts/ISS-054.md:824,826`,
`planning/pressure-tests/2026-09-05-round-456.md:675-690`,
`planning/pressure-tests/2026-09-05-round-460.md:260-270`,
`config/private-compositions.json:29-40`, `package.json`).

## Scope

Exactly thirteen files: `planning/drafts/ISS-036.md` (edit, converted to slice
form), eight new drafts `planning/drafts/ISS-056.md`..`ISS-063.md`,
`planning/roadmap.json` (edit), `planning/drafts/EPIC-SELFHOST.md` (edit),
`planning/drafts/ISS-038.md` (bounded edit binding its protected-path
sentence to `ISS-036`'s enumerated list and noting the seven-day re-issue),
and this round record.

## Non-goals

No source, test, workflow, contract, or provider byte. No issue creation,
labeling, or closure by this lane — the board note below names the exact
rows the controller host must create and mirror after this commit. No
live-window Decision text; Decision #302 ruling 3A already defers that to
after CF-0 lands, and this lane files nothing.

## DAG before and after

Before (`EPIC-SELFHOST` direct-edge block, `planning/roadmap.json` at base):

```
ISS-009, ISS-010 → ISS-014
ISS-038 → ISS-029
ISS-029, ISS-033 → ISS-020
ISS-012, ISS-031 → ISS-023
ISS-008, ISS-023, ISS-055 → ISS-021
ISS-013, ISS-054 → ISS-036
ISS-021, ISS-049, ISS-050, ISS-051, ISS-052 → ISS-033
ISS-020, ISS-039, ISS-040 → ISS-019
ISS-019 → ISS-037
ISS-037 → ISS-034
ISS-034 → ISS-027
ISS-027 → ISS-015
```

After (adds eight rows, changes `ISS-036`'s row):

```
ISS-009, ISS-010 → ISS-014
ISS-038 → ISS-029
ISS-029, ISS-033 → ISS-020
ISS-012, ISS-031 → ISS-023
ISS-008, ISS-023, ISS-055 → ISS-021
ISS-013, ISS-062, ISS-063 → ISS-036
ISS-021, ISS-049, ISS-050, ISS-051, ISS-052 → ISS-033
ISS-020, ISS-039, ISS-040 → ISS-019
ISS-019 → ISS-037
ISS-037 → ISS-034
ISS-034 → ISS-027
ISS-027 → ISS-015
ISS-054 → ISS-056
ISS-054 → ISS-057
ISS-059 → ISS-058
ISS-057 → ISS-059
ISS-054 → ISS-060
ISS-059 → ISS-061
ISS-056, ISS-058, ISS-060, ISS-061 → ISS-062
ISS-054 → ISS-063
```

### Transitive-reduction check

`ISS-054` is removed as a direct `ISS-036` dependency: the new chain
`ISS-036 → ISS-062 → ISS-056 → ISS-054` (and independently
`ISS-036 → ISS-063 → ISS-054`) already reaches it, so a direct
`ISS-054 → ISS-036` edge would be transitively implied and rejected by
`scripts/planning/check.mjs`'s `validateReducedEdges`. `ISS-013` is **kept**
as a direct edge: no credential-free sub-slice consumes `ISS-013` (none of
`ISS-056`..`ISS-063` names it in `blocked_by` or in "ISS-054 surface
consumed"), so no path from `ISS-036` reaches it except the direct one; per
`docs/planning/work-model.md:111-114`, a direct edge is retained when the
alternate path does not itself establish the same dependency, and here there
is no alternate path at all.

Within the eight new rows: `ISS-058`'s and `ISS-061`'s only dependency is
`ISS-059`; `ISS-059`'s only dependency is `ISS-057`; `ISS-057` and `ISS-056`
and `ISS-060` and `ISS-063` each depend only on `ISS-054`. `ISS-062` depends
on `ISS-056, ISS-058, ISS-060, ISS-061`; none of those four is reachable from
another of the four (`ISS-056 → ISS-054` only; `ISS-058 → ISS-059 → ISS-057
→ ISS-054`; `ISS-060 → ISS-054` only; `ISS-061 → ISS-059 → ISS-057 →
ISS-054`), so all four edges on `ISS-062` are direct and none is implied.
`pnpm run planning:check` is the mechanical proof; see the gate table below.

## Enumerated `protectedPathPolicies` path list and its derivation

`docs/architecture/host-custody.md:44-46` names the two review-producer
workflow files directly:
`.github/workflows/host-custody-bootstrap-build.yml` and
`.github/workflows/host-custody-bootstrap-review.yml`. The kit source paths
are named only implicitly, through the two bundle names at
`host-custody.md:34-38` traced to their build entry points in
`config/private-compositions.json:33` and `:38`
(`packages/host-custody/build/composition.ts` and
`packages/host-custody/build/broker-service-composition.ts`), both under
`packages/host-custody`. `ISS-036`'s repaired draft enumerates exactly three
`protectedPathPolicies` rows; whether row 3 is read as a directory prefix is
a property of the supplied ruleset semantics
(`docs/architecture/contract-decisions.md:6702-6704`), not of the path's
spelling, and remains subject to `ISS-036`'s own draft-ruleset measurement
(acceptance criterion 3) before any persistent ruleset is authorized.
Ordered by UTF-8 `path`:

1. `.github/workflows/host-custody-bootstrap-build.yml`
2. `.github/workflows/host-custody-bootstrap-review.yml`
3. `packages/host-custody`

`ISS-038.md` is edited in this same change to bind its protected-path
sentence to this exact list by name.

## F4/F5/F6 dispositions

- **F4** (two negative controls need `observedProtection`, which `ISS-036`
  does not produce): settled by registering `ISS-061` as an offline
  fixture-only harness with the explicit non-goal that it never becomes the
  `ISS-038` producer. `ISS-036`'s repaired acceptance criterion 9 states that
  those two controls are exercised live only once `ISS-038` supplies a real
  read-back producer, and that `ISS-061`'s fixture does not itself satisfy
  that criterion.
- **F5** (the receipt cannot survive seven days to its `ISS-038` consumer):
  the repaired `ISS-036` draft reclassifies the receipt as a re-issuable
  output and names the ruleset (numeric ID + semantic digest) and the
  verifier anchor as the durable outputs. `ISS-038.md` is edited to state it
  consumes a receipt re-issued within seven days of its own producer run.
  Neither draft specifies the re-issue procedure's operator cost beyond
  repeating operator actions O7/O8's chronology-locked window; that residual
  is left for the first live-window Decision (ruling 3A), not invented here.
- **F6** (draft wording the schema does not support): the repaired `ISS-036`
  removes "complete API routes/page counts/ETags" as a receipt claim (the
  receipt has no page-count member and GraphQL page ETags are nullable) and
  moves "before-and-after ruleset" and "permission census" to the probe's
  authenticated archive rather than presenting them as receipt members.

## Attempt 3 correction (F1/F2 disposition)

Independent review attempt 2 (exact head
`5af55932931c25efa53ab7cfb060731cc6846add`) returned `BLOCK_FIXABLE`:

- **F1**: the enumerated `packages/host-custody/` row's trailing slash
  produces an empty final path segment, which `isContractRelativePath`
  (`packages/contracts/src/runtime.ts:250-262`) rejects; a receipt carrying
  that literal refuses with `path:invalid` before any relation runs.
  Corrected in `planning/drafts/ISS-036.md`, `planning/drafts/ISS-038.md`,
  and this file to `packages/host-custody` (no trailing slash), preserving
  the three-row count, strict UTF-8 ascending order, and both cited kit
  source entry points (`config/private-compositions.json:33` and `:38`).
  The adjacent derivation sentence in each file now binds directory-prefix
  interpretation to the ruleset semantics
  `docs/architecture/contract-decisions.md:6702-6704` states, not to the
  path's spelling, and keeps that reading subject to `ISS-036`'s existing
  draft-ruleset measurement (acceptance criterion 3). This also corrects N4's
  loose `config/private-compositions.json:29-40` citation to the exact two
  entry-point lines, in the two files (`ISS-036.md`, this file) where the F1
  edit already touches the derivation; `ISS-038.md` never cited that range.
- **F2**: ISS-060's verification plan claimed the already-registered
  `test:harness-workflow-mutations` command already runs against the two new
  workflow files; `test/conformance/workflow-structure.test.ts`'s only
  subject is `.github/workflows/conformance.yml`
  (`test/conformance/workflow-structure.test.ts:11`). Corrected per the
  review's remedy (b): ISS-060 now states truthfully that its own
  implementation extends that same test file with coverage of the new
  bytes, names the file in its predicted footprint, and keeps the `ISS-006`
  command registered unchanged — no root-script or `implementedRootScripts`
  amendment is performed in CF-0.
- **N1** (carried from the original CF-0 instruction): `ISS-036` acceptance
  criterion 1's evidence clause now names the probe's authenticated archive
  (before-and-after ruleset configuration, app/token permission census) as
  required durable evidence, resolving the empty-destination gap the
  reviewer found in the F6 resolution note.
- **N5**: `ISS-036`'s operator action O1 now states it constructs the
  `orchestration-work/v1` block directly from `ISS-060`'s reviewed
  issue-form bytes, needing no separately published live template. O3 and
  every Decision-gated action (O4/O5/O6/O8/O10/O11) are unchanged.
- **N2** (control-count derivability) and **N3** (DAG clause order) are
  non-blocking and out of this bounded correction's footprint. N2 is carried
  to `ISS-058` (CF-3), the owning readiness work for the negative-control
  enumeration, without changing the control count or scope here. N3 remains
  a cosmetic observation about generated-block ordering; unchanged.

No self-certification: this correction claims no review, no independent
confirmation, and no acceptance. Independent review attempt 4 is a separate
pending dispatch.

## Verification

`pnpm typecheck`, `pnpm format:check`, `pnpm planning:check`,
`pnpm planning:board-check`, `git diff --check origin/main..HEAD`, and
`git status --porcelain` (empty after commit) — see the gate table in the
completion report.

## Predicted footprint

Exactly these thirteen files:

1. `planning/drafts/ISS-036.md`
2. `planning/drafts/ISS-056.md`
3. `planning/drafts/ISS-057.md`
4. `planning/drafts/ISS-058.md`
5. `planning/drafts/ISS-059.md`
6. `planning/drafts/ISS-060.md`
7. `planning/drafts/ISS-061.md`
8. `planning/drafts/ISS-062.md`
9. `planning/drafts/ISS-063.md`
10. `planning/roadmap.json`
11. `planning/drafts/EPIC-SELFHOST.md`
12. `planning/drafts/ISS-038.md`
13. `planning/pressure-tests/2026-09-06-round-467.md`

## Review attack surface

An implied direct edge that `validateReducedEdges` should have caught (in
particular, a stray `ISS-054 → ISS-036` edge left in place alongside the new
`ISS-062`/`ISS-063` edges); a sub-slice draft (`ISS-056`..`ISS-063`) whose
"Does not claim" section quietly asserts live evidence; milestone drift (any
of the eight new drafts registered under a milestone other than "Self-hosting
release", or under a parent other than `EPIC-SELFHOST`); a
`protectedPathPolicies` path not derivable from `host-custody.md:34-46` and
`config/private-compositions.json`; an acceptance criterion in any of the
nine touched/new drafts that is not an observable property with the cheapest
discriminating evidence; a draft missing one of the sections
`docs/planning/work-model.md` and this repository's `type:slice` convention
require. Also attack: `ISS-013` dropped from `ISS-036`'s `blocked_by` without
a named alternate path that establishes the same dependency (see the
transitive-reduction check above — there is none, so it must stay); and the
`ISS-063` gating language read as authorizing its own authoring rather than
naming the Decision precondition ruling 2A requires.

## Attempt ledger

1. Source, attempt 1 of 8 (Sonnet 5 medium, routing row 13). Planning repair
   only; no review, no acceptance claimed.
2. Independent review, attempt 2 of 8 (Opus 5 high, routing row 8), exact
   head `5af55932931c25efa53ab7cfb060731cc6846add`. Returned
   `BLOCK_FIXABLE` with blocking findings F1 and F2 and five non-blocking
   observations (N1-N5); no repo mutation.
3. Source, attempt 3 of 8 (this correction), Sonnet 5 medium, routing row 13.
   Bounded same-file repair of F1/F2 plus N1/N5, per Decision #302 ruling 1A
   and Decision #272; see "Attempt 3 correction" above. No review, no
   acceptance claimed by this attempt.
4. Independent review, attempt 4 of 8, pending. A second consecutive
   plan-review `BLOCK` parks this repair under a Decision per Decision #272
   rather than a further repair attempt.

## Routing

Per Decision #302's readiness audit §6 (`~/.claude/skills/model-routing/SKILL.md`
rows): CF-0 source is Sonnet 5 medium row 13 (draft authoring), budget intent
at most $8, this attempt. CF-0's independent review is Opus 5 high row 8
(decomposition judgment), a separate dispatch not run here. The eight
registered sub-slices' own source/review routing is recorded per-row in the
audit §6 table and is not re-decided by this record; each sub-slice is routed
at its own dispatch, not at registration time, per
`docs/planning/work-model.md:77-82` ("Registration precedes elaboration").
