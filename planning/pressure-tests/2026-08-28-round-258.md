# ISS-022 process/handle raw-probe review — round 258
`BLOCK_REPLAN`.
The process challenge is supplied in argv, so the claimed private IPC nonce is neither private nor channel-delivered; the child only echoes an externally observable command-line value.
Process and handle-replay timeout/output-limit arms issue unchecked `SIGKILL` and still await `close` without a subsequent bounded settlement, so kill refusal can hang the probe past the literal ten-second runner bound.
Process overflow is not retained in the returned facts; an oversized stderr race that still closes under the already-requested `SIGTERM` can become indistinguishable from the clean row.
Happy-path direct-child ownership, hostile grandchild non-authority, provider `exit` then `close`, exact `SIGTERM`, WeakMap/function-key confinement, clone/worker/serialization/replay/release rejection, and custody-root admission are otherwise scoped correctly.
Focused tests pass 3/3 and typecheck passes, but they contain no nonce-mismatch, malformed/multiple, overflow, timeout, or kill-refusal mutant to close these trust paths.
