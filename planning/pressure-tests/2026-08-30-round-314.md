# ISS-022 diagnostic terminal verifier follow-up — round 314

`PASS` at exact base/HEAD
`3787c1d5830ab3005cceee68b509208d9384811d`.

Round 313's two terminal-boundary blockers are closed. The GitHub adapter now
projects job status and conclusion from closed provider vocabularies, and the
verifier's closed live-job rows require every recorded provider job and the
unique record job to be `COMPLETED` with an exact terminal conclusion. Missing
status, null or unknown conclusion, and `queued`, `in_progress`, `waiting`,
`requested`, or `pending` state cannot satisfy the terminal join.

The final response reauthenticates the current failed run/attempt/SHA and takes
`verifiedAt` only from its single canonical GMT `Date`. Run creation/start/update,
every provider-job start/completion, every present artifact creation, and the
inclusive record-writer chain are closed no later than that instant; the record
chain is exactly `started <= recorded <= artifact-created <= completed <=
verifiedAt`. Every present artifact, including the diagnostic record, retains at
least 30 complete days from the same instant. Stale Date, future run/job/writer
timestamps, malformed or non-GMT Date, and shortened retention refuse.

The repair preserves the four bounded arguments, clean exact protected-main
stable checkout, registry/contracts and provider-run bindings, FAILURE-only
diagnostic arm, Round 311 replay, closed terminal digest, and `BLOCK_REPLAN`-only
output. It adds no public record member, workflow/job, helper, decision,
publication, promotion, command, or portable-engine provider vocabulary.

Proportionality passes. The follow-up adds 194 changed lines to Round 313's
697-line surface; the resulting five-file `+881/-10` diff remained readable in
one sitting, and the repair reuses the existing API projection and time joins
rather than adding a mechanism.

Focused evidence passed: hosted-terminal API and generic terminal tests (13),
hosted aggregate tests (3), repository TypeScript check, touched-file Prettier
check, and `git diff --check`.
