# Pressure test — round 50 author repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `8d82ee0bae684125b0883240f3d54c1688df927a`

This author-side repair resolves the human-selected planning-repair path for
the missing `dispatch-brief/v1` member census. It grants no implementation,
merge, promotion, or release authority. A fresh independent exact-head review
must issue the next governing disposition.

## Bounded repair

`contract-decisions.md`, the sole contract authority, now defines exactly three
closed brief records:

- one five-member action record binding bounded action/capability identifiers,
  the action-plan digest, and immutable subject digest;
- one six-member directive record with nine closed section kinds, a bounded
  non-prose code, subject binding, and an all-null/all-present schema/digest
  reference pair; and
- one four-member opaque resource record binding access, resource kind, and
  identity digest without a repository or host path.

The five-member brief envelope binds one action, explicit directives for every
kind, the opaque footprint, and the closed worker role. Directives and resources
are bounded, dense, duplicate-free arrays. All directive subjects equal the
action subject. ISS-002 AC12 now names the executable structural, grammar,
cross-binding, duplicate, and hostile-reflection mutants.

ISS-011 still owns module emission, ISS-021 still owns the versioned rendering
template and goldens, and ISS-008 still owns rendered-byte digest binding. The
repair moves no runtime behavior or authority between issues.

## Bounded proportionality check

The in-scope threat is free-form module/operator text becoming executable worker
instruction outside a reviewed template. A single open prose field fails that
boundary. Separate schemas for every human section would duplicate the same
identifier/reference shape nine times and create unnecessary versioning.
One closed directive union plus one action and one opaque-resource record is the
smallest shape that preserves explicit sections, subject binding, footprint,
and deterministic rendering. Digest-only references without directive kinds
would be smaller but could not prove section completeness; host or repository
paths would violate the engine/adapter boundary.

Deletion test: if the directive union or one required kind can be removed while
ISS-011 fresh-context goldens still express all nine sections without prose,
delete it in a reviewed replacement. Review horizon: re-evaluate after ISS-011
and ISS-021 publish their first cross-OS emission/rendering goldens.

## Required independent attack

Review the exact committed repair head cold. Attack identifier strings as prose
smuggling, missing/duplicate directive kinds, reference half-groups, subject
substitution, resource path leakage, adapter vocabulary in fields/enums,
unbounded arrays, arbitrary schema references rendered as prose, and ownership
drift into ISS-002 from ISS-011/021/008. Confirm the three-record shape is the
smallest credible alternative and re-run planning and live board gates. Older
PASS dispositions do not apply to this repair.

