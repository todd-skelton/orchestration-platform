# Pressure round 408 — project-cycle journal and terminal literals

Status: author repair after BLOCK_FIXABLE review; exact-head re-review pending.
No implementation, durable append, replay authority or completed ISS041 claim.

## Scope and decisions

The smallest coherent remaining public group is orchestration-event/v1,
event-journal/v1, reduced-state/v1 and cycle-receipt/v1. Separating them leaves
unchecked bytes, a partial output union, an unauthenticated reducer or a receipt
hash cycle. Mandatory readiness input remains in
TEMP/orchestration-journal-family-readiness-20260831.md.

- Event v1 is complete for the admitted PROJECT path and every current typed
  output/composite/skip. Missing release producers trigger versioned replan.
- The journal is one length-framed append-only stream, not NDJSON or rewritten
  JSON. Genesis requires actual create-once fresh-root admission.
- Every step has immutable STARTED and optional TERMINAL events. Physical prefix,
  position, predecessor and owning output identities are recomputed.
- Replay retains pending external identity/resources/targets and refuses
  UNKNOWN padding, duplicate effects and false prefixes.
- Ordinal15 is upstream-only: pre15 state -> STARTED -> terminalizing state and
  prefix -> receipt -> TERMINAL event -> final replay.
- Earlier UNKNOWN/live/resource-unknown paths end at reduced UNKNOWN without
  starting15 or fabricating an UNKNOWN cycle receipt. A future post-STARTED
  terminal-unknown producer requires versioned replan.

## Attack surface and evidence

| Boundary | Required discriminator |
| --- | --- |
| Complete union | Every output/composite/skip/null/phase cell; wrong ordinal, missing dependent, release output and opaque payload refuse |
| Physical bytes | Independent exact header/event frames; invalid complete frame/UTF-8/BOM/LF and noncanonical nested records corrupt; partial final frame is retained unhealthy and blocks append while complete prefix replays |
| Prefix/genesis | Caller empty versus actual fresh root, wrong cycle/session/plan,0/1/max/over-limit, duplicate/reorder/fork and append after terminal; complete suffix loss is earlier RUNNING absent future tip |
| Resume/fold | STARTED without terminal resumes same identity; conflicting/duplicate terminal never repeats an effect; all known/unknown/reclaim routes and complete census |
| Terminal DAG | Mutate pre15/started prefix/reduced/step/receipt/event identities; self, enclosing-event or post-prefix dependency refuses |
| Portability | Identical canonical state/receipt and replay on all three OSes without clock/locale/directory-order dependence |
| Full replay preimages | Rehash after substituting configuration/policy/prior/module/mapping/observation/review/disposition/mutation/reclaim context or retained raw bytes; owning relation refuses |
| Invalid-input closure | Mapping/preflight/mutation invalid cells require typed member null plus one exact CANONICAL_JSON evidence row; valid/unavailable branches forbid it, and decoded substitution refuses |
| Honest suffix semantics | Complete-frame deletion replays as earlier RUNNING prefix with no receipt; partial final frame is retained unhealthy suffix over usable complete prefix; authenticated rollback detection remains replan |

## Non-goals, footprint and review

Six documentation paths: ledger, routine note, ISS010/026/041 notes and this
record. No source, manifest, command, service, provider, board, private journal,
snapshot/index/cache, release producer or ISS026 implementation. No tests/build
or compiler ran. Predicted implementation is one bounded contracts module,
specialized strict parser/serializer, pure reducer, registry/harness and
independent tests. Need for a fifth family, unbounded history or release output
is a named replan. Root owns checks/review/CI; this author claims no PASS.

The first review at22193eb found four bounded issues: ordinal1's null predecessor,
missing typed replay preimages/raw evidence, impossible truncation claims, and
the row14 UNKNOWN edge. This repair binds ordinal1 through event/header prefix,
closes every owning relation tuple plus retained-evidence census, states honest
complete/partial suffix behavior, and ends uncertain reclaim at14. Scope and
acyclic terminal DAG are unchanged; no PASS is claimed.

Follow-up review at5c8829d found two remaining closure gaps. This repair adds
the explicit typed-null/exact-evidence matrix, including MUTATION_OBSERVATION,
and changes the later exhaustive routine row so uncertain/live reclaim ends at
14. It changes no family, scope or DAG and remains pending re-review.
