# ISS-022 diagnostic provider record pressure review — round 312

`PASS` at exact base/HEAD
`9b78908f3dbc02fdb3c86f694bfdeb6bf5bb70df`.

The uncommitted 10-file, `+874/-33` diff adds only
`github-conformance-diagnostic-provider-record/v1` to the existing stable
record job. The parser closes fields, provider conclusions, ordered unique
job/artifact subsets, exact registry complements, provider-run scalars,
protected ref, candidate/harness/test identities, current run/attempt, writer
time, artifact digest/length, and minimum retention. All-SUCCESS with no
missing member refuses. The composer authenticates present current-attempt
observation archives and replays any present receipts-only aggregate against
the exact registry, raw evidence, and provider/candidate/harness/test context;
missing, extra, duplicate, reordered, stale, rebound, changed, or PASS-aggregate
substitution refuses or remains an explicit missing diagnostic.

The workflow remains four jobs. Only the stable `record` job runs `if:always()`;
candidate bytes gain no writer or verdict authority. Exactly one
run/attempt-qualified `archive:false` filename is selected. A valid PASS
aggregate still takes only the landed provisional-record path; the diagnostic
arm has no aggregate digest, terminal verifier, decision, publication, or
promotion effect. Record/API/output failure stays closed.

Proportionality: the concrete threat is losing or laundering authenticated
ISS-022 non-PASS evidence. Deleting the record loses the provider census;
omitting archive replay admits the Round 309 counterexamples. Reusing the
landed archive verifiers and record job is smaller than a new job, terminal
chain, schema family, or helper. The 907-line review surface remained readable
in one sitting, with most growth confined to the composer and focused mutants;
no portable-engine GitHub policy, command, credential, issue, or roadmap edge
was added.

Focused evidence passed: four touched conformance test files, 54 tests;
`pnpm run typecheck`; and `git diff --check`.
