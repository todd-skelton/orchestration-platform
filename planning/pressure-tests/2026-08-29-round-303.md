# ISS-022 nullable-environment implementation review — round 303

## Disposition

`BLOCK_REPLAN`.

This was the bounded cold implementation review of the nine-file uncommitted
diff on `codex/iss-022-nullable-environment-implementation` at exact base/HEAD
`266a45f9e92ef384b60999a43cd3df29dd211701`.

## Blocking finding

The nullable diagnostic arm is not executable. The generic environment parser
now correctly admits `filesystemProfileDigest:null`, but
`validateEnvironmentForSuite` unconditionally requires that field to be a
SHA-256 for every suite. Consequently `createConformanceJobEvidence` cannot
produce the required ISS-022 `UNKNOWN|UNSUPPORTED` diagnostic receipt from a
`selection:null` environment. The new contract tests expressly pin that blanket
refusal even for a future suite; there is no closed ISS-022 exception or
suite/report/job join.

The same omission leaves the selected arm incomplete: the implementation
derives the acyclic pre-custody, custody, receipt, and locator identities, but
never constructs either final `conformance-environment/v1` arm, recomputes its
`Denv`, or equal-binds that digest into an ISS-022 observation/job receipt.
`osImageDigest` is merely accepted as a suite coordinate here, with no raw
inventory-byte equality in this path. Therefore the touched tests cannot attack
selected/null environment substitution, mixed arms, final-environment cycle
reintroduction, or diagnostic UNKNOWN-to-receipt behavior. Passing profile and
parser tests do not supply that missing authority join.

## Preserved properties and proportionality

The syntax-only parser change does not weaken ISS-002 or current non-ISS-022
joins because the reducer still requires a non-null filesystem digest. The job
parser retains the exact four-result union, and aggregate construction remains
PASS-only. `DpreCustodyEnvironment` has the required ordered variable-member
census, is recomputed rather than caller supplied/selectable, and removes the
custody cycle. Caller `environmentDigest` is removed from ISS-022 coordinates.
No engine, adapter, trust, workflow, schema-member, issue, edge, helper, or
publication surface is widened.

The nine-file, 258-line footprint is reviewable and the acyclic digest helper is
proportionate. The smallest repair is within the predicted existing
suite/observation/job-receipt join: construct and validate both exact final
environment arms, permit null only for the authenticated ISS-022 non-PASS arm,
and add the named substitution/mixed/cycle/UNKNOWN mutants. No new mechanism is
justified.

Focused evidence: four touched test files passed (27 tests), `pnpm run
typecheck` passed, and `git diff --check` passed. Those checks expose no syntax
or type defect; the passing tests currently omit the blocked end-to-end arm.
