# Pressure test — round 31

**Disposition:** `BLOCK_REPLAN`

The thirty-first independent review found one authority-lineage mismatch. The
39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R31-001` — Launches bound mutable gate/fence digests, so every publication, activation, abort, or fence-reduction CAS invalidated the only READY/LIVE pointer before it could resume. | Split gate/fence identity into immutable transaction roots, create-once ordinal head histories, and canonical current pointers. Launches bind roots plus observed heads. A same-source `AUTHORITY_REBIND` transition may advance exactly one adjacent head while retaining lifecycle/attempt/process; it cannot launch, attach, or invoke broker/project work. Broker attachment binds immutable attempt roots and validates descendant launch history. |

## Re-review focus

- Crash between authority-head record creation, head-pointer CAS, rebind record,
  and launch-pointer CAS for every publication/activation/abort/fence transition.
- Attempt nonadjacent, skipped-history, changed-root/source/process, second-child,
  and reattachment variants; all must refuse.
- Verify broker attachment remains bound to immutable initial LIVE identity while
  every later call validates the canonical descendant chain.
