# Pressure test — round 63 independent ordinary-resolution review

**Disposition:** `PASS`

**Reviewed head:** `1052f761b6da2ad1bde9a926b908d0655c7acf3d`

This was a fresh independent cold review of the exact clean head above on
`iss-002-versioned-contracts`. Round 61's earlier `PASS` was not borrowed and
round 62 was treated only as an author repair requiring this gate.
`docs/architecture/contract-decisions.md` was the sole decision authority. The
review was limited to the round-62 ordinary-resolution repair and its required
architecture, compatibility, issue, planning, and board restatements.

## Disposition

The repair closes the deleted-`producerEpochKey` ambiguity with one exact,
closed, four-digest selected producer-authority tuple and does not restore the
sparse-history machinery that the architecture deleted. The schema census,
canonical order, outcome arms, digest formula, run/packet equality, trust
boundary, acceptance mutants, and board mirror agree. No blocker was found.

This exact-head `PASS` authorizes implementation of the amended ISS-002
ordinary-resolution contract only within the issue's stated scope and
predicted implementation footprint. It does not authorize merge, release,
promotion, installation, runtime mutation, or self-certification. A substantive
contract or planning change requires another independent exact-head review.

## Direct attack results

- **Exact census and canonical order:**
  `pointer-mutation-commit-resolution/v1` has exactly thirteen members. The
  producer members are required non-null SHA-256 values named
  `producerAuthorityPathInstanceDigest`,
  `producerAuthorityReceiptDigest`, `producerAuthorityTipDigest`, and
  `producerAuthorityValueDigest`. That is the correct lexicographic canonical
  JSON order (`Dp`, `Dr`, `Dt`, `Dv` by field name), while their semantic
  authority tuple remains `Dp/Dt/Dv/Dr`. Omission, rename, null, extra fields,
  and serialization-order drift therefore refuse rather than selecting an
  implementation-defined representation.
- **Swap and substitution resistance:** each producer field is equal-bound to
  the correspondingly named ORDINARY old-authority and KNOWN packet-authority
  field, which are themselves the one selected epoch used by all nine run
  checkpoints. A `Dp/Dt/Dv/Dr` field swap, partial tuple, stale tuple, or
  coordinated four-field substitution either fails the named equality or
  changes the canonical resolution digest while still failing the independently
  recomputed run/packet selection. No caller-chosen epoch key or unsigned
  history head can satisfy this relation.
- **Closed outcome arms:** `SELECTED` requires only non-null
  `selectedTargetTipDigest`; `LOST_CONFLICT` requires only non-null
  `conflictReceiptDigest`; and `UNKNOWN_TERMINAL` requires only non-null
  `unknownEvidenceDigest`. The other two nullable arm members are null, and
  non-null `outcomeEvidenceDigest` equals the sole applicable arm digest. The
  enclosing ORDINARY commit still requires the selected target, recomputed real
  winner, or empty target registry slot respectively, so arm success cannot
  substitute a stale or wrong-kind slot.
- **Digest formula and order:** the resolution digest is exactly
  `H(F("pointer-mutation-commit-resolution/v1", canonical resolution bytes))`.
  The existing closed framing supplies the domain, part count, type, length,
  and canonical bytes; there is no second tuple framing whose order could drift
  from the record. Missing, extra, reordered, renamed, cross-domain, or
  outcome-arm-mutated records therefore recompute differently or refuse before
  `Dcommit` use.
- **Run and packet cross-binding:** an ORDINARY `Dcommit` requires KNOWN packet
  authority and binds its common old and packet `Dp/Dt/Dv/Dr`, checkpoints
  zero through eight, the recomputed ordinary-resolution digest, outcome, and
  exact target slot. The packet cross-binds that commit and the complete
  identity tuple to its top authority and registry facts. The new resolution
  tuple must equal both common authority tuples; no independent parse success
  can widen a live packet or mutation capability.
- **Cycles and rotation insertion:** the resolution excludes the selecting
  run-current value/proposal/tip, selector readbacks, `Dpost`, packet wrappers,
  membership material, and every rotation field. Its acyclic position remains
  resolution to terminal core to selector value/proposal/tip to `Dpost`.
  `AUTHORITY_ROTATION` forbids an ordinary resolution altogether and retains
  only checkpoint 5 plus its derived RESUMABLE/SELECTED/UNKNOWN evidence. An
  inserted rotation field, later selector artifact, or post-CAS new-epoch write
  is a closed-union error, not advisory residue.
- **Deleted surfaces:** scoped searches found no positive
  `producerEpochKey`, epoch-key alias, membership index/proof, sparse root/tree,
  Merkle surface, materialization coordinator, directory census, rotation
  receipt, or alternate producer representation in the reviewed authority
  contract. Negative mentions exist only to require refusal or document
  deletion. Full linear authority-history validation remains the sole producer
  history proof.
- **Trust and self-certification:** ISS-002 supplies pure structural validators
  only. Runtime authority still requires the canonical state-root custody,
  selected exact authority tip, kernel-exclusive lock, and ISS-004 private live
  capability; ISS-004 must derive the producer projection from a fully walked
  selected history and cross-bind it during the one lock-held epoch. A record,
  digest, packet, worker, candidate, author repair, or this review cannot grant
  runtime or release authority by itself.
- **Adapter boundary:** the added field and enum vocabulary is generic pointer,
  outcome, and authority vocabulary. It introduces no branch, worktree, label,
  milestone, queue, deployment, consumer, provider, repository-policy, or host
  selector term into an engine contract. The existing generated vocabulary
  lint remains the implementation gate.
- **Planning and board consistency:** ISS-002 restates the exact producer
  fields, tuple equality, closed arms, forbidden aliases, and required mutants;
  its scope, non-goals, ownership, dependency, milestone, and predicted
  implementation footprint remain unchanged. The registered roadmap graph
  passes, and the repository's own delivery issue/project mirror matches the
  amended draft exactly. No consumer board or Chase Sets system was read.

## Bounded proportionality check

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Four named producer-authority digests | A terminal resolution can be replayed under a stale or different selected producer epoch. | Deleting any member leaves one component of the selected pointer tuple unauthenticated. | Reusing the already-required `Dp/Dt/Dv/Dr` tuple is smaller than an epoch key, tree root, membership proof, or second receipt. Retain repaired shape. |
| Equality to both run-old and packet-known authority | A coordinated record can remain internally valid while moving between a run and packet. | Deleting either equality admits stale/coordinated substitution at that boundary. | Two pure equality checks against existing fields add no record or state machine. Retain. |
| Three nullable outcome members plus one equality digest | A selected, lost, or unknown resolution can carry evidence for another arm. | Deleting the XOR/equality permits mixed or unbound terminal evidence. | The existing three-field nullable XOR is smaller than nested outcome records or another pointer. Retain unchanged. |
| Canonical-record-only resolution digest | Alternate tuple framing can introduce a second order or representation. | Deleting the digest leaves the persisted resolution unbound; adding separately framed members duplicates its schema. | One existing domain over one closed canonical record is the smallest unambiguous binding. Retain. |
| Rotation exclusion | A rotation can manufacture ordinary post-CAS terminal evidence under a successor epoch. | Allowing the record reopens epoch crossing and self-written terminal truth. | Absence in the existing closed rotation arm is smaller than a rotation-resolution protocol. Retain. |

The repair adds no pointer kind, tree, membership proof, history root,
coordinator, receipt, capability, selector layer, or mutation owner. Four
existing-vocabulary digests and pure equality checks are proportional to the
current stale-epoch substitution threat and are strictly smaller than the
deleted sparse design.

## Footprint and verification evidence

The immediate repair range
`3d43e7b6186dc0cfbf06553cd223eb21ca433d6b..1052f761b6da2ad1bde9a926b908d0655c7acf3d`
changes exactly five paths with 112 insertions: the sole decision authority,
supervisor ledger, compatibility contract, ISS-002 draft, and round-62 author
record. It changes no implementation, topology, wrapper, shared script,
glossary, unrelated package, or runtime state. This review adds only this
round-63 pressure record.

- `git rev-parse HEAD` and initial `git status --short --branch` — exact clean
  reviewed head and expected branch.
- Cold reads of `AGENTS.md`, `docs/planning/work-model.md`, the sole decision
  authority, supervisor ledger, compatibility contract, ISS-002, and rounds
  61–62 — complete.
- `git diff --stat` and the scoped repair diff — exactly five repair paths and
  the bounded ledger/restatement change above.
- Scoped producer-field, deleted-surface, outcome, packet, vocabulary, and
  registration searches — PASS; no positive alias or unexpected implementation
  surface found.
- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `node scripts/planning/board-check.mjs` — sandboxed `gh` spawn refused
  `EPERM`; the approved read-only rerun passed (`board contracts verified`).
- `node scripts/format.mjs check` — sandboxed formatter spawn refused `EPERM`;
  the approved read-only rerun passed.
- `git diff --check 3d43e7b6186dc0cfbf06553cd223eb21ca433d6b..1052f761b6da2ad1bde9a926b908d0655c7acf3d`
  — PASS.
- `pnpm --filter @orchestration-platform/contracts test` — PASS (12 files, 52
  tests through the current pre-authorization capability slot).
- `pnpm run contracts:compatibility-check` — the sandboxed signature lookup
  stalled without output and was stopped; the approved read-only rerun passed
  (1 file, 1 test). These current tests do not yet implement the repaired
  resolution and were treated only as regression evidence, not authorization
  or hidden executable proof of the new ledger.

## Routing log

Task `ISS-002-round-63`; reported model `gpt-5.6-sol`; effort `high`; row `11`
fallback; placement `provisional`; transcript: one bounded independent cold
review with sequential read-only checks; budget intent: bounded single review
artifact, with no subagent or second premium attempt.

## Implementation authorization

This exact-head `PASS` authorizes implementation of the round-62 ordinary-
resolution repair as part of ISS-002 only. The implementation must execute the
exact field census, semantic and canonical orders, nullability/equality matrix,
digest formula, run/packet cross-binding, deleted-surface refusal, and mutant
matrix above. A worker or candidate release never certifies itself; independent
implementation review and the stable promotion path remain required.
