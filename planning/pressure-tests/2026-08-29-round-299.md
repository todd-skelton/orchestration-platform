# ISS-022 nullable-environment authority review — round 299

## Disposition

`BLOCK_REPLAN`.

This was the one bounded cold review of the uncommitted two-file amendment on
`codex/iss-022-nullable-environment-replan` at exact base
`add69913e8d97c043703f62e7690a95d89e5b337`. Two authority contradictions keep
the amendment from being executable.

## Blocking findings

1. **The job-receipt contract cannot carry one required diagnostic result.**
   The new ISS-022 null arm requires a complete 21-row report with a physical
   `UNKNOWN|UNSUPPORTED` and a matching non-PASS observation/job receipt, but
   the governing generic `conformance-job-receipt/v1` section still closes
   `normalizedResult` to `PASS|FAIL|UNSUPPORTED`. An `UNKNOWN` physical row
   therefore has no literal job-receipt arm even though the landed parser now
   admits `UNKNOWN`. Replan must make the public contract, parser, receipt
   producer, and mutants agree on `PASS|FAIL|UNSUPPORTED|UNKNOWN`, while keeping
   every non-PASS receipt diagnostic-only and the aggregate PASS-only.
2. **The selected environment equality is a digest cycle.**
   `Denv` hashes the environment record; `DcustodyInstance` includes `Denv`;
   `DcustodyReceipt` includes `DcustodyInstance`; and the new selected arm
   requires that same environment's `custodyObservationDigest` to equal
   `DcustodyReceipt`. That requires an infeasible
   `Denv -> DcustodyReceipt -> DcustodyInstance -> Denv` fixed point. The landed
   profile constructor currently avoids the cycle only by accepting a
   caller-supplied `environmentDigest`, which the amendment correctly refuses
   as authority. Replan must define one acyclic, recomputable custody/environment
   order before claiming the four exact selected-arm equalities.

## Pressure result and proportionality

The generic nullable parser is otherwise explicitly non-authoritative;
ISS-002 and every non-diagnostic composition retain non-null filesystem
digests. The `selection:null` arm requires the complete ordered 21 rows, a
physical non-PASS, exact non-null ABI/helper evidence, null filesystem/custody,
and no locator/profile/custody selection. Caller-filled null, six-physical-PASS
null, sentinels, synthesis, mixed arms, and `osImageDigest` substitution are
explicit refusals. The diagnostic provider/terminal path remains non-PASS and
the aggregate remains all-PASS only.

The syntax-only null extension is proportionate, and no new schema member,
workflow job, helper, issue, or roadmap edge is justified. The smallest repair
is to close the result vocabulary and replace the cyclic digest input with an
acyclic pre-custody identity while preserving all stated equality and refusal
mutants. Focused inspection covered the exact diff, the generic parser/job/
aggregate joins, the ISS-022 profile constructor, and `git diff --check`.
