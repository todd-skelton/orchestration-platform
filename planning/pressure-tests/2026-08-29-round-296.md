# ISS-022 profile-layer split follow-up review — round 296

## Disposition

`PASS`.

This final narrow review covers the isolated, uncommitted PR1 at base
`db22f2d820bafaf50fc90590aa645983263ea3b8`. Round 295's named test-boundary
blocker is closed without restoring or depending on the stashed suite layer.

## Verified repair

- The profile test replaces `process.execPath` during
  `withIss022ExecutableCustody()` and separately mutates the retained file's
  bytes in place during the callback; both executions reject.
- Physical rows 0, 4, and 5 receive closed, valid mutations (`rootStable=false`,
  `locatorStable=true`, and `rootStable=true`) whose handler predicates produce
  exact `UNKNOWN` rows. Each makes construction select `null`, makes the
  retained non-null selection fail validation, and makes the null diagnostic
  selection validate. A null selection with all six PASS rows refuses.
- No current profile source, test, export, or census row imports or references
  `iss022-suite`. The shared amendment is exactly the profile export, source
  census/test census rows, and their two expectations. The profile production
  blob is byte-identical to Round 294 (`b88c2837ba9fe3c4ab599913c9eaae761ab03218`).

Focused evidence passed:
`pnpm exec vitest run test/conformance/iss022-profile.test.ts test/conformance/stable-bundles.test.ts --reporter verbose`
(2 files, 11 tests),
`pnpm exec tsc --noEmit -p packages/conformance/tsconfig.json`, and
`git diff --check`.

## Proportionality

Before this verdict, PR1 is 696 insertions: 417 source, 171 tests, five shared
export/census lines, Round 293's 72 lines, and Round 295's 31 lines;
implementation plus tests is 588. That is substantial but remains one cohesive
profile/custody authority layer with focused attacks against demonstrated
substitution and crossed-arm threats. The repair changed tests only, added no
helper, service, suite dependency, or production mechanism, and the five
shared lines are the minimum needed to expose and census the independently
landable layer. No further review round is justified.
