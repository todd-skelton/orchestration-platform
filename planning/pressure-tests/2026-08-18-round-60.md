# Pressure test — round 60 author host-capability repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `c0f6115acb824d9ff1c490766edcf083bb851d3d`

This author-side repair answers only `OP-R59-001`. Round 59's `BLOCK_REPLAN`
remains governing. This record grants no implementation, merge, promotion,
installation, or release authority; only a fresh independent exact-head `PASS`
may authorize implementation.

## Bounded repair

- The identity projection now carries one exact dense 1–256 capability-name
  array: 64-character lowercase bounded identifiers, strict ASCII byte order,
  unique and case-sensitive. Its canonical bytes are the third identity frame,
  so any capability change creates a new host identity.
- The reviewed `worker-host-renderer-artifact/v1` row carries the identical
  capability array. One total admission relation snapshots all 1–16 rows,
  validates every closed record/array, recomputes every identity, rejects any
  unreadable row, and proves unique identities. There is no package-internal
  extraction rule or subordinate digest.
- ISS-012 and step 7 use that same relation and exact action-core capability
  membership. Host producers, N0/successor installers, routing, dispatch, and
  cycle drafts now exercise missing/extra/future, bounds/order/duplicate/case,
  moved/cross-host/coordinated, and manifest/read-back mutants.

## Proportionality and ownership

Adding one array to the existing identity and mapping row is smaller and more
portable than defining a package-internal record location or another registry/
digest. The reviewed manifest is already the common build/install/route/dispatch
surface, so one pure total relation gives every owner identical semantics.

ISS-002 owns the schema/formula/relation; each host issue emits conformance-
proven capability rows; ISS-020/014 own reviewed install mapping; ISS-012 owns
route membership; ISS-008 owns step-7 binding; ISS-026 composes the relation.
No dependency edge or mutation owner changes.

## Required independent attack

Review the exact committed repair head cold. Attack capability grammar, zero/
one/256/257, order, duplicate, case, holes, extras, future/wrong-family records,
partial mapping acceptance, identity framing, action/catalog capability drift,
host conformance claims, route/step-7 relation divergence, cross-host and
coordinated substitution, provider leakage, self-certification, removed
surfaces, and all earlier repaired action/catalog/cardinality invariants. Older
PASS dispositions do not apply.
