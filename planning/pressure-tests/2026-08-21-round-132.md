# ISS-002 pressure test — round 132

## Review basis

This is a fresh independent cold removal review of the bounded
recovery-authorization native-receipt implementation at exact initially clean
and pushed head `520a36ea64a66af3468536c2a62b33dee5cec4cb` on
`iss-002-versioned-contracts`, with exact parent
`50994ffb3d54ffe84ee18c07ba6e9927bc255c0a`. The sole worktree is
`D:/Users/ToddS/Source/Repos/orchestration-platform`.

The authority basis was only `AGENTS.md`, `docs/planning/work-model.md`, the
sole implementation authority in `docs/architecture/contract-decisions.md`,
the registered `planning/drafts/ISS-002.md`, rounds 130–131, and the exact
parent-to-head source/test delta. No Chase Sets repository, project, issue,
board, backlog, consumer source, or other consumer system was read or touched.

The exact delta is four paths, 402 insertions and one deletion:

```text
packages/contracts/src/index.ts                 +7 -1
packages/contracts/src/recovery.ts            +108
packages/contracts/src/registry.ts             +12
test/contracts/recovery-native-receipt.test.ts +275
```

It adds no package, dependency, command, provider, persistence, runtime,
roadmap, draft, issue, board, or registry-topology change. `git diff --check
50994ff..520a36e` passed. Before this record, local `HEAD` and
`origin/iss-002-versioned-contracts` both resolved to the exact reviewed commit
and the checkout was clean.

## Disposition

`PASS` for the bounded recovery-authorization native-receipt implementation at
exact reviewed head `520a36ea64a66af3468536c2a62b33dee5cec4cb`.

The implementation is proportional to the round-131 authority. It adds the
two total closed parsers, their exact domain-separated digest functions,
explicit generic serializer routes, compatibility/vocabulary registration,
and deletion-grade focused tests while reusing the existing validated
transaction/operation path constructor. It does not reintroduce the seven
deleted core copies or implement any later provider, persistence, composition,
retention, or runtime authority. No repair or replan is required.

## Exact structural implementation

The implemented censuses are exact, unique, and in ascending canonical member
order:

| Schema | Count | Exact members |
| --- | ---: | --- |
| `native-consume-receipt/v1` | 6 | `authorizationCoreDigest`, `nativeGeneration`, `operationId`, `recordedAt`, `schemaVersion`, `transactionId` |
| `native-removal-receipt/v1` | 9 | `authorizationCoreDigest`, `nativeConsumeReceiptDigest`, `operationId`, `priorNativeGeneration`, `recordedAt`, `removalDisposition`, `schemaVersion`, `successorNativeGeneration`, `transactionId` |

Both parsers first call the existing `snapshotJson` closed detached snapshot.
That boundary refuses proxies, symbols, inherited/non-enumerable/accessor
fields, exotic/class instances, descriptor/prototype traps, and unreadable
input without invoking user code. The receipt layer then requires all and only
its exact fields and applies the shared lowercase SHA-256, canonical UUIDv7,
safe-decimal, and millisecond UTC timestamp grammars.

Consume requires a canonical safe-decimal `nativeGeneration`. Removal admits
exactly null or a lowercase SHA-256 digest in
`nativeConsumeReceiptDigest`, admits exactly `ABSENT|DISABLED`, and requires
`successorNativeGeneration` to equal the safe-decimal increment of
`priorNativeGeneration`. The maximum successor is admitted only with its
adjacent predecessor; maximum prior, out-of-range successor, same generation,
skips, and noncanonical decimals refuse. The comparison is reached only after
both operands pass the shared pre-conversion safe-integer bound.

The public generic parser routes both exact schema literals. Generic
`serializeContract` returns only the existing success arm
`{ ok: true, bytes, digest }`, with canonical detached bytes and the appropriate
tagged digest. `schemaVersions`, the compatibility matrix, and
`schemaVocabularyDefinitions` register both schemas; the removal vocabulary
closes `ABSENT|DISABLED`. There is no untagged serializer fallback.

The implementation reuses the existing native path constructor:

```text
installation/recovery-authorizations/<transactionId>/native/<operationId>.json
```

It validates each input independently as UUIDv7 and is sensitive to both.
Every otherwise valid transaction/operation pair remains structurally valid;
the implementation adds no consume/removal pair equality, inequality, or
selected-state relation.

The receipts contain only the retained aggregate `authorizationCoreDigest`.
Each of the seven removed copies remains absent and is explicitly rejected as
an extra field on either schema:

```text
capabilityDigest
capabilityReferenceDigest
hostIdentityDigest
installationId
projectId
stateRootDigest
userIdentityDigest
```

## Independent byte, digest, and path reconstruction

The review independently rebuilt each frame from the normative prefix, NUL
separators, big-endian part count, canonical-part tag `0x07`, big-endian byte
length, and exact canonical UTF-8 bytes. It did not call either implementation
digest helper. The reconstructed values match the committed goldens:

| Fixture | Canonical byte length | Required digest |
| --- | ---: | --- |
| consume generation zero | 310 | `ed3e4437579cdb1d3f4d417027fafc5cdc427da1ca4c4748e8b3044f9a150da5` |
| direct-CREATED removal, null consume digest, `ABSENT` | 411 | `8b52bd3636ffa894092c85e5f81c129d8a94eef6715f9918ed0bce9869559d88` |
| post-CONSUMED removal, non-null consume digest, `DISABLED` | 475 | `c0da986cbe35f88f1bacc9b357af0c3afff1554af2d3acf7c3419c841a25ea46` |

The formulas are exactly:

```text
Dnc = SHA256(frame("native-consume-receipt/v1", canonical consume receipt bytes))
Dnr = SHA256(frame("native-removal-receipt/v1", canonical removal receipt bytes))
```

For the consume bytes, substituting the removal domain independently produced
`843dc336a65d2cafb4a8952df4142befe0e016606eed11c62af6906f55d6198b`;
raw untagged SHA-256 produced
`9592f1fa8e7326c0bf97a3c3521614017b0c9f660988cde3bfc6bbe41c6104f5`.
Neither equals `Dnc`. Exact goldens therefore kill tag deletion, exchange,
cross-schema reuse, common-`Dv` substitution, field-wise framing, and untagged
canonical hashing.

The independently reconstructed consume path is:

```text
installation/recovery-authorizations/018f0f4d-7b2d-7a13-9a2b-123456789abc/native/018f0f4d-7b2d-7a14-aa2b-123456789abc.json
```

Changing either UUIDv7 changes that path; malformed transaction or operation
input refuses.

## Attack and deletion evidence

The focused suite directly attacks missing, renamed, null-at-required, numeric
cross-type, and extra fields across all three structural fixtures; both valid
nullable removal arms; every closed disposition; both schemas spread into the
opposite parser; each of the seven deleted core copies; and reverse-ordered
noncanonical bytes. Exact canonical-byte fixtures pin all fields and their
order. Schema-version mutation and compatibility evidence refuse legacy,
future, missing, cross-family, and unknown versions.

Safe-decimal evidence admits zero, maximum consume generation, and the
maximum-successor adjacent pair. It refuses same-generation, non-adjacent,
leading-zero, maximum-prior overflow, above-safe-bound, numeric, and negative
inputs. Receipt-own path tests mutate each input, reject malformed inputs, and
affirm that consume and removal parsers accept arbitrary otherwise valid UUIDv7
operation substitutions rather than inventing cross-operation authority.

Digest evidence pins `Dnc` and both nullable-arm `Dnr` values, distinguishes
tagged from canonical untagged digest, distinguishes the two domains, refuses
cross-type helper inputs, and pins the exact existing serializer result for
both schemas. Generic canonical-byte parsing admits exact bytes and refuses
reordered bytes.

The focused parsers are total over null, arrays, scalars, and a throwing proxy.
The shared snapshot tests additionally cover transparent proxy detection,
symbols, accessors, non-enumerables, subclasses, exotic prototypes,
cross-realm/holey/custom-iterator arrays, invalid Unicode, and frozen/sealed
accepted inputs. Because both new parsers unconditionally enter that shared
snapshot before semantic reads, these hostile cases have the same bounded
refusal path. Generic parsing and serialization inherit that total parser
boundary.

| Retained mechanism | Concrete in-scope threat | Deletion result | Smallest shape retained |
| --- | --- | --- | --- |
| Exact 6/9 closure | Unknown or wrong-type facts enter receipt identity. | Member/extra/cross-schema mutants survive without it. | Two flat parsers using the shared snapshot. |
| Nullable prior-consume digest | Direct-CREATED and post-CONSUMED removal are structurally indistinguishable. | Null/non-null arm evidence fails. | One nullable digest; no selected-history object. |
| Safe-decimal adjacency | Same, skipped, or overflowing generation is blessed. | Zero/max/overflow/nonadjacent mutants survive. | One shared increment comparison. |
| `Dnc`/`Dnr` domains | Cross-receipt or untagged identities collide semantically. | Exact independent goldens fail. | Two one-canonical-part frames. |
| Two-input path | A caller can substitute transaction or operation scope. | Either sensitivity test fails. | Reuse the existing constructor. |
| Aggregate `authorizationCoreDigest` | Later composition can substitute another core. | Recomputed-`Dac` equality would no longer discriminate substitution. | One aggregate binding; seven copies stay deleted. |
| Explicit serializer/registry routes | A native receipt falls through to generic identity or is absent from compatibility. | Tagged result/canonical-byte/compatibility evidence fails. | Two dispatch arms and two registry rows. |

Every retained mechanism answers a current structural threat. No nested proof
bag, signature, provider vocabulary, selected-history object, persistence
locator, archive policy, retention class, coordinator, or third domain was
added.

## Unauthorized surfaces remain absent

The delta contains no provider or broker call, credential/secret/reference
materialization, create-once persistence, lookup, retry, read-back,
disposition or timestamp authentication, selected-state/path/schema/operation
composition, selected proposal/receipt/tip, fail-closed runtime `UNKNOWN`
composition, post-selection receipt, archive, tombstone, deletion, retention,
compaction, capability, pointer proposal, CAS, command, filesystem mutation,
runtime recovery, merge, release, installation, external mutation, promotion,
or self-certification authority.

`ABSENT|DISABLED`, `recordedAt`, parsed bytes, paths, and `Dnc|Dnr` remain
structural caller-supplied evidence only. ISS-032 still owns authenticated
provider operation, persistence/read-back, disposition, and durable receipt
time. ISS-004 still owns selected-state/path/operation/time composition and
fail-closed `UNKNOWN`. A worker or candidate release never certifies itself;
only external review and the stable promotion path may grant successor
authority.

## Verification evidence

All required gates ran strictly one at a time. The standalone full-suite
process fully exited before the separate bootstrap verifier began. Focused and
standalone Vitest used increased `--testTimeout=30000 --hookTimeout=30000`.

The initial `pnpm run planning:check` launcher produced no checked-in script
output and failed closed after about 130 seconds because it could not fetch or
verify the signed registry release bytes for pinned `pnpm@11.22.0`. The six
gates thereafter used the identical checked-in script or lockfile-pinned binary
directly. Sandboxed board, formatting, and Vitest launches failed only at
child-process spawn with `EPERM`; their identical read-only commands were rerun
with approved process-spawn access.

1. Direct equivalent of `pnpm run planning:check`, `node
   scripts/planning/check.mjs` — PASS: `planning contracts verified`.
2. Direct equivalent of `pnpm run planning:board-check`, `node
   scripts/planning/board-check.mjs` — PASS: `board contracts verified`. It
   read only this repository and its registered delivery project.
3. Direct equivalent of `pnpm run format:check`, `node scripts/format.mjs
   check` — PASS: all matched files use Prettier style.
4. Direct lockfile-pinned equivalent of `pnpm run typecheck`, `node
   node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` — PASS.
5. Direct lockfile-pinned equivalent of standalone `pnpm test`, `node
   node_modules/vitest/vitest.mjs run --testTimeout=30000
   --hookTimeout=30000` — PASS: 32 files and 379 tests in 87.54 seconds. The
   process fully exited before step 6.
6. Direct equivalent of separate `pnpm run verify:bootstrap`, `node
   scripts/verify/bootstrap.mjs` — PASS: manifests/registries, formatting,
   typecheck, 32 files and 379 tests in 79.59 seconds, private compositions,
   planning contracts, and source-tree status invariance.

Additional serialized focused evidence:

- `recovery-native-receipt.test.ts` — PASS: 1 file, 6 tests.
- `compatibility.test.ts` — PASS: 1 file, 1 test.
- Independent framing/path reconstruction — PASS: all exact bytes, digests,
  domain negatives, untagged negative, and path golden matched.

## Authorization boundary

This `PASS` clears exact head
`520a36ea64a66af3468536c2a62b33dee5cec4cb` only for the bounded native-receipt
structural implementation: exact closed parsers, the existing receipt-own path
relation, `Dnc`/`Dnr`, canonical serializer routes, compatibility registration,
and the focused evidence above.

It does not authorize any provider, persistence, read-back, disposition/time
authentication, selected-state composition, post-selection receipt,
archive/tombstone, broker/capability/CAS/command/filesystem/runtime mechanism,
merge, release, installation, mutation, or promotion.

After this record was written, the sole intended worktree change was
`planning/pressure-tests/2026-08-21-round-132.md`; no source, test, draft,
issue, roadmap, board, or other planning file was edited, and no commit was
created.
