# Pressure round 455 — ISS-048 stage-three replan and archive layout

Status: bounded ISS-048 stage-three replan and archive-layout definition round,
artifact attempt 1 of this round's own ceiling of eight. The protected base is
`e6c9d4f1bfeb1347ba4f229d1c965b3c3bb7725e`. This packet is docs and planning
only. It does not complete stage three, perform its own independent review,
execute the native experiment, or grant any authority.

## Authority and declared inputs

Decision #278 ruling 1 admits exactly this round: one bounded stage-three replan
and archive-layout definition round, with ISS-048 staging rounds held distinct
from the ISS-022 experiment's proportionality budget, consistent with rounds
429, 430, 431, and 454 having been admitted. Ruling 2 keeps `vitest.config.ts`
closed for stage three and registers `.test.mjs` discovery as a separate later
slice. The same ruling records that the `.gitattributes` gap is to be decided
here.

Declared inputs are the read-only stage-three decomposition audit and its slice
definitions 3.R and 3.0 through 3.5; rounds 429, 430, 431, and 454; Decision
#258's bounded classifier sub-slice and its boundary that stage three may begin
only after that sub-slice lands; and observation O2 of the sub-slice 3.0
independent review receipt, which recorded the uncovered `.gitattributes` trees.
None of those inputs certifies this packet, and this packet certifies nothing.

## What the amendment closes

The ledger gains one clearly delimited subsection, "Single-OS archive layout,
roots, and the post-case census", placed after the retained-file and presence
rules and before the disposition text. It closes eight definitions, each
derivable from landed code that this round cites and does not change.

- The three sibling runner-temp roots and their emptiness preconditions. Cited:
  `scripts/conformance/hosted-native-lock-acquisition.mts:642-656, 688`;
  `scripts/build/native-lock-inputs.mjs:128-130, 150, 151-162`;
  `probes/portable-primitives/experiment/case-context.mjs:111-113`. The
  externality rule at `scripts/build/native-lock-inputs.mjs:132-144` is the
  reason acquisition outputs may never lie inside the preparation root.
- The archive-relative mapping. The archive root is the preparation root; every
  path is the identity mapping except the single declared removal of a leading
  `build/` from `build/transcripts/`, which is forced by
  `probes/portable-primitives/experiment/case-context.mjs:307-321` writing under
  the case artifact root while the ledger's fixed transcript path starts at
  `transcripts/`. Non-collision is cited at
  `scripts/build/native-lock-experiment.mjs:278-284, 297-303, 307, 321,
  342-346, 367, 380, 392-397, 484, 539-548` and
  `scripts/build/native-lock-inputs.mjs:441-442, 502-514`.
- The archive status of `case-diagnostics.json`: in, at
  `build/case-diagnostics.json`, required exactly once when the case phase ran
  and refused otherwise, not a report member, refusing on custody disagreement.
  Cited: `probes/portable-primitives/experiment/case-context.mjs:345-364`.
- The archive status of the fixed-lock custody root: out, structurally disjoint
  from the archive root, with no archive path resolving inside it and no
  retained file named `native-lock`; its evidence enters only as the report's
  custody member. Cited:
  `probes/portable-primitives/experiment/case-context.mjs:111-113` and the
  ledger's stable-custody clause.
- The second, post-case census that supersedes the sealed preparation census as
  the archive census, and the binding rule that every sealed row must reappear
  unchanged while the excess is exactly the fixed control files, the fixed
  transcript files, and `build/case-diagnostics.json`. Cited:
  `scripts/build/native-lock-inputs.mjs:403-405, 502-514`.
- The retained bound, stated numerically: at most 8192 retained entries under
  `build/inputs/headers/`, at most 512 further retained entries, and at most
  512 MiB of retained bytes over the second census. Cited:
  `scripts/build/native-lock-distribution.mjs:8-11, 30, 44, 106, 172` and
  `scripts/build/native-lock-experiment.mjs:286-291, 322`. No retained byte is
  dropped and no hash substitutes for a file.
- The per-OS reduction choice: a separate per-OS reducer, with the landed
  transcript reducer byte-stable. Cited:
  `probes/portable-primitives/experiment/reduction.mjs:26-31, 286-290, 291,
  372-376`, whose own comment anticipates a separate verifier, and
  `scripts/conformance/hosted-native-lock-plan.mts:158-171` for the honest
  digest cost, which is smaller for an amendment and is therefore not the
  argument.
- The `.gitattributes` decision: a bounded normalization slice for
  `probes/portable-primitives/experiment/`, `probes/portable-primitives/native/`,
  `scripts/build/`, and `test/native-lock-experiment/` lands after slice 3.5 and
  before any stage-four source, as its own reviewed slice, because no cross-OS
  digest comparison exists before stage four's aggregate. Cited:
  `.gitattributes` in full and
  `scripts/conformance/hosted-native-lock-plan.mts:36-76`.
- The registration of the later `.test.mjs` discovery slice as a separate
  reviewed slice after stage three, per ruling 2. Cited: `vitest.config.ts:12`
  and the five censused `.test.mjs` rows at
  `scripts/conformance/hosted-native-lock-plan.mts:63-76`.

The ISS-048 draft's stage-three sentence now names the ordered sub-slices, and a
new subsection registers 3.0 as landed and 3.1 through 3.5 with their title,
scope fence, decisions relied on, acceptance evidence, controls that are
executable synthetically versus unsupported or unknown by construction until
stage four, exact footprint, non-goals, review attack surface, and routing.

## Scope and non-goals

In scope: exactly three files, all documentation or planning. Out of scope and
absent from this packet: any source, test, workflow, or configuration file; any
Vitest change; any change to the four case IDs, twelve control IDs, four control
arms, report members, per-OS or control vocabularies, or the archive name; any
native, compiler, addon, probe, or lock execution; any provider or
administrative operation; any selection, profile, capability, release, or
promotion. Nothing here makes the ordinary conformance action non-byte-stable,
and nothing relaxes the immediate one-attempt owner-death property, the
one-attempt rule, or any death-case prohibition.

## Acceptance evidence

Exact issue and draft parity. Every archive-relative path in the amended layout
is derivable from exactly one of the four fixed prefixes `build/`,
`preparation/`, `controls/`, and `transcripts/`, with the report as the only
other archive member. The collector's missing-or-extra rule is decidable for
every retained file, including `case-diagnostics.json`, which has an explicit
presence rule, and including the custody root, which is explicitly out with an
explicit exclusion rule. The retained bound is stated both structurally and
numerically. The mapping is total, injective, and prefix-preserving, so no path
escapes its prefix.

## Verification

Author verification is limited to `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm planning:board-check`, `git diff --check`, and
exact-head inspection. `pnpm planning:board-check` reports one expected mismatch
until the controller host mirrors the ISS-048 issue body to this draft. No local
Vitest, test, build, native compiler, addon, probe, lock experiment, provider,
or administrative command runs in this round. The hosted three-OS bootstrap is
the docs-only landing gate and the sole executable evidence.

## Predicted footprint

The exact predicted footprint is:

- `docs/architecture/contract-decisions.md`
- `planning/drafts/ISS-048.md`
- `planning/pressure-tests/2026-09-05-round-455.md`

## Review attack surface

Review must attack a layout amendment that silently widens what may be
archived; `case-diagnostics.json` admitted without a presence rule; a rebasing
rule that lets a control or transcript path escape its fixed prefix; a bound
that would let a real run drop retained bytes or substitute a hash for a file;
stage-three source smuggled into a docs round; and a replan used to relax the
death property, the one-attempt rule, or any attempt ceiling.

## Attempt ledger and routing

Artifact attempt 1 is this packet. Independent exact-head semantic review
attempt 2 remains pending and must be performed by a different lane before any
pull request; this lane does not call this round reviewed. Routing follows
model-routing row 8 on `claude-opus-5` at high reasoning effort, as an
irreversible ledger amendment rather than implementation. Placement is
provisional, budget intent is at most $12, actual spend is unknown, and no new
ceiling is granted. This round closes nothing by itself.
