# ISS-022 provider-neutral decision-contract follow-up — round 316

`PASS` at exact base/HEAD
`75ad198417fd56152c2621506258156c0c8506d8`.

Round 315's two blockers are closed. `BLOCK_REPLAN` now requires all three
OS-profile slots null. Helper and ABI slot presence must match, and the closed,
bounded, stable-ordered observation join requires both non-null for every OS
represented by an available observation; null is therefore possible only for
an absent OS. PASS still requires 63 ordered PASS observations and all slots
non-null. BLOCK accepts zero through 63 observations, including 63 PASS rows,
because the later authenticated diagnostic composer may prove a missing job or
artifact rather than a non-PASS observation.

The premature scalar-expectation interfaces and their authority-sounding core
and review validators are deleted. The remaining joins consume actual core,
observation, review, and final-decision records, snapshot them, recompute the
observation/core/review digests, and require the exact disposition. Parsers,
serialization, and digest helpers grant no aggregate or diagnostic authority;
authentication of aggregate, terminal, environment, OS-profile, locator, and
custody evidence remains explicitly deferred to the later stable terminal
composer.

The repaired provider-neutral three-file implementation is `+767`, smaller
than Round 315 and readable in one sitting. It adds no adapter, GitHub
vocabulary, live fetch, writer, publication, command, workflow, board, helper,
or production mechanism. Focused contract tests (4), repository TypeScript,
touched-file Prettier, and `git diff --check` pass, including mutants for
non-null block OS profiles, mismatched helper/ABI presence, present-OS null
slots, reversed observations, and the 63-PASS diagnostic syntax.
