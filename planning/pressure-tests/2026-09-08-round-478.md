# Pressure round 478 - bounded dogfood workspace preparation

Date: 2026-09-08. ISS-074/PR #334 is externally accepted. Its issue remains
open only for this expressly designated real-use acceptance, admitted by the
controller's consumer policy. ISS-075 is a distinct useful implementation under
EPIC-KERNEL/M2, blocked by ISS-074 for authority timing. The executing stable
pilot and tree-equivalent main base are externally selected immutable revisions;
the candidate cannot certify or activate itself.

ISS-075 uses its ordinary eight-attempt ceiling. This packet reserves author 1
and independent reviewer 2. It preserves every prior issue's attempt, failure,
review, hosted-execution, and spend history; no old counter is reset. Chase Sets
runtime mechanics are advisory here. The controller retains all four gates,
hosted Windows/macOS/Linux proof, review admission and later real-use authority.

## G0, profile and proportionality

G0 is YES for each of the six acceptance criteria before implementation. The
mapping, full `quality-contract/v2` `contract` profile, weighted blocking rule,
not-built list, deletion test, and all twelve two-sided quality pairs are source
material in `planning/drafts/ISS-075.md`. The packet is reviewable without any
private worker output. Only a confirmed correctness/security defect or confirmed
zero in an objective subcase weighted at least 3 blocks; other notes remain
advisory follow-up and cannot delay landing.

The smallest complete shape is one private transition, one concrete Git/pnpm
adapter, existing `.mjs`/declaration composition, and a thin direct-Node command.
Engine-only has no controller invocation. A monolith fails the required portable
transition/repository-policy separation. Therefore neither smaller alternative
meets all criteria.

Not built: scheduler, queue, generalized workspace registry, dependency cache
manager, cleanup/reclamation, dispatch, provider operation, credentials or
environment inspection, automatic repair, public capability/contract family,
package/compiler change, production activation, or release/operator authority.

## Implementation sweep and attack

The portable transition validates the closed external authority and canonical
disjoint path set, scans all three current worktree states before recording or
creating any selected worktree, then writes immutable per-role intent before
each Git or installer effect. Existing unowned paths/branches/worktrees, dirty
state, malformed records, aliases, head/branch drift and checkout-contained
state refuse without deletion. Confirmed worktrees and dependency receipts are
re-observed on restart. The adapter alone owns Git and the existing pnpm launcher.

The exact installer tuple is `install --offline --frozen-lockfile
--ignore-scripts`. Process output is discarded. A nonzero exit, spawn uncertainty,
or missing post-install modules marker remains a closed incomplete/refusal; no
network fallback or success receipt is possible. Runtime output contains only
closed status/reason/phase, run/issue, approved heads, and selected worktree IDs.

Tests are source-only in this attempt. Fault-seam tests cover immutable intent,
interruption after mutation, repeated setup, known/unknown installer failure and
resume, malformed authority, candidate-as-pilot, and unrelated external state.
Real synthetic Git tests cover spaces/Unicode paths, exact detached and branch
heads, repeat without duplicate install, path/branch collision, moving base,
dirty state, checkout-contained state and direct Node 24 composition. The two
multi-cycle Git cases carry only a 30-second per-test bound, justified by the
observed Windows five-second default; no global timeout or assertion is relaxed.

The prescribed source deltas after the complete sweep corrected one adapter
call signature and unsupported matcher annotations, bound the issue identity,
moved dependency-record
validation ahead of every selected Git mutation, bound all roots and the full
authority digest into the durable plan, rechecked worktree cleanliness after
every installer outcome, rejected a raced source branch at the wrong base,
required a regular pnpm marker, and extended containment checks to every
registered worktree and the Git common directory while still admitting exact
owned targets on resume. The existing repeat assertion attacks that last
distinction. Static TypeScript, changed-file formatting and planning graph checks
pass after these deltas. The board gate remains controller-owned; its local
provider-read subprocess was sandbox-refused and supplied no acceptance evidence.

No local test runner, build, native experiment, provider/network action,
credential probe, environment/store inspection, or raw trace/private-output read
is authorized. Static TypeScript, changed-file formatting and planning checks
are the only source-side verification. After this single implementation sweep,
reviewer 2 applies the prescribed-delta cadence over any requested changes and
direct callers while inheriting this complete sweep.

## Falsifiable outcome

After independent exact-head review and actual hosted three-OS proof, the stable
successor must prepare the next genuinely useful repair-handoff issue with all
three exact worktrees, dependency receipts, restart identity and partial-failure
evidence. A custom setup helper, wrong-head adoption, duplicate mutation,
unbounded process text or new generalized machinery falsifies the extraction.
