# ISS-013 pure snapshot contracts — round 347

Disposition: `BLOCK_FIXABLE` at `28d615c1958d757ab1e68e4e3133a429723c9ddd`,
base `2d649ec3ebf4437affd6bad497e663b560ed24c0`.
Independent cold reviewer: `/root/iss013_contracts_review`, Sol high, row 11.

The new canonical-byte path checks intrinsic byte length but then traverses a
caller-controlled prototype through `instanceof`. For each new schema, the
host set a genuine Uint8Array's prototype to a proxy whose getPrototypeOf trap
increments a counter and throws. Both executed canaries failed: the parser
refused only after one caller invocation, where zero was required.

The original full suite also exposed one stale advisory test-path count:
939 tests passed, 12 were platform skips, and the census expected 30 paths
instead of the newly registered 31. Correct that exact restated census.

No other blocker was found in the closed union, scalar bounds, canonical
digests, configuration bindings, capability subsets, or immutable snapshots.
Project CLI success and SDK observation remain unimplemented by this slice.

Proportionality: closed input admission prevents caller execution, while
digest and binding checks prevent substituted facts. Deleting those checks
restores their concrete threats. Existing helpers and a local intrinsic byte
guard are smaller than a new validation framework.
