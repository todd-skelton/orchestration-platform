# Pressure round 454 — ISS-048 authenticated classifier census

Status: bounded ISS-048 stage-3 sub-slice 3.0, source artifact attempt 2
after read-only definition audit 1. The protected base is
`43971851c6bddb782be09b4fee042c228dbfd9af` (PR #276). This packet does not
complete stage 3, perform independent review, execute the native experiment,
or grant authority.

## Lineage and scope

Rounds 426, 429, 430, and 431 remain cumulative inputs. Decision #258 admitted
the bounded four-file classifier sub-slice, which landed on protected main in
PR #276. Decision #278 records the stage-3 decomposition and identifies this
3.0 census extension as required by construction. Neither decision permits the
classifier or this source lane to certify itself.

This slice extends the authenticated hosted native-lock plan census with the
three landed classifier modules declared by `stableCaseFiles` and the landed
classifier test. It adds focused removal evidence at the existing real-clone
finalization seam: appending a byte to each file independently must move its
applicable harness or test bundle digest and the provider-run digest. A direct
manifest vector also retains the raw-UTF-8 path-order refusal category.

## Non-goals and acceptance evidence

No new module, runner, control, workflow, ID list, or vocabulary is added or
changed. No native execution, Vitest configuration change, classifier change,
provider or administrative operation, selection, profile, capability, release,
promotion, or authority path is in scope. Nothing in this packet makes the
ordinary conformance action non-byte-stable.

Acceptance evidence is limited to six independent real-clone mutants: the
existing `contracts.ts` and `hosted-plan.mts` vectors plus one for each newly
censused file. Each classifier-module mutant changes `harnessBundleDigest` and
`providerRunDigest`; the classifier-test mutant changes `testBundleDigest` and
`providerRunDigest`. A deliberately misordered census must refuse with exactly
`paths:order-refused`. These tests are executable evidence candidates, not
self-certification or acceptance.

## Verification, footprint, and review attack surface

Author verification is limited to `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm planning:board-check`, mechanical Node path-order
comparison by code unit, `git diff --check`, and exact-head inspection. No local
Vitest, test, build, native compiler, addon, probe, lock experiment, provider,
or administrative command may run. Hosted three-OS bootstrap after independent
review is the sole executable evidence.

The exact predicted footprint is:

- `scripts/conformance/hosted-native-lock-plan.mts`
- `test/conformance/hosted-plan.test.ts`
- `planning/pressure-tests/2026-09-05-round-454.md`

Review must attack raw-UTF-8 ordering masked by Windows path behavior; a census
row without a digest-moving mutant; collateral change to the ordinary harness
census; and confusion between the harness and test lists or their respective
digests.

## Attempt ledger and routing

Read-only definition audit 1 is complete. Source artifact attempt 2 is this
packet. Independent exact-head review attempt 3 remains pending and must be
performed by a different lane before any PR. Routing follows model-routing row
4 on `gpt-5.6-sol` at high reasoning effort. Placement is provisional, budget
intent is at most $15, actual spend is unknown, and no new ceiling is granted.
This round closes nothing.
