# Planning pressure test — round 7

Disposition received: `BLOCK_REPLAN`.

The seventh independent reviewer found six remaining gaps in the routine-cycle
type flow, resource disposition, credentials, Codex tool admission, consumer
scaffold, and production bootstrap transcript.

| Finding | Replan disposition |
|---|---|
| `OP-R7-001` routine-cycle mutation flow skipped typed plan/apply boundaries | Expanded `routine-cycle/v1` to fourteen exhaustive producer/consumer steps, including disposition planning, immutable mutation planning, action application, reclaim, and terminal reduction. Added compile-time flow and stale-authority evidence to `ISS-026`. |
| `OP-R7-002` cleanup/review disposition had no owner on all routes | Made `disposition.plan` the sole post-review classification and required every allocation path to pass through `resource.reclaim` before terminal reduction. |
| `OP-R7-003` cold-host credential authority was undefined | Published the native credential-reference/broker contract and added `ISS-031` probe plus `ISS-032` implementation, with explicit dependency edges into dispatch, host, engine, supervisor, and final certification. |
| `OP-R7-004` Codex tool admission depended on ambient effective configuration | Published a closed per-role tool-name/schema/capability manifest and strengthened `ISS-023` with raw census and schema/capability mutation evidence. |
| `OP-R7-005` the first consumer adapter sat outside the frozen workspace manifest | Predeclared `adapters/first-consumer/`, its exports, placeholders, and workspace globs under `ISS-000`; narrowed `ISS-028` to filling that scaffold without root-manifest edits. |
| `OP-R7-006` production N0 installation lacked an executable operator protocol | Expanded `ISS-027` with exact run/artifact/digest selection, protected-environment comparison, attestation verification, canonical bootstrap argv, mutation boundary, recovery, staging permissions, and a 30-minute installed-supervisor watch. |

This is a failed-round record, not a pass. The revised 33-issue packet requires
another fresh independent review before registration.
