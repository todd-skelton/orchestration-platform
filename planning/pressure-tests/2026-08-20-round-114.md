# ISS-002 pressure test — round 114

## Review basis

This is a fresh independent cold removal review of exact candidate head
`559245b1ee7d77bd075be98a92dde01d26a1b870` on
`iss-002-versioned-contracts`, with parent boundary
`59648f8694f318682ff8ffe71ccfa6a535cd3923`. The checkout was clean before
this required pressure record was written.

The review is bounded to removal of round 113's successor-origin CONSUMED
chronology and test blocker. The reviewer used only this repository's
instructions, work model, contract authority, registered `ISS-002` draft,
prior pressure record, and exact parent-to-head change. No Chase Sets
repository, project, or issue was accessed.

## Successor chronology composition

One shared pure helper now executes the four inclusive chronology edges for
both successor-ACTIVE retirement and successor-origin CONSUMED retirement:

```text
prior owner RETIRED proposal <= Dsrc independent review
Dsrc independent review <= selected successor owner ACTIVE proposal
selected successor owner ACTIVE proposal <= Dsrp observation
Dsrp observation <= retirement Dabs observation
```

Genesis-origin CONSUMED retirement does not carry successor provenance and
skips this helper. Complete valid genesis- and successor-origin fixtures both
return `[]`.

Fully rebuilt complete successor graphs refuse all four reverse chronology
cases. Each case returns its one exact expected issue. Fully rebuilt controls
accept equality at each of the four successor chronology boundaries, and a
separate complete control accepts
`consumedAt == retirement Dabs.observedAt`.

## Executable removal evidence

An in-memory mutation proof ran a 32-case baseline with 32 of 32 controls
passing. It then isolated ten mutants:

- deletion of each of the four successor chronology comparisons;
- changing each of those four inclusive comparisons from `<` to `<=`;
- deletion of the consumption chronology comparison; and
- changing the inclusive consumption comparison from `<` to `<=`.

All ten mutants were killed by exactly one committed test, with zero
survivors. The committed reverse cases therefore discriminate comparison
deletion, while the equality controls discriminate accidental strictness.

The repair adds no schema field, record kind, digest domain, lifecycle state,
runtime capability, mutation authority, or promotion path. It neither widens
the engine/adapter boundary nor changes the self-hosting trust model.

## Proportionality and deletion bounds

| Mechanism | Concrete in-scope threat | Removal result | Smallest credible shape |
| --- | --- | --- | --- |
| Shared four-edge successor chronology helper | A structurally valid successor graph could claim review, activation, or observations in an impossible order. | Each deleted edge admits its fully rebuilt reverse case. | Reuse one pure helper over records already required by both public wrappers. |
| Inclusive comparisons | A valid history whose adjacent events share a timestamp could be rejected. | Every `<` to `<=` mutant is killed by one equality control. | Preserve the four direct `<` reverse checks; add no clock policy or tolerance mechanism. |
| Consumption chronology comparison | Consumption could be claimed after the absence observation that retires it. | Deleting the check admits the committed reverse case. | Retain the existing direct comparison. |
| Consumption equality control | A valid equal-timestamp consumption boundary could become accidentally strict. | The strictness mutant is killed by the equality control. | Retain one fully rebuilt public-wrapper equality case. |

## Verification evidence

All gates were serialized. The standalone full suite exited before bootstrap
began; neither full-suite process overlapped another.

1. Planning — PASS, `planning contracts verified`, through the approved direct
   checker after its registry wrapper failed closed.
2. Board — PASS, `board contracts verified`, through the approved direct
   checker after the restricted subprocess failed closed with `spawn EPERM`.
3. Format — PASS through the approved direct checker.
4. Typecheck — PASS.
5. Standalone full test gate — PASS: 28 files and 349 tests in 323.60 seconds.
6. Separate bootstrap verification — PASS: 28 files and 349 tests in 185.03
   seconds, plus all bootstrap phases and source-tree status invariance.
7. Focused ISS-002 contracts — PASS: 18 files and 152 tests in 6.42 seconds.
8. Focused ISS-002 compatibility — PASS: one file and one test in 1.11
   seconds.

Before this required pressure record was written, every gate and probe left
the source tree clean and exact HEAD remained
`559245b1ee7d77bd075be98a92dde01d26a1b870`.

## Final status

`PASS` for exact candidate head
`559245b1ee7d77bd075be98a92dde01d26a1b870`, with parent boundary
`59648f8694f318682ff8ffe71ccfa6a535cd3923`.

Round 113's bounded successor-origin CONSUMED chronology and test blocker is
removed. The shared pure chronology helper, fully rebuilt reverse and equality
controls, and zero-survivor mutation proof make deletion or accidental
strictness observable without widening authority.

This disposition grants no authorization for any remaining `ISS-002`
release, gate, fence, cleanup, or recovery surface, and no authorization for
merge, release, or promotion. A candidate never certifies itself; those
surfaces require their own implementation evidence and independent review.
