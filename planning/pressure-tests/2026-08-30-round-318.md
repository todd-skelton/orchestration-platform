# ISS-022 decision-core writer pressure review — round 318

`BLOCK_REPLAN` at exact base/HEAD
`78004217f75b28f1ae0e4d0ce0e19487ea289f49`.

The uncommitted ten-file, `+680/-2` slice correctly extends the existing
terminal writer with the authority-approved `portable-primitives-decision`
submode and keeps the ordinary four-argument verifier unchanged. Composition
consumes authenticated provider/diagnostic terminal records, record-bound
second-read aggregate/observation archives, exact raw reports and receipts,
stable bundles/registry/contracts, and recomputed vector, observation, profile,
writer, and core identities. PASS and BLOCK_REPLAN arms retain the required
slot/null behavior without caller-supplied expected digests or synthetic
evidence. No workflow, publication, board, root-command, helper, or production
mechanism enters the diff.

Two external-output authority blockers remain:

1. Output exclusion is scoped only to `git -C <stableRoot> worktree list`.
   That table cannot identify a different repository checkout. The read-only
   counterexample
   `D:/Users/ToddS/Source/Repos/130-point-proxy/node_modules/stream-to-promise/node_modules`
   is an absolute, real, empty directory inside another Git checkout, while the
   implemented worktree census contains no `130-point-proxy` row; it therefore
   passes every check in `decisionOutputCustody`. This violates the required
   outside-every-worktree/checkout boundary and permits decision output inside
   unrelated source custody. Revalidate the canonical output root against a
   closed checkout-overlap rule that detects foreign checkout ancestors as
   well as this repository's registered worktrees, with focused inside/ancestor/
   symlink mutants.
2. After `writeFile(..., { flag: "wx" })`, moved/populated and final-census
   failures return refusal without removing the just-written attempt-qualified
   core. The module imports no `unlink`/`rm`, and no refusal/catch path performs
   cleanup. An extra entry or directory swap can therefore produce `REFUSED`
   while leaving authority-shaped output behind. Add bounded cleanup tied to
   the originally observed directory/file identity, refuse when safe cleanup
   cannot be proven, and test extra-entry and movement races plus exact
   post-refusal census.

These checks are deletion-discriminating and proportional: retaining the
existing composer/terminal submode is smaller than another writer, but neither
checkout overlap nor refused-output residue can be delegated to callers.

Focused evidence: conformance TypeScript and touched-file Prettier passed;
`git diff --check` passed. The three touched test files produced 16 passes and
one five-second timeout in the direct-entrypoint spawn test. Changed-test
census confirms there is no direct success/custody test for
`writeHostedPortablePrimitivesDecisionCore`; the only new CLI case checks
refusal for the current checkout root.
