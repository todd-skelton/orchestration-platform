# Pressure test — round 39

**Historical branch A status:** superseded by the A/B reconciliation. This
record preserves branch-A review history but grants no authority to the
reconciled head.

**Disposition:** `BLOCK_REPLAN` (removal round; seven sentence-level deletions found and applied)

The thirty-ninth review verified the round-38 repairs (retention record fully
gone, rotation content confined to canonical sections, request-side-only
`ISS-014`, archive schemas present) and swept the whole packet a final time.
It found no removable machinery — every remaining state, schema, receipt, and
proof was examined and retained with reasons — but caught a fifth generation
of the prose-drift class: normative rules restated in slots earlier rounds
vacated.

| Finding | Repair applied |
| --- | --- |
| `OP-R39-A` — The schema-lifecycle rule (v1-everywhere, superseded generations deleted, no diagnostic namespace) was restated three times in `supervisor-contract.md` — already drifting in scope between copies — and twice in `compatibility.md`. | One canonical statement kept per document (`supervisor-contract.md`'s closed-schema census; `compatibility.md`'s registry opening); the other four copies deleted. |
| `OP-R38-B` residue — Three FULL_REQUIRED restatements survived the round-38 fold ("no deletion or degraded compaction of owner lineage"; "Run audit is FULL_REQUIRED."; "Authority history remains FULL_REQUIRED."). | Deleted; the bare-absence half of the owner sentence, which is load-bearing, was kept. |
| `OP-R39-B` — The "no armed rotation intent selected → head-plus-one refuses" clause was duplicated inside `supervisor-contract.md` and `state-machines.md`; the chain-walk copies defer to definitions that already carry it. | Chain-walk parentheticals deleted; the clause lives in each document's armed-intent definition. |
| Trivial — "and retention behavior" in `contract-decisions.md`'s framing bullet was vestigial after the retention deletion. | Word removed. |

## Examined and found not further removable

The eleven-kind registry and per-row policy; framing and goldens; the
four-value classification and enumerated tombstone census; the
`Dphys`/`Dobs`/`Ddest` separation; the recovery-authorization selection
chain; the reservation/descriptor/attachment/attempt-log chain; the
eleven-slot packet; the anchor lifecycle archive (probed as an OP-R38-A-shaped
candidate; retained — it is the `RETIRED` evidence the state machine
requires); the nine-stage phase matrix; all fourteen state machines; every
draft's single-statement rules; and the Decisions-vs-AC near-duplication in
`ISS-002`/`ISS-004`, retained deliberately because those two issues own the
contract and the execution respectively and the split is the packet's
draft convention.

Out-of-scope observation recorded for `ISS-002`'s implementation phase: the
surviving pre-replan code in `packages/contracts` still declares the deleted
retention field; its removal is already covered by `ISS-002` AC13's deletion
requirement.

## Re-review focus

- The seven deletions are pure subsets of retained statements; verify no
  load-bearing clause was lost (especially the owner bare-absence rule).
- The next round closes the revision: it must find the repairs landed and
  record the nothing-further-removable finding.
