# Pressure test — round 81 external-ledger final removal review

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `d5c9a42416d4b83173a21773cd7f13bdb47b0ac9`

This was a fresh independent cold exact-head review of the initially clean head
above on `iss-002-versioned-contracts`. There was no authoring role or authoring
context. The review began with `AGENTS.md`,
`docs/architecture/contract-decisions.md`, and
`docs/planning/work-model.md` as the sole decision authority, then read the
incorporated normative external-bootstrap ledger in
`docs/architecture/supervisor-contract.md`, compatibility, ISS-002/006/020/022,
rounds 70/78/79/80, the exact
`8b94edde6217361406ca18f93628b9d78182925a..d5c9a42416d4b83173a21773cd7f13bdb47b0ac9`
delta, and the current external implementation/tests and inventory timeout
line. No Chase Sets repository, issue, board, project, backlog, or other
consumer system was read or touched. The board check read only
`todd-skelton/orchestration-platform` and its registered delivery project.

## Disposition

`BLOCK_REPLAN`, solely because the required once-only default named standalone
`pnpm test` gate was not green. It reached the unchanged Vitest suite but the
planned-command execution census exceeded its separate 240-second local
ceiling: the file completed at 242.248 seconds, the named test spent 241.811
seconds, and Vitest reported 21 files/266 tests passed with that one timeout.
The inventory relationship matrix that caused `OP-R80-001` was no longer the
failure.

The separate unchanged `pnpm run verify:bootstrap` subsequently passed its
full 22-file/267-test phase in 164.29 seconds and completed manifests,
registries, formatting, typecheck, private compositions, planning contracts,
and source-status invariance. That later green run bounds the defect to
first/default-gate execution timing or launcher-state reproducibility; it does
not retroactively make the required standalone named gate green. The once-only
instruction forbids retrying until a pass appears.

The exact semantic removal otherwise passes. `OP-R80-001` is removed by the
single intended inventory-test timeout change. `OP-R79-001` and all six
round-78 findings remain removed, no new external-ledger schema, path, digest,
lifecycle, equality, or trust blocker was found, and round 70 block 1 is
contractually implementation-ready. Authorization remains withheld at this
exact head only because the required default gate is not reproducibly green.
Round 70 blocks 2 and 3 remain independently unauthorized. This record grants
no merge, release, promotion, installation, runtime mutation, live capability,
or self-certification authority.

## `OP-R80-001` removal audit

The exact delta has two paths only:

- `test/reference-inventory/inventory.test.ts` changes the one local timeout on
  the key-independent relationship test from `120_000` to `300_000`;
- `planning/pressure-tests/2026-08-18-round-80.md` records the prior independent
  block and proposed bounded repair.

The executable test body, all relationship loops, excluded/accepted cases, and
all four numeric assertions remain byte-for-byte unchanged. No production
file, parser, canonicalizer, digest, schema, path, lifecycle validator,
registry, wrapper, global timeout, or other test is changed. The standalone
full gate did not report the inventory test as failed; the bootstrap full-suite
phase passed all 267 tests. The original 120-second inventory ceiling is
therefore no longer the blocking gate.

## External-ledger semantic removal audit

### `OP-R79-001` — remains removed

Both retirement branches now select one exact existing absence predicate.
`RETIRE_UNUSED` and `RETIRE_CONSUMED` require
`DESTINATION_STATE_ROOT_ABSENT`, while E0 alone requires
`RUNTIME_AUTHORITY_ABSENT`. Consumed teardown observes the destination-state
root after terminal cleanup and before the lifecycle archive while the
applicable destination and anchor locks remain held. Its receipt equal-binds
the same-lock `Dabs` and exact selected owner/anchor CONSUMED graph. An opaque
digest, the runtime-only reason, another state root, observation, helper,
custody instance, or lock section contradicts a named equality.

ISS-002/006/020/022 and compatibility require the same exact branch census and
mutants. They incorporate rather than redefine the normative ledger. There is
no remaining implementer choice about the consumed-retirement absence premise.

### `OP-R78-001` — remains removed

The use intent and nested proposed-genesis input contain only independent
bootstrap preimages: authority path, grant/transaction, genesis position, `G`,
and `Dsc`. They contain no `Dgb`, GENESIS `Dh`, E0 `Dv`, `Dbg`, or downstream
selection evidence. The graph remains constructible and one-way:

```text
independent facts -> owner/anchor ACTIVE -> use intent
  -> Dgb -> GENESIS Dh -> E0 Dv -> Dbg
  -> proposal/Dr -> tip/Dt -> Dgp
  -> anchor/owner CONSUMED -> consumption receipt -> Dgse/context
```

### `OP-R78-002` — remains removed

The anchor-consumption census has distinct
`runtimePostSelectionReceiptDigest` and
`runtimePostSelectionReceiptReadbackDigest` members. Framing and equality
separately bind the selected `Dgp` and the parsed persisted receipt readback;
receipt identity cannot substitute for persistence evidence.

### `OP-R78-003` — remains removed

`external-destination-absence-observation/v1` retains one exact census, domain,
framed order, content-addressed path, ISS-022-admitted producer, same-lock
create-once/readback boundary, reason matrix, and physical/destination/state/
observation/helper/custody equalities. E0 and both retirement branches are
closed; a caller-selected SHA-256 value carries no absence authority.

### `OP-R78-004` — remains removed

The successor-review candidate has exact domain and framing over destination,
selected prior RETIRED tip/value/receipt, teardown archive, canonical prior
installation, and canonical successor authority. It excludes the independent
review record, `Dsrc`, successor anchor/owner graph, `Dsrp`, and readbacks.
External authentication of the recomputed candidate plus distinct author and
reviewer identities prevents self-review and coordinated candidate
substitution.

### `OP-R78-005` — remains removed

Retirement remains an explicit forward crash order:

```text
Dabs -> prior-anchor lifecycle archive -> teardown receipt
  -> anchor RETIRED value/proposal/tip
  -> destination-owner teardown archive
  -> owner RETIRED value/proposal/tip
```

The lifecycle archive contains the selected prior anchor triple/lifecycle and
`Dabs`, but no teardown receipt or future lifecycle value. The receipt binds
the already-known archive. No archive or receipt names a future value,
proposal, or tip, and every crash prefix resumes the same derived artifact or
CAS.

### `OP-R78-006` — remains removed

Physical leaf identity retains one portable algorithm: canonical unpadded
base64url decodes to 1--3072 shortest-form UTF-8 bytes without BOM, surrogates,
or noncharacters; Unicode 15.1.0 normalization and unconditional default
special-casing lowercase data are pinned; lowercase precedes normalization for
the two case-insensitive profiles; exact re-encoding must reproduce the decoded
bytes. Darwin is lowercase NFD, Windows lowercase NFC, and Linux case-sensitive
NFC. The one-component, alias, separator, dot, prefix, trailing-dot/space,
control, alternate-stream, reserved-device, `CLOCK$`, numbered `COM`/`LPT`,
and superscript alias rules remain exact. Helper/profile/custody rotation cannot
move `Dphys/Ddest`.

## Exact schema, path, digest, and trust audit

The external ledger's literal member censuses remain closed and in canonical
order. Scalar grammars, nested-record identities, nullable groups, lifecycle
ordinals, UUIDv7 values, timestamps, safe decimals, enums, raw-digest versus
canonical-byte framing, persistence status, and exclusions remain exact.
Owner position, owner/anchor mutation, consumption/teardown receipt, successor
review/post, lifecycle archive, E0 transaction, and history ordinal paths are
derived from named inputs without caller-selected path keys or directory
enumeration.

The accepted bootstrap graph cross-binds stable `Dphys/Ddest`, current admitted
`Dobs`, selected owner/anchor ACTIVE, use intent, `Dsc`, `Dgb`, GENESIS `Dh`,
authority `Dp/Dv`, `Dbg`, proposal `Dr`, tip `Dt`, `Dgp`, selected owner/anchor
CONSUMED, final consumption, and downstream `Dgse`. Repeated digests equal
their parsed selected upstream records rather than merely co-hashing copied
values. The complete history walk rejects fork, gap, reorder, truncation,
substituted head, or unarmed excess.

Structural validation grants no mutation authority. External independent
review, ISS-022-admitted physical/helper/profile/custody facts, destination →
anchor → runtime kernel lock order, the selected runtime authority, ISS-004's
private revocable handle, and stable promotion remain separate trust
boundaries. A worker or candidate cannot author its own review, absence
premise, live capability, or promotion.

Current `packages/contracts/src/external.ts` and
`test/contracts/external.test.ts` remain the deliberately partial pre-ledger
implementation: raw-`Dphys` `Ddest` derivation and constructed authority paths
only. The green focused and bootstrap evidence is regression evidence for
existing surfaces, not a claim that the planned external ledger is already
implemented.

## New definite blocker

### `OP-R81-001` — the default named full gate is not reproducibly green from a cold invocation

The actual unchanged `pnpm test` command reached Vitest and failed only
`test/planned-command-execution.test.ts:39`. Its 240-second ceiling expired
after 241.811 seconds; the file completed at 242.248 seconds. All 21 other files
and 266 other tests passed, including the inventory relationship matrix repaired
by this delta. This is a timeout, not an assertion failure.

The later bootstrap verifier ran the same default Vitest suite successfully,
22 files/267 tests in 164.29 seconds. Focused contracts and compatibility also
passed. The difference demonstrates that the contract assertions are green but
the named standalone gate depends on execution/launcher state or timing that is
not bounded by its current ceiling. A release gate cannot reinterpret its first
red result as success because a later enclosing gate warmed state and passed.

The smallest replan is to make the cold/default named gate deterministic before
another review. First reproduce with an explicit cold launcher/cache snapshot
and no concurrent repository command. If valid planned-command census work can
again exceed 240 seconds, raise only that test's local ceiling to a bounded
measured value or remove repeated package-manager launcher negotiation while
preserving every command/owner/argv assertion. Do not weaken the census, alter
production behavior, introduce a global timeout, or treat a retry as release
evidence. Return the exact clean repair head through fresh independent review
and run the named full and separate bootstrap gates once each.

## Bounded proportionality and trust analysis

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Closed same-lock `Dabs` branches | A candidate uses runtime-only or opaque absence to authorize E0 or destructive teardown. | Remove a reason/equality and the round-79 implementer choice returns. | Retain the existing two-reason observation family and exact branch matrix; no new record. |
| Upstream-only use intent | Bootstrap requires a digest fixed point or authenticates itself downstream. | Insert `Dgb`, `Dh`, `Dv`, or `Dbg` into the intent and the graph cycles. | Retain independent preimages and downstream equality checks; no coordinator. |
| Distinct `Dgp` and readback | Receipt identity is mistaken for verified persistence. | Collapse the members and missing/substituted persisted bytes lose their named equality. | Retain the two existing digests; no new receipt family. |
| Exact successor-review candidate | Reviewer and installer authenticate different projections or self-review. | Remove the candidate formula or identity inequality and coordinated substitution is possible. | Retain the non-persisted framed candidate over existing facts; no candidate pointer. |
| One-way retirement artifacts | A fixed point or future-selected value makes crash recovery unconstructible. | Reverse an edge and an upstream artifact again depends on a future digest. | Retain `Dabs→archive→receipt→anchor→owner`; no sequencer or second archive. |
| Pinned portable leaf algorithm | Helper/profile rotation aliases or moves a physical destination. | Unpin decoding, Unicode data, profile, or reserved names and conforming helpers may disagree. | Retain the fixed algorithm and cross-OS vectors; no alias inventory. |
| Deterministic cold full gate | A required exhaustive census can be red on its first named invocation even though later warmed execution passes. | Keep the present execution/ceiling relation and the exact reviewed run remains red. | Measure the cold launcher path, then bound only the local census or eliminate redundant launcher negotiation; retain every assertion and the unchanged named gate. |

The first six mechanisms protect current public bytes and the one actual
multi-writer destination boundary. The seventh protects release-evidence
reproducibility only. Nothing supports a third absence reason, proof tree,
inventory, coordinator, migration, verdict route, global timeout, or
candidate-owned authority.

## Round 70 blocks 2 and 3 remain unauthorized

This review authorizes no implementation of either remaining round-70 block:

2. active release, cleanup gate, recovery fence, and cleanup archive records;
3. recovery authorization, reservation, descriptor, launch, attachment, and
   attempt-log records.

Their registry names, lifecycle prose, partial implementation, or block-1
ledger do not provide their missing literal schema ledgers. Each requires its
own decision amendment and fresh independent exact-head review.

## Verification evidence

- Initial branch/status/full-SHA checks — PASS; the worktree was clean,
  `iss-002-versioned-contracts` was exact, and the reviewed full SHA was
  `d5c9a42416d4b83173a21773cd7f13bdb47b0ac9`.
- Exact
  `8b94edde6217361406ca18f93628b9d78182925a..d5c9a42416d4b83173a21773cd7f13bdb47b0ac9`
  diff — two paths, 322 insertions and one deletion; only the round-80 record
  and one `120_000→300_000` inventory-test timeout change; `git diff --check`
  passed.
- The first restricted `pnpm test` invocation did not reach the suite because
  the managed launcher could not verify its registry signature without
  network access. The approved actual named rerun reached unchanged Vitest and
  BLOCKED only at `test/planned-command-execution.test.ts:39` after 241.811
  seconds against 240 seconds (21 files/266 tests passed; no assertion failure).
- `pnpm --filter @orchestration-platform/contracts test` — PASS (12 files, 70
  tests).
- `pnpm run contracts:compatibility-check` — PASS (1 file, 1 test).
- Separate unchanged `pnpm run verify:bootstrap` — PASS: manifests/registries,
  formatting, typecheck, 22 files/267 tests in 164.29 seconds, private
  compositions, planning contracts, and source-tree status invariance.
- Direct repository-only `node scripts/planning/board-check.mjs` — PASS
  (`board contracts verified`) against only this repository and its registered
  delivery project.
- Source status immediately before this report remained clean. This review adds
  only this round-81 pressure record and makes no implementation, test, or
  commit change.

## Final status

`BLOCK_REPLAN` for exact clean planning head
`d5c9a42416d4b83173a21773cd7f13bdb47b0ac9`. `OP-R80-001` is removed,
`OP-R79-001` and `OP-R78-001` through `OP-R78-006` remain removed, no new
external-ledger semantic blocker exists, and round 70 block 1 is contractually
implementation-ready. Authorization remains withheld only because the
required once-only named standalone full gate exceeded the separate planned-
command census ceiling even though the later bootstrap full-suite phase passed.
Make the cold/default gate deterministic without weakening its assertions,
then return the exact clean head for fresh independent review. Round 70 blocks
2 and 3 remain unauthorized.
