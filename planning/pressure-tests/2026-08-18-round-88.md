# Pressure test — round 88 successor trusted-digest repair review

**Disposition:** `BLOCK_REPLAN`

**Reviewed candidate head:** `b8e6f0df0894b355348a1dcbfe9d24536602fcdc`

**Reviewed parent:** `b3397845b0ceb569ecf07ce29ef1b9b162f3d67a`

This was a cold independent local removal review of the exact initially clean
head above on `iss-002-versioned-contracts`. It is bounded to removal of
`OP-R87-001` and `OP-R87-002`. It grants no merge, release, promotion,
installation, runtime mutation, live capability, or self-certification
authority.

The review began with `AGENTS.md`, `docs/planning/work-model.md`, the sole
decision authority in `docs/architecture/contract-decisions.md`, its
incorporated normative external-bootstrap schema ledger in
`docs/architecture/supervisor-contract.md`,
`docs/contracts/compatibility.md`, ISS-002, ISS-020, ISS-022, and rounds 86–87.
The exact parent-to-candidate diff was then read cold. No Chase Sets repository,
issue, project, board, backlog, or consumer system was accessed. The board gate
read only this repository and its registered delivery project.

## Semantic finding removal

Both round-87 substitution paths are closed by the implementation.

For `OP-R87-001`, the private closed review expectation now contains
`candidateDigest`, validates it as SHA-256, and requires both the nested
independent-review candidate and the trusted expected candidate to equal the
candidate recomputed from the supplied core. The authenticated expectation is
therefore no longer replaceable by a merely self-consistent candidate/core
pair.

The original legal coordinated reconstruction was repeated independently. It
changed only the selected retirement proposal's canonical `proposedAt` from
`2026-08-18T12:00:00.000Z` to `2026-08-18T12:00:01.000Z`, then rebuilt the
proposal receipt, selected tip, core prior-retired fields, and nested candidate
digest while retaining the original trusted review expectation. The candidate
changed from
`0a7a2788c7882442a98e7cef4085a3ede2ea7c1114bd94715d12b8bd76abd4dc`
to
`1b8bbdf4d87975be49a57c2a5616929ef8e0f28f92ab5357e5ce11316060cf5e`.
The repaired validator refused solely with
`expected.candidateDigest:mismatch`. The original valid core returned `[]`.

For `OP-R87-002`, the private closed post expectation now contains
`reviewCoreDigest`, validates it as SHA-256, requires the recomputed `Dsrc` and
the receipt's `reviewCoreDigest` to equal that trusted digest, and supplies the
trusted digest—not a caller-selected recomputation—as the ordinary
`ACTIVATE_SUCCESSOR` owner transition evidence. Thus both the post receipt and
the selected owner transition are pinned to the externally trusted reviewed
core.

The original full-chain substitution was also repeated independently. It
changed the supplied core's nested `reviewReceiptDigest` to 64 `f` characters,
recomputed `Dsrc`, and rebuilt the complete successor ACTIVE value, proposal,
derived mutation ID, proposal receipt, tip, and post receipt while retaining
the original trusted post expectation. `Dsrc` changed from
`1e8ef93f6121e90eb2557005ef9a6add3c4a8f688b2bbac6463f5f0e8916d651`
to
`20e48685fde07ee8dadbd98f9597062a4ba422cc4108f05fdd36c83ddcb75aa0`.
The repaired validator refused with `expected.reviewCoreDigest:mismatch` and
`ownerMutation:transitionEvidenceDigest:mismatch`. The original valid post
composition returned `[]`.

## Blocking finding

### `OP-R88-001` — the trusted candidate equality has no discriminating repository regression

Round 87 required both an independent expected-candidate mutant and the legal
coordinated prior-proposal/tip/core rebuild above. The candidate adds neither
to `test/contracts/successor.test.ts`.

The only coordinated candidate substitution test already existed in the
parent. It changes `successorAuthority.reviewedInstallerDigest`, recomputes the
candidate, and retains the original trusted expectation. That case refuses
independently on `successorAuthority:expected-mismatch`; it therefore remains
green even if the new `expected.candidateDigest` equality is deleted. Merely
adding `candidateDigest` to the valid fixture closes the expectation census but
does not exercise its authority meaning.

This leaves a confirmed authority-contract repair without executable deletion
evidence, contrary to the repository rule that public contract behavior and
authority composition have executable compatibility tests and to the exact
round-87 repair condition. A later refactor could silently remove the trusted
candidate comparison while all current repository tests remain green.

The smallest repair is test-only: add one mutation of
`reviewExpected.candidateDigest` against the unchanged valid graph and one
coordinated legal timestamp/proposal/tip/core reconstruction retaining the
unchanged trusted expectation. Both must return non-empty issues; the latter
should isolate `expected.candidateDigest:mismatch`. No implementation, public
schema, persisted record, digest formula, path, coordinator, or capability
change is needed. The full core/owner/post regression added for `OP-R87-002` is
discriminating and needs no expansion.

## Exact delta and retained contract surface

The exact `b339784..b8e6f0d` delta has three paths, 290 insertions, and three
deletions:

- `packages/contracts/src/successor.ts`: the two trusted expectation members
  and their equality/transition-evidence use;
- `test/contracts/successor.test.ts`: valid fixture updates and the complete
  `Dsrc`/owner/post substitution regression; and
- `planning/pressure-tests/2026-08-18-round-87.md`: the incorporated 234-line
  blocking review record.

`git diff --check` passes. The two new members are confined to non-persisted
closed expectations. None of the five public successor schemas, registry rows,
exports, version admission rules, framed digest domains/parts, persistence
paths, or exclusion edges changed.

All five public records still parse through their specialized closed parsers
and public dispatch:

- `destination-owner-prior-installation/v1`;
- `destination-owner-successor-authority/v1`;
- `destination-owner-independent-review/v1`;
- `state-mutation-destination-owner-successor-review-core/v1`; and
- `state-mutation-destination-owner-successor-review-post-selection-receipt/v1`.

The candidate, core, and post goldens remain respectively
`0a7a2788...abd4dc`, `1e8ef93f...d651`, and `e1c3c259...6f1b`. The two canonical
paths still match the ledger. The core still excludes successor anchor/owner
and post-selection fields; the candidate still excludes independent review and
downstream selection; and the post remains downstream of the core and selected
owner graph. The acyclic candidate→core→anchor/owner→post split is unchanged.
Exact current-version equality remains accepted while missing, legacy,
unknown, malformed, and future versions refuse. The focused suite retains the
non-resurrection checks for deleted diagnostics, coordinators, inventories,
retention/compaction, migrations, v2/v3, and superseded symbols.

## Bounded proportionality

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Trusted expected candidate digest and equality | A caller replaces the exact reviewed candidate/core preimage while reusing an opaque review receipt. | The independent legal reconstruction proves deleting the equality admits the changed candidate; the repository currently lacks this deletion test. | Retain the implemented private expectation member/equality and add the two bounded regressions; no review record or coordinator is needed. |
| Trusted expected `Dsrc` used by post and owner transition | A caller rebuilds a complete successor owner/post graph around an unreviewed core. | The committed full-chain regression becomes admissible if the trusted equality or trusted transition-evidence use is deleted. | Retain the current private expectation member and two equalities; no anchor implementation or new record is needed. |
| Existing five parsers, digest framing, and acyclic split | Open or cyclic records alter public bytes or make upstream review depend on downstream selection. | Existing schema, golden, path, exclusion, and compatibility mutants fail when their matching boundary is weakened. | Retain the bounded existing public surface unchanged. |

Nothing supports a sixth public record, new persisted member, coordinator,
proof tree, inventory, migration, candidate verdict, anchor shortcut, or live
capability. The missing evidence is exactly two small tests around the already
correct private equality.

## Verification evidence

The required repository gates ran sequentially in the requested order:

1. `pnpm run planning:check` — PASS, `planning contracts verified`.
2. `pnpm run planning:board-check` — PASS, `board contracts verified`, against
   only this repository and its registered delivery project.
3. `pnpm run format:check` — PASS, all matched files use Prettier style.
4. `pnpm run typecheck` — PASS.
5. Standalone `pnpm test`, once only — PASS, 24 files and 280 tests in 111.81
   seconds.
6. Separate `pnpm run verify:bootstrap`, once only — PASS: manifests and
   registries, formatting, typecheck, 24 files/280 tests in 111.09 seconds,
   private compositions, planning contracts, and source-tree status
   invariance.
7. Focused `pnpm --filter @orchestration-platform/contracts test` — PASS, 14
   files and 83 tests.
8. Focused `pnpm run contracts:compatibility-check` — PASS, one file and one
   test.

The first sandboxed planning invocation stalled before the repository script
emitted output because the sandbox blocked pnpm's signed-release/spawn
preflight. The reviewer-owned process was terminated, and the identical named
read-only command ran once through the normal approved path and passed. All
remaining gates used that approved read-only path. There was no assertion
failure, repository-test retry, isolated failing-test rerun, timeout override,
implementation edit, or test edit.

The two independent reconstructions used an in-memory esbuild bundle and the
candidate's own canonical constructors. No temporary or source file was
created. They reproduced the exact round-87 digest changes and issue arrays
reported above.

Immediately before writing this report, the branch and both full SHAs still
matched, the source worktree was clean, and the exact candidate diff passed
`git diff --check`. This review adds only this uncommitted round-88 record.

## Final status

`BLOCK_REPLAN` for exact candidate head
`b8e6f0df0894b355348a1dcbfe9d24536602fcdc`. The implementation closes
`OP-R87-001` and `OP-R87-002`, and the committed regression closes the
`OP-R87-002` deletion test, but `OP-R88-001` leaves the trusted candidate
equality without the explicitly required discriminating executable evidence.
After that bounded test-only repair, the repaired exact head requires a fresh
ordinary independent review. Green gates do not override the missing authority
regression and grant no merge, release, promotion, installation, runtime
mutation, live capability, or self-certification authority.
