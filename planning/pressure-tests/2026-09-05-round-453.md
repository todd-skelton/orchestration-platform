# Pressure round 453 — ISS-011 sole-generator and refusal-category census

Status: ISS-011 evidence-only artifact, absolute attempt 2 after the read-only
audit 1, at protected base `a41b0b4d1a83cf1b971e0f759bda8d00d0b328a6`.
This is neither source completion, review, hosted execution evidence, registry
admission, authority, nor ISS-011 closure.

## Lineage, decision, and scope

Rounds 401 and 434-450 are cumulative lineage: Round 401; 434; abandoned 435
with heads `1c8cc9f6…` and `fa0e0d7c…`; 436; abandoned 437 with heads
`225197e4…` and `2aecb4c6…`, independent PASS and hosted failure `33583110269`
retained; 438; 439; 443; 444; 446; 448 and PR #266; and 450 and PR #270.
Decision #275 records the frontier gate; this slice proceeds under every option
there and closes neither item 7 nor ISS-011.

This exact two-file source attempt sharpens only the existing temp-root child
process seam for the ISS-000 placeholder surface that ISS-011 will replace.
Vector A isolates refusal inputs and exact messages; B censuses the only emitted
registry; C proves refusal state preservation and correct-stub repetition; D
proves cwd independence; and E walks the real repository source roots for the
sole-generator census. No generator, manifest, module source, fixture, package,
script, adapter, documentation, draft, workflow, runtime, or authority surface
changes.

R1: the placeholder surface is ISS-000-owned while ISS-011 is the replacement
owner. This hardens the replacement invariant and closes neither ISS-000 item 7
nor ISS-011. R2: the `owns non-empty module rows` guard is unreachable because
the preceding byte-equality gate admits only `"[]\n"`; the test is renamed for
that byte rule rather than claiming row-level refusal. R3: the ABI requirement
to refuse extra or partial ignored output diverges from the placeholder, which
does not enumerate `.generated` and cannot clean a stale killed-run PID temp
file; this known divergence is recorded, not asserted.

## Acceptance evidence

The two positive controls retain `"[]\n"` and `"[]\r\n"` success. Every refusal
asserts status `1`, empty stdout, and the complete error message for its input
class:

- missing, lone-CR, and mixed-ending manifests:
  `CAPABILITY_NOT_IMPLEMENTED: ISS-011 owns malformed module manifests`
- no-final-LF, BOM, whitespace-edited, extra-LF, object, and non-empty manifests:
  `CAPABILITY_NOT_IMPLEMENTED: ISS-011 owns edited module manifests`
- an extra generator argument:
  `CAPABILITY_NOT_IMPLEMENTED: ISS-011 does not accept module arguments`
- a forged pre-existing stub:
  `CAPABILITY_NOT_IMPLEMENTED: ISS-011 refuses an edited generated stub`

After success, `.generated` is exactly `registry.ts`; its 69 bytes equal
`export const moduleRegistry = Object.freeze([]) as readonly never[];\n` and
hash to `ec5fd221aaf493285cd3a81e35cc7e46ab9c99d589741093471035586ae66b40`.
The temporary-root recursive `modules/` census is exactly
`build/generate-registry.mjs`, `manifest.json`, and `.generated/registry.ts`,
with no temporary sibling. Refusal after a correct seeded stub leaves its bytes
unchanged; manifest and argv refusals before publication do not create
`.generated`; a forged stub remains forged; a correct seeded stub repeats
byte-identically. An unrelated cwd stays empty. The real-root extension-filtered
walk has a nonzero visited count and finds exactly
`adapters/self/build/composition.ts` and `modules/build/generate-registry.mjs`
referencing `modules/.generated`.

Discriminating deletions remain: deleting the argv guard turns A-argv red;
deleting the byte gate turns edited-manifest rows red; deleting tracked-text
normalization turns lone-CR and mixed-ending rows red; deleting stub comparison
turns forged-stub evidence red; and deleting `wx`/rename publication turns B/C
red.

## Non-goals, verification, and review attack surface

Non-goals are manifest rows, registry activation, row grammar, source census,
module artifacts or packaging hashes, delivery/repair semantics, release work,
public-contract change, ISS-039 payloads, module script wiring, generator source
change, runtime or authority activation, and ISS-011 closure. Tests grant no
authority.

Author verification is local gates only: `pnpm typecheck`, `pnpm format:check`,
`pnpm planning:check`, `pnpm planning:board-check`, `git diff --check`, and
exact-head two-file inspection. No local Vitest, module, native, compiler,
addon, probe, provider, or lock experiment is run. Hosted Ubuntu, macOS, and
Windows are the sole executable evidence, including repeated three-OS output
claims. Independent exact-head review by another lane remains required; there is
no self-certification.

Review attacks category/message collapse, status/stdout omissions, accidental
real-checkout mutation, output-census gaps, vacuous source traversal, confusing
the unreachable guard for row validation, asserting R3 divergence as compliant,
placeholder evidence relabeled as closure, omitted lineage, and any footprint
growth or authority claim.

## Footprint and attempt ledger

The predicted and actual byte-stable footprint is exactly:

- `test/module-registry-placeholder.test.ts`
- `planning/pressure-tests/2026-09-05-round-453.md`

Audit 1 is complete. Source 2 is this artifact. Independent review 3 is pending
from a different lane. A BLOCK permits only the defined bounded two-file repair
as attempt 4 and re-review as attempt 5; footprint growth or a second BLOCK class
parks the slice. Attempts 6-8 remain the ordinary ceiling.

Routing is model-routing row 2, gpt-5.6-terra medium, placement provisional,
with budget intent at most $10, actual spend unknown, and no new ceiling. This
lane transcript is the routing record.
