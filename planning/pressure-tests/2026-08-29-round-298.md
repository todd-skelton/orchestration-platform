# ISS-022 registry/contracts pressure review — round 298

## Disposition

`PASS`.

This was one bounded cold review of the uncommitted 178-insertion/4-deletion
slice on `codex/iss-022-registry-observation` at exact base
`4b91de2e97ac78013e85e49fd0f1de530cbf5bab`. No blocking authority gap was
found in this slice.

## Pressure result

- `UNKNOWN` is admitted only as generic non-PASS job evidence. The ISS-002
  handler/observation contracts remain closed to `PASS|FAIL|UNSUPPORTED`, the
  generic aggregate still requires every receipt and its result to be `PASS`,
  and hosted artifact admission likewise rejects non-PASS. The focused UNKNOWN
  aggregate mutant refuses.
- The runner catalog adds only `ISS022_PORTABLE_PRIMITIVES`. The exact ISS-022
  constructor/parser binds one `iss022-portable-primitives` suite, the portable-
  primitives owner, REQUIRED custody/helper, NONE walk, and its recomputed exact
  vector-census digest. Alternate token, owner, digest, requirement, walk, or
  cross-suite census refuses.
- The constructor fixes exactly three jobs in semantic
  `LINUX, MACOS, WINDOWS` order with their exact IDs. The exact parser compares
  canonical bytes to that constructor after generic validation, so generic
  sorted-ID policy cannot replace OS authority. Missing, extra, duplicate,
  renamed, reordered, OS-substituted, and extra-suite mutants refuse.
- The diff touches only the three conformance source files and two focused test
  files named for this slice. It adds no hosted plan/workflow, root command,
  observation artifact, helper, or promotion path.

Focused evidence passed:

`pnpm exec vitest run test/conformance/contracts.test.ts test/conformance/iss022-suite.test.ts --reporter verbose`
(2 files, 13 tests),
`pnpm exec tsc --noEmit -p packages/conformance/tsconfig.json`, and
`git diff --check`.

## Proportionality

The two-value catalog extension, one exact constructor/parser pair, the
non-PASS type extension, and focused mutants are the smallest credible shape.
Deleting exact canonical equality reopens sorted-order and cross-suite
substitution; broadening into hosted execution or observation would exceed the
reported authority boundary. No further mechanism or review round is
justified.
