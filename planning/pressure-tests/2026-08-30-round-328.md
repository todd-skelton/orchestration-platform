# ISS-003 concrete loader repair follow-up — round 328

`PASS` for exact code delta
`39525b9429af422203aa8184d8eb0e761fa8ca5e..b97d316ff442f8f0f1df5c69fa412d1c7afce854`,
with authority draft `planning/drafts/ISS-003.md` SHA-256
`25BC4736B4BAD7C800E58AFF5BE54AFCC8C35180230EF679BCBCC4CEFD24E763`.

This is the single bounded follow-up authorized by Round 327's three findings.
Round 327 remains historical `BLOCK_REPLAN`; this review covers their closure
and repair regressions, not a new CLI/bootstrap or promotion authority grant.
The draft is unchanged, and the code delta contains only `loader.ts` and its
tests: 189 additions/15 deletions.

## Closure

1. `packages/config/src/loader.ts:206-209,655-657` now compares the closed OS
   literals without coercion. Malformed factory scalars return a loader with
   the fixed `INTERNAL_ERROR` row; malformed invocation scalars resolve to
   `ARGV_REFUSED`. The added mutant matrix includes throwing conversion hooks,
   null-prototype values, symbols, BigInt and revoked proxies, with zero canary
   calls (`test/config/loader.test.ts:107-159`). Construction still snapshots
   the exact callback; no new caller-code execution or rejection path is added.

2. `loader.ts:815-817` completes every adapter observation before the final
   source check. `:575-597` opens a fresh source handle, binds both retained and
   fresh identities to the captured exact Node coordinates, hashes both from
   offset zero against the originally parsed bytes, rechecks both identities,
   then checks pathname identity and physical path. No adapter callback follows
   those checks. Missing/replaced/unreadable source or changed bytes resolve to
   the fixed `PATH_REFUSED` row; fresh and retained handles remain private and
   are closed. The old pre-census digest check is removed rather than retained
   as redundant machinery. The new tests discriminate same-inode writes at the
   source observation and final root observation, plus final replacement and
   removal (`loader.test.ts:196-257`). The unchanged-source success control
   still passes. This closes the observed window without claiming an atomic
   guarantee against mutation after the operation returns.

3. The blanket environment-path check is deleted. Closed environment scalar
   validation remains; project/config/state paths are admitted only after their
   existing precedence selection (`loader.ts:731-752,770-779`). Each of the
   three shadowed relative environment values now permits valid CLI success;
   removing its overriding flag restores `PATH_REFUSED`, and a wrong-typed
   shadowed value still gives `CONFIG_REFUSED`
   (`loader.test.ts:161-194`). No OS-default or discovery rule changed.

## Verification and proportionality

Local independent in-memory imports of exact source on Node 24.15.0 passed
six factory and six invocation scalar mutants with zero canary calls, one
unchanged-source control, nine selected/shadowed/malformed environment checks,
and all four final-observation source mutants with exact `PATH_REFUSED` rows.
Temporary fixtures were removed; no source was transformed on disk, native
build invoked, full suite run, implementation edited or board accessed.

[Hosted run 33330467124](https://github.com/todd-skelton/orchestration-platform/actions/runs/33330467124)
records exact head `b97d316ff442f8f0f1df5c69fa412d1c7afce854` and all three
requested jobs successful: Windows `99308007482` has 82 files, 780 passed/5
skipped; Ubuntu `99308007368` and macOS `99308007483` each have 82 files, 773
passed/12 skipped. Loader coverage is 12/17 executed on Windows and 9/17 on
each POSIX host. Every log reports no source-tree status changes. Code/draft
diff checks pass locally; this hosted run is code evidence, not evidence that
the uncommitted draft was executed.

Deleting coercion and blanket path admission is the smallest repair for their
named threats. Moving the existing byte binding after callbacks and adding one
fresh read closes stale-source acceptance; removing that final check restores
the reproduced mutant. A broker, lock service, new adapter seam or authority
surface would be larger and is unnecessary. Native observation, timeout,
public exports, ISS-000 setup/census and all other scaffold are unchanged.
This follow-up is materially shorter than the repair delta; no remaining
blocker or repair regression was found within this authorized review.
