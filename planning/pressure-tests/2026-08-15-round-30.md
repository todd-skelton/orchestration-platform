# Pressure test — round 30

**Disposition:** `BLOCK_REPLAN`

The thirtieth independent review found one pre-activation decision race. The
39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R30-001` — Abort could win after a fresh fence-backed launch had started but before active-release CAS, leaving that noncomplete pointer without an abort terminal path. | Added cleanup-gate `ACTIVATING` as the forward-only decision point. Activation and abort race only by `PENDING` CAS; if abort wins, either launch source is denied and reaches source-specific `TERMINAL_ABORTED` archive/pointer removal before broker cleanup. If `ACTIVATING` wins, abort refuses even while active release remains N. |

## Re-review focus

- Race `PENDING` → `ABORTING` against `PENDING` → `ACTIVATING`; exactly one CAS
  must win and no read-then-write active-record race may authorize both paths.
- Kill a fence-backed pre-activation chain at READY, LIVE before/after recovery
  attachment, channel denial, `TERMINAL_ABORTED`, child exit, archive, and pointer
  removal.
- Crash after `ACTIVATING` but before active-release CAS; only forward recovery
  may resume, and abort must refuse.
