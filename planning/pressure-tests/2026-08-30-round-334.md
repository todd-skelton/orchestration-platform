# ISS-022 review binding repair — round 334

`PASS`, independent bounded follow-up by `/root/iss022_review_projection_review`.
Head: `a23b70511301692b0b60ea5567a8d0a4fe5715cd`;
base: `8f3a4353c01cb81e87c7e3d119aea923e6b2905c`.

The two-file repair removes unconstructible caller bindings. Provider identities
derive from the authenticated canonical core; review, protection, byte identity,
pagination, and destination checks remain. Fixtures preserve substitution refusals.
Independent typecheck and diff check passed. Fixtures were inspected, not executed:
native-dependent setup remains unavailable; hosted three-OS tests are outstanding.

Proportionality: removing the duplicate input is smaller than adding a locator or
arguments. Authenticated core identity retains substitution protection. The later
terminal still owes protected stable-checkout and stable-subject verification.
