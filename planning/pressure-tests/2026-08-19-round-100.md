# Pressure test — round 100 successor-authority direct-binding review

**Disposition:** `PASS`

**Reviewed candidate head:** `455b91b921098342be140b0d286b98a506057054`

**Reviewed parent boundary:** `f3ef41d3d1e5654d5eae5704ee65cfe24582b6fd`

This is a fresh cold independent bounded review of the exact initially clean
head above on `iss-002-versioned-contracts`. It covers removal of `OP-R99-001`
and preservation of the already-closed E0 genesis/successor provenance,
readback, and totality properties from rounds 97–99. It does not claim complete
E0 acceptance: the authority documents do not yet close the selected
active-release tuple, so known `OP-R97-002` remains explicitly unauthorized.

This review grants no merge, bootstrap execution, installation, release,
promotion, runtime mutation, consumption, teardown, receipt production,
self-certification, or live capability. No Chase Sets repository, issue,
project, board, backlog, consumer, or runtime was accessed. The board gate read
only this repository and its registered platform delivery project.

## Disposition summary

| Item | Round-100 result | Evidence at this exact head |
| --- | --- | --- |
| Nested `Dsrc.successorAuthority` to selected anchor | **Removed** | The seven anchor-owned members are compared directly to the parsed selected anchor. Independent mutants for every member and one coordinated nine-member reconstruction all refused through the named direct comparisons. |
| Nested `Dsrc.successorAuthority` to reviewed operation | **Removed** | Release manifest and subject are compared directly to the parsed reviewed operation. That operation is independently required to be the exact anchor-bound `BOOTSTRAP_INSTALL` arm and is composed into `Dsc`. |
| Prior RETIRED provenance, teardown, `Dsrc`, successor owner ACTIVE, and `Dsrp` | **Preserved** | The production delta deletes none of the round-99 composition. Foreign prior lineage, actual-versus-nested `Dsrp`, current observation, whole persisted receipt readback, and coordinated internal readbacks all refused. |
| Genesis/successor branch totality | **Preserved** | Valid genesis and successor controls returned `[]`; all eight one-null successor cases and a non-null genesis provenance case refused. Hostile proxies in every successor-provenance position and the newly read nested successor authority refused without throwing. |
| `OP-R97-002` selected active release | **Still unresolved and unauthorized** | The unchanged coordinated foreign active-release `Dp/Dt/Dv/Dr` tuple plus matching expectation and recomputed E0 value digest still returned `[]`. No contract was invented for it. |
| Fresh definite blocker in the reviewed claim | **None found** | Source inspection, deletion attacks, mechanical gates, focused contracts, and compatibility all agree. |

The `PASS` is therefore bounded to the round-100 claim. It must not be read as
ISS-002 completion or as authority to admit E0 while `OP-R97-002` remains open.

## `OP-R99-001` is removed

The successor branch first parses the actual closed `Dsrc` and actual selected
anchor. It then extracts the parsed nested
`destination-owner-successor-authority/v1` record and equal-binds every one of
its non-schema members to an independently selected fact:

| `successorAuthority` member | Independently selected source |
| --- | --- |
| `bootstrapGrantDigest` | selected anchor |
| `bootstrapTransactionId` | selected anchor |
| `globalBootstrapIdentityDigest` | selected anchor |
| `installationId` | selected anchor |
| `projectId` | selected anchor |
| `reviewedInstallerDigest` | selected anchor |
| `reviewedReleaseManifestDigest` | parsed reviewed `BOOTSTRAP_INSTALL` operation |
| `reviewedReleaseSubjectDigest` | parsed reviewed `BOOTSTRAP_INSTALL` operation |
| `stateRootDigest` | selected anchor |

The remaining `schemaVersion` member is already fixed by the closed nested
parser. Thus the literal census is total; no caller expectation remains as the
sole authority for any nested member.

The operation-side facts are not merely another digest-only caller sibling.
The same validator parses the closed reviewed-operation union, recomputes
`Dsc` against that exact operation, and independently requires the operation
to be `BOOTSTRAP_INSTALL` with transaction, grant, reviewed installer, and
independent-review receipt equal to the selected anchor. `Dsc` in turn binds
the operation digest and reviewed manifest/subject/installed bytes. The two new
release equalities therefore connect `Dsrc` to the exact operation already
used by the E0 genesis composition.

The in-memory deletion attack changed each of the nine members independently,
rebuilt the review candidate, and moved the exact caller review expectation
with it. Every case refused with its corresponding
`successorAuthority.<member>:mismatch`. A coordinated reconstruction changed
all nine members together and produced all nine direct mismatches. Rehashing
additional downstream siblings cannot erase these comparisons because their
selected anchor and reviewed-operation sides are parsed independent inputs.
The committed mutant preserves the bootstrap-grant deletion edge; the
independent review probe exercised the complete nine-member matrix without
writing a probe file.

## Previously closed E0 properties remain closed

The exact production delta is additive inside the already-selected successor
arm. It removes or relaxes no parser, nullability branch, provenance validator,
readback derivation, or equality from the parent.

Independent probes against the exact checked-out sources and committed fixture
established:

- valid genesis and fully constructed valid successor controls both returned
  `[]`;
- each of the eight required successor-provenance inputs set to null refused
  with `successorProvenance:successor-required`;
- supplying successor provenance to the genesis branch refused with
  `successorProvenance:genesis-forbidden`;
- hostile `ownKeys` proxies in all eight provenance positions and in nested
  `Dsrc.successorAuthority` returned findings and did not throw;
- coordinated proposal/value/tip readback substitution in `Dsrp`, its caller
  expectation, nested intent, digest, and whole readback still refused through
  all three derived-readback comparisons;
- changing the actual `Dsrp` still mismatched both the nested intent record and
  its derived persisted whole-record readback;
- changing only the whole-record readback still refused;
- a current owner-observation substitution still refused through the direct
  `ownerProposal.observationDigest` comparison; and
- a foreign prior RETIRED owner tip still refused through the prior-lineage and
  successor compositions.

These results preserve the round-99 prior RETIRED selection and teardown
archive, recomputed `Dsrc`, selected successor owner graph, current `Dobs`,
actual/nested/whole `Dsrp`, internal readbacks, branch nullability, and hostile-
input totality properties.

## Known unauthorized boundary

`OP-R97-002` is unchanged. The E0 authority value's active-release
`Dp/Dt/Dv/Dr` still compares only to the four digest members in the caller
expectation. The independent probe moved all four authority and expectation
members together and recomputed the E0 authority-value digest; the public
validator returned `[]`.

No authority reviewed here defines the missing selected active-release record,
pointer graph, installed-byte proof, or its composition into this E0 boundary.
This review therefore neither invents that contract nor treats a digest-only
expectation as authority. The tuple remains unusable for E0 admission until an
authorized slice closes it.

## Proportionality and exact delta

The concrete in-scope threat was a fully self-consistent caller reconstruction
of `Dsrc.successorAuthority` that differed from the selected successor anchor
and reviewed bootstrap operation. The deletion test moves the nested record and
caller expectation together; removing any retained direct comparison admits
that member. The smallest credible repair is exactly the direct equalities now
present. A new schema, digest, path, validator, lifecycle state, receipt,
coordinator, or duplicate successor machine would be larger and is unnecessary.

The exact `f3ef41d..455b91b` delta has three paths and 290 insertions:

- `packages/contracts/src/genesis.ts`: 38 insertions;
- `test/contracts/genesis.test.ts`: 28 insertions; and
- `planning/pressure-tests/2026-08-19-round-99.md`: 224 insertions.

The production change is only the nine direct comparisons and local parsed
nested-record binding. The test change is one coordinated caller-expectation
mutant asserting the new bootstrap-grant mismatch. The pressure record is the
committed parent review. No schema census, framing domain, persistence path,
registry, lifecycle, compatibility surface, runtime capability, consumer,
adapter, or authority document changed. `git diff --check
f3ef41d3d1e5654d5eae5704ee65cfe24582b6fd..455b91b921098342be140b0d286b98a506057054`
passes.

## Verification evidence

The mandated gates ran strictly sequentially. The standalone full test exited
before the separate bootstrap verifier began; the focused slots began only
after bootstrap verification completed. Increased committed timeouts were left
unchanged.

1. Planning check — PASS, `planning contracts verified`, through
   `node scripts/planning/check.mjs`. The `pnpm run` wrapper remained silent for
   two minutes and was stopped before the direct literal checker ran.
2. Board check — PASS, `board contracts verified`. The sandboxed read-only run
   failed closed at the GitHub CLI subprocess with `spawn EPERM`; the identical
   approved read-only rerun passed.
3. Format check — PASS, all matched files use Prettier style. A silent `pnpm`
   wrapper was stopped before the direct checker; its sandboxed pinned-Prettier
   subprocess then failed closed with `spawn EPERM`, and the identical approved
   read-only rerun passed.
4. Typecheck — PASS with pinned TypeScript 7.0.2.
5. Standalone full test — PASS, 28 files and 305 tests in 173.07 seconds. The
   sandboxed run failed closed during Vite configuration with `spawn EPERM`;
   the identical approved read-only rerun passed.
6. Separate bootstrap verification — PASS: manifests and registries, format,
   typecheck, 28 files/305 tests in 97.53 seconds, private compositions,
   planning contracts, and source-tree status invariance. The sandboxed run
   failed closed at its first Git subprocess with `spawn EPERM`; the identical
   approved rerun passed.
7. Focused contracts — PASS, 18 files and 108 tests in 7.09 seconds. The
   sandboxed capability slot failed closed during Vite configuration with
   `spawn EPERM`; the identical approved read-only rerun passed.
8. Focused compatibility — PASS, one file and one test in 805 milliseconds.
   The sandboxed capability slot failed closed during Vite configuration with
   `spawn EPERM`; the identical approved read-only rerun passed.

The adversarial probes bundled the exact checked-out TypeScript and committed
fixtures entirely in memory. No probe, implementation, or test file was
written. Their sandboxed esbuild subprocess failed closed with `spawn EPERM`;
approved read-only runs then established the semantic results above.

## Final status

`PASS` for the bounded round-100 claim at exact candidate head
`455b91b921098342be140b0d286b98a506057054`. `OP-R99-001` is removed, and the
previously closed E0 genesis/successor provenance, observation, readback,
nullability, and totality properties remain closed. No fresh definite blocker
was found in this delta.

`OP-R97-002` remains unresolved and unauthorized, so this result is not full E0
acceptance, ISS-002 completion, merge authority, or runtime authority. Return
the selected active-release boundary only when exact architecture authority
defines its missing composition; do not infer it from this bounded pass.
