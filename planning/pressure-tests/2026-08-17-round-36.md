# Pressure test — round 36

**Disposition:** `BLOCK_REPLAN` (removal round; removals found and applied)

The thirty-sixth review was the packet revision's first removal round under the
amended `work-model.md` protocol. It examined every remaining state, schema,
receipt, proof, and issue against the declared topology and found ten
removals or simplifications; all were applied. Because a removal round is
clean only when nothing further is removable, a follow-up removal round is
required.

| Finding | Repair applied |
| --- | --- |
| `OP-R36-001` — Round 34 banner-deferred the superseded bodies of `supervisor-contract.md` and `state-machines.md` instead of deleting them; the drift was already live (the body's closed census said thirteen kinds and "the other twelve" while `ISS-004` said eleven; rolling-accumulator and sparse-projection sections survived). | The affected bodies rewritten to the amended decisions (twelve-kind census, chain walk, single-epoch rotation, attempt log); banners removed; `compatibility.md` rewritten to the deletion policy in the same pass. |
| `OP-R36-002` — `ISS-022`'s paginated authority-node directory enumeration (0/1/256/257/10k page boundaries, native cursor) was node-census residue contradicting "the chain walk alone proves set completeness". | Enumeration dimension deleted; `ISS-022` probes create-once/readback existence semantics at constructed canonical paths, including reliable absence observation for the head-plus-one and head-plus-two probes. |
| `OP-R36-003` — 104 of 170 registered blocked-by edges were transitively implied; the DAG was a transitive summary the work model forbids, inflating every fan-in gate (`ISS-019` 17 blockers, `ISS-033` 13, `ISS-026` 11). | `planning/roadmap.json`, all draft frontmatter, and all epic DAG blocks regenerated as the exact transitive reduction (37 issues reduced; `ISS-019` now 2, `ISS-027` chain now linear); `planning:check` gained a permanent transitively-implied-edge rejection; `work-model.md` records the reduction convention. |
| `OP-R36-004` — `rotation-verification-receipt/v1` (a round-34 addition) attested a permanently recomputable check every new-epoch run already performs under the lock. | Deleted. Rotation terminalization is the first fresh new-epoch run's lock-held walk plus its writing of the rotation run's ordinary terminal resolution (also closing `OP-R35-001`). |
| `OP-R36-005` — The `READY_ONLY` descriptor state was a weaker create-once copy of the CAS-selected reservation's own durable pre-launch record. | Deleted; the descriptor exists only as `LIVE` and binds the selected reservation. |
| `OP-R36-006` — The separate terminal-summary document duplicated its sole consumer, the `TERMINAL` attempt-log record, and left a crash window between the two writes. | Folded into the `TERMINAL` attempt-log record; "summary as launch authority" is now structurally impossible rather than a checked rule. |
| `OP-R36-007` — `ISS-035` and `ISS-038` each ran a full three-host physical reboot drill against the same closed contract, and `ISS-035`'s disposition restated `ISS-031`'s. | `ISS-035` proves Ed25519 custody and scheduler-receipt feasibility without a reboot and references the `ISS-031` disposition; the single physical reboot drill lives in `ISS-038`. Command renamed to `probe:host-custody`. |
| `OP-R36-008` — "Coordinated recomputed substitutions for every field refuse" was a 2^N open set in three issues. | Bounded: `ISS-014` AC4 seven enumerated mutants; `ISS-032` AC2 one per census field plus one coordinated; `ISS-002` AC6 one per bound field plus one all-field mutant per matrix. |
| `OP-R36-009` — The 1000-record chain benchmark had three owners and one undefined "published time budget"; `ISS-030` used a fourth vague figure. | `ISS-006` owns the golden and publishes the budget; `ISS-002/004/030` reference it. |
| `OP-R36-010` — `ISS-027`'s declared blockers were transitively implied by its own serial tail. | Subsumed by `OP-R36-003`; `ISS-027` now declares `[ISS-034]` only. |

## Examined and retained

Destination-owner/anchor CAS (the one real multi-writer boundary); the
recovery-authorization attachment as a separate selected pointer (distinct
producer and authority); the reservation itself (retry idempotence across
crash); `LOST_CONFLICT` classification (crash-recovery, not concurrency,
machinery); `ISS-039/040/041` (thin, bounded evidence); `ISS-037` separate
from `ISS-027` (distinct identities and credentials); the remaining reboot
drills (`ISS-031` synthetic cold-session, `ISS-030` supervisor re-entry,
`ISS-034` production binding — each proves a distinct property on a distinct
artifact); closed-record snapshots, fail-closed `UNKNOWN`, tombstoned removal,
epoch fencing, and stable-predecessor promotion.

## Re-review focus

- The follow-up removal round must find nothing further removable for the
  packet to reach `PASS`.
- Verify the reduced registration graph leaves every issue runnable at the
  same point as before (transitive closure unchanged) and that the checker
  rejects a reintroduced redundant edge.
