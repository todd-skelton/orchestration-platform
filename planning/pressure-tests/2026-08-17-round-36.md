# Pressure test — round 36

**Disposition:** `READY_FOR_INDEPENDENT_REVIEW`

This author-side follow-up establishes `PASS_REPLAN` readiness; it is not an
independent review receipt and does not certify the amendment.

## Deterministic evidence

- The 39 pre-existing issues have exactly 544 dependency-closure pairs before
  and after reduction: 544 retained, zero missing, zero added, zero cycles.
- Pre-existing direct edges fall from 164 to 62. The full branch has 66 direct
  edges after adding `ISS-041/042/043`; the planning checker rejects redundant
  edges. Lifecycle timing and external-authority semantics were considered as
  correctness gates before removal.
- PR49 retains `ISS-039` (blocked directly by `ISS-009,ISS-011`) and `ISS-040`
  (blocked directly by `ISS-011`) under `EPIC-MODULES`, both in Self-hosting
  release. This branch reserves their keys and does not duplicate their drafts
  or migration addendum.
- The original migration manifest still contains the exact 183-source snapshot.

## Required deletion test and smallest alternatives

| Mechanism                                                                                         | Delete/smaller-alternative result                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authority-history checkpointing                                                                   | Deleted from current planning; the O(n) chain plus measured `ISS-006` threshold is smaller. Un-park only after a supported OS exceeds five seconds at 1,000 records.                                |
| `ISS-041` walking skeleton                                                                        | Retained as a disposable fixture because deleting it leaves the contracts package without an early runtime consumer. It blocks nothing and its only durable output is review context for `ISS-026`. |
| `ISS-042` operator status                                                                         | Retained as one read-only projection because the smaller alternative is raw-journal inspection, which does not meet the observable-operator outcome. It is never authority and blocks nothing.      |
| `ISS-043` review calibration                                                                      | Retained as advisory evidence because deleting it leaves the assumed independent reviewer unmeasured. It does not grant authority or block `ISS-037`; the report is review context.                 |
| Separate rotation receipt, terminalization write, node census, coordinator, legacy diagnostic API | Deleted. Selected authority, exact chain record, old CAS-armed checkpoint, and live capability already provide the necessary evidence.                                                              |

An independent exact-head review must still verify the contract prose, issue
frontmatter, generated epic DAG mirrors, vocabulary scan, and checker outputs
before issuing `PASS_REPLAN`.

## Subsequent independent block and repair

The first independent review of the author-ready head returned `BLOCK_REPLAN`:
history still used placeholder fact groups, rotation borrowed ordinary commit
language without a closed evidence union, ISS-030 contradicted the shared
packet and reduced DAG, and ISS-002 understated the 58-file amendment. The
follow-up repair defines exact `Dop`/`Dsc`/`Dgb`/`Dgse`/`Drot`/`Dh` and downstream
GENESIS formulas, closes commit evidence over ORDINARY/AUTHORITY_ROTATION,
refuses rotation post-checkpoint-5 artifacts, corrects ISS-030, and records the
complete multi-issue footprint. This addendum is author repair evidence only;
the repaired exact head still requires new independent review.

## Exact-schema follow-up

The subsequent schema audit found that the repaired formulas still exposed
conceptual labels instead of executable JSON keysets. The follow-up makes the
supervisor ledger normative for literal versions, canonical member order,
types/nullability/enums, persistence, digest domains/parts, and exclusions. Its
closed field census is: reviewed operation 9 bootstrap or 15 promotion;
successor core 17; genesis bootstrap input 14; non-persisted genesis selection
38; rotation input 12; history GENESIS 7 or ROTATION 13; selected authority
value 22; and commit common 18 plus ORDINARY 4, rotation base 8, and exactly one
9-field RESUMABLE, 8-field SELECTED, or 1-field UNKNOWN addition. History record
bytes now unambiguously carry `authority-history-record/v1` while
`authority-history/v1` remains digest-domain-only. The adjacent public-v1 audit
also pins the existing global-identity, checkpoint-evidence, evidence-slot,
packet, and fixed unknown-evidence member names. This is author planning
evidence, not independent certification.
