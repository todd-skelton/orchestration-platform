# ISS-003 pure command emission — round 329

`PASS` for the bounded source review; no blocking finding. This is the single
pressure round for this slice, not CLI activation, release certification or
promotion authority. Hosted full three-OS execution evidence remains pending.

## Exact reviewed coordinates

Worktree: `C:\Users\ToddS\.codex\visualizations\2026\08\18\01a012b8-313d-7470-aee6-f69ee2363a52\iss003-command-protocol`.
Base HEAD: `72c6bcfa648433ef9187c73903fabb62165e8f7b`.
Reviewed all seven tracked modifications and both untracked source/test files;
no staged delta. This record itself is the only reviewer-created file.
The slice is 482 additions/5 deletions, including the 179-line source and
274-line test. Exact on-disk SHA-256 values:

| File | SHA-256 |
| --- | --- |
| `packages/cli/src/command-result.ts` | `E72CF747F65706E59E1327479438B89CAB4D16F6EE3599536D83E09E6333EE43` |
| `test/cli/command-result.test.ts` | `6A75E9FC8B78FAB1E92FA2E6646B34905A3013337801C222888259B297A24D80` |
| `packages/cli/package.json` | `080E829E76904EE6606017D1DE42A259E77BDE1E239FBF23348086CB8D379721` |
| `pnpm-lock.yaml` | `5DA7E6D733428608C5C161446C9C4F9DA7C86EDD307A8A01B04A9EF24277CC8B` |
| `scripts/planning/check.mjs` | `AE5B4607D8B5D1EEA6FB546FBC8640F1A0050775F38DED754B1632D05D5A7805` |
| `scripts/verify/bootstrap-contracts.mjs` | `695C36AE37A5D3231AC610D5C645D75AE1F14C5B256FBB1652638E12E4420C00` |
| `test/bootstrap-contracts.test.ts` | `EE1EBE3F314CEE01677951C5E5A96F900B1F9FD685037693C2ADE8D39A93B0C7` |
| `test/planned-command-execution.test.ts` | `75F12EC25B2A91ED8D8C082569D49E8EB6EE5B8DFCF99446D098D20D8F577ED5` |
| `test/planning-contracts.test.ts` | `26B03E101679346D8D1CC96E54D759BCE7F614ACC8427FEE5F88E601021B4993` |

Authority draft `planning/drafts/ISS-003.md` is unchanged, SHA-256
`25BC4736B4BAD7C800E58AFF5BE54AFCC8C35180230EF679BCBCC4CEFD24E763`.
Read repository `AGENTS.md`, `docs/planning/work-model.md`, that draft, the
configuration contract parsers, canonical runtime, CLI exports and registries.
No consumer repository, consumer data, board or consumer-specific skill used.

## Boundary checks

- `command-result.ts:62-93` validates before returning a frozen emission;
  existing `contracts/src/runtime.ts:249-255` supplies canonical JSON with one
  LF and valid UTF-8 scalar strings. Stderr is the empty literal; six fixed
  failures bind exact diagnostics/outcomes to exits 2/3/5/70; argv refusal
  fixes the command to empty. No input-derived diagnostic or partial write.
- `command-result.ts:95-150` admits only the exact settled result/error union.
  Public snapshots reject accessors, proxies, revoked proxies, exotic records,
  cycles, symbols, malformed Unicode and non-JSON values before caller code
  could execute. The descriptor walk checks the original result's recursive
  frozen state, not merely the parser's frozen copy. Only the two registered
  config schemas can succeed; altered error members become `INTERNAL_ERROR`.
- `command-result.ts:158-178` passes the supplied registration owner to the
  existing envelope parser; it adds no owner registry. Every one of the 31
  placeholder rows matches its owner. Substituted owners, config placeholders
  and handler-supplied placeholder failures refuse. Tests at
  `test/cli/command-result.test.ts:129-162` also join the actual registrations.
- `packages/cli/package.json:17-20` implements only package-local tests and
  adds the explicit contracts dependency, mirrored in the lockfile. ISS-000
  census amendments agree at `scripts/planning/check.mjs:155`,
  `scripts/verify/bootstrap-contracts.mjs:732-734`, and
  `test/planned-command-execution.test.ts:59-63`; both census test suites add
  placeholder-reversion mutants. No remaining restatement was found stale.
- The CLI root export, live bin, static runner, command registrations, native
  setup and public contracts are unchanged. No handler invocation, raw argv,
  process mutation, filesystem operation or candidate authority is introduced.

## Independent evidence and proportionality

Direct Node 24.15.0 imports of exact source, using an in-memory resolution hook
for existing TypeScript imports, passed 121 checks: both frozen typed successes
(including non-ASCII UTF-8), six exact failures, all 31 placeholder owner rows
and their substitution controls, plus malformed nested/accessor/proxy/revoked,
cycle, symbol, scalar, shallow-frozen and canary inputs. Canary callbacks ran
zero times. Canonical envelopes, one LF, no BOM, empty stderr and exact exits
were checked. `git diff --check` passed and source/test hashes were rechecked.
The submitter reports CLI/planning/bootstrap-census 85/85, planned-command
execution 2/2, typecheck, formatting and planning checks green; those checks
were not independently rerun. No broad/full/bootstrap/native build was
run, and no compiled artifact or implementation edit was created.

| Mechanism | In-scope threat / deletion test | Smallest alternative and disposition |
| --- | --- | --- |
| Public parser plus canonical emission | Schema/owner drift or input fragments become output; remove validation | Reuse landed parsers rather than a second validator; keep one prepared emission |
| Fixed failure rows and command membership | Wrong exits, diagnostics or reflected command input; remove row/membership checks | Six literal rows plus existing census are smaller than caller-supplied diagnostics or another registry |
| Original-result frozen walk | A shallow-frozen result is silently normalized; delete the walk | Checking only the parser copy misses the demonstrated mutant; retain the bounded descriptor walk |
| Package test/census amendments | New test entrypoint is mistaken for a placeholder; revert one census | Update existing census rows and two reversion mutants together; no new scaffold mechanism |

This record is materially shorter than the reviewed slice. PASS is limited to
the pure internal emission and its test/scaffold amendment; it does not claim
that dispatcher execution, the eventual single stdout write, or hosted
three-OS acceptance has been demonstrated by this round.
