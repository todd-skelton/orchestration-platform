# Pressure round 436 — ISS-011 ambient-boundary replan

Status: definition-only fresh sub-slice after the declared Round 435 block.
Protected base is `6ba4dbe2c02ad47ec78075baabd0f16cf49e7643`.

## Failure boundary

Round 435 proved that a finite inventory of host globals cannot establish the
closed no-ambient contract. Adding the two observed Node 24 names would leave
the same class open when another runtime adds a global. A dedicated
`types: []` compilation was also rejected as the sole mechanism: the planning
module's public-contract dependency legitimately traverses Node-backed
contract implementations, so that configuration refuses existing required
dependencies before it discriminates the module body.

## Fresh source admission boundary

The next source candidate must bind the exact normalized tracked bytes of
`modules/planning/src/index.ts` in the walking-skeleton consumer before calling
the module. Its independently reviewed SHA-256 literal is the temporary
quarantine admission identity. The existing syntax-token inspection separately
admits exactly the public-contract static dependency and refuses exports from
other dependencies, import-equals, dynamic import, and every `require`
reference. Independent exact-head review evaluates the admitted source bytes
for ambient or effect access; the digest ensures that later source cannot
inherit that review without changing the expected identity and obtaining a new
review.

The denial matrix must include single-quoted static imports, static exports,
literal and computed dynamic imports, literal, computed, and aliased `require`,
and source mutations using `Date`, `Math.random`, `process`, `fetch`,
`navigator`, and `WebSocket`. Every such mutation changes the normalized source
identity even if a future host recognizes a new global. The scanner may retain
an advisory known-name finding, but that finding is not the closed admission
claim. When the four-row manifest activates, its already-defined normalized
source digest subsumes this temporary pin.

## Scope, non-goals, and deletion test

The fresh source slice may reuse the abandoned planning semantics only as a
declared Round 435 input. It may add the quarantined planning entrypoint, the
focused existing consumer vectors, and one new pressure record. It may not
change the manifest, generator, generated stub, root configuration, public
contracts, adapter behavior, runtime dispatch, or any authority surface.

Deleting the exact normalized-source pin reopens unreviewed ambient mutations.
Deleting the import inspection reopens dependency effects without an explicit
source-category refusal. Adding a parser, sandbox, VM, ambient-package shim,
new public entrypoint, or second module is larger than the observed failure and
forces another replan.

## Acceptance, verification, and review attack

Acceptance requires the complete Round 435 descriptor, result, substitution,
readiness, capability, canonical-order, determinism, actual-preparation-seam,
quarantine, and cross-OS normalized-generator vectors, plus the exact source
identity and expanded effect mutation matrix above. Local verification remains
typecheck, focused formatting, planning, and diff checks; Vitest and the full
bootstrap run remain hosted on all three operating systems.

Independent review should recompute the source digest from normalized tracked
bytes, inspect those exact bytes for effect access, mutate every import form and
both previously missed globals, verify the manifest/generator/stub census, and
reject any claim that a source digest alone proves semantics, installation, or
runtime authority. Predicted footprint is the planning entrypoint, focused
walking-skeleton consumer test, and one source pressure record. A fix outside
that footprint is fresh replan evidence.
