# Pressure test — round 27

**Disposition:** `BLOCK_REPLAN`

The twenty-seventh independent review found two executable recovery omissions.
The 39-issue packet remains unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R27-001` — Successor abort after cleanup-gate creation could neither prove whether a fence had been published nor resume the post-gate/pre-fence window. | Added the gate's `fencePublication` discriminator, strict before/after-publication revoke predicates, and an immutable `cleanup-gate-pre-fence/v1` predecessor continuation. It can narrowly consume only `PENDING` matching authority, publish the prepared fence after consumed read-back, or complete `ABORTING`; project authority always refuses. |
| `OP-R27-002` — N0 `ABORTING` referred to an exact bootstrap abort command that did not exist in the exhaustive CLI contract. | Added canonical idempotent `orchestration-bootstrap abort --request <file> --output <file>`, its request/receipt schemas, root wrapper placeholder, producer acceptance criteria, and operator invocation. |

## Re-review focus

- Kill successor promotion after gate creation before consume, after consume
  before fence publication, and on both sides of abort revocation; exactly one
  gate- or fence-bound continuation must remain reachable.
- Verify `ABORTING` never receives a recovery-admin client and broker-internal
  revocation uses fence presence/absence matching the gate discriminator.
- Run the exhaustive bootstrap command census and replay `abort` across every
  N0 pre-mutation boundary; post-mutation and moved-transaction inputs refuse.
