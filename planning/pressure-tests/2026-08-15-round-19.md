# Pressure test — round 19

**Disposition:** `BLOCK_REPLAN`

The nineteenth independent review found four implementability gaps in the new
pre-N0 path and repeated recovery semantics. The 39-issue packet remains
unregistered pending fresh review.

| Finding | Repair applied |
| --- | --- |
| `OP-R19-001` — A second predecessor crash deadlocked because the shim admitted only one recovery launch/channel for the transaction lifetime. | Added canonical `activation-recovery-launch/v1` with adjacent CAS generations, one live attempt at a time, terminal proof, exact binding retention, sequential fresh channels, and multi-crash tests at every step 13–15 boundary. |
| `OP-R19-002` — The reduced broker could not be composed through the production operation-registry factory without either missing-row refusal or an unintended downstream surface. | Added separate closed `createHostCustodyBootstrapBrokerService({ hostMethodManifest })`, an exhaustive three-method manifest, profile binding through every value, and cross-profile/downstream-operation refusal tests. |
| `OP-R19-003` — “Independent review” of the pre-N0 kit lacked an authenticated producer and verifier contract. | Defined two pinned workflows, exact landed-tree/PR-review identity rules, GitHub artifact attestation, distinct numeric reviewer constraints, a closed review receipt binding every kit/runtime asset, pinned GitHub CLI verification, authenticated API read-back, and negative controls. |
| `OP-R19-004` — The seven-command reduced lifecycle and its final-broker upgrade were named but not command/state/schema complete. | Published the exhaustive argv/actor/input/output/transition table, explicit retain/remove teardown outcomes, and CAS-bound final broker plan/install/verify schemas preserving the host-key reference/public key/generation without export. |

## Re-review focus

- Crash the predecessor repeatedly and confirm only adjacent sequential recovery
  generations can reconnect and eventually finish.
- Substitute production and reduced broker profiles/manifests in both directions.
- Verify the pre-N0 review receipt from external GitHub identities and reject
  stale, dismissed, self-reviewed, or partially paginated evidence.
- Walk every reduced-service command, teardown disposition, and retained-service
  upgrade state without an implicit transition.
