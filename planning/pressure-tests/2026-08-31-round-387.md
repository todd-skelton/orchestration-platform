# Breaker checkpoint literal prerequisite replan — round 387

Status: PROPOSED, independent exact-head review and host verification pending.
Base: `6b0e513`. No implementation authorization or self-PASS is claimed.

The existing current-policy consumer supplies complete facts but no generic
breaker history or recovery. The six-state table alone does not close breaker
instance identity, total edges, project recovery observations or selected
history. A CLOSED-only schema or an empty fixture history array would hide
those gaps. This packet therefore proposes one complete existing family,
with per-capability checkpoints and closed inline recovery/probe observations.

Four decisions are explicit: one project receipt carries per-capability known
states; changed configuration/policy is conservatively UNKNOWN and retains
the predecessor; recovery advances one phase per capability/cycle with no
automatic UNKNOWN exit; ISS-013 owns exact project observation literals while
the actual public journal owns beginning/current-end selection. Receipts are
upstream of their enclosing future events, never a second authoritative log.
The actual fresh-root/lease/census observation is available only to the
quarantined fixture; null/empty inputs never reset production holds.

The cheapest discriminators are unchanged OPEN under NO_TRIP, removed held
capability under changed config, wrong opening/transaction/probe/policy joins,
failed versus successful completion, fresh next-cycle policy after recovery,
unknown sticky-state refusal, and reused/nonempty-root genesis refusal. Each
implemented equality must have an independent removal mutant. Complete
state/edge/refusal, canonical frame, bounds and hostile-input vectors precede
runtime work; public history/currentness evidence cannot be replaced by a
well-formed prefix or copied digest.

The smaller alternative is retaining the existing observer-only seam; a
policy interpreter, private history family or broad production reducer would
be larger and weaken the purpose of the first consumer. The proposed footprint
is only the architecture subsection, minimal lifecycle clarification,
ISS-002/013/025 ownership notes and this record. No new public family, owner,
issue edge, manifest, CLI, parser, SDK runtime, probe/provider or ISS-026 work.
The author ran no verifier, tests, builds or probes; independent review must
decide whether this complete packet is sufficiently bounded before any code.
