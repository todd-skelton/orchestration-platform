# Pressure round 437 — quarantined planning source identity

Status: blocked and abandoned ISS-011 source candidate after the landed Round
436 replan. PR #252 was closed unmerged.

## Declared inputs and scope

Round 435's abandoned source and consumer vectors are declared implementation
inputs, including both blocked exact heads and reviews. Round 436 supplies the
new admission boundary. This candidate does not continue or reset Round 435: it
starts from the independently reviewed replan and receives its own bounded
pressure round.

The packet adds only `modules/planning/src/index.ts`, focused vectors in the
existing walking-skeleton consumer, and this record. The empty manifest, empty
generator contract, generated stub, public contracts, adapters, runtime, root
configuration, and authority surfaces remain byte-stable.

## Source admission and behavior

The consumer normalizes the tracked planning source and verifies the fixed
SHA-256 identity before each actual preparation-path invocation of `plan`.
Independent exact-head review inspects those same admitted bytes. The token
inspection is a separate dependency boundary: the only static dependency is
the public contracts source, while static exports from another dependency,
import-equals, dynamic import, and every `require` reference are findings.

The ambient-name scan is advisory regression evidence. It includes the prior
clock, randomness, process, and network cells plus Node 24 `navigator` and
`WebSocket`; it is not claimed as a complete inventory. Every listed mutation,
and any future unknown ambient mutation, changes the normalized source identity
and therefore cannot inherit the exact-byte review.

Planning semantics remain the Round 434 definition: exact descriptor and
catalog; valid alternate-descriptor and review-subject refusal; TRIP or no
eligible canonical READY `work.read` row yields bound no-action; exact NO_TRIP
selects the first canonical eligible row and emits the complete review-required
implementation brief. The actual ISS-013 branch and queue loader, snapshot,
policy, composition, retained await, and post-await binding seam supplies every
input.

## Acceptance, verification, and review attack

Acceptance fixes the complete descriptor and descriptor digest, complete
action/core/brief/directive/footprint result, valid substitution refusals,
readiness and capability negatives, canonical order, repeated-input byte
equality, adapter decision projection, normalized generator identity, empty
manifest, exact source identity, dependency-loading mutants, and the two missed
ambient regressions. Local verification is typecheck, focused formatting,
planning, and diff checks; local Vitest remains prohibited on this Windows
checkout. Hosted bootstrap supplies the focused test and three-OS evidence.

Review should independently recompute the normalized source hash, inspect the
exact admitted bytes for ambient/effect access, mutate an unlisted identifier
to prove identity denial does not depend on the advisory census, attack every
dependency-loading form, substitute descriptor/result fields, bypass the
actual preparation seam, and verify all placeholder and authority surfaces are
unchanged. The digest proves identity continuity only; it does not prove
semantics, safety, installation, execution, release, or authority. A production
source change after review, manifest/generator edit, new public contract,
second module, sandbox/VM, or runtime integration forces replan.

## Exact-head follow-up

The first review found two test-only gaps. The empty placeholder manifest check
compared checkout line endings even though Windows `core.autocrlf` materializes
the unchanged Git blob with CRLF. It now compares normalized tracked text to the
fixed empty contract, matching the source and generator identity rules without
changing or activating the manifest. The repeated-input determinism calls also
invoked `plan` directly after the prepared call; every planning invocation now
passes through one helper that verifies the admitted normalized source identity
immediately before delegating to `plan`. Production source, semantics, scope,
and every placeholder and authority surface are unchanged by this sole
follow-up.

## Hosted disposition

The initial exact head was
`225197e4407267ce59d08284a2a8055630c0f8d0`. Its independent review admitted
one follow-up for a checkout-line-ending assertion and two direct plan calls
that bypassed the source identity gate. The sole follow-up produced
`2aecb4c6d7f25055c66dfaba641ff9d1c99f5002` and received an independent PASS.

Hosted run `33583110269` then failed the same focused assertion on macOS, Linux,
and Windows. The normalized source identity check passed, but the
`planningSourceEffects` projection for an appended mutation equaled the clean
projection. A plain Node diagnostic against the exact head showed that all
thirteen appended static-import/export, dynamic-import, `require`, and ambient
mutations returned that same clean projection. On Linux the remainder was
1,523 passed tests and 13 skips; the focused failure is executable evidence,
not a platform outage.

No second follow-up is admitted. PR #252 remains closed and unmerged with empty
closing references. Both exact heads, reviews, the complete hosted run, and the
diagnostic are declared inputs to Round 438. The manifest, generator, generated
stub, runtime, and authority state on protected main never changed.
