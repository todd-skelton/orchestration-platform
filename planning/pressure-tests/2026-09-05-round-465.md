# Pressure round 465 - ISS-054 closure record and census append

Status: planning artifact attempt 1 of eight (Decision #297 ruling A). The
protected base is `66365f1ec896002bc7ed6f29768c0b8f46b2950f` (PR #301, the
gap slice), which carries the Round 460 Slice E closure census (PR #296) and
everything before it. This record claims no confirmation and no acceptance:
it appends the census with the gap slice's landed vector and drafts a closure
record, but whether ISS-054 (#255) is thereby closeable remains the
controller host's and Todd's decision after an independent confirmation lane
re-derives it.

## Authority

- Decision #297 ruling A (`gh issue view 297 --comments`): on the gap slice
  landing, the census on #255 is appended with that vector and ISS-054
  closes as completed citing PR #296 and the gap slice.
- Round 460 (`planning/pressure-tests/2026-09-05-round-460.md`): the Slice E
  closure census, source of the tables and gap list edited here.
- Round 462 (`planning/pressure-tests/2026-09-05-round-462.md`): the gap
  slice's own record; source of the count change (60 -> 61) and the exact
  issue array.
- PR #301 (merge `66365f1ec896002bc7ed6f29768c0b8f46b2950f`, source
  `66ad2a5cb5b616d3472f3442c9db1e4f5919a971`): the landed gap slice.
- The attempt-2 independent exact-head review receipt for the gap slice
  (`iss054-gap-review.receipt.md`, PASS, no blockers): observation 1 states
  every `RP:NNNN` citation at or after line 1949 of
  `test/contracts/repository-protection.test.ts` is now offset by +5, and the
  prose "60 one-axis relation mutants" is now 61. This lane's edits carry out
  that observation's named remedy.
- `planning/drafts/ISS-011.md` "Closure census": the form followed for the
  new "Closure record" subsection.

## Scope

`planning/drafts/ISS-054.md`: shift every `RP:NNNN` citation at or after 1949
by +5 (five-line insertion at old line 1949 in the landed test file, per the
gap slice's diff); change "60 one-axis relation mutants" to "61"; replace the
census table 2b "change its conclusion" gap cell with the landed vector;
rewrite the gap list to report zero open gaps; add a "Closure record — Round
465" subsection. Plus this round record.

## Non-goals

No source change. No test change. No roadmap change. No issue closure by
this lane (closing #255 is the controller host's action on this record
landing, per Decision #297 ruling A). No independent review or confirmation
claimed.

## Shifted-citation table

Every `RP:NNNN` citation in `planning/drafts/ISS-054.md` whose old value was
at or after 1949 shifts to old+5. Every entry below was verified by reading
`test/contracts/repository-protection.test.ts` at the base head
(`66365f1ec896002bc7ed6f29768c0b8f46b2950f`) at the new line and confirming
the cited `relation:`/`test(...)` label matches. Citations below 1949 and all
`RPB:`/`VA:` citations were left unchanged (confirmed unaffected by the same
read).

| Old | New | Verified label at the new line |
|---|---|---|
| RP:1950 | RP:1955 | `relation: "triggering BUILD workflow digest equals the BUILD row"` |
| RP:1955 | RP:1960 | `relation: "triggering BUILD workflow path equals the BUILD row"` |
| RP:1960 | RP:1965 | `relation: "triggering BUILD workflow ref equals the BUILD row"` |
| RP:1965 | RP:1970 | `relation: "producer run identity differs from the triggering BUILD run"` |
| RP:1970 | RP:1975 | `relation: "triggering BUILD completes no later than the producer start"` |
| RP:1975 | RP:1980 | `relation: "request identities are unique across the nine observations"` |
| RP:1989 | RP:1994 | `relation: "variable publication strictly precedes the producer start"` |
| RP:1999 | RP:2004 | `relation: "variable publication is no later than receipt issue"` |
| RP:2012 | RP:2017 | `relation: "producer start is no later than receipt issue"` |
| RP:2027 | RP:2032 | `relation: "observation start is no earlier than the producer start"` |
| RP:2036 | RP:2041 | `relation: "observation completion is no later than receipt issue"` |
| RP:2045 | RP:2050 | `relation: "page observation time lies inside the observation window"` |
| RP:2054 | RP:2059 | `relation: "request identity recomputes from purpose and request branch"` |
| RP:2081 | RP:2086 | `relation: "complete reduction digest recomputes over the page projection"` |
| RP:2090 | RP:2095 | `relation: "terminal pagination digest recomputes over the complete page rows"` |
| RP:2099 | RP:2104 | `relation: "recomputable reduced value digest is pinned to its projection"` |
| RP:2126 | RP:2131 | `relation: "REPOSITORY projection member repositoryId"` |
| RP:2135 | RP:2140 | `relation: "RULESET projection member rulesetId"` |
| RP:2144 | RP:2149 | `relation: "RULESET projection member protectedPathPolicies"` |
| RP:2156 | RP:2161 | `relation: "WORKFLOW_BUILD projection member workflow"` |
| RP:2165 | RP:2170 | `relation: "WORKFLOW_REVIEW projection member workflow"` |
| RP:2174 | RP:2179 | `relation: "WORKFLOW_RUN projection member triggeringBuild"` |
| RP:2183 | RP:2188 | `relation: "ENVIRONMENT_VARIABLE projection member variableValue"` |
| RP:2194 | RP:2199 | `relation: "ruleset semantic digest recomputes over its five-member projection"` |
| RP:2203 | RP:2208 | `relation: "REST NEXT relation targets the immediately following page"` |
| RP:2212 | RP:2217 | `relation: "REST NEXT relation is mandatory on every nonterminal page"` |
| RP:2223 | RP:2228 | `relation: "REST NEXT relation is forbidden on the final page"` |
| RP:2238 | RP:2243 | `relation: "REST FIRST relation is forbidden on the first page"` |
| RP:2253 | RP:2258 | `relation: "REST PREV relation targets the immediately preceding page"` |
| RP:2262 | RP:2267 | `relation: "REST LAST relation is forbidden on the final page"` |
| RP:2277 | RP:2282 | `relation: "REST nonterminal nextRequestDigest hands off to the following page"` |
| RP:2286 | RP:2291 | `relation: "REST terminal page carries a null nextRequestDigest"` |
| RP:2295 | RP:2300 | `relation: "REST page ordinals stay adjacent from one"` |
| RP:2304 | RP:2309 | `relation: "REST page request digests never repeat across the observation"` |
| RP:2318 | RP:2323 | `relation: "GraphQL first request cursor is null"` |
| RP:2332 | RP:2337 | `relation: "GraphQL cursor is acquired from the immediately prior page"` |
| RP:2343 | RP:2348 | `relation: "GraphQL nonterminal page reports hasNextPage true"` |
| RP:2352 | RP:2357 | `relation: "GraphQL terminal page reports hasNextPage false"` |
| RP:2373 | RP:2378 | `relation: "GraphQL cursors never repeat across the observation"` |
| RP:2382 | RP:2387 | `relation: "request identity frame part order"` |
| RP:2395 | RP:2400 | `relation: "page request frame nullable-text part tag"` |
| RP:2408 | RP:2413 | `relation: "complete reduction frame part order"` |
| RP:2422 | RP:2427 | `relation: "terminal pagination frame apiKind part tag"` |
| RP:2436 | RP:2441 | `relation: "reduced value frame domain and part order"` |
| RP:2449 | RP:2454 | `relation: "ruleset semantics frame encloses the canonical projection"` |
| RP:2488 | RP:2493 | `test("exposes exactly the twelve reviewed public names and the retained receipt type", ...)` |
| RP:2515 | RP:2520 | `test("registers the family vocabulary, compatibility and serialization together", ...)` |
| RP:2555 | RP:2560 | `test("accepts canonical bytes and refuses noncanonical, prefixed and BOM bytes", ...)` |
| RP:2581 | RP:2586 | `test("pins the archived independent canonical and framed digest goldens", ...)` |
| RP:2634 | RP:2639 | `test("routes every public helper argument through its Packet A parser", ...)` |
| RP:2747 | RP:2752 | `test("accepts the canonical relationally sealed receipt as a detached frozen tree", ...)` |
| RP:2758 | RP:2763 | `test("accepts every disposition while only ACCEPTED stays binder eligible", ...)` |
| RP:2783 | RP:2788 | `test("accepts one, two and sixty-four page REST and GraphQL chains", ...)` |
| RP:2798 | RP:2803 | `test("refuses zero-row, sixty-five-row and cyclic page chains", ...)` |
| RP:2825 | RP:2830 | `test("keeps every Packet B relation deletion mutant discriminating", ...)` |
| RP:3393 | RP:3398 | `test("refuses every fresh root scalar grammar on one axis with its exact code", ...)` |
| RP:3427 | RP:3432 | `test("refuses a missing, unknown, renamed or schemaVersion-bearing fresh root", ...)` |
| RP:3451 | RP:3456 | `test("treats a reordered fresh top level as a positive, not a refusal", ...)` |
| RP:3463 | RP:3468 | `test("refuses the historical nine-purpose array and every purpose-census defect", ...)` |
| RP:3508 | RP:3513 | `test("refuses a non-null triggeringBuild on any fresh row", ...)` |
| RP:3518 | RP:3523 | `test("pins the 300,000 millisecond fresh window at both boundaries", ...)` |
| RP:3539 | RP:3544 | `test("refuses fresh page times outside the observation window, page by page", ...)` |
| RP:3599 | RP:3604 | `test("refuses hostile reflection at every fresh root with zero trap executions", ...)` |
| RP:3648 | RP:3653 | `test("refuses cross-realm, subclass, cyclic, iterator, hole and extra-key fresh inputs", ...)` |
| RP:3678 | RP:3683 | `test("keeps every fresh typed-child deletion mutant discriminating", ...)` |
| RP:3795 | RP:3800 | `test("leaves the public surface at exactly the reviewed twelve names and the type", ...)` |
| RP:3806-3808 | RP:3811-3813 | the three absent-name array elements inside that test: `"observedProtectionSchemaFields"` (3811), `"freshObservedProtectionPurposes"` (3812), `"parseObservedProtection"` (3813) |

68 individually shifted numeric citation instances (66 single citations plus
the two endpoints of the one affected range, `RP:3806-3808` ->
`RP:3811-3813`); the review receipt's observation 1 estimated "67 distinct
citations", counting the range as one. `RP:1925` and `RP:1935` (both below
1949) were confirmed unchanged. Citations `RP:1913`, `RP:1843`, `RP:1853`,
`RP:1863`, `RP:1873`, `RP:1883`, `RP:1893`, `RP:1903` (all below 1949) were
likewise confirmed unchanged by the same read.

## New citation (not a shift)

The census table 2b "change its conclusion" cell and the gap-list item both
now cite `RP:1950` for the new mutant itself, verified at the base head:
line 1950 is `relation: "triggering BUILD conclusion is the SUCCESS
literal"`, and the exact expected array
`["apiObservations.8.triggeringBuild.conclusion:mismatch"]` was verified
against line 1952. Both cells cite `RP:2830` (the shifted location of the
discriminating test) as the containing test, matching the entry's format for
every other mutant in the table.

## Count change

"60 one-axis relation mutants" (table 1, row 2) is now "61 one-axis relation
mutants". This was the only prose occurrence of that count in the draft; no
other count required changing.

## Census cell text (table 2b, "change its conclusion")

Replaced the bold "no landed vector — see the gap list below" cell with:

> mutant `triggering BUILD conclusion is the SUCCESS literal` RP:1950 under
> RP:2830, expected array
> `["apiObservations.8.triggeringBuild.conclusion:mismatch"]`, landed by PR
> #301 at `66ad2a5` (main `66365f1ec896002bc7ed6f29768c0b8f46b2950f`), Round
> 462; the code is emitted by the structural literal gate
> (`repository-protection-parse.ts:1241`), so its path prefix is the numeric
> index rather than the relation-layer `WORKFLOW_RUN` label.

The gap list's lead sentence was changed from "Exactly one clause ... has no
landed vector" to "Zero clauses ... have no landed vector at
`66365f1ec896002bc7ed6f29768c0b8f46b2950f`", and item 1 was rewritten to
state the clause is resolved, citing the same vector, expected array, PR, and
merge SHA. The two precision notes ("bind producer to BUILD" and "add a
producer conclusion") were kept unchanged.

## Closure record subsection

Added "#### Closure record — Round 465" before "## Predicted footprint and
dependencies", stating: every acceptance criterion and every corpus clause
has a landed vector at `66365f1ec896002bc7ed6f29768c0b8f46b2950f`; the landed
PR chain with merge SHAs from `git log --merges` (anchor PR #261
`957e55b4344099a2d606d64e562d4dc839e1ac17`, Packet A PR #280
`1a951ce6afdc9c59d0bc5b820d8c56a1cffc4b0f`, Packet B PR #281
`e6c9d4f1bfeb1347ba4f229d1c965b3c3bb7725e`, Packet C PR #291
`10f1e31ab96aa271a0ae9b2330c27614001c49d6`, Packet D PR #293
`149d9e8840060d4b0ca9e81c7147f46cfa99c790`, Slice E PR #296
`c051b727ecb7be6bc9b5822034059662f999f718`, gap slice PR #301
`66365f1ec896002bc7ed6f29768c0b8f46b2950f`), each cited with its exact merge
subject; the attempt ledger for the gap slice (attempt 1 source, attempt 2
independent review PASS) and for this record (attempt 1 this source, attempt
2 independent confirmation pending); the three follow-ups that remain logged
and unresolved and are not closure blockers (byte-identical helper
duplication, the waived verifier-anchor cast under Decision #282 ruling 2 A,
the head-sensitive cross-file public-surface lists); that closing issue #255
is the controller host's action on this record landing; and an explicit
no-self-certification statement. A one-sentence parenthetical was added to
the end of the pre-existing "What Slice E does not establish" paragraph
noting it is Slice E's own historical scope statement, superseded for current
status by the new subsection — this is the only edit outside the five listed
in scope, made because that paragraph's "whether the one gap above is
material" clause would otherwise misstate the now-zero gap count; it changes
no fact, only points the reader at the corrected section.

## Verification

Local gates only, run against this lane's final working-tree contents and
re-confirmed against the committed head (exit codes recorded in this lane's
completion report, since the commit SHA does not exist while this file is
being written):

| Gate | Exit |
|---|---|
| `pnpm typecheck` | 0 |
| `pnpm format:check` | 0 |
| `pnpm planning:check` | 0 |
| `pnpm planning:board-check` | 1 (expected — see below) |
| `git diff --check origin/main..HEAD` | recorded post-commit |
| `git status --porcelain` | recorded post-commit (empty after commit) |

`planning:board-check` fails on exactly one named row: "ISS-054 body does not
match its source draft." This is the expected, predicted-in-advance
divergence between the edited draft and issue #255's live GitHub body, which
the controller host mirrors only after this record lands; it is not a defect
in this lane's edit. No vitest, no runner, no shim, no native execution. No
independent review or hosted run performed by this lane.

## Predicted footprint

Exactly two files: `planning/drafts/ISS-054.md` (edited) and this round
record, `planning/pressure-tests/2026-09-05-round-465.md` (new).

## Review attack surface

1. **A citation resolving to the wrong line.** Every shifted citation above
   was independently verified by reading the base test file at the new line,
   not by arithmetic alone; the shifted-citation table records the exact
   label found there.
2. **A citation below 1949 wrongly shifted.** Spot-checked: `RP:1925`,
   `RP:1935`, `RP:1913`, `RP:1843`, `RP:1853`, `RP:1863`, `RP:1873`,
   `RP:1883`, `RP:1893`, `RP:1903`, `RP:1541`, and every `RP:NNNN` below 1949
   in tables 1, 2 and 2b were confirmed unchanged against the base file.
3. **A stale 60.** A repository-wide search of the draft for `60 one-axis`
   and for `sixty` found exactly one prose occurrence of the count (now
   fixed) and no other instance describing the relation-mutant total; the
   remaining "sixty-*" occurrences are page-count fixture names, unrelated.
4. **The gap list still reporting a gap.** The lead sentence now reads "Zero
   clauses ... have no landed vector"; item 1 is rewritten as resolved; no
   other list item was touched.
5. **An invented PR number or merge SHA.** Every merge SHA and PR number in
   the new closure-record table was read from this worktree's own
   `git log --merges` output at the exact base head, not transcribed from
   memory or from the census's own prose; the seven rows match the seven PRs
   Round 460's heads table already names, cross-checked one for one.

## Attempt ledger

- Attempt 1: this source change (planning-only census append and closure
  record), `claude-sonnet-5` at medium effort, row 13 (Decision #297 ruling
  A).
- Attempt 2: independent confirmation, pending, by a different lane.

No new ceiling is requested or inferred; this is attempt 1 of the ordinary
eight-attempt ceiling.

## Routing record

Row 13, model `claude-sonnet-5`, effort `medium`, `placement: provisional`,
budget intent at most $5, actual spend unknown.
