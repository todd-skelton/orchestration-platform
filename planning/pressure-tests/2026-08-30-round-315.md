# ISS-022 provider-neutral decision-contract pressure review — round 315

`BLOCK_REPLAN` at exact base/HEAD
`75ad198417fd56152c2621506258156c0c8506d8`.

The uncommitted provider-neutral three-file, `+799` slice closes and freezes all
three record shapes, uses canonical serialization and the specified
domain-separated identities, recomputes the stable-harness subject, decision
writer, and fixed custody profile, and exactly joins review disposition and the
final decision. No GitHub vocabulary, adapter, live fetch, command, workflow,
publication, board, helper, or production mechanism enters the module.

Two authority blockers remain:

1. The `BLOCK_REPLAN` union is not closed. Its parser checks only null aggregate
   and non-null diagnostic terminal after admitting three arbitrary nullable
   OS-profile slots. A block core with a non-null `osProfileDigests` slot is
   therefore parseable, serializable, digestible, and final-decision joinable.
   The validator merely compares that array with the caller's expectation.
   Likewise, helper/helper-ABI nullability is never related to OS evidence, so
   a present Linux observation can validate with null Linux helper and ABI
   slots. Require all three OS-profile slots null in the block arm and permit a
   null helper/ABI slot only when the corresponding OS evidence is absent.
2. The evidence validator cannot prove the required per-OS chain. It receives
   aggregate, diagnostic-terminal, provider-run, helper, ABI, and OS-profile
   digests as caller expectation scalars and receives no environments, OS
   profiles, custody receipts, or aggregate/diagnostic records from which to
   recompute them. Consequently an ordered synthetic observation census with
   arbitrary valid environment digests and independently chosen matching slot
   expectations can validate; candidate/run/environment/profile/custody
   mixing is invisible. In the block arm, any 0–62 all-PASS subset is treated
   as an authenticated missing row without joining that gap to the diagnostic
   evidence, while 63 PASS observations are rejected even when the authenticated
   failure is a missing job or artifact. Replace caller equality with the
   smallest stable-evidence recomputation/join needed to prove the aggregate or
   diagnostic arm and each OS's environment/helper/ABI/profile/custody chain.

The existing four focused tests pass, as do repository TypeScript, touched-file
Prettier, and `git diff --check`; they omit the non-null block OS-profile,
present-OS null helper/ABI, synthetic environment, mixed-chain, and authenticated
63-PASS diagnostic counterexamples. The 799-line surface remained readable in
one sitting. Repair is confined to this module and focused mutants; no adapter,
terminal writer, publication path, command, or workflow widening is justified.
