# ISS-003 pure resolver post-repair review — round 322

`PASS` at exact base/HEAD
`78004217f75b28f1ae0e4d0ce0e19487ea289f49` with the repaired resolver slice
uncommitted.

Round 321's two blockers are closed.

1. Project and config selection now bind the admitted canonical paths to CLI,
   then environment, by exact host-path equality. Lower-precedence environment
   values cannot replace a CLI selection. With neither explicit project arm,
   the already-admitted discovered project remains accepted; with neither
   explicit config arm, config must be exactly
   `<admitted projectRoot>/.orchestration/project.json` using the selected
   OS path implementation. Windows and POSIX mutants accept each selected arm
   and refuse explicit, environment, default, and lower-arm substitutions with
   the fixed `PATH_REFUSED` row. The repair is lexical only: it adds no
   filesystem call, discovery, admission, parser authority, or path mutation.
2. `pnpm --filter @orchestration-platform/config test` now runs exactly
   `vitest run test/config` and passes 30/30. The required ISS-000 scaffold
   restatements recognize only this filtered config-test capability, with
   reversal mutants in both planning and bootstrap contract suites. The CLI
   package test and root shell matrix remain ISS-003 placeholders;
   `loadConfiguration`, other commands, and other ISS-003 surfaces are not
   claimed implemented.

The closed records, state-root precedence, fixed failures, no-throw boundary,
three-OS defaults, private provenance/paths projections, landed-contract
validation, and path-token nonleakage remain unchanged. The package dependency,
index, lock, test script, and four required census/mutant edits are the complete
repair footprint and remain reviewable in one sitting.

Focused evidence: config package tests pass 30/30; bootstrap/planning census
tests pass 69/69; repository TypeScript and `git diff --check` pass.

## Proportionality

Exact canonical comparisons and one OS-aware default join are the smallest
repair for the demonstrated substitution threat. Reusing the existing Vitest
suite and amending each frozen census restatement is smaller than a new test or
authority mechanism. No further scope is warranted.
