# Pressure test — round 37

**Disposition:** `BLOCK_REPLAN` (removal round; repairs verified, further gaps and removals found)

The thirty-seventh review verified the round-35/36 repairs and served as the
required follow-up removal round. The removed-machinery sweep, v1-everywhere
census, twelve-kind consistency, dispatch-brief issue ownership, and the
reduced dependency graph (zero transitively implied edges; every prose
consumption claim transitively satisfied) all verified clean. It then found
four authority-model gaps in the repaired rotation protocol, seven smaller
defects, and four further removals.

| Finding | Repair applied |
| --- | --- |
| `OP-R37-001` — "Armed rotation intent" was load-bearing in six documents and defined in none: the single permitted head-plus-one exception had no acceptance predicate. | Defined in `contract-decisions.md`: the selected CAS-armed run-current journal for the rotation target plus its create-once intent record; equality set is head-plus-one ordinal, predecessor digest equal to the head record digest, and rotation identity plus successor authority facts; with no armed intent selected, any head-plus-one file refuses. Mirrored into the docs and `ISS-002`/`ISS-004` ACs. |
| `OP-R37-002` — The successor run had no written, enumeration-free way to locate the rotation run it must terminalize. | Decision added: the successor run recomputes the rotation target and mutation identity from the selected successor authority value and the head chain record, then constructs the journal path. Mirrored into `supervisor-contract.md` and `ISS-004` AC4 with a derivation mutant. |
| `OP-R37-003` — "The first fresh new-epoch run" was not written as resumable: a crash after the walk but before the terminal-resolution write deadlocked a strict reading. | Reworded everywhere to "each fresh new-epoch run, until the rotation run's terminal resolution is selected", with the write idempotent create-once; crash-after-walk resume mutants added to `ISS-004` AC4 and `ISS-015` AC14. |
| `OP-R37-004` — The `SELECTED` resolution rule and phase matrix still required a complete single-epoch nine-stage run, which the rotation exception violates. | Rotation exception added to the resolution table and phase matrix: stages 0–5 bind the old epoch, the terminal resolution binds the successor epoch, and the run remains single-epoch because nothing executes after its CAS. |
| `OP-R37-005` — `EPIC-KERNEL` still named the folded terminal-summary record. | Deleted. |
| `OP-R37-006` — `ISS-030` denied the twelve-slot evidence packet its own AC requires. | The false negative deleted. |
| `OP-R37-007` — "The ten tombstone-enabled families" was an unenumerated closed count. | Enumerated or dropped in the registry census (resolved with the retention-kind deletion below). |
| `OP-R37-008` — `dispatch-brief/v1` was absent from the exhaustive `routine-cycle/v1` protocol, so `ISS-026`'s producer census could not be satisfied. | Step 4 emits the brief inside the module plan; step 7 binds the host-rendered bytes' digest into the dispatch plan; `ISS-026` AC7 gains the row. |
| `OP-R37-009` — `ISS-002` AC12 re-duplicated the rendering goldens `ISS-011` had just disclaimed to `ISS-021`. | Narrowed to closed parsing, prose-field refusal, and the digest-binding validator. |
| `OP-R37-010` / `OP-R37-011` — Two stale post-reduction statements (`ISS-030` asserting a direct ISS-004 edge; `ISS-039` citing the old eleven-blocker count). | Reworded to transitive-consumption phrasing and the dependency-closure phrasing. |
| `OP-R37-A` — Authorized retention/compaction (`AUTHORITY_RETENTION` kind, `COMPACTED` classification, `AUDIT_DEGRADED` mode, four-step compaction protocol, five restatements of its operation matrix) existed for a data volume the architecture proves cannot occur; everything else is already FULL_REQUIRED. | Deleted. The registry has eleven kinds; every class is FULL_REQUIRED; terminal attempt history grows only with rare attempts; loss of any required record is `UNKNOWN` and blocks mutation. Strictly fail-closed and strictly smaller. |
| `OP-R37-B` — The `platform-configuration/v0-fixture` migration path was a parser, migration, tests, and disposition row with no possible reader. | Deleted; a v0 record refuses like any unknown version; the public entry points are parse/serialize/validate. |
| `OP-R37-C` — Three documents each stated the rotation protocol two or three times, regenerating the drift class rounds 34–36 removed elsewhere. | One canonical statement per document; other occurrences reduced to cross-references. |
| `OP-R37-D` — `host-custody.md` restated teardown dispositions four times. | Folded to one statement plus the two unique facts. |

## Examined and retained

The eleven-slot evidence packet (registry-derived, one per kind); the
`ACTIVATION_RECOVERY_LAUNCH` kind (carries rebind and per-source terminal
state no attempt-log record holds); `ISS-035` after its round-36 narrowing
(pre-package custody admission gate); the four-actor serial certification
chain (distinct authority identities); `routine-cycle.md` skip-routing rows
1–7 (derivable but cheaper to enumerate for conformance generation); and the
round-34/36 retention lists, unchanged.

## Re-review focus

- The armed-intent equality set closes the head-plus-one forgery surface and
  cannot be satisfied by a record forged without the selected CAS-armed
  journal.
- The each-fresh-run wording admits no second writer: the walk is read-only,
  the write is create-once, and no other mutation may precede it.
- The eleven-kind census, four-classification table, and FULL_REQUIRED rule
  are stated identically everywhere they appear.
- The next removal round must record an evidence-backed nothing-further-
  removable finding for the packet to reach `PASS`.
