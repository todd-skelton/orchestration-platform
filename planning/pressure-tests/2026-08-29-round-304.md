# ISS-022 nullable-environment implementation follow-up — round 304

## Disposition

`PASS`.

This is the single permitted post-repair review of Round 303's closure finding
on `codex/iss-022-nullable-environment-implementation` at exact base/HEAD
`266a45f9e92ef384b60999a43cd3df29dd211701`.

## Repaired closure

The stable suite now accepts exact provider inventory bytes rather than an
`osImageDigest` or timestamp, snapshots those bytes, derives `osImageDigest`,
and produces `observedAt` itself. It computes the acyclic pre-custody identity,
then custody instance/receipt and locator, then constructs the selected final
environment and only afterward computes `Denv`. The selected environment
equal-binds exact ABI, OS profile, helper profile, and custody receipt; neither
final nor pre-custody environment identity is caller input.

The null arm is now executable but narrowly gated. Job evidence admits it only
for the exact ISS-022 registry and canonical stable report. That report
revalidates the complete ordered 21-row census and raw normalizations, requires
a physical `UNKNOWN|UNSUPPORTED`, recomputes exact non-null ABI/helper evidence,
requires null filesystem/custody, and equal-binds the non-PASS result and
`Denv` into the receipt. Caller-filled or all-PASS null, mixed selection and
environment arms, sentinel digests, raw-inventory substitution, digest/custody
substitution, result laundering, caller time/digest fields, and incomplete or
reordered reports refuse.

Generic nullable parsing remains syntax-only. ISS-002 and every other suite
still require a non-null filesystem profile, while the aggregate independently
refuses every non-PASS receipt and can emit only PASS. The custody chain remains
acyclic, and no observation, provider, terminal, publication, workflow job,
helper, schema member, issue, edge, engine/adapter, or stable/candidate trust
surface changed.

## Proportionality and evidence

The 754-insertion repair remains a one-sitting review: 383 lines are focused
mutants/tests and the implementation stays within the five previously predicted
parser/profile/suite/receipt/digest files. Its exact-report gate reuses existing
normalizers and the generic receipt producer; a smaller exception without that
gate would admit caller-selected nulls. No additional mechanism is justified.

Focused evidence passed: four touched test files, 29 tests; `pnpm run typecheck`;
and `git diff --check`.
