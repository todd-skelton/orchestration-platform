# ISS-022 cold pressure review — round 243

`BLOCK_REPLAN` at exact main `78f6cb1297785d0f632bb4b487045a9f6f4dc2d4`.

No exact Node-first primitive candidates or OS inputs are fixed for volume/filesystem/ancestor identity, destination/runtime locks, create-once, CAS/replace/fsync, process identity/tree/termination, helper ABI, or custody profile.
The probe cannot test actual ISS-004 WeakMap/nonces, epoch revocation, rotation, or stale handles before ISS-004 exists; it needs a closed probe-fixture model and must leave production equivalence to ISS-004 acceptance.
The three existing external records bind locator/absence facts but no closed capability decision, lock/CAS/durability/process result, selected algorithm/profile, barrier, crash point, reboot generation, or consumer-admission digest.
“Every step” and the positive/negative matrix are not finite: exact operation states, barrier IDs, crash prefixes, expected bytes, errno/status mapping, owner-death/duplication cases, durability flush order, and UNSUPPORTED/UNKNOWN reduction are absent.
`Dphys` inputs lack literal per-OS observation algorithms and alias corpora; absent/existing leaves, case/Unicode profiles, mount/volume changes, symlink/junction/reparse swaps, permissions, and same-lock absence cannot be reproduced from the packet.
The “read-only” scope contradicts required create/replace/CAS/kill/reboot mutations, and standard disposable hosted runners cannot reboot and re-enter the same durable fixture; operator actions and lifecycle timing are therefore impossible as written.
No same-host principal stack or native helper is authorized now: first run the closed Node-only matrix, retain UNSUPPORTED as non-PASS, and require a new reviewed selection only for a measured Node gap.
The footprint omits substantial ISS-006 stable registry, runner-token, handler, observation, bundle, vector, and hosted-test amendments; current authority is hard-coded to `ISS002_CONTRACTS`.
Consumer ownership omits direct dependent ISS-031 and does not close how ISS-004, ISS-005, ISS-020, and ISS-031 verify one immutable decision; the registered parser-equivalence probe obligation is also unaddressed.
Proportionality: specify one bounded disposable Node probe plus exact receipts and a feasible persistence lane; delete production-service simulations, speculative helpers, and reboot claims until discriminating evidence requires them.
