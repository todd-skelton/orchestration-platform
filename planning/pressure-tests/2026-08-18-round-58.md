# Pressure test — round 58 author host-identity repair

**Disposition:** `AUTHOR_READY_REQUIRES_INDEPENDENT_REVIEW`

**Repair base:** `17e9e61983ee43a4c9ef091d87d5d0bee0a96721`

This author-side repair answers only `OP-R57-001` and `OP-R57-002`. Round 57's
`BLOCK_REPLAN` remains governing. This record grants no implementation, merge,
promotion, installation, or release authority; only a fresh independent exact-
head `PASS` may authorize implementation.

## Bounded repair

- `worker-host-identity/v1` is one exact two-member closed projection over the
  installed host renderer artifact digest. It has a literal schema/domain,
  canonical member order, two ordered framed parts, explicit provider/path/
  runtime/route/output/self exclusions, and an exact stability rule: any
  artifact change—including its embedded contract/capability census—creates a
  new identity.
- Host builds produce the artifact and identity inputs but do not authorize
  them. N0/successor release installation recomputes the identity from installed
  bytes and reviewed static metadata before selecting/read-backing the manifest
  row. Coordinated identity/artifact, moved registration, and cross-host
  substitutions now discriminate.
- ISS-012 now owns step-5 `route-selection/v1` emission of exactly one opaque
  identity admitted by the complete installed active-release census. Its new
  evidence covers first/two-host selection, requested-capability membership,
  and missing/duplicate/unknown/stale/moved/cross-host/route-manifest mutants.

## Proportionality and ownership

The identity projection reuses the existing host artifact digest and adds no
subordinate digest, registry, resolver, provider field, path, network lookup,
dependency edge, or mutation owner. It is the smallest domain-separated
preimage that distinguishes exact host bytes while remaining opaque to the
engine; routing separately validates the census embedded in those bytes.

ISS-002 owns the schema/formula and pure relations; host issues own deterministic
build inputs; ISS-020/014 own reviewed N0/successor selection; ISS-012 owns route
selection; ISS-008 owns step-7 lookup/binding; and ISS-026 composes the cycle.

## Required independent attack

Review the exact committed repair head cold. Attack every identity member,
domain, order, frame, exclusion, stability rule, embedded descriptor/census,
coordinated host swap, route/manifest equality, capability mismatch, producer
footprint, provider leakage, self-certification, and all prior repaired removal/
no-prose/catalog/cardinality invariants. Older PASS dispositions do not apply.
