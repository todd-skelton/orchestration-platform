# Pressure test — round 45 independent review

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `feb2c6859d61599e91bb85277372e773c40a63e0`

This was a fresh independent cold exact-head review under the ordinary removal-
review standard. The worktree was clean before review and the reviewed head was
exactly the hash above. The excluded
`archive/2026-08-17/linear-contracts-wip` tree was not inspected or used.
Round 44 is author-side evidence only and grants no authority to this head.

## Finding

| Finding | Evidence and bounded repair |
| --- | --- |
| `OP-R45-001` — The repaired packet's claimed 45-path footprint and ten-pressure-record census do not describe the reviewed head. | The non-archive diff from the packet's named branch-B base `e77fc77a35d91d35d80cda3887f64c65840182b7` through reviewed head `feb2c68` contains **47** paths. The 45-path count was true at `e30ff05`, before the repair commit added round 43 and round 44. The reviewed diff has twelve pressure-history/reconciliation paths: three branch-A records, modified round 36, and rounds 37 through 44. All other advertised categories match, so the exact total is 47. Repair only the footprint prose in `ISS-002.md` and the next governing author reconciliation: state 47 paths and twelve pressure records for this reviewed head, or explicitly freeze and name the earlier 45-path substantive delta while separately enumerating later review receipts. Do not delete review history, restore either consumer migration authority, or add a checker/mechanism merely to preserve 45. Recompute the total at the next exact-head gate because any later receipt is outside this reviewed head. |

## Independently validated evidence

- Consumer-board decoupling is complete in the governing surface. The planning
  checker reads no deleted migration snapshot, consumer issue census, or
  consumer Project. The board checker reads the complete issue connection for
  `todd-skelton/orchestration-platform` and exactly the single Project named by
  `planning/roadmap.json`; it validates independent totals, cursor continuity,
  identities, duplicates, and the 100/101 provider boundary. The live read-only
  board verifier returned `board contracts verified`.
- The roadmap contains 46 registered issues and 73 direct dependency edges.
  An independent traversal found zero transitively implied direct edges and
  closure counts of 544 for ISS-000 through ISS-038, 615 through ISS-043, and
  654 through ISS-045.
- The runtime registry contains exactly eleven kinds. The repaired normative
  packet text assigns the target its one registry-ordered slot and exactly ten
  non-target slots, and ISS-002 AC11 now requires an executable mutant for
  eleven total slots, ten non-target slots, one target occurrence, and no
  omitted kind. No target-plus-eleven wording remains in the governing packet.
- The simplified schema tables independently count Dop 9/15, Dsc 17, Dgb 14,
  Dgse 38, Drot 12, Dh 7/13, selected authority value 22, and Dcommit common 18
  plus ORDINARY 4 or rotation base 8 plus exactly one RESUMABLE 9, SELECTED 8,
  or UNKNOWN 1 arm. History records serialize only as
  `authority-history-record/v1`; `authority-history/v1` remains the digest
  domain.
- No deleted-compaction ordering demand remains. The governing compatibility
  attack surface instead requires executable retention/compaction/degraded-
  audit export and dispatch-reachability negatives. The architecture retains
  only FULL_REQUIRED linear history and an O(n) chain walk; checkpointing is
  parked behind the ISS-006 cross-OS measurement gate.
- The trust model remains external and fail closed: records authenticate
  relationships rather than writers; mutation additionally requires custody,
  the selected authority, the kernel-exclusive lock, and an ISS-004 private
  live capability. Workers, candidates, receipts, and structural parser success
  cannot certify or authorize themselves. Unknown or contradictory authority
  exposes no mutation capability.
- The engine/adapter boundary remains explicit. Engine contracts use opaque
  identities and closed platform enums; repository, branch, worktree, label,
  milestone, queue, deployment, and consumer vocabulary belongs to adapters
  and is guarded by the generated denylist mutant.
- Executable acceptance evidence is proportionate and discriminating: exact-
  byte and schema mutants, chain/rotation/outcome-slot matrices, the new packet
  census mutant, deleted-surface reachability negatives, hostile reflective
  shapes, and cross-OS vectors are owned by ISS-002/ISS-006. Current planning
  and board contract mutants pass independently.

## Bounded proportionality check

The fixed eleven-slot packet defends omission, duplication, substitution, and
wrong-outcome authority at the actual mutation boundary. Deleting its census
admits incomplete evidence; a twelfth slot defends no in-scope threat. Target
plus ten and one census mutant are the smallest credible shape.

Complete pagination of this repository and its one registered Project defends
provider-cap truncation. Deleting independent totals or cursor/identity checks
admits an incomplete board census. Reading a consumer backlog or Project
discriminates no platform-board defect, so the current single-repository,
single-Project checker is the smallest complete alternative.

The only in-scope threat from deleted retention/compaction/degraded-audit
machinery is accidental public reachability. An API/export/dispatch negative is
the cheapest evidence; restoring lifecycle fixtures or ordering tests would
reintroduce machinery without a consumer.

`OP-R45-001` is itself a proportionality finding: no runtime contract or new
checker is required to fix a two-path accounting error. Correcting or precisely
scoping the prose census is smaller than deleting historical receipts or
changing the substantive reconciliation.

## Verification

- `git status --short`, `git rev-parse HEAD`, and
  `git status --porcelain=v1 -uno` — clean reviewed head
  `feb2c6859d61599e91bb85277372e773c40a63e0`.
- `git diff --name-status e77fc77a35d91d35d80cda3887f64c65840182b7..feb2c6859d61599e91bb85277372e773c40a63e0 -- . ':(exclude)archive/2026-08-17/linear-contracts-wip/**'`
  — 47 paths, including twelve pressure records.
- Independent roadmap traversal — 46 issues, 73 edges, zero implied direct
  edges, closure 544/615/654.
- `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `pnpm exec vitest run test/planning-contracts.test.ts test/board-contracts.test.ts`
  — PASS (2 files, 48 tests).
- `node scripts/planning/board-check.mjs` — PASS read-only
  (`board contracts verified`); the sandboxed attempt failed to spawn `gh`
  with `EPERM`, and the same command completed outside that process sandbox.
- `pnpm run planning:check` reproduced the prior environment-specific wrapper
  hang and was interrupted; its exact owned Node command passed as recorded
  above.

## Next gate

Apply only `OP-R45-001`, issue a new bounded author reconciliation, and return
the resulting clean exact head to ordinary independent review. No
implementation, board mutation, consumer-backlog mutation, merge, or release
authority is granted.
