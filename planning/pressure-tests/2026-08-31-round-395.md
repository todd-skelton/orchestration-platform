# Pressure round 395 — bounded project preflight literals

Status: author proposal; independent exact-head review pending. No PASS,
implementation, host verification or ISS-041 completion is claimed here.

## Scope and evidence

The accepted route-docs base is `7421bc7d10e90f994ec4a98243486229b0e52373`.
The next consumer is the started walking skeleton's mandatory ordinal 6,
including declared NO_WORKER actions. The finite routine census already names
`project-preflight/v1` under ISS-013, outside ISS-002's additive ownership.
Module/action, route/core/brief, project-facts and immutable review-subject
contracts provide the complete supplied preimages; the literal is presently
missing. A skip or private receipt would not satisfy the public consumer.

The packet defines only that one result family and a closed inline observation
union, canonical hashes, complete terminal/null/reason matrix and supplied
relation. It reuses full project facts for ordinary work and concrete immutable
subjects for review. Unknown observations never imply an empty frontier.

## Explicit replan and non-goals

Ordinary eligibility now conservatively requires an unchanged entire frontier,
with FRONTIER_CHANGED for unrelated changes. A pure module may depend on any
row; chosen-row equality alone cannot establish that its old choice remains
valid. This avoids arbitrary dependency predicates or a second invocation.
Review instead compares the complete concrete immutable subject. No new source-
namespace restriction or candidate producer authority is inferred from the
invoking adapter configuration. The routine table explicitly names its existing
unknown-stop behavior; workerless routes still reach step 6 before 7–10 skips.

Out: code/parsers, SDK services/callbacks, CLI/publication, provider operations,
allocation/dispatch/mutation/release families, new issue/edge/roadmap, private
journal, runtime permission, resource exclusivity and ISS-026 implementation.
Actual fresh reads, retained materialization and ownership admission remain
with existing owners; matching digests alone establish none of them.

## Attack surface and cheapest discriminating evidence

| Mechanism | Removal attack / required future evidence | Smaller alternative considered |
| --- | --- | --- |
| Actual input/action/route join | Swap a valid plan, descriptor, target, mapping or route; rehash downstream result and still fail the changed join | Copying expected digests loses preimages; reuse existing module/route relations |
| Whole current observation | Foreign config/project, reused observation ID, missing/extra capability or partial frontier cannot become ELIGIBLE; every unknown/null cell is exercised | Selected-row-only checks miss module dependencies; full existing facts add no new family |
| Fixed ordinary refusal order | Combined missing/changed/capability/readiness/frontier faults return the first specified reason | No arbitrary policy or retry classifier |
| Exact immutable review target | Both worker arms/candidate pass unchanged; ordered content/source/attempt/certification-reference changes refuse even after observation rehash | Reuse concrete subject identity, not a third target wrapper or resolver |
| Canonical and authority separation | Independent bytes/full frames/digests and inline hashes; malformed/hostile/boundary inputs; coordinated forged tuples can satisfy structure without runtime authority | No freshness Boolean, ownership proof service, signing or private event family |
| Workerless continuation | NO_WORKER still takes a fresh step-6 observation; failure means no allocation/launch, never a fabricated skip | No fictitious host or step-6 bypass |

## Footprint, verification and decision gate

Exactly four documentation paths: `contract-decisions.md`, `routine-cycle.md`,
the owning ISS-013 draft and this record. Later implementation predicts one
public parser and framed identity, one inline observation helper/hash and one
supplied relation, focused compatibility tests, and inseparable registry/census/
harness amendments. No SDK runtime service is authorized by this packet.

Host verification is targeted formatting, planning and whitespace checks;
ordinary independent review binds the exact submitted commit before code.
Required later evidence is the full ledger vector matrix and actual fixture
continuation, not an author assertion. No tests, builds, compiler, probes or
provider mutations ran in this author packet. Root owns review status and
adjacent draft/board reconciliation.

One existing family and inline reuse are proportional to one missing consumer
step. Need for new resource/release/source authority, a general resolver, or a
larger runtime service is a named replan boundary. This record requests that
independent review attack the literal joins, frontier narrowing and authority
boundary; it does not certify them.
