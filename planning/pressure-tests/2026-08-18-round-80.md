# Pressure test — round 80 external-ledger final removal review

**Disposition:** `BLOCK_REPLAN`

**Reviewed head:** `8b94edde6217361406ca18f93628b9d78182925a`

This was a fresh independent cold removal review of the exact initially clean
head above on `iss-002-versioned-contracts`. There was no authoring role or
authoring context. The review began with `AGENTS.md`,
`docs/planning/work-model.md`, and
`docs/architecture/contract-decisions.md` as the sole decision authority, then
read the incorporated `Normative external bootstrap schema ledger` in
`docs/architecture/supervisor-contract.md`, the compatibility contract,
ISS-002/006/020/022, rounds 70/78/79, the exact
`d53494a82da59551e346bcb4ec75b8103289a56d..8b94edde6217361406ca18f93628b9d78182925a`
delta, and the current external contract code and tests. No Chase Sets
repository, issue, board, project, backlog, or other consumer system was read
or touched. The board check read only `todd-skelton/orchestration-platform` and
its registered delivery project.

## Disposition

`BLOCK_REPLAN`, solely because the mandatory unchanged default full-suite and
separate bootstrap verification path is not green at this head on Windows.
The exact semantic removal otherwise passes: `OP-R79-001` is removed, all six
round-78 findings remain removed, and no new external-ledger contract blocker
was found. The default suite repeatedly times out the CPU-heavy
`test/reference-inventory/inventory.test.ts:547` test at its fixed 120-second
ceiling, including in a confirmed quiet window. The same test passes in
isolation, and the entire suite passes when only the test timeout is raised to
300 seconds, so this is a bounded verification-ceiling defect rather than an
external schema, digest, path, lifecycle, or trust defect.

The smallest repair is to raise only that test's local timeout from `120_000`
to `300_000`, preserve all assertions and production bytes, run the unchanged
default full suite and separate bootstrap verifier to green, and return the new
exact clean head through fresh independent review. This review does not
authorize that test edit and made no implementation or test change.

Semantically, round 70 block 1 is implementation-ready. It remains unauthorized
at this exact head only because the required verification gate is ungreen.
Round 70 blocks 2 and 3 remain independently unauthorized. This record grants
no merge, release, promotion, installation, runtime mutation, live capability,
or self-certification authority.

## `OP-R79-001` removal audit

The repair selects one exact existing absence branch for consumed teardown:

- `RETIRE_CONSUMED` requires
  `reason:DESTINATION_STATE_ROOT_ABSENT`, the same reason as
  `RETIRE_UNUSED`, and not E0's `RUNTIME_AUTHORITY_ABSENT`;
- the observation occurs after terminal cleanup and before the lifecycle
  archive while the applicable destination and anchor locks remain held;
- `Dabs` is create-once and read back before lock release and equal-binds the
  same `Dphys`, raw-derived `Ddest`, state-root digest, current admitted `Dobs`,
  admitted helper, and custody instance;
- the consumed teardown receipt binds the same-lock `Dabs`, exact prior anchor
  CONSUMED tip/value/receipt, selected owner CONSUMED tip/value/receipt,
  retirement transition, destination, process/custody proof, and recomputed
  lifecycle-archive digest; and
- the anchor matrix now makes `RETIRE_UNUSED` use a teardown receipt over that
  reason and `RETIRE_CONSUMED` use a receipt over the same reason plus the
  selected CONSUMED graph. Compatibility repeats the two retirement branches,
  and ISS-022 is required to produce and mutate both exact absence branches.

Substituting `RUNTIME_AUTHORITY_ABSENT` into either retirement branch, using a
different state root/observation/helper/custody/lock section, selecting an
ACTIVE rather than CONSUMED graph, or supplying an opaque SHA-256 value now
contradicts a named branch equality. There is no longer an implementer choice
between runtime-only absence and complete destination-state-root absence.

ISS-002, ISS-006, and ISS-020 already incorporate the normative ledger and
require every external lifecycle branch, closed absence, cross-branch member,
and one-way retirement mutant. They do not need to restate the new reason as a
second authority source. ISS-022 required the producer-side branch census and
is amended explicitly. The three changed documents therefore agree without
creating an alternate issue-local contract.

## Round-78 removal audit

### `OP-R78-001` — remains removed

The use intent contains only independent bootstrap preimages. Its nested
proposed-genesis input has authority path, bootstrap grant/transaction,
genesis position, `G`, and `Dsc`, but no `Dgb`, `Dh`, E0 `Dv`, or `Dbg`.
The complete construction remains:

```text
independent facts -> owner/anchor ACTIVE -> use intent
  -> Dgb -> GENESIS Dh -> E0 Dv -> Dbg
  -> proposal/Dr -> tip/Dt -> Dgp
  -> anchor CONSUMED -> owner CONSUMED -> consumption receipt -> Dgse
```

`Dsc` and the authority `Dp` are precomputable identities; `Dgb` consumes the
already-known intent; `Dh` consumes `Dgb`; the E0 value consumes `Dh`; `Dbg`
consumes that selected value; and only the proposal consumes `Dbg`. No
downstream digest enters an upstream preimage.

### `OP-R78-002` — remains removed

The anchor-consumption census has separate
`runtimePostSelectionReceiptDigest` and
`runtimePostSelectionReceiptReadbackDigest` members. Framing places `Dgp` with
the selected E0 tuple and places its persisted readback after the value,
proposal, and tip readbacks. The readback must equal the parsed persisted
receipt; receipt identity and proof of verified persistence cannot collapse.

### `OP-R78-003` — remains removed for every branch

`external-destination-absence-observation/v1` has one closed census, domain,
framed order, content-addressed path, admitted producer, same-lock create-once
and readback boundary, and exact physical/destination/state/observation/helper/
custody equalities. E0 selects `RUNTIME_AUTHORITY_ABSENT`; both retirement
branches select `DESTINATION_STATE_ROOT_ABSENT`. `OP-R79-001` closes the only
branch that remained undecided in round 79.

### `OP-R78-004` — remains removed

The successor-review candidate has exact domain
`destination-owner-successor-review-candidate/v1` and frames destination,
selected prior RETIRED tip/value/receipt, teardown archive, canonical prior
installation, and canonical successor authority. It excludes the review
record, `Dsrc`, successor anchor/owner graph, `Dsrp`, and readbacks. The
external receipt authenticates that recomputed candidate, and distinct author
and reviewer identities plus downstream equalities prevent self-review or a
coordinated candidate substitution.

### `OP-R78-005` — remains removed

Consumed and unused retirement preserve the same one-way crash order:

```text
Dabs -> prior-anchor lifecycle archive -> teardown receipt
  -> anchor RETIRED value/proposal/tip
  -> destination-owner teardown archive
  -> owner RETIRED value/proposal/tip
```

The lifecycle archive contains the prior selected anchor triple/lifecycle and
`Dabs`, but no teardown receipt or future lifecycle value. The teardown receipt
then binds that already-known archive. Both are created and read back before
the anchor RETIRED proposal. The owner archive and owner RETIRED selection are
downstream. Every crash prefix either resumes the same exact artifact/CAS or
retains the selected predecessor; no digest points backward from an archive or
receipt to a future value, proposal, or tip.

### `OP-R78-006` — remains removed

The physical leaf algorithm is portable and singular: canonical unpadded
base64url decodes to 1--3072 bytes; the bytes must be shortest-form UTF-8
without BOM, surrogates, or noncharacters; Unicode 15.1.0 normalization and
unconditional default-special-casing lowercase data are pinned; lowercase is
applied only for the two case-insensitive profiles and precedes normalization;
and exact UTF-8 re-encoding must reproduce the decoded bytes. Darwin is
lowercase NFD, Windows lowercase NFC, and Linux case-sensitive NFC.

The one-component, separator, dot, prefix, trailing-dot/space, control,
forbidden-character, alternate-stream, reserved-device, `CLOCK$`, numbered
`COM`/`LPT`, and superscript alias rules are exact. The helper observes this
algorithm but cannot select another Unicode version or profile, and rotating
helper/profile/custody facts cannot move stable `Dphys/Ddest`.

## Exact schema, path, digest, and trust audit

The external ledger's literal member censuses remain closed and in ascending
canonical order. Scalar grammars, nested-record schemas, all-null/all-non-null
groups, lifecycle ordinals, UUIDv7 identities, timestamps, safe decimals,
closed enums, raw-digest versus canonical-byte framing, persistence status,
and exclusions remain exact. Owner position, owner/anchor mutation,
consumption/teardown receipt, successor review/post, anchor archive, E0
transaction, and history ordinal paths are all derived from named inputs rather
than caller-selected keys or directory enumeration.

The full bootstrap and history composition remains acyclic:

```text
reviewed facts -> Dop -> Dsc/G/Dp -> owner/anchor ACTIVE -> intent -> Dgb
  -> GENESIS Dh -> E0 value/Dv -> Dbg -> proposal/Dr -> tip/Dt -> Dgp
  -> anchor/owner CONSUMED -> consumption receipt -> Dgse/context
```

`Dgse` is downstream and non-persisted. It cannot enter `Dgb`, `Dh`, `Dsc`,
the E0 value, `Dbg`, proposal, or tip. The complete authority-history walk
recomputes `Dop`, `Dsc`, `Dgb`, branch `Dh`, selected head, and downstream
genesis selection; canonical ordinal paths, exact predecessor digests,
head-plus-one armed equality, head-plus-two absence, and FULL_REQUIRED
retention reject fork, gap, reorder, truncation, or substituted head. No new
cycle is introduced by the consumed-retirement repair.

`Dgp` and its persisted readback remain distinct. Repeated owner/anchor/E0
digests equal their selected and parsed upstream records rather than merely
co-hashing copied values. Structural validation still grants no mutation
authority: external independent review, the ISS-022-admitted helper/profile/
custody facts, kernel destination→anchor→runtime lock order, selected runtime
authority, ISS-004's private revocable handle, and stable promotion remain
separate trust boundaries. A worker or candidate cannot author its own review,
absence premise, capability, or promotion.

Current `packages/contracts/src/external.ts` and
`test/contracts/external.test.ts` remain the deliberately partial pre-ledger
implementation: raw-`Dphys` `Ddest` derivation and constructed paths only. The
exact delta changes three planning/architecture files, 14 insertions and 9
deletions, and changes no parser, canonicalizer, digest implementation, runtime
state, or test. Green focused evidence is regression evidence for existing
surfaces, not a claim that the planned external ledger is already implemented.

## New definite blocker

### `OP-R80-001` — the default verification ceiling is below observed valid execution time

The required default full suite and separate bootstrap verifier are not green.
In the first approved full-suite run, the inventory relationship test at
`test/reference-inventory/inventory.test.ts:547` exceeded its local 120-second
timeout and produced no assertion failure. Its exact isolated rerun passed all
59 inventory tests in 96.18 seconds. The entire 22-file, 267-test suite then
passed unchanged except for `--testTimeout=300000` in 140.53 seconds.

The unchanged bootstrap verifier was retried after the full-suite pass. It
repeatedly completed manifest/registry, formatting, and typecheck checks before
the same default inventory timeout. One contended run also put the planned-
command census just over its separate 240-second ceiling. A final unchanged
bootstrap run in a parent-confirmed quiet window again failed only the
inventory timeout: 21 files and 266 tests passed; the inventory test took
160.323 seconds against 120 seconds. Thus the exact default release gate cannot
report green even though the assertions complete successfully with a bounded
longer ceiling.

This is a definite readiness blocker because the repository requires a green
unchanged bootstrap gate; a review cannot reinterpret timeout as success. It
does not reopen `OP-R79-001` or any external-ledger semantic finding. The
smallest repair changes only the inventory test's existing local timeout from
`120_000` to `300_000`, matching the already successful bounded rerun. It adds
no assertion, production branch, schema, path, receipt, pointer, lock,
capability, or authority route.

## Bounded proportionality and trust analysis

| Mechanism | Concrete in-scope threat | Deletion test | Smallest credible alternative and result |
| --- | --- | --- | --- |
| Closed same-lock `Dabs` branch | A candidate retires a consumed installation while runtime-only or opaque absence is mistaken for complete terminal cleanup. | Remove the consumed reason/equality and the round-79 implementer choice returns. | Reuse `DESTINATION_STATE_ROOT_ABSENT` and the existing observation/receipt/lock graph; retained with no new record. |
| Upstream-only intent and downstream `Dgse` | Bootstrap requires a digest fixed point or lets downstream consumption authenticate itself upstream. | Insert `Dgb`, `Dh`, `Dv`, `Dbg`, or `Dgse` into an upstream preimage and the graph cycles. | Keep the existing one-way digest order and equality checks; retained with no selector or coordinator. |
| Distinct `Dgp` and readback | Receipt existence is mistaken for verified persisted bytes. | Collapse the members and a missing or substituted persisted receipt loses its named equality. | Keep the existing two digests; retained with no new receipt family. |
| Exact successor-review candidate | Reviewer and installer authenticate different projections or the candidate reviews itself. | Remove the candidate formula or identity inequality and coordinated substitution is again possible. | Retain the non-persisted framed candidate over existing facts; no candidate pointer. |
| One-way retirement artifacts | A receipt/archive fixed point or future-selected value makes crash recovery unconstructible. | Reverse one edge and an upstream artifact again depends on a future digest. | Retain `Dabs→archive→receipt→anchor→owner`; no coordinator or second archive. |
| Pinned portable leaf algorithm | Helper/profile rotation aliases or moves one physical destination. | Unpin decoding, Unicode data, profile, or reserved-name rules and conforming helpers may disagree. | Retain the fixed algorithm and cross-OS vectors; no alias inventory or sequencer. |
| 300-second local inventory timeout | A valid exhaustive mutant relationship test cannot finish within the release gate's current ceiling on supported Windows execution. | Keep 120 seconds and the unchanged quiet-window bootstrap remains red despite passing assertions under a bounded longer run. | Raise only this test's local ceiling; do not weaken assertions, global timeouts, or production behavior. |

The first six mechanisms protect current public bytes and the one actual
multi-writer destination boundary. The seventh repairs evidence execution only.
Nothing in this review supports a third absence reason, new receipt, proof
tree, inventory, global sequencer, migration, verdict route, or candidate-owned
authority.

## Round 70 blocks 2 and 3 remain unauthorized

This review authorizes no implementation of either remaining round-70 block:

2. active release, cleanup gate, recovery fence, and cleanup archive records;
3. recovery authorization, reservation, descriptor, launch, attachment, and
   attempt-log records.

Their registry names, lifecycle prose, current partial code, or block-1 ledger
do not provide their missing literal schema ledgers. They require their own
decision amendments and later fresh independent exact-head reviews.

## Verification evidence

- Initial branch/status/full-SHA checks — PASS; the worktree was clean,
  `iss-002-versioned-contracts` was exact, and the reviewed full SHA was
  `8b94edde6217361406ca18f93628b9d78182925a`.
- Exact
  `d53494a82da59551e346bcb4ec75b8103289a56d..8b94edde6217361406ca18f93628b9d78182925a`
  diff — three planning/architecture paths only, 14 insertions and 9 deletions;
  `git diff --check` passed.
- `pnpm run planning:check` — the Windows package-manager wrapper produced no
  output for 120 seconds and was boundedly terminated. Direct
  `node scripts/planning/check.mjs` — PASS (`planning contracts verified`).
- `pnpm run planning:board-check` — the wrapper produced no output for 120
  seconds and was boundedly terminated. Direct sandboxed execution reached the
  known `gh` spawn `EPERM`; approved read-only rerun — PASS (`board contracts
  verified`) against only this repository and its registered delivery project.
- `pnpm run format:check` — the wrapper produced no output for 120 seconds and
  was boundedly terminated. Direct sandboxed execution reached formatter spawn
  `EPERM`; approved read-only rerun — PASS (all matched files use Prettier
  style).
- Direct TypeScript gate — PASS:
  `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`.
- Direct ISS-002 focused contracts slot — sandboxed Vite/esbuild spawn reached
  `EPERM`; approved read-only rerun — PASS (12 files, 70 tests).
- Direct ISS-002 compatibility slot — approved read-only rerun after the known
  spawn failure — PASS (1 file, 1 test).
- Default full `node node_modules/vitest/vitest.mjs run` — BLOCKED only by the
  120-second inventory-test timeout (21 files, 266 tests passed; no assertion
  failure). Exact isolated inventory rerun with a bounded 300-second ceiling —
  PASS (1 file, 59 tests, 96.18 seconds). Full rerun with the same bounded
  ceiling — PASS (22 files, 267 tests, 140.53 seconds).
- Separate unchanged `node scripts/verify/bootstrap.mjs` — BLOCKED after
  manifests/registries, formatting, and typecheck by the same inventory-test
  120-second ceiling. A final run in a confirmed quiet window again reached
  21 files/266 tests before that sole timeout; the test took 160.323 seconds.
  Because the verifier stops at the red test command, it does not reach its
  later private-composition, planning, and final source-status checks. A direct
  sandboxed private-composition probe reached expected esbuild spawn `EPERM`
  and was not treated as a substitute for the incomplete bootstrap verifier.
- Final source status before writing this report remained clean. This review
  adds only this round-80 pressure record and makes no implementation, test, or
  commit change.

## Final status

`BLOCK_REPLAN` for exact clean planning head
`8b94edde6217361406ca18f93628b9d78182925a`. `OP-R79-001` is semantically
removed, `OP-R78-001` through `OP-R78-006` remain removed, no new external-
ledger blocker exists, and round 70 block 1 is contractually implementation-
ready. Authorization remains withheld only because the unchanged default full
suite and bootstrap verifier cannot clear the inventory test's current
120-second ceiling. Raise only that local timeout, rerun all gates unchanged,
and return the new exact clean head for fresh independent review. Round 70
blocks 2 and 3 remain unauthorized.
